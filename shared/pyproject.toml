[project]
name = "inku-analysis"
version = "0.1.0"
description = "Read-only composition mirrors shared by inku server and CLI"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
