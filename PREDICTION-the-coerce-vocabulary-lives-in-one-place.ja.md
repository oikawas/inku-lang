# 予測: coerce の語彙は 1 か所に在る — 着手前に凍らせる数字

- **エージェント**: Opus 5（`claude-opus-5[1m]`）／実装セッション
- **契約**: `no-git-sync/fable5/claude_code/tasks/stock/the-coerce-vocabulary-lives-in-one-place.md`
- **起点**: `e188850b`（main・clean）／枝 `feat/the-coerce-vocabulary-lives-in-one-place`
- **測った日**: 2026-08-17。**この文書はコードを 1 行も書く前に凍らせたものである**

---

## 1. 満点（着手前・実測）

| 面 | 実測 | どこで |
|---|---|---|
| server の全走 | **3454 passed / 31 skipped**（8 warnings・86.45s・**exit 0**） | `testbox.sh --sync --server`・`container:ddl-server`・run tree `HEAD 37de42a` |

**⚠ コンテナの数字であって Mac の数字ではない**（F-1 はコンテナで走り、Mac では skip する）。
契約 §0-A 10 が部分木ハッシュから引いた見込み（3454 / 31）と一致した。

**回していない面**: web・cli・Android（本契約は 1 ファイルも触らない）。凍結コーパスは枝の先端で回す。

---

## 2. 段 0 の測定（着手前・実測）

### 2-A. 戻り値は真偽か、一致した語そのものか

**判定の 56 か所すべてが真偽を返す。「一致した語そのもの」を返す箇所は 0 か所である。**

| 通る道 | 箇所 | 戻り値 |
|---|---|---|
| `any(marker in X for marker in (...))` | 24 | 真偽 |
| `_any_marker_in_text((...), text, lower)` | 12 | 真偽（`_marker_in_text` の `any()`） |
| 素の `"x" in lower` / `"x" not in lower` | 20 | 真偽 |

**➡ T-284 は skip。P-6（語順を逆にする）は空振りする予測。**

**⚠ ただし順序が効く箇所は既存の宣言側に在る** —— `MATERIAL_WEIGHT_HINTS`・`COLOR_MARKERS`・
`SHAPE_INTENT_MARKERS`・`MOTIF_INTENT_MARKERS`・`SEMANTIC_VISUAL_EVENT_HINTS` は
`for markers, value in ...` で回して**最初に当たった組の値を返す**。本契約はこの 5 系統の中身を動かさない（段 4）。

### 2-B. 移設で壊してはならない機構

**`_marker_in_text` は ASCII の語に語境界の正規表現を使い、素の `in` は生の部分一致である。**

```python
# _marker_in_text: "slow" は "slowly" に当たらない
re.search(rf"(?<![a-z]){re.escape(marker_lower)}(?![a-z])", lower)
# 素の in: "slow" は "slowly" に当たる
"slow" in lower
```

**➡ 照合の式は 1 文字も変えない。リテラルの tuple だけをモジュール先頭の定数へ置き換える。**
**寄せ集めて `_any_marker_in_text` に揃えると、`slow` / `light` / `sense` などの短い英単語で判定が変わる。**

---

## 3. 移設の確定件数（着手前・実測。契約 §0-A の数字と違う）

| | 契約 | 実測（`e188850b`） | 差の理由 |
|---|---|---|---|
| 判定の箇所（記述を照合するもの） | — | **56** | — |
| **移す箇所**（未宣言の語を含む） | 39 | **44** | 下の 1〜3 |
| 据え置く箇所（語が全部宣言済み） | — | 12 | 選んだ範囲の外（作者裁定 2026-08-17） |
| **移す未宣言語（異なり）** | 110 | **119** | 下の 1〜3 |
| **T-283 の期待値** | 684 | **693** | 574 + 119 |
| `note` / `color_hint` を照合する箇所（触らない） | 3 | **7** | 下の 1 |

1. **契約が語彙に数えている `compose.py:842`（`_has_focal_event_hint`）は `ins.note` を照合している。**
   9 語（`visual event`・`vanishing trace` …）は層が自分で書いた note なので移さない（**−9 語**）。
   同じ理由で `compose.py:830`（`machine_note`）・`compose.py:743` `:752`（`descriptive_hint`）・
   `compose.py:1186`（`green_context`＝層が返した文）・`normalize.py:451`（`hint.lower()`）・
   **`normalize.py:184` の `sensory_markers`（`color_hint` を照合）** も据え置く。
2. **契約の走査道具は `not in` を見ていない。** `compose.py:737` の `"crescent" not in ddl.lower()` は
   記述を判ずる未宣言語である（**+1 語**）。
3. **道具は「名前に束ねた tuple」を見ていない**（引数が `Name` だと素通りする）。記述を照合するものが 6 本あり、
   5 本に未宣言語がある: `compose.py:1623 markers`（60 語・未宣言 25）・`:1703 mark_markers`（未宣言 8）・
   `:1702 size_markers`（未宣言 4）・`:1077 POLYCHROME_MARKERS`（未宣言 2）・`:256 blur_markers`（未宣言 1）。
   **⚠ 契約 §0-A 5 の「`blur_markers` は全語が宣言側にも在る」は誤りで、`にじ` が未宣言である**
   （`scene_markers` のほうは実測どおり全語が宣言済み）。

**移せない語彙が 3 か所ある** —— 正規表現に埋まっているもの（`compose.py:230` の
`背景を…(塗|ぬ|埋|し)|(fill|paint) background`・`:505` の `領域|region`・`:1709` の `半径|radius`）。
**marker ではなくパターンなので `COERCE_MARKERS` の形に移せない。対象外にして報告に書く。**

---

## 4. 立てる系統の見込み

- **44 か所 → 43 系統**（`compose.py:1601`(日) と `:1602`(英) は 1 つの判定が言語で 2 行に割れているので 1 系統にする）
- **⚠ 片方の言語にしか語が無い系統が出る**（`compose.py:737` の `crescent` は英語だけ）。
  **揃えるために語を足さない**（契約 §0-B 2）。**T-281 はその系統を名指しで例外に挙げる形にする。**
- **同じ語を持つ別系統が出る**（`右上` は `:1575` と `:1845` の 2 か所にあり、別の判定である）。
  **契約 §0-B 1 のとおり束ねない。異なり語数は増えないので T-283 の 693 は動かない。**

---

## 5. 摂動の予測（**赤くなる本数**）

**⚠ 予測は「どの番号が」ではなく「何本が」で凍らせる。**

| 番号 | 何を壊すか | 予測 | 内訳の見込み |
|---|---|---|---|
| **P-1** | 移設した系統から語を 1 つ落とす | **2 本** | T-282・T-283 |
| **P-2** | 移設した系統に語を 1 つ足す | **1 本** | T-283 |
| **P-3** | `compose.py` へ未宣言の照合リテラルを 1 つ書き戻す | **1 本** | T-280 |
| **P-4** | `_coerce_marker_values` の呼び出しを 1 つ関数の中へ移す | **1 本** | T-285 |
| **P-5** | `ja.py` にだけ系統を足し、`en.py` に足さない | **1 本** | T-281 |
| **P-6** | 移設した系統の語順を逆にする | **0 本** | 段 0 で該当 0 か所。**空振りを実測として報告する** |
| **P-7** | `note` を照合する箇所の文字列を `COERCE_MARKERS` に載せる | **1 本** | T-280 の逆向き |

**合計 7 本 → 予測 7 本ぶんの赤の総数は 7 本。**

**⚠ P-3 は契約の例（`("上端",)` を置く）のままでは空振りする** —— `上端` は移設後に宣言側へ入るので、
**「未宣言のリテラルが無い」という主張に当たらない**。**未宣言の語で当てる**。
**契約の例のままの版も 1 度当てて、空振りすることを実測として報告する**（0 本の予測）。

**⚠ P-1 / P-2 は挙動を変えうる。** 落とす語・足す語が `test_coerce_golden.py`（43 件・32 分岐に証人）の
当たる語だと **+1 本**（golden が赤くなる）。**当たらない語を選ぶ。選んだ語を報告に書く。**

**⚠ メタゲートの道連れは無い見込み** —— `test_coerce_split.py` は分岐の順序を凍らせているが、
**本契約は分岐を 1 つも増やさず順序も変えない**。

---

## 6. 満点の予測（実装後）

| | 予測 | 理由 |
|---|---|---|
| passed | **3454 → 3459**（+5） | 新しい検査 T-280・T-281・T-282・T-283・T-285 |
| skipped | **31 → 32**（+1） | T-284（段 0 で該当 0 か所のため skip・理由を文字列に書く） |

**⚠ 外したらその向きも報告に書く。**
