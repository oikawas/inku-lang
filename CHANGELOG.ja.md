# inku 変更履歴

**日本語の詳細履歴** — 現行仕様は [SPEC.ja.md](SPEC.ja.md)、短い導入は [PROJECT_CONTEXT.ja.md](PROJECT_CONTEXT.ja.md) を参照する。

この文書は時系列の実装・設計記録である。仕様との不一致がある場合は、現行契約を記す `SPEC.ja.md` を優先する。

**本書が持つのは v2.5.0（2026-07-25、render engine 12）以降**の 30 版である。それより前は書庫にある。

| 書庫 | 範囲 | 版数 | 中身 |
|---|---|---|---|
| [v0.1 〜 v1.71](docs/history/changelog-v0.1-v1.71.ja.md) | 2026-04-02 〜 2026-05 | 73 | 着想から Stage 1.5 の整備まで。**「起源」もここ** |
| [v1.72 〜 v2.4 系](docs/history/changelog-v1.72-v2.4.ja.md) | 2026-07 | 47 | 推敲 UI の再構成から参照コーパスの凍結まで |

**描画の版だけを追うなら [docs/spec/render-engine-history.ja.md](docs/spec/render-engine-history.ja.md)。** render engine 1〜15 を新しい順に並べ、版ごとに動いた件数と不変の件数を持つ。

---

### v2.5.0 — 演奏の脱・規則化と「暴れる」（render engine 12）（Build 706、2026-07-25）

**engine 11 までの演奏は、揺らいでいるように見えて周期的だった。** 幅のエンベロープは `max(0, sin(pi t))` の固定した山で、**どのストロークも中点でいちばん太く、左右対称に細っていた**。補正イベントは `sin((i % 5) * pi / 2)` で **周期 5 の反復**を打っていた。閉じた輪郭は継ぎ目がやせて中央が太る定型を持ち、材質アウトラインは等間隔の破線と等間隔の粒で描かれていた。engine 12 はこの 4 つをすべて seed 由来の低周波雑音へ置き換え、さらに**中心線そのものに長さ基準のジェスチャ**を足す。

- **エンベロープの脱・規則化:** `_edge_window(t) * _swell(t, seed)` へ置換した。`_edge_window` は端の 16% だけレイズドコサインで落ちる窓で、**中央に山を作らない**（端点が固定されている事実だけを表す）。`_swell` は seed ごとの低周波雑音で、**「どこがいちばん太いか」がストロークごとに動く**。閉じた輪郭には端点が無いので端の窓を掛けず、`_swell` だけを使う（`CLOSED_ENVELOPE_FLOOR` は削除。継ぎ目の偽のやせが消えた）。
- **補正イベントを長さ基準へ:** 標本番号の剰余 `i % 5` をやめ、seed ハッシュのキックにした。**標本数が変わっても質感が変わらない**。
- **中心線ジェスチャ:** `ToolGrammar` に 10 番目のフィールド `gesture` を足した。ストローク長に比例した振幅で中心線自体を低周波に振り、**曲がり・丸まり・自己の重なり**を生む。ペン幅で正規化される `energy_lateral` とは別の量である。端点は窓で固定され、決定性は seed が担保する。
- **材質アウトライン層:** ジェスチャ後の中心線へ追随させ（`<polyline class="material-outline">`）、破線は可変 mark/gap、オフセットは蛇行、粒は非一様な `t` にした。`rotring` の線は素通りさせる（機械の極）。
- **「暴れる」（wild）トグル:** 演奏の上限を外すスイッチを**作品全体に一つ**置いた（`WILD_GAIN = 3.5`）。**外すのは振幅の上限と自己交差の禁止だけで、端点の固定と決定性は ON でも保つ。** 生成時のパラメータとして `render_seed` の隣に記録し（`history.render_wild`、NULL = 記録前の作品で OFF と区別する）、再現に使い、**エディション ID `rh3` の材料に含める**。UI は InputPanel のトグル、日英の i18n 込み。変奏（Stage 1.5）とは層が違い、これは Renderer 層のノブである。
- **`rh3` の材料変更を版で吸収した:** `render_wild` を payload に足したので材料は変わったが、**同じ payload の中に `render_engine_version` が入っている**ため、旧材料の値は必ず `"11"` 以下を、新材料の値は必ず `"12"` 以上を含む。新旧が衝突しないので形式名は `rh3` に据え置いた。この論法は **engine 版を同時に上げたから成り立つ**ので、材料の追加だけを単独で行ってはならない旨を SPEC に明記した。
- **参照コーパス `render-engine-12/` を凍結（199 / 220 が変化）:** **21 件が動かず、その内訳がそのまま engine 12 の説明になっている**。`rotring` の 12 件はバイト一致した（揺れ項がすべて 0 で、脱・規則化が届く先を持たない＝**道具語彙の機械の極は同じ位置にある**）。`cloudform` の 9 件もバイト一致したが理由は別で、雲形は Catmull-Rom パスとして書かれ `stroke_engine` を通らず材質アウトライン層も持たない。**engine 12 が作った穴ではなく、engine 12 が露わにした穴である**（輪郭自体は道具ごとに違い、10 道具の digest はすべて異なる）。
- **「動いた分だけ保存する」規律を生成器に実装した:** README と SPEC §15.7 は以前からそう定めていたが、生成器は新規ディレクトリのとき無条件に全 220 件を書いていた。engine 11 は全件が動いたので**区別がつかなかった**。`gen_render_reference.py` に前版 manifest との digest 比較を入れ、`changed_from_previous` を実測で決め、**その ID の SVG だけを書く**ようにした。遡って実物を解決するテスト（`_resolve_svg`）と、**遡りが空振りしていないことを確かめるテスト**を追加した。
- **版を上げる条件に「演奏できる語彙が増えたとき」を足した（作者裁定）:** 新しい道具を足しても既存コーパスはその語を使わないので**出力が動かず CI は落ちない**。結果が変わるときだけを条件にすると、版を上げずに語彙を足せてしまい、「その語を演奏できる engine 12」と「できない engine 12」が併存する。**同じ版なら同じ結果、という版数の意味が入力の集合のほうで崩れる**ため、条件を拡張した（SPEC §15.6）。
- **積み残さなかったもの（明示）:** Android への追随は行っていない（engine `"11"` のまま）。てざわりへの「コンピュータ」追加は着手していない（設計の裁定のみ済み）。`cloudform` をストローク合成へ載せる修正は行っていない（事実として記録しただけ）。過去エンジンの保持・選択は実装していない（§15.8 のまま）。保存済み SVG は差し替えていない。既存履歴の `render_wild` は backfill していない（NULL のまま＝記録前と OFF を区別する）。
- **SPEC:** §13.4 に「暴れる」の節、§15.6 に版を上げる条件の拡張と `rh3` の材料の注記、§15.7 に engine 12 の実測（199/21 の内訳）を追記した。
- **検証:** pytest **1066 passed / 30 skipped**（+7 = engine 12 で再ベースラインした golden 6 件と、遡り解決の新テスト 1 件）、cli 68 passed、ruff clean、`npm run check` **0 errors / 2 warnings**（217 files）。再ベースラインした 6 件は `_swell` を 1e-6 だけ動かすと全件落ちることを確認した（判別力の実測）。コーパス生成器は 2 回連続実行で tracked 差分ゼロ。

---

### Android Phase 3d — 組み込み Nature プラグイン展開の追随（android Build 148083、2026-07-25）

**Stage 1.5 展開層の移植が Phase 3d で完了した。** Android の展開層は web/server と同じ入力に対して同じ文字列を返すようになり、**残る遅れは描画層（engine 11 のまま）だけになった**。

- **移植内容:** `NATURE_PLUGIN_RE`（`Nature.(風|うねり|無風|wind|undulation|stillness|calm)`）と `naturePluginTerms` によるタグ抽出、`dropNaturePluginSentences` によるプラグイン文の除去、`applyNaturePluginMacros` によるマクロ文の決定的挿入を `WebDdlExpander.kt` へ写した。**呼び出し順序は server 正本と同じ**（`_sanitize_placement_words` → `_avoid_gray_background` → `_apply_nature_plugin_macros`）。
- **語の対応:** 「風」/`wind` → 横の帯 + ゆっくり揺れる、「うねり」/`undulation` → 波打つ軌跡 + 大きくゆっくり、「無風」/`stillness`/`calm` → 揺らぎなし + 静止。**`stillness` があるときは他の語より優先し、風とうねりのマクロを出さない**（server と同じ分岐）。
- **検証:** `testDebugUnitTest --rerun-tasks` を XML 自力集計で **64 件 / failures 0 / errors 0 / skipped 0**（ベースライン 62 + 新規 2）。参照コーパス `ddl_expand.json` の 3 ケース（`A-plugin-enabled` / `A-plugin-disabled` / `B-plugin-instructions-present`）が `assertEquals` で完全一致し、**この 3 件の期待値は現行 server 実装で再計算しても一致する**（受け入れ時に照合）。恒真回避テスト（`enablePlugins=false` で出力が変わること）も併せて通る。
- **判別力の実測（受け入れ側）:** うねりマクロの文言を 1 語だけ変える摂動を入れると `WebDdlExpanderPhase3dTest` が `ComparisonFailure` で落ちることを確認した。**通っていること自体ではなく、落ちることを確認している。**
- **英語版 `ANDROID_SPEC.md` の退行を受け入れ時に修正した:** 実装セッションが英語版を**日本語版の内容で丸ごと上書き**しており（en と ja の内容 SHA が一致・英語 1848 行が消失）、マージ後に英語本文を復元したうえで Phase 3d 節を英語で追記した。両版の冒頭「追随状況」も現状（master は v2.5.0 / engine 12、Android は engine 11、展開層は 3d まで）へ改めた。
- **積み残さなかったもの（明示）:** engine 12 への描画層追随は行っていない（`render_engine_version` は `"11"` のまま）。葉プラグイン文書 `nature-leaves.inku-plugin.md` の読み込みと `saijiki.py` 相当は移植していない（作者裁定によりスコープ外・3e 以降）。`android/VERSION` は `2.0.0-android.1` のまま。web/server/cli/shared のコードは変更していない。
- **採番:** Android のみの変更のため `APP_VERSION` と `web/BUILD_NUMBER` は動かしていない（`android/BUILD_NUMBER` のみ 148082 → 148083、Gradle 自動採番）。
- **検証（master 側の退行確認）:** pytest 1066 passed / 30 skipped、cli 68 passed、ruff clean、`npm run check` 0 errors / 2 warnings（217 files）。

---

### v2.6.0 — てざわりに「コンピュータ」を足す（render engine 13）（Build 707、2026-07-25）

**11 番目の道具を足した。核は「手が震えない」ではなく「誤差なく反復する」。** 手は同じ値を二度出せない。計算機は同じ値しか出せない。**周期＝線に沿った方向の反復／格子＝空間方向の反復**で、同じ性質の 2 軸である。`rotring` との区別はここで立つ — **rotring は反復すべき揺れを持たない**（道具文法が全項 0）が、**コンピュータは揺れを持ち、それを正確に繰り返す**。engine 11 の対称エンベロープへの後戻りではない（engine 11 のそれは**選べない既定**、engine 13 のこれは**選べる語彙**。SPEC §15.8 と衝突しない）。

- **演奏（`stroke_engine.py`）:** `ToolGrammar` に `periodic` / `quantize` / `width_steps` を足した（既存 10 道具はすべて 0 で無改変）。`periodic` のとき雑音源を整数周期の正弦へ差し替え（energy は 5 周期 + 10 周期、gesture は 2 周期。**いずれも seed を引数に取らない**）、中心線の座標を `ストローク長 × 0.018` の格子へ丸め、幅を 4 段に落とす。**進行方向のジェスチャは 0 にする**（機械は前後に迷わない。残すと尖点が出て「破損」に見えた）。`wild` は `periodic` の道具に効かせない。
- **材質は「標本化の残り」（`raster-bleed`）:** 手の道具の材質層は**道具が線の脇に落とすもの**（黒鉛の粉、筆の毛）だが、計算機にはそれがない。**あるのは格子へ丸めるときに捨てた差である。** 標本ごとに丸める前後の差（残差）を取り、**残差が 0 でない標本にだけ**格子 1 セルの正方形をストロークの下に敷く。セルは**格子に丸めた座標**に置き、濃さは**残差に比例**する（セル半分で上限 0.45）。**幾何が「誤差なく反復する」／材質が「誤差を捨てた跡」を見せる。** seed を使わないので同じ図なら同じ跡が出る。**端点は意図へ固定され多角形の角は anchor で戻される**ので、そこには残差が無くセルも出ない（直線 41 標本中 39、四角 80 中 76、弧 62 中 60）。
- **材質は第 1 版から作り直した（作者目視による差し戻し）:** 最初の実装は「まっすぐな定規の線 + 全数同一の等間隔 dash」で、旧作 `rh2:9e991c…` の「直線の雨」を道具の性質として置き直す設計だった。実装して目視すると、**材質層が意図した始点→終点に固定される一方、演奏された中心線は最大 55.4px 離れる**（振れ幅 102.8px）ため、点線が線から切り離されて**独立した罫線**に見えた。**作者裁定「この点線には絵画上の意味がない」** で捨てた。代案の「格子ハロー」（量子化した輪郭を 1〜2 セル外へ淡く敷く）も試作して退けた — 帯の幅が格子に比例するので線を密に置く構図で画面が潰れ、かつ**丸めの大小と無関係に一様に付く**ので理由が絵から読めない。
- **数値は実物を並べて決めた:** 格子の粗さ（0.012 / 0.018 / 0.022）× 濃さ（0.30 / 0.45 / 0.55）の 9 面と、セルを**格子に乗せる**か**理想位置に置く**かの 2 面を描いて作者が選んだ（**0.018 / 0.45 / 格子に乗せる**）。理想位置版は粉が散って見え、格子版は段に沿った画素の列に見える。
- **語彙とスキーマ:** 歳時記に日本語「コンピュータ」／英語 `computer`、Score の `Weight` リテラル、線幅 2.0px、web の歳時記に専用プレビューと日英解説。Stage 2 の道具表には「格子に乗る」「段に落ちる」「誤差なく反復する」を使った。
- **参照コーパス `render-engine-13/` を凍結（228 件）:** 新規は `A-computer-*` の 8 件だけで、**既存 220 件は digest が 1 件も動かない**（engine 12 とバイト一致）。**版木を足しても前の刷りは変わっていない**ことの校正刷りである。これは同時に、**結果が変わるときだけを条件にすると版を上げずに語彙を足せてしまう**ことの実例でもある（SPEC §15.6 の「演奏できる語彙が増えたとき」で版を上げた最初の例）。
- **生成器のガードが正しく発火した:** 材質を作り直して再生成したとき「凍結済みコーパスを同じ版で書き換えるな」で停止した。**engine 13 は未リリースで、凍結されていたのは差し戻した実装の出力**なので、版を上げずに `render-engine-13/` を削除して凍結し直した（前の凍結はブランチの履歴に残っている）。
- **積み残さなかったもの（明示）:** Android への追随は行っていない（engine `"11"` のまま・別契約）。閉輪郭専用の半径変調は作っていない（作者裁定。機械の円がロットリングの円とほぼ同じで正しい）。走査線・ディザ・面（`surface`）のテクスチャには手を出していない。`TEXTURE_SPECS` / `TEXTURE_FILTER_WEIGHTS` / `_SPECK_SPECS` に `computer` を足していない。既存 10 道具の演奏値は 1 つも変えていない。過去エンジンの保持・選択は実装していない。既存履歴の backfill もしていない。
- **格子について未裁定で残したこと:** 格子は**絶対座標**に効き、**目盛はストローク長に比例する**（`step = 長さ × 0.018`）。したがって置かれた位置で格子の位相が変わり、同じ長さの線を等間隔に 30 本置くと**異なる図が 9 種**できる。キャンバス基準の 1 枚の方眼へ変えるかは別の裁定とし、現状のまま据え置いた。
- **SPEC:** §15.9 を新設（道具の核・格子・材質＝標本化の残り・第 1 版を捨てた経緯）、§4 の語彙表・§15.6 の版数・§15.7 のコーパス表を更新。英語版は §12.6 として対応。
- **検証:** pytest **1091 passed / 30 skipped**（+2 = 材質の新テスト）、cli 68 passed、ruff clean、`npm run check` **0 errors / 2 warnings**（217 files）。コーパスは再生成の差分ゼロ。**摂動 3 件で判別力を実測**した — 不透明度の係数を 0.45→0.44、格子への丸めを外す、残差 0 の除外を外す、のいずれでもテストが落ちる。**丸めの摂動は最初素通りした**（開いたストロークは標本が既に格子上にあるため）ので、**継ぎ目補正で標本が格子から外れる閉輪郭の検査を足して**効くようにした。

---

### Android `2.1.0-android.1` — 描画層が render engine 12 へ追随（`raster` 以前の脱・規則化・材質層・暴れる・`rh3`）（android Build 148084〜148089、2026-07-25）

**Android の描画層が engine 11 から 12 へ追いついた。** Stage 1.5 展開層は Phase 3d で追いついていたので、**これで遅れているのは engine 13（コンピュータ）だけになった**。

- **4a 演奏の脱・規則化:** `ToolGrammar` に 10 番目のフィールド `gesture` を足し、`WILD_GAIN = 3.5` / `GESTURE_EDGE = 0.16`、`smoothNoiseSalted` / `edgeWindow` / `swell` / `gestureWave` を移植。エンベロープ・補正イベント（`correction-kick`）・中心線ジェスチャを engine 12 と同形にした。
- **4b′ 材質アウトライン層（差し戻し 1 回）:** 初回は**移植ではなく別実装**だった。`points` を突き合わせると **0/234 点一致・最大 16.4px ずれ**、`stroke-dasharray` も全別値。**既存テストが `path d`・class 文字列・要素数しか見ておらず、この層の幾何を 1 点も比較していなかった**ため「SVG 11 件 100% PASS」のまま通っていた（**契約が既存テスト名で条件を書いたことが穴だった**）。4b′ で `outlineOffsetPx` の `scale` 二重掛け、`hash01` の seed 文字列化（符号なし 64bit）、`variedDashPattern` / `scaleDash` の書式を server 正本へ揃え、**`points` と `stroke-dasharray` の完全一致比較**を条件に加えた。
- **4c 暴れる（wild）の結線と版数:** `render_wild` を `line` プリミティブの `synthesizeStroke` にだけ通し、輪郭・ハッチ・弧には渡さない（**server がそうなっているため**）。`render_engine_version` を `"12"` へ。判別は 2 件の対で行う — `15_line_brush_wild` は `02_line_brush` と**異なり**、`16_circle_pen_wild` は `01_circle_pen` と**バイト一致**する（輪郭経路へ配線した実装は前者を通して後者で落ちる）。
- **4d `rh3` と記録・UI:** エディション ID を `rh2` → `rh3` へ（`render_wild` を材料に含む 7 キーの canonical JSON）。Room を version 4 へ上げ `MIGRATION_3_4` と `renderWild: Boolean?` を追加、UI に「暴れる」トグル（日英・既定 OFF）を結線した。**`rh2` の生成経路は撤去済み。**
- **受け入れで見つけた別の欠陥を直した（git 管理セッション）:** 検査を契約が名指しした 4 件から**参照 SVG 全 16 件**へ広げると、`11_cloudform_pencil` の `stroke-dasharray` が **`1,3`**（Android）対 **`1.000000,3.000000`**（server）で食い違った。server は style / texture の両方の dash を `_scale_dash` に通すが、Android は**素のリテラルを返していた**。**正方形キャンバスでは数値が一致するので見えないが、pillar / wide では server 側だけが目盛を伸ばす。** 出どころは Phase 2b′（`c6e2c9e`）で、**本契約より前からの欠陥**。dash 双方へ `scale` を適用し、server が Phase 1 で削除した `rope` の残骸も除いた。**全 16 件を `path d` / `points` / `stroke-dasharray` で比較するテストを恒久化**した（名指しの一覧は穴を残す）。
- **検証:** `testDebugUnitTest --rerun-tasks` を XML 自力集計で **68 件 / failures 0 / errors 0 / skipped 0**（着手時 64・4b′ 到着時 67）。**判別は受け入れ側で 3 件実測** — `swell` に 1e-6 で 8 件、`outlineOffsetPx` の floor に 1e-6 で 2 件、texture dash の `scale` を外すと 1 件が落ちる。着手時点の赤 14 件はすべて緑になり、**参照コーパスは 1 バイトも改変されていない**。
- **積み残さなかったもの（明示）:** engine 13（コンピュータ）は移植していない。`server` / `web` / `cli` / `shared` は変更していない。閉輪郭・ハッチ・弧への `wild` 配線は行っていない（server に無い）。既存履歴の `renderWild` は backfill していない。

---

### v2.6.1 — 英語 UI の用語を版画・音楽・短歌の語彙へ揃える（Build 708、2026-07-25）

**英語表示だけの改修である。日本語は 1 文字も変えていない。** 直訳調だった英語 UI を、inku が拠って立つ三系統のメタファー — **楽譜と演奏**（音楽）、**版木と刷り**（版画）、**詞書と歳時記**（短歌・俳句）— の英語圏での正統な術語へ揃えた。素人向けに薄めず、生成 AI 業界語（prompt / generate）ではなく工房の語（write / paint / perform）を使う。

- **英語表示は 3 系統あった:** i18n パック `en.ts` の **641 項目**、コンポーネント内の `isJapanese ? '…' : '…'` **132 箇所（12 ファイル）**、`getLang() === 'ja'` 分岐 **15 箇所**。**i18n パックだけ直すと 147 箇所が旧語彙で残る**ので、3 系統すべてを棚卸ししてから当てた（書き換えは 199 / 29 / 8 = 236 項目）。
- **五つの推敲操作を対称にした:** `Vary Touch with Words` / `Vary Layout` / `Vary Reading` / `Vary Color Catalog` → **`Another performance`** / **`Another composition`** / **`Another reading`** / **`Another catalog`**、変奏は **`Variation`**。「何を引き直し、何が保たれるか」が名前だけで対比できる。ツールチップは 1 文目に「何が起きるか」、2 文目に「何が保たれるか」を置いた（例: `Another performance` → *Same interpretation, same composition — only the performance sways… Instant, no LLM call*）。**ボタン幅が厳しくても名詞を省略しない**（対称性が崩れるくらいなら折り返す）。
- **変奏の強度を音楽の語へ:** `Small / Medium / Large` → **`Subtle / Moderate / Sweeping`**。音楽の変奏に Large は使わない。**同時に配置推敲のコスト表示 `Moderate (Stage 2 LLM and API)` を `Medium` へ直した** — 一語二義を作らないためで、これは「一語一義」を守るために辞書の外へ手を伸ばした唯一の箇所である。
- **一語一義を敷いた:** `interpretation`（Stage 1 の解釈）／`reading`（言葉の読み直し）／`performance`（演奏）／`variation`（変奏）／`sway`（揺らぎ）／`color catalog`（色カタログ）。**`palette` は 0 件**（色カタログは inku 独自概念なので置換しない）、**UI 文中の `rendering` も 0 件**（技術文脈のサーバー設定のみ残す）。推敲の候補は `variation` を避けて `option` にした。
- **文化語は最小限の固有名詞に:** ローマ字残しは **`Saijiki`** と **`inku`** だけ。`Okugaki` → **`Colophon`**（書誌学・ブックアートの術語）、`Kotobagaki (caption)` → **`Headnote`**（和歌研究の確立訳）、添景は既に `staffage`（風景画の点景）。ローマ字を増やすとエキゾチシズムに寄り、道具としての信頼を損なう。
- **来歴の語:** `Generation Info` → **`Provenance`**。`Artwork` / `artwork` → **`Work`**（美術の register。残存 0）。`Unleashed` → **`Wild`**（実装名 `WILD_GAIN` と一致）。主動作ボタン `Generate` → **`Paint`**（API `/api/paint` と一致）。
- **マイクロコピーを文単位で書き直した:** 段階表示は `DDL generation` / `JSON generation / SVG rendering` → **`Interpreting your words…`** / **`Writing the score, then performing…`**。版差の通知は *This artwork was rendered with engine N* → **`This work was performed by engine N. What you see now is a new impression by engine M.`**（版画の刷りの語）。プロバイダ失敗は *Stage 1 did not answer* → **`The interpreter did not answer in time, so a stock set of instructions was performed.`**
- **文体:** Sentence case を全ラベルへ（Title Case は作者名だけが残る）。三点リーダは `…` 一文字。感嘆符を落とした（`Autonomous refinement completed successfully!` → `Autonomous refinement finished.`）。
- **歳時記のカテゴリ名は 1 語も触っていない（実測による除外）:** 英語カテゴリ名は web の文字列ではなく `saijiki.py` の `name_en` で、**`prompt_block("en")` を通って英語 Stage 1 プロンプトへ流入している**（golden fixture `stage1_prefix_en.golden.txt` が固定）。**これは UI 表示ではなく英語版 DDL の語彙仕様**なので、辞書側の推奨（touches→touch、motions→gestures 等）は適用せず作者へ報告した。歳時記の語（circle, fine brush …）も同様に不変。
- **辞書と実装が食い違った 2 点は実測を採った:** ① 辞書は五操作の根拠を「日本語の『別の◯◯』と同じ設計」としていたが、**日本語正本は「◯◯を変える」の動詞形**である（作者裁定により英語だけ名詞形にし、日本語は不変）。② 契約は `settingsGenerationLabel` を「世代の意味なので触るな」に分類していたが、**日本語正本は `'生成'`（行為）** だったので `Generation` → `Painting` に直した。
- **日本語が動いていないことを機械で担保した:** `ja.ts` の md5、三項式の日本語側リテラル（132 箇所）の md5、`getLang()` 分岐の日本語側（14 箇所）の md5 — **3 つとも着手前と一致**。`LC_ALL=C sort` でロケールを固定しないと内容が動かなくても md5 が変わる（起票時に実際に踏んだ）。
- **検証:** `npm run check` **0 errors / 2 warnings**（217 files、警告は既存の a11y 2 件）。i18n の鍵は **641 のまま増減なし**、日英で完全一致。`server` / `cli` / `android` と `saijiki.ts` / `saijiki.generated.ts` の差分ゼロ。**摂動 4 件で判別力を実測** — `ja.ts` を 1 文字、三項式の日本語側を 1 文字、`palette` を 1 件混入、鍵を 1 つ削除、のいずれでも対応する検査が落ちる。
- **積み残した（明示）:** **英語ドキュメント（README.md / SPEC.md / `manual/en/`）の用語は未追随**。UI 側だけが新語彙になっているので、`artwork`（SPEC 25 / manual 26）・`palette`（SPEC 12）・`Okugaki`（SPEC 7）・`Unleashed`（SPEC 7）・`Generation Info`（SPEC 3 / manual 5）などが古い。**別作業とした。** 履歴ゼロの空状態文言は現行 UI に鍵が無いので新設していない（鍵を増やさない方針）。歳時記カテゴリ名（上記）も未実施。

---

### v2.7.0 — 一枚の方眼と、暴れるの到達（render engine 14）（Build 709、2026-07-26）

**engine 13 が残した 2 つの穴を、1 つの版としてまとめて塞いだ。** どちらも演奏結果が動くので、まとめれば参照コーパスの再凍結が 1 回で済む。

- **方眼は紙の性質であって、置かれた対象の性質ではない。** engine 13 の格子は目盛が**ストローク長に比例**していた（`step = 長さ × 0.018`）。そのため**長さの違うものは目盛が違い**（100px→1.8px / 400px→7.2px / 800px→14.4px）、**同じ長さでも置かれた位置で位相が変わり**（同一長の線を 30 箇所に置くと **30 種すべて異なる図**になる）、1 枚の絵の中に**大きさの数だけ別々の方眼**が同居していた。engine 14 は目盛を **`キャンバス短辺 × quantize`** で決める。値は `0.018` のままだが、**意味が「ストローク長に対する比」から「キャンバス短辺に対する比」へ変わった**。`stroke_engine` はキャンバスを知らないので **renderer が px へ直して `grid_step` として渡す**。**長さ相対の経路は 4 箇所とも削除**した（切り替えフラグも既定値も残さない）。正方 1000px で **18.000000px**、短辺基準なのでアスペクトで変わる（pillar は 3.600px）。**同じ絵の中のすべてのストロークが同じセルへ落ちる** — 大小 3 対象を 1 つの Score に同居させた実測で、18px 格子から外れた座標が **188/194 → 0/194** になった。方眼が対象の大きさで縮まなくなったので**連続する標本が同じセルへ丸め込まれることが起き、重なったセルは重ねて描く**（作者はこの見えを含めて 18px を選んでいる）。
- **「暴れる」が輪郭へ届くようになった。** engine 12 の暴れるは **`line` プリミティブにしか届いていなかった** — **11 道具 × 8 プリミティブ 88 組のうち 63 組が ON/OFF でバイト一致**しており、SPEC §13.4 の「作品全体に一つのトグル」という位置づけとずれていた。engine 14 は `synthesize_along`（円・楕円・三角・四角・多角形・弧・塗り・ハッチ）にも中心線ジェスチャを足して `wild` を通す。**OFF の出力は 1 バイトも変わらない**（既存 228 件で OFF に動いたのは格子由来の 7 件だけ）。**バイト一致してよいのは 25 組ちょうど**で、内訳は `cloudform` × 全 11 道具（`stroke_engine` を通らない既知の穴。本件でも直していない）、`rotring` × 7（`gesture = 0`）、`computer` × 7（`periodic` が `WILD_GAIN` を飛ばす）。
- **素朴な移植は 3 点で壊れる（試作で実測して仕様に落とした）:** ① **弧長で振幅を決めてはならない** — 閉輪郭の周長は寸法ではなく、多角形が星形になった。**`周長 / τ`＝半径相当で測る**。② **ジェスチャの平均を差し引く** — 平均が 0 でないと図形全体が伸縮する（円が一回り縮んだ）。**大きさは楽譜が決めるものであって、演奏が変えてよいものではない**。③ **角（anchor）の手前で窓を 0 へ落とす** — 意図へ戻される頂点の隣にジェスチャが乗るとトゲが出る。
- **材質が墨に追随するようになった（本件でいちばん見落としやすかった点）:** 輪郭と弧の材質アウトラインは**幾何から引かれていて演奏後の中心線を見ていなかった**。暴れるを届かせると **9 組すべてで墨だけが動き、材質が幾何の上に取り残された**。これは engine 12 が線について直したのと同じ型の不具合である（材質層が中心線に追随せず独立した罫線として背景に残る）。**ON のときだけ演奏後の中心線から作り、OFF では engine 13 のまま**にした。
- **参照コーパス `render-engine-14/` を凍結（347 件）:** `corpus_format_version` を `"1"` → `"2"`（ケースの入力に `wild` が加わったため）。`changed_from_previous` は **126 件**で、内訳は**既存 7 件**（`A-computer-*` から `cloudform` を除いたもの）と**新規 E 群 119 件**（暴れる ON の全数 88 + 塗り 15 + 面 16）、**不変 221 件**。**手の 10 道具は 1 件も動いていない。** `A-computer-cloudform` も動かない。
- **契約に赤を先出しした（起票時に git 管理セッションが一時パッチで実測）:** セル数 7 行と先頭 5 セルの座標・不透明度、既存 228 件のうち動くのは 7 件、wild が一致してよいのは 25 組、材質が離れる 9 組。**実装はこの全部を 1 桁も違わず再現し、受け入れ側で測り直しても同じだった。**
- **摂動で分かったこと（検査が 1 段少なかった）:** 「長さ相対へ戻す」を `synthesize_stroke` の内側だけに当てると**格子の検査 2 本が落ちなかった**。`_add_raster_bleed` が被覆セルを渡された目盛へ**再スナップする**ため、内部で目盛が食い違ってもセルの整列と一辺の一様性は renderer 側で保たれてしまう。`synthesize_along` と返り値まで含めて完全に戻すと 4 件落ちる。**同じ性質を 2 箇所で強制していると、片側だけの摂動は下流に吸収される。**
- **受け入れ側でも摂動を当てた:** 目盛 0.018→0.017 で 4 件、材質アウトラインを幾何へ戻す摂動を**弧側だけ / 閉輪郭側だけに分けて**当て、**どちらでも検査が落ちる**ことを確認した（強制点が 4 箇所あるので、片側だけの摂動では検査面の穴が見えない）。
- **SPEC:** §15.10 を新設（英語は §12.7）、§13.4 の暴れるに到達範囲の経緯、§15.9 の格子記述、§15.6 の版数と §15.7 のコーパス表を更新。**英語版の「Unleashed」を UI に合わせて「Wild」へ揃えた**（v2.6.1 で作った食い違いの一部解消。README と `manual/en/` はまだ残っている）。
- **検証:** pytest **1100 passed / 30 skipped**（+9 = `test_one_lattice.py` 3 / `test_wild_reach.py` 5 / コーパス 1）、cli 68 passed、ruff clean、`npm run check` 0 errors / 2 warnings（217 files）。コーパスは 2 回連続生成で差分ゼロ。**`android/` に差分が無いので Android のテストは回していない。**
- **積み残さなかったもの（明示）:** `cloudform` のストローク化、死にフィールド（`absorbency` / `contact` / `thickness`）、SPEC §17.A の `thickness` / `angle` / `rotation` / `length` dimension、過去エンジンの保持機構、既存履歴の backfill、`gen_android_reference.py` への波及はいずれも行っていない。**Android は engine 12 のままで、遅れが 2 版に広がった。**

---

### v2.7.1 — 英語用語の正本と、それを強制する lint（Build 710、2026-07-26）

**v2.6.1 は英語 UI を辞書へ揃えたが、揃った状態を保つものを残していなかった。** 文字列を 1 つ足すだけで禁止語が戻り、同じ概念に 2 つ目の英語が当たる。**規則の正本を文字列の隣に置き、それを機械で検査する番人を付けた。**

- **`web/src/lib/i18n/GLOSSARY.md`（200 行）が英語用語の正本。** 英語表示が 3 系統あること（`en.ts` 641 / 三項式 132 / `getLang()` 分岐 15）、コア用語の対応、五つの推敲操作と変奏の強度の固定値、文体規則、禁止語と**許容される例外**、触ってはいけない経路（`saijiki.py` の `surface_en` / `name_en`、`saijiki.ts`、`ja.ts`、三項式の日本語側）、新しい文字列を足すときの手順、検査が何を見ているか、まだ揃っていない食い違いまでを書いてある。出典は Fable の翻訳辞書と 2026-07-25 の作者裁定。
- **`web/scripts/i18n-lint.mjs`（221 行）が規則を機械で検査する。** `npm run lint:i18n`。**英語表示 788 文字列を走査し、許容例外 36 件を名前で通す。** `--list` で通した例外も出る。
- **文面と検査は一対**である。片方を変えたら同じ commit でもう片方も変える、と正本に書いてある。
- **「残存ゼロ」を条件にしない設計。** `generation`（世代）・`prompt`（LLM プロンプトの表示）・`created`（完了・日時）・`image`（Vision が実際に見る画像）・`render`（サーバー技術設定）は正当な用法があるので、**どこにも書いてはいけない語**と**決められた場所にだけ許される語**を分けてある。
- **判別力を受け入れ側で実測した。** `colorCatalogTitle` を `Color palette` に変えると `ERROR en.ts colorCatalogTitle: "palette" — use "color catalog" — palette is a different concept in inku` で 1 件落ち、戻すと 0 errors に戻る。
- あわせて `en.ts` の 1 項目を Sentence case へ是正した（`Instructions (Normalized DDL)` → `Instructions (normalized DDL)`）。**これは lint を通すために見つかった取りこぼしである。**
- **検証:** `npm run check` 0 errors / 2 warnings（217 files）、`npm run lint:i18n` **788 文字列 / 36 例外 / 0 errors**、pytest 1100 passed / 30 skipped、cli 68 passed、ruff clean。**`android/` に差分が無いので Android のテストは回していない。**
- **積み残し（明示）:** 英語ドキュメント（`README.md` / `manual/en/`、`SPEC.md` の残り）の用語追随は行っていない。**lint は `web/` の表示文字列だけを見ており、ドキュメントは見ていない。**

---

### 英語ドキュメントの用語追随（版数なし・2026-07-26）

**v2.6.1 が UI の英語を辞書へ揃えたとき、英語ドキュメントは旧語彙のまま残っていた。** UI だけが新しく、`README.md` / `SPEC.md` / `manual/en/` は `artwork` / `Generation Info` / `Vary Touch with Words` と書いたままだったので、そこを追随させた。**日本語正本とコードは 1 バイトも触っていない。**

- `artwork` → `work`（美術の register）、`Generation info` → `Provenance`、`Okugaki` → `Colophon`、`Kotobagaki` → `Headnote`、五つの推敲操作を確定した名前（`Another performance` / `Another composition` / `Another reading` / `Another catalog`）へ、変奏の強度を `subtle / moderate / sweeping` へ。
- **英語 UI の表記規則は、SPEC で述べ直すのをやめて `web/src/lib/i18n/GLOSSARY.md` を指すようにした。** SPEC.md には「短い英語ラベルは Title Case」と書いてあり、v2.6.1 で Sentence case へ移った実装と矛盾していた。**正本を 2 箇所に置かない。**
- **意図して直さなかったもの:** ① **CLI の実出力の引用**（`inku-cli` は今も `Artwork Lineage:` と印字するので、マニュアルは原文どおりでなければ嘘になる）、② CLI のサブコマンド名とフラグ値（`okugaki`、`refine generate`、`--kind layout`）、③ **JSON / API のフィールド名**（`palette`、`resolved_palette`、`palette:<name>`）、④ `Headnote` / `Colophon` に添えたローマ字の注記、⑤ **改訂履歴の「英語 UI の Title Case 統一」**（過去のビルドが実際にそうしたという記録であり、いまも真である）。
- **`palette` は当初「12 件が旧語」と数えていたが、分類すると 0 件だった。** 日本語正本も「色パレット」と書いており、残りは色カタログ API のフィールド名である。**辞書が禁じているのは「色カタログの意味での palette」であって、一般名詞のパレットではない。** 生の grep 件数をそのまま作業量として扱うと、正しい用法まで消すことになる。
- **機械置換が壊した英文を 5 箇所直した**（`not an work governor` / `by an work-page boundary` など、`artwork` → `work` で冠詞と複合語が崩れた箇所）。**一括置換は必ず後始末が要る。**
- **lint はここを見ていない。** `npm run lint:i18n` が走査するのは `web/` の表示文字列だけで、ドキュメントは対象外である。ドキュメント側の退行を機械で止める仕組みは無い。
- **採番していない**（ドキュメントのみの変更のため。下記の採番規則の最初の適用例）。

### 採番規則の強化（2026-07-26 作者裁定）

**「バージョン番号が速く上がりすぎている。0.0.1 単位での上昇をより多用する」。** 2026-07-21 にも同じ趣旨の裁定を受けていたが、その後も **engine の版上げを minor の理由として扱って**しまい、v2.5.0（engine 12）→ v2.6.0（engine 13）→ v2.7.0（engine 14）と 5 日で minor が 3 回上がった。**engine 版は演奏互換性のカウンタであって、ユーザー向けバージョンの粒度ではない。**

- **patch (+0.0.1) を既定とする。** 機能の追加・改修、UI・用語の改修、バグ修正、テストや参照コーパスの追加、**および render engine の版上げ**はすべて patch。
- **minor (+0.1.0) は 3 つの場合だけ** — 作者が節目と明示したとき、タグを打つ外部向けリリース、互換性が切れるとき（保存済みデータ・API・エディション ID の形式変更）。
- **ドキュメントのみの変更は採番しない。** CHANGELOG には版数なしの日付見出しで記録する（Android entry と同じ形）。
- `web/BUILD_NUMBER` は別勘定で、反映のたびに上がってよい。
- **迷ったら patch に倒す。既に公開した番号は振り直さない**（v2.5.0 / v2.6.0 / v2.7.0 はそのまま）。

---

### v2.7.2 — 誰も読まない 2 つのフィールドを退役させる（Build 711、2026-07-26）

**`contact` と `thickness` は宣言だけがあって、描画のどこからも読まれていなかった。** とくに `contact` は値を運んでいない — `touching` のときは `both_ends` 以外を禁止し、それ以外のときは書くこと自体を禁止していたので、書ける値は 1 つしかなかった。

- **`Relation.contact` と `Dimension` の `thickness` を消した。** `RelationContact` 型も消えた。SPEC §17.A の「`thickness` dimension 未対応」の行も、実装されないまま残っていた宣言だったので落とした。
- **保存済み作品は今までどおり再生できる。** pentala の 1780 件のうち `contact` を持つのは 41 件で、`extra="forbid"` があるため素朴に消すと再生時に弾かれる。各モデルが**検証の前に退役フィールドだけを落とす**ようにした。**未知のフィールドは今も拒否する。**
- **生産者側も止めた** — 日英の第二段階プロンプトとその例文、関係の補修経路、対を割るプラグイン。参照ダンプからは `contact` の列挙が消えた。
- **`absorbency` は退役させなかった。** 調査の結果、値そのものは読まれないが、**地の texture seed が Score 全体のハッシュ**（`_texture_seed`）なので、消すと地を持つ作品の粒配置が変わる。実測で **23 件中 18 件の出力が動いた**ため取りやめ、フィールドの説明にその理由を書いた。**次に engine の版を上げるときに、意図した変更として扱う。**
- **出力が変わらないことを実データで確かめた。** pentala の保存済み Score から 312 件（`contact` / `absorbency` を持つ 62 件全部 + 無作為 250 件）を旧実装と新実装で描き比べ、**変化 0 件**。関係を持つ 639 件でも **0 件**。**engine の版は上げていない。**
- **検証:** pytest **1101 passed / 30 skipped**（退役フィールドの互換テスト 1 件を追加）、cli 68 passed、ruff clean。**`web/` にも `android/` にも差分が無いので、`npm run check` と Android のテストは回していない。**

---

### Android `2.1.1-android.1` — 描画層が render engine 14 へ追随（コンピュータ・一枚の方眼・暴れるの到達・てざわり語彙の是正）（android Build 148090、2026-07-26）

**engine 13 と 14 の 2 版を 1 契約でまとめて追いついた。** これで Android は server と同じ render engine 14 を名乗る。

- **5a 機械項と格子:** `ToolGrammar` に `periodic` / `quantize` / `width_steps` を足し 11 道具へ同調。`grid_point` / `machine_energy` / `machine_swell` / `machine_gesture` を移植し、`StrokeSample` に `residual` を追加。**周期文法は `wild` を無視する**（`wild && !periodic`）。
- **5b renderer の格子と raster bleed:** 目盛は**キャンバス短辺 × `quantize`**（正方形で 18.0px）。`RASTER_BLEED_OPACITY = 0.45` の `<rect class="raster-bleed">` を敷き、直線・輪郭・弧・ハッチの全経路へ `gridStep` を渡す。
- **5c 暴れるを輪郭へ + 版数:** `wild` を輪郭・塗り・弧・ハッチへ結線（**雲形には渡さない**）。`render_engine_version` を `"12"` → **`"14"`**（`DefaultSvgRenderer` と `LocalFallbackPipeline` の両方）。
- **5d てざわり語彙の是正:** 表示語彙を server の `saijiki.py` と同じ **10 語ちょうど**へ（コンピュータ・ビュラン・ドライポイントを追加、髪と縄を除去）。**`hair` は Score の値としては保持**（保存済み作品の再生のため）。`rh3` の 4 値を engine 14 の実測値で固定。
- **受け入れで見つけて直した（git 管理セッション）:** 語彙から縄は消えていたが、**描画表には残っていた** — `ropeTwists`（呼び出し元ゼロ）、スタイル表の `"rope" -> 0.88`、線幅表の `"rope" -> 10f`。実装セッションが足した検査は**プロンプト文字列しか見ていなかった**ため通っていた。3 か所を削除し、**スタイル表・線幅表・文法表を見る検査**を足した（分岐を戻すと落ちることを確認）。
- **検証:** `testDebugUnitTest --rerun-tasks` を XML 自力集計で **71 件 / failures 0 / errors 0 / skipped 0**（着手時 68）。**判別は受け入れ側で 3 件実測** — 格子の目盛に ×1.000001 で 2 件が落ち、周期文法の `wild` 無視を外すと 4 件が落ち、`rope` の分岐を戻すと語彙テストが落ちる。
- **積み残さなかったもの（明示）:** `server` / `web` / `cli` / `shared` と参照コーパスは変更していない。Stage 1.5 の 3e 以降（葉文書プラグイン）、てざわり以外の歳時記カテゴリの語彙照合は範囲外。

---

### 凍結済み DDL コーパスの記録入力から退役フィールドを外す（版数なし・2026-07-26）

**v2.7.2 の退役は、描画出力ではなく「コーパスが記録している入力」の側に差分を残していた。** `reference-corpus` CI の `ddl-engine` ジョブが `9c70e8f` 以降 3 連続で赤になり、生成器のガード（`DDL corpus changed without an identity-field change`）が**設計どおり発火していた**。

- **動いていたのは 1 ケース `B-invalid-touching` の記録入力にある `contact` キーだけ**で、**出力 29 件は全件バイト一致**だった
- **出力が 1 バイトも動いていないので、版は起こさず記録側を直した**（作者裁定 2026-07-26）。「凍結物は書き換えない」規律の例外にあたるので、書き換えた事実をここに残す
- 版を起こす選択（`ddl-engine-2` の新設）は、**出力が同一の版**を作ることになり版の意味が薄れるため採らなかった
- **検査漏れの記録**: v2.7.2 ではスキーマ変更後に pytest と ruff を回したが、**参照コーパスの生成器そのものを走らせていなかった**。pytest はコーパスの出力を見る一方、凍結ガードは生成器を実行しないと通らない。**スキーマを触ったら生成器も回す**
- **検証:** `gen_ddl_reference.py` と `gen_render_reference.py` がともに終了コード 0、再実行後の差分は無し。pytest 1101 passed / 30 skipped

---

### v2.7.3 — CLI の英語表示を用語辞書へ揃える（Build 712、2026-07-26）

**英語表示の第 4 の系統は CLI である。** v2.6.1 と v2.7.1 が `web/` を辞書へ揃え、lint で固定したが、**`npm run lint:i18n` は `web/` の表示文字列しか見ておらず、CLI には届いていなかった**。

- **禁止語 `artwork` が残っていた 3 か所を `work` へ**（辞書 §2「作品 = **work**」）。`Artwork Lineage:` → **`Work lineage:`**（Sentence case）、`show or control artwork lineage` → `show or control the lineage of a work`、PNG フォールバックの警告文 1 行。
- **マニュアルは実出力を引用しているので、日英とも同じ commit で追随させた**（`manual/{ja,en}/cli-reference-for-ai.md`）。
- **`palette` 6 件は対象外**。`catalog.get("palette")` などサーバー API の**フィールド名**であり、表示文字列ではない。**生の grep 件数をそのまま作業量にしない。**
- **積み残し（明示・裁定待ち）**: サブコマンド名 `okugaki` は辞書では `colophon` だが、**コマンド名は識別子であり、変えると利用者の手順が壊れる**。今回は触っていない。`README.md` / `SPEC.md` / `manual/en/` の旧語彙（`artwork` ほか）も未追随のまま。
- **検証:** cli 68 passed、ruff clean、`inku-cli --help` の実出力で新しい文言を確認。server 1101 passed / 30 skipped。

---

### coerce にゴールデンの赤を敷く（版数なし・2026-07-26）

**coerce の 34 分岐は、凍結コーパスに 1 つも守られていなかった。** 分割の契約（`normalize` / `compose`）は「参照コーパス `render-engine-14` の 347 件が赤にならないこと」を受入の核に据えていたが、**そのコーパスは coerce を通らない**（`gen_render_reference.py` は `coerce_score` を import せず、docstring に "no ... coerce path supplies fixture values" と書いてある）。coerce を通る唯一のコーパス `ddl-engine-1` の B 群 14 件も、34 分岐のうち 10 しか発火させない。

- **34 分岐 × 4 スイートを全数実測した**（分岐を 1 つずつ恒等関数に置き換えて、どのスイートが赤くなるかを数えた）。凍結コーパス 376 件は **34 分岐すべてで緑**。`test_api.py` + `test_composer.py` は 1 分岐のみ。`test_coerce.py`（125 件）は 31 分岐を捕まえる一方、**`_dedupe_instructions` / `_with_presence_auxiliary_shape_repair` / `_with_total_density_budget` の 3 つは無防備**だった。
- **ゴールデン 39 ケースを追加した。** 保存作品 21 件（pentala の `history` から分岐被覆で選抜）と、実作品が届かない分岐のために組んだ合成入力 18 件。coerce 後の Score 全体・instruction 数・**分岐ごとの発火数**を固定する。発火数まで固定してあるので、**失敗したときにどの分岐が動いたかが名前で出る**。
- **34 分岐すべてが、無効化すると 1 件以上のケースを動かす**（最大 17 件・最小 1 件）。判別力ゼロの分岐は無い。
- **`_dedupe_instructions` は素朴に作ると空振りした。** 無効化しても当初の 38 件は 1 件も動かなかった。**11 番目の `_with_structural_duplicate_repair` のキーが「dump から `color_hint` を除いたもの」＝より粗く、4 番目が落とす行を必ず落とす**ためである。片方だけの摂動が下流に吸収される型。完全同一 10 行 + モチーフ要求（モチーフ修復は合計 10 行を超えると足さない）という 1 ケースを足して破った。
- **「順序を 2 つ入れ替えれば赤になる」は成り立たない。** `dedupe_instructions` と `with_ddl_coverage` を入れ替えても 3 つのゲートすべてが緑だった。**同じケースで両方が発火する組でなければ、入れ替えても出力は動かない。**
- **設計プランの数値 3 点を訂正した。** 分岐は 32 でなく **34**（instruction リストでなく値を返す `_with_background_dominance_governor` と `_presence_from_ddl` が漏れていた）。振り分けは normalize 6 / compose **28**。「発火率を測る手段が無い」も誤りで、`_record_branch_fire` は要素ごとの dict 不一致も数えるため書き換えだけの分岐も検出する。実際の欠落は `cli.py` の `_compose_response_as_paint_result` が `/api/compose` 応答から `coerce_*` を引き継いでいない 1 箇所だけだった。
- **採番しなかった。** 動くコードは 1 行も変えておらず、反映するものが無い。この検査面は、それが守るための coerce 分割と同じ版で採番する。
- **検証:** server **1143 passed / 30 skipped**（着手時 1101 / 30）、ruff clean。

---

### v2.7.4 — coerce を `normalize` と `compose` に割る（Build 713、2026-07-26）

**構図を決めている場所に名前をつけた。絵は 1 画素も変わらない。** `coerce.py`（4212 行）は、描画可能化のための機械的補修と、入力を解釈して構図を書く処理が、**34 分岐の一列に混ざっていた**。構図を決めているのは Stage 2 でも Renderer でもなくここである（本番 60 作品で instruction の 27% は DDL でなく coerce 製）にもかかわらず、その責務の境目に名前が無かった。

- **分割の判定基準は「`ddl` を引数に取るか」**とした。主観的な線引きを避け、機械検査できる基準にするため。`normalize` は Score だけを見る。`ddl` を読む処理は入力の解釈であり、すべて `compose` に置く。
- 実測の振り分けは **normalize 6 分岐 / compose 28 分岐**。共有は関数 4 つ（`_closed_shape_area` / `_cluster_count` / `_expanded_count` / `_shape_extent`）と定数 1 つ（`VISIBLE_ON_BACKGROUND`）だけで、いずれも `ddl` を読まないため `normalize.py` に置いて `compose.py` から一方向 import した。**第 3 のモジュールは要らなかった。** 最終構成は normalize 34 関数 / 7 定数、compose 123 関数 / 55 定数、`__init__` が `coerce_score` 1 つ。
- **`coerce_score` の呼び出し順を 1 行も動かしていない。** normalize 系と compose 系は交互に並んでおり（`dedupe` は 4 番目、密度予算は 29〜30 番目、`drop_invalid_relations` は 33 番目）、**まとめて呼ぼうとすると順序が変わって結果が変わる**。分割はモジュールの分割であって、パイプラインの並べ替えではない。受け入れ側で main の分岐列 39 件と分割後を機械照合し、順序も条件行 16 件も完全一致を確認した。
- 両方の責務を持つ唯一の関数 `_coerce_and_repair_instruction` は 3 つに開いた（normalize の `_coerce_instruction` と `_repair_coerced_instruction`、compose の `_with_ddl_instruction_hints`）。**元の適用順序を厳密に保っている。**
- **同一性の確認**: ゴールデン 39 ケースが**バイト差 0**。34 分岐を 1 つずつ恒等関数に差し替えたときに動くケース数も、**分割前の実測と 1 件違わず一致**した（最大 17・最小 1・ゼロ無し）。
- **CLI の診断値の引き継ぎ漏れも直した**。`_compose_response_as_paint_result` が `/api/compose` 応答から `coerce_*` を 1 つも引き継いでおらず、`--input-mode ddl` の batch で `coerce_branch_counts` が空になっていた。pentala に対する実地確認で **0 キー → 34 キー**。
- **積み残し（意図的）**: Topology は入れていない。座標リテラル 94 対は値も所在も動かしていない（分割後の所在は `compose.py`）。分岐は 1 つも削除していない。**39 ケースで 34 分岐すべてが発火したため、死んだ分岐は見つかっていない。**
- **検証:** server **1147 passed / 30 skipped**（着手時 1143 / 30。C-1〜C-4 の 4 件を追加）、ruff clean、cli **69 passed**（+1）、`npm run check` 0 errors / 2 warnings / 217 files。

---

### v2.7.5 — 明示された個数は 240 未満なら literal（Build 714、2026-07-27）

**「二百三十三本」と書いても 2 本しか描かれなかった。** 明示された個数の 24% を Stage 2 が落としており、密度予算で説明できるのは 4% だけだった。残りはプロンプトが自分自身と矛盾していたことに由来する。**矛盾を消す方向で直した。ただし受入条件は満たしていない。届かない原因が Stage 2 でなく coerce にあることが実測でついた。**

- **矛盾は 3 つあった。** ①「小さな scatter が 120 個を超えたら代表化の道具（density / cluster_count / fade / preserve_space）を使え」と「代表化は 300 個以上」が、**120〜299 帯でモデルから見て競合していた**（この帯の遵守率 20% は全帯で最低）。②「2 以上の同一図形には複数 instruction 生成を絶対禁止」が、**個数の違う群まで 1 件へ畳ませていた**。③ **作例 4 件が、要求 137 に対し `"count":96` を出して見せていた**。規則より作例が強い。
- **閾値を 240 に置いた。** `MAX_EXPANDED_PER_INSTRUCTION = 240` に合わせた値である。300 に置くと 241〜299 が「literal と規定されているのに coerce が 240 で切る」**構造的に守れない帯**になる。**「二百三十三本」は 233 本になる。**
- **禁止規則を「同一図形の同一配置の反復を N 件の instruction へ展開すること」に限定した。** 直したのは 4 経路（`composer.py` の日英 system prompt、`schema.py` の `count` と `arrangement` の description、`api.py` の空描画リトライ）。**1 箇所だけ直すと経路によって指示が食い違う。** 規則に反していた作例 2 件も同時に整合させた。
- **プロンプトが効く層では動いた。** coerce の手前（Stage 2 の出力）で測ると、最低だった **120〜239 帯が日本語 33%→55% / 英語 37%→92%**。300 以上は日本語 55%→88% / 英語 88%→100%。
- **final Score は動かなかった。** 受入条件 6 帯のうち満たしたのは 2〜11 の 100% 維持だけである。**完全に遵守した Score を coerce へ通す実験で、単群 25 文のうち通過したのは日本語 20 件・英語 11 件**（`_with_context_density_governor` が 64 / 48 / 16 へ置き換える）。**受入条件 90〜95% は、契約が禁じた coerce と compose に触れない限り到達できない値だった。**
- **日英差も同じ層にある。** 静けさ密度マーカーの発火は日本語 36/87 に対し**英語 72/87**、縦密度マーカーは 15 対 48。受入条件「帯ごとの日英差 10pt 以内」も、プロンプトだけでは満たせない。
- **受け入れで 1 行削った。** 「代表化した事実と元の要求値を診断 metadata に残す」という指示を日英とも入れていたが、**Score に `metadata` フィールドは無く、全モデルが `extra="forbid"`** である。モデルが従えば検証エラーになる指示だった。不在をテストで固定した。
- **測定の注記**: 段 5 の 174 標本のうち **13 件は Stage 2 の応答でなく決定的フォールバック**（空 instructions 後の再試行 10・ハードタイムアウト 3）。除いて集計し直しても帯の率は数 pt しか動かない。ベンチは `--input-mode ddl` による Stage 2 単離測定であり、**本番の遵守率そのものではない**（本番は Stage 1 を経て `original_text` が付き `tenkei` も効く）。
- **積み残し（意図的）**: `coerce/` に個数補正の分岐を足していない。compose 側の決定的な強制も入れていない。密度ガバナーの日英マーカー非対称も直していない。**次の契約で扱う。**
- **検証:** server **1147 passed / 30 skipped**、cli **69 passed**、ruff clean、`npm run check` 0 errors / 2 warnings / 217 files。engine 版・参照コーパス・ゴールデンは触っていない。

---

### v2.7.6 — 書かれた個数は、後からの読みより強い（Build 715、2026-07-27）

**v2.7.5 でプロンプトの矛盾を消したのに final Score が動かなかった理由が、この版で解ける。** 落としていたのは `_with_context_density_governor` で、**静けさ・膜・記憶系の文脈を読み取ると、見つけた反復を残らず間引いていた**。書かれた個数の群も含めて。

- **静けさは場面の読みであり、「二百三十三本」は読みではない。** 記述に平文で書かれた個数の群は、3 つの個数上限（縦 48 / 大形 16 / 視覚 64）を素通りする。**大きさを整える処理（象徴形・単独形・意図しない塗り）はそのまま働く** — いくつを触るものではないため。判定は「DDL に書かれた個数と一致するか、または 240 以上の要求に対する 80〜120 の身代わりか」
- **凍結 50 ケースが 31/50 → 50/50。日英とも 25/25** で内訳も同じになった（着手時は日本語 20/25 に対し**英語 11/25**）。この検査面は**LLM を 1 回も呼ばずに 2 秒で回る**（`server/scripts/measure_count_preservation.py`）
- **日英差の正体は英語マーカー `"one "` だった** — `one hundred twenty short black pencil lines` に当たり、**要求した個数を英語で書いたこと自体が、その個数を切る分岐を起こしていた**。外したが、**単独では 1 件も救わない**（5 件中 4 件は `thin` / `trace` / `pale` / `blur` にも当たる）。**帰属は「マーカー是正 0 件 / 免除 19 件」**
- **合計予算が比例縮小をやめた。** 400 を超えると各群に按分を割り当てていたので、**数えられる 12 個の四角を削って、数えられない 200 個の点の分を払っていた**。大きい群から順に代表化し、予算を下回った時点で止める。全群を代表化しても超えるときだけ、大きい群が一つの上限を分け合う（**1 まで潰れる群を作らない**）
- **増やす欠陥も直った。** 旧実装は按分が要求値を上回るときも上書きしており、**要求 120 の群を 232 へ、150 を 173 へ増やしていた**。ゴールデン `S-total-density` はこれで、**39 件中この 1 件だけ再凍結**した（合成ケースで、保存作品ではない）
- **検査面自体の欠陥を 1 つ直した。** 前版で置いたピン 19 件は `pytest.xfail()` を**命令形で**呼んでおり、**結果に関わらず xfail になる＝個数が通るようになっても赤くならない**。アサーションへ置き換え、**DDL が言及していない個数（137 / 200 / 300）は依然として切られること**を判別テストにした。免除が分岐の無効化にならないことの担保である
- **摂動**: 免除を常に False に倒すと **31/50（日本語 20 / 英語 11）へ戻り、着手時の実測と完全一致**。DDL 個数の抽出を空集合にしても同じ値。合計予算を旧実装へ戻すと新設 3 テストが 3 件とも落ちる
- **本番相当のベンチは回していない**（作者裁定）。決定的ゲートが全て緑で、残る未知「Stage 2 が遵守した個数を出す頻度」は v2.7.5 で測ってある（coerce 手前で日本語 78〜100% / 英語 67〜100%）
- **積み残し**: 代表化の値が 80〜120 に収まらないことがある（180 → 75。`_clustered_visual_count` の既存挙動）。**プロンプトと SPEC は「240 以上は 80〜120」と言っており、決定的な層がそれに従っていない**
- **検証:** server **1207 passed / 30 skipped**（着手時 1181 / 30 / 19 xfailed）、cli **69 passed**、ruff clean、`npm run check` 0 errors / 2 warnings / 217 files。参照コーパス `render-engine-14` / `ddl-engine-1` は**再生成せず**緑。engine 版は据え置き

---

### v2.7.7 — 代表化の値が、仕様の帯に収まる（Build 716、2026-07-27）

**プロンプトと SPEC は「240 以上は 80〜120 で代表化する」と書いているのに、下限は 48 だった。**

- 露見するのは 1 経路だけである。240 以上の代表化は元から 100〜120 に収まっており（`_clustered_visual_count(240)=100`）、**帯を外れるのは 121〜239 の群**。v2.7.6 の規則ではこの帯は literal なので、**合計 400 超で予算に倒されたときにしか代表化されない**。そこで **180 → 75** が出ていた
- 下限を 80 にした。**`MIN_VISUAL_CLUSTERED_COUNT` / `MAX_VISUAL_CLUSTERED_COUNT` を対にして定数へ書いた** — 帯は一つの規則であり、片方だけが名前を持っているのは、この食い違いが 1 年近く見えなかった理由でもある
- `[180, 150, 130]` は **`[80, 150, 130]`**（従来 `[75, 150, 130]`）。合計は 360 で予算内
- **判別テスト**: 121〜2000 の 10 点で、代表化の結果が必ず帯に入ることを固定した。**下限を 48 に戻すと 121〜239 側で落ちる**
- **ゴールデン 39 件は 1 件も動かない**（`S-total-density` は 200 → 84 で、元から下限より上）
- **検証:** server **1217 passed / 30 skipped**、cli **69 passed**、ruff clean。個数保存 **50/50 のまま**

---

### v2.7.8 — 種と痕跡の作り直し（render engine 15）（Build 717、2026-07-27）

**`renderer.py` への 5 つの変更を 1 つの版にまとめた。** どれも同じ層に属し、版を 4 回上げれば Android の追随が 4 回要る。

- **印の種を allowlist にした。** `_seed_for_instruction` は **instruction の dump 全体のハッシュ**で、**coerce が書いた色の注記を書き換えるだけで絵が変わり**、構図フラグの A/B が交絡していた。入るのは何であるか・道具・幾何・変奏・表面・`arrangement.jitter` だけ。**全 49 フィールドの感度は 30 が動き 19 が動かない**
- **地の種は支持体の名前にした。** `_texture_seed` も Score 全体のハッシュで、**絵のどこを触っても紙の粒が入れ替わった**。`material` + `grain` + 演奏 seed だけから作るので、**濃さを上げると同じ紙が濃くなる**。これで **`ground.absorbency` の退役が解けた** — 値は一度も読まれていないのに、消すと粒が動くので退役できずにいたフィールドである
- **`cloudform` を、ほかの閉輪郭と同じ道に載せた。** class に `stroke-engine-touch` を名乗りながら**一度も `stroke_engine` を通っておらず**、材質層の 3 機構がすべて不在だった。**雲形専用の合成は書いていない**（内部塗りが作る密なポリラインをそのまま手描き経路へ渡す）
- **角のある図形と `pen` に材質層を与えた。** `_render_corner_shape` には材質輪郭の呼び出しが**そもそも無く**、`triangle` / `polygon` はどの道具でも裸だった。**本番で最も使われる `pen`** も本体のストロークだけだった
- **強さは距離ではない、と直した。** 材質強度の梯子は「層が読みにくい」への対処のたびに `outline_offset` の倍率を上げ（最終 2.8 倍）、下限 3.5px を敷いていた。**帯の実測半幅と比べると痕跡は `pencil` で 4.5 倍・`chalk` で 6.5 倍離れており、痕跡でなく第 2 の輪郭として読めた**。倍率を 1.0・下限を 0 にした。**仕様表は悪くなかった**（表の値はもともと半幅の 0.7〜2.3 倍）。濃さの梃子は触っていない
- **コーパスは 350 件（新規 4・削除 1）。318 件が動き、動かない 32 件が版の説明になる** — `computer` と `rotring` の（`cloudform` を除く）7 図形と `D-canvas` の `rotring` 4 件。**機械の 2 極は演奏 seed を消費しない**ので最初の変更が届かず、**新たに共有した `cloudform` でだけ動く**
- **新規 4 件はコーパス史上はじめて `ground.seed` を明示しないケース**である。engine 14 まで地のあるケースは全て seed を固定しており、**`_texture_seed` は 347 件を通じて 0 回しか呼ばれていなかった**
- **`hair` の材質層は与えたうえで撤去した**（作者裁定「`hair` の全面廃止が妥当」）。廃止される道具に層を足しても削られるだけなので、段 4b は `pen` のみになった。**廃止本体は別契約**
- **範囲の限定**: 「count を変えても筆致が保たれる」は **`layout="scatter"` のときだけ**成立する。`horizontal` / `vertical` / `radial` / `grid` では 12 → 13 で先頭 12 本も動く。種の漏れではなく、**区間を個数で割る配置では位置が動くのが当然**である
- **検証:** server **1402 passed / 30 skipped**（着手時 1268 / 6 failed / 30）、cli **69 passed**、ruff clean、`npm run check` 0 errors / 2 warnings / 217 files。**生成器を 2 回回して凍結物がバイト一致**（CI のガードが通る状態）

---

### v2.7.9 — 銀筆（Build 718、2026-07-27）

**`hair` は筆ではなかった。** 0.5px・stiffness 0.93・energy_width 0.08 という値は、手の道具のなかで最も細く最も揺れない線であり、それは**銀筆**の物理である。engine 15 では「全面廃止」と裁定されていたが、**廃止すると `pencil` に寄せることになり線が 3 倍太くなる**ため、作者裁定が**改名**へ変わった（2026-07-27）。

- **改名は絵を動かす。** 演奏 seed の材料に `weight` が入っているので、道具名の文字列が変われば同じ Score が別の演奏になる。**`A-hair-line` と `A-silverpoint-line` は 2299 → 2306 バイト、座標 126 個のうち 120 個が動いた**。作者は比較シート 2 枚を見たうえでこれを払うと裁定した
- **動いたのは名前を持つ 16 件だけ。** コーパス 350 件のうち `A-silverpoint-*` 8 件と `E-wild-silverpoint-*` 8 件が入れ替わり、**共通の 334 件はマニフェスト entry がバイト差 0・SVG 実体 302 本が全部バイト一致**。coerce ゴールデン 39 件のうち採り直したのは 10 件で、**旧 expected の `hair` を `silverpoint` へ置換すると新 expected と完全一致する**
- **保存済み作品は読み込み時に置換する。** `Instruction.weight` の `field_validator(mode="before")` が `hair` を `silverpoint` へ書き換える。**pentala の本番 DB で `hair` を持つ 445 件を全部引いて replay し、444 件が受理され `silverpoint` が 581 instruction**。受理されなかった 1 件は `Weight` enum に一度も入っていない `rope` を持つ作品で、**改名前から replay 不能**だった
- **`GRAMMARS` と幅の値は 1 つも変えていない。** 変更行は `stroke_engine.py` / `renderer.py` の 3 行で、キー文字列以外の全文字が一致する。**8 値と機械の極の付属 3 値を直接ピンする検査を新設**し（`test_silverpoint_rename.py`）、「手の道具でもっとも揺れない・もっとも膨らまない・`rotring` より細い」という順位も入れた
- **これが唯一の振る舞いの変更**: 歳時記の登録から `_PRUNED` を外し、**銀筆を語彙へ戻した**。てざわりは 10 語から 11 語になり、**材質マーカーの先頭が `鉛筆` → `銀筆`** に変わる。Stage 1 のプロンプトに載るので、第一段階がこの語を出力しうる。**H1（銀筆が実際に選ばれるか）を測るには語彙に無ければならない**ため
- **プロンプト digest の固定値を 16 個採り直した。** Stage 1 base / actual の日英、Stage 2 の `SYSTEM_PROMPT` 日英・tool schema、`_stage2_prompt_digest` 日英、判別値 3 つ。**Stage 1 の golden fixture は差し替えていない** — `test_saijiki_golden.py` の許可差分が「削剪」から「改名」へ移っただけで済み、**fixture は Build 591 の実物のまま**である
- **engine の版は上げていない。** `render_engines/default.py` は `"15"` のまま。**段 4 の入れ替えを行った最初の 1 回だけ生成器の同一性ガードが発火した** — ガードは「`cases` が動いたのに identity フィールドが動いていない」で判定するので、**版を上げない改名と、凍結物の無断書き換えを区別できない**。2 回目以降は exit 0 でバイト一致
- **触っていないもの**: `interpreter.py` の few-shot 検索キーワード `"髪"`（作者が書く入力語を引く検索キーであり出力語彙ではない。ただし**銀筆を足していないので、語彙に戻った銀筆はこの細線の作例を引かない**）／`language_support/ja.py` `en.py` の `material_weight_hints`（銀筆の行が無い。`hair` の行も元から無く、改名による退行ではないが、**Stage 2 が weight を落としたときの救済経路が銀筆には無い**）／`brush_fine`（面相筆）と太さの軸／Android（`gen_android_reference.py` の 2 箇所は改名したが**生成器は未実行**で、Kotlin 実装と、凍結 fixture 36 件のうち `hair` を含む 3 件はそのまま）
- **検証:** server **1411 passed / 30 skipped**（+9 は新設した判別検査）、cli **69 passed**、ruff clean、`npm run check` 0 errors / 2 warnings / 217 files、`npm run lint:i18n` **788 / 36 例外 / 0 errors**。判別テスト 4 本（置換経路の恒等化で本番 445 件が全て `ValidationError`／`GRAMMARS` の 1 値を動かすと 16 本だけ動く／`_PRUNED` に戻すと digest が改名前の値へ完全に一致する／生成器 2 回でバイト一致）
- **記録**: 種が道具の「名前」をハッシュしているのが、改名に値段がつく理由である。**直すと全作品が一度動くので、それは engine の版に属する**

---

### v2.7.10 — PNG は resvg だけで焼く（Build 719、2026-07-27）

**警告は前からあった。それでも誤らせた。** cairosvg は `feTurbulence` / `feDisplacementMap` / `feGaussianBlur` を実装しておらず、**失敗もせずに落とす**。地の粒も材質フィルタも消えた「きれいに見える PNG」が返り、**その絵が判断の材料に使われていた**（4 回）。CLI の stderr 警告・サーバーの起動時 WARNING・docstring・成果物へ記録する `png_rasterizer` と、注意書きは v2.2.1 から四重に置いてあった。**人を誤らせるのはログ行ではなく絵である**ため、文書化ではなく撤去を選んだ（作者指示 2026-07-27）。

- **フォールバックは無くなった。** `rasterizer.py` から `_cairosvg_renderer` と `BACKEND_CAIROSVG` と `_BACKENDS` 表が消え、`svg_to_png` は resvg が無ければ `RasterizerUnavailable` を上げる。**これが唯一の振る舞いの変更**であり、resvg の入っていない環境では PNG 出力が静かに劣化するのではなく停止する（作者裁定済み）
- **依存宣言 3 本を落とした。** `shared` / `server` / `cli` の `pyproject.toml` から `cairosvg>=2.7.0` を外し、lock 2 本から 145 行・148 行が消えた。`api.py` の起動ログはフォールバックの分岐を失い、`cli.py` は 1 回だけ出す警告と「resvg-py or cairosvg」というエラー文 3 か所を失った
- **番人を 3 つ置いた。** ① 3 つの `pyproject.toml` を `tomllib` で読み、依存に `cairosvg` が無いこと ② `shared/src` / `server/src` / `cli/src` / `server/scripts` の全 `.py` に `import cairosvg` が無いこと（**行頭の import 文だけを見るので、なぜ消えたかを述べる散文は残せる**） ③ **入っていても到達不能であること** — `pytest.importorskip` で cairosvg が import できる環境だけ走り、resvg を塞いだうえで例外になることを確かめる
- **増えた skip 1 はその 3 つ目である。** 依存を落とした結果 cairosvg が venv から消えたので、番人 ③ は skip になる。**入っている環境でだけ意味を持つ検査なので、これは正しい**
- **副次: `server` の venv から `pillow` が消えた。** cairosvg の推移依存だったもので、`server` と `shared` のコードは `PIL` を一度も import していない。`cli` は `pillow>=12.0.0` を明示依存に持つので残る（**コンタクトシートを組む道具は `cli` の venv にある**）
- **撤去しなかったもの**: `android/scripts/render_png_review.py` は `cairosvg.svg2png` を呼んだままである（番人 ② の走査範囲は `android/` を含まない）。**Android の目視レビュー用スクリプトであり、今回の撤去理由がそのまま当たる経路**なので、扱いは作者裁定を仰ぐ。`server/Dockerfile` の `libcairo2` も cairosvg のための OS 依存だが残っている
- **文書 4 本を現在形に直した**: `SETUP.ja.md` / `SETUP.md`（「resvg を優先し CairoSVG はフォールバック」→ resvg のみ・不在なら停止）、`manual/ja/application-install.md` / `manual/en/application-install.md`（「CairoSVG が必要とする OS ライブラリ」→ resvg-py は wheel なので追加の OS ライブラリは要らない）。**過去の版を述べる CHANGELOG と PROJECT_CONTEXT の記述は履歴なので触っていない**
- **検証:** server **1413 passed / 31 skipped**、cli **69 passed**、ruff clean（`server` / `cli` とも）、`npm run check` 0 errors / 2 warnings / 217 files。**SVG は 1 バイトも変わらない**ので参照コーパスは再凍結していない。render engine は `"15"` のまま
- **記録**: 黙って劣化するフォールバックは、文書化ではなく撤去する。**「差が無い」と「差が写っていない」を区別できない道具を、絵を判断する経路に置かない**

---

### v2.7.11 — 番人は、見る場所ではなく見ない場所で書く（Build 720、2026-07-27）

v2.7.10 の取りこぼし 2 件と、**規則そのものを SPEC へ書く**という作者裁定（2026-07-27）の反映。

- **`android/scripts/render_png_review.py` を resvg へ替えた。** 休眠スクリプトではなく、
  `headless_render_compare.sh` と `headless_batch_compare.sh` から `PNG_REVIEW=true` のときに
  呼ばれる**現役の目視ハーネス**である。**server と Android の SVG を PNG 化して差分を増幅し、
  3 枚並べたシートと `metrics.json` を作る道具**なので、**フィルタを落とすラスタライザを挟むと
  「両方とも平らに潰れた場所で一致する」**。v2.7.10 で cli から依存宣言を落とした結果、
  **`uv sync` 済みの環境では既に ImportError で落ちる状態だった**
  - 実測: `feDisplacementMap` の scale だけが違う 2 枚を通すと **mean 9.5% / rms 27.4% の差**を出す。
    **cairosvg ならフィルタごと飛ぶので差 0 になる組**である
- **番人②の走査範囲を「名指し」から「リポジトリ全体 − 除外」へ変えた。**
  v2.7.10 は `shared/src` / `server/src` / `cli/src` / `server/scripts` の 4 根を名指ししたので、
  `android/` を見ていなかった。**名指しの一覧は不完全になりうるが、不完全だと言うことはできない**
  - 除外は 14 個（`.git` / `.gradle` / `.pytest_cache` / `.ruff_cache` / `.venv` / `__pycache__` /
    `bench` / `build` / `dist` / `no-git-sync` / `node_modules` / `out` / `out2` / `site-packages`）
  - **走査が実際に外へ届いていることを見る検査を足した**（`android` / `cli` / `server` / `shared` の
    4 つが走査結果に含まれること）。**番人の値打ちは覆う範囲までしかない**
  - **摂動で確かめた**: `android/scripts/render_png_review.py` に `import cairosvg` を戻すと落ちる
- **走査を広げて分かったこと（直していない）**: 追跡外の `no-git-sync/` に **cairosvg を呼ぶ
  過去の測定スクリプトが 3 本**ある（`fable5/render_with_relations.py`、
  `rfc/phase0-scripts/ab.py`、`rfc/phase0-scripts/ps_ab.py`）。**何を回したかの記録**なので
  書き換えず、走査からも外した。**venv から cairosvg が消えているので、再実行すると ImportError で止まる**
  （`render_with_relations.py` だけは ImportError を捕まえて PNG 生成を飛ばす。**絵は出ない**ので安全側）
- **SPEC に規則を書いた（作者裁定）** — `SPEC.ja.md` **§15.12「PNG は演奏を写す」** /
  `SPEC.md` **§12.9**。①黙って落とすラスタライザを使わない（対象は少なくとも `feTurbulence` /
  `feDisplacementMap` / `feGaussianBlur`。**性能や画質ではなく観測の話**である）
  ②**`cairosvg` は使用を禁じる** ③無い実装より誤る実装のほうが悪い（フォールバックを置かない）
  ④守り方（唯一の入口と番人 3 つ、**②の走査範囲は見ない場所で書く**）
- **`server/Dockerfile` の `libcairo2` は今回落としていない** — **次回の配布で落とす**という
  作者裁定（2026-07-27）。リリース runbook に **A-1b** として手順を起票した（一度きり・済んだら削除）
- **検証:** server **1414 passed / 31 skipped**（+1 は走査範囲の検査）、cli **69 passed**、
  ruff clean、`npm run check` 0 errors / 2 warnings / 217 files。**SVG は 1 バイトも変わらない**

---

### v2.7.12 — 紙が、どう作られたかを言う（render engine 15 へ畳む）（Build 721、2026-07-27）

**`plain` / `paper` / `washi` / `ink_wash` は地の層で完全に同一だった。** 分岐していたのは
`mezzotint` と `charcoal_ground` だけで、`material` は種の材料に入るだけだった。
**作者裁定で engine 15 へ畳んだ**（版を上げない。本番 DB に engine 15 の作品が 0 件・未公開・
engine 13 の前例。**条件は「公開前に済ませること」**）。

- **最初の実装は却下相当だった。** 繊維と刷毛を**描画要素として積んだ**ところ、既定プロファイルで
  地が 2 要素 → 40 要素になり、**絵全体の 46%** を占めた。**DDL が明示した図形は 2 つしかない**
- **直した方針: 支持体は雑音の性格であって、描くものではない。**
  `display` はフィルタの中（**`washi` = 異方性乱流を直交 2 枚で交差させる `feBlend`**、
  **`ink_wash` = 横へ引き伸ばして `feGaussianBlur` でぼかす**）、
  `editable` は**地が既に描いている粒の形**を変える（washi は同じ数の粒を繊維へ引き伸ばし、
  ink_wash は粒を横帯で濃淡させる）
- **要素は 1 つも増えない。凍結物がそれを言っている** — `C-ground-washi` は
  **circle 20 → 0 / path 1 → 21**（総数 21 のまま）、`C-ground-ink_wash` は
  **circle 20 → 12**（`count × 0.6` で**減る**）
- **数値は作者が実物を見て決めた。** 比較シート `material-sheet.png` の 2 行のうち
  **`tone=warm grain=coarse opacity=0.18` の行を採る**（2026-07-27 裁定）＝
  **washi をこれ以上強めない**。0.18 は既に全経路の上限（`min(0.18, ground.opacity)`）である
- **コーパスは engine 15 のまま再凍結した。** 動いたのは **350 件中 3 件**
  （`C-ground-washi` / `C-ground-ink_wash` / `C-groundseed-auto-washi`）。
  **`changed_from_previous` を作り直しても 318 件で完全一致**することを先に実測してから採用し、
  **2 回目の生成が exit 0 でバイト一致**することを確かめた。manifest の `reason` に畳んだ旨を追記
- **Android の期待値は動かなかった。** 再生成した 36 件が **`31ff75d`（engine 15 の凍結）と
  バイト一致**する。**Android の参照ケースは地を 1 件も持たない**ためで、
  **「畳むと Android の期待値が作り直しになる」という前提は実測で外れた**。
  `feat/android-engine15` の契約はそのまま着手できる
- **検査面を新設した（実装には付いていなかった）** — `test_ground_seed.py` に 5 件。
  材質が**粒の描き方**を変えること（washi / ink_wash）、**filter のほうも**変わること
  （`feBlend` / `feGaussianBlur` の有無）、**要素数が増えないこと**
  - **seed を明示して固定する**のが要点。**固定しないと `material` が導出 seed に入っているぶんで
    層が動き、描き分けの分岐を `if False` にしても素通りした**（実測。主張と違う理由で通る検査だった）
  - **摂動 2 種で落ちることを確認**（雑音表を空にする / 繊維の分岐を殺す）
- **既存検査 1 件を書き直した** — `test_explicit_ground_seed_still_bypasses_the_derivation` は
  「明示 seed なら paper と washi は同一」と主張していたが、**材質が描き分けられた今は偽**である。
  **材質ではなく演奏 seed を動かす**形に置き換えた（材質を固定したまま導出だけを揺らせる）
- **検証:** server **1419 passed / 31 skipped**（+6）、cli **69 passed**、ruff clean、
  `npm run check` 0 errors / 2 warnings / 217 files。**render engine は `"15"` のまま**

---

### v2.8.0 — 奥書の道は colophon で通る（**互換が切れる**）（Build 722、2026-07-27）

**打鍵する名前は英語の術語で付いている** — `paint` / `refine` / `lineage` はいずれも用語辞書と
一致しており、辞書は「API `/api/paint` と一致」と**明記**している。**その欄でローマ字だったのは
`okugaki` だけ**だったので、**CLI サブコマンド名と API パスを `colophon` へ移した**
（作者裁定 2026-07-27）。**エイリアスは残していない。**

- **minor 採番の理由は互換が切れることである。** 採番規則の「互換性が切れるとき（保存データ・
  API・エディション ID の形式変更）」に当たる。**patch ではない**
- **語を運んでいたのはパスだけだった** — 応答モデル `OkugakiItem` のフィールドは
  `id` / `target_node_id` / `branch_snapshot` / `model` / `at` / `language` / `body` /
  `warnings` / `fact_sheet` で、**`okugaki` という名前のフィールドは一つも無い**。
  だから改名はパスで閉じる
- **動かしたもの（4 経路）**:
  `GET|POST /api/lineage/{node_id}/okugaki` → **`/colophon`**、
  `DELETE /api/okugaki/{okugaki_id}` → **`/api/colophon/{colophon_id}`**、
  CLI サブコマンド `okugaki` → **`colophon`**、web の fetch 3 か所
- **動かしていないもの（辞書 §6 の識別子）**: DB のテーブル名 `okugaki` と索引
  `uq_okugaki_user_idempotency`、**`model_settings` の `okugaki_model`（保存済みユーザー設定）**、
  モジュール名 `okugaki.py`、i18n の鍵 `okugaki*`
  - **鍵名のローマ字は例外ではなく通例である** — 変奏は `variation` という完璧な辞書語を
    持ちながら、web の鍵は **`hensouAxis` / `hensouSmall` / `hensouMedium` / `hensouLarge` …
    とローマ字のまま**である。辞書が禁じているのは**表示に出る語**であって、鍵ではない
- **番人を 2 つ置いた**: ① `app.routes` に `okugaki` を含むパスが 1 本も無く、
  `/api/lineage/{node_id}/colophon` と `/api/colophon/{colophon_id}` が在ること
  ② CLI が `colophon` を受け、**`okugaki` では `SystemExit` になること**（エイリアス不在の確認）
- **辞書に境界を書いた** — `GLOSSARY.md` の 奥書 の行に「CLI サブコマンドと API パスも colophon」
  を追記し、**§6 に「例外が 1 つある」節**を足して、**何を動かして何を動かさないか**と
  **`hensou*` が反例であること**を記録した
- **文書 4 本を追随**: `SPEC.ja.md`（`inku-cli okugaki` → `colophon`）、
  `SPEC.md`（**「(okugaki)」の併記と「the CLI subcommand keeps its name」を削除**）、
  `manual/{ja,en}/cli-reference-for-ai.md`（§2.5 の見出しと用例）
- **検証:** server **1420 passed / 31 skipped**（+1）、cli **70 passed**（+1）、ruff clean、
  `npm run check` 0 errors / 2 warnings / 217 files、`npm run lint:i18n` **788 / 36 例外 / 0 errors**。
  **描画には触れていない**（render engine は `"15"`、SVG は不変）

**変奏の語も同じ版で通した（Build 723）。** 奥書は「1 語がローマ字」だったが、
**変奏は衝突が逆向き**だった — 辞書は `variation` を**変奏だけ**に予約している
（候補は `option`。「~~variation~~（変奏と衝突）」）のに、実装では
**本物の変奏がローマ字 `hensou` で、変奏でない 4 種が `*_variation`** を名乗っていた。

- **系譜の derivation kind を入れ替えた（保存済みの値）**: `hensou` → **`variation`**（本番 7 行）、
  `touch_variation` → `touch_change`（25）、`model_variation` → `model_comparison`（11）、
  `layout_variation` → `layout_change`（6）、`language_variation` → `language_comparison`（1）。
  本番 0 件の 4 種（`render_engine_` / `age_` / `hacho_` / `external_seed_`）も `_change` へ揃えた
- **`vary_seed` → `composition_seed`（186 箇所）。これは変奏ですらない** —
  SPEC §12 が「記述者が持つ口は 入力テキスト・`vary_seed`・`tenkei`・そして**変奏**」と
  **並べて**書いており、Stage 1.5 の**構図**の seed である。
  **変奏の seed は `variation_seed`、強度は `variation_amplitude` で、どちらも改名していない**
- **CLI の旗も移した**: `--vary-seed` → `--composition-seed`、`--vary N` → `--composition-count N`
- **web の i18n 鍵まで揃えた**（`hensou*` → `variation*` 14 個ほか）。
  **鍵名のローマ字は本来なら通例**だが、「辞書を完全に通す」という作者裁定による
- **保存済みデータは migration で移す。壊さない** — 起動時に
  `history.vary_seed` 列を `composition_seed` へ RENAME し、`lineage_edges.derivation_kind` を
  表で UPDATE する。**本番 DB の複製で実測**（件数 25/11/7/6/1 が保存され、`composition_seed` は 9 件）、
  **2 回目は 0 行＝冪等**
- **凍結したもの（改名すると壊れる）**: **rh2 payload の鍵 `vary_seed`** は
  **同一性 ID の材料**なので名前のまま置いた。**改名したら保存済み全作品の rh2 が動き、
  検査が実際に捕まえた**（`test_legacy_render_hash_v2_calculation_remains_available`）。
  `ddl_expander` の salt `#hensou` / `#vary` も hash の材料なので不変
- **新旧の対応は `no-git-sync/opus5/name_convantion/RENAMES.md` に記録した**（作者指示）。
  外部スクリプトが動かなくなったときはここを引く
- **採番は v2.8.0 のまま**（**未公開なので畳む**。タグ最新は `v2.7.2`）。Build だけ 723 へ
- **検証（Build 723 時点）:** server **1420 passed / 31 skipped**、cli **70 passed**、ruff clean、
  `npm run check` 0 errors / 2 warnings / 217 files、`npm run lint:i18n` **788 / 36 例外 / 0 errors**

**記述の語も同じ版で通した（Build 724）。** 辞書は **記述 = `description`（動詞は write）** と定め
`prompt` を禁じているのに、**要求の主フィールドだけが `text` という第三の語**だった。

- **`text` → `description`、`original_text` → `original_description`**。
  `/api/paint` / `/api/paint/stream` / `/api/interpret` / `/api/compose` の要求、
  `/api/paint` の応答、CLI の成果物 JSON、web の送信すべて
- **`dh1`（記述の同一性 ID）は影響を受けない** — `description_hash()` は**値だけ**を hash しており
  鍵名を含まない（実測して確かめてから着手した。`rh2` で痛い目を見た直後だったため）
- **改名していない `"text"` が 4 つある** — **LLM プロバイダへ送る payload**
  （Anthropic の content block、Gemini の `parts`）。**外部 API の契約である**
- **`history` の DB 列 `input` は第三の語のまま残っている**（server 9 箇所・web 30 箇所）。
  **記述を指す語が `description` と `input` の 2 つになった**ので、次の一手として測ってある
- **検証（Build 724 時点）:** server **1420 passed / 31 skipped**、cli **70 passed**、ruff clean、
  `npm run check` 0 errors / 2 warnings / 217 files

**その語の付け先を、同じ版で直した（Build 725）。** Build 724 は語を移したが、
**`description` を「作者が書いた記述」ではなく「文脈を注入したあとの、Stage 1 へ渡す文字列」に
与えてしまい**、作者の本文のほうが `original_description` へ降格していた。
**辞書の語が指すのは作者が書いたものである。**

- **`description` = 作者が書いた記述**（`/api/interpret` と `/api/paint` では必須）。
  **Stage 1 が実際に読む文字列には `stage1_input` という別の名前を与えた**（省略可）。
  **省略した要求では記述そのものが Stage 1 へ渡る**ので、文脈を注入しない client は
  `description` だけ送ればよい
- **`/api/compose` の `original_description` も `description` へ**（省略可。DDL だけで叩けるため）。
  web の送信 4 箇所がこの経路（契約は 3 箇所と見積もっていたが、実測は 4 箇所だった）
- **`input` は据え置き**（作者裁定）。保存・表示される本文であって記述とは限らない —
  本番 1780 件のうち **DDL 風が 4 件・空が 38 件**で、中立な語のほうが筋が通る
- **内部の関数引数は 1 つも動かしていない** — `composer.py` の `original_description` と
  `_call_compose_detail` の引数は**初めから意味が正しい**（Stage 2 へ渡す作者の原文）。
  **片側だけ動かすと `TypeError` になり、1 回目の着手はそれで 42 件赤にした。**
  動かしてよいのは **① Pydantic のフィールド定義 ② `req.` の読み出し ③ client の送信本体**の 3 つだけ
- **判別テストを 4 本足した** — `stage1_input` を省いた要求で Stage 1 が `description` を読むこと、
  送った要求では `stage1_input` を読むこと、**そのとき履歴に残るのは `description` のほう**であること
  （augmented が保存されない）、CLI の payload が両者を分けること。
  **実装を 2 点摂動して、それぞれが実際に赤くなることを確かめてから戻した**
- **採番は v2.8.0 のまま**（未公開版へ畳む）。Build だけ 725 へ
- **検証（Build 725 時点）:** server **1423 passed / 31 skipped**、cli **71 passed**、ruff clean、
  `npm run check` 0 errors / 2 warnings / 217 files

**残っていたローマ字も同じ版で通した（Build 726）。** 打鍵する名前を全数
（CLI サブコマンド 46・オプション 84・API パス 61）調べたところ、
**奥書と同じ形はあと 1 つだけだった** — **添景の旗 `--tenkei`**。

- **辞書は 添景 = `staffage` と既に定めており（GLOSSARY :58）、web はその語で表示していた。**
  **ローマ字が残っていたのは打鍵する側 1 箇所だけ**だった。`--staffage` へ移し、
  **エイリアスは残していない**（奥書と同じ方針・2026-07-27 作者裁定）
- **help の文言は第三の語だった** — `scenery level` と書かれており、
  `tenkei` でも `staffage` でもなかった。`staffage` へ揃えた
- **動かしていないもの**: API の要求・応答フィールド `tenkei`（server 27 箇所）、
  DB 列 `history.tenkei`、`tenkei_for_node()` 等の内部識別子、web の `tenkei.ts` と i18n 鍵。
  **鍵名のローマ字は通例**という規則がそのまま効く。**外部スクリプトが直すのは旗の綴りだけ**で、
  payload を組み立てている側は触らなくてよい
- **番人を 2 つ置いた** — `--tenkei` が `SystemExit` になること、**旗の一覧そのものに
  `tenkei` を含むものが無いこと**。**2 本目は最初 上位パーサしか見ておらず、
  エイリアスを残す摂動を素通りさせた**（実際の旗はサブパーサ側に付いている）。
  **サブパーサまで降りるよう直してから、摂動で 2 本とも赤くなることを確かめた**
- **`/api/saijiki` と歳時記のカテゴリ鍵 9 個はローマ字のままでよい** — 辞書が
  歳時記 = `Saijiki`（固有名詞・大文字）と定めているとおりで、**ローマ字が正しい英語表記**である。
  `renga` / `hacho` も同じ扱い。**`sumi` / `washi` は識別子ではなく DDL の語彙値**
  （`sumi` は `black` の同義語として `ink` / `obsidian` / `黒` と並ぶ、記述者が書く語）
- **採番は v2.8.0 のまま**（未公開版へ畳む）。Build だけ 726 へ
- **検証（Build 726 時点）:** server **1423 passed / 31 skipped**、cli **73 passed**、ruff clean、
  `npm run check` 0 errors / 2 warnings / 217 files、`npm run lint:i18n` **788 / 36 例外 / 0 errors**

**CI を 8 回ぶりに緑へ戻した（Build 727）。** v2.7.9 の銀筆改名は、render コーパスと
coerce ゴールデンを**版を上げずにその場で再凍結**したが、**DDL コーパスだけ同じ commit で
取りこぼされ**、以後すべての push で `ddl-engine` job が落ちていた（`render-engine` は毎回緑）。

- **再凍結が運んだのは改名 2 種だけ** — `B-trigger-auto` の coerce 出力の道具名と、
  **`vary_seed` を記録したままだった manifest の入力 15 件**。ケース ID の増減は無く、
  **`ddl_engine_version` は 1 のまま**（render engine が 15 のまま据え置かれたのと同じ理由）
- **仕組みは引き継ぎの記述と違っていた** — 凍結物の `hair` は
  「保存値を validator が書き換える」ものではなく、**coerce 自身が生産していた道具名**だった。
  **摂動を validator に当てたら空振りし、`coerce/compose.py` の literal に当てて初めて動いた**
- **ガードは書き込みの後に発火するようにした**（render 生成器と同じ順）。
  **手前で止めると、裁定済みの改名を再凍結する道が構造的に無い**。
  **1 回目に発火し 2 回目が exit 0 でバイト一致する**のがガードの守りたい性質である
- **SPEC §15.6 に「改名では版を上げない」を条文化した**（日英）。v2.7.9 の判断が条文に無く、
  字義どおりなら発火する状態だった。**語彙を増やすのと名前を替えるのは別**である
- **`check_frozen_corpora.py` を足した** — CI と同じものを手元で回す。
  **テストは代替にならない**（`test_*_reference.py` は凍結物と manifest の整合しか見ず**再生成しない**）ので、
  **1423 件が緑のままコーパスがずれる**。実際 3 回ともそうなった。
  **CI は最後の砦（別 OS での再現性）であって検出器ではない**、という置き方に改めた
- **摂動で赤を確認した** — `coerce` が生産する道具名を 1 箇所変えると検査が exit 1 を返し、
  復旧手順（`git checkout -- server/reference/`）まで案内することを見た
- **採番は v2.8.0 のまま**（未公開版へ畳む）。Build だけ 727 へ
- **検証（Build 727 時点）:** server **1423 passed / 31 skipped**、cli **73 passed**、ruff clean、
  `npm run check` 0 errors / 2 warnings / 217 files、**両コーパスの生成器が exit 0 でバイト一致**

**記述の語を打鍵する側でも揃え、壊れていた 2 経路を直した（Build 728）。**
`--original-text` は**埋める先の鍵が Build 725 から `description` なのに、綴りに退役した
第三の語 `text` を残していた**。`--description` へ移し、**エイリアスは残していない**
（奥書・添景と同じ方針）。`refine generate --text` も同じ鍵を埋めていたので揃えた。

- **同じ改名が 2 つのコマンドを黙って壊していた** — **`inspect` と `refine generate` は
  `_paint_payload` を通らず自前で payload を組んでおり**、**Build 724 以降も旧鍵 `text` を
  `/api/paint` へ送り続けていた**。`description` が必須なので、**その要求は 422 で落ちる**。
  **cli のテストは実サーバを叩かないので 73 件が緑のまま気づけなかった**
- **捕まえたのは「`*-text` 系の旗を全数分類した」から**である。`--original-text` だけを
  名指しで直していたら、この 2 つは壊れたまま残った
- **`paint` / `batch` の help が記述を `prompt` と呼んでいた**（辞書が退けている語）。
  4 箇所を直した。**`review evaluate --prompt` などはそのまま** —
  **あちらは LLM のプロンプトという別の指示対象**で、禁じられているのは記述を指す用法である
- **番人を 4 つ置き、3 点の摂動で全部が赤くなることを確かめた**（エイリアス残存／
  help が `prompt` へ戻る／`inspect` が旧鍵へ戻る）。
  **`inspect` の検査は `ApiClient` を差し替えて送信そのものを覗く** —
  静的な走査では LLM プロバイダ payload の `"text"` と区別がつかない
- **位置引数（`paint <text>`）は動かしていない**。打鍵するのは値であって名前ではない
- **採番は v2.8.0 のまま**（未公開版へ畳む）。Build だけ 728 へ
- **検証（Build 728 時点）:** server **1423 passed / 31 skipped**、cli **76 passed**、ruff clean、
  `npm run check` 0 errors / 2 warnings / 217 files

### 2026-07-27 — 英語ドキュメントの用語（**採番なし**・ドキュメントのみ）

**README と `manual/en/` を辞書に照らして全数走査し、直したのは 6 箇所だけだった。**
**ずれの本体は英語ではなく日本語の正本にあった。**

- **英語だけが禁止語 `prompt` を使っていた 6 箇所**を `description` / 日本語どおりの語へ直した。
  対応する日本語は `指示`欄 / 記述文 / 指示文 / 短い言葉 / 入力テキスト / 指示文生成モデル で、
  **どれも「プロンプト」とは書いていない**。ここは英語側の一方的なずれである
- **残る `prompt` 7 件はすべて正当**: 日本語も「プロンプト」と書いている 2 件（生成 AI 批評の一節・
  UI の JSON/プロンプト表示）、**LLM のプロンプトを指す** 4 件（Prompts パネル・`--prompt` 旗）、
  **コマンド例のファイル名 `prompts.txt`**（日英で同一の識別子なので動かさない）
- **`artwork` は README にも `manual/en/` にも 1 件も無かった**（引き継ぎの記述は不正確だった）。
  出現するのは `CHANGELOG.md` と `PROJECT_CONTEXT.md` の**歴史的記述**だけで、これは書き換えない
- **`generation` / `image` / `create` の残存は、いずれも日本語の 生成 / 画像 / 作る を忠実に訳したもの**
  である（README.ja「もう一度生成」「による生成です」「生成後は歳時記を参照し」、
  manual/ja「画像の作成方法」「バッチ生成」「Stage 2が作る」）。
  **英語だけを辞書へ寄せると日英の対応が壊れる**ので、**手を付けずに作者裁定へ回す**
- **`SPEC.md` の `scene-tone palette` 7 件も同じ理由で保留** — `SPEC.ja.md` に対応する語が
  見つからず、正本を確かめないまま訳語を作れない。**`jitter` 3 件は識別子と信号の意味で正当**

### v2.8.0 — 六色を六色と呼ぶ（Build 729、2026-07-27）

**Stage 2 のシステムプロンプトが「場のトーンで palette を選ぶ」と書いていた。**
そこで選ばせているのは**六つの抽象色**であって色カタログではない
（カタログは server-owned で、Stage 2 のあとに解決される）。
**しかも `palette` はカタログ JSON の実在するフィールド名**でもあり、
**実装の中で一語が二つの意味を運んでいた**。日英とも「抽象色 / the abstract colors」に改めた。

- **これはモデルの挙動を動かす変更である。** Stage 2 digest は
  **`a1bb4ff70488fb35` → `261373d0123a740f`（日）／`397195ed887f6adb` → `f5bd29704e5906f2`（英）**
- **日本語プロンプトはバイト数が動かない**（42,824 のまま）。
  **長さだけを見る検査は素通りする**ので、捕まえるのは digest の固定値のほう。
  英語は 40,989 → 41,001
- **`SPEC.md` は写していた文言のほうを追随させた**（`scene-tone palette` → `scene-tone color` ほか 4 箇所）。
  **`SPEC.ja.md` にこの規則の記述は無い**（日英 SPEC は節番号ごと乖離している）
- **SPEC に残る `palette` 6 件は正当** — カタログ JSON の識別子 4 件と、一般的な美術語 2 件
- **挙動が同じであることは決定的には確かめられない**（プロンプトの語を変えた以上、
  LLM を回さないと分からない）。**決定的な検査はすべて緑**: server 1423 passed / 31 skipped
- **採番は v2.8.0 のまま**（未公開版へ畳む）。Build だけ 729 へ

### v2.9.0 — プロンプトが禁じて、かつ要求していた六つ（Build 730、2026-07-27）

**原則が禁じる語を、後段の規則・語彙表・作例のほうが要求していた。**
明文の禁止をすべて拾い、同じプロンプトの残り全部と機械的に突き合わせて六件見つけた。
二件（日本語の「塗りつぶす」・英語の `rise` / `fall`）は別セッションの発見で、残り四件は今回の検査。

- **日本語の背景の定型を許可動詞へ**: 「背景を○色で塗りつぶす。」→**「背景を○色で埋める。」**。
  原則2 は `塗りつぶす` を禁止動詞に挙げながら、§背景色 がその語で書けと命じ、作例 7 件が従っていた。
  **英語側は最初から `Fill background with X.` で衝突していない**（`fill` は許可動詞）。
  **矛盾は翻訳で片側にだけ生じていた**
- **解析側はすべて旧表現を受け続ける**ので、保存済み作品はそのまま演奏できる。
  Stage 2 の規則が新旧両方を名指し、`ddl_expander` の灰背景是正が `(?:塗りつぶす|埋める)` にマッチする。
  **残る背景検出（`api.py` の色判定・`compose.py` の節分割・`explicit_surface` マーカー）は
  `背景を` で判定していて動詞を見ない**ため無改修。**coerce に触っていないので凍結コーパスが動かない**
- **英語の `rise` / `fall` を運動語として書き分けた**（`rise (as motion), fall (as motion)`）。
  **同じ行の `scatter` は既に `(as motion)` / `(as arrangement)` と書き分けてあった**のに、
  かたむきの `rising` / `falling` には適用されていなかった。角度語である旨の一行も足した
- **「てざわりのない線・弧の文を出力してはいけない」に、自分の作例が違反していた** —
  日 20 文 / 英 9 文。**規則が悪例として名指しした「放射状に線を引く」が、
  作例に「放射状に細い線を八本引く。」としてほぼ原文で実在した**。
  関係・わりあい・かたむきだけを示す最小文を例外として明記し、**実作例 11 文にてざわりを補った**
- **原則5「使えるのは Saijiki の語彙のみ」を、語彙表に無い 13 語が破っていた**（面・地・版画技法・
  色とりどり等）。**原則5 のほうを実態へ書き換えた**（語彙 + 本書が定める定型）。語彙表は不変なので
  reference §1・web 表示・SPEC / README への波及が無い
- **未知対象の近似先から `多角形` / `polygon` を削った** — `saijiki.py` が
  「歳時記語彙ではない」と明記した隠しマーカーを、本文が出力させていた。
  Stage 2 の polygon 規則（`sides=5-8`）と coerce の `shape_intent_markers` 経由の生成は不変
- **許可動詞に `敷き詰める` / `tile` を追加** — うごき の語彙表は 6 語、原則2 の許可動詞は 5 語で、
  §配置選択 は「敷き詰める」の出力を要求していた
- **`ペン(既定)` を「既定値であって推奨値ではない」と明記** — 語彙表が既定と宣言する道具を、
  本文が二箇所で「寄せるな」と禁じていた
- **作例 1 件が禁止語 `広げる` を使っていた**（「半径を広げて並べる」）。
  これだけは後段のどの規則も正当化していない、素の違反だった

**検査の副作用**

- **Stage 1 の golden は Build 591 の fixture のまま**にし、差分を `_REORDERED_JA` / `_REORDERED_EN` へ
  **宣言**として 14 件追加した。**宣言のない差分は今も失敗する**
- **プロンプトの固定値を 20 個再凍結**（Stage 1 base 6・actual 3・base 共通 1・Stage 2 6・
  歳時記摂動 2・スキーマ摂動 2）。Stage 1 base 日 `9c8064958c8e3960` → **`f56778ec689949f8`**、
  Stage 2 combined 日 `261373d0123a740f` → **`b5b40bbc27885eb1`** / 英 `f5bd29704e5906f2` → **`778f6a6e2dfa124f`**
- **例プールの 1 件が選ばれなくなった** — 英語の雪の例にてざわりを補った結果、
  てざわり補充の fallback が発火しなくなり、置換先が 1 つずれて砂の例が k=5 の枠から外れた。
  テストは「プールから消えていないこと」へ置き換えた
- **`SPEC.ja.md` の Stage 1.5 の作例が灰背景のままだった**（`_avoid_gray_background` が白へ書き換えるので、
  仕様の例のほうが実装より古かった）。今回まとめて是正した
- **README の DDL は書き換えない** — あれは seed 付きで保存された実出力の引用である。
  旧表現は解析側が受け続けるので、記録はそのまま再現できる

**`埋める` は既に「面を要素で埋める」の意味を持つ**（Stage 2 の密度規則に「密集/埋める=120〜350」がある）。
決定的な層は `背景を` で判定するので機械的な誤りは起きないが、**LLM が密度指示と読む余地は残る**。
緩和として Stage 2 の背景規則へ「この文は面を要素で埋める指示ではないので、密度・数量の規則を当てない」を
日英とも明記した。**LLM は一度も回していないので、効いているかは未確認**。

決定的な検査はすべて緑: server **1424 passed / 31 skipped**、ruff・cli 76・
`npm run check` 0 errors・`lint:i18n` 0 errors・**凍結コーパスは両方バイト一致**。

---

### Android `2.1.2-android.1` — 描画層が render engine 15 へ追随（銀筆・種の allowlist・プロンプト同期・明示個数の免除）（android Build 148090 据え置き、2026-07-27）

**engine 14 から 15 への追随に加え、`hair` → `silverpoint` の改名、server プロンプトの複製の同期、明示個数の免除までを 1 契約でまとめた。** これで Android は server と同じ render engine 15 を名乗る。テストは **71 件（うち失敗 20）→ 89 件 / 失敗 0**。

- **段 1 seed 鍵:** manifest の `vary_seed` を `composition_seed` へ。契約は 1 か所を名指ししていたが、**実際に読んでいた test は 3 ファイル**あった（Phase3a / 3b / 3d）。`WebDdlExpander` の引数名 `varySeed` と salt `#vary` は凍結対象なので不変。
- **段 2 engine 15:** 印の種を allowlist（`color` / `color_hint` / `at` / `relation` を落とし `arrangement` は `jitter` だけ）、`cloudform` を手描き経路へ、角のある図形と `pen` に材質層、**強さは距離ではない**（`OUTLINE_OFFSET_GAIN` 2.8 → 1.0、下限 0.0035 → 0.0）。**変更 1 だけで失敗 19 → 11**。
- **地の種は移していない** — Kotlin に canvas ground の描画経路が存在せず、`_texture_seed` に対応する行が 1 つも無い。**移す先が無いので実施しなかったと記録する**。
- **段 3 銀筆:** 契約の 22 箇所に加え **2 箇所**（埋め込みスキーマの enum と description、線幅表のキー）。保存済みデータの置換は `ServerScoreCompat.kt` を新設し、**coerce 側と renderer 側の両方から呼ぶ** — server は pydantic の validator が一点を押さえるが、Android には対応する一点が無いため。
- **段 4 プロンプトの番人:** 4 定数を丸ごと差し替え、SHA-256 を `PromptFingerprintTest` で固定。**「manifest にある全ての名前が検査対象に入っているか」も別テストで見る**（手で並べた一覧は追加された prompt を黙って見逃すため）。
- **段 5 明示個数:** `ServerScoreCounts.kt` を新設。**真の障害物は契約が名指しした 3 関数のどれでもなく `temperQuietSymbolicShape` の範囲だった** — server は角のある図形かつ `color_hint` が coerce 自身の捏造を示すときだけ効くが、Kotlin は円・楕円・弧まで含み `color_hint` のゲートが無く、**記述が明示した個数が governor に届く前に 8 個へ切られていた**。server どおりに絞って 42/50 → **50/50**。
- **契約に無かったが直した 7 件:** `burin` / `drypoint` の材質輪郭（server に一度も存在しない）を削除、surface グループの位置を材質輪郭の後ろへ、`performedOutline` の粒が render seed で撒かれていたのを instruction seed へ、粒の広がりの強度（1.8）を発生源へ、直線の粒の個数表を直線専用（18 / 34 / 26）へ、縦負荷判定を server に合わせ、engine 14 を固定していた Kotlin 側の主張 2 つを**凍結参照に合わせて**是正。
- **検査の穴が 2 つ見つかった（実装ではなく検査の側）:** ①角のある図形の材質層は**実装ごと消しても 85/0 の緑**だった（参照コーパス 25 件に triangle も polygon も 0 件）。②粒の摂動も落ちなかった（**個数だけを固定していたので 48 個が全部動いても緑**）。前者は `CornerShapeMaterialLayerTest` を新設、後者は**位置の digest** へ替えて閉じた。
- **摂動は 10 通り実施し、すべて実際に落ちることを確かめてから戻した。** 置換表を恒等にすると 5 件、`outline_offset` 倍率を 2.8 に戻すと 6 件、明示個数の免除を殺すと 3 件。
- **参照コーパスは 1 バイトも変更していない**（`git diff 60d64c3..HEAD -- server_reference/` が空）。判別点は **`05_circle_rotring` 不変 × `12_cloudform_rotring` 可動**で、engine 14 の凍結と `cmp` して確認済み。
- **未対応（報告のみ・裁定が要る）:** 埋め込み Stage 2 tool スキーマが server と **237 行**乖離（`canvas` の地・`background` の旧表現・`weight` の説明。**地を Stage 2 に見せると Android が描けない支持体を要求されうる**）、server の temper 3 つのうち Kotlin は 1 つだけ（**個数には影響しない**ことを確認済み）、`surfaceSeed` が全 dump でなく allowlist 版を使っている、`renderHash` の engine 版フォールバックが server に無い既定値である、UI の「render engine」表示が **`1` のまま**。
- **`server` / `web` / `cli` / `shared` は 1 バイトも変更していない。** `ANDROID_SPEC.ja.md` / `ANDROID_SPEC.md` は未追随のまま。
- **検証:** 実装セッションの実測で `testDebugUnitTest --rerun-tasks` が **89 件 / failures 0 / errors 0 / skipped 0**（20 XML）。**受け入れの再テストは作者裁定により行っていない**ため、git 管理セッションでは Android のテストを回していない。`android/BUILD_NUMBER` はパッケージを作る task でしか自動採番されないので **148090 のまま**。

---

### v2.9.1 — 推測しない規則でモデルを解く（Ollama Cloud を別 provider に）（Build 731、2026-07-27）

**モデル参照の解決から推測を全部外した。** これまでは修飾のない文字列を「`/` があれば NVIDIA」「`gemini-` で始まれば Gemini」「それ以外は OVMS」で当てていた。**当たっているうちは見えなかったが、OVMS の接続先が止まった今、同じ経路は静かな失敗になる**（`/health` は 200 を返すので描画するまで気付かない）。

**規則は 3 段で、推測の枝は無い。**

1. **明示修飾**（`ollama-cloud:gemma4:31b`）→ それを使う
2. **一意所有** — 設定済み provider のうち、その model ID を持つものが**ちょうど 1 つ**ならそれ
3. **段の既定**（`stage1_provider` / `stage2_provider`）

- **規則 2 が「ちょうど 1 つ」であることが肝。** `gpt-oss:20b` は **`ollama` と `ollama-cloud` の両方に在る**ので**決めない**。決めないものは規則 3 へ落ちる。**この曖昧性は Ollama Cloud を provider として足した時点で原理的に生じたもの**で、それ以前は「修飾なしでも大体当たる」で済んでいた。
- **分割点が一意である根拠**: provider ID は `[a-z0-9_-]` しか通さないので `:` を含み得ない。**最初の `:` が唯一の分割点**であり、モデル ID 側は `:` を何個持ってもよい（`qwen3.5:4b-q4_K_M`）。
- **削除した 4 枝**: `anthropic:`（前段で拾われるので**一度も実行されない死んだ枝**だった）・`gemini-` 接頭辞・`/` → NVIDIA・**既定の落とし先 `ovms`**。
- **web の `providerOfModel` からも `gpt-oss:` の名指し特例と provider ID の直書き 6 行を削除した。****規則がモデル名を名指しで例外にしている時点で破綻の印**である。

**Ollama Cloud を別 provider として設定に載せた**（`https://ollama.com/v1`・鍵必須・検証済み 18 本）。**律速は容量でなく同時実行**（同時 8 で 429。その時点で無料枠は 7.6% しか使っていない）なので、**provider 単位の同時実行上限**を新設し、provider 定義から読む（**利用者が上げられる設定にはしない。429 を返すのは provider の性質であって好みではない**）。外部送信であることと cloud / local の別は 18 本すべての日英コメントに書いてツールチップへ出す。

**設定の形を対へ一本化した。** `okugaki_model` と デモの `prompt_model` は単一の修飾文字列だったが、`stage1_provider` + `stage1_model` と同じ**対**にした。**旧い単一文字列は読み取りで受けて割る**ので、古いクライアントが送っても壊れない。

**挙動が変わるのは 1 箇所だけ** — **どのカタログにも無い素の文字列**。`my-model` は今まで `ovms` へ、`meta/llama-x` は NVIDIA へ落ちていたが、これからは**段の provider** へ行く。カタログに在るものは規則 2 が拾うので答えは変わらない（理由が「`/` を含むから」から「設定に在るから」に変わるだけ）。

**検査面**

- **期待値表は `web/scripts/model-ref-expectations.json` が唯一の正本**で、**server の pytest と node の `npm run lint:models` が同じファイルを読む**。「日英 2 実装が同じ答えを返す」ことが、たまたま一致している 2 つの表ではなく**仕組みの性質**になる。表がカタログと食い違ったら赤くなる検査も置いた。
- **web にテストランナーは足していない**（vitest も `*.test.ts` も 0 のまま）。**Node が `.ts` を型剥がしで直接読めるので、`i18n-lint.mjs` と同じ素の node スクリプト**にした。
- **摂動して赤くなることを確かめた**: 規則 2 を「ちょうど 1 つ」から「1 つ以上」へ緩めると Python 4 件・node 3 件が落ちる。**JS で `indexOf`/`slice` を `split(':', 1)` に戻すと 56 件中 10 件が落ちる**（**Python の `split(':', 1)` は 2 要素、JS は 1 要素で後半が消える**。同じ規則を両言語へ写すときの最大の罠）。

**測って分かったこと（直していない）**

- **規則 3 は保存済みの段設定に届いていない。** `provider_for_model` に渡る settings は**カタログ側の dict** で `stage1_provider` を持たないため、実運用では常に既定（`nvidia`）へ落ちる。**本契約より前からある別のずれ**なので範囲外とし、呼び出し側が段の鍵を渡せば効くことをテストで固定した。
- **`normalize_model_settings` は 1 回 約 5.0ms。`/api/paint` 1 回あたり 2 回**で、**変更前は 4 回だった**（同じ判定の別実装が二重に正規化していた）。**差し引き呼び出し回数は減っている。**
- `api.py` の `_resolved_stage_model` / `_resolved_vision_model` は「要求が現在の設定と同じ文字列なら修飾する」という推測を続けている。下流が新規則で受けるので害は無いが、**同じ文字列かどうかで分岐する理由はもう無い**。

**検証:** server **1487 passed / 31 skipped**（新規 56 件）、cli 76、ruff clean、`npm run check` 0 errors、`lint:i18n` 788 / 36 / 0、**`npm run lint:models` 56 checks passed**。**作者裁定により受け入れの再テストは行っていない**（数値は実装セッションの実測）。**Score にも Renderer にも触れていないので凍結コーパスの再生成は不要**。Android は追随していない。

---

### v2.9.2 — モデル名は、それを走らせる場所とともに名指す（Build 732、2026-07-27）

**モデル名を出すところでは、必ずプロバイダ名を併記するようにした。** `モデル ID だけでは「どこで走るのか」を言っていない`のに加えて、**v2.9.1 で `gpt-oss:20b` が `ollama` と `ollama-cloud` の両方に在ることが確定した**ので、**モデル名は一意ですらない**。

**表示は `プロバイダ名 / モデル名`**（作者裁定・2026-07-27）。

| 場所 | 前 | 後 |
|---|---|---|
| 描画中の 1 行・作品情報の Stage 1/2・次に使うモデル・バッチ | `Google Gemma 4 31B Instruct` | **`NVIDIA NIM / Google Gemma 4 31B Instruct`** |
| 履歴テーブルの Stage1 / Stage2 列 | `gemma`（8 文字に潰す） | **`NVIDIA NIM/gemma`**（潰す規則はそのまま、プロバイダ名を前へ） |
| 系譜ノードの詳細 | **保存された生の文字列**（`nvidia:google/…` がそのまま出ていた） | `NVIDIA NIM / Google Gemma 4 31B Instruct` |

- **`models.ts` に `providerLabel()` と `modelDisplayName()` を足し、表示はすべてここを通す**。**プロバイダとモデルの表示名の登録簿は `registerModelCatalog()` が育てる**ので、利用者が足したプロバイダの表示名も出る（v2.9.1 で入れた仕組みをそのまま使う）。
- **モデル選択カードは対象外** — カードは**プロバイダ見出しの下に並んでいて文脈がある**。**モデル比較の選択欄は元から `label · providerLabel` を出していた**。
- **CLI は元から併記していた**（`Stage1 provider:` / `Stage1 model:`）ので変更なし。
- **`statusModelName` は `modelDisplayName` へ置き換え、`shortModel` は短縮規則を素のモデル ID 側へ移した**（前はプロバイダ接頭辞ごと 8 文字に切っていた）。

**検証:** `npm run check` 0 errors / 2 warnings / 217 files、`npm run lint:i18n` 788 / 36 / 0、`npm run lint:models` 56 checks passed。**server・cli・描画には触れていない**ので pytest と凍結コーパスは対象外。

**記録（この改修で判ったが直していない）**: **web の静的フォールバックカタログは `ollama-cloud` を 3 本しか持たない**（server は 18 本）。そのためサーバーのカタログが届く前の一瞬だけ、`gpt-oss:20b` が一意所有と見えて `Ollama` と表示される。**カタログ登録後は曖昧になり段のプロバイダへ落ちる**。v2.9.1 からある性質で、表示規則の側の問題ではない。

### 2026-07-29 — ドキュメントを三層へ組み直し、日英を同じ内容にした（**採番なし**・ドキュメントのみ）

**読む層・仕様の層・書庫の層に分け、英語 SPEC を日本語の節構成へ作り直した。** 台帳 [I-032] の全段。

- **検査を新設した。** `server/scripts/check_docs.py` が **①日英の骨格**（文書ごとに `shape` / `sections` / `entries` の 3 段階・**13 対**）、**②相対リンクの飛び先**、**③公開文書が名指しする非公開パス**（凍結）を見る。**宣言の無い差分は赤**になる
- **書庫へ出した。** CHANGELOG は現行 + 2 書庫へ、engine の版史は `docs/spec/render-engine-history` へ。`SPEC.ja` は 2253 → **1836 行**
- **README を短くした。** `README.ja.md` 539 → 295 行、`README.md` 585 → 318 行。Quick Start が 74% 地点から 33% / 38% 地点へ上がった。**外へ出した 3 対は `docs/guide/`**（`gallery` / `how-it-works` / `revision`）
- **英語 SPEC を日本語の節構成へ組み直した。** 節番号を 1 対 1 に揃えたうえで、§13 → §4・§8・§9 → §7・§14 → §10 と §5・§6 → §12 の順に書き起こした。**§12 は 4 小節から 13 小節へ**。`SPEC.md` は 1269 → **2907 行**
- **英語 CHANGELOG の欠落 76 版を遡って訳した。** 書庫 1 は英語版が存在しなかったので新設（**73 版・2169 行**・見出しの並びまで日本語と一致）、書庫 2 は v1.85 / v1.86 / v1.86.1 を足した

**分かったこと 3 件（次の日英作業に効く）**

- **文書の対応を h2 の題で測ると間違える。** 英語 §12 は "Security and Operations" という題だったが、**本当に運用なのは冒頭 51 行だけ**で、残り 504 行は日本語 §15.4〜15.12 と h3・h4 とも 1 対 1 だった。**題は内容より古くなる**
- **節を英語化した段は、同じ内容の古い英語を他節に残す。** 英語 §12 の Renderer 小節 186 行のうち **約 110 行が §13.11 / §13.8 / §8.4 / §4.7 との重複**だった。**削除の前に識別子の集合を突き合わせ、旧節の全段落を新文書へ機械照合した** — 17 件が浮き、うち **2 件は本当に落ちる英語独自項目**だった
- **「英語版が無い」という宣言を信じる前に、他の文書を grep する。** v1.85 / v1.86 / v1.86.1 は**英語本文が `SPEC.md` の「洗練の会計」にあり、日本語 SPEC には無かった**＝日英で置き場が食い違っていた。**v1.85 の中に埋まっていた GLOSSARY / `lint:i18n` の方針は SPEC 内で唯一の記述**だったので、書庫へ落とさず §7.8 へ移した

**日本語は README 以外ほぼ動かしていない**（`SPEC.ja.md` は段 4c を通じて 1 行も変えていない）。**残る宣言は 2 件** — 英語 §15 / §17 は写さない裁定、§18 以降の運用節は英語だけで成立する。**`check_docs.py` 緑、server 1487 passed / 31 skipped、cli 76 passed、ruff 緑**。

### v2.9.3 — 面を痕跡にし、太さを軸にする（render engine 16）（Build 754、2026-07-29）

**描画の版を 15 から 16 へ上げた。** 3 つの変更を 1 つの版にまとめてある — どれも同じ層に属し、版を 3 回上げれば Android の追随が 3 回要るため。台帳 [I-001] の全段。

**コーパス 365 件のうち 333 件が動き、不変は 32 件。不変の 32 件は全部 `rotring` と `computer`** で、engine 12 の 12 件・engine 15 の 32 件に続いて **3 版連続で同じ側に立っている**。文法がすべてゼロで種を消費しないので、**手を変える軸は、手を持たない道具に届かない**。

#### 面は塗るのでなく演奏する

**8 つのてざわり語のうち 6 つは、図形の外接矩形に一様乱数で円を撒いていた。図形そのものを一度も見ていない。**

| 図形 | 粒が図形の外に落ちていた割合（engine 15） | engine 16 |
|---|---|---|
| 三角 | **46 / 90（51%）** | **0** |
| 雲形 | **43 / 90（48%）** | **0** |
| 多角形 | 20 / 90（22%） | **0** |
| 円 | 12 / 90（13%） | **0** |

- **輪郭の内側へ位置を決め、粒 1 つを 1 筆として演奏する**。走査は `_render_fill_strokes` と同じ `_scanline_segments` を使うので、凹形（雲形）もそのまま扱える
- **`bleed` は bbox 中心の楕円 1 個だった** — 図形が何であれ同じ絵だった。輪郭を外へ押し出した帯 3 重にし、**最内輪は輪郭の上（オフセット 0）に置いた**。滲みは縁の両側に起こるので、帯が図形から離れた所に浮かない
- **`hatch` / `crosshatch` は 1 バイトも変えていない**。この 2 語は engine 15 の時点で中心線を `synthesize_along` へ通しており、面を撒いていなかった。**この 8 件が不変であることが、変更が「撒いていた 6 語だけ」に閉じていることの担保になる**
- **同じ語がプロファイルごとに無関係な 2 つの絵になっていた** — display は `feTurbulence` + `feDisplacementMap` + `feGaussianBlur` をかけた矩形、editable は円の散布だった。**両プロファイルとも同じ機構へ揃え、display の clipPath も廃止した**（`bleed` は外へ染み出すので clip はむしろ描いたものを消す）
- **速度は 1.44 倍**（面を持つ本番作品 119 件・display で 56.2 s → 80.9 s）。90 個の円を 90 本の合成ストロークに置き換えたぶん

#### 微小な塗りは置く

走査線が引けない小ささの塗りは**領域 fill へ縮退**していた。**縮退は失敗を防いでいたのであって、正しかったわけではない** — 手の道具で塗った小さな図形が、そこだけ機械の塗りになっていた。

- **1 筆の打点として置く**。図形の長い方の軸に沿って運び、幅は短い方の軸が決める
- **機構が切り替わる境界は、短辺がキャンバスの約 3%**（実測 2.9〜3.2%・道具 5 種 × seed 6 種。**切り替わりは 1 度きりで行きつ戻りつしない**）
- **運びの下限 0.90 を実測で選んだ。** 0.30 では `_edge_window` が両端の 16% ずつで幅を 0 まで落とすため、**10px の塗り円が「輪郭だけになって内部が空く」**。1.10 では打点が図形より濃くなる（墨量 115%）
- **`rotring` はどの大きさでも領域 fill のまま**（`_uses_hand_stroke` の手前で分岐する）
- 本番の手の道具の `filled` 閉図形のうち **75.3% が打点へ回る**（150 作品の実測）

#### 太さは道具名から独立した軸になる

**太さは道具名の性質だった。** 細い線を求めることは別の道具を求めることであり、「細いペン」は書けなかった。`Instruction` に `thinness`（`fine` / `extra_fine`）を置いた。

| 道具 | 既定 | `fine` | `extra_fine` |
|---|---|---|---|
| silverpoint | 0.5 | 0.5 | 0.5 |
| rotring | 1.0 | 0.6 | 0.5 |
| pencil | 1.5 | 0.9 | 0.525 |
| pen / computer | 2.0 | 1.2 | 0.7 |
| drypoint | 2.6 | 1.56 | 0.91 |
| chalk / brush_thin | 3.0 | 1.8 | 1.05 |
| burin | 3.2 | 1.92 | 1.12 |
| crayon | 4.0 | 2.4 | 1.4 |
| brush_thick | 8.0 | 4.8 | 2.8 |

- **下限は新しい数ではなく最も細い道具そのもの**（`MIN_STROKE_WIDTH = WEIGHT_TO_STROKE_WIDTH["silverpoint"]`）。「銀筆より細い線は引けない」と読める。**銀筆は太さの指定を受け付けない。これは仕様であって欠落ではない**
- **倍率は 3 案を実際に描いて墨の被覆率で選んだ。** **退けた 0.7 / 0.45 は太筆の `fine` が既定の 99%** で描いても変わらない。**退けた 0.5 / 0.25 は道具の区別が潰れる**（`extra_fine` で 11 道具の相異なる幅が 9 → 6 に落ちる。**太さの軸が道具の軸を食う**）。**採った 0.6 / 0.35 は 9 → 8**（銀筆とロットリングだけが合流する）
- **材質輪郭にも太さを通した。** `base_width` を公称幅に据え置くと**墨だけが細って材質が取り残される**。比例項を持つのは太筆とクレヨンの 2 道具だけで、残りは絶対幅なので細さで動かない（**細いペンの線には、太さの変わらない材質の帯が付く**）。**距離（offset）は動かしていない**
- **演奏 seed の allowlist に入れた**（19 → 20）。その帰結として**太さを変えると線の通り道も変わり、銀筆は幅が変わらないのに手だけ変わる**
- **coerce が添える線には付けない。** 添景は coerce の声であって記述者の要求ではないので、**記述された細さは記述者が書いた図形にだけ乗る**
- **`thinness` は歳時記の語ではない**（作者裁定・2026-07-29）。Stage 1 は太さ語を読んで正規化DDL へ書くが、語彙表と歳時記の表示には出ない

#### 版

- **`render_engine_version` 15 → 16**、参照コーパス `render-engine-16/` を凍結（365 件・収録 SVG 333 枚）
- **`ddl_engine_version` 1 → 2** — **DDL 層の振る舞いは変わらないまま、instruction の dump に `"thinness": null` の 1 行が増えた**。凍結済みディレクトリは書き換えない規約に従い `ddl-engine-2/`（29 件）を新規に焼いた（`ddl-engine-1/` は 1 バイトも触っていない）
- **`ddl_version` 1 → 2**（作者裁定・2026-07-29） — **DDL の語彙が増えた**。「極細の黒い線」はそれまで DDL に書けなかった文である。**保存済みの作品は `"1"` のまま残る**

**検証:** server **1596 passed / 31 skipped**、cli **76 passed**、ruff 緑（`src tests scripts`）、`check_frozen_corpora.py` **2 回連続でバイト一致**、`npm run check` 0 errors / 2 warnings / 217 files、`check_docs.py` 緑。**受け入れで 3 段それぞれの核へ摂動を当て、輪郭を bbox へ戻すと S-3 が 4 件、太さの倍率を恒等にすると T-1 系が 13 件、打点への分岐を削ると F 系が 15 件、それぞれ赤になることを確かめた。**

**記録（判ったが直していない）**: **Stage 2 が `thinness` を埋める率は実測 10%** で、設計時の観測 96% は再現しなかった。決定的な層（スキーマ・プロンプト・coerce・Renderer）はすべて緑なので、**運ばれるかどうかは LLM の層の問題として残る**（台帳 [I-036]）。**Android はまだ engine 15 のままである**（[I-029]。プロンプト 4 定数の指紋は焼き直してあるので Kotlin 側は赤い＝意図した検出）。

### v2.9.4 — 生成情報は行の意味を言い、指示書は名前で呼ばれる（UI 調整 4 巡目）（Build 755、2026-07-29）

**作者の指示 32 件を Build 733〜753 で消化した。** 台帳 [I-002] の全段。**Build 749 は engine 16 が同じ採番器から先に取ったので、この巡は 748 → 750 と飛んでいる。**

#### 生成情報ドロワーの詳細タブ — 19 行 → 38 行

`HistoryItem`（`api.py`）と `PaintResponse` の全フィールドを詳細タブの 19 行と突き合わせ、**表示していなかった 18 属性を足した**（作者裁定「A 群 + B 群すべて」）。**全 38 行の見出しに tooltip を付け、5 つの小見出しで区切った**（解釈 / 演奏 / 同一性 / 由来 / 実行）。

- **`render_wild` は 3 状態である** — `null` は「この列を持つ前に保存された作品」で OFF と区別する。**`記録なし` と表示する**
- 足した属性: `seed_text` / `variation_amplitude` / `variation_seed` / `focus` / `interpret_fallback`（従来はキャンバス上のバッジのみ）／プロンプト digest 3 種 / `instruction_lang_requested`（従来は解決後の言語だけ）／`note` / `lineage_generation` / `derivation_kind` / `batch_run_id` / `batch_line_number` / `ui_lang` / `render_color_catalog_sub` / `render_canvas_aspect_ratio`
- **「由来」群は 4 項目とも無い作品では群ごと出さない**
- **ドロワーはウインドウ外クリックでも閉じる**（既存の Escape は不変）
- **`derivation.ts` を新設**して `LineagePanel` にあった派生種別の対応表をそこへ移し、`CanvasPanel` と型ごと共有した
- **見送った属性は 13**（色の実マップ・キャプションで既出のもの・プロンプトタブにあるもの・サーバー内部パス・系譜タブにある ID 群・★ボタンで表す `starred`）

#### 指示書という名前

**利用者が編集する対象を指す 9 キーを「指示書」へ統一した**（`DDLを編集` → `指示書を編集` ほか）。**技術的な表記は残す** — 来歴の `DDL 仕様`・`正規化DDL`・`Stage 2 ユーザー入力 (正規化DDL)`・`元にする作品の構成 (DDL)` はそのまま。ガイドの題は「指示書（DDL）簡易ガイド」とし、DDL の定義文を添えた。

- **記述タブの指示書ボックスの右下に「指示書から描画」ボタン**を置いた。既存の `replay()`（`/api/compose` = Stage 2 のみ）を結線しただけで、新しい描画経路も新しい派生種別も作っていない。DDL が空か実行中なら押せない
- そのボタンの下に**状況表示と停止ボタン**を出す（`InputPanel` 側は `hideRunStatus` で二重に出さない）
- **各モーダルの描画ボタンを `--action-*` の黒塗りへ揃えた**（accent 塗りだった 5 箇所）。**▶ は付けない**

#### ダークが既定になった

**新規ユーザーの `ui_theme` 既定と、未ログイン画面の初期値を `dark` へ**（作者裁定で既存ユーザーの一斉変更はしない）。**`AuthPanel` はライトの値を直書きした 1 組しか持っていなかった**ので、直書きをローカルトークンにして `html[data-theme='dark']` に暗い 1 組を置いた。**背景の作品（`/login-background.svg`）は反転していない** — 白い紙に墨の線という素材で、`--canvas-paper` がダークでも紙のままなのと同じ規律である。

#### 系譜タブ

- **カードから星を付け外しできる**。`updateHistoryStarState` が `lineageGraph` も更新するようにした（従来は 3 経路しか更新しておらず、系譜タブに星を足しても画面が変わらなかった）
- **ダブルクリックで作品タブへ遷移**（シングルは選択のまま）。**ダブルクリックはシングルのハンドラを 2 回通す**ので、`openNode` に「選択中の作品なら取り直さない」ガードを足した
- **起点からスター付きノードへ至る辺をオレンジで描く**（矢じりも同色）

#### マスコット

**立方体の Incu と蟹の Yuragi の 2 つに決着した**（設定はチェックボックス 2 つ → ラジオ 2 つ）。`KiwiMascot` / `CrabMascot` と、死んでいた localStorage 3 キー・i18n 3 キーを削除した。**選択は prop で貫かず、言語パックと同じモジュール状態にした** — `RunStatus` は 10 箇所から呼ばれるので、prop にすると 10 経路に同じ引数が増える。

#### 履歴管理モーダルの説明

**切る箱を先に数えた** — `Tooltip` の吹き出しは `position: absolute` なので祖先の `overflow` で切られる。`.history-modal` / `.settings-tabs` ×2 / 一覧 3 つが該当する。

- **切る箱の中は native `title`**（ブラウザ自身の tooltip はウインドウにだけ従う）、**外は `Tooltip` ですべて下向き**
- **ラベルの言い換えでしかなかった `title` 2 件**を動作の説明に差し替えた
- **タブ 4 つを吹き出しに揃えるには `.settings-tabs` の `overflow: hidden` を外す必要がある**（角丸を各ボタンへ移すセグメント表示の作り直し）。**未裁定として残した**

#### そのほか

info モーダルのバージョン・ビルド日時を先頭へ（**日時は `BUILD_NUMBER` の mtime を `__BUILD_DATE__` として `vite.config.ts` が define で注入する**。別ファイルは持たない）／`inku` を押せる見た目のボタンへ／バッチの記述ボックスの高さを外枠へ移して表示崩れを直した／バッチとデモにラベルを追加しデモの並びを組み替えた／デモの共通設定をデモ自身の 2 項目の下へ。

#### 用語辞書の例外を増やした（作者承認）

**`prompt` を 6 キー・`generat` を 2 キー**、`GLOSSARY.md` §5-2 と `i18n-lint.mjs` の許容リストへ同一 commit で足した。**プロンプト digest はプロンプトそのものの指紋**なので、辞書の言い換え（記述 = description）では意味が変わる。**`generat` は辞書 §2「世代の意味のときだけ使ってよい」に該当する。**

**検証:** `npm run check` **0 errors / 2 warnings / 218 files**、`npm run lint:i18n` **877 / 44 / 0 / 0**、`npm run lint:models` 56、server **1596 passed / 31 skipped**、cli **76**、ruff 緑。**描画には触れていない**ので凍結コーパスは対象外。

**受け入れで直したもの（実装セッションの報告に無い）**: **`test_current_user_theme_can_be_updated` が判別力を失っていた。** Build 744 が既定を dark にしたとき、テストの `"light"` を機械的に `"dark"` へ置き換えたため、**既定 dark に dark を PATCH する形**になり、**`update_user_settings` の `row.ui_theme = ui_theme` を潰しても緑のまま通る**（実測）。**既定でない側（light）を要求してから読み戻す形へ直し、摂動で赤くなることを確かめた。**

### 2026-07-29 — 版史は、その版の時点の名前で書く（**採番なし**・ドキュメントのみ）

**版を刻印した節は、その版の時点の名前と材料で書く**（2026-07-29 作者裁定）。**現在の姿は SPEC が持ち、版史は起点を持つ**という切り分けである。

食い違っていたのは `docs/spec/render-engine-history` の **§版と同一性 ID（v2.4.5）**で、**英語だけがこの節を 2 度「現在」へ更新していた**。日本語は v2.4.5 時点の記録として内部整合していたので、**日本語のこの節は 1 行も動かしていない**。

- **英語の `composition_seed` を `vary_seed` へ戻した**（改名は v2.8.0。v2.4.5 の時点では `vary_seed` だった）
- **英語の同一性リストから `render_wild` を落とした**。材料に入ったのは **engine 12（v2.5.0）**で、v2.4.5 の節に書かれていたのは年代の取り違えである
- 落とした事実は消さず、**日英の engine 12 節へ「`rh3` の材料に `render_wild` を加えた。形式名は `rh3` のまま据え置く」を 1 行ずつ足した**（年代の正しい場所へ移した）
- **`vary_seed` は死んだ名前ではない** — **旧形式 `rh2` の材料の鍵名としては凍結されており現役である**（`SPEC.ja.md` §4.7）。一括置換をかけると保存済み作品の `rh2` の記述まで巻き込む
- **どの番人もこれを捕まえられない**。`check_docs.py` のこの対は **`shape`（見出しの骨格）**なので本文中の 1 語は永久に赤くならず、`npm run lint:i18n` は web の表示文字列しか見ない。**v2.8.0 の改名から今日まで残ったのはそのためである**

**検証:** `check_docs.py` 緑（宣言済みの差分 2 件のまま・内部参照 56）。**動くコードは 0 行**なので採番しない。

---

### Android `2.1.3-android.1` — 描画層が render engine 16 へ追随（太さの軸・微小な塗り・版の宣言）（android Build 148090 据え置き、2026-07-29）

**engine 16 のうち server と共有している部分を移植し、Android も render engine 16 を名乗る。** テストは **89 件 / 失敗 15 → 99 件 / 失敗 0**。

- **段 1 seed:** `serverInstructionJson` と `surfaceSeed` の**両方**の dump へ、`weight` の直後に `thinness` を足した。**seed の材料は Kotlin に 2 箇所ある**ので、1 箇所だけでは面の検査が赤で残る。`CornerShapeMaterialLayerTest` の期待値は engine 16 へ採り直した
- **段 2 プロンプト:** server の 4 定数を丸ごと同期した（`STAGE1_*` が 18,945 / 17,932 バイト、`STAGE2_*` が 43,822 / 41,887 バイト）。`*_LITERT` は対象外
- **段 3 太さ:** schema・coercer・線幅・材質輪郭へ `thinness` を通した。倍率は `null=1.0` / `fine=0.6` / `extra_fine=0.35`、**下限は新しい定数ではなく道具表の最小値（銀筆の 0.5）から導く**。材質輪郭は細めた幅を使い、**offset は動かさない**。**coercer は与えられた値を保存するだけで、DDL 文からは作らない**
- **段 4 微小な塗り:** 走査線が 3 本未満で `renderFillStrokes` が `null` を返したときに限り、`fill-dab-v1` を **1 本の path** で置く。`rotring` は領域 fill のまま
- **段 5 版:** renderer metadata と render-hash fallback の 2 箇所を `"16"` にした。engine version としての `"15"` は **0 件**
- **面の質感 6 つ（`stipple` / `grain` / `paper_grain` / `wash` / `aquatint` / `bleed`）は実装していない。** 作者裁定 `D-20260729-android-declares-the-shared-part` により範囲外で、**部分実装のまま版を宣言する**。レポートにその 1 行がある
- **受け入れではレポートの数値を引き写さず、再現と摂動を行った。** 全 99 件を回して 0 failures を確認し、**段の数だけ 5 つの摂動**を当てて、契約が名指しした assertion がそれぞれ赤くなることを見た。`surfaceSeed` の 1 行削除 → `test06SurfaceHatchExactParity`、日本語 Stage 1 プロンプトへ 1 文字 → `PromptFingerprintTest`、線幅の下限除去 → `ServerRendererThinnessTest`、走査線の境界 3 → 4 → `DefaultSvgRendererFillDabTest`、版を `"15"` へ戻す → `Engine16VersionTest`
- **レポートより判別力が 1 段強かった箇所が 2 つある** — 摂動 1 と 3 は端から端までの参照 SVG 照合（`testEveryReferenceSvgMatchesOnPathsPointsAndDashes`）も赤にした。レポートはどちらも「1 件赤」と書いている
- **`CornerShapeMaterialLayerTest` の期待値が焼いた参照 SVG 由来であることを、受け入れ側で SVG から digest を再計算して確かめた** — `31_triangle_pencil` は `contour-stroke-v1 controls-64 events-2` / 材質輪郭 2 / 粒 48 / digest `6fcd7fdf…` と `ecaf7129…`、`32_polygon_brush_thin` は `controls-102 events-1` / 材質輪郭 2 / 粒 0 / digest `1d646f6c…`。**4 つとも実装の出力ではなく凍結 SVG から再現し、テストの期待値と一致した**
- **判別点は `01_circle_pen` 可動 × `05_circle_rotring` 不変**（engine 15 の凍結と比べて `0202eec…` → `b79faee…`、`60e774d…` は不変）。**機械の極が動かないのは 3 版連続**である
- **`fill_dab_group` の 10 ケースだけは座標を `1e-5` 以内で比べている** — fixture が contour を小数 6 桁へ丸めて保存しており、保存済み contour を再演奏すると最終桁が 1e-6 だけずれるため（例: `495.275170` 対 `495.275171`）。**端から端までの凍結 SVG 照合は文字列 exact のまま**で、`26_tinyfill_circle_pen` に `fill-dab-v1` が出ることを含め全件緑
- **`server/` と `server_reference/` は 1 バイトも変更していない**（`git diff 0350cc0..HEAD` が両方とも空）
- **採番は `android/VERSION` だけ。** `APP_VERSION`（`v2.9.4`）・`web/BUILD_NUMBER`（755）・`android/BUILD_NUMBER`（148090）はいずれも不動で、**pentala 反映も不要**
- **検証:** Android **99 件 / failures 0 / errors 0 / skipped 0**（23 XML）。マージ後の主 checkout で server **1596 passed / 31 skipped**、cli **76**、ruff（`src tests scripts`）緑、`npm run check` **0 errors / 2 warnings / 218 files**、`check_frozen_corpora.py` はバイト一致
- **残り:** `ANDROID_SPEC.ja.md` / `.md` は **engine 15 にも未追随**（[I-013]）。本契約の範囲外

---

### 2026-07-29 — 宣言順は仕様である（**採番なし**・ドキュメントのみ）

**2026-07-29 の作者裁定 2 件（[I-037] / [I-040]）を文書へ落とした。** 裁定は engine 改訂の設計議論セッションで下されたが、**設計側は台帳へ書き込めないため伝達だけが残っていた**（記録は設計ハンドオフ §2.2 の表のみで、`DECIDED.ja.md` には入っていなかった）。

- **事実は SPEC へ**（`SPEC.ja.md` §5.1 → `SPEC.md` の対応節）: **Stage 2 の tool schema はプロパティの並び順ごと LLM へ渡り、任意フィールドの充填率は宣言位置に単調に従属する**。`Instruction.thinness` だけを 5 点振った実測で **先頭 0% / 14 番目（render engine 16 の宣言位置）18% / 19 番目 48% / 22 番目 83% / 末尾 89%**（異なり入力 25 件・同じ Stage 1 出力・同じ Stage 2 プロンプト・`nvidia:google/gemma-4-31b-it`。全 5 群がそろった 21 件で対応をとった集計）。**先頭が 0% なので「意味の近い語の隣にあるのが悪い」のではなく、後ろにあるほど埋まる**
- **規則は版史へ**（`docs/spec/render-engine-history` 日英の「版を上げる条件」）: **DDL 変換層（`DDL_ENGINE_VERSION`）はもう一つの条件を持つ＝ `Instruction` のフィールド宣言順を変えたとき**。**振る舞いを 1 行も変えていなくても Score の分布が変わり、絵が変わる**
- **書き先が 2 文書に分かれたのは、2026-07-28 の再編で版数規則の正本が SPEC から版史へ移っていたため**（`SPEC.ja.md` が「そちらが正本である」と明示している）。作者の裁定は「SPEC に書く」だったので、**事実と規則を分けて両方を満たした**
- **この条件だけは凍結コーパスで捕まえられない** — コーパスは Score を固定して演奏を見る検査なので、**Score そのものの分布が動く変化を 1 件も動かさない**
- 実行規約（非公開）へ 2 項を足した: **照合で鍵を並べ替えない**／**並べ替えたら投げる前に位置を assert する**。**同じ罠に 2 セッションが独立に落ちている** — `json.dumps(..., sort_keys=True)` での照合が「内容一致・並び順違い」を「バイト一致」と誤報告し、その並びこそが原因だった
- **`thinness` を実際に末尾へ動かすのは別の契約**（`thinness-declaration-position.md` 段 1。**未着手**）。本節は文書だけで、**動くコードは 0 行**

**検証:** `check_docs.py` 緑（宣言済みの差分 2 件のまま・内部参照 56）。

---

### 2026-07-29 — README の導入を書き直し、英語を追随させた（**採番なし**・ドキュメントのみ）

**作者が `README.ja.md` の導入 3 段落と「しくみ」節を書き直した**（美術館での発端、最初の一文との付き合い方、DDL には意味も感情も実装されないこと、仕様が進めば昔と同じ絵は返らないこと）。本節はその追随と是正の記録である。

- **英語 README は、更新後の日本語から訳し直した**（英語を英語のまま書き足していない）。食い違っていたのは **4 段落**
- **英語 README が揺らぎを `variation` と書いていた。** 辞書は **揺らぎ = `sway`**、**`variation` は変奏（Stage 1.5 の振り）だけに予約**している。**5 箇所を `sway` へ替え、変奏を指す 4 箇所は据え置いた** — 判定は日本語の対応語（ゆらぎ・揺らぎ 対 バリエーション・変奏）で 1 件ずつ切り分けた
- **`npm run lint:i18n` は web の表示文字列しか見ないので、README の違反は何サイクルでも残る。** 2026-07-28 に SPEC で 49 箇所見つかったのと同じ型で、今回も人が引いて見つけた
- 日本語側は誤字 `Rendrer` → `Renderer`、読点で繋がっていた 2 文、英数字まわりの空白 6 箇所、数詞「3つ」→「三つ」（README 内の既存は漢数字）、冗語「LLMモデル」→「LLM」を是正した
- **消えた 2 情報（Stage ごとのモデルの具体例・ギャラリーの seed の説明）は作者裁定で戻さず、英語側も同じく削除して揃えた**

**検証:** `check_docs.py` 緑。**動くコードは 0 行。**

---

### v2.9.5 — 太さは末尾に立って初めて書かれる（DDL engine 3）（Build 763、2026-07-29）

**`Instruction.thinness` の宣言を `weight` の直後（14 番目）から末尾（23 番目）へ移した。** engine 16 で入れた太さの軸は、宣言位置のせいでほとんど絵に出ていなかった。

- **搬送は 18% → 89%**（異なり入力 25 件・`nvidia:google/gemma-4-31b-it`。太さ語を含む 19 件で 25/28 instruction）。**フィールドの中身は 1 文字も変えていない。移したのは位置だけである**
- **`_stage2_prompt_digest` の `sort_keys=True` を外した**（[I-038]）。**指紋が並び順に対して盲目だったので、絵を動かす変更が `history.stage2_prompt_digest` に 1 度も残っていなかった**。日 `32e65db9dcb68e99` → `e11b7daa7c65a5fe`、英 `31d357f591d4cf9b` → `e1eacdb0176f7f98`。**保存済みの値は書き換えない** — 過去の値は過去のスキーマ順を指す値としてそのまま残る
- **判別テストを 4 件足した**（`test_thinness_declaration_position.py`）。**この性質を捕まえるゲートは他に無い** — 凍結コーパスは Score から始まるので Stage 2 を通らず、宣言順を変えても既存の検査は 1 つも赤くならない。実測でも**段 1 だけなら 1596 件が緑のままだった**
- **`DDL_ENGINE_VERSION` 2 → 3。** `DDL_VERSION` は `2` 据え置き（語彙は増えていない）、**render engine も `16` 据え置き**（演奏は 1 バイトも動かない）
- **参照コーパス `ddl-engine-3/` を焼いた。29 ケースは `ddl-engine-2/` とバイト一致で、`changed_from_previous` は空である。** **その空白がこの版の説明である** — コーパスは Score を固定して変換を見る検査なので、**書かれる Score そのものが変わる変化を 1 件も動かさない**
- **`gen_ddl_reference.py` は新しい版を切るたびに全ケースを「動いた」と記録していた。** engine 2 では全 dump が実際に変わったので見えなかったが、この版では manifest が版の意味と逆のことを主張するところだった。**前の版の manifest と突き合わせて決めるようにした**（`gen_render_reference.py` と `server/reference/README.md` の手順 4 が既に定めていたもの）
- **`ddl_engine_version == "2"` を固定していたテストが 5 件赤くなり、採り直した**（`test_api.py` 4 箇所・`test_ddl_reference.py` 1 箇所）

**検証:** server 1600 passed / 31 skipped（+4 = 新規テスト）、cli 76 passed、ruff clean、`npm run check` 0 errors / 2 warnings / 218 files、`npm run lint:i18n` 877 / 44 / 0 / 0、`npm run lint:models` 56、`check_frozen_corpora.py` バイト一致、`check_docs.py` 緑。摂動 2 件（段 1 を戻す・段 2 を戻す）を受け入れ側でも当て直し、それぞれ P-1+P-3・P-3 が赤くなることを実測した。

**Build 番号が飛んだ。** 762 は `feat/ollama-cloud-provider` が先に採っていたため 763 を採った。

---

### v2.9.6 — API キーを持たずに始められるようになった（Build 765、2026-07-29）

**手元の Ollama を provider に選ぶだけで、API キーを 1 つも用意せずに inku を動かせる。** 実測に基づく推奨は **Stage 1 `qwen3.5:4b-q4_K_M` + Stage 2 `ministral-3:8b-instruct-2512-q4_K_M`**（合計 9.4GB・被覆 71%）で、旧・承認済みの `qwen3.5:4b` + `gemma4:e4b`（13.0GB・64%）から乗り換えた。

- **`SETUP.ja.md` / `SETUP.md` に「API キーなしで動かす（ローカル Ollama）」を新設した**（Web UI と CLI の間）。環境変数表へ `OLLAMA_BASE_URL` / `OLLAMA_CONTEXT_LENGTH` を追加。`deploy/compose.yaml` は `api` へ `OLLAMA_BASE_URL` を渡し、`extra_hosts` で `host.docker.internal` を gateway へ写す（**コンテナから見た「手元」はホストではないため**）
- **Ollama のモデル一覧を、実際に計測した 10 本へ差し替えた**（`MODEL_CONFIG_VERSION` 2.2.0 → 2.3.0）。**タグに量子化まで書く** — 素タグは上流で中身が差し替わるので、計測と結びつかなくなる
- **Stage 2 はローカル Ollama へ tool ではなくスキーマで訊く**（`response_format`）。**tool 定義はプロンプトに乗り、Score スキーマが大きすぎて Ollama がプロンプトの 75% を捨てていた**
- **思考を止める**（`reasoning_effort="none"`）。**Ollama は指定が無いと自分で思考を始め、その思考は答えと同じ予算を食う**ので、予算を超えた分だけ何も返らなくなっていた。止めると同じ作業が 8 倍速く、被覆は変わらない
- **速度はリリース表示から外し、デベロッパーモード限定にした**（2026-07-27 裁定の実装形。`speed_developer_only`）。**GPU の無い 1 台で測った数字は、他人の環境への約束にならない**
- **保存済みカタログの再取り込みを nvidia 限定から全 builtin provider へ広げた** — 保存された一覧は設置環境自身のもので、どのモデルが在るかはそちらが決める。ただし**計測が変わったカタログより長生きしてはならない**ので、版が上がったときは builtin のメタデータを同じ id へ貼り直す
- **README 日英に 1 文足した** — 「少なくとも 1 つの LLM provider が要る」の直後に、API キー無しの道が SETUP にあることを書いた。**この行は API キーが必須だと読める書き方のままだった**

**受け入れで直したもの（実装セッションの報告に無い）。** 分岐点が Build 730 で、その後 main の `e653f52`（Build 731）が `PROVIDER_GROUPS` の id を修飾なしへ揃えていたため、**片方のブランチだけでは 1 つも赤くならない食い違いが 3 つ出た**。

- **`web/src/lib/models.ts` の追加 10 本が `ollama:` 付きで書かれていた**ので、修飾なしへ直した（サーバの `VERIFIED_OLLAMA_LOCAL_MODELS` と同じ表記）
- **`test_web_fallback_list_matches_the_catalog` は `ollama:` を前提にした正規表現で id を読んでいた**。緩めると群自身の `id: 'ollama'` まで拾うので、走査の開始をその群の `models: [` へ移した
- **`web/scripts/model-ref-expectations.json` が差し替え前の Ollama カタログを記述していた**。`gpt-oss:20b` は ollama-cloud の単独所有になり規則 2 で決まる／`qwen3.5:4b-q4_K_M` は ollama の単独所有になり同じく規則 2 で決まる／`llama3.2` はどのカタログからも消えたので、**退役した id は規則 2 で決まらなくなる**という逆の教訓を担う。両者が担っていた「どのカタログにも無い、コロンを含む id は段の既定へ落ちる」は新しい 1 件で持ち直した
- **Ollama 一覧の差し替えで、2 つの provider が共に載せるモデルが出荷カタログから消えた**。`test_sole_ownership_decides_and_ambiguity_does_not` はその材料に依存しており、**テスト自身が「the ambiguity this rule exists for is gone」と名指しで失敗した**。第 2 の所有者は期待値表で明示的に注入し、Python と node の両方が組み立て前に足すようにした（3 つの assertion は不変）

**検証:** server 1628 passed / 31 skipped、cli 76 passed、ruff clean、`npm run check` 0 errors / 2 warnings / 218 files、**`npm run lint:models` 58**（+2 = 足した case）、`npm run lint:i18n` 877 / 44 / 0 / 0、`check_frozen_corpora.py` バイト一致、`check_docs.py` 緑。**摂動 4 件を機構ごとに当て直した** — `reasoning_effort` を外すと Stage 1 の思考テスト／再取り込みを nvidia 限定へ戻すと版上げ 2 件／`speed_developer_only` を切るとリリース表示 1 件／`response_format` の分岐を殺すとスキーマ 1 件と Stage 2 の思考 1 件が、それぞれ赤になる。

**版数は patch。** レポートは minor を推していたが、**規則の minor は「作者が節目と明示」か「互換が切れるとき」に限られ**、保存データ・API・エディション ID の形式はどれも動いていない。

---

### v2.9.7 — 作品カードは見分けるものだけを載せ、バックアップは時刻を言うようになった（Build 766、2026-07-29）

**UI 調整 5 巡目**（[I-044]）。作者が対話で 5 つの指示を出し、その都度受けて Build 756〜760 として配備した。**5 件すべてを作者が実 UI で受け入れている。**

- **履歴管理モーダルのタブ 4 つが、他のボタンと同じ吹き出しを持つようになった**（[I-042]）。`.settings-tabs` は角丸を切るために `overflow: hidden` を持っており、**中の吹き出しが箱にクリップされて見えないので native `title` を使っていた**。`overflow` を外し、角丸を先頭・末尾ボタンへ移した。`Tooltip` はボタンを包むので**ボタンどうしが兄弟でなくなり**、区切り線のセレクタを `.tooltip-wrap` 基準へ書き換えた
- **作品カードの操作行から動詞が消えた。** `ごみ箱` / `復元` / `完全削除` は選択してツールバーへ集約し、`再現` は**モーダル内の 3 箇所すべて**から外した（1 箇所だけ直すと同じモーダルの別の見え方に残る）。**`再現` 機能そのものは `CanvasPanel` に残っている**。残ったのは 星 / ハッシュ / **世代番号** / **モデル名** の 4 つで、後ろ 2 つがこの巡の追加である。**行内の削除ボタンは表と系譜行に残す**（作者裁定。集約はサムネイルパネルの規律であってモーダル全体の原則ではない）
- **DB バックアップに常駐スケジューラを新設した。** `ensure_scheduled_db_backup()` の**呼び出し元は管理者専用の `GET /api/settings/status` 1 箇所だけ**で、「N 日ごと」は**管理者が画面を開いた最初のときに実施される**遅延実行だった。ここへ「何時何分」を足しても設定欄が嘘をつくので、**着手前に作者へ示して裁定を得た**。`_lifespan` が 1 分ごとに期限を問うタスクを持つ（`INKU_DB_BACKUP_SCHEDULER=0` で外せる）。**tick が粗いのは意図で、予定は前回バックアップの時刻から導く**ため、起床が遅れてもコピーが遅れるだけで飛ばされない
- **設定に `backup_hour` / `backup_minute` を足した**（既定 3:00）。**間隔が「日」を決め、時刻が「その日のいつ」を決める**ので、前回が夜遅くても後続が引きずられない。**保存済みデータの読み替えは不要で、API も追加のみ**（削除・改名なし）
- **保持しているデータの一覧を `/api/settings/status` に相乗りさせた**（世代 / 種別 / 日時 / 容量）。**世代 1 が最新**で、番号が大きいものほど次に消える。**手動は間引かれないので番号の外**（`—`）。**payload は 50 件で打ち切るが合計件数と合計容量は全ファイルぶんを返す**ので、打ち切りが使用量を隠さない
- **「再読み込み」がバックアップを 1 本書くことがあった。** `GET /api/settings/status` に残っていた `ensure_scheduled_db_backup()` は**常駐スケジューラを足す前は唯一の引き金だった**もので、足した後は重複であり副作用だけが残っていた。作者裁定で削除した。**期限が来ている状態を作ってから status を読み、自動バックアップが 1 つも生まれないことを見るテスト**を足した
- **バックアップ時刻の欄が読めなかった**（作者「謎すぎて理解できない」）。原因は 2 つ重なっていた — **欄が 2 つから 3 つに増えて 1 トラックが約 108px しかなく `input[type="time"]` が入りきらない**ことと、**説明を 3 欄まとめて下に置いたので時刻欄の説明として読めない**こと。**時・分を隣と同じ数値欄 2 つへ置き換えた**（退けた案 = 一文で読ませる / `type="time"` のまま幅と説明を直す）
- **`再読み込み` を `表示更新` へ**（en `Reload` → `Refresh`）。**このキーは DB・ログ・ユーザー設定の 3 箇所で共有**されている。`設定再読み込み`（別キー・2 箇所）は作者が名指ししていないので触っていない
- **`SPEC.md` §22 の記述が嘘になっていたので直した。** 「予定されたバックアップは設定 status endpoint が読まれたときに作られる」は**この版が撤去した挙動そのもの**である。**日本語 SPEC に運用節は無い**ので英語だけを直した（2026-07-28 の裁定 — 日本語が概念の正本、英語が運用の担い手）
- **新しいトークン 1 本** `--thumb-plate-fg-read`。台座の字の色 0.42 は**形で分かる星には足りるが読ませる数字には薄い**。台座の一族と同じくテーマ不変

**検証:** server 1635 passed / 31 skipped（+7 = 足した 7 本）、cli 76 passed、ruff clean、`npm run check` 0 errors / 2 warnings / 218 files、**`npm run lint:i18n` 897 / 47 / 0 / 0**（+20 文字列・+3 例外）、`npm run lint:models` 58、`check_docs.py` 緑。`check_frozen_corpora.py` は回していない（`renderer.py` / `stroke_engine.py` / `schema.py` / coerce / `saijiki.py` の差分が 0 行）。

**摂動 8 件はすべて実装セッションが当て、すべて赤くなった**（時刻の無視・書き込みの欠落・並びの反転・手動への採番・タスクを作らない `_lifespan`・例外で抜けるループ・環境変数の無視・削除した副作用の復活）。**受け入れ側は再測定していない** — 作者裁定「二重のテストは不要」による。**時刻のテストが既定の 3:00 ではなく 22:45 を要求している**のは、4 巡目の「既定値を変えるとその既定を確かめていたテストが恒真になる」を踏まないためである。

**分岐点が古かったが、食い違いは出なかった。** 起点 `217580f` 以降に main が同じファイルを触ったのは 2 commit で、`test_api.py` の `ddl_engine_version` ピン 4 箇所（`"2"`→`"3"`）と `APP_VERSION` の 1 行だけである。**本ブランチの新テストは層の版を 1 つも焼いていない**ので、どちらも自動マージで解けた。衝突は `web/BUILD_NUMBER` の 1 件のみ。

**版数は patch。** UI 5 件とサーバー 2 件の改修だが、**保存データ・API・エディション ID のどの形式も動いていない**（設定への鍵 2 つは追加のみ）。render engine は **16**、`ddl_version` / `ddl_engine_version` は **2 / 3** のまま。

---

### 2026-07-29 — README の冒頭にプロジェクトのアイコンを置いた（**採番なし**・ドキュメントのみ）

`no-git-sync/mascot/incu/incu-icon-512.png` を `docs/assets/incu-icon-512.png` として公開し、README 日英の冒頭（`# inku` の直上）へ中央寄せ 120px で置いた。素材のファイル名 `incu` は作者の指示でそのままにしている。

- **置き場は `docs/assets/` でなければならない。** `.gitignore` は `docs/*` を無視し `!docs/assets/` `!docs/spec/` `!docs/history/` `!docs/guide/` の 4 つだけを再包含するので、それ以外に置いた画像は作者のディスクでは解決し GitHub では 404 になる。ギャラリーの 3 枚が通っているのと同じ経路である。
- **`<p align="center">` が GitHub のサニタイザを通ることを push 前に実測した。** `POST /markdown` へ掛けたところ `align` と `width` はどちらも残り、`<img>` が `<a>` でラップされるだけだった。既存の `<table align="center">` と同じ扱いである。
- **アプリケーションは何も変わっていない。** `web/BUILD_NUMBER` も `APP_VERSION` も動かしていない。pentala への反映も行っていない。web の favicon（`web/static/favicon*.png`、`web/src/lib/assets/favicon.svg`）は差し替えていない。
- **GitHub のソーシャルプレビュー（リンクを共有したときのカード画像）は設定していない。** これはリポジトリ設定画面からのアップロードでしか登録できず、REST API に口が無い。README の画像とは別のものである。

---

### 2026-07-29 — トップページで、日本語話者に日本語で名乗る（**採番なし**・ドキュメントのみ）

**GitHub には閲覧者の言語で README を切り替える仕組みが無い。** `README.ja.md` は README として認識されず、トップページに出るのは `README.md` の一本だけである。相互リンクは以前からあったが、**`README.md` の 322 行目＝最終行**にあり、日本語話者はページ全体をスクロールしないと辿り着けなかった。

- **冒頭に言語バーを置いた**（アイコンの直下・`# inku` の直上）。日英とも中央寄せで、いま読んでいる側が `<strong>`、もう一方がリンクになる。
- **`README.md` の英語の一文の直後へ、日本語の紹介を引用ブロックで置いた。** 文面は `README.ja.md` の冒頭からそのまま採っている。**日本語が正本なので、英語ファイルへ埋める日本語も新しく書き起こさない。**
- **見出しを 1 つも足していない。** `check_docs.py` の README ペアは `shape` モード＝見出しレベルの並びだけを比べるので、見出しを持たない段落と `<p>` は日英の骨格をずらさない。**同じ紹介文を `##` で入れると赤になる。**
- **`<p align="center">` と引用ブロックが GitHub のサニタイザを通ることを push 前に実測した**（`POST /markdown`）。`align`・リンク・区切りの `｜`・`<strong>` はすべて残った。
- **右サイドバーの About 欄は変えていない。** 設定画面か認証付き API でしか変えられないため、日英併記の文案（223 字・上限 350）を作者へ渡すに留めた。

---

### v2.9.8 — 画面は小さくでき、選べないものは選べなくなった（Build 776、2026-07-29）

**2 本のブランチを同時に入れた版である。** UI 調整 6 巡目（`feat/ui-adjustments-6`）と Ollama Cloud のモデル評価（`feat/ollama-cloud-provider`）は互いに独立で、衝突は `web/BUILD_NUMBER` の 1 件だけだった。

#### 表示の量を書き手が決める

- **UI モードを 3 つ持ち、ログインユーザーごとに保存する。** `user_accounts` へ `ui_mode`（既定 `simple`）と `ui_custom`（JSON）を足した。**シンプル**は必須のものだけ（ユーザーメニュー・設定入口・単発の記述入力・描画操作・キャンバス）、**フル**は従来どおり全部、**カスタム**は必須に 7 つの表示群を個別に足す。**新しいアカウントはシンプルから始まる。**
- **変えるのは表示層だけである。** 機能経路・履歴・保存データは動かない。隠れている道具もモードを戻せばそのまま動く。**表示できない入力方式や作品タブを選んだままモードを変えたら、単発入力またはキャンバスへ戻す。**
- **習熟度の呼び名は使わない**（`GLOSSARY.md` で `Beginner` / `Expert` を退けた）。用語は `UI mode` / `Simple UI` / `Full UI` / `Custom UI`。
- 左レールに常時表示の切り替えボタンを置いた。設定「その他」にモード選択・カスタム項目・常時表示項目の説明・「シンプルに戻す」を置いた。
- **favicon をプロジェクトのアイコンにした**（64px / 192px / 互換用の 3 枚）。

#### 選べないものに印を付ける

- **Ollama Cloud の 18 本のうち 10 本は、無料枠では叩くと 403 を返す。** 提供元の一覧には載るので、印が無ければ選べてしまい、**描こうとして初めて壁に気づく**。`requires_subscription` を付けて選択不可にした。
- **この印は一覧を取り直しても外さない。** 提供終了（EOL）は一覧が反映するので取り直しで外れるが、有料プラン限定は一覧に現れない。**印の出所が一覧か実測かの違いである**（SPEC §15 系に明記した）。
- **叩ける 8 本を 4 実行ずつ測り、推奨度とツールチップ本文を与えた。** **推奨度 5 は出していない** — 全成功した 1 本の補正が重い側で規則を満たさず、**4 試行では共有基盤の当たり外れと実力を分けられない**（同じ `gemma4:31b` の中央値が同日に 11s → 105s → 35s と動いた）。速度はデベロッパーモード限定のまま。
- **起動直後の控えの一覧（`models.ts`）からも有料の 2 本を外した。** 控えには印が付かないので、サーバーのカタログが届くまでの数百ミリ秒だけ普通に見えてしまう。**規則は「一覧を新しく保て」ではなく「印で守るものを、印が出ない場所に置くな」**であり、テストがそれを固定する。
- **モデルが書いた `version` の推測で Score を捨てない。** 捨てるのは推測のほうにした。**この経路は全 provider の 5 箇所（tool / text / JSON）を通る**ので、影響範囲としては本版でいちばん広い。
- **`MODEL_CONFIG_VERSION` を 2.3.0 → 2.4.0 へ上げた（本セッションの裁定）。** 上げると保存済みカタログへ組み込みのメタが貼り直され、有料 10 本に印が届く。**保存済みのモデル一覧と有効/無効の選択は残る**ので失うものが無い。**ただし実装セッションが挙げた理由は実測と違っていた** — pentala の保存済みは `2.2.0` で、**上げなくても貼り直しは走る状態だった**。上げた本当の効き先は「**すでに 2.3.0 で保存されている設置**」である。

#### 共有機の事故と、その手当て

- **並行ブランチが `rsync -a server/src/inku_server/` を実行し、相手のブランチが足した `ui_mode` / `ui_custom` を pentala から消した。** 分岐後に相手が足したファイルを自分のブランチは持たないので、**ディレクトリごと送れば消える**。`AGENTS.md` は「`server/` を全体 rsync しない」と書いていたが、**`server/src/inku_server/` 単位でも同じことが起きる**とは書いていなかった。
- 手当ては 3 つ。**フロントは PATCH 応答を要求値と照合し、未知フィールドを無視した 200 を成功扱いしない**（これが無いと、楽観更新が黙ってシンプルへ戻る）。**deploy helper は、リモートにある機能がローカルに無い同期を rsync の前に拒否する。** 配備専用の統合ブランチを作って両方を載せた（本マージで用済み）。
- **台帳へ [I-053] として起票した。** 恒久対策は未裁定である。

#### 番号が飛んだ

**`web/BUILD_NUMBER` は 766 の次が 776 である。** 767〜771 を UI 6 巡目、772 と 774 を Ollama Cloud、773 と 775 を配備用の統合ブランチが使った。**採番器はブランチごとの値ではなく共有の連番なので、自分の +1 が空いているとは限らない。**

#### 検証

**マージ結果と、pentala で全緑が確認された統合ブランチのツリーを突き合わせたところ、差はドキュメント 5 ファイルと `BUILD_NUMBER` だけで、ソースは 1 バイトも違わなかった。** つまり pentala で回った検査がそのまま本マージのコードに効く。

server **1668 passed / 31 skipped**（+33）、cli 76 passed、ruff clean、`npm run check` 219 files / 0 errors / 2 warnings、**`lint:i18n` 918 / 47 / 0 / 0**、`lint:models` 58、`check_docs.py` 緑。`check_frozen_corpora.py` は回していない（`coerce/`・`ddl_expander.py`・`renderer.py`・`stroke_engine.py`・`schema.py`・`saijiki.py` の差分が 0 行）。

摂動は実装セッションが当てた — `requires_subscription` の検査 5/5、配線と `version` の検査 6/6、UI モードの永続化 1 件。**受け入れ側は再測定していない**（作者裁定「二重のテストは不要」）が、**マージした木で初めて回る検査はすべて自分で回した**。

**版数は patch。** DB の列 2 本は既定値つきの migration を伴う追加のみで、保存データ・API・エディション ID のどの形式も動いていない。render engine は **16**、`ddl_version` / `ddl_engine_version` は **2 / 3** のまま。

### 2026-07-30 — クライアントアプリの実装は、開発サーバーへ送らない（**採番なし**・運用と build context のみ）

開発サーバーに `android/` が 174M、`macos_swift/` が 587M 残っていた。どちらも 5〜6 月に一度 rsync され、そのまま古くなっていた。消したものは `1.48.0-android.1` を宣言しており、Mac は `2.1.3-android.1` である。

- **その機械は、そもそも両方をビルドできない。** JDK も gradle も Android SDK も Swift toolchain も入っていない。ビルドとテストは Mac で行う。systemd ユニットも drop-in も `compose.yaml` も `deploy/` も、どちらのディレクトリも参照していなかった。
- **中身の大半はビルド生成物だった。** `android/` の 174M のうち 168M が `app/build/` の中間物と古い `app-debug.apk`、`macos_swift/` の 587M のうち 577M が macOS 用の索引ストア。ソースは Mac と突き合わせ、**固有のファイルは 1 つも無かった**（`macos_swift/` の 12 ファイルは全ハッシュ一致）。
- **ベンチコンテナの src に 3 つ目の複製（1.9M）があった。** 3 つとも削除した。
- **恒久の除外を書いた。** `.dockerignore` に `android` と `macos_swift` を足し、運用文書 3 箇所へ常設ルールとして置いた。ただし `server/Dockerfile` と `web/Dockerfile` は `shared/` `server/` `web/` を名指しで `COPY` するので、**イメージの中身は前も後も変わらない**。効くのは docker daemon へ送る context の大きさだけである。
- **`macos_swift/` は git 管理外（`.git/info/exclude`）である。** 正本は Mac の作業ツリーだけで、**開発サーバーの複製はバックアップではなかった**。

### v2.9.9 — 機械の注記が、色の通り道から出た（Build 780、2026-07-30）

色カタログ改修の段 1-A。**`color_hint` は 4 つの役割を兼ねていた**（色記述 / coerce の冪等ガード / renderer の効果注記 / 描写マーカー）。そのうち機械が書く診断文だけを新しい `Instruction.note` へ移した。

#### 分けたもの、残したもの

- **書き込み 92 箇所のうち 85 箇所を `note` へ移した**（compose 72・normalize 3・API 10）。**残した 7 箇所は描写マーカー**（`soft light` / `scent layer` / `waiting buds` / `five-sense presence` / `membrane haze` / `reflection` / `fading`）で、これは renderer が絵の性格として読む語であって診断文ではない。**役割は書き手でなく読み手で数える。**
- **読み戻しガード 20 箇所を追随させた。** `color_hint` を読んで冪等性を守っていた分岐が、移した先を読まなければ 2 回目の coerce で診断文を二重に積む。
- **`note` は宣言の 2 番目に置いた。** 任意フィールドの充填率は宣言位置に単調に従属し、**後ろにあるほど埋まる**（実測: 位置 0 で 0% / 位置 23 で 89%）。`note` は **LLM に埋めさせたくない**フィールドなので、その規則を逆に使って前へ置いた。Stage 2 の日英プロンプトにも「出力しない」を明記した。`thinness` は末尾のまま動かしていない。
- **本番の新規描画で `color_hint` を持つ instruction は 74.7% から 7.6% へ落ちた。** 7.6% は**色記述を実際に持つ instruction の割合と一致する** — つまり残ったのは色の話をしているものだけである。**診断文が色解決を誤起動させていた 2825 件は 0 になった。**

#### 動いたのは色だけである

- **coerce を通る 14 ケースで、演奏 seed は 14/14 不変、経路の幾何（`d` 属性のダイジェスト）も 14/14 不変、色属性が動いたのは 6 件。** `color_hint` が動いたのは 8 件で、**そのうち 2 件は色が変わらなかった**（診断文が色語に当たっていなかった＝誤起動していなかった）。
- これは engine 15 段 1 の **seed allowlist 化**が効いている。engine 14 までは seed が instruction の全 dump 由来だったので、**`color_hint` を書き換えるだけで筆致が動いていた**。いまは 20 フィールドの allowlist で、`color` も `color_hint` も `note` も入っていない。
- **したがって「筆致が動いたら 1-A の効果ではなく事故」という判別が成立する。** `note` を allowlist へ足す摂動を当てると `test_seed_payload` が赤くなる。

#### コーパスが、この改修の主題を見ていなかった

- **受け入れ中に判別力の欠落が 1 つ見つかった。** 実装は DDL コーパスの生成器に射影を入れ、`note` を `color_hint` へ再結合して**凍結ファイルをバイト一致に保っていた**。理由は「機械専用の診断は DDL engine の同一性の外に置く」である。
- **射影を外して測ると、`ddl-engine-3` の coerce ケース 14 件と manifest の全部が動いた。** 契約は「コーパスはバイト一致が正解」と書いていたが、**それは render コーパスだけの話**で（あれは `coerce_score` を 1 度も呼ばない）、**DDL コーパスは coerce を通っている**。
- **決め手は、凍結ファイルが既に `branch_report` の 34 個の coerce 分岐カウンタを持っていたこと。** 機械診断はこの成果物の構成要素であって、外に置く根拠がない。
- **書き込み先を `color_hint` へ退行させる摂動を当てると、射影があるうちは `check_frozen_corpora.py` が緑のままだった。** **CI が回すのはコーパス再生成だけで pytest は 1 件も走らない**ので、これは CI がこの改修の主題を見られないことを意味する。射影を撤去して 14 件 + manifest を焼き直したところ、同じ摂動で**赤（6 ファイル検出）**になった。
- **`DDL_ENGINE_VERSION` は 3 のまま。** 動いたのは記録された出力だけで、これが「動いた分だけ保存する」の意味である。**render コーパス（`render-engine-16` 365 ケース / SVG 333 枚）はバイト不変**。

#### 番号が飛んだ

**Build 777 / 778 / 779 は並行ブランチが取っており、778 と 779 はどちらも 2 本が同じ番号を書いた。** 本版は 780 を採った。**採番器は共有の連番であって、ブランチごとの値ではない。** しかも**測ってから採るまでの間に動く** — 本版は 779 で docs まで書いた後、配備の直前に測り直して開発サーバーが 779 になっているのを見つけ、780 へ採り直した（相手が書いたのは 08:04:50）。**採番は配備の直前にもう一度測る。**

#### 検証

server **1695 passed / 31 skipped**（+27）、cli 76、ruff clean、`npm run check` 219 files / 0 errors / 2 warnings、`check_frozen_corpora.py` バイト一致、`check_docs.py` 緑。

**摂動は実装側が 112/112**（書き込み 92・読み戻し 20）。**受け入れ側は 5 件を独立に当てて 5/5 が赤**（seed allowlist への追加・書き込み channel の退行・読み戻しガードの退行・宣言位置を末尾へ・コーパスの盲点）。**5 件目は赤にならなかったので、その場でコーパスを焼き直した。**

**版数は patch。** スキーマへの追加は既定値 `None` を持つ任意フィールドで、保存済み Score は `note` の有無どちらでも検証を通る（`extra="forbid"` は追加では当たらない）。render engine は **16**、`ddl_version` / `ddl_engine_version` は **2 / 3** のまま。

### v2.9.10 — 止まった機械を手放し、モデルは測った段で薦められる（Build 781、2026-07-30）

台帳 [I-056] の実施と、ローカル LLM の推奨度を段ごとに分けたもの。**OVMS（Intel OpenVINO Model Server）は 2026-07-30 に provider から退役した** — `/health` は答えるのにモデルを出さなくなっており、同じモデルは Ollama から届く。

#### 退役は「名づけ」に参加し、「経路」には参加しない

- **組み込みの一覧から消すだけでは、設置済みからは消えない。** 未知の provider id はカスタム provider として温存され、`MODEL_CONFIG_VERSION` による貼り直しは `if builtin` の内側にあるので届かない。**`RETIRED_PROVIDER_IDS` を `normalize_model_settings` の入口に置いて落とす**形にした。
- **ただし参照の分割とラベル引きには残す。** 作品 6 件が OVMS のモデルを記録しており、**うち 5 件は `ovms:gemma3-4b-api` と修飾済み**（裸の `gemma3-4b-api` が 1 件）。退役 id を分割の側から外すと、その 5 件が「NVIDIA NIM / ovms:gemma3-4b-api」と表示され、**id だけを出すより悪くなる**。所有表（モデル参照の規則 2）には載せず、`connection_for` は退役 id に対して `ValueError` を上げる。
- **台帳に無かった生きた落とし先を 2 つ消した** — `api.py` のデモ指示文生成が持っていた直書きの `"qwen-api"` と、`interpreter.py` の `/no_think`。どちらも OVMS のモデルを名指ししていたので、設定していない環境のデモが退役済みの provider を叩いていた。死んだ古い routing の写し 3 本（composer / interpreter / trainer）も落とした。
- **`OPENAI_MODEL` / `OPENAI_MODEL_STAGE1` は読み手が 0 になった**ので compose 2 本の passthrough と開発サーバーの `.env` から削除した。**`INKU_LLM_BACKEND` は残した** — 消すと既定が anthropic になり、鍵の無い経路へ落ちる。

#### 一つの数字では、二つの段の食い違いを言えない

- **ローカル Ollama のモデルは段ごとに測ってあり、二つの段の答えが食い違っていた。** inku が薦める組み合わせは「Stage 2 を 32% しかこなせない Stage 1 モデル」と「英語の Stage 1 を壊す Stage 2 モデル」である。`recommendation_stage1` / `recommendation_stage2` を足した。
- **段の値は `recommendation_llm` を置き換えるのではなく狭める。** 段の値を持たないモデルは両段ともこれまでの値を読む。したがって**通しで測った cloud 8 本・NVIDIA 32 本は不変で、migration は要らない**。
- **設定 UI は既に Stage 1 / Stage 2 / 共有のタブを持っていながら、両段を同じ `'llm'` へ畳んでいた**（`SettingsModal.svelte`）。並び順も段を読むようにし、共有タブは二つの低い方を使う。
- **メタキーの一覧が 3 箇所にあった**（正規化・版上げの貼り直し・取り直し時の持ち越し）。足し忘れた経路で黙って消える構造だったので、**`MODEL_METADATA_KEYS` 1 本にまとめた**。
- ホバーカードは、段ごとに測ったモデルだけ `オススメ度 / Stage 1` と `オススメ度 / Stage 2` の 2 行を出す。**通しで測ったモデルは 1 行のまま** — 2 行にすると同じ星を 2 度書いて、誰も測っていない計測があるように見える。Vision は分割しない。判定は `modelStageRecommendations()` に出して、**描かずに検査できる形**にした。

#### 恒真テストと、判別力を持たない標本

- **実装セッションの摂動が 1 回空振りした。** 「`MODEL_RECOMMENDATION_KEYS` から段の 2 鍵を削る」を当てて 22/22 が緑のままだった。原因は 2 つあり、どちらも**検査が自分の言い分を自分で供給していた**型である。
  - **恒真だった** — `for key in MODEL_RECOMMENDATION_KEYS: assert key in MODEL_METADATA_KEYS` は**一覧を一覧自身と比べていた**。定数から消せばループが回らない。期待鍵をテスト側にべた書きして直した。
  - **標本が判別力を持たなかった** — 貼り直しの検査で、保存済みに鍵を**持たせていなかった**。`{**builtin, **stored}` の土台合成が答えを供給しており、`metadata_keys` を一度も通っていない。**保存済みが古い値 1 を持ち、カタログが 5 を言う**標本へ差し替えた。
- **これで契約の記述が 1 つ誤りだったことも分かった。** 「`metadata_keys` に足さないと版を上げても貼り直されない」は誤りで、**効くのは保存済みが古い値を持っているときだけ**である。
- **前任が置いた番人を書き換えた。** `test_recommendation_levels_stay_absent` は「方法を測るまで推奨度を入れるな。入れるならこのテストを消せ」と書いてあった。**消すのではなく、方法がどこにあるかを指す形に置き換えた**。

#### 版数と検証

**版数は patch。** provider の撤去は保存データの形式変更ではなく（退役 id は読み取り時に落とす）、段の鍵は追加のみで migration を要さない。**`MODEL_CONFIG_VERSION` は 2.4.0 → 2.5.0**（メタの鍵が 2 つ増えたので、値だけの変更ではない）。render engine は **16**、`ddl_version` / `ddl_engine_version` は **2 / 3** のまま。決定的な層には触れていないので、凍結コーパスは両方ともバイト不変。

server **1732 passed / 31 skipped**（+37）、cli 76、ruff clean、`npm run check` 219 files / 0 errors / 2 warnings、`npm run lint:i18n` 918 / 47 / 0 / 0、`npm run lint:models` 68 checks、**`npm run lint:recommendations` 37 checks（新設）**、`check_docs.py` 緑。**`lint:recommendations` は手元の確認手順に加わる** — CI が回すのは凍結コーパスの再生成だけなので、CI の側は変わらない。

**摂動は実装側が 5 件**（退役 id を共有キー一覧から抜く 2 本赤・共有タブを `Math.max` に 3 件赤・段を無視して `recommendation_llm` を返す 6 件赤・分割条件を外す 3 件赤・vision の除外を外す 1 件赤）。**受け入れ側は 3 段に 1 件ずつ独立に当てて 3/3 が赤**（`RETIRED_PROVIDER_IDS` を空集合に → 9 本赤・段の 2 鍵を `MODEL_RECOMMENDATION_KEYS` から削る → 2 本赤・`modelStageRecommendations()` を常に `null` に → `lint:recommendations` が 2 件赤）。**2 番目は実装セッションが空振りさせた摂動そのもので、直した後は赤くなる。**

#### 番号は飛んでいない

**このブランチは 777 / 778 / 779 を使ったが、その間に main が 780 に達した。** 本版は 781 を採り、ブランチ内の 3 つは commit の中にだけ残る。**採番器は共有の連番であって、ブランチごとの値ではない。**

### v2.9.11 — 抽象色が六語から九語になり、黄に出口ができた（Build 782、2026-07-30）

Score の `color` に **`yellow` / `orange` / `purple`** を足した。**カタログの `palette` には黄が12色あり公称13.6%だったのに、実際に描かれた黄は0.6%だった** — 出口となる語が無かったからである。既存6語の順序は動かさず、3語を末尾へ足した。

**tool schema の `color` は宣言位置17・フィールド数25のまま**にした。任意フィールドの充填率は宣言位置に依存するが、`color` は必須フィールドなので位置そのものは搬送に効かない。それでも先行実測をこの位置で測っているので、比較可能性のために動かさなかった。

**Stage 2 のプロンプトに規則を 2 行足した**（陽光・実り・金属・灯りは yellow/orange、夕暮れ・薄明・花の陰は purple。加えて「3語は他の抽象色と同格である」）。**規則が無いと `orange` は0.6%のままである** — 先行実測で、schema だけ広げたアームは `yellow` 7.6% / `orange` 0.6%、規則を足したアームは 8.5% / 2.4% だった。実装後の固定60入力での実測は日本語 `yellow` 13.7% / `orange` 6.0%、英語 6.5% / 3.0%、新語合計は日 19.7% / 英 10.1% で、受入閾値（yellow 5%・orange 1%・合計 8%）を日英とも越えた。**instruction 総数は日 -0.8% / 英 +5.7%** で、`thinness` のときの「運ぶと Score が痩せる」現象は起きていない。

**`purple` は受入条件に載せていない。** 本番 DB の異なり入力821件で紫系の需要は7件（0.9%）しかなく、標本60件のうち紫向きの入力は3件なので、率が判定力を持たない。**日本語で0件だったことを異常としない。**

**歳時記の いろ にも 3 語を足した。** これで Stage 1 の語彙表にも黄・橙・紫が出るので、**記述言語の語彙そのものが増えた** — `ddl_version` を **2 → 3** へ上げた。据え置く案もあったが、`ddl_version` を 1 → 2 へ上げた理由が「太さが道具名から独立した語になった。語彙が増えたら版を上げるという規約に従う」だったので、同型の変更を据え置くと前例と食い違う。**`docs/spec/render-engine-history` の「上げる条件」が「文法の追加・変更・廃止」と書いていたのが食い違いの出どころ**で、その条文も「語彙の追加・変更・廃止、または文法の」へ改めた。

**coerce も 1-B で広げた。** `COLOR_MARKERS` に日英 3 語ずつ（黄/金、橙/蜜柑/灯火、紫/菫/藤 と yellow/gold、orange/lantern、purple/violet/lilac）を足した。**これを入れないと `ddl-engine-4` は `ddl-engine-3` の完全な複製になる** — schema と歳時記だけを変えて両生成器を回しても、渡す前の実測では両コーパスが1バイトも動かなかった。**1-B は Stage 2 の出口を広げる改修だが、コーパスは Stage 2 を呼ばない**（DDL 文字列を入力に取る）ので、coerce に色語が届かないかぎりこの層は改修を見られない。

**`renderer.COLOR_MAP` に3語の既定色を足した**（`yellow` `#a18308` / `orange` `#a95a00` / `purple` `#583a84`）。`_resolve_color` の1行目が `cmap[color]` なので、足さなければ新語は `KeyError` になる。起草時の案は「`FALLBACK_TO_SIX` で `yellow`/`orange` を `red` へ、`purple` を `blue` へ丸める」だったが、**Stage 2 は `yellow` を8.5%出すので、丸めを入れると「黄を選んだ分だけ赤が増える」**。実描画の赤は44.6%で**それを下げるのが段1の目的**なので、暫定物が目的の逆を向く。3色は既存の有彩3色と同じ register（L 0.42〜0.62 / C 0.12〜0.13）で選び、**`renderer` 自身の分類器 `_hue_from_hex()` が各語を自分の語に分類することを検査に固定した**。**11カタログの `map` は触っていない**ので全カタログが同じ黄を描く。カタログごとの黄は段 1-C の仕事である。

**`ddl_engine_version` を 3 → 4 へ上げ、`ddl-engine-4` を33ケース（`a_expand` 15 / `b_coerce` 18）で焼いた。** `changed_from_previous` は新設4件ちょうどで、**既存29ケースは1バイトも動いていない**。**`render-engine-16`（365ケース / SVG 333枚）はバイト不変** — `COLOR_MAP` への3行の追加は既存6語の解決を1行も変えていない、その証明である。

#### 既存の凍結挙動が1件動いた

**`server/tests/golden/coerce_golden.json` の H-01 が動いた。** DDL が文字通り「黄色いクレヨンの小さな四角」と書いているケースで、`with_color_delivery_repair` が 0 → 1 になり `color_cycle` が `["red","black"]` → `["red","red","black","yellow"]` になった。**これは 1-B の目的そのもの**（黄を読めるようになった）で、`gen_coerce_golden.py --refreeze` は生成器の正規手順である。ただし `red` の重複は 1-B 起因ではない — **`_with_color_cycle_delivery` は `base_color` を無条件に cycle の先頭へ挿しており、その2行下と4行下は重複を見ているのに、この行だけ見ていない**。台帳へ起票した。

#### 検査が自分の答えを供給していた

**受け入れで当てた摂動が1件、実装側の摂動6件では出なかった型を掘り出した。** `_resolve_color` が黄・橙・紫だけ `cmap["black"]` を返すようにする — **1-B の可視効果そのものを打ち消す退行**である。これで **1751件すべてが緑のままだった**。原因は新設テストの `test_all_catalogs_resolve_all_nine_colors` が、**自分で作った内包の鍵を、その鍵の一覧と比べていた**ことで、`_resolve_color` が何を返しても真になる。値を固定し、3新語が `_hue_from_hex()` で自分自身へ分類されることを足したところ、同じ摂動で **11件が赤**（カタログ1つにつき1件）になった。**`render-engine-16` がバイト不変であることは、この穴を塞がない** — 新色はコーパスのどのケースにも現れないからである。

#### 添景は新色を追いかけない（既存の穴）

`ddl_expander` は `COLOR_MARKERS` とは別に自前の色語タプル（`_JA_COLORS` / `_EN_COLORS`、6語）を持っており、添景展開の主色を決める `_dominant_ja_color` がそれを読む。**60 seed で測ると、黄・橙・紫は 0/60 で自色の添景を得られず全部「黒い」になる**（赤 48/60・緑 46/60）。ただし**これは 1-B が開けた穴ではない** — 開発サーバーの DB を読むと、**1-B の前から DDL 282件のうち19件が「黄色い」を含んでいた**。Stage 1 は語彙表に無くても黄を書いていた。1-B が変えたのは、**その黄が coerce に読まれて Score まで届くようになった**ことである。添景の側は台帳へ起票した。

#### Android の複製は2版ぶん古い

`pipeline/ServerScoreCoercer.kt:64` は6語の集合を持ち**外れた色を黙って `black` へ書き換える**。`pipeline/ServerScoreSchemaJson.kt:5` は tool schema の凍結複製で、`Instruction` は**フィールド数24・`thinness` が14・`note` が無い** ＝ [I-036] と v2.9.9 の `note` 追加のどちらも届いていない。**1-B が作った遅れではない**ので本版では追わず、台帳 [I-029] の材料として渡した。

#### 摂動

**実装側6件** — `Color` から3語削除（6件赤）/ プロンプト追記を旧文へ戻す（2件赤）/ 歳時記から3語削除（17件赤）/ `COLOR_MARKERS` から3語削除（生成器 exit 1・5ファイル変化）/ `COLOR_MAP` から3色削除（11件赤）/ `yellow` を `red` の hex にする（1件赤）。**受け入れ側**は `COLOR_MARKERS` の摂動を独立に再現して**ちょうど5ファイル**（`b_coerce` 4件 + `manifest.json`）が動くことと生成器の非零終了を確かめ、そのうえで上記の `_resolve_color` の摂動を当てた。

### v2.9.12 — カタログの palette が、絵へ届く（Build 783、2026-07-30）

**11 のカタログはそれぞれ 8 色の名前つき palette を持つが、それが描画へ届く経路は `color_hint` の文字列照合しか無かった。** 記述チャネルはほぼ空である — 保存済み 7463 instruction のうち、Stage 2 が書いたセグメントに色語を含むものは **945 件（12.7%）** しかない。残る 87% は `cmap[color]` ＝ カタログの `map` 6 色（と v2.9.11 が足した 3 既定色）で終わっていた。**本版は palette から決定的に選ぶ経路を作る。**

**割当は作品ごとに 1 回だけ計算し、材料は `(render_seed, catalog_id, 抽象色)` の 3 つだけ**である。instruction の全 dump を使うと `color_hint` を書き換えるだけで色が変わって A/B が交絡するので使わない。`performance_seed` も使わない — **色は演奏ではなく作品の性質**だからである。有彩 6 語は **OKLCh** の色相帯へ分類する（**CIELAB は純青 306° と純マゼンタ 328° が隣り合うので青と紫を分離できない**）。無彩 3 役は **`map` 値と同じ hex の候補を先に予約してから** 残りを L の近い順に取る — 契約が書いた素朴な「L 最大 / 最小 / 中間」は、**無彩の palette 色が 3 つ未満のカタログ 5 つで白と黒を同じ hex に潰す**（`desert_mineral` は無彩が 1 色）。背景も同じ割当を通る。

**`catalog_id` を renderer へ通した** — それまで `renderer.py` にこの識別子は 1 件も出現していなかった。`api.py` の 2 呼び出し・`RenderEngine.render()`・`DefaultRenderEngine`・`render()` の 4 ファイルで、省略時は `DEFAULT_COLOR_CATALOG_ID` として扱う。**`_hint_hues` は ASCII を単語境界で照合するようにし**（CJK は語境界が無いので文字一致のまま）、**語として成立しない 5 トークンを語彙表から落とした** — `blu` / `ai`（青）・`vert` / `tall`（緑）・`shu`（赤）。`vertical` 166 件・`constraint` 20 件・`blur` 13 件の誤起動がこれで止まる。**真正なフランス語 `vert` も読めなくなる**が、誤起動を止める側を採った。`brown` は帯を持たないので `orange` へ送る。

#### ケース表を拡張しなければ 1 件も動かなかった

**`render-engine-16` の 365 ケースは、`palette:` キーを 1 つも持たず、`color_hint` が 0 件、instruction の色は `black` 364 と `green` 1 の 2 語だけ、背景は全件 `white` だった。** 解決連鎖は記述も候補集合も空で `map[color]` に落ちるので、**改修が層を通っても出力は 1 バイトも動かない**。作者裁定でケース表を拡張し、**F 群 110 件**（11 カタログ × 9 抽象色の 99 件、記述 6 件、非白背景 5 件）を足した。**動いた 110 / 不変 365**。不変の側が本版の境界そのものである — 6 キーの `map` だけを渡され、`palette:` も `color_hint` も持たず、背景が `white` の呼び出しは engine 16 と同じ絵を出す。

**manifest の `color_map_digest` の意味を改めた。** 旧来は生成器が自前で持つ 6 キーの `DEFAULT_COLOR_MAP` 一枚の digest で、**`renderer.COLOR_MAP` を変えても動かなかった**（v2.9.11 の 3 語追加も digest 不変で通った）。F 群はケースごとに違う `color_map` を持つので、**475 ケースすべての `(case_id, catalog_id, color_map)` 集合の digest** とした。**凍結 SVG を読むだけでは恒等割当の摂動が素通りする**ので、F 群 110 件を現行 renderer で毎回再演奏して digest と突き合わせるテストも足した。

#### 到達指標（彩りは増えない。無彩が増える）

保存済み 1847 作品 / 7463 instruction を、**v2.9.9 が診断文を `note` へ移した後の面**で測った。**一度も選ばれない palette 色 12 → 6 / 88**、**解決される distinct hex 76 → 82**、**誤起動由来の色決定 148 → 0**、**実描画の無彩 57.9% → 61.4%**。帯を決めるのは抽象色であり、保存済み作品の抽象色は **69.5% が無彩**なので、**本版が動かすのは「どの palette 色が使われるか」であって帯の比率ではない。**

#### 紙に溶ける 8 役（本版では直していない）

**白地との明度差 ΔL が 0.15 未満の役は、engine 16 の 0 / 88 から 8 / 88 になった。** 7 件は黄と橙で、palette のその 2 帯が明るいことによる。**残る 1 件は退行である** — `cool_material` の `black` が `#2c3e50`（L 0.356）から `#e5e8e8`（L 0.929）へ移り、紙 `#fcfcfc` との ΔL が **0.635 → 0.062** になる。このカタログの黒 `#2c3e50` は彩度 0.039 で**無彩のしきい値 0.035 をわずかに超える**ため無彩候補から外れ、残った唯一の候補を「L が最も近い未使用候補」規則が**距離の上限なしに**取る。本番の `cool_material` は **102 作品 / 412 instruction で、うち 205 件（49.8%）が `color=black`**、背景は白 76 / 黒 19 である。`desert_mineral` の `yellow` は紙と**同一 hex**（ΔL 0.000）になる。**hex の一致だけを見る検査はどちらも通す** — 3 役が互いに異なることは満たされているからである。**この退行は実装の逸脱ではなく契約の設計にあった** — 契約の警告は「白と黒が同じ hex になる」ことだけを見ていて、明度差を見ていなかった。**2026-07-30 の作者裁定により、修正せずに配備して段 2 で扱う**（台帳 [I-062]）。

#### 摂動

**実装側 3 件** — `work_assignment` を map 恒等割当に戻す / ASCII の照合を部分文字列へ戻す / 無彩を「L 最大・最小・中間」へ戻す。**受け入れ側は 4 件を独立に当てて 4/4 が赤** — 恒等割当（13 件）・**`catalog_id` を seed 材料から外す**（6 件）・無彩のしきい値 0.035 → 0.05（4 件）・帯が空のときの最近傍を殺す（10 件）。**2 番目は「`catalog_id` が本当に seed に入っているか」を確かめるもの**で、実装側の 3 件はどれもこれを見ていない（新設テストの `test_catalog_id_participates_in_multi_candidate_choice` は palette の違いだけでも通るので、`catalog_id` を無視した実装でも緑になる）。

pytest **1771/31**（+20）、cli 76、ruff clean、`npm run check` 219/0/2、凍結コーパスは再生成バイト一致。**Android は engine 16 のまま**（台帳 [I-029]）。**版数は patch** — engine の版上げであり、保存済みデータの形式は変わらない。

---

### 2026-07-30 — Android の engine 版数が、一箇所で決まる（android Build 148090、**採番なし**）

**Android は engine 版数の文字列を 3 箇所に別々に持っており、値が 2 種類に割れていた。** 描画 metadata の生成元（`DefaultSvgRenderer`）と `renderHash` のフォールバック（`LocalFallbackPipeline`）は `"16"` を、**UI の版数表示が読む `CompatibilityConstants` だけが `"1"` を**持っていた。engine 16 への追随（台帳 [I-029]、2026-07-29 完了）でも表示は取り残されている。3 箇所を `CompatibilityConstants` へ寄せ、値を実体の `16` に揃えた。

**値は動かしていない。** engineVersion は**エディション ID `rh3` の材料**であり、`16` から動かすと保存済みの ID と一致しなくなる。本変更で動いたのは UI 表示の `1` → `16` だけで、metadata の JSON も `renderHash` も 1 バイトも変わらない（`Engine16VersionTest` の metadata 検査と `ServerScoreParityTest` の rh3 期待値 4 件が緑のまま）。`renderEngineId` も同じ形で割れていたので `"default"` のまま同時に寄せた。

**既存 99 件はこの定数を一度も読んでいなかった** — 定数が `"1"` のままでも全件緑になる状態で、**直そうとしている値を誰も見ていなかった**。リテラルを固定するテストを足して判別力を作った。**一致検査だけでは足りない** — 表示側と metadata 側が同じ定数を参照した瞬間に**何を入れても真になる**。実測でもそうなっており、定数を `"17"` にする摂動で赤くなったのは `testCompatibilityConstantsDeclareEngine16` / `testRendererMetadataDeclaresEngine16` / `testRenderHashDefaultsMissingAndBlankMetadataToEngine16` の 3 件で、**一致検査 `testUiConstantsMatchRendererMetadata` は緑のまま通った**（恒真であることを承知で残してある。連動が切れたときに効く）。

**フォールバック機構そのものは撤去していない**（作者裁定）。`renderHash` の呼び出し元 2 箇所はどちらも metadata が `renderer.render()` 由来なので、この既定値には現状到達しない。server 側（`db.py`）が同じ既定値を持たない件は台帳 [I-011] に残る。**[I-012]（UI の版数表示が `1`）は解消。**

Android 単体テスト **101 / failures 0 / errors 0 / skipped 0**（起点 99 + 新規 2）。**`APP_VERSION`・`web/BUILD_NUMBER`・`android/VERSION`・`android/BUILD_NUMBER` はいずれも不動**（Android のみの変更で、パッケージングタスクを走らせていないため Gradle の自動採番も起きない）。**pentala 反映は無し**（`android/` は全経路から恒久除外）。`server/` `web/` `cli/` `shared/` の差分はゼロ。

**`ANDROID_SPEC` は追随していない。** 冒頭の「追随状況」は `2.0.0-android.1` / engine `11` のままで、実体（`2.1.3-android.1` / engine 16）と 5 版ずれている。台帳 [I-013] は「engine 15 に未追随」と記録していたが、**実際の遅れはそれより大きい**。

---

### Android `2.1.4-android.1` — 描画層が render engine 17 へ追随（カタログ palette の割当・抽象色 9 語）（android Build 148090 据え置き、2026-07-31）

**engine 17 のうち server と共有している部分を移植し、Android も render engine 17 を名乗る。** テストは **110 件 / 失敗 12 → 112 件 / 失敗 0**。

- **遅れは engine 17 だけではなかった。** 参照コーパスを焼いた 2026-07-29 以降に決定的な層が 4 回動いており、`thinness` の宣言が末尾へ（`2125b82`）、`note` が新設（`b484f3f`）、抽象色が 9 語へ（`74bf869`）、そして engine 17（`9090973`）が入っていた。**契約はこの 4 系統を段に割った**
- **段 1 seed:** 全 dump（`surfaceSeed`）へ `note` を足し `thinness` を末尾へ移した。**allowlist 側（`serverInstructionJson`）は `note` を持たず `thinness` は `weight` の直後のまま** — server の `_SEED_INSTRUCTION_FIELDS` がそうなっている。**両方を「揃える」と 30 件の参照 SVG が全部ずれる**
- **段 2 語彙:** schema の 3 つの enum（`color` / `background` / `color_cycle`）と coercer の allowlist を 9 語へ、DDL マーカーへ 黄・橙・紫を足した
- **段 3 割当:** OKLCH 変換・6 つの色相帯・無彩の取り合い・SHA-256 の**ビッグエンディアン符号なし 64bit** による選択を移植し、`resolveColor` の採点方式を廃止した。ヒントの ASCII 語は**単語一致**へ変わり、`HUE_HINTS` から 5 語（`blu` / `ai` / `vert` / `tall` / `shu`）を削った
- **段 4 材質層:** `pen` の**線**にだけ材質層が出ていなかった。`usesMaterialOutline("pen")` は `true` を返すのに線の経路の集合に `pen` が無く、**同じ性質を 2 箇所が読んで食い違っていた**。**コーパスに `pen` の線が 1 件も無かったので engine 16 まで誰も見ていなかった**
- **段 5 プロンプト:** server の 4 定数を丸ごと同期した（`STAGE1_*` が 18,963 / 17,956 バイト、`STAGE2_*` が 44,116 / 42,191 バイト）。`*_LITERT` は対象外
- **段 6 版:** `CompatibilityConstants` の 1 箇所（2026-07-30 に集約済み）。engine version としての `"16"` は **0 件**
- **カタログの中身（`ColorCatalogs.kt`）は範囲外。** 起票時の前提「server は 9 キー / Android は 6 キー」は**誤りで、server の 11 カタログの `map` も 6 キー**である（9 キーなのは `renderer.COLOR_MAP` の既定値）。**engine 17 の追随はカタログ表を 1 行も触らない**ので、engine 18 の入れ替えと衝突しない
- **面の質感 6 つ（`stipple` / `grain` / `paper_grain` / `wash` / `aquatint` / `bleed`）は実装していない**（[I-043]）。**部分実装のまま版を宣言する**のは engine 16 と同じ扱いである

**契約を渡す前に検査面を焼いた。** 既存コーパスは **32 ケースとも `color_hint` 0 件・色語は `black` のみ・`palette:` キー 0** で、`renderer.render` を `color_map` も `catalog_id` も渡さずに呼んでいた。**割当機構を 1 行も書かずに `"17"` を宣言すれば全緑になる状態**だった。さらに**既存テストは経路（path / points / dash）しか比べておらず、色を見ている assertion が 1 つも無かった**ので、ケースを足すだけでは足りなかった。焼いたのは `renderer_color_assignment.json`（11 カタログ × 9 色 × 2 seed ＋ OKLCH 91 hex ＋ 帯 ＋ ヒント 15）・`score_schema_contract.json`・SVG 6 ケース（割当の分岐 1 つずつ）と、それを読む 9 件のテストである。

**受け入れでレポートの数値を引き写さず、再現と摂動を行った。** 全 110 件を回して 0 failures を確認し、**段の数だけ 6 つの摂動**を当てて狙った assertion が赤くなることを見た（dump を戻す → `test06SurfaceHatchExactParity` と参照 SVG 走査、coercer を 6 語へ → `ServerScoreVocabularyTest`、版を `"16"` へ → `Engine17VersionTest` 3 件、符号なし剰余を符号つきへ → 割当 5 件、`pen` を外す → 参照 SVG 走査の `38`、プロンプトへ 1 文字 → `PromptFingerprintTest`）。**レポートの段 1 の記述だけは実測と違った** — 「dump を戻すと `ServerRendererThinnessTest` が赤」とあるが、あのテストはスキーマを見ており dump を見ていない。

**受け入れで欠陥を 2 件見つけ、どちらもゲートが無かったのでその場で足してから直した**（作者裁定）。

- **OKLab の係数 2 つが別の版だった**（`0.0040720403` / `0.8086758033` → `0.0040720468` / `0.8086757660`）。**焼いてあった `oklch` 91 色を読むテストが存在せず**、通っていた。追加したら 233 件が不一致。**今日の 11 カタログでは 1 件も割当を変えない** — 差は ΔL 6.5e-9 で、判定境界への最短距離（chroma 8.8e-4 / 帯 0.80 度）の **2 万分の 1** である。**engine 18 が全カタログ色を入れ替えるので、留めるべきは結果ではなく定数のほうだった**
- **`render_color_map`（metadata）が割当済みの色を載せていた。** 描画用の map に割当を畳んだものをそのまま metadata へ入れており、**11 カタログ全部で 3 鍵（`yellow` / `orange` / `purple`）が増え、4 本では既存の値が上書き**されていた（`cool_material.black` が `#2c3e50` → `#e5e8e8`、`sea_stone.blue` が `#005bae` → `#191970`、`fresco_study.red`、`weathered_heritage.white`）。**server の `render_color_map` はカタログの map + palette の 14 鍵のまま**で、割当は触れない。**engine 16 まで一致していた parity が今回の変更で壊れていた**

- **[I-062]（`cool_material` の `black` が紙との明度差 0.062）は直していない。** server の engine 17 の現在の振る舞いであり、移植は忠実に再現する
- **採番は `android/VERSION` だけ。** `APP_VERSION`（`v2.9.12`）・`web/BUILD_NUMBER`（783）・`android/BUILD_NUMBER`（148090）はいずれも不動で、**pentala 反映も不要**
- **検証:** Android **112 件 / failures 0 / errors 0 / skipped 0**（25 XML）。マージ後の主 checkout で server **1771 passed / 31 skipped**、cli **76**、ruff（`src tests scripts`）緑、`npm run check` **0 errors / 2 warnings / 219 files**
- **残り:** `ANDROID_SPEC.ja.md` / `.md` は **engine 15 にも未追随**（[I-013]）。本契約の範囲外

### Android `2.1.4-android.2` — 画面が呼ぶものだけを残す（ツールチップ・表示モード・マスコット・添景水準・系譜表）（android Build 148090 据え置き、2026-07-31）

**実装セッションの 2 巡を差し戻したのち、git 管理セッションが直してマージした**（作者裁定）。

- **呼び出し元の無い Composable 11 個を整理した。** 4 個（`ProvenanceTooltipTarget` / `UiModeContainer` / `MascotWidget` / `TenkeiSelect`）は画面から呼ぶようにし、7 個（`CustomModalContainer` / `ToastQueueWidget` / `ConditionChipsContainer` / `LineagePanel` / `UnreadWordsPanel` / `AIRefineModal` / `ManualRefineModal`）は削除した。**契約に無い 3 機能**（モーダルの scrim・Toast キュー・条件チップの折りたたみ）も落とした
- **表示モードとマスコットが表示を変えるようになった。** 設定は既に Room へ保存・復元されていたが、読む側が製品に無く「選べるが何も変わらない」状態だった。`ComposeScreen` が `UiModeContainer` で simple / full を切り替え、full のときだけマスコットと条件の帯を出す
- **添景は水準であってモチーフではない。** `TenkeiOptions` を `web/src/lib/tenkei.ts` の 3 水準（`none` なし / `sparse` 控えめ / `auto` おまかせ）へ直した（実装は `moon` / `cloud` / `bird` / `mountain` / `water` というモチーフの一覧を発明していた）。**選んだ水準が `PaintRequest.tenkei` を通って `WebDdlExpander` へ届く**ようにした（それまで `LocalFallbackPipeline` の 2 箇所が既定値 `auto` を渡していた）
- **カタログに存在しないモデルの推奨を 1 行落とした**（`qwen/qwen-2.5-coder-32b-instruct` は server・web・Android のどこにも無かった）。残る 3 行は実在の ID
- **`lineage_nodes.history_id` を TEXT にした。** `Long?` / INTEGER で焼かれており、`history_items.id`（TEXT）と結合できなかった
- **migration の検査が実際にデータベースを開くようになった。** それまでの 2 本は `startVersion` / `endVersion` を読むだけで SQL に触れず、制約の検査は data class を 2 つ作って等値を見るだけだった（`uq_lineage_primary_parent` は一度も働かなかった）
- **`androidx.room:room-testing` を入れた。** `kotlinx-serialization-core` が推移的な BOM に `strictly 1.7.3` で固定され、room-testing の 1.8.1 生成コードと食い違って実機で `AbstractMethodError` になるため、**androidTest の configuration だけ 1.8.1 へ force した**
- **採番は `android/VERSION` だけ**（`2.1.4-android.1` → `2.1.4-android.2`。実装セッションは段ごとに `.11` まで上げていた）。`APP_VERSION`（`v2.9.12`）・`web/BUILD_NUMBER`（783）・`android/BUILD_NUMBER`（148090）はいずれも不動で、**pentala 反映も不要**

#### 摂動（製品側に当てた）

前 2 巡の摂動はすべてテストの期待値の書き換えだったので、**製品のコードを 4 回摂動して狙った検査が赤くなることを見た**。

- `TenkeiOptions` へ `moon` を戻す → `tenkeiOptions_areExactlyTheThreeWebLevels` が赤（単体）
- `history_id` を `Long?` / INTEGER へ戻す → migration 3 件が赤・他 7 件は緑
- `uq_lineage_primary_parent` から `UNIQUE` を外す → migration 3 件が赤
- **スキーマを変えずに** migration へ `DELETE FROM history_items` を足す → **既存行を読む 2 件だけが赤**（制約の検査は緑）＝ 検査本体が効いている

#### 直していないこと

- **段 6 の中核は未実装のまま。** colophon（奥書）は Android に 1 件も無く、`unread_words` に相当するテーブルも、推敲が `lineage_edges` へ派生を積む結線も無い。残るのは 11 種別の表（`DerivationKind.kt`）とその検査だけ
- **系譜のデータ層は誰も使わない。** `LineageDao` と `HistoryItemEntity.lineageNodeId` は宣言だけで、リポジトリからも ViewModel からも呼ばれない。表と migration は正しく焼けているが、行は 1 つも書かれない
- **`TooltipTest` は製品の呼び出し箇所の引数を読まない。** `ProvenanceTooltipTarget` は `CanvasHeroCard` から呼ばれるようになったが、テストは自前の文字列で直接呼ぶ
- **`state.selectedTenkei` が展開へ届くことを見る検査が無い**（型としては通したが、経路を通す検査は置いていない）
- **マスコット設定の二重**（既存の Kiwi / Crab と新しい Incu / Yuragi）は [I-066] の裁定待ちなので触っていない

---

### v2.9.13 — 複数の作品が、ひとつのループになる（Build 789、2026-07-31）

**5 本の独立した枝を統合した版である**（Build 784〜788）。中核は **複数作品のアニメーション書き出し**で、`POST /api/history/export-animation` が保存済みの SVG を並べて 1 本の APNG または GIF にする。

**呼び出し元は 2 つで、並び順の決め方が違う。** 履歴管理はチェックした作品を `at` 昇順（同時刻は id 順）で古いものから並べ、系譜は選択中の作品から親を辿って**起点から選択作品の順**へ反転する。どちらも 2 件以上でなければボタンが押せない。

**要求順はサーバー側で保たれる。** `db.get_items` は `id.in_(ids)` で引いた行を、要求された `ids` の位置で並べ直して返す（DB の返却順ではない）。API は `dict.fromkeys` で重複を落としてから所有者検査を通し、**保存済み SVG を持たない作品が 1 つでもあれば 409 を返す**。

**設定は `設定 > エクスポート` にあり、localStorage の `inku-animation-export-settings` に載る。** 形式は APNG（可逆）と GIF（256 色）、切り替えは **カット / クロスフェード / 白を介したフェード / 横スライド** の 4 種、表示ウェイトは 0.1〜30 秒、解像度（Y 軸）は 1K=1080 px・4K=2160 px・8K=4320 px。**遷移フレーム数は解像度で変える**（1K=6・4K=4・8K=2）。符号化するピクセル総量には **600,000,000** の上限があり、超えると 400 を返す。

**ラスタライズは既存の `svg_to_png`（resvg）を通す。** 新規依存の **Pillow 12.3** が担うのは、ラスタライズ済み PNG の合成・遷移・符号化だけである（`cairosvg` の番人 4 件は無傷で、`animation_export.py` は `cairosvg` を import しない）。

#### 同じ版に入った 4 つ

- **系譜の空白部をドラッグしてパンできる**（Build 784）。マウスの主ボタンのみで、カード・ボタン・入力・メニュー項目の上では始まらない。`.lineage-scroll` に `role="region"` と `aria-labelledby` を足した
- **記述タブの結果ログの開閉状態が次回描画へ継承される**（Build 785）。`inku-result-log-open` に載る
- **設定 > その他から履歴パネルの設定 3 つを削除した**（Build 786）。**挙動は従来の既定へ固定される** — 再編集は常に新バージョンとして保存し、履歴選択はキャンバスサイズも色カタログも上書きしない。i18n の鍵 6 つと `HistorySelectionBehavior` 型が消えた
- **生成ラベルの横に i 印を置き、自動補正が何をするか 8 項目で説明する**（Build 787）

#### 受け入れで測ったこと

**マージ結果の web 9 ファイルは、作者が目視裁定し pentala が稼働させている合成物と SHA-256 が全件一致する。** 衝突は 5 枝の相互のみで（`web/BUILD_NUMBER`・`+page.svelte`・`SettingsModal.svelte`・i18n 3 ファイル）、**分岐点 `0505961` から main までの 32 commit はこの 9 ファイルに 1 行も触れていない**（触れたのは `server/scripts/gen_android_reference.py` 1 件で、本版の server 変更とは重ならない）。

**摂動 3 件はいずれも狙った 1 件だけを赤くした** — ①`ids` の要求順を `sorted(set(...))` へ壊す → `test_history_animation_export_preserves_requested_order` ②**形を変えずデータだけ壊す**フレーム順の反転（フレーム数も形式も不変）→ `test_builds_looping_apng_in_input_order` の pixel 検査 ③遷移フレームを常に空にする → `test_builds_gif_with_transition_frames`。

**テストが通らない面を直接叩いて確かめた** — 4 パターン × 2 形式の 8 通りはすべて出力を返し、上限ガードは 8K × 40 件で発火する。**単体テストが通るのは `cut` と `crossfade` だけで、`fade_white` と `slide`、上限ガード、4K / 8K、404 / 409 の経路には検査が無い。**

#### 直していないこと

- **`fade_white` と `slide` に自動検査が無い**（本サイクルでは手で 1 回叩いて出力を確認しただけ）
- **`get_items` はゴミ箱の作品を除外しない。** UI からは選べないが、id を直接送れば書き出せる
- **8K の記憶域は上限で RGBA 約 2.4 GB になる**（実測: 8K × 3 件クロスフェードでピーク RSS 1.37 GB）。pentala は 64 GB 搭載で 59 GB 空きのため本版では上限を下げていない
- **SPEC は更新していない** — API 一覧も出力形式の節も持たず、render engine・DDL・coerce のいずれも動いていない

pytest **1775/31**（+4）、cli 76、ruff clean、`npm run check` **220/0/2**（+1 ファイル＝`animationExport.ts`）、`lint:i18n` **934/47/0/0**（+22 −6）、`lint:models` 68、`lint:recommendations` 37。**決定的な層に差分が無いので凍結コーパスは焼き直していない。** **Android に差分は無い**（`android/VERSION` 据え置き）。**版数は patch** — 追加は任意の API と UI だけで、保存形式も API の互換も切れていない。

---

### v2.9.14 — 十三のカタログが、九色ぜんぶを持つ（Build 790、2026-07-31）

**engine 17 は palette から選ぶ経路を作ったが、選ばれる先の表そのものは engine 16 のままだった。** カタログは 8 色の palette と 6 キーの `map` を持ち、**帯を持たないカタログが多かった** — 記述が緑を求めても緑が無ければ最近傍の黄が代役に立つ。保存済み 7463 instruction を通すと、**記述が求めた帯をカタログが持たない事例が 140 件**あった。**本版が入れ替えるのはデータだけである** — 解決連鎖・帯の定義・無彩のしきい値・seed の材料はすべて engine 17 のままで、`renderer.py` に差分は無い。

**13 のカタログがそれぞれ 10 色の palette を持ち、内訳は無彩ちょうど 3・有彩ちょうど 7 に固定される。** 有彩 7 色は 6 帯すべてを埋め、1 帯だけが 2 色を持つ。`map` は **6 キーから 9 キーへ**広がり、**9 キーすべてがその本の palette の中から選ばれる**（従来は `map` と palette が別々の色を持てた）。UI の色見本は `map` の派生になり、**有彩 6 キーを先頭へ**並べる — Android が `swatches.take(4)` と `take(8)` で 2 つの画面に描くので、無彩が先頭に来るとその枠を黒・灰・白が占めて帯の色が画面から外れる。**130 色は全カタログを横断しても 1 つも重複しない。**

**`desert_mineral` を退役させ、`moss_bark` / `neon_plate` / `lantern_dew` を足した。** 退役は無彩が 1 色しかないカタログで、engine 17 の潰れ防止規則がそのために置かれていた。**退役した 10 の id は `get_color_catalog` も `render_color_map_for_catalog` も `None` を返す**（`default` へ落とさない）。本番の 117 作品がこの id を記録しているが、**保存済みの migration は行っていない** — 未知 id は従来どおり既定カタログで描かれる。

#### 到達（間違った帯からの回収であって、彩りを増やす版ではない）

保存済み 7463 instruction を engine 17 と engine 18 の両方へ通した実測。

| 指標 | engine 17 | engine 18 |
|---|---|---|
| 記述が求めた帯をカタログが持たない | 140 | **0** |
| 有彩の的中 | 2184 / 2455（89.0%） | **2455 / 2455（100%）** |
| 無彩の的中 | 4917 / 5008（98.2%） | **5008 / 5008（100%）** |
| 解決される distinct hex | 79 | **91** |
| 一度も選ばれない palette 色（使用中カタログ） | 7 | **9** |

**未選択は減らずに増える。** 使用中カタログの 9 は紫 8 件と `sea_stone/Coral Orange` で、**本番の紫の需要が 0.9% しかない**ことによる。全体では 39 で、差の 30 は**新設 3 本ぶん**である（本番に 1 作品も無いカタログなので構造的に到達しない）。**動いたのは間違った帯からの回収だけ**で、`default` が黄 +106 / 緑 −106、`sea_stone` が緑 +58 / 黄 −58、`cool_material` が赤 +37 / 橙 −37 と緑 +35 / 黄 −35 という形になる。全体の帯分布は無彩 67.2%（不動）・赤 11.4 → 11.8・青 10.0（不動）・緑 8.4（不動）・黄 2.3（不動）・**橙 0.8 → 0.3**・紫 0.0 → 0.03。

#### `sea_stone` の紫は空のままにした

**1 本だけ帯を埋めない。** 作者裁定により `sea_stone` は紫を持たず、engine 17 が用意した最近傍の代役が `Night Sea #191970` を返す。**これはこのカタログの `blue` と同じ hex**なので、青と紫を並べた作品では 2 つの図形が同じ紺になる。**目視で確かめてある** — 形は重なっても分かれて読めるので、絵は壊れず「同じ色を 2 度使った」ように見える。

#### 紙に溶ける役が 8 から 2 へ減り、[I-062] が閉じた

**engine 17 の退行** — `cool_material` の `black` が `#e5e8e8` になり紙との ΔL が 0.062 だったもの — **は消えた**（同カタログの黒は `#26282a`）。**残る 2 件はどちらも黄で、`vivid_material` の `#fff200`（ΔL 0.026）と `open_air_light` の `#ffce00`（ΔL 0.127）である**。黄の帯が明るいのは黄の性質なので直していない。**10 seed × 13 カタログで測っても、溶ける役はこの 2 つだけだった。**

#### 受け入れで見つけた検査の欠落（その場で足した）

**受け入れ側の摂動 4 件はすべて製品データだけを壊す形にした**（テストにも型にも触れない）。①有彩 hex を 1 段ずらす → 期待値表とコーパスの 2 件 ②`cool_material` の `Spruce` を彩度しきい値の下へ押す → 帯が 1 つ空になり 4 件 ③**黒を紙の明るさへ戻す（＝ [I-062] の再現）** ④対照として同じ黒を暗いまま別の hex へ動かす。

**③で赤くなったのは期待値表と凍結コーパスの 2 件だけだった。** どちらも**カタログのデータを変えれば丸ごと焼き直されるもの**なので、**「役が紙に溶けない」という性質を名指しする検査は 1 つも無かった**。engine 17 でこの退行が 5 指標すべて緑のまま通ったのと同じ形なので、**受け入れの側で検査を 1 件足した** — 13 カタログ × 8 seed の割当を紙との明度差で数え、**溶ける 2 件を hex で名指しして固定する**。③でこの検査が赤くなり、④では緑のままである（同じ役を動かしただけでは発火しない）。

#### コーパスと版

- **`render_engine_version` 17 → 18。`ddl_engine_version` は 4、`ddl_version` は 3 のまま** — DDL の語彙も文法も動かない
- **参照コーパス `render-engine-18/`** — **493 件**（A 88 / B 72 / C 58 / D 28 / E 119 / **F 128**）、**動いた 70 / 不変 423**
- **A〜E の 365 件は 1 件も動かない。F 群の外で動いたものは 0 件**である
- 動いた 70 の内訳は、**既存 id のまま演奏が変わった 42 件**と、**新設 28 件**（新カタログ 27 ＋ `F-hint-missing-purple-sea-stone`）。消えたのは 10 件（`desert_mineral` 9 ＋ `F-hint-missing-purple`）
- **演奏が変わらないまま記録だけ変わったケースが 58 件ある** — `input.color_map` が 6 キーから 9 キーになったもので、digest は 1 バイトも動かない
- `color_map_digest` は `bbb2f7be3cab3d70c7330520728ac4b0` → **`96f2809778344689d8fc1dbab03827b0`**

#### 直していないこと

- **Android の追随**（台帳 [I-070]）。`ColorCatalogs.kt` は **11 本・`map` 6 キー・`desert_mineral` 在り・`swatches` は手書き 8 色**のままで、engine 18 のデータは 1 つも入っていない。**移植されていないのは連鎖ではなくデータ**である（`oklchFromHex`・帯・彩度しきい値は `ServerRendererStyle.kt` に在る）
- **保存済み `history` の migration**（退役 id は既定カタログで描かれる）
- **`renderer.py`**・`_oklch_from_hex`・確定案の色と名前と並び

pytest **1847/31**（+72 = 新設 `test_color_catalog_content.py` の 68 ＋ カタログが 11 → 13 になったパラメタ化 `test_abstract_color_vocabulary.py` +2 と `test_palette_color_assignment.py` +2）、cli 76、ruff clean、`npm run check` **220/0/2**、`lint:i18n` **934/47/0/0**、凍結コーパスは再生成バイト一致。**版数は patch** — engine の版上げであり、保存済みデータの形式は変わらない。

### v2.9.15 — 四桁で作品を引き、画面の手触りが揃う（Build 801、2026-08-01）

独立した 7 枝（Build 791〜800）を統合した版で、**台帳の 4 項目が閉じた**（[I-049] / [I-050] / [I-058] / [I-071]）。

#### 履歴を四桁で引く（[I-071]・Build 798）

作品パネルが表示している **render hash の下位 4 桁**で作品を引けるようにした。適用は 3 経路
（`/api/history`・`/api/history/lineage-groups`・`/api/history/lineage-groups/{root}/items`）。

- **ASCII 英数字ちょうど 4 文字のときだけ** hash の照合を足す。大文字小文字は区別しない
- 従来の検索（記述・DDL・Stage 1/2 model・catalog ID）は **4 文字でもそのまま効く**。hash は OR で足される
- **3 文字・5 文字・記号入りは hash 検索に入らない**
- **この形のときだけ FTS を迂回する** — `_use_history_fts` は 3 文字以上で FTS を使うので、
  迂回しないと hash 列を見ない索引に当たって 0 件になる
- 検索ラベルは日本語 `検索（記述 / ハッシュ値下位4桁）:` / 英語 `Search (description / last 4 hash characters):`
- 3 経路が持っていた**同じ 5 条件の OR が 3 箇所に複製されていた**ので `_history_search_clause` 1 本へ畳んだ

#### 併記ラベルが lint の視界に入る（[I-058]・Build 797）

`lint:i18n` に**第 4 経路**を足した。`en.ts` の値・`isJapanese ? …` の三項・`getLang() === 'ja'` の分岐に加え、
**コンポーネントのマークアップに直接書かれた `日本語 / English` 形のテキストノード**から英語側を抽出する。

- **この経路が運ぶのは 17 件**（実測: 経路を外すと 949 → **932**）
- モデル情報カード・モデル選択カード・設定画面が対象
- `isJapanese ? '状態 / Status' : 'Status'` の 2 箇所を `状態 / Status` へ統一した ＝
  **英語 UI に日本語が出る箇所が 2 つ増えた**（同じカードの他の 15 箇所は前から無条件併記だった）

#### 設定の数字に手がかかる（[I-050]・Build 799）

`NumberStepper.svelte` を新設し、DB バックアップ設定の 4 欄（保存間隔 1–365 日 / 最大保存世代数 1–100 /
時 0–23 / 分 0–59）へ **自前の −/+ ボタン**を置いた。**Firefox には `::-webkit-inner-spin-button` に当たる
CSS フックが無い**ので native spinner をやめた、という [I-050] の前提どおりの解である。
ボタンの寸法は `--btn-sm-padding` / `--btn-sm-radius` / `--btn-sm-font-size` を使う。
値は上下限へ clamp し、上下限では対応するボタンが disabled になる。直接入力とキーボード操作は残る。

#### 同じ動作に当たっていた 2 つの語（[I-049]・Build 800）

`settingsReloadSettings` を `settingsReload` と同じ表示へ揃えた（日本語 `設定再読み込み` → `表示更新`、
英語 `Reload settings` → `Refresh`）。**キー・クリック処理・取得 API は不変**で、動くのは表示文字列だけ。

#### Info モーダル（Build 793〜796）と、そのほか

- **題は `inku-lang` から `inku` へ**。フォントを 18px → 24px にし、左に 32px の Incu アイコン
  （`/favicon-192.png`。既存の静的資産で、animation は持たない）を置いた。横幅 520px → **780px**
- コンセプト本文と制作者説明を**作者指定の日英文**へ差し替え、作者名を日英共通の
  `及川 信一郎 (Shinichiro Oikawa)` にした。「用語と層」の前置きは「inku の UI で使われている代表的な用語。」へ
- **語彙表から「詞書」と「読み取り」の 2 行を落とした**。これで英語表示に `kotobagaki` が 1 件も無くなったので、
  **`GLOSSARY.md` と `i18n-lint.mjs` が持っていたその例外を外した**（受け入れ側で実施）
- 作者指定文が `image` を使うため、**`appInfoConceptBody` を `image` の例外へ足した**（辞書に理由つきで記載）
- **ユーザープラグインの ON/OFF スイッチを行の右端へ**（Build 791）。動作・保存 API は不変
- **ダークテーマでスターが常に消灯して見えた**（Build 792）。`:global(html[data-theme='dark']) .hash-row-star` が
  `.hash-row-star.starred` より詳細度で勝っていたためで、`:not(.starred)` を足して解いた。
  同じ規則の `font-size: 10px` は `var(--btn-sm-font-size)` へ寄せた（**11px になるので星はわずかに大きくなる**）

#### 受け入れ

**決め手は SHA-256 だった** — 7 枝を順に `--no-ff` で入れると衝突は **`web/BUILD_NUMBER` の 6 回だけ**で、
`SettingsModal.svelte`（791 + 799）・`en.ts` / `ja.ts`（793–796 + 798 + 800）・`i18n-lint.mjs`（793–796 + 797）は
すべて自動合成された。その合成結果が、**作者が目視し pentala が Build 800 で稼働させている木と
11 ファイル中 9 件で完全一致**した。残る 2 件は **`+page.svelte` の `APP_VERSION` 1 行**と
**`GLOSSARY.md` の docs commit `407f536` ぶん**（-2 / +11 行）で、どちらも帰属を確認した。

**摂動 3 件のうち 1 件が検査の欠落を掘り出した。**

1. `len(search) == 4` を `== 5` へ → **2 件が赤**（3 経路のうち API と系譜の両方）
2. `_use_history_fts` の迂回ガードを外す → **1 件が赤**。
   **FTS がテストでも本当に有効であることが、これで示せた**（系譜側は FTS を通らないので緑のまま）
3. **形を変えない摂動** — `ilike(f"%{search}")` を `ilike(f"%{search}%")` へ（末尾一致 → どこかに含む）。
   **120 件すべてが緑のまま**だった。**契約は「下位 4 桁」なのに、検査は「4 文字で引ける」しか見ていない**

3 を捕らえる検査を受け入れ側で足した — **同じ 4 文字を hash の途中に持つ行**をもう 1 つ置き、
検索が 1 件だけを返すことを固定する（摂動 3 で赤・無改変で緑）。

pytest **1847/31**（**件数は増えない** — I-071 は既存の 2 つのテスト関数へ assertion を足す形なので）、
cli 76、ruff clean、`npm run check` **221/0/2**（+1 ファイル = `NumberStepper.svelte`）、
`lint:i18n` **949/47/0/0**、`lint:models` 68、`lint:recommendations` 37。
**決定的な層に差分が無いので凍結コーパスは焼き直していない。Android にも差分は無い。SPEC は
検索対象のフィールドを列挙していないので不変更。版数は patch。**

### v2.9.16 — 地が、描く手に抵抗する（render engine 19、Build 804、2026-08-01）

絵画で地が持つ役割は、描く手に抵抗することである。吸い込む地では線が滲み、弾く地では掠れる。
engine 18 まで地と描画は独立に合成されるだけで出会っておらず、描画側が `canvas.ground` を読む箇所は
`renderer.py` の mezzotint 判定 1 つだけだった。**`canvas.ground` を持つ作品は保存 1847 件中 31 件 = 1.7%、
凍結 SVG では 0 件**なので、地の側に条件を置くと誰にも届かない。

engine 19 は**到達 99.7% の側に既定の支持体を置く**。紙は 1 種の定数で、吸う / 弾くは道具の側の性質である
（作者裁定 2026-07-31）。

#### 支持体と、道具ごとの受けやすさ

- `stroke_engine` にモジュール定数の表として持つ。**`Score` のフィールドには足さない**
  （`absorbency` は engine 15 で退役しており、`test_ground_seed.py` がその不在を固定している）。
  `canvas.ground.material` ごとの表は次段へ回し、差し替え口（`Support` 引数）だけ開けた
- 道具は 2 つの量（`absorb` / `tooth`）のどちらを受けるかで分かれる。**筆は吸われて膨らみ、
  蝋や硬い道具は弾かれて墨が切れる。`rotring` と `computer` は 0** — 機械に紙との接触は無い
- 強さは 4 段（g0〜g3）を実装し、**採択は g2**。他の段は単調性を検査できる形で残してある

#### 掠れは、墨の本体を切る

**幅を細めるだけでは見えない。** 弾かれるべき道具がちょうど最も細い道具（pencil 1.5px / chalk 3px /
crayon 4px）なので、幅を 0.25 倍にしても 520px ラスタで 0.20〜0.52px にしかならずアンチエイリアスに沈む。
掠れは「墨がつかない＝白が残る」現象であって「線が細くなる」ことではない。

そこで**包絡が閾値 0.55 を超えたサンプルでは墨を置かず、そこでストロークを切る**。
SVG の 1 個の `path` は subpath を複数持てる（`ring_path` が既にそうしている）ので、
**切っても要素は 1 つも増えない**。閉じた輪郭は even-odd の帯を保つため切らない。

#### 受け入れで見つけた穴 — 揺らいだ直線には掠れが届いていなかった

位置に `variation` を持つ直線は、renderer が**揺らいだ中心線のまわりに outline を作り直す**別の経路を通る
（`outline_for_centerline`）。そこで**切れ目が捨てられていた**。幅の応答だけが残るのでバイトは動き、
凍結コーパスでは「動いた」と数えられる — **見える側の半分だけが黙って落ちていた**。

- 実測（pencil・seed 20260731・直線 5 本）: **variation なし 10 subpath / variation あり 5 subpath**（切れ目ゼロ）。
  弧は `variation` の有無に関わらず切れており、この穴は直線だけのもの
- 到達は小さくない。**保存 1858 作品のうち 912 件 = 49.1%** が位置揺らぎ付きの直線を含み、
  そのうち弾かれる道具（pencil / crayon / chalk / silverpoint / drypoint）で描かれたものが **242 件 = 13.0%**
- 切れ目を `StrokeResult` に載せて `outline_for_centerline` へ持ち越した。**直線の分岐と同じサンプルで両岸を割る**
- **受入テストの図柄から弧を外した。** 弧は揺らぎの有無に関わらず切れるので、同じ図柄に残したままだと
  直線の切れ目が 1 つも無くても subpath が増え、**検査が自分の言い分を自分で供給する**（実測で踏んだ）

#### 版とコーパス

**`render-engine-19` を凍結した。493 ケース中 227 が動き、266 は不変。** 順序は「版は 18 のまま実装を入れて
差分を測る → 版を 19 へ上げて焼く → 焼いたコーパスを先に commit してから `check_frozen_corpora.py`」。
コーパスの出力先は `current_render_engine().version` から作られるので、**版を上げた瞬間に比較対象の旧版が
生成器の視野から外れる**。

- **`changed_from_previous` に `rotring` / `computer` のケースは 1 件も入っていない**
- 揺らいだ直線の穴を直した焼き直しで動いたのは **18 ケース中 7 件**（すべて pencil）。
  `brush_thick` は吸われる側なので切れておらずバイト不変。**動いた総数は 227 のまま** —
  その 7 件は幅の応答だけで既に動いており、**それが掠れの欠落を隠していた**

#### 受け入れ

**契約の満点 381/493 は、実装では 227/493 になった。** 差の帰属は実装セッションが実測で切り分けている —
参照実装 `ground_resist_probe.py` が g0 以外の段で直線ストロークの outline を頂点ごとの法線で作り直しており、
**抵抗の bias を全部 0 にしても 177/493 が動いた**。**381 のうち 46% は地の抵抗と無関係**だった。
本実装は固定法線のままにしてある。**§7-1 は作者受理により決着**（2026-08-01）。

摂動は実装セッションの 9 件に加えて受け入れ側で 4 件当てた。

1. **形を変えない摂動** — `TOOL_SUPPORT_BIAS["pencil"]` の tooth を 1.00 → 0.00（データだけを壊す）→ **5 件が赤**
2. **対照** — g2 の滲み量 0.70 → 0.72（同じ機構を動かすが性質は保つ）→ **37 件すべて緑**
3. 墨を切る閾値 0.55 → 0.10 → **2 件が赤**
4. 揺らいだ中心線への切れ目の持ち越しを外す → **3 件が赤**（要素数の対照は緑のまま）

pytest **1897/31**（ベースライン 1847 + 新規 37 + 帰属 1 + 受け入れで足した 12）、cli 76、ruff clean、
`check_frozen_corpora.py` 緑。**Android は本契約の範囲外**（`ServerStrokeEngine.kt` 642 行と
fixture の再生成は別契約。台帳 [I-072]）。

**未裁定として残るもの**: 材質輪郭は切れ目をまたいで描かれ続ける。墨が切れた白地の上を材質輪郭の破線が渡る。
切るなら polyline を path へ変える必要があり、コーパスの材質輪郭が全件動く（台帳 [I-073]）。

### v2.9.17 — ツールチップを、見るかどうかを選べる（Build 807、2026-08-01）

Codex の枝 `fix/ui-mode-control-tooltip`（2 commit / Build 805〜806）を統合した版。
**完了レポートは 2 commit のうち後半（ツールチップ表示切替）だけを扱っており、
前半（UI モード操作の明確化）にレポートは無い。** 本節は両方を記録する。

#### ツールチップの表示切替（Build 806）

左レールの UI モードボタンと設定ボタンのあいだに、共通ツールチップの表示 / 非表示ボタンを置いた。

- 選択はユーザーアカウント単位で DB へ持つ（`user_accounts.tooltips_enabled`）。
  **新規ユーザーも既存 DB の migration 既定値も `true`（表示）**
- `GET /api/auth/me` が `tooltips_enabled` を返し、`PATCH /api/auth/me/settings` で保存する。
  **未設定と旧データは表示として扱う**（`row.tooltips_enabled is not False`）
- 非表示にすると、ログイン後の root に `tooltips-disabled` が付き、
  **共通 `Tooltip` コンポーネントの bubble だけが一括で消える**。
  ボタンや機能そのものは無効化しない
- 切替は optimistic update で、API 保存に失敗したら以前の状態へ戻す
- 日英の文言を 2 つ足した（`tooltipsShow` / `tooltipsHide`。
  **ラベルと `aria-label` は「いま押したらどうなるか」を示す**）

#### UI モード操作の明確化（Build 805・レポート無し）

- **UI モードボタンのアイコンを、3 本線からパネル配置の印へ替えた** —
  汎用メニューや設定の歯車と見分けがつかなかった
- `Tooltip` に `disabled` 属性を足し、**UI モードのメニューを開いているあいだは
  そのボタン自身のツールチップを出さない**（唯一の呼び出し元がここ）

#### 受け入れ

**決め手はマージ結果と稼働木の突き合わせだった** — Codex の枝は engine 19 のマージ commit から
切られており衝突は 0。マージ結果は **pentala が Build 806 で稼働させている木と 111 ファイル中
110 件で md5 完全一致**し、残る 1 件も `+page.svelte` の `APP_VERSION` 1 行だけだった（差分 2 行）。

**摂動 4 件のうち 1 件が検査の欠落を掘り出した。**

1. `update_user_settings` が `tooltips_enabled` を書かない → **赤**
2. **形を変えない摂動** — migration の既定値を `DEFAULT 1` から `DEFAULT 0` へ（データだけを壊す）
   → **118 件すべてが緑のまま**
3. `_user_to_dict` が行を読まず `True` を返す → **赤**
4. 新規アカウントの既定を `False` へ → **赤**

2 を捕らえる検査を受け入れ側で足した。**既存の migration テストは列が増えたことしか見ておらず、
`user_accounts` に行が 1 つも無かった** ので、**既存アカウントが受け取る値は誰も見ていなかった**。
列より前から在るアカウントを 1 行入れ、migration 後の `ui_theme` / `ui_mode` / `tooltips_enabled` を
読む形にした（摂動 2 で赤・対照として `ui_theme` の既定を `light` から `dark` へ替えても赤・無改変で緑）。

pytest **1897/31**（**件数は増えない** — 既存の 2 つのテスト関数へ assertion を足す形なので）、
cli 76、ruff clean、`npm run check` **221/0/2**、`lint:i18n` **951/47/0/0**（+2 = 新しい 2 文言）、
`lint:models` 68、`lint:recommendations` 37、`check_frozen_corpora.py` 緑。
**決定的な層に差分が無いのでコーパスは焼き直していない。Android にも差分は無い。
SPEC は表示設定を列挙していないので不変更。版数は patch。**

### v2.9.18 — 打鍵名が `perform` になり、控えのカタログが正本に揃う（Build 808、2026-08-01）

**取り残されていた Codex の 2 枝を回収した版。** どちらも完了レポートが無く、`main` へ入らないまま
pentala の配備だけが先に進んでいた。台帳では **[I-023] が既決なのに未反映**、
**[I-025] が未決なのに実装済み**という食い違いになっていた。

#### `refine generate` の退役（[I-023]・Build 801）

- 公開サブコマンドを **`refine perform`** へ改めた。**旧 `refine generate` は隠し互換として残る** —
  `argparse.SUPPRESS` でヘルプから外し、`_choices_actions` から取り除いて一覧にも出さない
- 引数の定義は 1 つの関数から両方へ配る。**同じ入力なら 2 つの綴りは同じ POST を投げる**
- CLI README と日英の CLI リファレンス 4 本も `perform` へ揃えた
- cli のテストは **76 → 77**

#### 控えのカタログを正本へ揃える（[I-025]・Build 802）

`web/src/lib/models.ts` の `ollama-cloud` の静的控えは **3 本**しか持たず、server の正本は **18 本**だった。
サーバのカタログが届くまでの数百ミリ秒はこの控えがそのまま出るので、
**起動直後だけ `gpt-oss:20b` が一意所有に見える**（v2.9.1 からの性質）。

- 控えを **18 本**にし、**無料枠で叩けない 10 本へ `requires_subscription: true`** を付けた
- **これは 2026-07-29 の裁定の前提を変える。** 従来は「控えには印が付かないので、
  印で守る前提のものを置かない」として無料の 8 本だけを置いていた。
  **控え自身が印を持てるようになったので、印は起動直後から効く** —
  `isModelUnselectable()` は描画される model をそのまま読み、
  `PROVIDER_GROUPS` は `modelCatalog` / `availableModelCatalog` の初期値である

#### 受け入れ

**マージした木で初めて回した server の全テストが 1 件落ちた。**
`test_the_web_fallback_offers_only_models_that_can_be_used` は
「控えに `SUBSCRIPTION_ONLY` を置かない」を固定しており、上の設計変更と正面から衝突する。
**枝は裁定の意図（選べないものを選ばせない）を、消すのではなく印で満たしている**ので、
検査を新しい前提へ移した — **`models.ts` の控えが正本と同じ id 集合を持ち、
印の集合が `SUBSCRIPTION_ONLY` と一致し、印の無い集合が `FREE_TIER_REACHABLE` と一致すること**。

**摂動 4 件のうち 2 件が、検査の不在をそのまま示していた。**

1. `refine perform` が処理へ届かないようにする → **赤**
2. `_choices_actions` の除去を外して `generate` をヘルプへ戻す → **赤**
3. **形を変えない摂動** — `glm-5.2` から `requires_subscription` を落とす → **書き換え前は全部緑**
4. **形を変えない摂動** — 控えから `gemma4:31b` を 1 本落とす → **書き換え前は全部緑**

3 と 4 は `lint:models`・`lint:recommendations`・`npm run check` のどれも赤にしない。
**枝が足した 15 本と 10 個の印を守る検査は 1 つも無かった。**
書き換えた検査は 3 と 4 の両方で赤になり、無改変で緑になる。

pytest **1897/31**（**件数は増えない** — 既存の 1 関数を差し替える形）、cli **77**（+1）、
ruff clean、`npm run check` **221/0/2**、`lint:i18n` **951/47/0/0**、`lint:models` 68、
`lint:recommendations` 37、`check_frozen_corpora.py` 緑。
**決定的な層に差分が無いのでコーパスは焼き直していない。Android にも差分は無い。版数は patch。**

> **配備で分かったこと**: `models.ts` は **Build 802 で pentala へ入っていたが、その後の配備で
> 消えていた**（本日の md5 突き合わせで、pentala の `models.ts` が main と同一＝枝の変更が無い状態だった）。
> **[I-053]（並行 rsync が相手の機能を消す）が実際に起きた 1 件。** 本版の配備で戻る。

### v2.9.19 — 色の一覧が 1 枚になり、選択がページを越え、動画に高さの軸が入る（Build 812、2026-08-01）

**Codex の 3 枝をまとめて回収した版。** どれも独立した UI / 書き出しの改修で、互いのコミットを含まない。
決定的な層（`coerce/`・`renderer.py`・`stroke_engine.py`・`schema.py`・`saijiki.py`・`language_support/`）
には差分が無いので、**参照コーパスは焼き直していない。Android にも差分は無い。**

#### 色カタログのモーダルがコンタクトシートになる（Build 810）

- 左に一覧・右に詳細という 2 段の構えをやめ、**13 カタログの palette を 1 枚で同時に見せる**。
  選ぶ前に、選べるもの全部が見えている
- 各色に色面・hex・英名を出し、UI が日本語のときは和名も添える。カタログ名・catalog ID・副題を並べる
- 10 列を基本に、980px 以下で 5 列、380px 以下で 3 列へ折り返す
- 選択中はテーマの `--accent` とチェックで示す
- **決定ボタンの専用の塗り（`primary-inline`）を外し、キャンセルと同じ `ghost-btn` へ揃えた** —
  寸法は `--btn-sm-*` トークンのまま、色の直書きは 1 箇所も無い

#### 履歴管理の選択がページを越えて残る（Build 811）

- `setPage()` と `setPageSize()` から `selectedIds = []` を外した。
  **ページを移っても表示件数を変えても、選んだ作品は選ばれたままになる**
- 現在ページの全選択／解除は、**別ページで選んだ ID を残したまま、現在ページの ID だけを加除する**
- **選択を捨てる場所は残っている** — 通常／ごみ箱の切替、スター絞り込み、検索条件の変更では従来どおり空にする
  （クリアは 7 箇所から 5 箇所になった。減った 2 つがちょうど `setPage` と `setPageSize`）
- ページ外の作品は、Animation／コンタクトシート出力のときに既存の ID 取得処理が拾う

#### 動画書き出しに Y 軸の高さが入る（Build 812）

- 解像度に **150px・300px・500px** を足し、さらに **「任意指定」で 64〜12000px** を選べるようにした
- API は後方互換の任意フィールド **`height_px`** を持つ。省略すれば従来の `resolution` の経路をそのまま通る
- `build_animation()` は `height_px` があればその高さでラスタライズし、**縦横比と既存の総画素数上限
  （600,000,000）を維持する**
- 既存の `1k` / `4k` / `8k` は残る。過去の localStorage 設定と API 利用はそのまま通る

#### 受け入れ — 摂動 1 件が検査の不在をそのまま示した

web は **すべての選択で `height_px` を送る**（`1k` を選んでも `height_px: 1080` が付く）ので、
**プリセットの 3 段も新しい派生の枝を通る**。その派生は遷移フレーム数の階段
（`6 if height <= 1080 else 4 if height <= 2160 else 2`）を高さから作り直しており、
**この階段が既存の `TRANSITION_STEPS` と一致しなければ、既存の書き出しが黙って長さを変える**。

摂動 3 件を当てた:

1. `height_px` を無視する（機能を無効化）→ **赤**
2. 範囲検査（64〜12000）を外す → **赤**
3. **階段を定数 2 へ潰す** → **123 件すべて緑**

3 が緑だったのは、新しい 2 件のテストのうち高さを見るほうが `pattern="cut"` で書かれていて、
**遷移フレームが 1 枚も作られない条件だった**から。
**プリセット高さでの往復（`height_px` 有り／無しでフレーム数が一致すること）と、
階段の境界（64 / 1080 / 1081 / 2160 / 2161 / 4320）を留める 2 件を足した。**
書き足したあとは 3 つの摂動すべてが赤になり、無改変で緑になる。

pytest **1901/31**（+4 = 枝の 2 件と受け入れで足した 2 件）、cli **77**、ruff clean、
`npm run check` **221/0/2**、`lint:i18n` **956/47/0/0**（英語表示文字列 +5）、
`lint:models` 68、`lint:recommendations` 37、`npm run build` 成功。**版数は patch。**

### v2.9.20 — 群れの位置が、記述へ返る（render engine 20、Build 813、2026-08-01）

**どこに置くかを決めていたのは、記述ではなく seed だった。**
展開後の印 137673 のうち **77.8% が宣言座標を参照しておらず**、画面の墨の **93.3%** は
Score が書いた座標ではなく renderer の配置規則が置いていた。**記述された座標を全部 y+0.2 しても
墨の重心の移動は中央値 0.0000** で、**`render_seed` だけを変えると画素の 4.06% が動く**。

#### 配置を二段に分けた

- **散らばりの形を決める段**（従来の layout 分岐＝`_expand_arrangement_layout`）と、
  **群れをどこへ置くかを決める段**（新設の `_fit_group_to_anchor`）を分けた。
  **layout 分岐は 1 つも書き換えていない** — 散らばりの形・密度・律動・揺らぎ・筆致は本版の管轄外
- 後段は**展開された群れの重心を宣言アンカーへ合わせる**
- **`radial` の `(0.5, 0.5)` 直書きを撤去した**。`center` が述べられていればそれを回転中心にし、
  述べられていなければ宣言アンカーの周りを回る

#### 枠は方向ごとに縮める

- 群れをアンカーへ寄せると、端の記述では印が枠 **[0.02, 0.98]** の外へ出る（32 ケース中 23 件）
- **軸ごと・方向ごとに、はみ出したぶんだけ縮める**。枠から遠い側の広がりは保たれ、
  **広がり比の最悪は 0.660**。相似形で群れ全体を縮める案は **0.315** まで潰れるので採らない
- 枠へ丸める案も採らない — `scatter` の端ケースで印 8 個が同じ座標へ積み上がる
- **アンカー自身が枠外にあるときは救えない**（`at.region` が端まで届き `count=1` の群れ）。
  本番 100 作品で 1 件残った。**未裁定として台帳 [I-079] へ起票**

#### 通さないものが 1 つある

- **`at.region` を持つ `grid` だけは後段を通さない。** 格子はその領域を敷き詰めるために `at` を
  畳まずに残しており、通すと**記述が述べた領域から群れを追い出して、誰も述べていない図形の中心へ
  寄せることになる**。本版の目的と逆向きなので通さない。`at.region` を持たない `grid` は通る
- 既存の `test_grid_uses_at_region_instead_of_margin` が落ちて見つかった。
  実装側で `test_a_grid_keeps_the_region_the_description_gave_it` として明文化した

#### 版とコーパス

- **`render_engine_version` 19 → 20**。`ddl_engine_version` 4 と `ddl_version` 3 は不変
- **参照コーパス `render-engine-20/`** — **525 ケース**（A–F の 493 ＋ **G 群 32**）。
  **既存 493 件はバイト不変**
- **manifest の「動いた 32」は新規ケースが全部そこへ入るためで、機構の効果ではない**。
  効果は engine 19 と engine 20 で同じ 32 ケースを描いて突き合わせて測り、**30 / 32**
  （動かない 2 件 `G-{vertical,horizontal}-nopath-center` は片軸を元から宣言から取っていた）
- **本番 100 作品**: 群れの重心とアンカーの距離は中央値 **0.0719 → 0.0000**、
  枠外へ出る印は **18 / 3890 → 1 / 3890**、`relation` の生存は前後不変（10 / 10）

#### 受け入れで見つけて直したもの

- **T-2（枠外の印を持たない）が、検査する枠を製品から import していた。**
  `FRAME_LO` / `FRAME_HI` を 0.005 / 0.995 へ緩める摂動でも**期待値が一緒に動いて緑のまま**で、
  赤くなるのは G 群の digest だけだった。**digest はコーパスを焼き直せば戻る記録**であって
  性質の検査ではない。テストが枠 **0.02 / 0.98 を自分で述べる**形へ直し、
  **製品の定数がその値であることを名指しする検査**を足した。
  直した後は同じ摂動が 2 本を赤にし、**枠を逆に狭める対照の摂動では T-2 が緑のまま**であることも確かめた
- 受け入れで当てた摂動は 3 種（枠の定数＝形を変えないもの・重心を群れの原点へ差し替え・
  `grid` + `at.region` の迂回の撤去）。後の 2 つはそれぞれ 3 本 / 2 本を赤にした

#### 契約の期待値と一致しなかったもの

- **契約 §2.4 の本番実測値と 1 桁まで一致していない。** 効果の向き・大きさは再現したが、
  **「画素差」と「墨の重心」の定義が契約に残っていなかった**（ハーネスが同梱されていない）。
  **engine 19 側の値も契約の数字と違う**ので、差は実装ではなく指標にある
- `Arrangement.center` の description が「省略=0.5,0.5」のままで、段 2 の後は誤り。
  **Stage 2 の tool schema へ渡る文字列なので触っていない**（記述文の変更は充填率を動かしうる）。
  **台帳 [I-080] へ起票**

pytest **1910/31**（+9 = 実装の 8 件と受け入れで足した 1 件）、cli **77**、ruff clean、
`npm run check` **221/0/2**、`check_frozen_corpora.py` バイト一致。**版数は patch。**

### v2.9.21 — 履歴のサムネイルが、どの engine で描かれたかを言う（Build 815、2026-08-01）

Codex の枝 `feat/history-tooltip-render-engine`（1 commit）を統合した版。

画面下部の履歴サムネイルの tooltip へ `render engine` の行を 1 つ足した。

- 値は `id / version` の形（`default / 20`）。**片方だけ記録されていれば在るほうだけを出し、
  両方とも無ければ「記録なし」**（`historyVersionNotRecorded`。日英とも既存の文言）
- 変更は `HistoryStrip.svelte` の **3 行だけ** — `HistoryItem` 型へ
  `render_engine_id` / `render_engine_version` を足し、tooltip の行を 1 つ足した。
  **API・DB・履歴の保存形式・描画は 1 行も変えていない**

#### 受け入れ

- **完了レポートは起点を main `d5940d8`（Build 808 / render engine 19）と書いているが、
  実測した `merge-base` は現在の main `0e4a686`**（engine 20 のマージ後）である。衝突は 0
- **表示される値が本当に届くことを測った。** strip の items は `GET /api/history` の
  `data.items` そのままで、応答モデル `HistoryItem` が 2 列を持つ。
  probe で一覧応答に `default` / `20` が載ることを確認した
- **この依存は既存の検査が既に守っていた** —
  `test_api.py::test_paint_can_save_server_generated_history` が
  **一覧応答の** `render_engine_id` / `render_engine_version` を見ている。
  `db._row_to_dict` から 2 列を落とす摂動で赤（`KeyError`）。**新しい検査は足していない**
- **表記は既存の作法と一致する** — `CanvasPanel.svelte` の来歴表が
  同じ裸ラベル `render engine` と同じ `id / version` 形式を既に使っている。
  `lint:i18n` は裸の表示文字列を見ないので、判断は既存の呼び出し元を数えて行った
- **tooltip の幅は悪化しない** — ラベル列は `54px` 固定で、
  新ラベル `render engine`（13 字）は既存最長の `Color catalog`（13 字）と同幅である。
  行が 1 つ増えるぶんは上へ伸びるが、strip は画面下端にある

pytest **1910/31**（不変）、cli **77**、ruff clean、`npm run check` **221/0/2**、
`lint:i18n` **956/47/0/0**（不変）。
**決定的な層に差分が無いのでコーパスは焼き直していない。`android/` にも差分は無い。
SPEC は履歴 tooltip の行を列挙していないので不変更。版数は patch。**

### v2.9.22 — 色カタログが、記述を読んで決まる（Build 817、2026-08-01）

台帳 **[I-082]** / 契約 `color-auto-select.md`（改訂 2）の実装を統合した版。

**自動描画（デモ）とバッチの色カタログが、乱択から「記述を読んで選ぶ」へ変わった。**
選択はサーバーで行い、`/api/paint` の `catalog_mode` が決め方を運ぶ。

- `catalog_mode` は **`fixed` / `auto` / `random`** の 3 値で、旧来の真偽値
  `random_color_catalog` を置き換えた。**送らない旧クライアントは `fixed`**
- **`auto`** は新設の `color_selector.py`（150 行）が 13 カタログの札を作って Stage 1 と同じ
  モデル解決で問い、**allowlist を通った id だけを採る**。失敗・タイムアウト・実在しない id は
  **要求された `catalog_id` へ倒れる**（落とし先は `default` ではない）
- **`random` は推敲専用として残した**（作者裁定 4）。「色カタログを変える」は
  **同じ記述を別の色で見るための操作**で、記述を読ませると毎回同じカタログへ収束して機能が消える
- デモ設定の鍵も `catalog_mode`（`fixed` / `auto` の 2 値）へ移した。
  **保存済みの旧鍵は `_normalize_demo_settings` が既定から作り直すので migration は不要**
  （配備先の本番 DB で実測）

#### 記述を読んだ選択は、乱択とキーワード一致のあいだにある

製品の `select_catalog_id` を段 0 と同じ 60 件・同じモデル・同じ `temperature=0.3` で通した実測:

| 指標 | 段 0（試作・v2.9.19） | **実装（Build 814）** | 一様乱択 | キーワード一致 |
|---|---|---|---|---|
| 使われたカタログ | 11 / 13 | **11 / 13** | 13 / 13 | 1 / 13 |
| 正規化エントロピー | 0.856 | **0.853** | 0.973 | 0.000 |
| 同じ原文 3 回で同じ id | 19 / 20 | **19 / 20** | — | — |

**一度も選ばれないカタログが 2 つある**（`fresco_study` / `ink_porcelain`。段 0 と同じ 2 つ）。
1 回の所要は中央値 1.26 秒・最大 55.74 秒で、100 呼び出し中 1 件が `APITimeoutError` になり
設計どおり落とし先へ倒れた。

#### 受け入れで見つけて直したもの

- **`inku-cli refine perform --kind color` が黙って壊れていた。**
  `cli.py:2965` が削除済みの `random_color_catalog: True` を送り続けており、
  **`PaintRequest` は未知フィールドを捨てる**（pydantic 既定 `extra="ignore"`）ので
  **200 が返り `catalog_mode` は `fixed` のまま**＝同じカタログを描き直していた。
  実測 = `PaintRequest(description="test", random_color_catalog=True).catalog_mode` は `"fixed"`
- **契約 §7.5 は web の呼び出し元だけを数えていた。** 実装セッションがそこで 2 つ目
  （`colorCatalogCandidateIds`）を見つけて作者裁定で残置したが、
  **CLI は誰も数えていない 3 つ目だった**
- **cli の 77 件は payload の鍵を 1 つも見ていない**ので緑のままだった。
  鍵を読む検査を 1 本足し、旧鍵へ戻す摂動で赤・
  **鍵を残して無関係の値を変える対照の摂動では 78 件すべて緑**であることまで見た

#### 受け入れで当てた摂動

実装が当てた 4 件（選択器を恒等化 / allowlist の撤去 / 札を 12 件で打ち切り /
enum から `random` を落とす）に加えて、受け入れ側で**形を変えない摂動**を 1 つ当てた。

- **デモ既定を `"fixed"` から `"auto"` へ反転**（データだけを壊す）→
  **`test_api.py::test_current_user_demo_settings_are_persisted` が 1 本だけ赤**。
  既存利用者のデモが黙って LLM を呼ぶようになる変更は守られている

pytest **1927/31**（+17 = 新設 `test_color_auto_select.py`）、cli **78**（+1 = 受け入れで足した 1 件）、
ruff clean、`npm run check` **221/0/2**、`lint:i18n` **956/47/0/0**（鍵の 1 対 1 改名なので不変）、
`lint:models` 68、`lint:recommendations` 37、`check_frozen_corpora.py` バイト一致。
**render engine は 20 のまま・`ddl_version` 3・`ddl_engine_version` 4 も不変。
`android/` は追随していない**（旧鍵のクライアント乱択が残る。台帳 [I-072] 系）。
**SPEC は英語の運用節（Modes）に `catalog_mode` を書き足した。版数は patch。**

### v2.9.23 — 共有カウンタが競合しなくなり、最初に読む量が半分以下になる（Build 820、2026-08-01）

3 段の契約（`module-split-and-merge-conflicts`）を 1 サイクルでマージした。
**依頼の前提は「巨大モジュールが衝突の原因」だったが、実測はそう言わなかった。**
過去 80 マージを `git merge-tree` で再現すると **21 件が競合し、そのうち 20 件（95%）は
1 行の `web/BUILD_NUMBER`** だった。`+page.svelte` の 2 件も行の重なりは各 1 行で、
原因は大きさではなく**両側が別々の設定を足して隣り合う「共通の追記点」**だった。

#### 段 1 — 衝突を出所で止める

- **`web/BUILD_NUMBER` の merge driver**（`.gitattributes` の 1 行 +
  `scripts/git/build-number-merge.sh`）。共有カウンタなので「両側が採番した」は
  意見の相違ではなく、**大きいほうが常に答え**である
- **driver の命令は `.git/config` にあり版管理されない**ので `scripts/git/setup.sh` を置いた。
  worktree は `.git/config` を共有するので 1 回で全 worktree に効くが、**新しい clone では未設定**
- **設定が失われる向きはすべて安全側**。未設定の clone・`driver` の unset・`.gitattributes` の
  行の消失は、どれも**従来どおり競合するだけ**で誤ったマージにはならない。
  **`merge.buildnumber.name` は意図的に書いていない** — name があって driver が無いと
  git は既定のテキストマージへ落ちず `fatal: custom merge driver buildnumber lacks command line`
  でマージ自体を中止する（実測）。name を落とすことで、設定の失われ方が 1 つ残らず安全側になる
- **設定永続化の追記点を機能ごとに畳んだ** — 7 キーを
  `web/src/lib/features/{color-catalog,tenkei,wild,export,result-log,batch}/` へ移した。
  **効いたのはファイル数ではなく「そのファイルが誰の通り道か」である。**
  キー・`$state`・load・persist・touch の 5 か所が、どの機能ブランチも触る `+page.svelte` から
  **他の枝が触らない 1 ファイル**へ移った

#### 段 2 — 最初に読む量

開閉フラグの後ろにある 9 個（`SettingsModal` `LineagePanel` `DdlEditorDialog` `HistoryManager`
`ReplayComparisonModal` `ProfileModal` `ColorCatalogModal` `AIRefineModal` `SaijikiDrawer`）を
`await import()` へ移し、`features/export/download.ts` と
`features/model-inspection/state.svelte.ts` を `+page.svelte` から抽出した。

- **最大チャンク gzip 166,815 → 79,533 B**（−52%）、チャンク数 1 → 31。
  client JS の**合計は 688,180 → 702,784 B と増える** — この指標は「最初に読む量」であって総量ではない
- **`SaijikiDrawer` だけは `{#if}` で包んでいない。** この抽斗は常に DOM にあり
  CSS の `transition: width` で開くので、開いた瞬間に mount すると**その 1 回だけ動きが出ない**。
  独立チャンクにはなるが取得は読み込み時である
- **`+page.svelte` は 7,411 → 6,836 行**（script 6,110 → 5,519 行）

#### 段 3 — 認可の網羅

エンドポイントの認可を網羅する検査が 1 本も無かった。`server/tests/test_route_authorization.py`
を新設し、**生きた `app.routes` を歩いて**（ソースの正規表現ではない）依存木を再帰的に辿る。
ガードなしの集合が `PUBLIC` の 6 本と**完全一致**すること、ルート総数が **80** であること、
`PUBLIC` が実在するパスだけを挙げていることを見る。

**`api.py` の APIRouter 分割そのものは行っていない**（下記「残したもの」）。

#### 受け入れで測ったこと

実装セッションの報告を鵜呑みにせず、受け入れ側で測り直した。

| 受入 | 実測 |
|---|---|
| A-1 `merge-census.py 80` | `conflicted=4` / `web/BUILD_NUMBER` **0 件** |
| A-2 対照① `merge.buildnumber` の両キーを unset | **21 / 20** → 戻して **4 / 0** |
| A-2 対照② `.gitattributes` の 1 行を削除 | **21 / 20** → 戻して **4 / 0** |
| C-2 製品ガードを剥がす摂動 | `/api/auth/me` から `Depends(_current_user)` を外すと **`test_every_route_is_guarded_or_listed_public` が赤**（余分な要素として `/api/auth/me` が出る）→ 戻して緑 |

**赤くなった名前**は `merge-census.py` の `conflicted` と `web/BUILD_NUMBER` の行、
および `test_every_route_is_guarded_or_listed_public` である。

使い捨ての repo で**実 `git merge`** も測った（base 800 / ours 803 / theirs 805）:
未設定 = CONFLICT、`name` のみ = `fatal`、設定済み = clean で **805**。
**マージ 2（段 1 の 818 と段 2 の 819）が実地の確認になった** — driver が働いて
衝突なしで 819 に解決した。

- **属性の出所は作業ツリーの `.gitattributes` である。** `merge-tree` は replay 対象の
  歴史のツリーではなく checkout の `.gitattributes` を読むので、**行を持たない checkout や
  古い枝の worktree では従来どおり競合する**（安全側）

#### 残したもの

- **段 2 の受入 B-2（script 部 ≤ 3,000 行）に達していない**（5,519 行）。
  契約 §2.2 が名指しした 5 単位を**全部切っても 4,388 行**で 3,000 には届かない、と実測が出ている。
  **受入の数字と、切る場所の表が別々に決められていた。**続きと切る単位の実測は台帳 [I-088]
- **`api.py` の APIRouter 分割は未実施**（4,474 行・80 ルート・ルートが共有する
  モジュール直下のヘルパが 80 個以上）。**分割の前後で 80 本と `PUBLIC` の一致を測れる検査は
  先に入った。**台帳 [I-089]
- **`.svelte.ts` のモジュール直下 `$state` と SSR** — 段 1 で作った 6 本と既存の
  `mascot.svelte.ts` / `i18n/index.svelte.ts` が同じ形で、この app は adapter-node で SSR が有効。
  段 1 が持ち込んだ問題ではなく既存の流儀に合わせた結果である。台帳 [I-090]
- **`/api/prompts` が未認証**のまま allowlist に理由つきで残っている（台帳 [I-086]）

pytest **1935/31**（+8 = `test_merge_driver.py` 5 と `test_route_authorization.py` 3。
`def test_` の一覧を `comm -23` で突き合わせ、**消えた名前が 0 件**であることを見た）、
cli **78**、ruff clean、`npm run check` **0/2/229**（files +8 = 新設の 8 本）、
`lint:i18n` **956/47/0/0**、`lint:models` 68、`lint:recommendations` 37、
`check_docs.py` 緑（内部参照 56）、`check_frozen_corpora.py` バイト一致。
**`lint:i18n` が動かないのは期待どおり** — 設定と機能を移しただけで表示文字列を 1 つも足していない。
**`check_frozen_corpora.py` が動かないのも期待どおり** — 決定的な層を 1 行も変えていない。
**render engine は 20 のまま・`ddl_version` 3・`ddl_engine_version` 4 も不変。
`android/` `macos_swift/` には触れていない。振る舞いを変えない改修であり、作者の目視（描画・設定・
履歴・系譜）を受けている。SPEC は日本語側の 1 件を訂正した。版数は patch。**

### v2.9.24 — 80 本のエンドポイントが、共通の通り道から出る（Build 821、2026-08-01）

契約 `api-router-split`（台帳 [I-089]）。**`api.py` 4,474 行 → 251 行**で、
**エンドポイント 80 本すべて**が `api_core/routers/` の 10 ファイルへ移った。
`api.py` に残るのは `app` の組み立て・`_lifespan`・ミドルウェア 3・起動時の 4 呼び出し・
`include_router` 10 行だけである。**振る舞いは 1 バイトも変えていない。**

#### 何をどこへ移したか

- **router 10 本** — `render` 8 / `settings` 9 / `history` 11 / `public` 9 / `me` 12 /
  `lineage` 8 / `users` 8 / `plugins` 8 / `auth` 4 / `feedback` 3。**割り当て不能は 0 本**
- **共有モジュール 5 本**（`api_core/{state,models,deps,common,rendering}.py`・計 806 行）。
  契約が推奨した `api/deps.py` という名前は採れない（`inku_server/api.py` と
  `inku_server/api/` は同居できない）ので `api_core/` へ読み替えた
- **依存の向きは `api.py` → `api_core/routers/*` → `api_core/{state,models,deps,common,rendering}`**
  の一方向。**router から `api.py` を import している箇所は 0 件**
- **全 router を prefix なしで作り、パス文字列は 1 文字も変えていない**
  （`lineage` の 3 本が `/api/history/...` の下に居るため）

#### ⚠ 契約の前提が 1 つ外れていた — ガードは「移す」ことができない

契約は「per-route のガードを router 単位の既定依存へ**移す**」と書いたが、**80 本のうち 49 本は
移せない**。本体が `actor` の値を使うので `actor: dict = Depends(_current_user)` を
シグネチャに残さざるを得ないためである（実測: **本体で使う 49 / 引数はあるが未使用 25 /
ガード引数なし 6**）。

**したがって router 既定は「移した先」ではなく 2 つ目の強制点として足された。**
新しいエンドポイントが既定を継ぐ利得はそのまま残るが、**既存の per-route を消す作業は
別物**であり、本契約では 1 行も行っていない（実際に寄せられるのは未使用の 25 本のうち
router 既定と同じガードを持つものに限られる）。

この食い違いは**摂動で表に出た** — 契約の指示どおり `history` router から
`dependencies=[...]` を外しても**認可テストは緑のまま**だった。強制点が 12 個
（router 既定 1 + per-route 11）あり、片方を外してももう片方が同じガードを依存木へ入れる。
**12 個すべてに当てて初めて赤**になり、`unguarded` に現れた 9 パス（= 11 ルート）は
**全部 history** で他の群は 1 本も混ざらなかった。

#### 受入

- **D-1 ルート本体の所在** — 生きた `app.routes` を歩いて `route.endpoint.__module__` を数える。
  **何もしない値 80/80 に対し、`inku_server.api` に残るルートは 0 本**。
  [I-088] の裁定に従い**行数は受入に据えていない**
- **D-2 API 表面の同一性** — 契約が同梱した期待値（endpoints 80 / operations 80 / schemas 79）と
  **完全一致**。**期待値は契約発行時に測ったものをバイト単位でそのまま入れており、焼き直していない**
  （受け入れ側で sha256 の一致を確認した）
- **D-2 を据えた理由が摂動で裏づいた** — 応答モデルから 1 フィールド削ると
  **認可テストもエンドポイント数 80 も緑のまま**で、D-2 だけが赤くなる
- **対照の摂動**（`include_router` 10 本の呼び出し順を逆順）は**全部緑のまま**。
  D-2 の正規化が並びを見ていないことの確認になった

#### 移動だけでは通らなかった 5 件

1. **`_build_number()` の `parents[3]` → `parents[4]`** — リポジトリ根を `__file__` からの
   相対で出しており、1 階層深くなった。直すまで `/api/info` が `None` を返し 5 テストが赤かった。
   **`__file__` に依存する箇所は全走査済み**（`reference.py` は深さが変わらないので `parents[3]` のまま）
2. **`_logger` を `logging.getLogger("inku_server.api")` に固定** — `__name__` のままだと
   ログチャネル名が変わり、`caplog` でチャネルを名指ししている 3 箇所が外れる
3. **テスト 12 ファイルの参照付け替え**（36 シンボル・128 箇所）。**同じ名前が複数モジュールに
   束縛されている 9 シンボルは、束縛先ではなく「呼び出し側」で決めた** — 例えば `_save_slots` は
   `state` が定義し `rendering` が import しているが、読むのは `rendering` の束縛なので
   `state` を patch しても効かない
4. `test_color_hint_note_split.py` の `API_SOURCE` を移動先へ（census の値 10 は不変）
5. ruff の未使用 import 5 件

#### 残したもの

- **`SPEC.ja.md` の「API 経路」の記述**を `api_core/routers/` へ訂正した（本サイクルで実施）
- **`api.py` に残った到達不能な定義 7 件**（`_validated_color_map` `_bearer_token`
  `_can_manage_user` `_strip_anthropic_prefix` `InterpretResponse` `_OUTPUT_DIR` `_OUTPUT_PNG_SIZE`）。
  **分割前から死んでいたもので、純粋な再配置の範囲外として消していない**
- **per-route の `Depends` を router 既定へ寄せる作業**（上記の 25 本ぶん）。台帳 [I-091]
- **`/api/prompts` が未認証**のまま allowlist に理由つきで残っている（台帳 [I-086]）

pytest **1937/31**（+2 = 契約が指定した `test_api_surface.py` と `test_route_module_split.py`。
`def test_` の一覧を `comm -23` で突き合わせ、**消えた名前が 0 件**であることを見た）、
cli **78**、ruff clean。
**`npm run check` と `lint:i18n` は回していない** — `web/` の差分が `APP_VERSION` と
`BUILD_NUMBER` の 2 行だけで、表示文字列も型も動いていない。
**`check_frozen_corpora.py` も回していない** — 決定的な層に差分が 0 件で、
凍結コーパスの生成器は `inku_server.api` を読んでいない（実測）。
**render engine は 20 のまま・`ddl_version` 3・`ddl_engine_version` 4 も不変。
`android/` `macos_swift/` には触れていない。版数は patch。**

### 2026-08-01 — プロジェクトコンテキストが、変更履歴の二枚目であることをやめる（**採番なし**・ドキュメントのみ）

`PROJECT_CONTEXT.ja.md` が `SPEC.ja.md` より長くなっていた（**93,165 字 対 75,721 字**）ため、
役割を測り直して現在形の入口へ戻した。

**測ったこと。** `## 現在の製品状態` の 1 節が **1,434 行 = 文書の 93%** を占めており、
中身は v1.89 から v2.9.24 までの**版ごとの段落 77 本**だった。
段落は 1 本あたり 14 行（v1.89〜v2.4 系）→ 19 行（v2.5〜v2.8 系）→ **27 行（v2.9 系）**と倍増し、
**v2.9 系だけで 698 行 = 文書の半分**に達していた。
入口として機能している部分（目的・アーキテクチャ・守るべき契約・確認先・更新規則）は **99 行 = 6.5%** しかない。

**それが重複だったこと。** 版段落の見出し文は CHANGELOG の節見出しと一字一句同じで
（例: v2.9.24「80 本のエンドポイントが、共通の通り道から出る」）、
CHANGELOG 側 82 行に対して 20 行という**圧縮率だけが違う複製**だった。
`PROJECT_CONTEXT` 自身の文書更新規則が「現行契約は仕様、時系列の記録は変更履歴へ置く」と述べており、
**自分の規則に反していた**。

**古びていたこと。** 「未着手・未対応・未追随」の類が **53 箇所**あり、
抽出して現物に当てた 2 件は**どちらも既に偽**だった
（`太さの軸` は v2.9.3 で完了済み・`SPEC.md` の `artwork` は 0 件）。
版段落は書かれた時点で凍り、直っても記述だけが残る。

**したこと。** 版段落 77 本を削除し、`## 現在の製品状態` を現在形へ書き直した
（版・語彙・パイプライン各層・web・server・cli・android・検査面）。
**日本語 1,533 → 213 行、英語 1,913 → 253 行。**
記述はすべて現物から測り直してある（道具 11・色 9・図形 8・色カタログ 13 本・
参照コーパス `render-engine-20` 525 件と `ddl-engine-4` 33 件・エンドポイント 80 本）。
文書更新規則へ 2 条を足した — **版ごとの段落を積まない**ことと、**未解決の課題を本書に書かない**こと。

**副産物。** v2.9.24 で `対象バージョン` 行の更新が日英とも漏れていた（`v2.9.23 / Build 820` のまま）ので直した。
削除した段落から**未裁定のまま台帳に載っていなかった 2 件を救出**し、起票した
（マスターグリッドの格子が絶対座標に効く点の裁定・`db.get_items` がゴミ箱を除外しないこと）。

`check_docs.py` 緑（内部参照 56 → 54。削除した段落だけが名指ししていた公開文書があるため。
**参照が 0 になった公開文書は無い** — 失われた 7 件の宛先はすべて他の公開文書からも名指しされている）。
コード・仕様・描画には触れていないので、pytest・ruff・`npm run check`・凍結コーパスは対象外。

### v2.9.25 — 版数を名乗る 5 つの値が、1 つのファイルへ寄る（Build 822、2026-08-01）

台帳 [I-085]。**同じ画面に 2 つの版数が出ていた** — `/api/info` は `2.7.2` を返し、UI は `v2.9.24` を表示していた。

**測ったこと。** 「アプリのバージョン」を名乗る値が **5 か所**から独立に供給されていた。
`server/pyproject.toml`（2.7.2 → `/api/info` と server 起動バナー）、
`+page.svelte:77` の文字列リテラル（v2.9.24 → UI 表示）、
`reference.py:84`（**その `+page.svelte` を正規表現で走査**）、
`web/package.json`（0.1.0 → Vite 起動バナー）、`cli/pyproject.toml`（0.1.0）。
**`_app_version` という同名の関数が 2 つあり、読む先が違っていた**（`api_core/common.py` は pyproject、`reference.py` は Svelte ファイル）。

**受け手を 4 領域で数えた。** `/api/info` の `version` を**解釈している受け手は 0 件**だった —
web は `developer_mode` と `render_engine_version` しか読まず、cli は応答を丸ごと印字するだけ、Android は `/api/info` を叩かない。
起票時に「API の応答が変わるので受け手を数える必要がある」と書いた懸念は、**数えた結果消えた**。

**したこと。** **`web/APP_VERSION` を単一情報源にした。**
`web/BUILD_NUMBER` と同じ場所・同じ機構で、読み手も同じ 3 つ（`vite.config.ts` の `define`、`api_core/common.py`、`cli.py`）。
**新しい仕組みは 1 つも増やしていない。**

- `/api/info` は版を 2 つ返す — `version` が**アプリの版**（`web/APP_VERSION`）、`release_version` が**配布物の版**（`pyproject.toml`）。
リリースを保留している間は一致しないが、それは 2 つが別の概念だからである
- `+page.svelte` は `__APP_VERSION__`（vite の define）を読む。**7,411 行の中の 1 行を動かすと reference dump が壊れる結合が消えた**
- `reference.py` の正規表現走査を撤去した（未使用になった `re` の import も）
- Vite 起動バナーは `package.json` の 0.1.0 ではなく `APP_VERSION` を読む

**採番の手数を減らした。** `scripts/bump.py` が 1 コマンドで 4 系統 6 ファイルへ書く
（`APP_VERSION`・`BUILD_NUMBER`・PROJECT_CONTEXT の対象バージョン行の日英・版マーカー表の日英）。
`--scan-build` は全ローカル ref を舐めて max+1 を出す（**配備先は見えないので別途確かめる**と出力に明記する）。
**パターンが 1 回ちょうど一致しなければ落ちる** — 文書の形が変わったら黙って飛ばさない。

**齟齬の検査を足した。** `server/tests/test_version_consistency.py`（8 件）が 4 系統の一致を見る。
**v2.9.24 の漏れはこの検査があれば止まっていた。**
判別力を摂動 3 件で測った — ①PROJECT_CONTEXT の対象バージョン行だけを `v2.9.23 / Build 820` へ戻す（**v2.9.24 で実際に起きた漏れ**）で 1 件だけ赤、
②`+page.svelte` に版数のリテラルを戻すで 1 件だけ赤、③版マーカー表の Build を古くするで 1 件だけ赤。
**対照の摂動**として `pyproject.toml` の版だけを `9.9.9` へ動かすと**全 8 件が緑のまま**で、
リリース版数の遅れを退行と誤認しないことを確かめた。

**API 表面のダイジェストは焼き直した** — 差分は 1 スキーマに 1 プロパティ追加だけ（`AppInfoResponse.release_version`）。
endpoints 80 → 80、operations の差 0、消えた property 0 を項目単位で確かめてから焼いた（`535566b6…` → `d4c57fed…`）。

pytest **1945/31**（+8 = 新設の整合検査）、cli **78**、ruff clean、`npm run check` **229 / 0 errors / 2 warnings**、
`lint:i18n` **956/47/0/0**、`check_docs.py` 緑。
**決定的な層に差分が無いので凍結コーパスは焼き直していない。`android/` にも差分は無い。版数は patch。**

### v2.9.26 — 誰の記述も届かなかった 2 枚の札が、届くようになる（Build 823、2026-08-02）

**色カタログ 13 本のうち 2 本（`fresco_study` / `ink_porcelain`）は、記述を読んで選ぶ経路から
一度も選ばれていなかった** — 本番から採った 60 件の記述で 0 件（v2.9.22 の実測）。
**直したのは 4 文字列だけ**で、`server/src/inku_server/color_catalogs.py` の
2 本の `sub` / `sub_ja` を書き換えた（`map` も `palette` も名前も動かしていない）。

**原因は、契約を書く前に測って 1 つ絞り込んだ。** 起票時の見立て「札の記述がモデルに届いていない」は
**偽**で、`build_catalog_card()` の当該 2 行には `sub`・`sub_ja`・palette 和名 10 語が全部載っていた。
一覧の位置でもない（0 件の両隣が 7 件と 9 件、6 件と 9 件）。
**残ったのは色の重なり**で、全 78 ペアのうち最も近いのが `default`—`fresco_study`（平均 ΔE 11.4）、
次が `default`—`ink_porcelain`（12.2）、3 番目が両者どうし（13.3。全ペアの中央値は 25.4）。
**そして、この 2 本は死んでいなかった** — 利用者が手で選んだ 45 件のうち 6 件（13.3%）がこの 2 本で、
13 本均等の 3.5/45 を下回っていない。**自動で選ばれないのは基準の食い違いだった**:
札はモデルへ「主題・光・季節・材質を読め」と指示しているのに、
**手で選ばれた 6 件の記述に該当する主題語は 1 つも無い**（60 件全体で 漆喰・壁画・顔料 = 0 件、
磁器・陶・窯 = 0 件。対照として 波 29・筆 23・夜 10 は実在する）。
そこで、素材の産地語を場面の語へ寄せた —
「漆喰、顔料、温かい石」→「日なたの壁、乾いた土、温かい陰」、
「墨、磁器、鉱物の差し色」→「澄んだ光、墨、冴えた鉱物の差し色」（英語側も同じ向きに）。

**効果は同じモデル・同じ 60 件で 4 巡走らせて測った**（`nvidia / google/gemma-4-31b-it`・
`temperature=0.3`・直列）。**書き直す前 0 / 0 → 書き直した後 5 / 1。**
正規化エントロピーは 0.845 → **0.890**、最大占有は `default` 14 → 12、
**13 本すべてが 1 回以上選ばれた**（前は 11 本）。
**4 文字列を旧へ戻すと 0 / 0 へ戻る**ので、動かしたのはこの 4 文字列である。
対照として `sub` だけ新しく `sub_ja` を旧へ戻すと 3 / 0 になり、
**`fresco_study` は英語側だけでも 0 からは動く**ことが分かった
（`ink_porcelain` の 1 件は n=1 なので日英どちらが効いたかは判別できない）。

**⚠ この変更の効果は、既存のテストでは 1 つも測れない。**
`test_api.py` は `default` の 2 文字列だけを固定し、`test_color_auto_select.py` の T-3 は
`assert catalog["sub"] in card` と**製品のデータを期待値にしている**ので書き直しても自動的に緑になる。
**新しい検査は足していない** — 「素材語だけの `sub` を書かない」型の検査は
n=12 の観察を法則へ格上げすることになり、`vivid_material`（素材語だけで 4 件）が反例として既に在る。
文言そのものを固定する検査は、データを変えれば焼き直される記録であって性質の検査ではない。
**受入は走行が持つ。**

**契約の記述が 1 箇所間違っていた** — `contactSheet.ts:185` の `entry.sub` は
色カタログの `sub` ではなく履歴の日時（`formatHistoryDate`）で、名前が一致していただけだった。
表示箇所は実測で 4 つ（`ColorCatalogModal` / `ManualRefineModal` / `CanvasPanel` の生成情報 /
AI シートのノート md）。**新しい文言はどれでも切り詰められない**ことを CSS と文字列幅で確かめた
（ブラウザでの目視はしていない）。

pytest **1945/31**、cli **78**、ruff clean、`check_docs.py` 緑（内部参照 54）。
**`sub` は描画にも Score にも入らないので凍結コーパスは焼き直していない**
（`render-engine-20` と `ddl-engine-4` の manifest に `"sub"` は 0 件）。
**web は 1 行も触っていない**（`colors.ts` の複製は `default` 1 本だけ）。
**`android/` の同じ 2 行は [I-070] の範囲なので動かしていない。** 版数は patch。

### v2.9.27 — 作者が 1 通で出した 13 項目が、1 つの版へ入る（Build 825、2026-08-02）

**作者が 1 通で出した 13 項目**を 1 本の契約にまとめ、1 サイクルで入れた。台帳の項目ではない。
触ったのは `web` 28 ファイル・`server` の実装 6 ファイルと検査 7 ファイル・`cli` 1 ファイルで、
**描画には 1 行も触れていない。**

**色カタログ 13 本のカラーチップが、同じ並びになる。** `default` の 10 色の並びを基点として、
残る 12 本の `palette` の**要素の並びだけ**を入れ替えた（54 行の移動。`name` / `name_ja` / `code` は
1 文字も変えず、`map` と `SWATCH_KEY_ORDER` も未変更）。`palette` は 2 人の読み手を持つ —
モーダルのチップ格子と、Stage 1 のモデルが読む 1 行の札 — ので、並びが揃うと格子が揃い、
札の色の言及順も揃う。

**この並べ替えで凍結コーパスが 1 度赤くなったが、描いた絵は 1 枚も動いていない。**
動いたのは `server/reference/render-engine-20/manifest.json` の 1 ファイルだけで、
**SVG 525 件はバイト一致**、manifest の差分 592 行は**追加行集合と削除行集合が（末尾カンマを除いて）一致**し、
JSON として読むと **deep-equal**（`sort_keys` で正規化すると完全一致し、生の直列化だけが違う）。
manifest が `color_map` を dict の挿入順のまま落としており、その挿入順が `palette` に従うためで、
**これは性質の検査ではなく、データを変えれば焼き直される記録**にあたる。焼き直して採用した。

**モーダル右上の × が、無かった 2 つに付いた**（色カタログ・モデル選択）。
**この 2 つ目は美観の問題ではなかった** — × が呼んでいた `onCloseSettings` は
`settingsOpen = false` にするだけで、`closeSettingsModal()` が持つ「モデル選択中なら
`cancelModelSelection()` で選びかけを巻き戻す」処理を通らない。
**× を出さないという条件が、巻き戻さずに閉じて古い snapshot を残す経路を覆い隠していた。**
× は背景クリックと Esc が使う `onClose` へ付け替え、呼び手の無くなった `onCloseSettings` は
prop ごと消した。

**バッチ実行中のタブに `(x/y)` が出る。** タブは `flex: 1` なので 3 タブの幅は内容に依らず等分で、
数字側は `tabular-nums` と桁数ぶんの `min-width` を持つので桁が増えても揺れない。

**失敗した行を、設定した回数だけ描き直す。** 既定は **0 回（＝従来どおり）** —
1 回を既定にすると、設定を開いたことのない利用者の失敗バッチのモデル消費が黙って倍になるため。
本ループを `paintBatchLine()` へ括り出し、本巡とリトライ巡が同じ実装を通る。
**中断（停止ボタン・abort）のときはリトライしない**（走らなかった行は失敗ではない）。
失敗レポートの `total` は元の行数のまま固定し、進捗表示だけを巡ごとに張り替えて `(2/5 ↻1)` と出す。

**履歴管理のサムネイルタブが、系譜ごとに作品を並べる。** 1 件だけの系譜は落とす。
**絞り込みは集計の `HAVING` で行い、`total` も同じ subquery を数える**ので、ページと件数が食い違わない
（クライアントで捨てると 1 ページ 8 件が痩せ、`total` と食い違う）。
1 系譜の中は**世代順**（`lineage_generation` 昇順・同世代内は `at` 昇順）で、
繋がりは線でなく**囲いと世代番号**で見せる（線はグリッドが折り返した瞬間に破綻する）。

**この段で、本改修とは無関係の既存の不整合が 1 つ見つかり、直した。**
`list_lineage_groups` は `coalesce(root_node_id, id)` で束ねるのに、
`list_lineage_group_items` は `root_node_id == :root` と厳密一致で絞っていた。
`root_node_id` は migration で追加されバックフィルが無いので、**それ以前に作られた根は NULL を持ち、
群の件数には自分が入るのにメンバー一覧からは自分だけ落ちていた。** 両者を同じ式へ揃えた。

**星とは別の、2 つ目の印を足した** — `for_revision`（「推敲のみ」）。
`starred` は潰さず独立した列で、**両方立てると AND**（両方の印を持つ作品だけ）。
`starred` が現れる 9 箇所すべてに対応物を置き（列・migration・索引・応答・既定・FTS 検索の 2 つの SQL・
一覧・系譜群の集計・別の一覧経路）、`PATCH /api/history/{item_id}/for-revision` を新設した。
**送り手は `web` `server` `cli` の 3 領域で数えて全部対応した**（`cli` は `history` と
`history export` に `--for-revision`）。**`android` は未対応**で、当面 `for_revision` を送らず読まない。

**ダウンロード先のフォルダを選べる**（File System Access API・Chromium 系のみ。
Firefox / Safari は従来どおりブラウザ既定へ落ちる）。
**意図と表示名はサーバー（`user_accounts` の 2 列）が持ち、ディレクトリハンドル本体はブラウザの
IndexedDB が持つ** — ハンドルは構造化クローンでしか運べずサーバーへ送れないため。
権限は**保存のたびに**確かめ、拒否されたらブラウザ既定へ落としたうえで**落ちた先が違うことを画面に出す**。
落とし口は 3 箇所から **1 箇所へ束ねた**（`features/export/save-target.ts`）ので、
SVG 3 プロファイル・PNG・コンタクトシート・アニメーションの 4 経路が同じ 1 本を通る。
**非対応ブラウザでは設定欄を無効化せず丸ごと出さない。**

**系譜表示からも AI コンタクトシートが作れる。** 複製せず、`HistoryManager` の本体を
`features/contact-sheet/run.ts` へ移して**両方が同じ実装を呼ぶ**
（ページ分割・シートを跨ぐ通し番号・連続ダウンロードの間隔待ちは、複製すると片方だけ静かにずれる）。
各パネルが供給するのは「どの id が選ばれているか」と「id を作品へ解決する方法」だけで、
系譜側は手元のグラフから解決するので fetch が要らない。

**系譜カードが、使ったモデルの短縮名を言う。** 提供者名を付けず、
ベンダー接頭辞（最初の `/` まで）を落とす（`google/gemma-4-31b-it` → `gemma-4-31b-it`）。
**文字列は切らない**（溢れは呼び出し側の `ellipsis` が受ける）。
**Stage 1 と Stage 2 が違うときだけ 2 つ出し**、同じなら 1 つ。完全な `提供者 / モデル` は `title` に残る。
モデルが記録されていない作品では何も出さない。

**描画パラメータの編集（`adjust`）にモデル選択が付いた。** `DdlEditorDialog` と同じ
`ModelCardPicker`・同じラベル・同じハンドラで、**選ぶと `stage2Provider` / `stage2Model` の既定を書き換える**。
Stage 1 は触らない（そのための画面は「使用モデル変更」）。

**「暴れる」が、作品メニューから届く 6 つのモーダルすべてに付いた**（`WildToggle.svelte` 1 本を作り、
6 箇所へ複製していない）。添景と揃えて**「継承」を持たせた** — 何も指定しなければ親作品の
`render_wild` を継ぐ。**これは実挙動の変更を伴う**: refine 経路は `wild` を送っておらず、
`paintOne` が `options.wild ?? false` で既定 false を送っていた。
**つまり、暴れた親を落ち着いた状態で描き直していた。**

**比較ボタンの食い違いは、コード上で見つからなかった。** モデル比較と言語比較の 2 つは
コンポーネント・`icon` / `block`・親の連なり・幅指定・色のすべてが同一で、
「`Tooltip` の包みで幅が縮む」という最有力の仮説は `CanvasPanel.svelte` に既に手当てが入っていた。
**推測で CSS を足していない。作者の変更前スクリーンショット待ち。**

**古いライブラリの点検は調査で止めた**（契約どおり）。**コードは 1 行も触れていない。**
`svgwrite` は上流の最新も 1.4.3 で上げようがなく、`cookie` の `overrides` は
`@sveltejs/kit` 2.69.3 の宣言が依然 `^0.6.0` なので外せない。
**宣言だけの完全な死蔵は 0 件。どれを上げるかは作者裁定を待つ。**

**web に単体テストの基盤ができた**（依存を増やさず、Node v26 の `node:test` と TypeScript 直接実行で足りた）。
`npm run test:unit` **14 件** — バッチのリトライ判断を純関数へ切り出した 9 件と、
短縮モデル名の 5 件（**末尾だけが違う 2 つの ID が同じ文字列にならないこと**を含む）。

pytest **2011/31 skipped**（+66）、cli **78**（不変）、ruff clean、
`npm run check` **235 FILES / 0 ERRORS / 2 WARNINGS**（警告は既存 2 件のまま）、
`lint:i18n` **975/47/0/0**、`lint:models` **68**、`lint:recommendations` **37**、
`test_api_surface.py` の台帳 **81 / 81 / 80**（新設ルート 1 と body schema 1。**既存項目の削除は 0**）、
`test_route_authorization.py` **81**、`check_docs.py` 緑、**凍結コーパスはバイト一致**。
受け入れでは**摂動を 7 つ当てた** — 並び順だけの入れ替え（新テスト 1 件だけが赤・既存の色カタログ検査
177 件は 1 件も見ていない）、名前だけ変えて順序は保つ**対照**（178 件すべて緑）、
`HAVING` の無効化（3 件）、`coalesce` を厳密一致へ戻す（1 件）、推敲の絞り込みの無効化（2 件）、
フォルダ名の解除の無効化（1 件）、リトライ上限の `<` → `<=`（14 件中 2 件）。
すべて戻して値を読み直し、復元を確認した。版数は patch。

### v2.9.28 — 「暴れる」が、ブラウザ既定のボタンを見せるのをやめる（Build 826、2026-08-02）

**v2.9.27 で 6 つのモーダルへ置いた `WildToggle` が、暗い画面の中で 1 つだけ明るい箱に見えていた。**

原因は CSS のスコープ。ボタンは `class="ghost-btn wild-btn"` と書かれていたが、
**`WildToggle.svelte` 自身の `<style>` に `.ghost-btn` の定義が無く**、
Svelte はスタイルをコンポーネント単位でスコープするので **`ghost-btn` は 1 つも効いていなかった**。
`.wild-btn` は寸法トークン（`--btn-sm-*`）しか持たないため、OFF 状態は
**ブラウザ自身のボタン**にそのまま落ちていた（ON 状態は `.wild-btn.active` が
`--accent` を塗るので正しく見えており、**押した状態だけ見ると気づけない**）。

`InputPanel` ほかが持っているのと同じ定義を `WildToggle.svelte` へ置いた。
**トークンだけを使い、px と色の直書きはしていない。**

**`.ghost-btn` を使いながら定義していないコンポーネントは、数えて `WildToggle` の 1 つだけだった**
（`ProfileModal` は 1 つの選択子にまとめて定義している）。

`npm run check` **235 / 0 / 2**・`lint:i18n` **975/47/0/0**・`test:unit` **14** はいずれも不変。
**比較ボタンは変えていない** — 黒く見えていたのは `--action-disabled-bg` で、
モデルや組み合わせを 1 つも選んでいないあいだの無効状態である
（選ぶと `--action-bg` になり、主入力の「描画する」と同じ塗りになる）。**作者裁定で現状維持。**
版数は patch。

### 2026-08-02 — 日本語版と英語版が、節ごとに対応する（**採番なし**・ドキュメントのみ・段 1／3）

**作者の指摘から始まった** — 「`SPEC.md` が `SPEC.ja.md` より古いようだ」。

実測すると**宣言版数は両方 `v1.92.0` で一致**しており、差は commit にあった。
`SPEC.ja.md` の最終更新は `1f900b22`（v2.9.24）、`SPEC.md` は `6dbf1625`（v2.9.22）で **2 サイクル遅れ**。
ただし英語へ来ていない 2 件は**どちらも英語に存在しない節（§17 残件・付録）の中**で起きており、
英語側に古い記述が残る事態にはなっていなかった（`api.py` も `+page.svelte` も英語には言及が無い）。

**drift が見えなかった理由は機構にある。** `check_docs.py` の日英対照は
`_heading_shape()` ＝**見出しレベルの並びしか比較しない**うえ、SPEC 対は宣言例外つきで登録されていたので、
**本文の drift はどの検査にも掛からなかった**。

**同時に 2 つの欠陥が見つかった。**

- `SPEC.md` の冒頭が「Sections 1 to 17 follow the Japanese file section for section」と書いていたが、
  **§15 と §17 は英語に無い**（1〜14 と 16）。**公開文書が自分の構成を誤って宣言していた**
- **§3 の語彙表を空行 1 つが割っていた** — 後半 5 行（relations / places / angles / proportions / colors）は
  ヘッダ行も区切り行も持たないため、**GitHub では表にならずパイプ記号のままの平文で出ていた**。
  公開仕様の中核である 10 カテゴリの語彙表のうち、半分が壊れて公開されていた

**2026-08-02 の作者裁定で「日本語＝概念の正本、英語＝運用の担い手」（2026-07-28）は失効した。**
日英は節ごとに対応させる。**正本が日本語である点は変わらない。**

本サイクル（段 1／3）で行ったのは次のとおり。

- **例外理由文が書いていたより構造差は大きかった** — 「英語に欠けるのは §15 と §17 だけ」とあったが、
  実際は **§1.1〜1.3・§3.1〜3.2・§11.1〜11.4 の 9 サブ節**も英語に無かった
- **§1**: 英語へ「このドキュメントの位置づけ」節（名の由来・エコシステム命名規約）と 1.1／1.2／1.3 を新設。
  逆に英語だけが持っていた「描画プログラムではない・記述が持続する作品で SVG は一度の演奏」の段を日本語 §1.1 へ
- **§3**: 英語へ 3.1／3.2 と「コアの性質」5 項を新設。日本語へ歳時記の名の由来・v1.92 の剪定と v2.7.9 の銀筆改名・
  カタログ id 一覧と命名方針・`sub`／`sub_ja`／`name_ja` の表示規則・render JSON の記録項目を追加
- **§11**: 日本語は v0.8 当時の計画、英語は現行の評価実務で**互いの翻訳になっていなかった**。
  新しい英語側へ寄せて 11.1〜11.4 を両言語で揃え、**当時の計画は 11.4 として両方に残した**
- **§15（開発方針）と付録（リポジトリ構成）を英語へ新設**
- `SPEC.md` §24 の **"Keep public English wording concise and readable" を撤回**（抄訳を促す唯一の残存文言）。
  代わりに「省略しない」「`check_docs.py` が唯一のゲート」を明記
- `check_docs.py` の宣言例外 2 件を**恒久の免除から一時の残作業記述へ**書き換え

**段 2 も同じ日に行った** — 英語だけが持っていた **§6.7（英語の指示文経路）・§7.8（参照 Web アプリケーション）・
§12.14（Renderer が持つもの）を日本語へ書いた**（185 行 / 14,332 字）。
英語側の 3 節の書き出しにあった「日本語版は無い」「2026-07-28 の裁定により」も外した。
**見出し形状は 116 個目まで一致する**（食い違いは §17 と §18 以降だけになった）。

**段 3 の SPEC 側も同じ日に行った** — 英語 §18〜§24（JSON Score・キャンバスモデル・モード・
履歴とデータの完全性・セキュリティと運用・CLI・正本の所在）を日本語へ書いた。
そのうえで**英語の「洗練の会計」節に見出しが 3 つ欠けていた**ことも見つけて足した
（v1.80 の 2 つと v1.88。**本文は英語にも在ったが見出しだけが無く、日本語の 5 に対して 2 だった**）。

**そして実装状況の棚卸しの日本語版 `docs/spec/implementation-status.ja.md` を新規に書いた**
（英語 429 行 / 30,080 字ぶん）。**この対は宣言例外を外し、いま `check_docs.py` の形状検査を実際に通っている。**

**§17 を除けば SPEC の日英の見出し形状は完全に一致する**（135 対 135）。
**残るのは §17 だけで、それは台帳 I-095 待ちである** — 未解決 31 項目のどれも台帳に無いので、
落とすと追跡から消える。作者裁定は「棚卸ししてから、現存するものだけ起票」。
**SPEC 対の宣言例外は、その 1 点だけを書いた一時の記述として残してある。**

### 2026-08-02 — 残件の一覧が、仕様から台帳へ移る（**採番なし**・ドキュメントのみ・段 2／3）

**`SPEC.ja.md` §17「残件と検討事項」を、所在を示すだけの節にした。** 日英とも同じ形で置き、
**SPEC 対の宣言例外を `check_docs.py` から削除した**。これで**見出し形状は 136 対 136 で一致する**。

**移す前に棚卸しをした**（作者裁定 2026-08-02）。§17 が抱えていた未解決 31 項目を現物で 1 件ずつ測り、
**現存する 24 件を台帳の 5 項目へ、群ごとにまとめて起票した**（Renderer / Stage 2 / Stage 1 /
Web UI / テストと仕様）。**5 件は落とした** — 3 件は既に実装されており（複数モデルの横並べ比較ビュー・
プラグインの読込機構と名前空間・英語版 Saijiki と英語 prompt）、1 件は**評価対象の学習モードが
v1.2 で廃止済み**、1 件は**タスクではなく事実の記述**（変換例の件数）だった。
解消済みの 48 項目は変更履歴が持つので落とした。

**棚卸しで、記述と現物のずれが 3 つ出た。**

- **揺らぎの次元は、仕様が書いていたより 1 軸多く未実装だった** — `Dimension` は 6 軸を宣言するが、
  renderer が読むのは `position_x` と `position_y` だけで、**`angle` `length` `rotation` に加えて
  `radius` も読まれていない**
- **弧を誘導する変換例は、英語側だけ済んでいた** — 英語の例プールに 12 件、**日本語には 0 件**
- **履歴の書き出しは半分だけ在った** — SVG・PNG・アニメーション・コンタクトシートは書き出せるが、
  **履歴の JSON は書き出せず、読み込みの口はプラグイン文書用の 1 つだけ**

**§17 は 2026-08-02 まで一覧そのものを抱えていた。** 解消済みの項目が節の過半を占め、
未解決の項目は台帳と二重に管理されていた。**記録は変更履歴が、追跡は台帳が持つ。**

### v2.9.29 — 依存を一斉に上げたら、緑のまま何も見ていない検査が 2 つ見つかる（Build 828、2026-08-02）

**pydantic・sqlalchemy・pytest・ruff・fastapi・starlette・uvicorn・cryptography・anthropic・openai・
@sveltejs/kit・svelte・svelte-check・vite・pillow を上げた。製品コードの修正は 0 行である。**

**この更新の実質は、版数ではなく検査の側に出た。**

**`fastapi` 0.141 は `include_router` がルートを `app.routes` へ展開するのをやめ、
不透明な包みを置くようになった。** `app.routes` から `APIRoute` を数えると **81 → 0**。
製品コードは `app.routes` を 1 箇所も読んでいないので**動作は何も変わらない**が、
**そこを読んでいた検査 3 つのうち 2 つは、列挙が空になったまま緑を返した** —
「`api.py` に残るエンドポイント」の一覧が空になったのは、条件を満たしたからではなく
**1 件も拾わなかったから**である。3 つとも `fastapi.routing.iter_route_contexts` へ寄せ、
**空でないことを明示して表明する検査を足した**。次に上流が列挙口を変えたら、今度は黙らず落ちる。

**`anthropic` の呼び出し表面には、検査が 1 つも無かった。** SDK を上げる前に
`server/tests/test_anthropic_call_surface.py`（13 件）を書き、**渡す鍵の集合と、
応答から読む属性の集合**を 4 箇所すべてについて凍結した。そのうえで 0.96.0 → 0.120.2 へ上げ、
**13 件とも緑・製品の修正 0 行**であることを確かめ、**実物の呼び出しを 1 回通した**
（Stage 1 と Stage 2 を経て SVG 6,724 バイト、合計 3.54 秒）。

**`ruff` 0.16 は既定のルール集合を 59 から 413 へ広げた。** リポジトリは ruff の設定を
1 つも持っておらず、**「All checks passed」の意味がその版の既定に委ねられていた**。
コードを 1 行も変えずに 353 件が赤くなり、最多の 76 件は **FastAPI が公式に指定している
`Depends()` の書き方**に当たるものだった。`select = ["E4", "E7", "E9", "F"]` を明示して
**従来と同じ 59 ルールへ凍結した**（cli 側も同じ pin）。**足された 354 ルールの採否は台帳が持つ。**

**`typescript` 7 は見送った。** `svelte-check` が TypeScript 7 単独を実行前に拒否し、
要求される TS6 との併存構成では **`--tsgo` 経路が 235 ファイルではなく 38 ファイルしか
型検査しない**（16%）。**依存を上げるために検査の網を 6 分の 1 にするのは筋が違う**ので、
6.0.3 のまま残した。→ 台帳

**保存済みデータの読み戻しを、上げる前に作った暗号文で確かめた** — `cryptography` 47.0.0 で
API キーを 3 本暗号化してから 50.0.0 へ上げ、**3 本とも復号できること**と
**鍵違いのときに例外でなく空文字を返す挙動**まで一致することを実測した。

**動かなかったことの確認**: `test_api_surface.py` の台帳は fastapi を 5 minor 上げても
**81 / 81 / 80 で digest までバイト同一**。凍結コーパスも**バイト一致**である
（地の texture seed は Score 全体の dump のハッシュなので、ここが動けば絵が変わっていた）。
### v2.9.30 — 設定が自分で名乗るようになり、暴れるの札が自分の名前を得る（Build 832、2026-08-02）

**設定を 1 つ足す費用が、`+page.svelte` の 6 行から 0 行になった。**
これまで「localStorage に持ち、描画要求に載り、画面に出る」設定を 1 本足すには、
ページ本体の 4 種類の列挙点（読み込み・保存・要求の組み立て・prop の転送）を
全部書き足す必要があった。**列挙点そのものを登録簿へ裏返した** —
`features/render-payload.ts`・`features/persisted-settings.ts`・`features/user-settings.ts` の
3 つは**どの機能も名指ししておらず**、各機能が自分の側で 1 行だけ自分を登録する。
ページは登録簿を 1 回呼ぶだけになった。

**受入は行数ではなく「1 本足すのに何行要るか」で測った**（探り針 2 本・実測 6 → 0 行と 4 → 0 行）。
`+page.svelte` は 7,023 → 6,986 行で、**37 行しか減っていない。減らないことは失敗ではない** —
通り道を畳むこととファイルを小さくすることは別の目標である。

**描画要求の振る舞いは 14 通りの呼び出しを 1 つずつ突き合わせて保存した。**
その照合で**改修前からあった非対称が 2 つ**見つかったが、直さずそのまま運んだ
（demo が `wild` を常に false で送っていたこと、その場再描画 2 本が水準を継承しつつ
おとなしく描くこと）。**この版で絵は 1 枚も変わらない**（凍結コーパスはバイト一致）。

**目視で見つかった 3 つの乱れを直した。** ①「暴れる」が添景ボタン群から離れて
行の途中に浮いていた（`.compare-head` が 3 つの子を `space-between` で配り、
`.ddled-foot` は左寄せの余白を添景だけに与えていた。どちらも 2 つを 1 つの箱へ入れた）
②その札には**名前が無く**「（継承）」とだけ出ていたので、**「筆致制限（継承）：」**を左に置いた
③**モデル比較の進捗表示が枠からはみ出していた** — 幅を広げる規則は
`64cbbfda` が `.compare-status` を狙って書いたもので、`2cbc93b3` が
進捗表示を `RunStatus` へ統一したときに**コメントだけが残り、規則は何にも一致しなくなっていた**。
今度は**このコンポーネント自身が書くクラス**に掛けてある（`.run-status` は子側の名前なので、
それを名指す規則は Svelte のスコープで消える）。

**添景の札も同じ形へ揃えた** — `添景（継承）：`。
**同じ行に並ぶ 2 つの札のうち片方だけに `：` が付いていると、揃っていないのではなく事故に見える。**
**入力パネルの暴れるボタンには札を付けていない**（あの行は継承の概念を持たず、どのボタンにも札が無い）。

### v2.9.31 — 系譜の詳細が版を名乗り、ハッシュが持ち出せるようになる（Build 833、2026-08-02）

**系譜のカードに 3 つ足した。** どれも新しい機構ではなく、**既に届いていたのに
画面へ出ていなかったもの**である。系譜の応答は履歴の行をそのまま載せているので、
`render_engine_id` / `render_engine_version` / `ddl_engine_version` /
`render_build_number` / `for_revision` は**改修の前から node に入っていた**。
server は 1 行も変えていない。

**①詳細が版を名乗る。** 「詳細」を開くと rh2 の直後に **render engine ／ 変換層 ／ Build**
の 3 行が並ぶ。**画布の来歴と同じ組み合わせ・同じ並び**にしてあるので、
系譜のカードと画布の詳細が同じ語彙で読める。記録の無い作品は周囲の行と同じ `—` を出す
（**版が古い作品ほど欠けるので、そこだけ別の言い方をすると欠落が事故に見える**）。

**②レンダーハッシュが持ち出せる。** rh2 の行にコピーボタンが付いた。
**コピーするのは表示されている短縮形ではなく `render_hash` の全体である** —
先頭 10 文字は目で見分けるための形であって、貼って使える同一性の記録ではない。
`navigator.clipboard` を持たない環境への退避は履歴管理と同じ形にしてある。

**③推敲マークが★の隣に来た。** これまで推敲マークを付け外しできるのは履歴管理の画面だけで、
**系譜を見ながら「これは推敲する」と決めた作品には、いったん画面を移らないと印が付けられなかった**。
★と同じ場所・同じ殻に ✎ を置き、押すと履歴管理の「推敲のみ」フィルタと同じ列が動く。
**★と推敲マークは独立している**（作品はどちらも持てるし、どちらも持たなくてよい）。

**結線は摂動で確かめた。** prop を 3 段（ページ → 画布 → 系譜）で通しているので、
**どこか 1 段でも繋ぎ忘れると「ボタンはあるが何も起きない」型の欠陥になる**。
中間の 2 箇所をそれぞれ外して型検査が赤くなること、戻すと緑に復することを見た。

### Android `2.1.4-android.3` — 描画層が render engine 19 へ追随し、乱択が消える（地の抵抗・13 本の色カタログ）（android Build 148091、2026-08-02）

**移植は 4 段で入った。** ①`ServerStrokeEngine.kt` に engine 19 の支持体応答（地の抵抗）
②`ColorCatalogs.kt` を **11 本・`map` 6 キー → 13 本・`map` 9 キー**へ（engine 18 のデータ）
③**乱択カタログの撤去**（台帳 [I-081]・作者裁定）④版の宣言を 19 へ。

**期待値は発注者が先に焼いて枝へ置いた。** 起点は **`tests=117 failures=13`**、満点は **0 failures** で、
**どの失敗がどの段の担当かを契約の表に書いてから渡した**。最終は **`tests=118 failures=0`**
（段 3 が検査を 1 つ足したので 1 件増えている）。**実装は fixture を 1 バイトも触っていない** —
移植の同一性検証で期待値の側が動くと、何を測ったのか誰にも言えなくなる。

**engine 19 の期待値は全件が動くわけではなかった。** 台帳の見立ては「全件」だったが、
**実測では 53 件中 16 件しか動かない**（道具の文法・法線・弧長・雲形の輪郭は engine 19 で不変）。
**移植の契約を書く日は、上流の版上げが下流の期待値を何件動かすかを先に数える。**

**乱択の撤去は 6 か所に及んだ。** `randomColorCatalogId()` の削除だけでは終わらず、
batch と demo の呼び出し 2 か所・保存キー 2 つ（`batch_random_color_catalog` /
`demo_random_color_catalog`）とその復元経路・`InkuApp.kt` の切替 UI 1 か所が要った。
**server が乱択を廃した改修の Android 側は、機能 1 つでなく設定 1 本ぶんの撤去である。**

**⚠ その撤去に足された検査は恒真だった。** `ColorCatalogSelectionDeterminismTest` は
テスト内の private ヘルパが `selectedCatalogId` を返すだけで、**製品コードを 1 行も呼んでいない**。
受け入れで**製品側の demo 経路へ乱択を戻す摂動**を当てたが、**118 件すべて緑のまま**だった。
**撤去そのものは正しく入っている**（`Random` の残存 0）ので、欠けているのは検査だけで、
**まともな検査には Robolectric か、選択を通す 1 つの純関数が要る**（台帳 [I-103]）。
**徴候は実装レポートの摂動の記録にあった** — 段 3 だけ、摂動の対象が製品コードでなく
**テスト自身の中の選択処理**だった。**摂動がテストを書き換える形になっていたら、
それは検査の判別力を測っていない。**

**engine 20（配置の主導権）は範囲外のまま**（台帳 [I-077]）。
**arrangement を宣言する fixture が 0 件なので、追随の前に server 側で生成器へケースを足す段が要る。**

**採番は `android/VERSION` と `android/BUILD_NUMBER` の 2 つ。**
`APP_VERSION`（`v2.9.31`）・`web/BUILD_NUMBER`（833）は不動で、**pentala 反映も不要**。
**`android/BUILD_NUMBER` は 148090 → 148091** — Gradle が `install*` のたびに自分で上げるので、
**実機へ入れた時点で動く**（手で編集すると二重に上がる）。**Pixel 9（tokay）へ配備し、
`versionCode=148091` / `versionName=2.1.4-android.3` で起動することを実測した。**

### v2.9.32 — 背景色が抽象 9 色すべてへ開き、灰を潰していた第 4 の塞ぎが外れる（Build 836、2026-08-02）

**本番 2,061 作品の背景色は、実際には白・黒・青の 3 色しか出ていなかった**（白 66.2% / 黒 21.0% /
青 5.4%。灰・赤・緑は Build 700 より前にしか無く、黄・橙は全期間 0 件）。**塞いでいたのは
schema ではない** — `Color` も Stage 2 の tool schema も、9 色を宣言したままだった。

**落ちていたのは搬送である。** DDL が非白背景を要求する率はむしろ上がっており（engine 20 期で
53% と過去最高）、下がったのは**届く率**のほうだった（Build 0–399 の 92% に対し 800–814 は 42%）。
落ち場所は 2 つとも決定的な機構で、LLM の揺らぎではない — **coerce の背景ガバナが 22 件**、
**Stage 1.5 の灰置換が 13 件**。

**ガバナが明示句そのものを読んでいなかった。** `_has_explicit_background_intent` の判定材料は
原文の情景語（夜・夕焼け・夜明け）だけで、「背景を黒で埋める」という**記述の側の指示は
一度も読まれていなかった**。22 件すべてでこの関数が False を返していた。**明示句は、
夕焼けを必要とせずに信じられなければならない。** 判定に句そのものを加えた。

**Stage 1.5 の灰置換とプロンプトの 5 色制限を落とした。** プロンプト本文は前景を 9 色で書きながら
**背景の記述だけが 5 色で取り残されていた**。触ったのは 13 箇所で、うち 7 つは Android の
`WebDdlSpec.kt` にある。**そこには server に対応の無い LiteRT 専用プロンプトが 2 本あり、
語形も違った**（「灰背景は禁止、灰は前景に使う」は 5 色を列挙しない）。
**5 色の集合だけを探す検査は、この行が残っても緑になる。**

**そして 4 つ目の塞ぎがあった。** 段 1〜3 を入れて本番と同条件で 60 記述を測ると、
Stage 1 は灰の句を 20 件出し Stage 2 は 19 件で `background="gray"` を返すのに、
**coerce を通ると 19 件全部が白になっていた**。`coerce_score` が入口で呼ぶ
`_visible_background` が、**ガバナより手前で灰を無条件に白へ替えていた**。

**その機構は二重の保険だった。** 外して測ると「灰の上の灰」は**前景を黒へ寄せる規則が
単独で発火して守られる**（`background: gray` / `color: black` / note に `made visible`）。
赤くなった 2 つの表明が守っていたのは可視性そのものではなく、**背景と前景の両方が動く旧来の形**
だったので、**背景は灰のまま・前景は黒へ**という表明へ裏返した。

**開くことと使われることは別である。** 黄・橙・紫の背景は本番で要求が一度も来ていないので、
出口が開いても作品に現れるとは限らない。**需要の無いものを完了条件に据えると、
実装が正しくても赤になる**ので、本改修の受入には入れていない。

**renderer は 1 バイトも変えていない**（render engine 20 のまま）。凍結コーパスは
`ddl-engine-4`（33）`render-engine-20`（525）とも byte-identical で、
**この層をコーパスは 1 件も通っていない** — 緑は正しさの証拠ではなく、
**誰も見ていないことの証拠**である。受入は新しく立てた 7 つの表明が持つ。

### 2026-08-02 — 仕様が、空の README への委譲をやめる（**採番なし**・ドキュメントのみ）

**仕様は「モジュール構成は本書に列挙しない」と正しく宣言し、`server/README.md` /
`web/README.md` / `cli/README.md` を正本に指していた。** 台帳 [I-087] は
「3 つのうち 2 つが空」として起票されていたが、**測り直すと 3 つとも内部構成を書いていなかった。**

- `server/README.md` は **0 行**
- `web/README.md` は 42 行の **SvelteKit 雛形そのまま**（`# sv` / `npx sv create`）
- `cli/README.md` は 1,000 行あるが、**`.py` への言及が 0 件**。見出しは使い方の手順 3 つと
  `## Command Line Help Reference` で、**39 行目以降の 960 行は `--help` の機械生成写し**である。
  cli パッケージの実体は `__init__.py` と `cli.py` の 2 ファイルしかない

**長さは実体だが、委譲された役割ではなかった。** 起票時の「`cli/README.md` だけ実体あり」は
行数を中身の代理にした判定で、**3 本目も同じく機能していなかった。**

**そして記述は既に別の場所に在った。** `PROJECT_CONTEXT.ja.md` の「現在の製品状態」が、
web は機能ごとのディレクトリと登録簿 3 本を、server は routers 10 ファイルの名前・共有 5 ファイル・
依存の一方向・認可の 2 つの強制点を**現在形で持っている**（英語 `PROJECT_CONTEXT.md` も同じ）。
**空の正本を埋めるのではなく、書かれている場所を指すことにした。**

`server/README.md` / `web/README.md` を参照するファイルは、**仕様の日英の該当行を除いて
追跡ツリー内に 1 つも無い**（`check_docs.py` が見るのは根の `README.ja.md` / `README.md` の対だけで、
パッケージ README は 1 つも見ていない）。README 自体は削除していない。

**隣接する 2 件は、起票から本サイクルまでに解消していた** — 「API 経路 = `api.py`」の行は
`api_core/routers/`（10 本）と `api.py` の役割へ日英とも書き換わっており、
「UI 巨大コンポーネントは v1.20 で解決」という取り消し線つきの誤りは `dc71ff4b`（残件の一覧が
仕様から台帳へ移った変更）で記述ごと消えていた。**残っていたのは委譲先の 1 行だけだった。**

### Android `2.1.4-android.4` — マスコットが絵文字をやめ、押しても何も起きない設定が消える（android Build 148091 据え置き、2026-08-02）

**Android の「マスコット移植済み」は、絵の移植ではなかった。** `IncuMascotView` は `Text("🧊")` ＋
ラベル、`YuragiMascotView` は `Text("🦀")` ＋ ラベルの `Row` で、**web の 151 行 / 208 行の実装は
1 行も来ていなかった**。今回、5×5 のピクセルグリッドとアニメーションを
**純 Kotlin の材料 ＋ Compose Canvas** へ移した。Incu は 15 秒で公転し各ピクセルが 4 秒で渦を巻き、
3 つの incubator ピクセルが 5 / 6 / 7 秒で色を替える。Yuragi は 1.5 秒で横歩きし、
鋏を左 11 秒・右 8 秒で振り、7 秒周期で瞬き（右目だけウインクが 1 つ多い）、12 秒周期で泡を吹く。

**材料を純 Kotlin へ出したのは、検査のためである。** Compose の合成関数は JVM 単体テストから
叩けない（Robolectric を入れていない）。**絵の材料（25 マスの表・色・周期）を Compose 型を含まない
オブジェクトへ置くと、実機も Robolectric も無しに検査できる。**

**押しても何も起きない設定が 2 つあった。** `showKiwi` / `showCrab` は状態・永続・設定行の
**16 出現すべてが「保存して読み戻す」だけ**で、**Kiwi や Crab を描く合成関数は 1 つも無かった**。
削除した。**server に保存済みの `show_kiwi` / `show_crab` の行は消していない**（読み手が居なくなるだけである）。

**`ANDROID_SPEC` の対応表は、実在しない正本を指していた** — `KiwiMascot.svelte` は
web にもう無い（`web/src/lib/components/` にあるのは `IncuMascot.svelte` と `YuragiMascot.svelte`）。
日英とも実際の移植内容へ書き換えた。

**個数は絵を守らなかった。** 受け入れで摂動を当てると、**脚を 1 マス横へ動かしても・
2 つのマスの呼吸の遅延を入れ替えても、127 件すべてが緑**だった。赤 13 / 目 2 / 空 10 という個数も、
遅延の集合も保たれるからである。**25 マスの配置そのものを web の現物と突き合わせる表明へ替えた**ところ、
両方の摂動が赤くなった。**対照の摂動（表の並び順だけ反転）は緑のまま**である —
描画は並び順ではなく `cell.x` / `cell.y` から位置を作るので、絵は変わらない。

単体テストは **118 → 127**。**`web/` `server/` `cli/` は 1 行も動いていない**ので pentala への配備は無い。

### v2.9.33 — `thinness` が末尾を降り、`surface` が席を取り戻す（`ddl_engine_version` 5、Build 837、2026-08-03）

**Stage 2 の出力が半分に縮んでいた。** 作者の観察は「同じ DDL からの描画だが、塗りは貧弱であり
図形も小さい。印象が薄い」だった。**原因は v2.9.5 で `Instruction.thinness` を宣言の末尾へ移したこと**である。
tool schema は並び順ごと LLM へ渡り、**任意フィールドは後ろにあるほど埋まる** — その規則自体は既に仕様に
書いてあった。**書いていなかったのは、末尾の席が 1 つしかないことのほうである。**

**`thinness` が末尾を取ったとき、`surface` は席を失った。** 本番の `compose()` をそのまま呼んで
tool schema の並びだけを差し替えた 168 本の実測で、`surface` の搬送は **92% → 42%**、
出力トークン中央は **172 → 94.5**、命令数は **2.45 → 1.43 個/回** だった。
`surface.opacity` を誰も書かなくなったので、**v1.71 から不変の既定値 0.28 がそのまま本番値になっていた**
（本番 DB の同一記述 41 群で中央 0.60 → 0.28）。**要素数は減っていない。数はそのままで、1 つずつが
薄く・細く・小さい。**

**`thinness` を `surface` の直前へ移し、末尾を `surface` へ返した。** フィールド本体（型・既定値・
description）は 1 文字も動いていない。動いたのは位置だけである。`thinness` 自身の搬送は 89% → 67% へ
下がるが、**Score 全体を縮めない位置がここである**。**`required` にする案は実測で逆効果だった** —
同一記述の対照 28 組で `surface` が上がったのは 0 組、下がったのが 11 組で、出力はさらに縮んだ。

**受入にコーパスは据えられない。** `gen_render_reference.py` も `gen_ddl_reference.py` も
`composer` を import せず、**参照コーパスは Stage 2 を通らない**。v2.9.5 の commit メッセージ自身が
「凍結コーパスはバイト一致のままである」と書いており、**検査は正しく「変わらない」と報告して退行だけが通った**。
そこで受入は **Stage 2 の充填率そのもの**に置いた — 配備物の並びを実行前に検査して旧並びなら走らない probe で
28 本を測り、`surface` 保有率 **93.3%**（しきい値 65%）・`thinness` 搬送 **62.2%**（同 40%）・
出力トークン中央 **165**（同 130）・命令数 **2.25 個/回**（同 2.0）で全項目を通した。

**宣言順を持つ現物は 4 つあった。** server の `schema.py`・Android の tool schema 複製・
Android の fixture（`score_schema_contract.json` の 2 表）・Android のテスト 1 件である。
**突き合わせの辺 3 本のうち、検査があったのは 1 本だけだった** — `server ↔ Kotlin` と `server ↔ fixture` を
見る検査を足し、**新しい任意フィールドを末尾へ足したら赤くなる**表明も置いた。

**`ddl_engine_version` を 4 → 5 へ上げた。** 決定的層は 1 行も変わっておらず、`ddl-engine-5/` は
33 ケースすべてが `ddl-engine-4/` とバイト一致で `changed_from_previous` は空である。
**その空白が、宣言順という条件の実物**であり、版史がそう書いている条件の 2 度目の適用にあたる。

### v2.9.34 — 演奏が libm の最終桁を読むのをやめる（render engine 21、Build 838、2026-08-03）

**このリポジトリで唯一の自動検査である CI が、2026-08-01 から 22 run 連続で赤かった。**
落ちていたのは `reference-corpus` ワークフローの `render-engine` ジョブだけで、`ddl-engine` は緑だった。
**2026-08-01 以降にマージした全部が、この後ろ盾の無いまま入っていた。**

**原因は、演奏 seed が印字前の座標を丸ごとハッシュしていたことである。**
**macOS libm と glibc の `sin`/`cos` は 1 ULP 違う** — 同一引数 60 個のうち `sin(t·2π)` が 9 個、
`cos(radians(t·360))` が 7 個、`sin(radians(t·360))` が 10 個で食い違う（Python は両側 3.12.13）。
その差はグループ G の展開座標に 1〜8 ULP として現れ、`_fit_group_to_anchor` が全点の重心を取るので
群れ全体へ広がる。**そして `_seed_for_instruction` は命令の dump 全体をハッシュするので、
5.551115123125783e-17 の差で seed が 7178797595915484867 から 2693192989206796227 へ変わる。**
seed が変われば揺らぎが変わり、描画では 0.08〜0.17px になる。

**engine 11 で全数値を小数 6 桁へ載せたとき、「以後はどの OS でも同じ値になる」と書いた。
揃ったのは印字される数値であって、その数値を読む seed ではなかった。** 6 桁整形は他の経路の 1 ULP を
すべて吸収しており、実際 **A〜F の 493 件は両 OS でバイト一致していた**。**ハッシュを通る arrangement
経路だけが増幅していた。**

**`_expand_arrangement` が返す座標を 9 桁へ量子化した。** 正規化座標の 1e-9 は 1000px の画布で 1e-6 px、
SVG が印字する精度より下なので見えない。**両プラットフォームで 525 件すべてが一致することを実測した**
（改修前は 6 件が相違）。**絵が動くのは 32 件で全部 G 群**、A〜F の 493 件は 1 件も動かない。
`render_engine_version` を 20 → 21 へ上げ、`render-engine-21/` に 32 ケースを焼き直した。

**受入に「両 OS で一致すること」は書けない。1 台では観測できないからである。**
そこで **`sin`/`cos` を 1 ULP ずらす摂動をローカルで当て、絵が動かないこと**を検査に据えた
（`test_render_platform_stability.py`）。**同じ摂動が量子化を外すと 12 件を動かすことを見る検査を対で置いた** —
これが無いと、摂動が製品へ届かなくなっても本命は緑のままになる。判別力は発行前に実測してある（12 → 0）。

**既存の検査 3 件は、量子化していない期待値との厳密比較だったので直した。** うち 2 件は量子化した展開を
量子化していない layout と突き合わせており、**目的である「寄せの段が何をしたか」ではなく丸めのほうを
読んでいた**。`check_frozen_corpora.py` の「CI will be green.」も落とした — **同じ OS で焼いて同じ OS で
照合している限り、CI の緑は約束できない。**

### v2.9.35 — `api.py` の到達不能な島を落とす（Build 839、2026-08-03）

**v2.9.24 のルーター分割が「純粋な再配置」の範囲外として残した 9 シンボルを消した。**
`_DEFAULT_OUTPUT_DIR` / `_OUTPUT_DIR` / `_OUTPUT_PNG_SIZE` / `_HEX_COLOR_RE` / `_validated_color_map` /
`InterpretResponse` / `_bearer_token` / `_can_manage_user` / `_strip_anthropic_prefix` である。
**`src` `tests` `cli` `web` `scripts` `shared` `android` を通した参照が、どれも自分の定義行だけだった。**

**9 つで 1 つの島を成すので、まとめてでなければ落とせない** — `_OUTPUT_DIR` の唯一の読み手は
`_DEFAULT_OUTPUT_DIR` で、`_validated_color_map` の唯一の読み手は `_HEX_COLOR_RE` である。
**島の外からの参照は 0。** `import re` と `Header` と `pydantic` の 2 名も道連れに死んだので落とした。

**環境変数の口は塞がっていない。** `INKU_OUTPUT_DIR` と `INKU_OUTPUT_PNG_SIZE` を実際に読んでいるのは
**`db.py:374-375`** で、同じ既定値を持つ。`SETUP.md`・`manual/{ja,en}/server-configuration.md`・
`SPEC.md`・`.env.example` の記載はそちらが支えており、消したのは**同じ env を読む死んだ複製**である。
`Bearer ` の除去にも生きた対応物が別に在る（`api_core/deps.py`）。

**`ruff` は未使用 import は見るが、モジュール直下の未使用の定義は見ない。** いま誰も止めていないので、
**この型の残置は次の分割でも同じように積まれる**。**検査に判別力があることは摂動で測った** —
api.py から**生きている** `_catalog_render_color_map` を消すと 2 件が赤くなる。

**`_catalog_render_color_map` は残した。** テスト 2 箇所からのみ参照される別の型の残置で、
落とすならテストの側の判断が要る（台帳が別項目として持つ）。`api.py` は 251 行から 196 行になった。

### v2.9.36 — ゴミ箱の作品が、作品に対する操作へ乗らなくなる（Build 840、2026-08-03）

**`db.get_items` は所有者と id だけで絞っており、ゴミ箱の条件を 1 つも持っていなかった。**
呼び出し元は本番 7 箇所あり（アニメーション書き出し・出力ファイル再生成・SVG 取得・近傍・系譜・
推敲助言）、**どれもゴミ箱の作品をそのまま扱っていた**。v2.9.13 のアニメーション書き出しの
受け入れで気づいたが、当時は直していない（[I-094]）。

**台帳の「UI からは選べない」は誤りだった。** 履歴パネルの選択とツール群のボタンには
**囲みの条件が無く、ゴミ箱ビューでも同じものが出る**。実測でも、ゴミ箱へ入れた 2 件を指定した
`/api/history/export-animation` は **200 で GIF を返していた**。id の直送に限った話ではない。

**`get_items` でゴミ箱を除外した。失われる経路は無い。** ゴミ箱ビューの一覧は別の照会
（`list_items(trashed=True)`）で、**画面へ呼び出す経路は UI 側が既に塞いでいる** —
`loadItemAndClose` はビューが `active` でなければ即 return する。実測では、除外を入れると
`export-animation` / `/svg` / `/neighbors` / `/lineage` が **404**、`rebuild-output-files` は
**count 0** になり、**ゴミ箱一覧は 200 / total 2 のまま**である。

**既存の検査が 1 件、`get_items` を「行がまだ在るか」の確認に使っていた** ので、
ゴミ箱を持っている側の照会（`list_items(trashed=True)`）へ替えた。
**新しい検査は摂動で判別力を測った** — 除外の条件を外すと 3 件すべてが赤くなり、
**同じ性質を保つ対照の摂動**（条件の順序を入れ替え `== 0` を `!= 1` にする）では **緑のまま**である。

### v2.9.37 — ゴミ箱ビューが、作品から物を作るボタンを出さなくなる（Build 841、2026-08-03）

**v2.9.36 でサーバーがゴミ箱の作品を断るようにしたが、ボタンは出たままだった。**
履歴パネルの接触シート 2 つとアニメーション書き出しには囲みの条件が無く、ゴミ箱ビューでも表示され、
押せば **404 が返るだけ**になっていた。**移動（ゴミ箱へ入れる）ボタンが既に使っていた
同じ `historyManagerView === 'active'` の囲みへ、3 つとも入れた。**
**ゴミ箱ビューに残るのは復元と完全削除で、この 2 つは囲みの外にある。**

**接触シートはクライアント側で作るのでサーバーを通らない**（`findSelectedItem` が
画面上の項目から解決する）。**404 にはならなかったが、同じ規律で隠した** —
サーバーがゴミ箱の作品への操作を断ると決めた以上、そこだけ物を作れるのは筋が通らない。
**系譜パネルの接触シートは対象外**で、こちらは表示中の系譜グラフから解決するため
ゴミ箱の作品を扱わない（`get_items` を通らないことを実測した）。

**⚠ この web には component を描画する検査基盤が無い** — `test:unit` は DOM の無い
`node --test` である。**そこで、ソースを読んで `{#if}` の入れ子を辿る構造検査を足した**。
**`{:else}` を見るのが要点で**、復元・完全削除は同じ囲みの `{:else}` 側に居るため、
else を見ない walk はそれらを「囲みの中」と誤って報告する（**書いている途中で実際に踏んだ**）。
**検査は対照とセットにした** — 作品ツール 3 つは囲みの中、復元と完全削除は囲みの外であること。
**判別力は摂動で測った**（囲みを外すと 1 件が赤くなる）。**描画そのものは見ていない。**

### 2026-08-03 — 仕様が、web にもう無いマスコットの描写をやめる（**採番なし**・ドキュメントのみ）

**仕様は 3 か所でキウイのマスコットを描写していた**（[I-105]）。**実測すると `web/src` `server/src` に
`kiwi` は 0 件**で、現物は `IncuMascot.svelte`（5×5 の画素で組んだ立方体）と
`YuragiMascot.svelte`（蟹）の 2 体である。

- **`Incu` は 15 秒に 1 回ゆっくり回る。`Yuragi` は左の鋏を 11 秒ごと、右の鋏を 8 秒ごとに持ち上げる**
- **どちらが出るかは設定で選ぶ**（既定は `Incu`）。切り替えは `SettingsModal.svelte` のラジオ
- **画面ごとに違うマスコットは出ない** — `RunStatus.svelte` が選択中の 1 体を出し、
  **単一描画・バッチ・デモ・DDL エディタ・系譜・推敲モーダルの 7 コンポーネントが同じものを使う**

**したがって「バッチのマスコットは小さな蟹」という記述は二重に誤っていた** —
バッチ専用のマスコットは存在せず、蟹は選べる 2 体のうちの 1 体である。**この段落は日英とも削除した。**

**キウイの振る舞いを列挙した 1 段落は、2 体を 1 文ずつ述べる形へ置き換えた**（作者裁定 2026-08-03。
「事実だけに縮める」「キウイと同じ密度で書く」も選択肢に出したうえで、中間を採った）。
**日英とも同じ 3 か所を直し、`check_docs.py` は緑**（内部参照 55）。

**同じ誤りが `docs/spec/implementation-status` にも生きていた**（日英 1 行ずつ）。**こちらも直した。**
**`docs/history/changelog-v0.1-v1.71.md` は触っていない** — あれは当時の記録であって、現在の記述ではない。

### v2.9.38 (Build 843) — 記述と Stage 1 のあいだに写生層（Stage 0.5）を置く

**短歌のように密度の高い記述は、Stage 1 が一度に噛み砕けない。** そこで**記述を物の言葉へ写した
自然文（写生文）**に変えてから後段へ渡す層を、記述と Stage 1 のあいだに新設した。

**写生文は 5 つの消費者すべてへ同じ文字列として届く** — Stage 1・プラグイン展開・Stage 1.5・
Stage 2・coerce。**1 つでも記述のままだと、層が開こうとした振り幅が出ない。**
**記述そのものは保存と表示に残る**（作品は作者が書いたものであって、層が書いたものではない）。

**区切りの大きさ `sketch_grain` を 2 値で選べる** — `fine`（細かく区切る・既定）と
`coarse`（大きく区切る）。**総量はほぼ同じで刻み方だけが違う**（実測: fine 10 文 / 1 文 9.6 字、
coarse 4 文 / 1 文 21.2 字）。**描画のたびに選べ、作品メニューからも選べる。**
**区切りを変えて描き直すと系譜に `sketch_grain_change` が付く**（同じ区切りなら再演のまま）。

**層が落ちたときは記述がそのまま Stage 1 へ流れ、描画は完走する。**
**失敗した回は写生文として記録しない**（本番相当の 240 実行で 1 件発生した）。
**保存済みの作品は、保存された写生文から描き直す**（層を呼び直さない）。

**`buildEmotionHint` を撤去した**（[I-112]）。16 語の感情語を検出して
「感情語をDDLに反映してください」と `stage1_input` へ足していたもので、**本番 2,125 作品の 6.1% で
発火し、その 78% が「静かな」1 ルール、名指しする `silverpoint` は 1 件も選ばれていなかった**。
**併存させられない構造上の理由もある** — `stage1_input` は記述より先に立つので、
残したままだと Stage 1 だけが写生文でない文字列を読む。**API の `stage1_input` は残す**（CLI が使う）。

**受入は T-1 / T-2 / T-3 / T-5〜T-10。** **T-3（疎→密の振り幅）は 4 巡合算 2.40**（閾値 1.8）。
**T-4（意味系統の搬送）は差し替えた** — 語の一致で数える指標は「翻訳された」と「失われた」を
区別できないことが実測で分かったため（歌 06 の写生文は「夜である」と書き、DDL は
「背景を黒で埋める」と書いた。**`night` 系統の語は `夜` と `night` の 2 語だけなので 0 と数えられる**）。
**設計図に意味語が現れないのは設計どおりである**（SPEC §13.3・設計原則 3 と 7）。
**絵の量で測る対照実験へ置き換え、後続契約の段 0 へ移した**（[I-118]）。

**受け入れで受入の穴を 1 つ塞いだ** — **写生文が記述の代わりに立つ読み点は 2 か所ある**
（`/api/paint` と `/api/interpret`）。**interpret 側の差し替えを外す摂動を当てても 2163 件が
全部緑だった**ので、その経路にも表明を 3 つ足した（層が届くこと・層を切ったときの対照・
`stage1_input` が写生文を上回らないこと）。

**凍結コーパスはバイト一致**（`render-engine-21` 525・`ddl-engine-5` 33）。
**コーパスは 1 件も Stage 0.5 を通らない**ので、これは退行の番人であって効果の証拠ではない。

### v2.9.39 (Build 845) — 「記述から自動選択」が色カタログの選択肢になり、選択はユーザーごとに保存される

**バッチタブのチェックボックス「記述ごとに色カタログを選ぶ」を廃止し、色カタログのダイアログの
先頭に「記述から自動選択」を置いた。**デモパネルの同名のチェックボックス（ユーザーごとのデモ設定が
持っていた `catalog_mode`）も畳んだ。**単一描画からはこれまで自動選択を選べなかった** — 選択肢は
バッチとデモにしか無く、機構そのものは `/api/paint` に前からある。

**選択はユーザーごとにサーバーへ保存する**（`model_settings.color_catalog_id`）。localStorage は
使わない — **描画は全経路が認証必須で、未ログインでは 1 枚も描けない**（`_current_user` に無認証
モードは無く、未認証で通るルートは 6 本とも登録画面のためのもの）。ブラウザ単位の値は
「別のユーザーの選択」にしかならない。**移行の種は置かなかった**（同じブラウザを共有する別の
ユーザーへ選択が漏れるため。既存ユーザーは初回ログイン時に inku Default から始まる）。

**番兵 `auto` はモジュールの外へ出さない。** 選択値を直接読む箇所は **16 あり**、そのうち
履歴の保存 payload・系譜・推敲・比較の 8 か所は `catalog_id` として送る／保存する。
翻訳は描画ペイロードの寄与者 1 か所に置き、そこから外は必ず実在する id にした
（`catalog_mode` を運べるのは `/api/paint` だけで、ほかの経路へ `auto` が漏れると 422 になる）。
**保存済みの作品は自動選択を引き継がない** — 推敲と描き直しは作品自身のカタログ id を読む。

**サーバーは現存するカタログか `auto` しか保存しない。** 引退した id は既定へ落ちる
（保存できても次の描画で 422 になるだけである）。

**受入の判別力を摂動で測った**（当てた先はすべて製品コード）: 番兵を翻訳しない → web 2 件、
patch がフィールドを落とす → server 3 件、許可リストを外す → server 2 件、モーダルから選択肢を
外す → web 1 件。**対照**（カタログの色だけ変えて搬送は変えない）**は 12 件とも緑**。

**API 表面は `DemoSettingsBody` から `catalog_mode` が 1 つ消えただけ**（endpoints 81・
operations 81・schemas 80 は不変）。凍結コーパスはバイト一致。

### v2.9.40 (Build 846) — 行頭の連番と角括弧のコメントは、記述者のもので、描画のものではない

**バッチは行に番号を振り、記述はしばしば出典を角括弧で添える。**どちらも Stage 0.5 と、その先の
全部へ流れていた。**記述の 3 つの入口（paint / interpret / compose）で 1 度だけ切る**ようにし、
**作品には原文のまま残す**（保存・表示・応答は `req.description` を読み続ける）。

**番号と見なすのは「数字＋区切り」だけ**（`.` `．` `、` `)` `）` `:` `：` と全角空白）。
`2026年` と `3本の線` は記述として残る。**閉じていない `[` も記述**で、行末まで飲み込ませない。
囲みは半角 `[]` と全角 `［］` の両方。

**記述タブとバッチタブは、その範囲の文字の背景を灰色にする。** textarea は自分の文字の一部を
着色できないので、**同じ字送りの層を後ろに敷いて背景だけを描く**（`LabelHighlight.svelte`。
padding 9px 10px・13px・line-height 1.65 は両方の入力欄と同値、バッチは折り返さず横スクロールに追従）。
**規則が Python と TypeScript の 2 つになるので、両方を 1 つのコーパスで測る**
（`server/tests/data/description-label-cases.json` の 20 件。**web の規則をサーバーから
ずらす摂動で web 9 件が赤くなる**）。

**単一描画の文字数メーターは、描画が読む文字数を数える**（番号とコメントは数えない）。

**受入の判別力**（摂動はすべて製品コード）: paint で規則を呼ばない → server 3 件／番号の規則を
無効化 → 23 件／区切りを `.` だけに狭める → 9 件／未閉じ `[` が行末まで飲む → 6 件／
web の規則がサーバーから乖離 → web 9 件／ミラー層に文字列を渡さない → web 1 件。
**対照**（灰色の濃さだけ変える）**は 99 件とも緑**。

### v2.9.41 (Build 847) — 設計図が出来たあとの層は、DDL だけを読む

**契約 `description-propagation-cut`（実装セッション・Opus 5）を受け入れた。**
写生文（Stage 0.5 の出力）は **5 つの消費者すべて**へ届いていた。**Stage 2 と coerce への 2 本を切り、
残る 3 つ**（Stage 1・プラグイン展開の発動判断・Stage 1.5）**だけにした**。
`ddl_engine_version` **5 → 6**、凍結コーパス `ddl-engine-6`（33 + 本番形 3 = 36）。

**背景の番人を 1 つ撤去した。** 判じていたのは**文字列の出自**（利用者が機械生成のプランを記述欄へ
貼ったか）で、記述が coerce へ届かなくなると判ずる素性が残らない。残すと本番の DDL の普通の形へ誤爆する
— **本番の濃色 604 件で、撤去前 54 件が白へ落ち、撤去後は 1 件**（LLM 0 回の反実仮想・母集団 2,023 件）。

**プラグインの種は記述へ戻した。** 何枚・何本を決める材料が写生文だと、同じ記述でも写生文が変わるたびに
解決される数が変わる。受入は等値でなく**解決された数**を、**入口ごとに両方向**（同じ記述 × 違う写生文 → 一致 /
同じ写生文 × 違う記述 → 不一致）で測る。

**搬送の検査に「足さない」方向を足した**（[I-107]）。`carriage.py` は「落とさない」だけを見ていた。
**切断の前にはこの方向を測れなかった** — coerce が `写生文\nDDL` を読む限り、足された instruction は
作者の書いた何かへ必ず遡れて、追加と配達が区別できない。

**Stage 2 は縮んでいない**（20 首 × 2 巡 × 前後 2 腕・80 実行・失敗 0）。`tokens_out` 中央値 270 → 273、
どのフィールドも席を失っていない。**coerce が書いた instruction は 87 → 74（−15%）**で、入力を減らした
ぶんこの層の著作が減った側へ寄っている。**「どの節にも答えない」追加は 6 件のまま動かない** —
DDL が唯一の**ルート**になっても、DDL が唯一の**作者**にはならない。

**Android も同じ切断と同じ撤去を通した**（JVM 単体 133 / 実機 Pixel 9 で 5 件）。

**受け入れで直した 1 件**: マージが `plugin_seed_text=req.description` を残していたので、
**v2.9.40 の規則（行頭の連番と角括弧のコメントはどの層にも渡さない）が種だけ素通りしていた**。
切った記述を読むよう直し、種を見るゲートを足した。

**マージ後に初めて落ちた 2 件**は、v2.9.40 のテストが切断前の前提（Stage 2 が記述を受け取る）を
持っていたためで、性質を保ったまま現物へ合わせた。**消えたテストは 0 件**（main 側の 3 件は
レポートが宣言した改名で、中身は裏返しと対照の新設）。

### v2.9.42 (Build 848) — 黙っている送り手には、既定だけが描かれる

**契約 `cli-feature-parity`（実装セッション・Opus 5）を受け入れた。**
`/api/paint` は 1 つのリクエストモデルを受けるが、**送り手ごとに名指しする鍵の数が違う**。
37 フィールドのうち **web は 33・CLI は 17**。**絵を変える 8 つ**を CLI へ足した —
`sketch` / `sketch_grain` / `sketch_text` / `variation_amplitude` / `variation_seed` /
`wild` / `catalog_mode` / `interpretation_seed`。綴りはサーバーの鍵名から機械的に導く。
`paint` と `batch` の両方が受け取る。

**CLI から描いた絵には、写生文が一度も存在しなかった。** 送らない鍵はエラーにならず、
pydantic が既定で埋めて 200 を返す。**Stage 0.5 は web で既定 on・サーバー既定 off** なので、
`cli/out2/` の 51 ラン・ベンチ・参照コーパスは**この層を一度も通っていない**。
本版で `inku-cli` から初めて発火した（実描画 2 本・保存された作品の `sketch_text` は非空・
`sketch_grain` は `fine`、旗なし側は `null`）。

**既定は 1 つも変えていない。** 旗を渡さなければ送信本文は前の版と同一
（実測: 旗なしの送信鍵は前版と同じ・辞書の 17 鍵は不変・8 本すべてを渡すと 25）。
`False` は `None` ではないので、`or None` を外すと 18 鍵目が毎回の実行へ乗る。

**成果物 JSON は 3 鍵を明示する** — `sketch_text` / `sketch_grain` / `sketch_fallback_used`。
応答は null のフィールドを本文から落とすので、書き写しただけでは
**「ここでは Stage 0.5 を頼まなかった」が、古い CLI・古いサーバー・切れたファイルと見分けられない**。

**送り手の点呼を置いた**（`server/tests/test_cli_sender_census.py`）。
`PaintRequest` のフィールドを列挙し、各フィールドが `_paint_payload` に現れるか、
**理由つきの除外表 12 件**（履歴・系譜 9・`color_map`・`history_at`・`auto_repair`）に
載っていることを表明する。**見た件数も表明する**ので、列挙が空になっても緑にはならない。
CLI は別の venv に住むので `cli/src/inku_cli/cli.py` を**テキストとして読み `ast` で解く**。
**`cli/` ディレクトリが無ければ skip**（pentala は部分木で `cli/` を持たない）。
既存の `TestClient` 検査 14 ファイルがこれを捕まえられなかったのは、**どれも自分が試す鍵を
明示送信するから**である。**送らない送り手に何が起きるかを見ている検査が 1 本も無かった。**

**`cli/README.md` の usage は 3 世代ずれていた** — `--original-text`（現 `--description`）・
`--vary-seed` / `--vary`（現 `--composition-seed` / `--composition-count`）・
`--staffage` と `--trace` の欠落・位置引数が `prompt text`。パーサから生成し直した。
**次に旗を足す人が README を忘れても赤くならない構造は残っている**（全数のゲートは [I-127] で裁定待ち）。

**サーバーの製品コードは 1 バイトも動いていない**（凍結コーパス `ddl-engine-6` /
`render-engine-21` はバイト一致。`server/` の変更はこの点呼テスト 1 本だけ）。

### v2.9.43 (Build 849) — 写生層が何をしたかを、作品が書き留める

**`sketch_text` が空であることは、4 つの別々の出来事を同時に意味していた** —
その作品が層より前に描かれた・作者が層を切った・その経路は層を呼ばない・
**層は走ったが落ちた**。4 つ目はどこにも記録されていなかったので、
**倒れた層と一度も走らなかった層が同じ姿で保存され、本番で 0.5 が何回落ちているか誰も数えられなかった。**

`history.sketch_state` を足した。**既定値なし・遡及更新なし**なので、`NULL` には
意味が 1 つだけ残る — **この行は列より前に書かれた**。値は 5 つ:

| 値 | 意味 | `sketch_text` |
|---|---|---|
| `fine` / `coarse` | 層が走り、その区切りで写生文を作った | 非 NULL |
| `fallback` | **層は走ったが落ち、記述のまま描いた** | NULL |
| `off` | 層は使えたが、呼び手が通さないことを選んだ | NULL |
| `not_applicable` | この経路は層を呼ばない（記述が無い＝指示書から新規作成・DDL 入力） | NULL |

**状態を名指すのは `sketch_state_of()` 1 つ**（`server/src/inku_server/sketch.py`）。
保存する 3 経路と応答の 2 経路がすべてここを通るので、**「切」が書き手ごとに違う意味を持つことはない。**
`POST /api/history` は本文の状態を受け取り（形を検査するので未知の値は 422）、
**本文が黙っているときは導出する** — そこで `NULL` を書くと、今日書かれた行が「列より前」を名乗ってしまう。

**記述を丸ごと落としていた送り手が 2 つ見つかった** — web のデモ保存と、`inku-cli` 自身の
`POST /api/history` である。**CLI が保存した作品は層について何も記録しておらず、
サーバーからは「列より前の作品」と読めていた。**同じ変更で 3 鍵とも運ぶようにした。

**web は 4 つの沈黙を区別して見せる。** 区切りのメニューの「切」には「（推奨しない）」が付く。
**付記は札そのものではなく別の関数**なので、メニューにだけ現れ、
**過去の作品の記録・畳んだ切替ボタン・現在値の要約・系譜の親の区切りには出ない**（実測 5 箇所）。

**書き手の点呼を置いた**（`server/tests/test_sketch_state.py`）。
行番号ではなく **`ast` で `sketch_text` を書いている構文**を数え、**見た件数 8 も表明する**ので、
**列を足して 1 経路だけ直す実装は弾かれる。** 摂動 15 件（段 5 つすべて）が赤くなることを確認した。

**既存行は 1 行も動いていない** — 配備の前後で本番 `history` は 2,172 行のまま
（検証で描いた 4 枚を加えて 2,176）、`sketch_text` 非 NULL は 23 で不変、
**既存 2,172 行はすべて `sketch_state IS NULL`** である。
migration は `inku-api` の起動時に走る。`renderer` / `coerce` は触っていない（凍結コーパスはバイト一致）。

### v2.9.44 (Build 851) — 記述は、作品の出自である

**記述は記録ではない。** プラグインが発火するか・Stage 1.5 が何を文脈に読むか・
プラグインの種・指示文の言語の **4 つを動かす**。それなのに CLI には、その DDL を
生んでいない文字列を記述の席へ座らせる旗（`inku-cli paint --description`）があった。
**旗を外し、記述は打った本文そのものになった。** 位置引数が正統で、例外のほうを旗にする
のが正しい向きである（`refine perform --description` は既存作品の記述を上書きする別の旗で、
同じ綴りの別定義なので残っている）。DDL で書き起こした作品には記述が無いので、
**`compose` の payload からは鍵ごと落ちる** — web の「指示書を新規作成」と同じ形で、
どちらも種は DDL に落ちる。

**削ると何も残らない記述を 400 で返す。** 行頭の連番と角括弧のコメントは v2.9.40 で
記述者のものと定めて描画から切り落としたが、**切った結果が空になる記述**
（`1. ` `[note]` `３．`）はそのまま後段へ流れ、**空文字から主題を作り出していた**。
`/api/interpret` `/api/paint` `/api/paint/stream` の 3 経路に番人を置いた。
**判定は 2 条件で、1 条件ではない** — 生の記述が空であることは `min_length=1` が既に
断っており、切った結果だけを見ると**札を 1 つも持たない文には「札だけです」と答えてしまう**。
**`/api/compose` には置かない**（記述の無い指示書を描く経路であり、置くと web の
「指示書を新規作成」が壊れる）。**本番 2,023 件で、この番人に当たる作品は 0 件**である
（`[demo]` 接頭辞の 14 件は削っても本文が残るので全部通る）。

**空白だけの記述は 422 で返す。** `min_length=1` は文字数しか見ないので `'   '` を通し、
札が 1 つも無ければ切りも起きない。**web は送信門番が止めるので踏まないが、CLI と Android は踏む。**

**web の送信門番は、描画が読む文字列を読む。** 文字数メーターは既に切った後の文字数を
見せていたのに、送信ボタンと一括処理の行数は生の文字列を数えていた。**同じ規則を扉へ移した**
ので、端から端まで灰色に見える記述は送れない。**規則を 2 つ書いたのではない。**

**種の連鎖から写生文が落ちた。** プラグイン展開の種が空のとき、落ち先が写生文だった。
**写生文は Stage 0.5 が毎回書き直す**ので、「同じ記述なら同じ数」という v2.9.41 の性質が
**何も赤くならないまま失われていた**（SPEC §12.15 はその性質を述べている）。落ち先を
DDL へ移した — 空の種で 14 要素（写生文の値）だったものが 16 要素（DDL の値）になり、
**種を渡したときは種が効く**（逆向きも表明にした）。

**検査は 3 面 42 件**（server 36 / web 6 / cli の裏返し）。摂動は段ごとに当てた —
番人を `/api/interpret` から外すとその経路の 5 件だけが赤くなり、共有生成器から外すと
paint と stream の 10 件＋番人の性質 1 件、空白の validator を外すと 3 件、
種の連鎖に写生文を戻すと 1 件、CLI の旗を戻すと 2 件、web の門番を生の文字列へ戻すと 1 件。
**`--description` を見ていた 2 件のテストは削除ではなく裏返して残した**（旧綴り
`--original-text` も同時に見るので、名前で数える棚卸しが改名と削除を取り違えない）。

### Android `2.1.4-android.6` — 描画層が render engine 20 と 21 へ追随する（配置の主導権・最終桁を読まない演奏）（android Build 148091 据え置き、2026-08-05）

**移植は 7 段で入った。** ①宣言順の 4 つ目の複製を server へ合わせる ②展開へ描画 seed を通す
③`grid` layout の移植 ④`radial` の回転中心を宣言アンカーへ ⑤群れの重心を宣言アンカーへ寄せる
（枠 0.02 / 0.98 の軸ごとの縮め）⑥展開後の座標を 9 桁へ量子化 ⑦版の宣言を `"21"` へ。
**Android 単体は 133 → 134 件・失敗 0**（着手時は 2 件赤く、それは段 0 が期待値を先に焼いてあったため）。

**期待値は発注側が先に焼いて枝へ置いた。** `renderer_arrangement.json` の 33 ケースは
**server の群 G と 1 対 1** で、そこへ `at.region` を述べた格子を 1 件足してある
（engine 20 の唯一の除外規則＝領域を敷き詰める格子は後段を通さない、を突くため）。
**比較は許容誤差なしの厳密一致**。2 ケースは engine 21 の量子化だけで動く 4.9e-10 の差しか
持たないので、**1e-6 の許容を置くと engine 20 だけ移した実装が 33 件すべて緑になる**。

**受け入れで移植のずれを 1 つ直した — 値ではなく判定の書き方が割れていた。**
server は `radius` を falsy で読む（`r = arr.radius if arr.radius else 0.3`）ので、
**述べられた `0.0` は「述べていない」と同じ扱いで半径 0.3 の環になる**。移植は
`optDouble("radius", 0.3)` で取っており、**明示された `0.0` をそのまま使って 12 個の印が
中心 1 点へ潰れていた**。`Arrangement.radius` は制約の無い `Optional[float]` なので
Stage 2 はこの値を出せる。**`radius` を述べた fixture が 1 つも無く、33 ケースは
どちらの読み方でも緑だった** — 期待値 `G-radial-zero-radius-edge` を server から焼いて足し、
摂動で 1 件赤くなることを確かめた（34 ケースへ。既存 33 件は 1 つも動いていない）。

**この 1 件から全体ルールが出た（2026-08-05 作者裁定）** — **Android 固有の問題
（OS の機能・ハードウェアの制約）以外は、裁定も実装も server に合わせる。**
**移植の受け入れでは「同じ結果か」でなく「同じ判定か」を見る**。falsy 判定を
既定値つきの取得へ置き換えた移植は、`0` や空文字だけが別の道を通り、
**値が一致する入力だけを見ている限り永久に見えない。**

**段 4 には今もゲートが無い。** `radial` の回転中心をキャンバス中央へ戻す摂動は
**0 / 134 件**しか赤くしない — 中心の移動は群れ全体の平行移動で、**直後の段 5 が
重心をアンカーへ寄せ直すときに完全に吸収される**。段 5 の除外は `grid` かつ `at` 在りのときだけで、
`radial` は決して該当しない。**段 4 を 1 行も実装しなくても 34 ケースは全部緑になる**（[I-130]）。

**摂動は 7 段ぶん当てた**（段 1 は `test06SurfaceHatchExactParity` と SVG 走査、
段 2 / 3 / 5-1 / 5-2 / 6 は `testArrangementExactParity` と SVG 走査、段 4 は 0 件）。
**すべて製品コードへ当て、戻したあと全件緑に復することを確かめた。**
**`server/` は 1 行も変わっていない**（凍結コーパス 2 群ともバイト一致・`APP_VERSION` と
`web/BUILD_NUMBER` は不動）。

### Android `2.1.4-android.7` — server の判定へ揃える 5 件（感情ヒント・Stage 2 の連結・背景ガバナ・temper 2 本・版フォールバック）（android Build 148091 据え置き、2026-08-05）

**5 件はどれも「Android に機能が無い」ではなく「同じ場所に別の判定が書いてある」だった。**
2026-08-05 の全体ルール（Android 固有の事情以外は server に合わせる）が 5 件すべての向きを決めており、
**どちらへ揃えるかを実装が考える余地は無い**。受け入れは「同じ結果か」ではなく
**server の条件式と Kotlin の条件式を 1 対 1 で読む**形で行った。

- **感情ヒントの撤去（[I-114]）:** `InkuRepository` が 16 語の感情辞書を引いて
  `[感情語をDDLに反映してください: …]` を作り、**`description` 自体へ連結してから** Stage 1 へ渡していた。
  server と web はこの機構を持たない。**web は `stage1_input` へ足していたので、Android の撤去は影響範囲が広い**
  （プラグインの発火・Stage 1.5 の文脈・種・言語がこの文字列を見る）。撤去後の `stage1Text` は `description` そのもの。
  木全体で `emotionHint` / `emotionDdlMap` / `感情語をDDL` の残りは 0 件。
- **Stage 2 への記述の連結を切った（[I-125]）:** server は v2.9.41 で `user_msg = ddl` の 1 行にしている。
  Android は `[原文]…[正規化DDL]…` を組む `buildStage2UserMessage` と、`context = "$originalText\n$ddl"` を作る
  `scoreFromWebRules` の **2 経路**（呼び出しは 3 箇所）で原文を運んでいた。両方を落とし、
  `scoreFromWebRules` からは引数ごと外した。`originalText` は Stage 1.5 の展開と記録では引き続き使う。
- **背景ガバナの語彙と 4 つ目の条件（[I-126]）:** `EXPLICIT_SURFACE_MARKERS` の 14 語目 `dark field` が
  Android にだけ無かった。**1 語だが絵が変わる** — `dark field of thin lines` は server が黒地を保ち、
  Android は `細い` が密度ガバナを発火させて白へ洗っていた。4 つ目の条件も割れており、
  Android の「色が在って図形が無い」は限定句のない `青と赤で塗る` にも真を返していた。
  server の `_color_only_constraint_from_ddl`（`だけ` / `のみ` / `に限定` / `only` / `limited to` を要求する）を
  `colorOnlyConstraintFromDdl` として移した。**明示句の正規表現は移していない** —
  語の一覧に包含されるので、単独で効く入力が存在しない。
- **未移植だった temper 2 本（[I-009]）:** `_with_quiet_single_shape_tempering` と
  `_with_unintentional_filled_shape_tempering` を移した。定数は server の値をそのまま写している
  （単体 0.34 / 0.24 / 0.17 / 0.14、塗り 0.42 / 0.30 / 0.20 / 0.20）。
  **塗りの一巡は密度ガバナの中では決して発火しない** — 塗りの閾値（面積 0.20）は単体の閾値（0.14）より高く、
  単体が先に寸法を丸めて必ず下回らせる。したがって server と同じく
  `withPresenceAuxiliaryShapeRepair` と `withContextDensityGovernor` の **間に独立した一巡**として置いた。
- **`renderHash` の版フォールバックを外した（[I-011]）:** `render_engine_id` / `render_engine_version` が
  metadata に無いとき `"21"` で埋めていたのをやめ、JSON `null` を置く（server は「無ければ無いまま」）。
  既存テストは削除ではなく**裏返して**残した（`testRenderHashDefaultsMissingAndBlankMetadataToEngine21` →
  `testRenderHashPreservesNullEngineVersionWhenMissingInMetadata`。欠落と明示 `"21"` が別の hash になることを見る）。
- **契約が名指ししていない共有関数 2 つも割れていた。** `closedShapeArea` は Android が π を掛けており
  （`arc` は ×0.35、`triangle` は ×0.5、`cloudform` も面積を持っていた）、server は `radius * radius` と
  `size[0] * size[1]` の素の積で、`arc` と `cloudform` を 0 と読む。`capSize` は Android が軸ごとに独立して
  切り詰め、server は**縦横比を保った比例縮小**で下限 0.01 を置く。**どちらも temper 3 本だけでなく
  背景・大形・雰囲気の判定にも効く**（Android 6 箇所・server 5 箇所）。揃えた結果、比例縮小が効いて
  `triangle-open-plain` の期待値は `[0.34, 0.1942857…]` になる。
- **受け入れで検査面の穴を 1 つ埋めた。** 段 0 で焼いた 19 ケースは reflection で temper 3 本を**直接**呼ぶので、
  **`normalizeServerScore` から独立した一巡の結線を外しても 135 件が全部緑だった** — 期待値は
  機構の中身を見ているが、**製品がそれを呼んでいるかは誰も見ていなかった**。`normalizeServerScore` を通す
  `FilledShapeTemperingWiringTest` を足して 136 件へ。期待値 `[0.36, 0.30]` は同じ入力を server の
  `coerce_score` へ通して実測した値で、結線を外すと `[0.6, 0.5]`（丸めが一切かからない姿）へ割れる。
- **摂動は 7 件当てた。** 段 2 = Stage 2 メッセージ 1 件、段 3 = `dark-field-black` 1 件、
  段 4 = `square-open-plain` 1 件、段 5 = `Engine19VersionTest` 1 件、共有関数を旧実装へ戻すと
  `triangle-open-plain` 1 件、結線を切ると新しいゲート 1 件。**段 1 は 0 件で、ゲートが無い** —
  単体テストは `pipeline.paint()` を直接呼び、`InkuRepository` を通らないため、感情ヒントを戻しても何も落ちない。
  すべて製品コードへ当て、戻したあと全件緑に復することを確かめた。
- **`server/` は実装の 3 commit で 1 行も変わっていない**（枝に載っている生成器 86 行は段 0 で発注側が置いたもの）。
  期待値 `server_reference/` も実装は書き換えていない。**全 59 ファイルは今日の木が焼き直すものと 1 バイト違わない**
  （`test_android_reference_fixtures_are_current.py` が全件を焼き直して比較する）。
- **検証:** Android 単体 **136 件 / 失敗 0 / skipped 0**、server pytest **2341 passed / 31 skipped**、
  cli **106 passed**、ruff clean、`npm run check` **245 files / 0 errors / 2 warnings**。
  `APP_VERSION` と `web/BUILD_NUMBER` は不動、`android/BUILD_NUMBER` は単体テストのみのため 148091 据え置き。
- **積み残さなかったもの（明示）:** [I-008]（埋め込み Stage 2 スキーマの乖離）・[I-067] / [I-068]（系譜データ層）・
  [I-064]（実機 espresso）は契約の対象外で触れていない。`android/ANDROID_SPEC.ja.md` は更新していない。
  実機 Pixel 9 での確認は行っていない（JVM 単体のみ）。pentala へは配備していない（`android/` は全経路から恒久除外）。

### v2.9.45 — 制限値を 1 か所へ集め、モデルに頼らない天井を置く（Build 852、2026-08-05）

**「配置数の上限を LLM に頼るのはプログラムとして危険ではないか」という作者の問い（2026-08-04）から出た改修である。**
数を縛る値は 4 つのファイルに散り、同じ 240 が別の名前で 4 通りに借用されていた。
本版はその値を `limits.py` の 1 か所へ集め、**決定的な層に天井を置く**。**既定値は 1 つも変えていない。**

- **制限値を `limits.py` へ集めた。** `Limits` の frozen dataclass に 8 つの値を置き、
  `coerce/` の読み手を全部そこへ通した。**値はすべて改修前の木から読み取ったままで、動かしていない**
  （400 / 240 / 240 / 80 / 120 / 1000 / 2000 / 2000）。**新設は `max_instructions = 64` の 1 つだけ。**
  美的なガバナ（`MAX_QUIET_*` ほか）は**同じ数字の別名ではなく別の目的を持つ数**なので、
  意図して元の場所に残してある。
- **直値 2 箇所が方針を破っていた（[I-110]）。** `compose.py` の `min(count, 120)` は
  **「代表化の帯の上端」を「無条件の上限」として使っていた**。SPEC は「240 未満は literal」と定め
  「二百三十三本は 233 本描かれる」と名指しで書いているのに、節の経路を通るとその 233 が 120 に切られていた。
  本番 2,172 作品・arrangement 6,419 件を数えると、**上限にちょうど当たった 92 件のうち 6 件がこの違反**だった
  （異なり入力は 233 と 137 の 2 種）。`_budgeted_count` へ寄せて閾値未満は素通し、
  閾値以上は**密度ガバナと同じ `_clustered_visual_count` へ委ねる** — 2 つの経路が
  帯の中の別の数ではなく**同じ数**に着地する。
- **総量に決定的な天井が無かった（作者裁定 2）。** 番人は 2 つとも `grid` を明示的に除外しており、
  **合計にすら入れていなかった**。命令の本数には上限が無く（`schema.py` は素の `list[Instruction]`）、
  arrangement 1 件は 2000 まで通る。**5 命令 × 2000 = 10,000 印 = Mac で約 42 秒・SVG 約 35MB** を
  止めていたのは、**プロンプトが「1〜5 命令」とお願いしていることだけ**だった。
  **穴は仮説ではない** — 番人の導入より後の 2026-07-18 の作品が総数 409（grid 400 + 非 grid 9）で、
  番人は grid の 400 を数えていないので超過に気づいていなかった。
- **`_enforce_hard_ceiling` を coerce の最後（2 つの出口の両方）に置いた。** **layout に答えない。**
  grid は `count` ではなく **`rows × cols`＝実際に描かれる印**で数え、超過分は**比を保ったまま**
  小さい格子へ落とす。命令数は 64 で切り、落とした本数を note に残す。
  **既に天井の下に在る作品はバイト単位で不変**（T-6 が対照として見る）。
- **既存テスト 11 件が落ち、すべて調べて更新した。** うち **6 件は「grid は総量から除外される」という
  旧方針を記録していた**もので、作者裁定 2 と正面から衝突する。**番人の除外は今も真**なので
  `_with_total_density_budget` に直接当てる形で残し、**入口経由では天井が届くこと**を別に主張する形へ分けた。
  **golden の H-12 / H-16 は 48 → 233 / 64 → 233 へ動いた — 本番の違反そのものが golden に写っていた。**
- **凍結コーパスは 2 群ともバイト一致**（`ddl-engine-6` 36 件・`render-engine-21` 525 件が 0 件動く）。
  **engine 版数は上げない。****ただしこれは回帰の網であって、改修が効いた証拠ではない** — 契約は
  受入に凍結コーパスを使うことを禁じ、T-1〜T-9 を別に据えている。
- **摂動は 10 本、すべて製品コードへ当てた。** 受け入れ側で 2 本を再現した。
  **P4（閾値の上を 120 固定へ）は T-4 だけを赤くする** — 帯の中に収まってはいるが 2 つの経路が
  違う数に着地する実装で、T-2・T-3 は緑のまま通る。契約が予言した判別点がそのとおり効いている。
  **P9（天井を無条件に掛ける）は T-6 を赤くする** — 「全部 400 へ clamp すれば T-5 は通る」を止める対照。
- **`inku-cli` を 9 本走らせたが、修正した経路には 1 本も届かなかった**（起点と HEAD で byte 一致）。
  節の経路は `_with_ddl_coverage` の **`len(instructions) != 1` の門**の奥にあり、
  **Stage 2 の出力がちょうど 1 本のときにしか通らない**。**対照**として門が開く形を通すと
  **起点 120 → HEAD 233** に割れるので、**仕掛けは差を検出できる。9 本が一致したのは分岐へ到達しなかったからである。**
- **契約に無かった発見 3 件:** ①同一の grid を 3 本並べると `_dedupe_instructions` が 1 本に畳むので、
  T-5 の fixture は構造的に相異なる 3 本にしないと穴を通らずに緑になる ②「四百個」の literal 要求は
  coerce 自身が構図アンカーを 1 本足すため 401 になり、天井が grid から 1 セル取る ③grid の実描画数は
  `count` ではなく `rows × cols`。
- **SPEC:** §13.10 の総量の段落の直後に、**天井の 1 段落を日英とも足した**（grid を含む総量と命令数に
  上限があること・プロンプトのお願いに依存しないこと）。既存の `:1502-1505` / `:1806` の趣旨は変えていない。
- **検証（マージ後の木）:** pytest **2358 passed / 31 skipped**（枝の上では 2338 → 2355。
  main 側の Android 3 件を含めて 2341 → 2358）・`def test_` は **1189 → 1200 で削除 0**
  （main にしか無い 3 件は Android の段 0 で、枝が古い main から切られているため）・
  cli **106 passed**・ruff clean・`npm run check` **245 files / 0 errors / 2 warnings**・
  `check_frozen_corpora.py` バイト一致。
- **積み残さなかったもの（明示）:** **可変にする側は後半契約 `limits-are-settings.md`**（設定・記録・UI・Android）。
  本版は設定も UI も持たず、`renderer.py` / `schema.py` / プロンプト本文 / `web` / `android` を 1 バイトも触っていない。
  **[I-132]**（総量の数が `limits.py` の外に直値で 3 箇所残っている）は**値を変えない本版では挙動が同一なので直していない**。
  `ANDROID_SPEC` は触っていない。resvg のラスタ化時間は測っていない（SVG 生成のみ）。

### v2.10.0 — 制限値が設定になり、描いた値が作品に残る（Build 853、2026-08-05）

**前半 v2.9.45 が 1 か所へ集めた 9 個の制限値を、設定として動かせるようにした改修である。**
**設定を per-install にしても再現性が壊れないのは、実効値を作品へ記録するからである** —
版がコードを同定し、記録された制限値が構成を同定し、二つ揃って挙動が決まる。
`history.render_limits` は **DEFAULT なし・backfill なし** で、既存行の `NULL` は
「既定値で描いた」ではなく「この列より前に描かれた」を意味する（`sketch_state` と同じ規律）。

- **設定の読みは要求ごとに 1 回**（`_effective_limits()`）。プロセス内キャッシュは置いていない
  （複数 worker で腐る）。**`coerce_score` の呼び出し元 5 経路すべてに `limits=` を通した** —
  履歴 SVG の描き直し・`POST /api/history`・`POST /api/compose`・`POST /api/render-score`・
  `/api/paint` と `/api/paint/stream` の共通生成器。
  **`Score.model_validate` の中の count 丸めだけは引数を受け取れない**ので、
  5 経路が同じ 1 個の `Limits` から明示引数と文脈変数の両方を立てる形にした。
  **要求に入らないコード（生成器・大半のテスト）は既定値で走る。**
- **プロンプトへ実効値を注入した。** 契約は「本文が数字を含むのは 10 箇所」と数えていたが、
  **実測は 12 + 8 + 1 箇所だった** — `composer.py` の `max_expanded_primitives` の 400 が
  日英 2 回ずつ、`interpreter.py` の `ddl_count_max` / `ddl_count_max_grid` が日英 4 箇所ずつ、
  さらに **`schema.py` の `count` の説明文**。説明文は tool schema としてモデルへ渡り
  `stage2_prompt_digest` に入るので、本文だけ直しても schema が古い数を教える。
  **400 を放置すると `max_expanded_primitives` を動かしたときにプロンプトだけが嘘をつく**ため、
  作者裁定 A で注入対象に加えた。**全体の動的生成はしていない — 数字の箇所だけ差し込む。**
  密度の帯（`40〜120` など）は**上端だけが天井で内側の縁は密度の散文**なので、
  天井へ丸めるだけで比例伸縮はしていない（伸縮は誰も選んでいない数を作る）。
- **`schema.py` の `Field(le=2000)` を外した**（静的な天井の消滅）。
  外すと JSON schema から `maximum` が消えて tool JSON が変わり **既定値の digest が動く**ので
  （実測 `5dc72855…` → `73e56ff2…`）、**`_score_tool_schema()` が実効値の `maximum` を
  pydantic と同じ鍵位置へ入れ直す**形で両立させた。鍵順は [I-038]（宣言順が搬送率を動かす）のため崩していない。
- **設定 UI に「制限値」タブ**（admin 限定・9 個すべて編集可・3 群・既定値へ戻すボタン）。
  **群の名前と並びはサーバーが返す**（`LIMIT_GROUPS`）ので UI が 4 つ目の群を発明できない。
  **丸められたら丸めた後の値を応答から表示する**（入力値を再表示しない）。
- **Android は `clusterCount()` の裸の 240 を `LITERAL_COUNT_THRESHOLD` へ寄せた。**
  **`MAX_EXPANDED_PER_INSTRUCTION` は使っていない** — 既定では同じ 240 だが server では別のフィールドで、
  そこへ向けると実在しない同一性を主張することになる。同じ関数の `120` は裸のまま残した。
- **CLI は作者裁定 B でフル制御にした** — `config show` が `render_limits` を返し、
  `config update` に 9 個すべての旗と `--limits-reset`。**渡した旗だけを送る部分更新**である。
  `cli/README.md` の help ブロックは手書きせず生成器の出力を貼った。
- **契約の断定 2 件が実測と食い違った。** ①「不正値は拒否ではなく丸める（既存の正規化と同じ流儀）」の
  括弧書きは偽で、**見本に指名された 2 つの正規化はどちらも範囲外で `ValueError` を投げる**。
  契約 §2.4 が丸めた後の値を表示せよと書いているので**丸める側を採り、既存 4 設定とは違う流儀になった**。
  ②`DEFAULT_LIMITS` の既定引数は契約の 14 でなく **13**（契約が import 行を数え込んでいた）。
- **受入は `server/tests/test_limits_are_settings.py`（15 検査・T-1〜T-12）。凍結コーパスは証拠に使っていない。**
  実装が当てた摂動は **6 本すべて製品コードのみ**（テストは 1 行も書き換えていない）。
  **摂動の途中で受入を 2 度強くしている** — ①日本語側だけ定数へ戻したのに T-2 が緑だった
  （英語側が動くので「どれか動いた」を満たす）ので、**どの言語のどのプロンプトが動くべきかを厳密な集合へ**
  書き直した ②同じ閾値を日本語プロンプト内の 2 行が強制していたので**強制点を数えて両方に当て直した**。
  **T-2 の逆脚は契約から書き換えてある** — 裁定 A で 400 を注入対象にしたため契約の
  「同じ閾値・違う予算 → 本文が変わらない」が恒偽になり、**どのプロンプトにも出ない唯一の制限値
  `max_instructions`** を逆脚に据え直した。
- **受け入れ側で足したもの: CLI の 4 ゲート**（`cli/tests/test_cli.py`）。
  **裁定 B で足った 9 個の旗と新しい経路 `PUT /api/settings/limits` を見ている受入が
  「4 領域の点呼で cli が 1 件以上」しか無く、鍵名を書き間違えても 200 のまま沈黙する**。
  旗ごとに違う値を渡して本文と 1 対 1 で突き合わせる形・部分更新であること・対照（旗が無ければ要求しない）を据えた。
  **摂動 3 本**（未指定も全部送る／鍵名を 1 つ改名／`--limits-reset` を置換でなく追記）を当て、
  **3 本目が空振りしたので受入を強くした** — `--limits-reset` を単独で渡すと本文は
  どちらの実装でも `{"reset_to_defaults": True}` になり、**置換と追記が見分けられない**。
  **値の旗と同時に渡す形へ書き直して赤にした。**
- **SPEC:** 閾値が構成であることを**日英とも 4 箇所ずつ**明記した（`:1534` / `:1538` / `:1837` / `:1839` 相当）。
  **契約が名指した `:1502-1505` / `:1806` は行番号が古く、現物は 4 箇所だった。**
  **消えた定数名 `MAX_EXPANDED_PRIMITIVES` / `MAX_EXPANDED_PER_INSTRUCTION` の言及も 0 件にした**
  （製品には既に無く、SPEC だけが名前で呼び続けていた）。「233 本の線は 233 本である」の趣旨は残し、
  **「その構成の閾値までは」を添えた。**
- **検証（マージ後の木）:** pytest **2373 passed / 31 skipped**・`def test_`（server）**1203 → 1218 で削除 0**・
  cli **106 → 111 passed**・ruff clean（server / cli）・`npm run check` **245 files / 0 errors / 2 warnings**・
  `lint:i18n` **1017 / 47 / 0 / 0**・`npm run test:unit` **113**・
  `check_frozen_corpora.py` **バイト一致**・`check_docs.py` 緑（内部参照 55）。
  **API 表面の基準は焼き直してある**（`Arrangement.maximum: 2000` の消滅と意図した追加だけで、落ちたフィールドは 0）。
  ルート総数は **81 → 82**。
- **minor にした理由:** 保存データに列が増え（`history.render_limits`）、API に経路が増え、
  Stage 2 の tool schema から静的な `maximum` が消えている。**engine 版数は上げていない**
  （既定値で凍結コーパスは 0 件動く）。
- **実施しなかったこと（明示）:** **`_cluster_count` の裸の 240 / 500 / 120 は直していない** —
  その 240 は `literal_count_threshold` と同じ境目なので**閾値を動かすと二つがずれる**が、
  契約が対象として名指していないため **[I-136] として起票した**（選択肢 3 つ）。
  **Android は実機 Pixel 9 で目視していない**（コンパイル成功まで）。
  **CLI から生きたサーバーへ設定を PUT して描く往復は通していない**（旗から送信本文への写像は実測済み）。
  **既定値は 1 つも変えていない・`renderer.py` は 1 バイトも変えていない・Android に設定 UI は作っていない。**
  `ANDROID_SPEC` への段 5 の記載も未実施。
- **台帳:** **[I-136]**（`_cluster_count` の裸の 240）と **[I-137]**（web が送る `sketch_grain_change` を
  server が知らず 422 になる。`android-lineage-wiring` セッションの起票）を採番した。

### Android `2.1.4-android.8` — 系譜の表に、書き込む使い手を与える（android Build 148092、2026-08-05）

**[I-068]。** 系譜の節と辺のテーブルは移植済みだったが、**保存の経路から誰も書いていなかった。**
本版で `InkuRepository.saveResult` が保存 1 回につき節を 1 つ書き、
**宣言された派生のときだけ**辺を 1 つ書く。**server が正本で、Android は移植先である。**

- **段 1: 何を書くかを決める純関数**（新規 `LineagePlanner.kt`）。
  **`db.py` の条件式と 1 対 1 で並べて写した**（レポート §2 に対比表）。
  **条件式を写すために足したものが 3 つあり、いずれも「こちらが良い」判断ではない** —
  ① **Python の真理値**（`isTruthy`）。`or {}` は**空リストを `{}` にする**ので
  `["not","an","object"]` は拒否・`[]` は受理になる。Kotlin で素直に型で分岐すると
  **両方拒否になって判定が割れる**（実行規約 §2-4 の `optDouble` と同じ形）
  ② **親を宣言して種別を書かない保存は「不正な種別」として拒否される** —
  server では `None not in LINEAGE_DERIVATION_KINDS` が真になるため、
  「親が要る」でも「種別が要る」でもなく `invalid lineage derivation kind` が返る
  ③ **`_canonical_json` を自前で書いた** — `JSONObject.toString()` は挿入順を保つので
  `sort_keys=True` の再帰・空白なしの区切り・`ensure_ascii=False`・**コードポイント順**の
  鍵の並べ替え（Kotlin の `String` 自然順は UTF-16 コードユニット順で追加面の鍵で割れる）を写した
- **段 2: 保存の経路から呼ぶ**（本契約の要）。**判定は行を作る前に済ませ、拒否なら履歴行も作られない。**
  **節 → 辺の順で 1 トランザクション**（SQLite の外部キーが子の節を先に要求するため。server と同じ順）
- **段 3: 派生種別を server の 16 種へ揃えた**（従来は宣言順 11 種）。
  **一覧は焼いた期待値から読む**ので、server に種別が増えたらここが赤くなる。
  **足した 5 種の日本語ラベルは web にも server にも既存が無く**（`derivation.ts` の `JA` は 12 種で
  この 5 つを持たない）、既存の作法（短い名詞）に合わせて当てた —
  `render_engine_change`=描画エンジン / `age_change`=経年 / `hacho_change`=破調 /
  `renga_reply`=連歌の付句 / `external_seed_change`=外部の種
- **摂動 5 本・的は全て製品コード**（テストは 1 行も書き換えていない）。**0 件の段は無い。**
  **P2（結線を外す）で JVM 143 件は全部緑のまま**で、赤くなるのは**実機の 5 件だけ**だった —
  **純関数の検査は段 2 の受入になりえない**という契約 §2 の警告がそのまま出た。
  **P5（辺の挿入をトランザクションの外へ）は P2 では見えない** — 節も辺も書かれてはいるので、
  トランザクションの検査だけが割れる
- **検証（マージ後の木・受け入れ側が実機で再実行）:** **JVM 単体 143 / 失敗 0**（38 クラス）・
  **計装 21 / 失敗 0**（実機 Pixel 9 `54100DLAQ0028F`・エミュレータは使っていない）・
  server `pytest` は `test_android_reference_fixtures_are_current.py` が
  **`lineage_wiring.json` を焼き直して現物と突き合わせている**。
  `APP_VERSION` と `web/BUILD_NUMBER` は不動・pentala 反映も不要
- **実施しなかったこと（明示）:** **UI の呼び出し箇所へ親を渡す結線はしていない** —
  `InkuRepository` の入口 3 つに口を開けただけで、`InkuViewModel` / `InkuApp` は既定値のまま呼ぶ。
  **したがって現状の実機操作では、節は毎回書かれるが辺は 1 本も書かれない。**
  **系譜を見せる UI も無い。**[I-067]（奥書・`unread_words`）は契約から外されている。
  **`history_visibility` 列は足していない**（Room version 6 への migration になり、契約の段に無い。
  判定は写して測っているが**実機では `normal` の道しか通らない**）。
  **系譜表に `user_id` 列が無い**ので server の「親は同じ利用者」条件には対応物が無い（1 台 = 1 利用者）
- **台帳:** **[I-068] を既決へ移した。** 本版の実装中に見つけた server 側の乖離は **[I-137]**
  （web が送る `sketch_grain_change` を server が知らず 422 になる）として起票済みで、
  **裁定が出るまで期待値 `lineage_wiring.json` の 16 種は焼き直さない**

### v2.11.0 — 添景水準を畳む: 記述に無いものを足す機構を落とした（Build 854、2026-08-05）

**記述が契約のすべてである**（2026-08-05 作者裁定）。**添景水準 (tenkei) は「どれだけ足してよいか」を
決める軸で、記述に無い要素を層が発明する許可そのものだった。**軸ごと畳み、**畳んだ後の挙動は
旧 `tenkei="none"` と一致する**ことを実測で確かめた（coerce 61 件・Stage 1.5 15 件・
英語 taste の経路 3 件で**差分 0**）。**API から鍵が消えるので minor である。**
控えはタグ `archive/tenkei-v1.97` と `no-git-sync/archive/tenkei/`。

- **coerce から挿入予算と発明する 6 分岐を落とした** — `_with_visual_event` /
  `_with_composition_diversity_repair` / `_with_context_energy_repair` / `_with_motion_floor` /
  `_with_surface_tension` / **`_with_focal_event_floor`**。
  **契約は 5 本を挙げていたが実測は 6 本だった**（同じ節が「9 / 31」と書いており、その差の 1 本）。
  **6 本目を残すと旧 `none` と一致しない。****配達する 3 分岐は残る** —
  `with_ddl_coverage` / `with_complex_motif_repair` / `with_shape_delivery_repair`。
  到達性解析で **41 定義・1,236 行**を削除（`compose.py` 3,646 → 2,410 行）
- **Stage 1 / Stage 1.5 の水準写像を落とした。** `_expand_ja` / `_expand_en` は**焦点の書き換えで
  終わる**ようになり、候補プール・カテゴリ計画・構図族・色/タッチの選択が到達不能になって
  **35 定義・467 行**が消えた（`ddl_expander.py` 1,081 → 594 行）。
  **変奏の軸は 7 → 1（焦点のみ）**。残る 6 軸は Stage 1.5 が自分で足した文を振るもので、
  候補プールと運命を共にする。**強度は今も効く**（40 seed 中 40 seed で小・中・大が別の焦点に落ちる）。
  `language_support/en.py` の `_english_taste_additions`（jazz / quilt / subway ほか 7 語群）も
  **`auto` のときだけ出ていた**ので添景であり、削除した
- **API・UI・CLI から旗を落とし、DB の記録は残した。** `tenkei` 列・migration・読み出し・
  書き込み側の 4 点はそのままで、**撤去前に保存された作品は描画条件を報告し続ける**。
  **宣言は `HistoryPostBody` から応答模型 `HistoryItem` へ移した** — そのまま消すと
  過去作の応答から鍵が落ちる（受入 T-4 が捕まえた）。**生成情報の「添景」行は開発者モードで
  値がある過去作にだけ出る。**web は `TenkeiSelect.svelte` ほか 4 ファイルを削除して
  **使い手 16 ファイル**を追随、cli は `--staffage` と送信 2 箇所・README の usage 2 箇所
- **`ComposeRequest.lineage_parent_node_id` も落とした**（水準の継承のためだけに在った旗）。
  **API 表面から落ちたフィールドは 7 個** — 6 模型の `tenkei` とこの 1 個で、
  endpoints・operations・schemas は **82 / 82 / 82** のまま
- **`ddl_engine_version` 6 → 7。** 凍結コーパスは弁別 case を畳んで焼き直し、
  **無発明を凍らせる case を 2 件新設**（`B-surface-tension-words` / `B-leaf-grain-words`）。
  `changed_from_previous` は 34 中 32 だが、**Score が動いたのは a_expand 10 / b_coerce 9** で、
  残りは branch_report だけが動いた
- **展開層の凍結コーパスは pytest では記録しか持っていなかった。**
  `test_ddl_reference_output_files_match_manifest` は**ディスクのファイルと manifest を
  突き合わせるだけ**なので、両方を焼き直さない限り一緒に動く（再生成するのは CI だけ）。
  **候補文を戻す摂動で 1 件も赤くならなかった**ため、a_expand 13 件を pytest 内で焼き直して
  digest を突き合わせる門を足した（**何件見たかも表明する**）
- **純明示バイパスとプラグイン転写ガードは水準から切り離して残した。** どちらも添景ではなく、
  前者は名前空間付き語を LLM に書き換えさせない転記の忠実性、後者は二重配達の防止である。
  **受け入れでバイパスの強制点が 2 箇所あることを実測し**（`/api/interpret` と paint 側の
  共通生成器）、**paint 側を切っても 2312 件が全部緑だった**ので、そちらにもゲートを足した
- **Android の参照 fixture `ddl_expand.json` を焼き直した**（39 → 30 case。`*-tenkei-*` 13 件が
  消え 4 件が入る）。**Kotlin は 1 行も触っていない**ので、**Android の JVM 単体は 143 件中
  4 件が赤い**（`WebDdlExpanderPhase3a/3b/3d` の参照突き合わせ。Kotlin が
  `input.optString("tenkei", "auto")` で読むため、鍵が消えると既定 `auto` で走る）。
  **作者裁定 2026-08-05 により、古い期待値に対して緑であり続けるより赤を出す側を採った。**
  Kotlin の移植は Android トラックで行う
- **検査:** server **2373 → 2313**（`def test_` 1312 → 1261。削除 88 / 新設 37）・
  cli 111 据え置き・`npm run check` **245 → 241 FILES / 0 errors**・`test:unit` 113 据え置き・
  `lint:i18n` 1017 → 1004・**凍結コーパスはバイト一致**

### 2026-08-05 — マニュアルを v2.11.0 へ追随させた（**採番なし**・ドキュメントのみ）

**`manual/` の日英 7 文書が v1.85 / Build 564 のまま 51 版ぶん取り残されていた。**台帳 [I-003]。

- **版を名乗る 11 箇所**を v2.11.0 / Build 854 へ直した。**マニュアル直下の索引だけ v1.82 / Build 563** とさらに古かった
- **`画像の作成方法` を全面改定した。**15 節 → 20 節。**写生（Stage 0.5）・変奏・暴れる・色カタログの「記述から自動選択」・UI モード・推敲マーク・再現・コンタクトシート・アニメーション書き出し・ハッシュ下位 4 桁の検索・設定 10 タブ**が全部未記載だった。**`言語比較` は現行 UI に無いので落とした**（`language_variation` は保存済み作品の派生種別としては残る）
- **`inku-cli リファレンス` へ 6 コマンドを足した** — `plugin` / `reference` / `colophon` / `user` / `group` / `config`。**`paint` の旗は 30 のうち 4 しか載っていなかった**ので、用途別の 6 表に組み直した（写生 3・変奏 2・`--wild`・`--catalog-mode`・`--interpretation-seed` ほか **26 が未記載**）
- **`サーバー設定方法` へ環境変数 19 個を足した**（コード 52 / 文書 33 → **52 / 52**）。**廃止済みの記載は 0 件**だった。層とプラグインの節 2.5 を新設し provider を 2.6 へ送った。**制限値の節 5.1 を新設**し、九つの値・既定・丸めの規則と、**環境変数は初期値を与えるだけで以後は DB の設定が正本である**ことを書いた
- **render hash を `rh2:` → `rh3:` へ直した。**canonical payload の内訳（Score / `render_seed` / `render_wild` / engine / カタログ）と、鍵名を改名すると保存済み作品の hash が全部作り直しになることを足した
- **`アプリケーションインストール` の前提を Python 3.10 以上 → 3.12 以上へ直した**（`requires-python` は `>=3.12`）。**この 1 行は導入が失敗する側の誤り**だった
- **AI 向けリファレンスの `--kind reading` の説明を直した** — **Stage 1.5 ではなく Stage 1** で、そもそも Stage 1.5 は LLM ではない。**日英が 1 文に混ざって壊れていた箇所**（`refine perform`）も直した
- **「沈黙する送り手に注意する」（0.8）を足した。**旗を書かなければサーバー既定で描かれ、**サーバー既定と Web UI 既定は違う**（写生はサーバー切 / UI 細かく）。**変奏は 2 旗が揃ったときだけ効くので、旗を渡したことは成功の証拠にならない**

**分かったこと 1 件**

- **`check_docs.py` は `manual/` を 1 組も見ていない**（対象 13 対はルート直下と `docs/` だけ）。日英対応を強制するゲートが無いまま、マニュアル配下の日本語 README と英語 README は最終更新が 11 日ずれていた。**今回そろえた 7 対 25/25・20/20 などの一致は、検査ではなく手作業の結果である**

### 2026-08-05 — マニュアルの日英対応にゲートが付いた（**採番なし**・検査のみ）

**`check_docs.py` が見ていた 13 対に `manual/` は 1 対も入っていなかった。**台帳 [I-140]。

- **`PAIRS` へ 7 対を足した**（13 → **20 対**）。全部 `shape`。**マニュアル直下の索引は相手がいないので入れない**
- **`server/tests/test_manual_parity_gate.py` を新設した**（`def test_` 3 つ・検査 22 件）。
  **`PAIRS` は構文木から読む** — `grep '"shape"'` は 3 つの mode を説明するコメント中の語を拾い、
  **13 対を 14 と数える**（発行側が踏んだ）。**7 対は parametrize で 1 対ずつ表明する**
  （合計だけ見ると「6 対 + 別の 1 対」でも通る）。宣言例外が `None` であることも見る —
  **理由を宣言すると失敗が「declared difference」の印字に変わり、行は残ったまま何も検査しなくなる**
- **段 2 を置いた理由 = `PAIRS` は製品コードでなく設定表である。**7 行を消しても
  **`check_docs.py` は exit 0 のまま**で、「見る対象が 20 → 13 に減った」ことを誰も報告しない
  （**依存の版上げで `app.routes` が 81 → 0 になっても 2 つの検査が緑だった**のと同じ型）
- **判別力**（発行前・実装後・マージ後の木のいずれでも実測）: ja 側にだけ見出しを 1 つ足す摂動が
  **現行 0/7 → 実装後 7/7**。`PAIRS` から 7 行を消す摂動で**新規テストが 8 failed / 14 passed**
- **検査:** server **2313 → 2335 passed / 31 skipped**（`def test_` **1262 → 1265**・消えたテスト 0）・
  ruff clean・`check_docs.py` 緑（内部参照 55）。`web` / `cli` / `android` / `server/src` は 1 ファイルも動いていない

**受け入れで直したもの 1 件**

- **契約が「マニュアルは rsync の対象外なので pentala の木には無い」と書き、実装がその前提を
  コメントへ写していたが、実測すると誤りだった。**pentala はマニュアルを持っている
  （**v1.85 のままの古い複製**で、日英はそろっているので `check_parity` はそこでも緑になる。
  「7 件の missing で赤くなる」も偽）。**`skipif` 自体は残した** — 木にマニュアルが無い場合への
  防御であって、pentala の記述ではない。**コメントを実測へ差し替えた**

### 2026-08-05 — Android が添景水準を畳み、server に追随した（**採番なし**・Android のみ）

**v2.11.0 で server が畳んだ添景水準（`tenkei`）を Kotlin へ移した**（[I-139]）。
**server が正本でクライアントは移植先**という規律に従い、条件式を 1 対 1 で読んで写している。
**版は据え置いた** — 作者指示により `web/APP_VERSION` / `web/BUILD_NUMBER` /
`android/VERSION` / `android/BUILD_NUMBER` のいずれも動かしていない。**配備も再起動もしていない。**

- **`WebDdlExpander.kt` は 1,347 → 540 行**。引数 `tenkei` と水準写像を落とし、
  **変奏の軸は焦点 1 本だけになった**（server の `AXIS_FOCUS` と同じ形）。
  到達不能になった 6 軸・カテゴリ計画・構図族・タッチ・色の機構も、server と同じく削除した
- **`data/model/Tenkei.kt` と UI の選択肢を削除**。**Android には移すべき相手が無いものが 2 つあった** —
  **Room に `tenkei` の列は無く、`tenkei` を server へ送ってもいない**ので、
  server 側の裁定（DB の列は残す・開発者モードで過去作にだけ出す）に対応する仕事は無かった
- **畳んだことで表明が偽になった 6 件のテストは、削除でなく測定点を移した。**
  移し先は**焦点**で、`assertEquals` で出力そのものを固定する形にしてある
- **検査:** JVM **143 / 赤 4 → 156 / 赤 0**（38 → 37 クラス）・計装 **21 → 20 / 赤 0**（実機 Pixel 9）。
  マージ後の木で server **2335 passed / 31 skipped**・cli **111**・ruff clean・`check_docs.py` 緑

**受け入れで分かったこと 2 件**

- **起票文の前提は 4 件中 1 件が誤っていた。**`testContextTextUsageAndDefaults` の赤は
  Kotlin の未追随ではなく、**焼き直したコーパスで 2 つの case の出力が同一になったため**で、
  **Kotlin をどう直しても真にならない表明**だった。発行前の摂動で見つけて契約へ書いた
- **参照コーパスの判別力が落ちている** — 焼き直しで **39 case / 出力の異なり 28（72%）→
  30 case / 14（47%）**。**入力を無視する移植でも 30 件中 16 件は緑になる**ので、
  **判別力を担うのは全件突き合わせではなく焦点の対照のほうである**。→ **[I-141]** へ起票した

### 2026-08-05 — ANDROID_SPEC の「追随状況」を実測へ合わせた（**採番なし**・ドキュメントのみ）

**[I-013]**（`ANDROID_SPEC` が engine に未追随）に対応した。**生きた記述だけを直し、
日付つきの履歴節は 1 行も書き換えていない** — そこはその日に何を移植したかの記録である。

- **冒頭の「追随状況」が 51 版ぶん古かった。**`2.0.0-android.1` / render engine `11`、
  master は v2.5.0 / engine 12、描画層は 1 版遅れ、と書いてあった。
  **実測は Android `2.1.4-android.8` / render engine `21`**（`CompatibilityConstants.kt` が名乗る）で、
  **master は v2.11.0 / render engine `21` / `ddl_engine_version` 7** なので**描画層の版は一致している**
- **「現在の実装状態」の 1 項目も古かった** — `Renderer engine 2 → 10` / `Phase 1 のみ完了`。
  engine 21 到達と、系譜がデータ層まで移植済みであることへ直した
- **⚠ 「層の版が同じ」は「移植が終わった」ではない**と明記した。
  **残る差は台帳が持ち、本書には写さない**（写すと古びる）。台帳の領域 `android` は本件を除いて 9 項目
- **最終更新の日付を `2026-07-25` → `2026-08-05` へ**
- **検査:** `check_docs.py` 緑。**この対は `sections` モード**（`##` の数だけを見る）で、
  **日英とも 62 で変わっていない**

### 2026-08-06 — 形を検査するテストが、サーバーだけを載せた木でも成立するようになった（**採番なし**・検査のみ）

**開発サーバーは 2 つのサービスが要るものだけを載せる**という運用へ是正したところ、
`server/tests` のうち **8 件が「その木を説明していないこと」で赤くなった**。
`android/` `cli/` `docs/` `scripts/` を読む検査が、それらの無い木で走ったためである。

- **4 件は「そのディレクトリが無ければ飛ばす」へ**（`test_merge_driver.py` の 5 表明と
  `test_rasterizer.py` の `cli/pyproject.toml`、`test_limits_are_settings.py` の 4 領域の数え上げ）。
  **skip はファイルでなくディレクトリに掛ける** — ファイルに掛けると、改名が赤ではなく沈黙になる
- **`cairosvg` 走査の広さの証明だけは、飛ばせなかった。** 旧版は
  **結果に `android` が含まれること**を証拠にしており、**その証拠こそ開発サーバーが供給できない**。
  **2 つに分けた** — ①**仕組みそのものを、そのために作った木で見る**（列挙されない根と
  除外される根を意図して置き、走査が前者だけを拾うことを見る。**どの木でも成立する**）
  ②**現物の木では、ディレクトリ一覧から導いた集合が走査に含まれることを見る**
  （書き下した並びと突き合わせない）。**どちらもクライアント実装を名指ししない**
- **摂動:** 走査を「名前の並び」へ戻すと、**Mac では 2 件・開発サーバーでは 1 件が赤**になる。
  **旧版はこの退行を開発サーバーでは捕まえられなかった**（`android` を要求する表明が
  そこでは元から赤いため）。**新しい①は部分ツリーでも赤くなる**
- **検査:** Mac で server **2336 passed / 31 skipped**（分割で +1）・ruff 緑。
  **開発サーバーの全体 pytest は 13 failed → 0 failed**（2276 passed / 91 skipped）

### v2.11.1 — 制限値タブの九つの数に、DBタブと同じ `−`／`+` を付けた（Build 856、2026-08-06）

**設定 > 制限値**の九つの数は、素の数値入力（ブラウザ既定のスピナーだけ）だった。
**DBバックアップタブは既に `NumberStepper` を使っている**ので、同じ部品へ寄せた。
ボタンの寸法・下限で `−` が無効になる挙動・端数の丸めまで、2 つのタブで同一になる。

- **`<label>` を `<div>` へ変えた。**ステッパーの最初の labelable な子は `−` ボタンなので、
  `<label>` で包むとラベルがボタンを指す。読み上げ名はステッパー側の `aria-label` が持つ
  （DB タブが `<div>` ＋ `<span>` にしているのと同じ形）
- **入力欄がカードの幅いっぱいに伸びていたのを止めた。** カードが 220px 以上あるのは
  **ヒント文のため**で、数字のためではない。**ステッパーを `min(136px, 100%)` に固定**し、
  ヒントはカード幅のまま残した。136px は打ち込める最大値＝絶対上限 `100000`（6 桁）が余る幅
- **検査:** `npm run check` 241 files / 0 errors / 2 warnings（既存の a11y 2 件）・
  `npm run test:unit` 113・`lint:i18n` 1004/47/0/0・`lint:models` 68・`lint:recommendations` 37・
  server **2336 passed / 31 skipped**・cli **111 passed**・ruff 緑
- **`manual/` は版マーカー 13 箇所を更新し、改訂履歴へ日英とも 1 節足した。**
  **本文の説明は変えていない** — 変更の仕方（管理UIか `inku-cli config update`）は動いていないので、
  `サーバー設定方法` 5.1 の記述はそのまま通用する

### v2.11.2 — Android の色カタログの決定点を 1 つに集めた（Build 857、2026-08-06）

**ランがどの色カタログを使うかを決める場所が、Android には 5 つあった。** 描画・DDL・デモ・一括・反復の
各経路が、それぞれ独自に `InkuUiState.selectedCatalogId` を読んでいた。
そのため「**設定以外は何もカタログを決めない**」という主張が 5 箇所同時についての言明になり、
**どの検査もその上に立てなかった** — デモ経路のランダム選択を撤去した際の受入は、
**テスト自身が持つ private helper**（同じフィールドを返すだけのもの）に対して表明していたので、
**製品コードにランダム選択を戻しても 118 件が全部緑のまま**だった。

- **server に合わせた。** server では paint が単一の補助関数を通してカタログを一度だけ解決し、
  受入は**実物の描画 API を通してモデル呼び出しだけを差し替える**。この 2 つを写した。
  **クライアントは server に合わせる**という規律に従い、こちら側の発明は入れていない。
- **決定点は `CatalogSelection.resolvedCatalogIdForRun` の 1 つ**になり、5 つの呼び出し元が通る。
  未使用になった乱数の import も落ちた。**server が持つ 3 つのモードはこの client に無い**が、
  来る日はこの関数の中に置く旨を実装のコメントに書いた。
- **server との違いを 1 つだけ意図して残した:** 一覧に無いカタログ id を渡されたとき、
  **server は 422 を返し、こちらは既定カタログへ落とす**。古いビルドが保存した設定で
  描けなくならないためで、**保存済み設定の後方互換という Android 固有の事情**にあたる。
- **結線の受入を実機に置いた。** 新しい計装テストが 5 つの経路を実物のリポジトリに対して回し、
  **保存されたものからカタログを読み戻す**。**⚠ 覆えたのは 5 つのうち 4 つ** —
  描画経路の `interpret` 引数は**ログ行にしか届かない**ので、摂動しても描画は 1 件も変わらない。
  この穴は台帳に起票してある。
- **検査:** Android JVM **159 / 失敗 0**（37 クラス・起点 156）・**計装 25 / 失敗 0**
  （実機 Pixel 9・起点 20）・server **2336 passed / 31 skipped**・cli **111 passed**・
  `npm run check` 241 files / 0 errors / 2 warnings（既存の a11y 2 件）・ruff 緑。
- **`android/VERSION` は `2.1.4-android.9`。** 描画層の版は動いていない
  （render engine `21` / `ddl_engine_version` 7 のまま）。

### Android `2.1.4-android.10` — 記述が手つかずで渡ることを見る（android Build 148092 据え置き、2026-08-06）

**[I-134]。** [I-114] は Android 独自の `emotionHint` の連結を `description` から落としたが、
**その撤去を守る検査が 1 つも無かった** — **JVM の単体テストは `InkuRepository` を 1 つも作らない**
（context と DB が要る）ので、**連結を製品コードへ戻しても 0 / 136 件しか赤くならなかった**
（2026-08-05 の実測）。**実機の計装に `DescriptionPassthroughTest` を置いて塞いだ。**

- **server に合わせた。** server の記述のゲートは
  「述語ではなくルートを通す」形で書かれており、**モデルだけを差し替えて残りは実物**である。
  ここも同じく、**実物の Room DB と実物の `InkuRepository`** を通し、
  **使われた記述は保存されたものから読み戻す**。
- **観測点は 2 つある。** `description` は **Stage 1 のプロンプトそのもの**になる
  （`LocalFallbackPipeline` が `prompt = request.description` と書いており、
  **テンプレートで包まない**ので表明は厳密に一致で書ける）。`originalText` は
  `PaintResult.originalInput` を経て `history_items.originalInput` に入る。
- **受入は 6 件**（T-1 `paint` の Stage 1 プロンプト／T-2 `interpret` のプロンプトと戻り値／
  T-3 `paint` の保存／T-4 `composeFromDdl` の保存／T-5 `renderFromScore` の保存 2 か所／
  **T-6 は逆向き**で、2 つの記述が 2 つの本文として届くことを見る＝**定数を返す実装が通らない**）。
- **⚠ `composeFromDdl` の `description` にはゲートを置いていない。** その入口は DDL を
  別の引数で受け取り、**`request.description` を 1 か所も読まない**ので、
  **何を表明しても緑になる**（[I-142] と同じ形）。`originalText` のほうは観測でき、T-4 が見ている。
- **摂動で判別力を測った** — 4 つの入口 × 2 つのフィールド = **強制点 8 つに 1 つずつ**当てた。
  **7 つが赤くなり、赤くならなかったのは上の `composeFromDdl.description` の 1 つだけ**。
  **T-2 は最初 `interpret.originalText` を見落としており**（この入口は何も保存しないので
  戻り値にしか現れない）、**摂動が 0 件だったことで分かって足した**。
- **検査:** Android 計装 **31 / 失敗 0**（実機 Pixel 9・起点 25）・JVM **159 / 失敗 0**（37 クラス・不変）。
  **`android/VERSION` だけを上げた** — `APP_VERSION`・`web/BUILD_NUMBER` は不動、pentala 反映も無し。

### v2.11.3 — 写生の区切りを変えた再描画が、保存できるようになった（Build 858、2026-08-06）

**web が送っていた `sketch_grain_change` を server が知らず、その保存が丸ごと失われていた**（[I-137]）。
記述タブから描き直すと、web は親との差から派生の種別を決める。
写生（Stage 0.5）の区切りが親と違えばこの種別を送るが、
**server の `LINEAGE_DERIVATION_KINDS` に名前が無く**、`db.add_item` が
`invalid lineage derivation kind` を投げ、**422 になって作品も履歴も系譜も残らなかった**。
web がこの種別を送り始めたのは v2.9.37（2026-08-03）で、**server は 16 種のままだった**。

- **発火するのは「区切りの変更」だけではない。** 区切りは `fine` / `coarse` / **無し**の 3 値で、
  **無しは写生層が切れている状態**である。判定は親との差なので、
  **写生層を入れる・切る操作でも同じ種別になる**。
- **いちばん太い経路の直前に立っていた。** 分岐は
  キャンバス変更 → 記述編集 → **写生の区切り** → 再描画の順で、
  **本番の 196 辺のうち再描画が 73 辺（37%）で最多**である。
  写生層を常用し始めれば、その手前で止まることになる。
- **まだ誰も踏んでいなかった。** 区切りを持つ作品は本番 2,748 件のうち **25 件**
  （`fine` 23 / `coarse` 2）で、**すべて 2026-08-04 に保存された、辺を持たない起点**だった。
- **server を正とはしなかった。** 「一つの辺に一つの原因」に沿って web が意図して足した種別なので、
  **server の側に 17 種目として足した**（作者裁定 2026-08-06）。
  **保存済みの行が 0 なので改名表への追加は要らない。**
- **既存の受入は、この食い違いを見られなかった。** `test_lineage_acceptance.py` は
  **`LINEAGE_DERIVATION_KINDS` 自身を回している**ので、集合に何が入っていても緑になる。
  **2 つの一覧を突き合わせるものが 1 つも無かった。**
- **`test_derivation_kind_parity.py` を置いた**（4 件）。
  **17 の名前を書き下ろし**、**web の `DerivationKind` を web 自身のソースから読んで**
  **両方向に**突き合わせる（web にあって server に無い／server にあって web に送り手が無い）。
  4 件目は **保存の経路そのもの**を通し、区切りを変えた子が 200 で保存され辺が書かれることを見る。
- **摂動で判別力を測った** — 強制点 5 つ（server の集合・web の union・Kotlin の一覧・
  焼いた期待値・保存の経路）に 1 つずつ当て、**5 つとも赤**。
  ①server から名前を外す = **3 件赤**（既存の受入は緑のまま）／②web の union から外す = **逆向きが 1 件赤**／
  ③web が server の知らない種別を足す = **1 件赤**（[I-137] と同じ形）／
  ④Kotlin の一覧から外す = **2 件赤**／⑤焼いた期待値から外す = **1 件赤**。
- **Android は追随だけ。** この端末に写生層は無いが、**種別の一覧は server のものを持つ**ので、
  `lineage_wiring.json` を焼き直し、`DerivationKindRegistry` を 17 種へ合わせた。
- **`manual/` の系譜の節に「写生の区切り」を日英とも足した。**
- **検査:** server **2340 / 31 skipped**（起点 2336・増えた 4 件が本項）・cli **111**・
  `npm run check` **241 FILES / 0 errors / 2 warnings**・`npm run test:unit` **113**・
  `lint:i18n` **1004/47/0/0**・ruff clean・
  Android JVM **159 / 失敗 0**（37 クラス・件数は不変で、既存の 2 件の中身が 16 → 17 になった）。

### v2.11.4 — 塗りに下地が入り、上に載るものが枝に分かれた（Build 859、2026-08-07）

**筆致が塗りそのものだったので、走査線は輪郭との交点で切るしかなかった**（render engine 21 → 22）。
切らなければ墨が形の外へ漏れる。**その切り口が、目に縞を読ませていた 3 つ目の規則性である** —
作者が縞と名指しした 3 作品の塗られた 11 図形で、1 図形の中の走査角は **0.1°**、
間隔の変動係数は **6.1%**、**端点は 1 つの例外もなく輪郭の上**にあった。

- **面を実体で持つ層（下地）を入れた。** 境界を下地が持つので筆は自由になり、
  走査角は 1 本ごとに **3.3〜3.6°**・間隔の変動係数は **30〜35%**・端点は道具の幅の
  **1.4〜1.5 倍まで両符号で**外れる。**振り幅はすべて道具が持ち、機械は 0.0** なので、
  `computer` の塗りは走査角の標準偏差 0.00°・端点は輪郭の上のままである。
- **下地は両枝に共通で、閾値が決めるのは上に載るものだけ。** 被覆率（線幅 ÷ 走査間隔）**0.2** を境に、
  上は走査線を被覆 **0.9** まで詰め、下は擦りの痕へ分かれる。
  **鉛筆の幅で隙間を閉じるには 8 倍の本数が要り、それはその道具の使い方ではない。**
- **閾値は被覆率であって道具名の一覧ではない。** 両者は engine 21 のコーパスを同一に切るので、
  **同じ道具を太さの語だけで枝の反対側へ送る case を足した。** これが無いと、
  被覆率を 1 度も読まない実装が同点で通る。
- **下地はフィルタで作らない。** フィルタは `display` でしか出ないので、
  フィルタで作った下地は `compat` と `editable` で**塗りそのものを消す**。
  テクスチャ枝の下地は薄い地 1 枚と斑を 3 重の輪で描く 6 枚で、**合成は元の平らな値ちょうど**になる。
- **塗りの終端は絵具の終わりになった** — 着地側が重く、離し際だけが細る。
  **輪郭を引く筆の既定は変えていない。**
- **微小な図形は打点へ、`rotring` は領域 fill へ縮退する。どちらにも下地は入らない**（engine 21 とバイト一致）。
- **凍結コーパスは 525 → 531 件**（`computer` / `silverpoint` / `crayon`+`extra_fine` /
  `brush_thick`+`extra_fine` / `chalk` / `chalk`+`extra_fine` を追加）。**動いたのは 52 件** —
  塗り 32・**chalk の輪郭 14**・新規 6。**chalk の 14 件は塗りではない**（道具の性質は枝をまたぐ）。
  **別の理由で動いたものは別の名簿へ分けた。**
- **図形内部の平均濃度**（円 r=0.3）: pen 15.4 → **69.6%**、pencil 6.9 → **47.8%**、
  silverpoint 2.8 → **50.9%**、brush_thick 52.1 → **82.7%**、crayon 22.2 → **71.9%**、
  chalk 14.9 → **68.3%**、computer 15.2 → **77.8%**。
  **元の帯の周期に残る力**は pen 39.0 → **4.0%**、pencil 28.3 → **4.4%**、
  silverpoint 19.0 → **0.9%**、**computer は 23.6 → 25.0% で不変**。
- **「縞が消えた」とは言えない。** 縞は wash と hatch でも出るもので、
  本版が触ったのは走査線の機構だけである。30 題の描き直しも行っていない。
- **受入 26 本を新設し**（`test_fill_underlay_and_branch.py`）、**摂動 40 本を製品コードへ当てて 40/40 赤**。
  既存の 5 本は**主張を書き直した** — engine 15 とのバイト一致を見ていた 2 本は、
  **engine 22 が意図してその 31 件・30 件を動かす**ので、
  件数・下地の存在・段どうしの排他へ置き換えた。
- **`ddl_engine_version` は動かない**（7 のまま）。Stage 2 までは 1 バイトも変わらない。
- **⚠ Android はまだ engine 21 のままである。** 参照 fixture は焼き直したので、
  **Android の JVM 単体が 159 件中 7 件赤い**（`DefaultSvgRendererFillDabTest` 2 /
  `DefaultSvgRendererPhase2fTest` 3 / `ServerStrokeEngineTest` 2）。**追随は次版で行う。**
- **検査:** server **2366 / 31 skipped**（起点 2340・**+26 = 新設 26 本**。既存 5 本はリネームで差引 0）・
  cli **111**・`npm run check` **241 FILES / 0 errors / 2 warnings**・ruff clean・
  `check_frozen_corpora.py` バイト一致・`check_docs.py` 緑。

### 2026-08-07 — Android の参照コーパスが版ごとに分かれた（**採番なし**・検査のみ）

**engine を上げるたびに Android が赤くなる構造を畳んだ。** 参照 fixture は平らな 1 ディレクトリに置かれ、
生成器がその場で焼き直していたので、**server が engine を上げた瞬間に移植側の期待値が書き換わり、
移植が名乗ったことのない版と比べられていた**。engine 22 のマージでは **JVM 159 件のうち 7 件**が
そうして赤くなり、赤の窓は前回・今回とも 2 日続いた。**描画改善は今後も続くと告げられている。**

- **`server/reference/` と同じ作法を当てた** — **版ごとにディレクトリを持ち、生成器は現在版しか書かず、
  旧版は焼き直さずに manifest で押さえる**。**render engine が支配する 54 ファイル**は
  `render-engine-<版>/` へ、**`ddl_expand.json`** は `ddl-engine-<版>/` へ移した。
  **どの engine も支配しない 5 つ**（`prompts` / `lineage_wiring` / `count_preservation` /
  `coerce_governors` / `score_schema_contract`）は**平らなまま**で、従来どおり焼き直して追随する。
- **engine 21 の 54 ファイルは履歴から復元した**（`d0a9739f`）。**engine 22 の木では焼けない** —
  旧版が「焼き直せないもの」であることが、この配置の要である。
- **移植は素の名前で fixture を頼み、`CompatibilityConstants.renderEngineVersion` が指す版を読む。**
  資源パスを自前で開いていた **20 のテストファイル**を 1 つの解決器へ寄せた。
- **Kotlin の実装は 1 行も変えていない。** それでも**赤 7 件は全部閉じた**（159 / 赤 0）。
  **engine 22 への追随は別の契約**で、`renderEngineVersion` は `"21"` のままである。
- **旧版を守るのは manifest だけである** — 現在版を焼き直す F-1 は旧版に届かないので、
  **各版のディレクトリを名前と digest で突き合わせる検査（F-4）**を足した。
  **旧版の digest を 1 文字変えると F-4 だけが赤くなる**（他の 124 件は緑のまま）。
- **摂動 6 本を製品コードと製品データへ当てた** — 解決器から版依存を外すと**元の 7 件がそのまま戻り**、
  `renderEngineVersion` を `"22"` へ向けると**その 7 件＋版を名乗る 2 件**が赤くなる。
- **検査:** server **2367 / 31 skipped**（**+1 = F-4**）・cli **111**・
  `npm run check` **241 FILES / 0 errors / 2 warnings**・ruff clean・
  `check_frozen_corpora.py` バイト一致・`check_docs.py` 緑・
  **Android JVM 159 / 赤 0**・**計装 31 / 赤 0**（実機 Pixel 9）。

### Android `2.1.4-android.12` — 描いた作品が、どの作品から来たかを名乗るようになった（android Build 148093、2026-08-07）

**[I-138]・Android 系譜の連作 1/5。** Android は**保存する作品を全部「起点」として書いていた** —
server は最初から親と派生種別を記録し、web は系譜パネルを出した版から宣言しているのに、
**移植だけが送っていなかった**ので、端末の履歴は関係の無い作品の平らな一覧だった。

- **判定は純関数 1 つに集めた**（`SubmitDerivationKind.kt`）。**分岐順序は web と同じ**
  （`canvasAspectChanged` → 親が無ければ `null` → 記述 / DDL → `replay`）で、
  **返す 4 種はすべて `DerivationKindRegistry` が知っているもの**である。
- **バッチ・デモ・ヘッドレスは辺を 1 本も書かない。起動時に復元された作品も親にしない** —
  **どちらも作者が選んだものではない**からである。
- **「新しい起点にする」は結線だけ作り、画面には出していない。** web はこれを系譜パネルに置くが、
  **移植にそのパネルはまだ無い**（連作 2/5 が作る）。**他所へ置くのはクライアント側の発明になる。**
- **記述が編集されたかの判定は、接頭辞を剥がした形も未編集として許した** —
  web は `source_text` という別の列で接頭辞を避けているが **Android にその列は無い**。
  そのまま比べると**バッチ由来の作品を親にした瞬間、何も編集していないのに `description_edit`** になる。
  **列を足す案は採らなかった**（server にも web にも対応物が無く、クライアント側の発明になる）。
- **計装の揺れを追って、製品の欠陥を 2 つ見つけて直した** — 起動時の設定復元と履歴復元が、
  **状態を読み → suspend し → 待つ前の写しを書き戻していた**。
  **起動直後に記述を打つ・履歴を選ぶ・キャンバス比を変えると、跡を残さず戻る**。
  `lineageDetached` が戻ると、**子になるはずの保存が起点になる**。
  **修正前は 3 走で毎回ちがう 1〜2 件が赤く、修正後は 4 走連続で 11/11 緑**（44 実行・赤 0）。
- **摂動 9 本を製品コードへ当てた**（契約の 7 本 ＋ 実装が自分で足した 2 本）。
  **`lineageDetached` を見る条件を落とす摂動は T-6 を赤にしなかった** —
  Android では親の出どころが `selectedHistory` だけなので、旗を落としても親は消える。
  **旗が効いているのは起動時の復元の 1 か所だけ**であることが摂動で分かり、
  **段 3 そのものに当たる摂動を足した。**
- **検査:** Android JVM **168 / 失敗 0**（38 クラス・起点 159）・**計装 42 / 失敗 0**
  （実機 Pixel 9・起点 31）。**`android/VERSION` だけを上げた** —
  `APP_VERSION`・`web/BUILD_NUMBER` は不動、pentala 反映も無し。

### 2026-08-07 — CLI の使い方が、パーサ自身の言葉で書かれるようになった（**採番なし**・検査のみ）

**`cli/README.md` の「Command Line Help Reference」は `--help` を手で写したもので、
写しとパーサを突き合わせるものが 1 つも無かった。** そのため旗の名前が変わっても README は旧名を載せ続け、
`--original-text`（現 `--description`）と `--vary-seed`（現 `--composition-seed`）を名乗ったまま、
`--staffage` と `--trace` は載らないままだった。**2026-08-04 に貼り直したのは中身だけで、
次に忘れても赤くならない構造は残っていた。**

- **マーカー間は生成物になった** — `<!-- HELP_START -->` と `<!-- HELP_END -->` に挟まれた **1,217 行**を
  `cli/scripts/gen_readme_help.py` が書く。**外側の散文 77 行は人のもので、生成器は触れない。**
- **`COLUMNS` を 80 に固定した** — argparse は端末幅で折り返すので、
  固定しないと**走らせた人によって生成物が変わる。**
- **現物は 3 つ（パーサ・README・生成器）あり、辺 3 本のうち 2 本を検査が見る** —
  経路ごとのバイト一致（**48 件**）と、生成器の `--check`（1 件）。
  **マーカーが境界であることも別に据えた**（境界を消すと「見る対象が減る」だけで緑になるため）。
- **突き合わせるのは旗の名前でなくブロック全体である** —
  **正しい名前のまま古い説明が載っている**のが元の欠陥だから。
- **既存の 8 本のゲートは残した。** あれは help の冒頭 6 語を見ており、バイト一致とは見ているものが違う。
  **説明文を 1 語変える摂動では、新しいゲートだけが赤くなる。**
- **再生成で動いたのは 4 行だけ** — `paint` と `batch` の usage 一行目の折り返しで、
  **旗の綴りも説明文も既に全部合っていた。**
- **摂動 5 本を製品コードと生成物へ当てた** — 生成器を消すと **1 件**、旗を足して再生成しないと **3 件**、
  説明文を 1 語変えると **3 件**、マーカーを消すと **50 件**が赤くなる。
  **5 本目は「赤くなること」ではなく、その状態で生成器を回すと 3 件が 0 件に戻ることを見た**
  （生成器が修復の道具として働くこと）。
- **検査:** cli **161**（**+50**）・server **2367 / 31 skipped**（不変）・
  `npm run check` **241 FILES / 0 errors / 2 warnings**・
  ruff clean（**cli の引数は `src tests scripts` になった**）・`check_docs.py` 緑。

### Android `2.1.4-android.13` — 描いた作品が、どこから来たかを画面で見せるようになった（android Build 148097、2026-08-07）

**連作 5 本の 2 本目。** 1/5 は親を結線しただけで画面に出しておらず、本版が**系譜そのものを画面に出し、
系譜へ書き込む最初の操作**を与えた。

- **グラフは純関数が組む**（`data/lineage/LineageGraph.kt`）。**server の `db.get_lineage` を実際に呼んで
  焼いた 11 case を、並び順まで全件再現する** — 並びは `(at, id)`、深さと節点数の丸め、
  **墓標は前後の節点が持つ hash と履歴を持たないまま**であること。
- **DAO に集合の問い合わせを 3 つ足し**、画面は **`DerivationKindRegistry` 自身の札**で世代を出す。
  **「新しい起点にする」が画面から view model へ初めて届いた。**
- **摂動 9 本を製品コードだけに当てた**（テストは 1 行も触っていない）。
  **丸めを外す摂動は 1 件しか赤くしない** — 11 case の突き合わせは丸めを外しても全部緑のままで、
  **契約が「出力ではなく実効値を見よ」と書いた理由がそのまま裏取りされた。**
- **⚠ 契約の外で 1 つ直した — アプリが起動しなくなっていた。**
  **`4c5e82f6`（2026-08-06）以降、起動のたびに落ちていた。** Kotlin は
  **第 2 引数に既定値を持つコンストラクタに `<init>(Application)` の署名を出さない**ので、
  `androidx.lifecycle` の既定ファクトリが反射で引けず `NoSuchMethodException` になっていた。
  **既存のテストが 1 件も気づかなかったのは、全部が Kotlin から view model を作るからである**
  （合成ブリッジを通り、反射の道を 1 度も歩いていなかった）。`@JvmOverloads` の 1 語で直し、
  **`MainActivity` と同じく `InkuApp()` を合成する計装ゲート**を据えた。
- **検査:** **JVM 173 / 失敗 0**（39 クラス・起点 168）・**計装 46 / 失敗 0**（実機 Pixel 9・起点 42）。
  **`android/VERSION` だけを 1 歩** — `APP_VERSION`・`web/BUILD_NUMBER` は不動、pentala 反映も無し。

### Android `2.1.4-android.14` — 描いた作品を、1 回に 1 要素だけ推敲できるようになった（android Build 148098、2026-08-07）

**連作 5 本の 3 本目。** 2/5 は系譜を画面に出したところまでで、本版が**系譜へ意図をもって枝を足す操作**を与えた。

- **推敲要素はタッチ・配置・読み取り・色カタログ・変奏の 5 種類から一度に 1 種類**（SPEC `:614`・`:678`）。
  ラジオ式の排他選択で、**変奏を選んだときだけ強度が直下に出る**。**系譜の各辺が 1 つの原因に帰属する**
  という規律が、この「一度に 1 つ」を要求している。
- **何を固定し・何を振り・どの辺を書くかは純関数が決める**（`data/refinement/RefinementPlan.kt`）。
  **色の推敲は DDL・Score・キャンバス・配置 seed・描画 seed を固定**し、**色以外は次回描画の設定ではなく
  表示中の親作品の実効カタログとキャンバスを継承する**。
- **`PaintRequest` に server と同じ 5 つの seed の口が開いた**（`render_seed` / `composition_seed` /
  `interpretation_seed` / `variation_amplitude` / `variation_seed`）。**判定も server のまま移した** —
  `render_seed` は `or` の falsy 判定なので、**`0` は「指定なし」と同じ道を通る**。
  **描画のたびに seed を採るので、これ以降の絵はこれまでと変わる。**
- **server に無い形を 2 つ落とした** — `renderHash` が `render_seed` に加えて読んでいた `seed` と、
  renderer が Score の中から読んでいた `render_seed`。**server の `Score` にその欄は無い。**
  比較描画の入口は、代わりに server の要求本文と同じ 5 つを intent で受ける。
- **摂動 11 本を製品コードだけに当てた。** **うち 2 本は最初緑のまま通り、受入側の穴として直してから
  当て直した** — **同じ絵の 2 枚目は履歴の行を置き換える**ので、二重保存は行数では検出できない。
- **⚠ 契約が書いた摂動 1 本は、server と向きが逆だった。** 「`render_seed` の `null` と `0` を
  同じ道へ通す」と書いてあったが、**同じ道が正しい実装**である。**裁定「server に従う」に従って
  条件をそのまま移植し、摂動のほうを反転した。**
- **検査:** **JVM 197 / 失敗 0**（41 クラス・起点 173）・**計装 62 / 失敗 0**（実機 Pixel 9・起点 46）。
  **`android/VERSION` だけを 1 歩** — `APP_VERSION`・`web/BUILD_NUMBER` は不動、pentala 反映も無し。

### v2.11.5 — 配置に、それだけの seed がついた（Build 860、render engine 23、2026-08-08）

**筆致を見比べるために seed を動かすと、構図まで動いていた。**

engine 22 まで、`render_seed` の 1 つが**配置**（arrangement がどこに墨を置くか）と**手**
（1 本ごとの筆致・色の割り当て・地・演奏の解決）の両方を決めていた。だから
「同じ構図で別の筆致を見たい」という操作が存在しなかった — **60 個の墨を撒く作品で
`render_seed` だけを動かすと、180 個の座標が全部動く。**
`SPEC.ja.md` の `:614` と `:678` は逆のことを書いている（**筆致の推敲は同じ Score の構図を保つ**）。
**これは機能の不足ではなく、物差しの側の欠陥だった。**

- **`composition_seed` が配置位相だけを担うようになった。** この鍵は
  **DB にも 4 つの描画ルートにも 4 つのクライアントにも既にあった**が、
  **renderer には 1 度も届いていなかった**。搬送路を 5 点作って届くようにした
  （`renderer.render()` の引数・engine の protocol・既定 engine・`_render_with_metadata`・
  `_render_score_svg` と `RenderSvgRequest`）。**`_render_with_metadata` の呼び出し元 4 つは 1 行も変えていない。**
- **`render_seed` は手を持ったままである。** タッチ・色の割り当て・地・演奏の解決は動かない。
  **配置と手が、初めて別々に振れるようになった。**
- **`composition_seed` が無いときは `render_seed` へ落ちる**ので、**これまでの絵は 1 枚も動かない。**
  **判定は `is not None`** なので、**`0` は「指定なし」ではなく 0 という seed である。**
- **参照コーパスは 531 → 535 件。既存の 531 件は 1 件も動いていない**
  （変更一覧に載るのは追加した 4 件だけ）。足した 4 件は**既存 case の双子**で、
  **Score も `render_seed` も同じで `composition_seed` だけが違う** — 展開した墨のうち
  **60/60・16/16・60/60・60/60 が動く**ので、2 つの seed を取り違えた実装を落とせる。
  **配置が seed で動かない layout（`G-radial-*`・`G-*-nopath-*`）は使っていない**（恒真になるため）。
- **`ddl_engine_version` は動いていない**（7 のまま）。Stage 2 までは 1 バイトも変わらない。
- **Android の参照 fixture に `render-engine-23/` が 55 ファイル増えた**が、
  **Kotlin は 1 行も変わっていない** — Android は自分が名乗る `render-engine-21/` しか読まないので、
  **engine を上げてもディレクトリが増えるだけである**（43 周目の設計。マージ後の JVM 197 / 赤 0 で実証した）。
- **残っている欠落が 1 つある** — 履歴の SVG 書き出し（`display` 以外のプロファイル）は
  `render_seed` を渡していないので、**配置は保存済みの絵を再現するが、手は再現しない。**
  改修前は両方とも再現していなかったので、**半分が直り、半分が残った。**台帳 [I-158] に起票した。
- **検査:** server **2379 / 31 skipped / 赤 0**（起点 2367・**+12** = 新しい受入 T-1〜T-12）・
  **摂動 11 本を製品コードだけに当てた**。**うち 1 本は最初 1 件も赤くしなかった** —
  コーパスを焼く生成器と、それを検算する 2 つのテストが**同じ引数列を別々に書き写していた**ので、
  生成器の配線を切ってもテスト側は渡し続けていた。**replay を名前のある 1 つの呼び出しへ出して塞いだ。**

### 2026-08-08 — API スキーマの `composition_seed` が、engine 23 での役割を述べるようになった（Build 861・**版数は据え置き**）

**engine 23 で配置を担うようになったのに、3 つの request model のうち 2 つは古い説明のままだった。**

- **`ComposeRequest` と `PaintRequest` の `composition_seed` は "Stage 1.5 composition variation seed" と
  名乗っていた**（`render.py:189` / `:327`）。**`RenderSvgRequest` だけが engine 23 の役割を書いていた。**
  **この文字列は OpenAPI に出るので、API を直接使う側が読む説明である。**
- **実測して 2 つの役割を両方書いた** — `paint` / `compose` の経路では、この seed は
  **`ddl_expander` の中間展開を再ソルトし**（`reference.py:421-426` の `seed_summary` が述べる役割）、
  **かつ `_render_with_metadata` を通って renderer の配置位相を決める**（`rendering.py:316`）。
- **変えたのは説明文だけである。** `test_api_surface.py` の台帳は **82 / 82 / 82 で不変**、
  **`PaintRequest` 36 項目・`ComposeRequest` 19 項目とも増減 0・`required` も不変**で、
  **動いたのは `composition_seed.description` の 1 項目だけ**（集合差で実測。digest だけが変わる）。
- **検査:** server **2379 / 31 skipped / 赤 0**（不変）・ruff clean・`check_docs.py` 緑。
  **`APP_VERSION` は動かしていない**（挙動が変わらないため）。**`manual/` の版マーカー 13 箇所は Build 861 へ。**

### Android `2.1.4-android.15` — 描いた作品を、モデルと言語で見比べられるようになった（android Build 148098、2026-08-08）

**連作 5 本の 4 本目。** 3/5 は 1 回に 1 要素を推敲するところまでで、本版が**同じ記述を別の条件で描き比べる操作**を与えた。

- **指示文の言語の解決を server から条件式のまま移した**（`language_support/registry.py`）。
  支援する言語は日英の 2 つ、要求できる語は `auto` を加えた 3 つ。`auto` は本文を読んで決め、
  **日本語の判定を先に当てる**ので、両方の文字を含む本文は日本語になる。
  **`(value or default)` の falsy 判定をそのまま写した** — 既定値つきの取得へ置き換えると
  **空白だけの値だけが別の道を通る**ため、`null` と空文字だけが既定へ落ちる綴りにしてある。
- **Stage 1・Stage 1.5・Stage 2 の 3 段とも、解決後の言語でプロンプトを選ぶ。**
  **英語の Stage 2 プロンプトは、この版まで製品コードから 1 度も読まれていなかった**
  （定数は在ったが参照が 0 件だった。実測）。Stage 1.5 の展開は日英の枝を既に持っていて、
  **配線だけが日本語固定で残っていた**ので、これも解決後の言語へつないだ。
- **履歴に 3 列足した** — 要求した言語・解決した言語・記述の本文（Room の版は 6 → 7）。
  **要求と解決は別々に書く**ので、`auto` で描いた作品が「何を頼まれ、何に落ち着いたか」を後から読める。
  **記述の同一性は `source_text` があればそれ、無ければ `original_input` から作る**（server と同じ二段構え）
  ので、**バッチの行番号つきで描いた作品と、同じ文を手で打った作品が同じ記述として並ぶ**。
  **[I-153]（`source_text` 列が無い）はこの列で塞がった。**
- **モデルの検分と言語の検分は、既存の推敲の骨格の上に置いた。** 開く・候補を作る・中断する・保存するの
  4 つの入口は 3 つのサブビューで共通で、**違うのは発注の一覧を返す 1 つの関数だけである**
  （SPEC `:688` の「比較のロジックを複製しない」）。
- **系譜の辺は `model_comparison` と `language_comparison` を 1 か所の分岐で書き分ける。**
  記録する metadata の鍵名は参照実装（web）と 1 対 1 で揃えた。
- **系譜カードの作品メニューに「モデル」「言語」が SPEC `:618` の並びで入った。**
  押すと対象作品つきでサブビューが開き、閉じると元の系譜ビューへ戻る。
- **⚠ SPEC `:686` は言語比較を「モデル比較と同じ 3 モード」と書くが、参照実装にそのモードは無い**
  （web は日英の組 4 通りをチェックボックスで選ぶ）。**契約の指示どおり参照実装の形を採り、
  食い違いは SPEC 側の課題として [I-156] に残してある。**
- **摂動 15 本を製品コードだけに当てた**（契約の 14 本 ＋ 自分で足した段の 1 本）。
  **うち 3 本は最初空振りし、検査の側の欠陥を露呈した** — 未知の fallback を素通りさせていた行、
  **判別点が空白ではなく空文字だった**こと、**仕事は走り続けているのに旗だけ下りるので「止まった」に見えていた**中断の検査。
- **受け入れで測ったこと:** **`source_text` の読み出し 2 点は、いまのデータの形では判定を 1 つも変えない** —
  書き込み側が接頭辞つきの `original_input` と接頭辞なしの本文を対で書くので、
  **接頭辞を落とす既存の代用と結果が必ず一致する**。**2 点とも落とす摂動で JVM も計装も全件緑だった。**
  効き始めるのは両者が食い違う行が現れたときで、**その置き場は [I-153] に追記した。**
- **検査:** **JVM 223 / 失敗 0**（43 クラス・起点 197）・**計装 81 / 失敗 0**（実機 Pixel 9・起点 62）。
  **`android/VERSION` だけを 1 歩** — `APP_VERSION`・`web/BUILD_NUMBER` は不動、pentala 反映も無し。

### v2.11.6 — 薄れが、群の一人ひとりに届く（Build 862、render engine 24、2026-08-08）

**「中心から縁へ薄れる」が、「全体がすこし淡い」として描かれていた。**

`Arrangement.fade` は群がどう落ちるかを宣言する — 中心から外へ（`outward`）か、
進む向きに沿って（`directional`）か。engine 23 まで、renderer はこれに**群ぜんぶで 1 つの定数**で答えていた。
`outward` に 0.40、`directional` に 0.48。**いちばん近い墨といちばん遠い墨に同じ数字が乗る。**
**本番では 6,425 群のうち 2,738 群（42.6%）・178,694 痕のうち 83,703 痕がこれに当たる。**

- **群の一人ひとりが自分の上限を持つようになった。** `outward` は 0.62 → 0.18、
  `directional` は 0.70 → 0.26 へ、群の中の位置で傾斜する。
  **語彙もフィールドも足していない** — 宣言は最初からそこにあった。
- **搬送路は `color_hint` である。** 色サイクルが hint を組み直したあとに札を書き、
  **正規化が小数点を潰す前に読み戻す。** 正規化後の値を見ると、書いた傾斜はそこで平らになる。
- **配置と触れと演奏 seed は動いていない。** `color_hint` は `_SEED_INSTRUCTION_FIELDS` の外なので、
  **薄れる群の経路座標は engine 23 とバイト一致し、変わるのは不透明度の属性だけである。**
  面の seed も、instruction をハッシュする前に札を落とすので動かない。
- **薄れられない群は、そのままにした。** 環は自分の中心から等距離であり、2 個の群も等距離である。
  **順位をつければ、誰も述べていない濃淡が描かれてしまう。**
- **⚠ 環は、重心から見ると等距離ではなかった。** `_rhythm_t` は 0〜1 を**両端含み**で返すので、
  **12 個の環は先頭と末尾が同じ点に重なり**、重心が軸から**半径 / 個数**（実測 0.025）ずれる。
  そこから測ると環に一周する濃淡が出たので、**`radial` の分岐だけが自分の回転中心から測る。**
  ほかの layout は重心のままである。
- **単員の群には、構造上どんな実装でも札を書けない。** `count == 1` の分岐は `_shift` を通らないので、
  **engine 23 でも `fade` は hint に載っておらず、もともと薄れていなかった**（実測: 不透明度 1.0）。
- **コーパスは 535 → 541 件。動いたのは 7 件** — 足した 6 件と、既存で 1 件だけ動いた
  `G-scatter-fade-edge` である。**残る 534 件は 1 バイトも動いていない。**
  この 1 件が engine 23 のコーパスにおける**薄れのすべて**だった。
  **歩かれていなかった道に 6 件を足した** — 経路に沿う `directional`、色サイクル、
  導出された面の seed、機械の道具（**機械も薄れる**。作者裁定）、そして**動いてはならない退化した 2 群**。
- **`ddl_engine_version` は動いていない**（7 のまま）。Stage 2 までは 1 バイトも変わらない。
- **摂動 13 本を製品コードだけに当てて、空振りは 0 本だった。** うち 2 本は**最初、片側の分岐にしか
  当てておらず**、名指しした検査が赤くならなかった。**強制点を数えて両分岐へ当て直したら赤くなった** —
  片側だけの摂動を「空振り」と報告していたら、受入の穴という誤った結論になっていた。
- **検査:** server **2,403 passed / 31 skipped**（`def test_` 1,215 → 1,230・**削除と改名は 0**）・
  ruff clean・凍結コーパスはバイト一致。
  **Android の参照 fixture に `render-engine-24/` が 55 ファイル増えたが、Kotlin は 1 行も変わっていない。**

### Android `2.1.4-android.16` — 描く前に写生するようになった（android Build 148098、2026-08-08）

**記述を「物の言葉」の散文へ書き直してから Stage 1 に読ませる層（写生・Stage 0.5）が、Android にも入りました。**
Android 系譜の連作 5 本の最後で、**`server/` `web/` `shared/` は 1 行も変えていません。**

- **つまみは 3 状態**（`切` / `細かく` / `大きく`）で、**既定は `細かく`**（層は走る）。
  **`細かく` と `大きく` は「どれだけ言うか」を変えず、区切りの大きさだけを変えます。**
- **履歴に 3 列足した**（Room の版は 7 → 8）— 写生文・区切り・状態。
  **`MIGRATION_7_8` は既存行へ何も埋め戻しません。** 埋め戻すと、
  **層より前に描かれた作品が「層を切って描かれた作品」に化けます** — **値が無いことが 6 つ目の意味を持ちます。**
- **⚠ 同じ名前の正規化関数が server と web に 1 つずつあり、未知・欠損のときの答えが違います**
  （server は既定へ丸め、web は `null` を返す）。**移植先では名前を分け、親との比較には web 側を使いました。**
  **取り違えると、区切りの記録が無い作品の判定が両方向とも逆になります。**
- **fallback の行は写生文を書きません**（**2026-08-08 作者裁定・server に合わせた**）。
  **fallback が運ぶのは記述そのもの**なので、それを写生文の列へ書くと
  **層を通っていない作品と通った作品が読み手から区別できなくなります。**
- **写生は非決定なので、再現は「写生をやり直す」ではなく「保存した写生文を送り直す」で行います。**
  記述も区切りも動いていない描き直しのときだけ、親の写生文を送ります。
- **推敲の候補も親の写生文を継ぎます。** 継がないと**親は写生文から・候補は生の記述から描かれ、
  その作品の推敲ではなくなります。**
- **摂動 12 本を製品コードだけに当てて、空振りは 0 本でした。**
  **うち 1 本は契約が 1 つの編集に 2 つの検査を期待していたので、当て先を 2 通りに割りました** —
  **片方は SQLite の列を直に読む検査で、正規化関数はその経路に無い**ためです。
- **検査:** **JVM 235 / 失敗 0**（44 クラス・起点 223）・**計装 93 / 失敗 0**（実機 Pixel 9・18 クラス・起点 81）。
  **`def`/`fun` の増減は +24・削除と改名は 0。**
  **`android/VERSION` だけを 1 歩** — `APP_VERSION`・`web/BUILD_NUMBER` は不動、pentala 反映も無し。
- **⚠ Stage 1 / Stage 2 の温度は server と食い違ったままです**（写生層 0.3 に対し 0.2 と 0.1）。
  **これは写生層より前から在る乖離**で、本周では直さず **[I-159]** として裁定待ちに載せました。

### Android `2.1.4-android.17` — 閉じる前に、書き終わるのを待つ（android Build 148098、2026-08-08）

**作品を保存すると、そのあとサムネイルが裏で作られて DB へ書き戻されます。**
この裏の仕事は投げっぱなしで、`InkuRepository.close()` は `thumbnailScope.cancel()` の 1 行でした。
**`cancel()` は「やめてくれ」と言うだけで、やめ終わるのを待ちません。**
そのため書き込みがまだ走っている最中に DB が閉じられることがあり、
**例外はテストの中ではなく裏の流れで起きるので、プロセスごと落ちて残りのテストが 1 件も走りませんでした。**
**「赤が 1 件」ではなく「93 件のうち 24 件しか記録が無い」という形で出る**ため、
件数を数えないと緑に見えます。**12 回中 5 回打ち切られていました**（[I-150]）。

- **`close()` が待つようにしました。** `thumbnailScope` の子の完了を待ってから畳みます。
  **⚠ `cancelAndJoin` にはしていません** — cancel を先に置くと**書き込みが取り消されて
  サムネイルが永久に書かれない**ので、「閉じたときには書き終わっている」という
  **表明できる性質が残らなくなります。**
- **計装 12 クラスのうち 4 つは、DB を閉じるのに repository を閉じていませんでした。**
  正確には `store.clear()` → `onCleared` 経由で**非同期に**閉じ、**500ms の待ちで間に合わせていました。**
  直接閉じる形にし、待ちは残しています（view model が始める他の仕事も守っているため）。
- **ゲートを 2 つ置きました。** 計装側は**保存 → `close()` → その場で行を読むとサムネイルの path が入っている**こと。
  server の pytest 側は**`database.close()` が必ず先行する `repository.close()` に覆われている**ことの構造検査です。
  **後者を server 側に置いたのは、pytest が毎周走るのに対し Gradle は `android/` に差分がある周にしか走らない**ためで、
  Kotlin のソースを pytest から読む前例が 4 本あります。
- **本番アプリは Room の DB を一度も閉じません。** したがってこれは**テスト足場の欠陥で、
  利用者に見える不具合ではありません。** 本番で起きるのは、アプリを閉じる瞬間にサムネイル 1 枚の
  書き込みが落ちる程度です。
- **摂動 2 本はどちらも名指しの検査を 1 件だけ赤にしました。** `close()` を `cancel()` だけに戻すと計装の T-1 が、
  1 クラスから `repository.close()` を消すと server の T-2 が赤くなり、戻すと 0 件に戻りました。
- **検査:** **計装 95 / 失敗 0**（実機 Pixel 9・19 クラス・起点 93）・**JVM 235 / 失敗 0**（44 クラス・起点と同じ）・
  **server pytest 2405 passed / 31 skipped**（起点 2403）。
  **`android/VERSION` だけを 1 歩** — `APP_VERSION`・`web/BUILD_NUMBER` は不動、pentala 反映も無し。
- **⚠ 予測より 2 件多く増えました。** 着手前に凍らせた予測は計装 +1・pytest +1 でしたが、
  **どちらのゲートにも対照を 1 本ずつ足した**ためです。
  計装側は「保存だけではサムネイルが書かれていない」ことを先に表明しないと本命が恒真になり、
  server 側は「見る対象が 0 個でも通る」形を避けるためです。**性質の数は予測どおり各 1 つです。**

### Android `2.1.4-android.18` — 描画材に名前をつける（UI リデザイン 段 A・android Build 148098、2026-08-08）

**`InkuApp.kt` の 1 ファイルに、色 89 箇所（57 値）・`.dp` 429 箇所（54 値）・`.sp` 8 箇所（5 値）が
直に書かれていた。** 残り 68 ファイルは 3 種とも 0 件で、**直書きは 1 ファイルに全部集中していた。**
本段はそれに名前を与えるだけで、**絵は 1 画素も動かさない。**

- **トークン層を新設した。** `ui/theme/Color.kt`（65 トークン ＋ `InkuColors` 9 ロール）・
  `Dimens.kt`（**新設**。53 値。`0.dp` はトークンにしない）・`Type.kt`（5 の sp 値）。
  `InkuApp.kt` の直書きは **色 0・`.sp` 0・`.dp` は `0.dp` の 20 箇所だけ**になった。
- **値を 1 つも動かしていないことを 2 通りで検算した。** トークンを記号へ正規化して突き合わせると
  **5,756 行 対 5,756 行で構造差分 0 行**（置換以外が混ざっていない）。加えて
  **移行前の 57 色 / 53 dp / 5 sp がトークン側の値集合にすべて含まれる。**
- **色トークンは 65 個で値は 57 種類。** 同じ ARGB を持つ 7 組に 2 つ以上の名前を与えたためで、
  **増えるのは正常・減ることだけが退行**という受入の読み方どおり。
  **いま一致しているだけの別の判断なので、名前を 1 つに畳んでいない**（段 B で片方だけ動かせなくなる）。
- **Claude Design へ出すプレビューを生成器つきで置いた。** `android/design/gen_design_preview.py` は
  **Kotlin のソースを読むだけで Gradle も Android SDK も要らない**ので CI に乗る。
  `reference-corpus.yml` に**バイト一致を要求するジョブを 1 本**足した
  （**CI で回せない生成器では「焼き直しを見張る」という目的そのものが消える**）。
- **摂動 8 本すべてで名指しの検査が赤くなった**（超過 5 件はいずれも設計どおりの重なり）。
  **⚠ 契約の P-3〈未使用の色を 1 つ消す〉は実行できなかった** — **65 トークン全部が参照されており、
  値が重複する 7 組はどれを消しても受入は緑のまま**で、削除には二重に判別力が無い。
  **守っている性質（移行前の値が失われること）を製品コードの側で直接起こす摂動へ置き換えた。**
- **検査:** **JVM 238 / 失敗 0**（45 クラス・起点 235）・**server pytest 2411 passed / 31 skipped**（起点 2405）・
  **計装 95 / 失敗 0**（実機 Pixel 9・19 クラス）・ruff clean・`check_docs.py` 緑。
  **`android/VERSION` だけを 1 歩** — `APP_VERSION`・`web/BUILD_NUMBER` は不動、pentala 反映も無し。
- **⚠ `.dp` の 53 値のうち 22 値が 4dp 格子から外れているが、1 つも寄せていない。**
  **寄せる仕事は段 B が画面を作り直すときに行う**（同じ周で両方動かすと、どちらが絵を動かしたか分離できない）。
  そのときは `Dimens.kt` の 1 ファイルだけを動かせばよい。
- **⚠ 版が `.17` でなく `.18` なのは、同じ日に [I-150] の修正が `.17` を使ったためである。**

### v2.11.7 — 群の一人ひとりが、自分の大きさを持つ（Build 863、render engine 25、2026-08-08）

**`Arrangement` は「同じ形をいくつも」と述べている。「全部を同じ大きさで」とは、どこにも書かれていない。**

engine 24 まで、群の展開は座標だけを書き換えていた。**N 個は 1 個の複製で、寸法は完全に合同だった。**
それは記述が求めた性質ではなく、**engine が自分で足していたいちばん大きな署名**である。

- **手の道具は、群の中で大きさが 1 つずつ違うようになった。** 振り幅は道具の文法が持つ
  `group_hand` で、**手の道具 9 種すべてに一律 ±25%**（`HAND_GROUP_SIZE`）。
  `fill_hand` に比例させると silverpoint が ±1.25%・brush_thick が ±25% になり、
  **作者が見て良しとした見本の絵から離れる**ので、1 つの定数にした。
- **`rotring` と `computer` は 0 のままである。** 機械の反復が正確であることは欠陥ではなく署名で、
  engine 22 が `fill_hand` に置いた規律をそのまま守る。**目視でも確かめた** —
  同じ Score を道具だけ替えて描くと、`rotring` の 24 個は SVG の上で `r="50.0"` ただ 1 種類、
  `pen` の 24 個は 0.766〜1.266 倍に散る。
- **大きさを決めるのは演奏 seed で、配置 seed ではない。** engine 23 が「構図」と「演奏」を
  分けたので、**寸法を配置 seed から作ると、その分離を作った日に壊すことになる。**
- **⚠ 展開器の引数は、名前と中身が engine 23 から食い違っていた** — 第 2 引数の名前は
  `performance_seed` だったが、そこへ渡っていたのは `placement_seed` である。
  **名前を現物へ合わせて改名し、演奏 seed を別のキーワード引数として足した。**
- **配置は 1 画素も動かない。** 群は展開のあとに `_fit_group_to_anchor` が anchor だけを読んで
  置くので、**anchor を動かす伸縮の規則は、置く相手を別の群にしてしまう。**
  4 つの規則はどれも自分の anchor を保存する — 線は**中点まわり**、四角と三角は
  `size` を掛けてから `position` を半分だけ引き戻し、円・弧・多角形は `radius`、
  楕円と雲形は `size` の両成分に同じ係数（**縦横比は変えない**）。
- **例外は 3 つだけ**: **`grid`**（升目は揃っていることが要点。作者裁定）・
  **単員の群**（違う相手が居ない）・**機械の道具**。
- **コーパスは 541 → 545 件。動いたのは 37 件で、504 件は 1 バイトも動いていない。**
  **⚠ 動いた 37 群は全部 `circle` だった** — コーパスは円しか歩いていなかったので、
  **`line` / `square` / `triangle` / `ellipse` の群を 1 件ずつ足した。**
  このうち線・楕円・四角の 3 つだけで本番の痕の 82.8% を占めるのに、**足すまでは
  4 規則のうち 3 つが 1 度も焼かれていなかった。**
  焼き手には「この 4 件が判別力を持つ」ことを表明させてから書き出す。
- **`ddl_engine_version` は動いていない**（7 のまま）。Stage 2 までは 1 バイトも変わらない。
- **摂動 12 本はすべて製品コードを壊し、空振りは 0 本だった。** ただし**うち 4 本は最初空振りし、
  そのつど受入を直して当て直している**（製品コードには触れていない）。
  **4 本とも「壊した層をゲートが通っていない」型**で、うち 2 つは
  **円だけで測っていた**という、コーパスと同じ穴を受入の側に開けていた。
- **⚠ 契約が予測していなかった 8 箇所が赤くなった。** 版を上げたことの帰結 5 箇所とは別に、
  **寸法が動くこと自体で**既存の検査が動いた。うち 3 つは engine 23 の凍結バイトに対して
  薄れと seed 分離を測るもので、**engine 25 の層だけを無効化してから比べる形にした** —
  engine 24 が engine 23 に対して `_apply_fade_levels` を外したのと同じ手で、**物差し（凍結 body）は動かしていない。**
- **検査:** server **2,442 passed / 31 skipped**（起点 2,411・`def test_` 1,238 → 1,255・**削除 0**）・
  cli 176・`npm run check` 241 FILES / 0 errors・ruff clean・凍結コーパスはバイト一致。
  **Android の参照 fixture に `render-engine-25/` が 55 ファイル増えたが、Kotlin は 1 行も変わっていない**
  （Android は自分が名乗る版のディレクトリしか読まない）。**JVM 238 / 赤 0 は起点と同じ。**

### v2.11.8 — 群の一人ひとりが、自分の傾きを持つ（Build 864、render engine 26、2026-08-08）

**engine 25 が「同じ大きさ」をやめた。残っていたのは「同じ向き」である。**

`Arrangement` は「同じ形をいくつも」としか述べていない。**全部を同じ向きで、とはどこにも書かれていない。**
engine 25 のあとも、群の N 個は 1 つの角度を共有していた。**改善計画 #5 の最後の段。**

- **手の道具は、群の中で向きが 1 つずつ違うようになった。** 振り幅は道具の文法が持つ
  `group_rot` で、**手の道具 9 種すべてに一律 ±12°**（`HAND_GROUP_ROT`）。
  前段が `group_hand` を 1 つの定数にしたのと同じ理由で、道具ごとに変えていない。
- **`rotring` と `computer` は 0 のままである。** 機械の反復が正確であることは欠陥ではなく署名で、
  engine 22 の `fill_hand`・engine 25 の `group_hand` に続いて 3 つ目の同じ規律になる。
- **座標は 1 画素も動かない。** `_turn_member` は `rotation` だけを書き換える。
  **`_apply_rotation` は anchor を中心に回す**ので、**前段が要した 3 つの座標補正に当たるものが、
  この段には無い。** 大きさは中心を動かしうるが、向きは動かさない。
- **除外は 5 つ**: **`line`**（線を回すと別の線になる）・**`circle`**（回しても見えないのに
  演奏 seed だけが動く）・**`rotation` を明示した群**（記述が向きを述べているなら、それが正本）・
  **`grid`**（升目は揃っていることが要点）・**単員の群**。
- **⚠ 明示の判定は `stated.rotation is not None` であって、truthy ではない。**
  **本番には `rotation: 0` を明示した群が 141 ある。** truthy で書くと、この 141 群だけが
  「述べていない」側へ落ちて回ってしまう。**`0` を明示した case をコーパスへ足して、
  この 1 点だけを見る受入を置いた。**
- **向きを決めるのは演奏 seed で、配置 seed ではない。** engine 23 が分けた構図と演奏の境界を、
  前段と同じ理由で守る。
- **⚠ 展開器の引数 `size_seed` を `member_seed` へ改名した。** 前段が寸法のために作った名前だが、
  向きも同じ種を読むので、**名前が実体より狭くなっていた。** 木に `size_seed` は 0 件。
- **コーパスは 545 → 549 件。動いたのは 3 件だけで、542 件は 1 バイトも動いていない。**
  動いた 3 件は前段が自分で足した `G-size-ellipse-edge` / `-square-` / `-triangle-` で、
  **円でも線でもない群がそのまま本段の対象になった。**
  **⚠ コーパスには `arc` の群も `cloudform` の群も `rotation` を明示した群も 1 つも無かった**ので、
  **4 件足した**（`G-angle-arc-edge` / `-cloudform-edge` / `-stated-zero-edge` / `-stated-30-edge`）。
  焼き手には「この 4 件が判別力を持つ」ことを表明させてから書き出す。
- **`ddl_engine_version` は動いていない**（7 のまま）。Stage 2 までは 1 バイトも変わらない。
- **摂動 12 本はすべて製品コードを壊し、空振りは 0 本だった。** ただし **1 本は当てる前に
  受入の穴が見つかっている** — 向きの量を寸法へ流す摂動は、「向きの層を無効化した同じビルド」と
  突き合わせる形では**両側に同じ壊れた係数が乗って緑のまま**になる。
  **engine 25 の凍結 43 件を、いま入っている製品で描き直して digest に当てる 1 本**を足して塞いだ
  （凍結ファイルどうしではなく、生きた描画を凍結の記録に当てる）。**足したのは受入で、製品ではない。**
- **⚠ 前段と違い、golden digest は 1 つも動かなかった。** 契約が予測しなかった赤は 6 箇所出たが、
  **6 件とも「コーパスに 4 件足したこと」の帰結で、「向きが動いたこと」の帰結は 1 件も無い。**
  `test_legacy_arrangement_layouts_keep_golden_output` が緑のままなのは、
  **あの golden の 4 つが全部 `circle` の群**だからである（engine 26 は円を回さない）。
  engine 25 では同じ検査が赤くなり、7 度目の採り直しをしている。
- **目視でも確かめた。** 本番の Score 1 枚を版だけ替えて描くと、engine 25 は `rotate(` が
  **1 つも無い**（Score が角度を述べていないので 109 の痕が全部同じ向きで並ぶ）。
  engine 26 では **109 個が全部別の角度**を持ち、**-11.95° 〜 +11.83°** の内側にいる。
- **検査:** server **2,466 passed / 31 skipped**（起点 2,442・`def test_` 1,255 → 1,270・**削除 0**）・
  cli 176・`npm run check` 241 FILES / 0 errors・ruff clean・凍結コーパスはバイト一致。
  **Android の参照 fixture に `render-engine-26/` が 55 ファイル増えたが、Kotlin は 1 行も変わっていない**
  （Android は自分が名乗る版のディレクトリしか読まない）。**JVM 238 / 赤 0 は起点と同じ。**

### Android `2.1.4-android.19` — 作品を主に置く（UI リデザイン 段 B・android Build 148106、2026-08-08）

**画面の 1 段目が描画設定で、作品は 4 段目に居た。** 段 A が値に名前を与えたので、
本段は**その名前を使って配置と導線を組み替えた** — 作品を最上部へ出し、設定は記述の直上へ集めた。

- **作品情報・描画設定・書き出しの 3 家族に分けた。** モデル選択の入口は **3 つから 1 つ**へ、
  書き出しは**ボトムシート 1 つ**へ。倍率とキャンバス比はそれぞれの家族へ移した。
- **マスコットは実行中にしか出ない。** web の `RunStatus` に対応する行（マスコット・モデル名・経過・停止）を作り、
  **出る条件は web と同じ「何かが走っていること」**にした。マスコットの選択は好みの設定なので設定に残る。
- **鑑賞を全画面に集めた。** ピンチ 0.25〜10 倍・パン・ダブルタップで fit ⇄ 実寸の往復・戻るキーで離脱。
  **通常表示のズームは条件式ごと廃止した。** 左右スワイプの向きは**右＝前・左＝次**に統一した
  （いままで全画面だけが逆だった）。
- **IME が出ている間、「描画する」がキーボードの直上に固定される。** 記述欄はフォーカス時に上部へ寄る。
  **IME のアクションキーには割り当てていない** — 日本語入力では改行が変換確定なので、
  **確定のつもりで描き始めてしまう。**
- **下タブは 5 つから 4 つへ**（記述 / 履歴 / 系譜 / 設定）。**デモの運転は設定の中へ移した。**
- **寸法と字の体系を畳んだ。** ボタンの高さ **5 段 → 3 段**（56 / 40 / 32）・角丸 **4 種 → 2 種**（card 16dp と pill）・
  余白 **4 段**（4 / 8 / 16 / 24）・字の最小 **11sp → 12sp**。`Dimens` の `Dp` 宣言は **56 → 41**、
  **4dp 格子から外れた直値は 22 → 0**（境界線の 1dp だけ例外）。**家族をまたいだトークンの借用も解いた。**
- **⚠ `uiMode`（フル / シンプル）の意味が変わった。** 従来の差はマスコットと重複チップ行の有無だけで、
  **その 2 つが本段で消えたので差が空になった。** シンプルでは `解釈`（DDL の欄・DDL から描画）を畳む形にした。
- **⚠ 計装では「IME の上に見えていること」を測れない。** Compose は計装中に window inset を 0 へ固定するので、
  `imePadding()` が何も持ち上げず、**摂動を当てても外しても赤いまま**になる
  （**製品ではなく計装の宿主を測る検査**だった）。受入は
  **「主操作が 1 つだけ在り、スクロールの外に居て、下タブが場所を譲っている」**へ据え直した
  （inset が 0 でも真になり、焦点の結線を切れば偽になる）。
  **キーボードの上に出ていることの証拠は実機のスクリーンショットである。**
- **⚠ 実機の計装がアプリのデータを一度消した。** `gradle :app:connectedDebugAndroidTest` は
  **既定で実行後にアンインストールする**ので、作品の入った実機で回すと DB ごと消える。
  **復旧できたのは、着手前に基準値を測るため DB を写していた偶然による。**
  旗（`-Pandroid.injected.androidTest.leaveApksInstalledAfterRun=true`）で回避できることは実測済みで、
  **規約へどう載せるかは台帳の [I-162] として起票した。**
- **摂動 11 本すべてで名指しの検査が赤くなった**（空振り 0・名指しの外れ 0）。
  余分に赤くなった 3 件はいずれも**段 A の T-8（デザインプレビューのバイト比較）**で、
  **これは焼き直される記録であって性質の検査ではない**（トークンを動かせば必ず赤くなる種類のものである）。
- **⚠ 作品情報の切替に `DDL` を足していない**（契約の家族表は 4 つ挙げていた）。
  DDL は `解釈` の欄と DDL エディタから読めるので、**4 つ目のタブは語彙の追加にあたる**と判断した。
- **⚠ 受入の置けていない項目が残っている** — 戻るキーの行き先・当たり判定 48dp・余白が 4 段であること・
  キャンバスが最上部に居ること。**契約にも T が無く、本段でも測っていない**（目視だけである）。
- **検査:** **server pytest 2,476 passed / 31 skipped**（マージ後の main で実測。起点 2,466 ＋ T-1〜T-10 の 10 本）・
  **JVM 238 / 赤 0（45 クラス）**・**計装 96 / 赤 0**（実機 Pixel 9・20 クラス・実装セッションの実測）・ruff clean。
  **`android/VERSION` だけを 1 歩** — `APP_VERSION`・`web/BUILD_NUMBER` は不動、pentala 反映も無し。

### 2026-08-09 — フォルダを焼くのは 1 つのコマンドである（**採番なし**・`shared/` と `cli/` のみ）

**目視材料の PNG は、ランごとに書き捨てのスクリプトで焼かれていた。** `851-…/harness/rasterize_one.py`・
`859-…/harness/rasterize_all.py`・`rasterize_full.py`・`864-…/harness/rasterize_on_pentala.py` は
**どれも同じ 1 行（`svg_to_png(svg, width=width)`）を呼ぶだけ**で、違うのは並列数と置き場だけだった。
**同じものが 4 つあり、次のランで 5 つ目が生えるところだった。**

**実装は `shared/src/inku_analysis/rasterize_batch.py`（213 行）に 1 本だけ置き、CLI はその薄い入口にした。**

- **`rasterize_dir(src, dst, *, width=None, workers=1) -> Report`**。**1 ファイル 1 子プロセス**（[I-075]。
  resvg の panic は in-process だと親を道連れにする）。子は `python -m inku_analysis.rasterize_batch --one`
  として**自分自身に再入する** — 焼く規則を 2 つ書かないためである
- **失敗したファイルは出力を残さない。** 子は隠し名の一時ファイルへ書き、**親だけが `os.replace` で最終名へ移す**。
  子が 0 で終わり、かつ一時ファイルが 0 バイトでないときに限る。**中断された実行が 0 バイトの PNG を
  1 枚残した事故**が契機で、「**失敗は 0 ではなく測っていない**」という規律が**ファイルの存在**で破られていた
- **`__main__` は落とした母集団を `UNRESOLVED (absent measurements, not zeros):` として理由つきで印字し、1 を返す**
- **この module は `inku_cli` も `inku_server` も import しない。** **`server/Dockerfile` は `shared/` と `server/` を
  COPY するが `cli/` は入れない**ので、コンテナの中で `python -m` として動くことが要件である
- **CLI は `inku-cli rasterize --in DIR --out DIR [--width N] [--workers N]`。** 段 1 の関数を呼ぶだけで、
  焼く規則を 2 つ目書いていない。**`cli/README.md` の help 節は生成器で再生成した**（48 → **49 経路**）

**pentala への配備に旗が無かったので足した** — **`deploy.sh --shared`**（`shared/` だけ・`--delete` なし・
`inku-api` を再起動）。**それまで `shared/` を含む旗は `--all` だけで、それは `cli/` `docs/` `manual/` まで
送るので [I-059] に触れる。つまり `shared/` を pentala へ送る合法な道が 1 本も無かった。**

**実測（2026-08-09・実物の SVG 24 枚・width 1618）:** **pentala 6 並列 17.7 秒 = 0.74 秒/枚**、
**Mac 4 並列 70.1 秒（CPU 136 秒 = 5.66 秒/枚）**。**PNG は 24/24 が両機でバイト一致**（resvg 0.3.3）。
**`sys.path` 操作なしで `inku_analysis` が import できることを現物で確認した。**

- **⚠ 契約の摂動 P-6 は、T-9 が守っている場合と食い違っていた** — 旗を 1 本足しても
  「`rasterize` という節が載っている」は消えない。**サブコマンドを足して生成器を回さない場合（P-6b）で
  はじめて赤くなる。****ゲートの形は正しく、契約の P 欄の書き方が誤っていた**（台帳へ起票）
- **⚠ 受け入れで穴を 1 つ塞いだ** — 契約の段 1 は「`__main__` が `UNRESOLVED` を印字する」ことも求めていたが、
  **`rasterize_dir` の戻り値にしかゲートが無かった**。**`python -m` はコンテナと pentala が通る唯一の入口**なので、
  **印字は製品の振る舞いである**。受入 2 本を足し、摂動 2 本（失敗があっても 0 を返す・UNRESOLVED を
  無条件に印字する）がそれぞれ 1 本だけを赤くすることを確かめた
- **検査:** **server pytest 2,485 passed / 31 skipped**（起点 2,476 ＋ 実装の 7 件 ＋ 受け入れの 2 件）・
  **cli pytest 182**（起点 176）・ruff clean（server・cli・`shared/src`）・`check_docs.py` 緑・
  **凍結コーパスはバイト一致**。**採番なし** — web の振る舞いも API も変わらず、
  **server のどの経路も `rasterize_batch` を import していない**

### v2.11.9 — 古い 8 作品で、書き出しがプロセスごと落ちるのをやめる（Build 865、2026-08-09）

**本番 2,769 作品のうち 8 件は、PNG に焼こうとするとサーバーのプロセスごと落ちていた**（台帳 [I-075]・2026-08-01 起票）。
`resvg` の Rust 側の assertion（`filter/displacement_map.rs`）で、**Rust の panic は Python の例外ではないので
`try: … except Exception:` では 1 つも捕まらない。**`api_core/rendering.py:85` の PNG 保存はその形で囲われており、
**守れているように見えて守れていなかった。**`inku-api` は `--workers` 指定の無い**単一 uvicorn プロセス**なので、
**1 件の書き出しが API 全体を落とし、その瞬間の全リクエストを道連れにする。**

**`resvg-py` を `>=0.3.3` から `>=0.3.4` へ上げた。これだけで 8 件とも直る。**

- **0.3.4 は 2026-08-02 公開**——**起票の翌日**である。**起票時には存在しなかったので、
  当時「版を上げる」は選択肢になりえなかった**
- **製品と同じ呼び方で対にして測った**（`svg_to_bytes(svg_string=…, width=…)`）:
  **0.3.4 は幅なし / 320 / 2160 の 3 つとも 0 / 8。0.3.3 は同じ経路で 6 / 8・8 / 8・6 / 8 が落ちる。**
  **⚠ 幅によって落ちる顔ぶれが変わる**ので、「どの寸法でも落ちる」は不正確である
- **絵は 1 画素も動かない** — `feDisplacementMap` を持つ **120 枚**を両版で焼いて **120 / 120 バイト一致**、
  **mac と pentala のあいだも 0.3.4 で 24 / 24 一致**（0.3.3 のときと同じ）、
  **0.3.3 で焼いた 24 枚と 0.3.4 で焼いた 24 枚も 24 / 24 一致**
- **⚠ 落ちるのは古い作品だけだった** — **render engine 11 以降の 1,065 件は 1 件も落ちない**
  （engine 10 以前の 790 件で 7 件・版の記録なしの 89 件で 1 件）。
  **新しい engine のほうが filter 付きの作品は多いので、母集団の偏りではない。**
  **renderer は engine 11 までのどこかで、発火する形を出さなくなったと読める**
- **退行の検査は、落ちていた 8 件のうち 1 枚を fixture として置いた**（本番 `4c257de0`・engine 4 / Build 591）。
  **Score から組み立てられない** — その形はもう生成されないからである。
  **検査は `rasterize_dir` を通す**（1 ファイル 1 子プロセス）ので、
  **panic が戻っても pytest 自身は死なず、赤いテスト 1 件として出る**
- **検査:** **server pytest 2,486 passed / 31 skipped**（起点 2,485・**+1 本 / +1 件**）・**cli 182**・
  ruff clean・`check_frozen_corpora.py` バイト一致（**SVG の比較なので版に影響されない**）。
  **採番なし** — 版数も API の鍵名も画面も変わらない

### 2026-08-09 — 焼き付けが、fade が成員ごとに届いているかをコーパスに問う（**採番なし**・検査のみ）

**参照コーパスを焼く瞬間に「その題は本当にその機構を見ているか」を確かめる番人が 3 つあり、
`fade` の 1 つだけが弱い形だった**（台帳 [I-166]・作者裁定 A）。

- **何が弱かったか** — size と angle の番人は `_apply_member_sizes` / `_apply_member_rotations` という
  **機構そのものを無効化して絵を比べる**のに対し、`fade` の番人は **`fade="none"` へ書き換えるだけ**だった。
  **engine 23 は `fade` を群まるごと 1 定数（outward 0.40 / directional 0.48）で答えていた**ので、
  **成員ごとの ceiling を 1 つも実装していなくても、宣言を消せば絵は変わり、番人は通ってしまう**
- **足したもの** — `_assert_fade_reaches_every_member`。**コーパスへ直接 2 つ問う** —
  「描かれる fading 群のどれかが 2 種類以上の ceiling を持つ」と「**縮退の 2 題は 1 つも持たない**」。
  **engine の挙動は 1 行も変えていない**（環も対も自分の中心から等距離で、順位をつければ誰も述べていない勾配になる）
- **⚠ 強い形を 6 題すべてに当てる直し方は採れない** — `G-fade-radial-edge` と `G-fade-count2-edge` は
  構造上フェードしないので、無効化しても絵が動かず必ず落ちる
- **⚠ 弱い番人は撤去していない**（残したまま問いを 1 つ足した）。**新しい問いが焼き付けから外れたことを見る検査**も置いた
- **検査:** **server pytest 2,488 passed / 31 skipped**（起点 2,486・**+2 本 / +2 件**）・ruff clean・
  `check_frozen_corpora.py` バイト一致（**絵は 1 バイトも動かない**）。
  **摂動 3 本**: fade の適用を素通しへ差し替えると **18 件が赤くなるが、弱い番人は緑のまま**で
  **新しい問いが赤くなる**（これが本件の主張そのものである）／結線を切ると**結線の検査 1 件だけ**／
  等距離の群にも傾きを返させると **3 件**（新しい問い＋環と対の 2 件）。
  **採番なし** — 版数・API の鍵名・画面・描かれる絵のどれも変わらない

### Android `2.1.4-android.20` — 移植が render engine 26 へ追随する（android Build 148106、2026-08-09）

**Kotlin 版の描画層は engine 21 を名乗ったまま 5 版ぶん遅れていた。** 22（塗りの下地と道具）・23（配置の seed）・
24（`fade` が全メンバーへ届く）・25（メンバーごとの大きさ）・26（メンバーごとの角度）を 1 周で追随させ、
`CompatibilityConstants.renderEngineVersion` を `"26"` にした。**`APP_VERSION` と `web/BUILD_NUMBER` は動かない。**

- **⚠ 追随の前に、コーパスが 5 版を「見られない」ことが分かった** — 参照 fixture の群は **34 題が 1 つ残らず円**で、
  **円にしか効かない規則しか通っていなかった**。**機構が無いのではなく題が無い**。
  `arc` / `cloudform` / `square` / 角度を明示した 2 題ほかを足して **34 → 42 題・SVG 42 → 51 枚**にした
- **焼き付け時の番人 4 つを Android の生成器へ移した** — うち 3 つは server と 1 対 1、
  4 つ目は **`fade` の番人が「宣言を消すだけ」で弱いことが実測で出た**ので、
  コーパスへ直接問う形を別に足した（**server 側は同日に台帳 [I-166] として直した**）
- **移植で見つかった既存の乖離 2 つ**（**群が全部円だったので誰も踏んでいなかった**）:
  cloudform の輪郭生成が `markIndex = 0` の決め打ちで**群の全メンバーが先頭の blob で描かれていた**こと、
  `rotate()` を空白区切りの生の double で書いていたこと
- **質感フィルタが全 tool で server より薄かった** — Kotlin が材質強度の係数を掛けずに素の spec を書いていた
  （pencil 0.7 / 1.96・crayon 1.8 / 5.04・chalk 2.2 / 6.16）。**表示専用の経路で、留めている検査は 1 つも無かった**
- **⚠ `drypoint` の質感フィルタは足していない** — Kotlin の呼び出し表 2 つがどちらも 4 道具しか挙げないので、
  定義だけ書いても誰も読まない（**受入が置けない**ため範囲外とした）
- **⚠ 段 0 が server 側の赤を 1 件暴いた** — 円以外の群を足したところ、
  Android 参照 fixture の F-2「anchor は必ず 9 桁」が落ちた。**16 桁は量子化漏れではなく正常**で
  （`square` の anchor は `position + size/2` の和）、**その前提は全題が円だったから成り立っていた**。
  **作者裁定 A（台帳 [I-165]）により、F-2 の母集団を「anchor が保存座標である primitive」へ絞った**
- **検査:** **Android JVM 243 passed / 赤 0 / skip 0（46 クラス。238 から +5 ちょうど）**・
  **実機 Pixel 9 の計装 96 / 96・赤 0（20 クラス）**・**server pytest 2,488 passed / 31 skipped**・ruff clean。
  **摂動 9 本すべて予告どおり赤くなり、空振りは 0 本**

### v2.11.10 — 手はもっと大きく振れる（Build 866、2026-08-09・render engine 27）

**engine 25 と 26 が入れた 2 つの振り幅を、巡 2b の目視を受けて広げた**（作者裁定 2026-08-08）。
**規則も除外も 1 つも変わっていない。振れる幅だけが変わった。**

- **成員ごとの大きさが ±25% → ±35%**（`HAND_GROUP_SIZE` 0.25 → 0.35）、
  **成員ごとの向きが ±12° → ±27°**（`HAND_GROUP_ROT` 12.0 → 27.0）。
  **手の道具 9 種はすべて同じ振り幅**で、**`rotring` と `computer` は 0 のまま**である
  （機械の反復が正確であることは欠陥ではなく署名である）
- **除外は 1 行も動いていない** — `grid`・単員・機械の道具・`line`・`circle`・`rotation` 明示の群。
  **`circle` は回しても見た目が変わらないので回さない**という判断も engine 26 のまま
- **コーパスは 549 件のうち 45 件が動き、504 件が動かない**（`render-engine-27`）。
  **case は 1 件も足していない**（`added` = 0）。動いた 45 件の内訳は circle 37 / ellipse 3 /
  line 1 / square 1 / triangle 1 / arc 1 / cloudform 1 で、**そのうち向きまで届くのは 5 件**である
- **⚠ 枠の補正が発火する群は増えなかった** — 発行時の見込みは「振り幅が広がれば増える」だったが、
  **実測では engine 26 と同じ 50 件中 40 件で、集合としても一致した**。
  `_fit_group_to_anchor` は**成員の anchor だけを読み**、`_scale_member` は 3 つの座標補正で anchor を保存し、
  `_turn_member` は anchor を軸に回すので座標を動かさない。
  **したがって振り幅をいくら広げても、枠の補正が見る入力は 1 ビットも変わらない**
- **⚠ 「痕が枠から出ない」は、振り幅の話をする前から成り立っていなかった** — 枠 `[0.02, 0.98]` は
  **anchor についての契約**で、痕の広がりについての契約ではない。**engine 26 の時点で 50 件中 41 件**の
  成員の外形が canvas `[0,1]` をはみ出しており、**engine 27 でも同じ 41 件**で、
  いちばん出ている 1 枚が 0.4% 画布ぶん深くなっただけである（最大 0.050187 → 0.054262）
- **engine 25 の凍結記録を再生する検査は、振り幅だけを 0.25 へ戻して走らせる形にした** —
  `_apply_member_sizes` を止める形にはしていない。**止める形は「成員ごとの大きさを丸ごと落とした実装」でも
  通ってしまい、engine 25 の番人が拒む読みになる**。角度側は木の値のまま走らせ、
  **それでも 43 件が一致すること**（＝角度規則がこの 43 件に届かないこと）を確かめた
- **検査:** **server pytest 2,501 passed / 31 skipped**（+13・赤 0。**関数は +5 本・削除 0 本**）・
  ruff clean・**凍結コーパスはバイト一致**。**摂動 7 本で空振りは 0 本**。
  **⚠ ただし 1 本、予告した T が赤くならなかった** — 定数を戻す摂動に対して
  「manifest 26 と manifest 27 を突き合わせる検査」は動かない。**それは焼き直される記録であって
  性質の検査ではない**。製品を通って赤くなったのは既存の `test_group_g_matches_the_current_renderer` で、
  こちらは摂動 6 本すべてで赤くなった

### 2026-08-09 — 英語の文書が、用語の正本に答えるようになる（**採番なし**・文書と検査のみ）

**`web/src/lib/i18n/GLOSSARY.md` は inku の英語すべての正本だが、機械が照合していたのは
web の表示文字列だけだった**（台帳 [I-161]・作者裁定「更新される文書だけを見る」）。
`npm run lint:i18n` が読むのは `en.ts` と web のコンポーネントで、
**README・SPEC・マニュアル・ANDROID_SPEC といった英語の文書は 1 本も見ていない**。
日英を見る `check_docs.py` は在るが、**見るのは骨格（見出しの階層列）だけで中の語は見ない**。
だから「緑なのに用語違反がある」が続いた。

- **英語 5 文書の 24 行を直した。** **日本語側は 1 文字も変えていない** — 日本語は既に正しく
  （`SPEC.ja.md` は「色カタログ」22 に対し `palette` 3 がすべてバックティック内）、
  **英語だけが旧語彙のまま**だった。直すと日英が揃う
- **直し方は 2 通りしかない。** **概念**（作品そのもの・色カタログそのもの）を指す **15 行は語を直し**、
  **コード識別子**（enum 名・フィールド名・タブの内部名）を指す **9 行はバックティックで囲んだ**。
  **「grep 残存ゼロ」は目標ではない** — 識別子は識別子のまま残すのが正しく、
  囲むことで機械の視野から外れる。`ANDROID_SPEC.md` の `Artwork` は Kotlin の enum メンバで、
  **画面には出ない語である**（`strings.xml` に 0 件）。ここを `work` に直すと SPEC が実装について嘘を書く
- **ゲートは `check_docs.py` に足した。** CI ではなく**マージ前に必ず走る唯一のゲート**だからである。
  見るのは `PAIRS` **17 対の英語側**を **4 語**（`artwork` / `palette` / `AI-powered` / `magic`）で。
  **`CHANGELOG.md` と `docs/history/changelog-*.md` の 3 本は凍った記録なので、宣言された除外**である。
  **対象の本数を毎回印字する** — 表が縮んだことが exit 0 の裏で起きないようにするため
- **⚠ フェンスの中も読む。** 本ファイルの他の検査はフェンスを飛ばすが、
  **24 行のうち `PROJECT_CONTEXT.md:54` はフェンスの中にあった**（`text` で描いたパイプラインの図で、
  中身は散文）。飛ばすとこの 1 行を誰も見ない。**代償**は、
  将来カタログ JSON の実例を載せる周が来たときに囲んで逃げる道が無いことで、そのときは除外の宣言が要る
- **設定表そのものを見る検査 27 本**（`test_terminology_gate.py`）。**合計だけを表明しない** —
  **禁止語 4 語を 1 語ずつ・対象 17 本を 1 本ずつ・対照として除外の 3 本が対象に無いこと**・
  **`main()` が結果を受け取っていること**・囲みの中は違反にしないこと・素の禁止語は検出すること。
  行を 1 つ消しても `check_docs.py` は exit 0 のまま「見る対象が減る」だけになるからである
- **検査:** **server pytest 2,528 passed / 31 skipped**（**+27・赤 0・削除 0 本**）・ruff clean・
  `check_docs.py` 緑（**対象 17 本・内部参照 55**）。**摂動 6 本**（うち 1 本は対照 =
  除外されている `CHANGELOG.md` に素の禁止語を足しても何も赤くならないこと。
  **同じ 1 文を `README.md` へ置き直すと赤くなる**ので、これは空振りではなく除外の働きである）。
  **⚠ P-2 だけ予測と食い違った** — 禁止語リストから `artwork` を消すと予測は 1 本赤だったが**実測 2 本**で、
  **素の禁止語を検出する検査の例文がその語を使っている**ためである。**欠陥ではなく結合で、
  番人としては弱くなっていない**（予測より多く赤くなる向き）。
  **決定的な層・`web/` `cli/` `shared/` `server/src` `android/app` は 1 バイトも動いていない**
- **採番なし** — 版数・API の鍵名・画面・描かれる絵のどれも変わらない

### v2.11.11 — 作品が、自分の色を憶えている（Build 867、2026-08-09）

**描き直しは保存された id をサーバーへ送り、サーバーが現行の定義を引いて色を決めていた。**
だから**カタログの定義が変われば、同じ作品が別の色で描き直される**（台帳 [I-123]）。
本番 2,769 作品のうち **1,274 件（46.0%）**がこの群に居た。

- **色の正本を「カタログ id」から「作品が持つ記録」へ移した。** 描画要求が作品を名指せば
  （`/api/render-svg` と `/api/render-score` の **`work_id`**、CLI の **`--from-work`**）、
  サーバーはその行の `render_color_map` で描き、**カタログの今日の定義を読まない**
- **改名されたカタログも退役したカタログも描けるようになった** — この経路は id の解決を通らないので、
  **現行 13 本に無い id でも 422 にならない**。記録を持たない古い作品は現行の定義へ落ち、
  **その落とし先でも 422 を出さない**（拒むと「記録より前の作品」だけが描き直せないまま残る）
- **色の出どころを応答で申告する** — `/api/render-score` は `render_color_source`、
  `/api/render-svg` は本文が SVG なのでヘッダ 2 本（`X-Inku-Color-Source` と
  `X-Inku-Color-Catalog-Id`）。**後者を足したのは、作品を名指した呼び手はカタログを指定していないので、
  どのカタログが描いたかを知る道が他に無いから**である
- **名札は現行名で出す。** 退役したカタログには`引退済み`、色の記録を持たない古い作品には
  `当時の色は記録がない`を添える。**既定名に落とさない**（落とすと「別のカタログで描かれた」と読める）
- **改名 10 対にあたる 57 件の名札を起動時に直す**（`catalog_id` 列だけ）。
  **⚠ `render_color_catalog_id` は書き換えない** — この id は名札ではなく**色の割り当ての種**でもあり
  （`_WORK_COLOR_SEED_FIELDS`）、**書き換えると `render_color_map` が 1 バイトも同じでも割り当てが変わる**。
  実測で 10 対とも seed 200 通り中 38〜70% が別の割り当てになった。**2 列が 57 行で食い違うが、
  実効 id は `render_color_catalog_id` が勝つので描画は不変**である
- **新規描画の経路は 1 行も変えていない**（`/api/paint` と `/api/compose`）。
  クライアントに色を持たせてもいない — web も CLI も色を組み立てて送らない
- **⚠ 契約が指定した摂動 P-2 は 1 件も赤くならなかった** — カタログの `map` を書き換える摂動だが、
  **`map` の値は描画に届いていない**（`_work_color_assignment` は `palette:` 由来の候補から色を選び、
  `map` は候補の無い帯の落とし先にしかならない）。**判別力は別の形で確かめてある** —
  記録を持つ作品は塗り替えても絵が動かず、参照を渡さない経路は同じ塗り替えで動く（両方緑の対）。
  **逆向きは P-1 が示した**（結線を切ると参照つき 7 本が赤・参照なしの対照は緑のまま）
- **検査:** **server pytest 2,561 passed / 31 skipped**（**+33**・赤 0・削除 0 本）・
  **cli 188**（+6）・**web `test:unit` 123**（+6）・ruff clean（server / cli）・
  `npm run check` 0 errors・`lint:i18n` 0 errors・`check_docs.py` 緑・
  **凍結コーパスはバイト一致**（決定的な層に触れていない）。
  **摂動 13 本**（契約の 7 本 ＋ 実装が足した 6 本）。**空振りは 1 本 = P-2 で、理由は上の通り**

### v2.11.12 — ログの方針を、アプリ自身が実行する（Build 868、2026-08-09）

**設定画面はログの方針（有効／保存日数／周期／圧縮）をアプリ DB に保存しながら、
実行だけを OS へ投げていた。** 投げ方は `systemd` の drop-in と `logrotate` の設定文を
**生成して管理者に貼らせる**形で、その drop-in が
`StandardOutput=journal+append:/var/log/inku/inku-api.log` だった（台帳 [I-167]）。

- **`journal+append:` は systemd に存在しない出力指定子である。** systemd 249 は毎起動
  `Failed to parse output specifier, ignoring` を記録して行ごと捨て、既定の `journal` に落ちていた。
  **`/var/log/inku/*.log` は 2026-05-07 から 0 バイト**で、それを前提にした 90 日保持も空振りしていた
- **コンテナ配布版には貼る先が無い** — systemd も logrotate も無いので、
  **プラットフォームが実行する方針は、2 つの配布形態で同じにならない**
- **DB バックアップと同じ作りへ寄せた。** 方針は DB に置いたまま、**アプリ自身が書き出し・回転・圧縮・
  古い世代の削除を行う**（`server/src/inku_server/logging_setup.py`）。
  置き場は **`INKU_LOG_DIR`**（既定 `~/.local/share/inku/logs`、コンテナ配布版は **`/data/logs`**）で、
  イメージが `inku-data` ボリュームを指すので**再起動しても残る**
- **標準出力には出し続ける** — `journalctl -u inku-api` と `docker logs` はこれまでどおり使える。
  コンテナ配布版では、daemon が標準出力から集めるぶんの上限を `compose.yaml` の `logging` が持つ
  （既定は無制限だった）
- **設定画面から 2 つのプレビュー欄を撤去した**（systemd drop-in と logrotate 設定）。
  代わりに**書き出し先といまあるファイル**を出す。API の `log_retention` も
  `systemd_dropins` / `logrotate_config` / `services` を落とし、`log_dir` / `files` を持つ
- **起動バナーが名乗る宛先も、方針から引くようにした** — それまで
  `log: journal + /var/log/inku/inku-api.log` と、**書けていないファイルを固定文字列で名乗っていた**
- **`manual/` の systemd 雛形 4 本から壊れた指定子を外し、logrotate の雛形は撤去した**
  （日英とも。**公開文書が同じ設定を配っていた**）

### v2.11.13 — 痕は自分の線の上に残る（Build 869、2026-08-09・render engine 28）

**「主線を正確にトレースしつつ、幅を少しはみ出して主線に道具のトーンを付け足す」という作者の要求
（2026-08-09）に対して、4 つの規則が同じ版で動いた。** どれも「道具が紙に触れている場所」の話である。

- **揺らぎの物差しが図形の代表寸法から線幅になった** — 振幅は
  **`AMPLITUDE_WIDTHS`（fine 0.35 / medium 0.6 / broad 2.0）× 線幅**で決まる。
  それまでは代表寸法の 8%（`medium`）だったので、**細い鉛筆で大きな弧を描くと軌跡が自分の痕の
  11 倍ずれていた**。**`medium` の 0.6 は作者が 0.6 と 0.9 の見本を見て決めた**（2026-08-09）。
  **代表寸法の 0.40 というクランプは残してある** — 自分の痕より小さい図形のための安全弁である。
  **実測: 描いた弧のずれ ÷ 線幅は 6 道具 × 細さ 2 × 半径 4 の 48 組で 0.595〜0.600 に揃い、
  半径によらず平らになった**（engine 27 では半径の 7.9〜8.5% ＝ 2.88〜12.21 線幅）
- **材質輪郭（道具のトーン）が、意図した幾何ではなく演奏された墨からオフセットを取るようになった** —
  **`wild` のときだけの挙動ではなくなり、すべての作品に効く**。
  装飾の頂点から墨の帯までの距離（中央値）は **16.09 / 5.99 / 8.11 / 6.09px（大きい弧・小さい弧・円・
  正方形）から 3.21 / 2.26 / 3.07 / 2.80px** へ。**線だけは engine 27 でも既に墨に沿っていた**
  （0.86 → 3.20px。直線の道具だけが正しく、ほかの図形が追いついた形である）
- **掠れが `stroke-dasharray` をやめ、紙の目を表す接触の場が閾値を越えた区間だけを描くようになった** —
  **触れている割合は道具ごとに従来の dash 表から引く**ので、ペンはほぼ連続・鉛筆は隙間が多いという
  性格は保たれている。**見本 1 枚 192 断片で、同じ長さの重複は 1 組・標本間隔の整数倍に乗るのは 2.6%・
  `stroke-dasharray` は 0 個**
- **トーンの濃さを道具に合わせて 2 つの規則で絞った**（作者裁定「四角の装飾は濃すぎる」への対応）—
  **層は自分の痕の 0.33 倍より太くしない**（上限が読むのは**公称**の線幅。
  紙の目や粉は線を細く引いたからといって細かくならない）・**層の中心を痕の内側に置かない**
  （床が読むのは**実際の**線幅。どこに置くかは実際に描かれた痕への問いである）。
  **装飾が置く墨は pencil −25% / pen −12% / crayon −22% / chalk −29% / brush_thin −32% /
  brush_thick −17%**。**⚠ 測ると、装飾の墨は engine 27 より既に減っていた**（四角で 962 → 803px²）—
  **濃く見えていたのは量ではなく位置である**。**縁からさらに離す案は同じ結果になった**
  （四角で 673.1 対 672.9px²。**効いたのは層の太さで、縁からの距離ではない**）
- **コーパスは 549 件のうち 454 件が動き、95 件が動かない**（`render-engine-28`）。
  **動かない 95 件は材質輪郭を持たない 5 道具**（rotring 22 / drypoint 21 / silverpoint 19 /
  computer 17 / burin 16）である。**⚠ 契約の予告は 54 件だった** — あれは「揺らぎが幾何に届く case」の
  数で、**材質輪郭を持つ 6 道具の描画がすべて変わることは、前提が変わるまで数えられていなかった**
- **旧い版の凍結記録を再生していた検査 6 本を据え直した** — どれも「後から載った層を無効化して
  旧版に到達する」形で、engine 28 では原理的に一致しない。**主張が機能についてのものは、
  版を挟まない「入 vs 切」へ作り直した**（fade・角度・構図 seed・成員ごとの大きさ）。
  **⚠ 2 本は置き換えではなく損失である** — `test_surface_stroke` の帰属の観測点は失われ、
  `test_anchor_authority` の担保は **447 件から 92 件**（材質輪郭を持たない 5 道具）に痩せた。
  **どちらもその旨をコードに書き、台帳へ起票した**
- **Android の参照 fixture に `render-engine-28/` を新設した**（64 ファイル）。
  **移植は `CompatibilityConstants.renderEngineVersion` で 27 を解決し続けるので、
  ディレクトリが増えただけで照合先は動いていない**（追随は別契約）
- **検査:** **マージ後の木で server pytest 2,624 passed / 31 skipped / 0 failed**
  （**test 関数は起点 `dc46dd1a` 比で +15 本・裏返し 9 本・削除 0 本**。うち 1 本は受け入れで足した）。
  **枝の上での増減は 2,559 → 2,598〈+39〉**（実装セッションの実測）。**cli 188 passed**・ruff clean・
  **凍結コーパスはバイト一致**・**摂動 8 本で空振り 0 本**。
  **⚠ 受け入れで 1 件直した** — **凍結したコーパスの 1 件が、焼いたあとに変わった上限の実装で
  再生できなくなっていた**（`C-fill-circle-chalk-extra_fine`。上限が実際の線幅を読んでいた頃の
  0.346500 を持ったまま、公称を読むいまの実装は 0.990000 を描く）。
  **焼き直した。ほかの 453 件と 95 件は 1 バイトも動いていない**
- **⚠ 受け入れで検査を 1 本足した** — **描いた絵の上で「ずれが線幅の 0.6 倍であること」を
  道具 4 種 × 細さ 2 で見る**。それまで定数と絵を同じ表明の中で結びつける検査が無く、
  **単体検査は関数と定数の一致しか見ず、大きさ間の不変性を見る 2 本は振幅を定数に落とした実装でも
  通った**（満点 8・物差しを戻す摂動で 8 赤・`thinness` を落とす摂動で 4 赤）

### Android `2.1.4-android.21` — 画面が読み手の言葉で話す（android Build 148106、2026-08-09）

**Android だけが日本語に固定されていた。** 操作画面の文言を言語パックへ出し、歳時記の語彙を
server の生成物へ移し、読み手が設定から言語を選べるようにした（既定は `ja`・[I-065]）。
**`APP_VERSION` と `web/BUILD_NUMBER` は動かない。**

- **歳時記が手写しから生成物になった** — `server/scripts/gen_saijiki_kt.py` が `saijiki.py` から
  `SaijikiGenerated.kt` を焼く（web の `gen_saijiki_ts.py` と同型）。
  **画面が持つ語彙は 9 分類 62 語（日本語のみ）から 10 分類 73 語（日英とも）になった**
- **⚠ なぜずれたかが分かった** — Android は 2026-07-26 に「てざわり」を 10 語へ同期し、
  **同じ周にその 10 語を `assertEquals` する検査が書かれた**。**server は翌 07-27 に銀筆を語彙へ戻した**が、
  **検査が前日の値を期待値として持っていたので、緑のままズレを守り続けていた**。
  この検査は Stage 1 プロンプトの列挙（銀筆を含む 11 語）を見る形へ置き換えた
- **画面の文言が言語パックから来る** — `InkuStrings`（interface）と `InkuStringsJa` / `InkuStringsEn`
  の**各 254 鍵**。`res/values-en/` は採っていない（端末ロケールに追随してしまうため）。
  **エラー文言は投げる場所でなく出す場所で訳す**（`InkuFailure`。それまで 13 箇所で
  `error.message` が直接画面へ出ていた）
- **新しい lint が数える直書きは、起点で 446 件・改修後 0 件**（日本語リテラル 452 − 除外 6）
- **設定から言語を選ぶと 3 つが同時に変わる** — 画面の文言・歳時記の語彙・作品の言語。
  鍵名は server と同じ `ui_lang`、不明な保存値はエラーにせず `ja` に落ちる
- **⚠ 描画の挙動が変わった** — Android は `instructionLang` を渡しておらず `auto` の枝に
  一度も入っていなかったので、**英語で書かれた記述でも常に日本語の Stage 1 プロンプトで描いていた**。
  web は定数 `'auto'` を毎回送り、server は `auto` のとき `ui_lang` を fallback に使う。
  **描画の 5 経路すべてで `AUTO` と `uiLang` を明示で送るようにした**（実行規約 §2-4 の「同じ判定」）。
  **日本語 UI のままでも、英語で書いた記述は英語で解釈される**（作者裁定 2026-08-09）
- **⚠ 触ってはならない 16 ファイルのうち 1 本を 2 行変えた**（作者裁定 2026-08-09）—
  `LocalFallbackPipeline.kt` は `resolveWithUiLang` の呼び出し 2 箇所を持つ唯一のファイルで、
  結線先がそこにしか無かった。**変更は引数の受け渡しだけで、16 ファイルの日本語リテラルは
  1,386 のまま差 0**（受け入れ側で起点と枝を数え直した）
- **歳時記の色が 9 色しかなく、10 分類目が 1 色目と同じ色になっていた**（`index % size` なので
  失敗せずに衝突する）。10 色目を足し、`android/design/preview/color.html` を焼き直した
- **検査:** **Android JVM 263 passed / 赤 0 / skip 0（49 クラス。243 から +20）**・
  **実機 Pixel 9 の計装 96 / 96・赤 0**（枝で実測。**マージ後の木の `app/src/main` と
  `app/src/androidTest` は枝と同一ハッシュ**）・**server pytest 2,626 passed / 31 skipped**・ruff clean。
  **摂動は実装が 10 本**（予測 9 ＋ 1。**P-5〈en のパックから鍵を削る〉は型で先にビルドが落ちたので、
  「en が日本語で答える」へ観測点を置き換えた**）、**受け入れ側がさらに 1 本**
  （**段 3 ①〈画面の文言が切り替わる〉に摂動が当たっていなかった** — パックを定数へ固定すると
  `testTheSettingsScreenOffersTheChoiceAndTheTreeIsProvided` だけが赤くなることを確認した）
- **⚠ 用語の食い違いを 1 件残した** — 系譜の起点を、辞書と Android は `Origin`・
  **web は `Root`** と出している（`web/src/lib/derivation.ts`）。**台帳へ起票し、web を触る周に直す**

### v2.11.14 — 描き直しも、記述を読む（Build 870、2026-08-09）

**web は描き直しのたびに記述（DDL）を server へ送っていたのに、server が受け取って捨てていた。**
`/api/render-score` は coerce を呼ぶ 5 箇所のうち **DDL を渡していない側**にいたので、
同じ作品でも「最初に描く」ときと「描き直す」ときで補修の判定が違っていた。
**site 4 を結線し、判定を `/api/paint` と揃えた**（作者裁定 2026-08-09・A-1）。

- **⚠ これは絵が変わる改修である。** 言葉でタッチを変える経路と、別のカタログで描き直す経路で、
  **記述に書いた本数や関係が絵に届くようになった**。golden 40 中 29 が動き、9 は図形の本数まで変わる
- **本番の足跡を測ってから裁定を仰いだ**（2,817 作品 / 系譜の辺 218 本）—
  **`catalog_change` 36 辺のうち子作品に DDL があるのは 20**、
  **`touch_change` 25 辺のうち保存された 22 件は DDL が全部 NULL**。**動きうるのは 20 作品規模である**
- **空の DDL は「渡さない」と同じ道を通る**（web は `ddl ?? ''` を送る）。
  受け入れで API を直接叩いて確かめた —— **省略と `""` で score・svg・render_hash が完全一致**
- **`inku-cli render-score` が自分の名前の宛先を叩くようになった**（作者裁定 B-1）。
  これまで `render-score` は `/api/render-svg` を叩いており、名前と宛先が食い違っていた
- **旗を 2 つ足した** —— `--ddl-text` と `--ddl-file`（`-` は標準入力）。同時指定はエラー。
  **これで「LLM を通らずに coerce の DDL 分岐を踏む」経路が CLI にできた**（実行規約「機能テストは `inku-cli` を通す」）
- **`RenderScoreRequest` に `svg_profile` を足した** —— 宛先を移した CLI が既存の `--svg-profile` を保つため。
  **既定は `display` で、これは移す前に `/api/render-score` が描いていたプロファイルと同じ**（web の 2 経路は 1 バイトも変わらない）
- **⚠ CLI の成果物の鍵が 3 つ動く**（宛先が変わった帰結）:
  **`score` は送った物から coerce 後へ**、**`render_hash` / `render_hash_short` は CLI 計算の rh2 から server 計算の rh3 へ**。
  **seed を固定すれば `svg` はバイト一致する**（受け入れで実測）
- **⚠ `--render-seed` を渡さない場合、`render_seed` も動く** —— **`null` から実際に描いた seed へ**。
  **これは受け入れで見つけた 4 つ目の差で、完了レポートには挙がっていなかった**。
  成果物が「描かれた seed」を名乗るようになるので**改善だが、鍵の値は変わる**
- **⚠ CLI に遊んだ関数が 2 つ残った** —— `_server_render_versions` と `_render_hash_for_score` は
  どこからも呼ばれなくなった（版数の問い合わせと hash 計算を server が返すようになったため）。**台帳へ起票した**
- **検査:** **server 2,626 → 2,631 passed / 31 skipped**・**cli 188 → 195 passed**（削除 0）・ruff 両方 clean・
  **凍結コーパスと `coerce_golden.json` は 1 バイトも動いていない**（coerce の判定を触っていないことの肯定形）。
  **摂動 7 本は受け入れ側で全部当て直した**

### v2.11.15 — 循環は序列を発明しない（Build 871、2026-08-09・ddl-engine 8, 9）

**`color_cycle` は成員へ 1 色ずつ順に配るだけの器で、先頭も序列も持たない。**
それなのに coerce が 2 種類の順序をそこへ書き込んでいた（[I-060]）。

- **①同じ色が二重になっていた** —— `_with_color_cycle_delivery` が instruction 自身の色を
  **見ずに先頭へ挿していた**ので、既に cycle にある色は**メンバーを 2 倍取った**。
  重みは誰も要求しておらず、**その大きさは cycle がたまたま何色あるかで決まっていた**。
  **本番の 3,391 の `color_cycle` 中 207（6.1%）・204 作品（7.4%）がこの形だった**
- **②新しい色が全部落ちていた** —— `_color_repair_order` が要求色を**黄・橙・紫より前からある 6 語の表**で
  絞っていたので、**旧色を 1 つでも書くと新色が全部消えた**（`{赤, 黄}` → `[赤]`）。
  表は**順位ではなく決定性のための既知の順**に改め、**表に無い色は落とさずその後ろに続く**
- **③届けた色を、同じ巡で主 stroke へ昇格できていなかった**（ddl-engine 9・作者裁定による範囲拡大）——
  **昇格は cycle にある色しか見えない**のに、cycle へ色を入れる補修より**先に**走っていた。
  本番は 1 巡しかしないので、**同じ入力を 2 度通すと違う Score が出ていた**。順を **補修 → 昇格**へ入れ替えた
- **昇格は「記述が既に説明している色を持つ stroke」を奪わない** —— instruction の主 stroke は 1 本なので、
  2 度目の昇格が 1 度目を消す。**巡をまたぐと赤と青が同じ stroke を永久に奪い合った**
- **三日月の番人が cycle の緑も見るようになった** —— 手前の昇格が `color` を書き換えた瞬間に
  発火しなくなり、**緑が cycle に残って renderer がその色で描いていた**
- **`ddl_engine_version` 7 → 9**。`ddl-engine-8/` と `ddl-engine-9/` を焼いた。
  **8 → 9 で b_coerce 21 中 8 件・golden 4 件が動き、a_expand 13 件は 1 バイトも動かない**。
  **非冪等なケースは 5 → 3**（残る 3 件は色ではなく座標と `weight` が動く別機構）
- **⚠ 版を上げると「7」を書き留めていた 6 箇所が赤くなる**（`test_ddl_reference.py` 1・`test_api.py` 4・
  Android の参照 fixture 1）。**契約はこれを予告していなかった**
- **⚠ Android の `ReferenceCorpus.kt` は `7` のままで server から 2 版遅れになった**。
  **参照 fixture が版ごとのディレクトリに分かれているので赤くはならない**（追随は Android トラック）
- **検査:** **server 2,631 → 2,658 passed / 31 skipped**（削除 0・新規 27）・**cli 195 passed**（据え置き）・
  **Android JVM 263 件 緑**・ruff は server / cli とも clean・**凍結コーパスは darwin でバイト一致**。
  **受入 10 本に摂動 10 本を当てた**。**⚠ 昇格の番人を外す摂動は冪等性テストを赤くしない** ——
  留めているのは `test_t10_*` の 2 本だけなので、**この 2 本を消すと番人の観測点が無くなる**

### v2.11.16 — 成果物が、自分を描いた層の名を書く（Build 872、2026-08-09）

**`inku-cli` が手元へ書く成果物 JSON は、その絵を描いた層のうち renderer の版しか名乗っていなかった。**
`render-score` に `--ddl-text` / `--ddl-file` が付いた（v2.11.14）ことで**この経路の絵は DDL 層の版に依存する**のに、
成果物には `render_engine_version` しか無く、**あとから JSON だけを見てもどの coerce が描いたのか分からなかった**（[I-182]）。

- **成果物が `ddl_version` と `ddl_engine_version` を記録する。** 書き出し口は 5 つあり、
  **`render-score` の成果物・compose 応答から作る paint 結果・履歴 payload・履歴 export の 2 箇所**すべてに載せた。
  値は server の応答をそのまま写す（server は 3 つの応答すべてで既に返していた。CLI が捨てていただけである）
- **古い作品では鍵は載るが値は `null` になる** —— `ddl_engine_version` は `history` の nullable 列で、
  server は値があるときだけ応答へ載せる。**履歴 export は `item.get(...)` の形にして、鍵を持たない行でも落ちない**
- **⚠ 古い server に当てると `render-score` が止まるようになった** —— `command_render_score` の
  `required` に両鍵を足したため、**両鍵を返さない server では `null` を書かずに `CliError` で停止する**。
  **版を名乗らない成果物を書くより、書かないほうがよいという既存の判断に揃えた**（`render_engine_version` と同じ扱い）
- **呼び出し元を失った道具を製品コードから外した**（[I-180]）—— `_SERVER_RENDER_VERSION_KEYS`・
  `_server_render_versions`・`_render_hash_for_score` の 3 定義と、**道連れの `_canonical_json` と `import hashlib`**。
  v2.11.14 で版数の問い合わせと hash 計算を server が返すようになった後始末である
- **「どの鍵が動いたか」を示す検査は残した** —— rh2 の計算を**テスト側のヘルパへ写して**、
  `test_render_score_without_ddl_changes_only_server_owned_output_keys` が
  **旧 rh2 と server の rh3 が違うことを示し続ける**。消えた関数そのものを見ていた検査 1 本だけを削除した
- **manual の日英「入力と出力」に、成果物が DDL 層の版を記録することを 1 文ずつ足した**。旗は 1 つも増えていない
- **検査:** **cli 195 → 197 passed**（削除 1・新規 3）・**server 2,658 passed / 31 skipped 据え置き**
  （**server は 1 バイトも動いていない**。`web` `shared` `android` `docs` も同じ）・ruff は server / cli とも clean・
  `check_docs.py` は consistent。**受入 6 本・摂動 7 本**
- **実装は Codex（別モデル）なので、受け入れ側で T も摂動も全部当て直した** —— **摂動 7 本は全部予告どおり赤くなり、空振り 0**。
  **⚠ P-5（履歴 export を `item[...]` の添字へ戻す）は、新設の受入だけでなく既存の
  `test_history_export_writes_contact_sheet_and_evaluation_json` も赤くする**。実装の報告は対象を絞って測っていたので 1 件だった
- **受け入れで `inku-cli` を隔離 API へ通した** —— `--ddl-file` あり／なしの 2 本とも成果物へ
  **`ddl_version` 3 / `ddl_engine_version` 9** が載り、`/api/info` と一致した。
  **前版の CLI が書いた同じ経路の成果物は版の鍵を 3 つしか持っていない**（`render_build_number`・`render_engine_id`・`render_engine_version`）

### 2026-08-09 — 仕様の列挙が、応答が実際に返している鍵に追いついた（**採番なし**・文書のみ）

**`SPEC.ja.md` §「render JSON が記録する描画文脈」の列挙に `ddl_version` と `ddl_engine_version` が無かった。**
server は `api_core/rendering.py` の `_base_render_metadata` で**両方を必ず積んでいる**（compose / paint / render-score の 3 応答すべて）のに、
仕様側の一覧だけが v2.11.16 より前から欠けていた。**列挙へ 2 鍵を足し、それが「絵を決めた DDL 層の版」であることと、
保存済み作品では版を記録する前の古い行にだけ欠けることを日英とも 1 文で述べた**（`history` の 2 列は nullable で、
`db.py` は値があるときだけ応答へ載せる）。**コードは 1 行も変えていない。**

### v2.11.17 — 同じ紙の目を、どの機械でも同じだけ数える（Build 873、2026-08-09・render engine 29）

**main の CI が赤かった原因はこれ 1 つで、退行ではない**（[I-178]）。
**engine 28 の凍結コーパスは Mac で焼かれており、Linux で焼き直すと 549 件中 6 件が別のバイト列になった。**
生成器の identity guard が `exit 1` するので、push のたびに赤い run が積まれていた。

- **割れたのは 6 件とも pencil で、`material-outline stratum-1` の polyline だけ**。輪郭本体は両 OS で一致していた。
  **割れ方は 2 種類** —— **構造が違う 3 件**（点の数や断片の数まで違う）と、**ファイル長が同じで座標だけ違う 3 件**
- **手当ては数える側に当てた** —— 接触判定が読む **segment 長・総弧長・sampling step・grain 幅・断片長**を、
  **SVG が書くのと同じ小数 6 桁の px 格子**へ載せた（`CONTACT_LENGTH_QUANTUM = 6`）。
  **座標を丸めるだけでは足りない** —— 1 ULP が標本を 1 本増やすと、**標本自身の分位点である閾値が別の値へ飛び**、
  交差位置も `length < 0.6` の足切りもまとめて変わる。点の数が動く 3 件はそれでは直らない
- **`render_engine_version` 28 → 29。`render-engine-29/` を焼いた**（`render-engine-28/` は 1 バイトも触っていない）。
  **549 件中 454 件が動き、95 件が動かない**。**動いた 454 件は engine 28 で `material-outline` を持つ 454 件と一致する**
  （pen 235 / pencil 83 / brush_thick 71 / crayon 31 / chalk 18 / brush_thin 16）。
  **動かない 95 件は材質輪郭を持たない 5 道具**で、engine 28 のときと同じ 95 件である
- **平台安定性ゲートが、いまの露出を見るようになった** —— これまで対象は group G の 50 件だけで、
  **割れた 6 件は 1 つも入っていなかった**。いまは**露出集合を現物から導き**（549 件を描いて `material-outline` を出す
  454 件を数える）、**本命は 27 件を 2 巡**する（arrangement 15・engine 28 で割れた 6・6 道具の代表 6）。
  **手書きの件数へ戻すと赤くなる番人**を、検査の本文を読む形で置いた
- **Android の参照 fixture `render-engine-29/` を焼いた**（64 ファイル）。**server 側の F-1 検査が現行版の
  ディレクトリを要求するためで、既存のディレクトリは 1 つも動いていない**。**Kotlin 側の追随は [I-177] のまま**
- **検査:** **server 2,658 → 2,661 passed / 31 skipped**（削除 2・新規 5）・**ruff clean**・
  **`check_frozen_corpora.py` は darwin でバイト一致**・**Android JVM 263 件 失敗 0**・`check_docs.py` consistent
- **実装は Codex（別モデル）なので、受け入れ側で T も摂動も全部当て直した** —— **摂動 6 本とも予告どおり赤くなった**。
  **⚠ 受け入れは 3 本で「もっと多くが赤い」を観測した**（実装はゲートの一部だけを走らせていた）:
  **P-2 は 3 件**（直接検査だけでなく本命と凍結 digest も）・**P-3 は 2 件**・**P-5 は 2 件**
- **⚠ レポートの「本命は 26 件」は 27 件だった** —— 代表は道具 6 つに対して 6 件取られており、
  pencil の代表（`A-pencil-arc`）は割れた 6 件のどれとも一致しない
- **受入⑤（Linux で焼き直して 0 件相違）は受け入れ側でも再現した** —— pentala の `/tmp/opus5-i178/` へ
  `server` と `shared` を rsync して生成器を回し、**`render-engine-29/` の相違 0 件**（配備ツリーには触れていない）

### v2.11.18 — 一色と述べた記述は、一色で描かれる（Build 874、2026-08-10・ddl-engine 10）

**[I-173] 段 A。** `arrangement.color_cycle` は `cycle[i % len(cycle)]` の純粋な循環なので、
**n 色は必ず 1/n ずつの等分になる。記述が色をひとつしか述べていなくても、循環が 2 色なら
その色は半分の成員にしか付かない。** 配分を述べる場所を作る話（段 B）ではなく、
**記述が何も述べていないのに等分が押し付けられている**ほうを止めた。

- **coerce の出口に枝を 1 本置いた**（`without_unrequested_color_cycle`）。
  **背景句を除いた DDL が抽象色をちょうど 1 つ名指し**・**「色とりどり」句が無く**・
  **その命令の循環が長さ 2 以上でその色と別の色を含む**とき、**循環をその 1 色へ畳み `color` にも据える**。
  **名指し色を含まない循環は畳まない** —— それは希釈ではなく不達で、届ける層の担当である
- **出口は 2 つあるので共通の関数から両方に効かせた**（`coerce/__init__.py:88` の
  `INKU_COERCE_DISABLE` 経路と `:201` の本経路）。
  **記述に無い色を配ってよいという旗は無い**ので、hard ceiling と同じ理由でこの規則も通す
- **⚠ 循環は空にせず `[名指し色]` の 1 要素を残す** —— 発行時の契約は「描画は同じだから空にせよ」と
  書いていたが、**成り立たなかった**。`_apply_color_cycle` は `if not cycle: return items` で早期 return し、
  **その先の `color_hint` の作り直しごと飛ぶ**。保存済み Score の `color_hint` には過去の機械 note が
  残っており（実測 202 件中 186 件）、**空にすると note の中の色語が `color` を上書きして名指し色が消える**
  （面積計測で 4 作品が該当・1 件は 49.7% → 0.0%）。**1 要素は循環ではなく、`len(cycle) <= 1` で Score から読める**
- **Stage 2 のプロンプト日英に「1 色しか述べていない記述では循環を作らない」を足した**（発生源は上流で、
  希釈の 62.9% は Stage 2 が書いている）。**ここにゲートは置いていない** —— 出力が決定的でないので
  「落ちないこと」しか観測できず、**だから機構は coerce の出口に置いた**
- **`ddl_engine_version` 9 → 10。`ddl-engine-10/` を焼いた**（34 件・A 13 / B 21。`ddl-engine-9/` は 1 バイトも触っていない）。
  **⚠ 前版から動いたのは 21 件だが、`score` が動いたのは 8 件で、残る 13 件は `branch_report` に
  29 個目の枝の鍵が増えただけである。** golden も同じ形で、**差分は 40 件・`score` が動いたのは 13 件**
- **Android の Kotlin が複製している Stage 2 プロンプト 2 本を server から丸ごと写した**
  （`WebDdlSpec.kt`）。**段 2 が server 側の文言を変えた時点で、指紋を突き合わせる
  `PromptFingerprintTest` は必ず赤くなる** —— 指紋の正本 `prompts.json` は生成物で、
  server 側の検査が現行版との一致を要求するため、**古い指紋へ戻す道は無い**。
  **`android/VERSION` 2.1.4-android.21 → .22**（web の版数とは別の名前空間）
- **検査:** **server 2,661 → 2,679 passed / 31 skipped**（新規 18）・**ruff clean**（server / cli とも）・
  **cli 197 passed**・**`check_frozen_corpora.py` は darwin でバイト一致**・
  **Android JVM 263 件 失敗 0**・`check_docs.py` consistent
- **⚠ 受け入れで Android の退行を 1 件見つけた** —— 完了レポートは Android を回しておらず、
  **マージ直後の実測で `PromptFingerprintTest` が赤**だった（JA 44,193 対 44,589）。
  **`android/` を触らないと書いた契約でも、server の文言が動けば Kotlin の複製は必ず遅れる**
- **⚠ 摂動 3 本が空振りし、受入 3 本が恒真だったことを実装が自分で見つけて直した** ——
  T-3 の見本が色語を 1 つも名指していなかった（条件 1 が先に止め、条件 2 は一度も評価されない）・
  T-5 の書き込みが既存の道と必ず一致していた・T-6 の見本で `background` の引き算がたまたま一致していた

### v2.11.19 — 儀式なしでアプリが開く（Build 875、2026-08-10・単独利用モード）

- **`INKU_SINGLE_USER` を立てたサーバーは、利用者を 1 人に定めて自動的にログイン済みにする。**
  compose を起動してブラウザを開けば、そのまま書ける。**マルチユーザー機構は 1 行も消していない** ——
  `_session_token` が 401 を投げていた場所にだけ分岐を足しただけで、**返り値の型も依存の木も変えていない**。
  **ルート 82 本の guard は 1 本も動いていない。**
- **既定の所在を分けた。****コードの既定は off**（動いている配備が版を上げただけで無防備にならない）、
  **配布物の既定は on**（`compose.yaml` に `INKU_SINGLE_USER=1`）。
  あわせて `INKU_BOOTSTRAP_ADMIN_PASSWORD` の必須チェックを任意へ緩めた ——
  **単独モードでは bootstrap 管理者が要らないため。**
  **⚠ compose の補間では「単独モードが off のときだけ必須」を表現できないので、
  off にする運用者は自分でパスワードを設定する**（`deploy/.env.example` に条件を書いた）。
- **単独の利用者は、初回に「最も古い `admin`」で解決し、その結果を `app_settings` へ pin する。**
  **利用者行に列を足さない**（`app_settings` は `key` が主キーなので、単独の利用者は構造的に 1 人しか居ない）。
  **名前ではなく id で指す**ので、**改名しても pin は動かない**。**pin は DB の 1 行なので、
  バックアップと一緒に退避され一緒に戻る**。`admin` が 1 人も居ない DB では単独モードは効かず、401 のままである。
- **`/api/info` が単独モードかどうかを 1 つの鍵で返す**（`developer_mode` と同じ、環境変数由来のフラグ）。
  **API 表面の件数は endpoint / operation / schema とも 82 のまま動いていない** ——
  動いたのは digest と当該応答の中身だけで、**それを見るのは件数ではなく集合差の検査である。**
- **web は、押しても自動ログインで戻るだけになる導線（ログアウト）を隠す。**
  **パスワードの変更とユーザー管理は隠さない** —— **配布版の既定では利用者の password が
  誰も知らない値になるため、そこが単独運用から通常運用へ戻る唯一の退路である。**
  設定画面にその条件を 1 行で案内する（日英）。**ログイン画面には 1 行も触っていない**
  （自動ログインが効けば `/api/auth/me` が 200 を返すので、そもそも出ない）。
- **`inku-cli` は、トークンが無くても要求を送り、401 が返ってきたときだけ従来の案内へ戻す。**
  以前はクライアント側で止めていたので、**単独モードのサーバーへ 1 度も届いていなかった** ——
  単独モードかどうかを知っているのはサーバーだけである。単独モードでないサーバーに対する文言は変わらない。
- **検査:** **server 2,704 passed / 31 skipped**（新規 14）・**cli 199 passed**（新規 2）・
  **web `test:unit` 125**（新規 2）・**ruff clean**（server / cli とも）・
  **`npm run check` 0 errors / 2 warnings**（既存の a11y 2 件）・**`lint:i18n` 0 warnings / 0 errors**。
  **摂動 11 件で 24 本が赤くなり、摂動の当たっていない受入は 0 本である。**
- **⚠ 走行中に下りた裁定のうち 1 件が誤りで、実装が実測して覆した** ——
  「CLI の追従は要らない」は `/api/info` が公開ルートであることに引きずられた断定で、
  **認証の要るコマンドには当てはまらなかった。**
- **⚠ 発行側の見積もりが外れた点を 2 つ残す** —— 偽になる文書を契約は「日英 4 本」と見積もったが
  **実測 8 箇所**（compose の必須チェックを述べた 2 箇所と、仕様側の 2 箇所が抜けていた）。
  受入の本数も、発行側の 13 本・走行中の裁定込みで 16 本に対し、**実測 18 本**である。

### v2.11.20 — 記述が述べた数だけ描かれる（Build 876、2026-08-10・ddl-engine 11）

- **平文で述べた 1〜11 の個数が、その節に対応する群へ効くようになった。**
  それまで数を守っていたのは「三つ**だけ**」「only three」の道だけで、
  **「黒いペンの円を三つ並べる」のように普通に述べた数は、下流の推測に上書きされていた。**
- **新しい枝 `with_stated_count_fidelity` を 1 本立てた。**既存の「だけ」の道に混ぜていない ——
  **混ぜると「どちらが直したのか」が `branch_report` から永久に読めなくなる。**
  note も別の文言にしてあるので、**作品ごとにどちらが効いたかを後から数えられる。**
- **節と群の対応づけは 2 段で、曖昧なら触らない。**
  ① 節から作った `(図形, 色, 太さ)` の三つ組を持つ群がちょうど 1 つならそれ、
  ② 三つ組が当たらないときは同じ図形の群がちょうど 1 つならそれ、**③ 複数・0 なら何もしない。**
  **曖昧なまま押し付ければ数字は増えるが、それは別の群の数を壊して作った数である**（摂動で実測）。
- **雲形が図形として読まれるようになった。**`_primitive_from_clause` は雲形の語を持っておらず、
  既定の `line` に化けていた（**本番の雲形の節 15 件のうち 12 件**）。
  あわせて節から作る器に雲形の幾何（中心と大きさ）を足した ——
  **renderer は中心と大きさが揃ったときだけ雲形を描く**ので、
  読めるようにするだけでは「間違った形」が「形が無い」に変わるだけだった。
- **本番から凍らせた 214 件で測った** —— **144 件が緑**（改修前は 0 件）。
  **届かなかった 70 件は取りこぼしではなく拒否である** —— 47 件は対応する群が 2 つ以上あり、
  20 件は「その群を書き換えると別の述べられた数の答えが誰もいなくなる」、
  3 件は同じ群を先の節が既に答えている。
- **12 以上の帯・代表化・総量予算には 1 行も触っていない**（どちらを優先するかの裁定が済んでいないため）。
- **⚠ 契約の満点 158 に 14 件届かない。理由は 1 つで、満点は保存済み Score で測られているのに、
  枝は coerce の出口で走る**からである。途中の枝が色・素材・図形を動かすので、
  保存時に一意だった対応づけが、直す時点では一意でない。**枝が走る位置での上限は 147 で、そのうち 144 に届いている。**
- **摂動 11 件のうち 3 件が空振りし、3 件とも受入か実装を直した** ——
  **①「総量予算より後に置く」という順序の主張には判別力が無い**（予算は数を減らす方向にしか働かず、
  1〜11 は減らす対象にならない。900 本の隣に置いて前後を比べても出力が完全に同じだった）。
  主張を測った内容へ書き換えた。**② 「既に満たしている群は触らない」歯止めは効いているのに、
  それを検出する case が無かった**（外すと 214 件が 144 → 132 に落ちるのに pytest は全部緑）。
  判別力のある case を 1 本足した。**③「1 群は 1 節に答える」歯止めは到達不能だった**ので外した ——
  先の節が答えた時点でその群は「別の数の唯一の答え」になり、後の節へ渡らない。
- **参照コーパス `ddl-engine-11/`（A 13 / B 21）を焼いた。**Score は 1 件も動かず、
  `branch_report` に 30 個目の鍵が増えただけである。`coerce_golden.json` は枝が 1 本増えたので 40 件全部が動いた。
  **Android の参照 fixture も同じ周に焼いた**（新しい版のディレクトリが増えただけで、既存は 1 バイトも動かない）。
- **検査:** **server 2,730 passed / 31 skipped**（新規 26）・**cli 199 passed**・
  **Android JVM 263**（debug・failures 0）・**ruff clean**（server / cli とも）・
  **`npm run check` 0 errors / 2 warnings**（既存の a11y 2 件）・
  **凍結コーパスは darwin でバイト一致**・**`check_docs.py` 一貫**。
- **⚠ 残る事実を 1 つ:** `_primitive_from_clause` は節のどこにある図形語も読む（「焦点」の「点」を図形と読む）。
  **本改修は歯止めで回避しただけで、読み違いそのものは直していない。**

### v2.12.0 — 何ができるかはグループが決める（Build 877、2026-08-10・権限グループ）

- **`user_accounts.role` の 3 値フラグが、判定から 1 箇所残らず消えた。**
  何ができるかは **`admins` / `leaders` / `users` の 3 つの権限グループへの所属**で決まる。
  **1 ユーザーは複数の権限グループに属せる**（多対多）。**判定の入口は `has_permission_group` の 1 本**で、
  `deps.py` も `users.py` も `feedback.py` もこれを呼ぶ。**分岐を散らさない。**
- **`role` 列は削除していない。** 移行後は**所属から機械が導出した写し**として書き続ける
  （`admins` を含むなら `admin`、`leaders` を含むなら `group_lead`、それ以外は `user`）。
  **理由はバックアップからの復元である** —— 列を落とすと、本版より後に取った DB を本版より前の版へ戻したときに起動しない。
  **写しを人が書く場所は 1 つも無く、所属を書いた同じ手が更新する。**
  **写しが判定に使われていないことは、構造検査ではなく挙動で測っている** ——
  `role` に `admin` と書いてあるが所属は `users` だけのアカウントが、管理者ルートでも
  ユーザー管理ルートでも 403 になる。
- **起動時の移行は 1 対 1 で、冪等である。** `admin`→`admins` / `group_lead`→`leaders` / `user`→`users`。
  **`admin` を「管理者は同時にリーダーでもある」と読み替えない** ——
  読み替えると、移行が広げた所属と管理者が意図して広げた所属を、あとから誰も区別できなくなる。
- **組織グループ（`user_accounts.group_id`）は別の実体としてそのまま残る。** 1 ユーザー 1 つ。
  **権限グループと組織グループは独立して判定する** —— 同じ `circle_a` に属していても、
  `leaders` を持つ人はユーザー管理へ通り、`users` だけの人は通らない。組織グループを移しても権限は動かない。
- **リーダーが触ってよい相手を、写しでなく所属で判定するようにした。**
  旧実装は `role == "user"` で絞っていたので、**写しがまだ `user` と言っているアカウントを取り違えうる**。
  いまは「`admins` も `leaders` も持たない」で絞る。
- **API の 3 schema の鍵が変わった** —— `UserAccountItem` / `UserAccountCreateBody` /
  `UserAccountUpdateBody` から `role`・`role_label` が消え、`permission_groups`・
  `permission_group_labels` が入った。**ルートは 1 本も増えていない**（総数 82・公開 6 のまま）。
  **API 表面の件数は endpoint / operation / schema とも 82 のまま動かず、動いたのは digest だけである**
  （`fc1378ba…` → `cd4148a7…`）。**件数が動かないまま中身が落ちる型の退行は件数では捕まらない**ので、
  **3 schema の鍵の集合差と、残る 79 schema のハッシュ一致**を検査が表明する。
- **CLI の旗が変わった** —— `user create --role {user,group_lead,admin}` は
  **`--permission-group {users,leaders,admins}`（複数指定可）**になり、**`--role` は受け付けない。**
  `me` と `user list` は応答を丸ごと印字しているので、印字の追従に追加実装は要らなかった（実装 0 行）。
- **web は権限グループの選択を `<select>` 1 つからチェックボックス 3 つへ変えた**（複数所属のため。空選択は作れない）。
  タブの出し分けは `$lib/permissionGroups.ts` に集約し、**正規表現ではなく実行できる検査**を置いた。
- **検査:** **server 2,747 passed / 31 skipped**（枝の新規 17）・**cli 201 passed**（新規 2）・
  **web `test:unit` 127**（新規 2）・**ruff clean**（server / cli とも）・
  **`npm run check` 0 errors / 2 warnings**（既存の a11y 2 件）・**`lint:i18n` 0 warnings / 0 errors**・
  **凍結コーパスは darwin でバイト一致**・**`check_docs.py` 一貫**。
- **摂動 12 件で 18 本が赤くなった。契約の予想は 21 本で、3 本少ない。**
  **理由は穴ではなく判別力の集まり方である** —— 到達性の検査（`admins` / `leaders` / `users` が
  どこへ通るか）は、移行ではなく `add_user`（運用中に権限を与える経路）で被験者を作っている。
  そのため移行の写像を壊しても到達性は動かず、赤くなるのは写像を 1 対 1 で見る検査だけになる。
- **⚠ 最初に据えた冪等性の検査は、名指した失敗に対して 1 本も赤くならなかった** ——
  所属を二重に挿す非冪等化は `UniqueConstraint` が先に落とし、**テストが走る前に collection error になっていた**。
  **制約が捕まえられない側**（写しから再導出する非冪等化。写しは強いほうしか名指せないので、
  `admins`+`leaders` を持つアカウントが必ず 1 つ落ちる）を測るよう検査を伸ばした。
- **⚠ 完了レポートが挙げた「偽になる文書」に 2 箇所の漏れがあった** ——
  レポートは `group_lead` という値で引いており、**`admin` 役割という語だけで書かれた仕様の 2 箇所**
  （日英 1 対）が網に掛かっていなかった。**語で引く網は、その語を使っていない偽を見つけない。**

### v2.12.1 — 述べた数は、目で数えられる先でも守られる（Build 878、2026-08-11・ddl-engine 12）

- **平文で述べた数が効く帯を、1〜11 から literal の閾値まで広げた**（既定では 239 まで）。
  **境目に 2 つ目の名前を与えていない** —— 帯は `limits.literal_count_threshold - 1` から来る。
  **別の定数として 239 を書くと、片方だけが動いたときに誰も気づかない。**
  **閾値以上は代表化の領分で、この枝は 1 件も触らない。**
- **強制した数が命令ごとの上限（240）か作品全体の上限（400）を越えるときは、切り詰めずに強制しない。**
  **この枝は総量予算・命令ごとの予算より後に走るので、越えた分を誰も削らない。**
  切り詰めると**述べた数でも代表数でもない中途半端な数**が絵に出るので、
  **届かないなら手を出さないほうが記述に対して正直である。**
- **本番から凍らせた 1,346 作品で測った** —— 12〜239 の数を述べて外している **309 作品・341 件**のうち、
  **203 件が本当になった**（改修前は 4 件）。**枝が発火した作品は 30 → 197**、
  作品あたりの痕は p50 **15 → 36**・p90 **131 → 230**、**400 痕を越えた作品は 0 のまま**。
- **⚠ 上の歯止めは本番で 1 件効いている。契約が「0 件」と見積もったのは測り方の問題だった** ——
  見積もりは**出口の天井が削った後の合計**を数えており、削られた結果はいつでも 400 以下になる。
  **削られる前の合計は 419 だった。**その作品では**枝が発火したのに述べた数は 1 つも本当にならず、
  他の群まで天井に巻き添えで削られていた。**歯止めはその無駄な発火ごと止めている。
- **まだ届かない数の理由は帯ではなく対応づけである**（曖昧なら触らない・先の節が既に答えている・
  句が群を名指せない）。**前の契約の裁定どおりで、本改修では動かしていない。**
- **参照コーパスに、この改修が通る case を 2 件足した**（合成。本番の記述は写さない）——
  ① 12〜239 の数を述べて Stage 2 が外した Score ② 強制すると 400 痕を越える Score。
  **足す前のコーパス 21 件は帯を上げても 1 件も動かず、焼き直しても記録は改修を見なかった。**
  `ddl-engine-12/` は **36 件（A 13 / B 23）**で、`changed_from_previous` は**足した 2 件だけ**である。
- **golden は 41 件中 5 件が動いた**（`H-01` `H-07` `H-10` `H-13` `H-18`）。
  **`branch_report` の鍵は 30 のまま**（枝は増えていない）。**契約の予測と完全に一致した。**
- **検査:** **server 2,769 passed / 31 skipped**（枝の新規 22）・**cli 201 passed**・
  **web `npm run check` 0 errors / 2 warnings**（既存の a11y 2 件）・**`test:unit` 127**・
  **ruff clean**（server / cli とも）・**凍結コーパスは darwin でバイト一致**・**`check_docs.py` 一貫**・
  **Android JVM 263 / 0 failed**（`composer.py` を触っていないので指紋の検査も緑）。
  **帯の受入は 25 → 46 本**（`def` は 14 → 19）。**摂動 8 件はすべて命中し、空振りは 0 である。**
- **⚠ 摂動が 1 度空振りし、受入の欠陥を 1 つ見つけた** —— 「帯を閾値の先まで広げる」摂動が、
  **240 以上は動かないことを見る受入を赤くしなかった。**その受入は 300 と 500 を使っており、
  **どちらも命令ごとの上限を超えるので、帯とは無関係に上の歯止めが断っていた** ——
  **帯を検査していなかった。****240 を足して直した。**240 は命令ごとの上限ちょうどなので歯止めは断らず、
  **帯の縁だけが退ける唯一の値である。**
- **⚠ 版を上げた時点で、契約が名指していない検査が 7 本赤くなった**（`ddl_engine_version` の期待値を
  直書きしている 4 本・Android の参照 fixture の 2 本・コーパスの件数を数える 1 本）。
  **どれも本改修の欠陥ではなく版数の追随である。**Android の fixture を焼き直して動いたのは
  **新しい版のディレクトリの 2 ファイルだけ**で、既存の版も、engine が司らない 5 ファイルも 1 バイトも動いていない。

### v2.12.2 — 作品が、自分の招待客名簿を持つ（Build 879、2026-08-11・共有と可視範囲）

- **作品を 1 件ずつ、相手を選んで共有できるようにした。** 保存するのは
  **（作品・宛先の種類・宛先）の 3 つ組で 1 行**なので、**同じ相手に対して作品ごとに
  違う権限を与えられる**（実測: 同じ利用者へ一方は `read`・他方は `write` を与えると、
  星付けが 404 と 200 に分かれた）。ACL が持つのは **id だけで名前を 1 つも持たない**ので、
  **利用者名も組織名も、変えれば共有はそのまま追従する**（実測: 改名しても行は 1 バイトも動かない）。
  **⚠ アカウントを消して同名で作り直すと復活しない** —— id が変わり、削除の後始末で行も消えるためである。
- **誰に何が見えるかの判定を、可視述語 1 本へ集めた**（挙動は変えていない）。**移した先は 55 箇所**
  （ORM 49 ＋ 生 SQL 6）。**うち 2 箇所は全文検索の経路**で、ここが漏れると**現れ方は「見えすぎる」ではなく
  「検索したときだけ見えない」**になる —— **見えるようになる向きの摂動では捕まらないので、別に受入を置いた。**
- **権限グループが既定の見える範囲を決める。** `admins` は全部、`leaders` は自分の組織、
  `users` は自分の作品に、**共有されたものを足したぶん**。
  **⚠ `leaders` を持つ既存アカウントは、上げた瞬間から自組織の作品が見えるようになる。**
  それ以外の利用者には、**誰かが明示的に共有するまで何も変わらない。**
- **系譜が持ち主をまたぐ。** 読める他人の作品を親にでき、根を継ぐので、**1 つの群が 2 人にまたがり、
  見える件数が人によって違う。****読めない節点は、本文を伏せた札として出る** ——
  `deleted`（削除済み）と `not_permitted`（非公開）を**別の語で区別する**。
  **どちらも空の点線カードで描かれるので、札が無いと「もう無い」と「持ち主に頼めばよい」を見分けられない。**
  **⚠ 辺の可視は子に従う。その帰結として、親の所有者も派生を見られない** ——
  例外を作ると、親に従わせる案が退けられた理由がそのまま親の側で復活する。
- **共有された作品には、一覧で分かる印が付く**（`HistoryItem.shared`）。
  **真のときだけ応答に出るので、この鍵を知らないクライアントから見た応答は 1 バイトも変わらない。**
  印が無いと、**人が選んで消す画面に他人の作品が自分のものと同じ見た目で並ぶ。**
- **宛先は名前で選べる**（`GET /api/auth/me/group-peers`）。**返すのは `id` と `username` だけ**で、
  **自分と同じ組織グループの人に限る**（組織を持たない利用者には空を返す ——「組織なし」は組織ではない）。
  **全員の名簿 `/api/users` は開いていない**（一般利用者には今も 403。**対照の検査を 1 本置いた** ——
  これが無いと、名簿を全員へ開いた実装でも新しい検査は通ってしまう）。
- **単独利用モードを選び直せるようにした**（`GET/PUT /api/settings/single-user`）。
- **CLI**: `history share` / `history unshare` / `history acl` / `history peers` /
  `single-user show` / `single-user set` を足した。
- **既存 DB はそのまま開く。** スキーマの変更は **`history_acl` の 1 テーブル追加だけ**で、
  既存テーブルの列は 1 つも動かない。起動時の `create_all` が作るので**移行コマンドは要らない**。
  **実測**: 旧版で作った DB を新版で開くと `history` の内容ハッシュも既存系譜ノードも一致し、
  他 10 テーブルの件数も一致した。**旧版へ戻しても行は残る**（共有が無効になるだけで、上げ直せば復活する）。
- **⚠ API 表面の番人を、件数から宣言つきの検査へ格上げした。** 前の版の表面を
  `api-surface-before-the-guest-list.json` として凍結し、**足したもの（5 ルート・7 schema）と
  変えたもの（`HistoryItem` に `shared`）を名指しで宣言**したうえで、
  **残り 82 ルートと 81 schema は凍結ファイルと完全一致**を求める形にした。
  **前の周の番人は「79 のどれも動いていないか」と「その後 1 つも増えていないか」を同時に測っていた**ので、
  **1 つも動かさずに 3 つ足しただけで赤くなった。**名前で選んでから digest を比べる形へ直し、
  **名前が消えた場合も捕まる**ようにした（素の件数より強い）。
- **⚠ 途中で見つけた欠陥を 3 件直した。** ① `inku-cli refine save` は
  **`--kind` の 4 つの選択肢がどれも 422 で、一度も成功したことがなかった**（写像が
  `refine perform` にしか無かった）。② 上の表面番人。③ 作品削除の系譜掃除が、
  **ACL 経由で他人の作品を消したときに空振りする**（行の持ち主が操作者ではないため、
  **作品は消えるのに節点が tombstone にならない**）。
- **検査:** **server 2,826 passed / 31 skipped**（枝の新規 26。**+22 は main 側の ddl-engine 12 の周**）・
  **cli 217 passed**・**web `npm run check` 0 errors / 2 warnings**（既存の a11y 2 件）・
  **`test:unit` 132**・**`lint:i18n` 0 errors**・**ruff clean**（server / cli とも）。
  **決定的な描画層に触れていないので、参照コーパスは 1 バイトも動いていない。**
  **新規受入 53 本**（server 34 / cli 14 / web 5）に対し、**摂動は 34 回当てて 32 回赤。**
- **⚠ 空振りした摂動 2 件は、受入の穴ではなく実装が二重に守っていたためである** ——
  読めない節点の本文は**早期 return と、読める行しか入れないハイドレーション**の両方が止めており、
  **片方を外してももう片方が拾う。**両方を同時に外すと赤くなるので、受入に判別力はある。


### v2.12.3 — 一覧は、見せる分だけを求める（Build 880、2026-08-11・初回ロード）

- **初回ロードが 50 MB の先読みをやめた。** `/api/history` は、画面下の履歴ストリップが見せる件数（21）ではなく、
  **開いてもいない履歴マネージャの 1 ページ分（65）を要求していた**。
  要求を実際に見せる件数へ直した。**実測 52,945,665 → 23,524,802 バイト**（同じ機械・同じ窓 1909×1056 dpr 1）。
  **応答の合計は 2,675 → 1,997 ms、操作できるまでは 5,769 → 4,808 ms。**
- **履歴マネージャは、開いたときに自分の 1 ページを取りに行く。** 先読みは
  **マネージャを開かない周にも毎回払われていた**（本番ログ 24 時間 1,751 要求のうち、開いた形跡は 6 件）。
  **払う人を「開いた人」に限定した。**
- **⚠ 途中で見つけた欠陥を 3 件直した。** ① 検索の `$effect` がモーダルの開閉を読んでいるため、
  **開くと取得が 2 本走っていた**（105.9 MB）。② モーダルは画面に出てから自分の格子を実測して
  **65 → 52 の変更を送るので、さらに 1 本走っていた**（99.4 MB）。
  **飛んでいる 65 件の答えは 52 件の求めを満たす**という判定を入れて 1 本にした。
  ③ その重複を**要求番号を採ってから落としていた**ため、
  **落とした 2 本目が飛行中の本物を「古い要求」に格下げし、返ってきた 52 MB が捨てられていた** ——
  判定を採番より前へ出した。**⚠ この 3 つ目は「要求が何本出たか」を数える受入では捕まらない**
  （本数は 1 で正しい）。受入は**届いた作品が画面に出ること**に据え直した。
- **⚠ 契約が推定した「直後の long task が 1,055 → 約 470 ms」は外れた。** 実測は **1,497 → 1,349 ms（−10%）**。
  理由も測れている —— **DOM ノード（74,721）と `<path>`（6,845）が 1 つも動いていない。**
  ストリップが描くのは前も後も 21 枚で、**減ったのは捨てるだけの 44 件を JSON として解く手間だけ**である。
  **体感の改善は描画側ではなく、待ち時間（−678 ms）と操作可能まで（−961 ms）から来ている。**
- **1 ページの件数を決める場所が 2 つある**ことが実測で分かった（ページ側の見積もり 65・モーダルの実測 52）。
  **この版では上の判定が吸収するので追加の取得は起きない**が、事実は残るので台帳へ起票した。
- **検査:** **web `test:unit` 132 → 146**（削除・改名なし）・**`npm run check` 0 errors / 2 warnings**（既存の a11y 2 件）・
  **`lint:i18n` 0 errors**・**server 2,826 passed / 31 skipped**・**cli 217 passed**・**ruff clean**（server / cli とも）。
  **server を 1 バイトも変えていないので、参照コーパスは動いていない。**
  **摂動は 11 本当てて 11 本とも着弾**（契約の 6 本＋走行中の裁定と自分で足したゲートに 5 本）。
- **⚠ 摂動が 3 度空振りし、そのたびに受入か実装を直した。** うち 1 つは
  **`preloadMatches` が「控えの一致」と「手元の件数」の AND なので、
  件数の側が単独で偽を作っていて宣言を立てても結果が変わらない**場面を測っていた ——
  **宣言だけが結果を決める場面**に置き換えて 2 本とも赤にした。

### v2.12.4 — サムネイルは画像であって、絵そのものではない（Build 882、2026-08-11・初回ロード）

- **一覧が作品の絵の全文を運ぶのをやめ、焼いた画像を運ぶようになった。** `/api/history` は一覧の全件について
  SVG の全文を返していた（**実測 23,524,802 バイト・中央値 1,816 ms。うちサーバーの直列化が 1,184 ms**）。
  費用は作品の点数ではなく**文字数**で決まる —— 2,200 万文字を検査してエスケープし、1 本の JSON にする時間である。
  **作品は保存の後で 1 度だけラスタライズし、正本 DB の隣の派生専用 `thumbs.db` に置くようにした。**
- **⚠ 絵は 1 画素も変わらない。** 行うのは**保存済み SVG のラスタライズ**であって再レンダリングではない。
  **engine は動かさない** —— engine 2 で描かれた作品は engine 2 の絵のまま画像になる。
- **一覧は PNG を優先し、まだ焼けていない作品は今までどおり SVG で描く。** 6 枚以上並ぶ系統は
  **7 箇所すべてが同じコンポーネントを通る**ので、混在しても破綻しない。
- **`GET /api/history` に `include_svg` が増えた（既定 `true`）。** web の一覧取得だけが `false` を送る。
  **`false` でも `svg` の鍵は消えず、空文字で返る** —— 鍵ごと落とすと「絵を求めなかった」と
  「そこまで古いサーバー」が同じ形になるためである。**`inku-cli history` にも `--no-svg` を足した。**
- **サムネイルの再作成は運用の機能として実装した。** 管理画面から起動でき、進捗が見える。
  **作り直している間も古い画像を出し続ける**（空にしない）。**HiDPI は設定で ON / OFF**（既定 OFF）で、
  OFF に戻すときは確認したうえで 2 倍の行だけを消す。**`thumbs.db` を丸ごと捨てても正本は壊れない。**
- **1 ページに出す件数と、頁送りが進む幅を一致させた。** v2.12.3 の判定で取得は 1 本になったが、
  **表示した 65 件に対して頁送りは 52 ずつ進んでいたため、53〜65 番目が 2 ページに重複して出ていた**（実測 13 件）。
  **取得済みの作品を捨てずに、重なりも飛びも 0 にした。**
- **検査:** **server 2,826 → 2,844 passed / 31 skipped**・**cli 217 → 218 passed**・
  **web `test:unit` 146 → 158**（削除・改名なし）・**`npm run check` 0 errors / 2 warnings**（既存の a11y 2 件）・
  **`lint:i18n` 0 errors**・**ruff clean**（server / cli とも）・**`check_docs.py` 一致**・
  **凍結コーパスはバイト一致**（engine を動かしていないため）。**摂動は 17 本当てて 17 本とも着弾。**
- **⚠ 摂動が 2 本、契約の書き方では起こせなかった。** ① `svg_to_png` は width があれば height を無視するので
  **両方与えても縦横比は崩れない**（崩せる向きで当て直した）。② `svg` を dict から消しても
  **`response_model` が既定値で戻す**ので鍵は落ちない（直列化の層で落とす向きに当て直した）。
- **⚠ 受け入れで穴が 2 つ見つかり、その場で塞いだ。** ① **CLI を通る受入が 1 本も無かった** ——
  一覧の絵を読んで書き出す経路は、空文字を受け取ると **0 バイトの絵と白紙の PNG を書いて成功と報告する**。
  **旗を書かない送り手が既定に頼っていることを、両側から測る検査を足した。**
  ② **`--no-svg` が生成された help 節に反映されておらず**、マージ後の木で番人が赤くなった（再生成した）。
- **本番規模の数字は測っていない。** 手元 DB は 83 件・SVG 合計 350 KB で、**本番の 1 件あたり約 1 MB とは母集団が違う**。
  応答バイト・バックフィルの所要・`thumbs.db` の実容量は**配備してから測る**。

### v2.12.5 — サムネイルは 8 つの核で焼かれ、1 枚の失敗で止まらない（Build 883、2026-08-11・初回ロード）

- **サムネイルの焼き方を子プロセスへ移した。** v2.12.4 はスレッドの中で `resvg` をプロセス内で呼んでいたが、
  **`resvg` はラスタライズの間 GIL を手放さない**ので、**`workers` は待ち行列の形しか変えていなかった**。
  実測: 同じ 12 件が 1 / 2 / 6 スレッドで **10.08 / 10.36 / 11.49 秒**（6 本はむしろ遅い）。
  本番では**8 コアのうち 1 コアが 99.4%、残り 7 コアは 0〜1.2%** だった。
  **既知の「6 並列で約 8 倍」は 1 ファイル 1 子プロセスで焼く別の道の数字**で、それをこの道へ持ち込んでいた。
  **⚠ 子プロセスへ渡すのはラスタライズだけで、`thumbs.db` への書き込みは親が行う**（SQLite の書き手を 2 つにしない）。
  **⚠ `spawn` を明示した** —— 多スレッドのサーバーから `fork` を継がせない。
- **1 枚が焼けなくても、残りが焼かれるようになった。** v2.12.4 は 1 件の例外で**残り全部が実行されず**、
  しかも**「完了・失敗 0」と報告していた**。本番の実測は **2,917 件のうち 481 件で停止**し、
  状態には何も現れていなかった。**⚠ 一覧は焼けていない作品の絵を持たないので、そこは何も描かれない。**
- **途中で終わった実行が、そうと分かるようになった。** 再作成の状態に `ended_short` が増えた。
  **「実行中でない・失敗 0」は完走した実行の姿でもあるので、それだけでは区別できなかった。**
- **検査:** **server 2,844 → 2,847 passed / 31 skipped**（新設 3 本）・**cli 218 passed**・
  **ruff clean**（server / cli とも）・**凍結コーパスはバイト一致**・**web に差分なし**。
  **摂動は 3 本当てて 3 本とも 1 本ずつ着弾**（番人を外す→T-R1／印を書かない→T-R2／スレッドへ戻す→T-R3）。
- **⚠ どちらの欠陥も、v2.12.4 を配備して本番の 2,917 件を実際に焼いて初めて出た。**
  手元の 83 件では 1 件も止まらず、1 コアに寄っていることも見えなかった。

### v2.13.0 — 並列度は管理者が入れる（Build 884、2026-08-11・初回ロード）

- **⚠ 互換が切れる。** `POST /api/settings/thumbnails/rebuild` が**本文を取らなくなった**。
  **並列度は要求ごとに渡すものではなく、保存される設定になった** —— `PUT /api/settings/thumbnails` の
  `workers`（1〜16・既定 4）で管理者が入れ、再作成はそれを読む。**決める場所を 1 つにした。**
- **機械にコア数を訊かない。** `server` `shared` `cli` を通じてコア数を読む箇所は**もともと 0 件**で、
  そのままにした。**コンテナでは母機のコア数が答えとして間違っている**からである。
  **知っているのは入れる人であって、機械ではない。**
- **子が殺されても実行が最後まで進むようになった。** pool が壊れると**仕事を渡す側**も投げるが、
  そこだけ番人の外にあった。**メモリを絞ったコンテナで最初に起きる壊れ方がこれである。**
  いまは渡せなかった件数が `failed` に積まれ、実行はリストの終わりまで進む。
- **検査:** **server 2,847 → 2,850 passed / 31 skipped**（新設 3 本）・**cli 218 passed**・
  **ruff clean**・**凍結コーパスはバイト一致**・**web に差分なし**。**摂動 3 本が 1 本ずつ着弾。**
- **API 表面は 4 箇所動いた** —— 再作成の POST が本文を失い、`ThumbnailRebuildBody` が消え、
  `ThumbnailSettingsBody` と `ThumbnailStatus` がそれぞれ `workers` を得た。
- **⚠ 保存直後の焼き付けは今回も変えていない。** あちらはスレッドのままで、
  `INKU_THUMBNAIL_WORKERS` は所要時間を変えない。

### v2.13.1 — 数は 1 箇所で読まれ、マクロはその数だけ置かれる（Build 885、2026-08-11・描画改善）

- **プラグインを「三つ」と書けば三つ置かれる。** 展開層は数を読んでおらず、`Nature.青葉を三つ置く。` は
  1 つのまとまりを返していた。**プラグインが渡すのは 1 単位で、句に述べた数はその単位を何回置くか**である
  （**1 単位の内訳はプラグイン文書の宣言と seed が決める。本文は中へ手を入れない**）。
- **数の読み手が 1 箇所になった。** coerce にあった 12 個の定義を `server/src/inku_server/counts.py` へ移し、
  展開層は同じ語を読む。**片側だけ塞がる穴が作れなくなった。**
- **数は文でなく句で読む。** 本番の 7 件のうち **5 件が同じ文の別の句に別の数**（`一つ` / `一本`）を持っており、
  文で切ると全部 1 単位に潰れていた。区切りは読点である。
- **作品に入らない数は切り詰めずに断る。** 述べた数 × 1 単位が上限を越えるときは 1 単位のまま据え置き、
  `plugin_warnings` に 1 行残す。**中途半端な数を描くより、描かなかったと分かるほうを採る。**
- **英語の道が算用数字を読むようになった。** これまでは 32 語の名詞表にある語を要求しており、
  `Draw 12 circles.` が読めなかった。**表は捨てた。**
  **小数・分数・比・百分率の一部である数字は個数として読まない**（実測: 除外が無いと半径 `0.11` が `0` と `11` に読まれた）。
- **日本語の道は触っていない。** 助数詞なしの算用数字は今も読まない（真 8 対 偽 8 で、裁定が要る）。
- **`ddl_engine_version` 12 → 13。** **参照コーパスに C 部（プラグイン展開）を新設した** ——
  この層は最初から版数を名乗っていたが、**凍結出力を 1 度も持っていなかった**。
  A 部のプラグイン仕事は `ddl_expander` の `Nature.` マクロ正規表現で、文書プラグインの管理器を呼ぶのは
  render の経路だけである。**コーパスは 36 → 40 件（A 13 / B 23 / C 4）で、動いたのは新設の 4 件だけ。**
- **⚠ 2 つの裁定が交わるところに穴が残っている**（**未裁定・記録のみ**）——
  **英語の記述で CJK を含む名前のプラグインを算用数字で数えると、数が読まれず 1 単位になる**
  （`Place 12 Nature.青葉 marks.` は 1 単位・`twelve` なら 12 単位）。
  算用数字の近く 12 文字に CJK があれば日本語の道へ譲る規則が、参照名そのものに当たっている。
  **C 部の 4 件目がこの挙動を凍結してある**ので、裁定が出れば必ず case が動く。
- **検査:** **server 2,890 passed / 31 skipped**（枝の新設 39 本・受け入れ側で C 部の判別検査 1 本・喪失 0）・**cli 218 passed**・**ruff clean**・
  **凍結コーパスはバイト一致**・**Android JVM 0 failures**・**web に差分なし**。
  **摂動は 15 本中 14 本が着弾**（空振り 1 本 = P-15。当て先の選び方の問題で、
  段 3 の実装そのものを当てると T-13 は赤くなった）。

### v2.13.2 — 更新は、画廊ごと運ばない（Build 886、2026-08-11・初回ロード）

- **12 秒ごとの更新が、一覧を全件取り直すのをやめた。** これまでは変化の有無にかかわらず
  毎回すべての作品を運んでいた。**いまは「何が変わったか」だけを先に問い、変わっていなければ何もしない。**
- **`GET /api/history/state` が増えた。** 返すのは総数と最新作の時刻と id の 3 つだけで、
  **絵を 1 バイトも読まない**（手元 83 件で本体 89 バイト）。可視性の判定は一覧と同じ 3 つを同じ順で通す。
- **判定は、いま画面の帯が出している 3 つの量と見比べる。** 覚えた値と比べる形は、
  起動直後にかならず 1 回取りに行ってしまう。
- **答えられない帯は、自分を「古い」と申告する。** 帯の先頭が最新作だと言えるのは
  1 ページ目・絞り込みなしのときだけである。**それ以外では黙るのではなく取りに行く**ので、
  将来この条件を外しても「永久に変化なし」にはならない。
- **タブに戻ったときの更新も 5 秒の下限を通るようになった。** これまで `force` の経路だけが
  下限を素通りしており、タブを行き来するたびに全件を運んでいた。
- **実測（手元 83 件・約 90 秒の窓）**: 一覧の取得が **8 本 → 0 本**、
  API の転送量が **468,880 バイト → 2,723 バイト**（**176 分の 1**）。
  **メインスレッドの停止は手元では起点でも 0 件**で、この規模では差が出ない。
- **`inku-cli history state` が増えた。** `--bytes` を付けると、応答をそれが届いた実バイト数で包む。
- **API の面はルート総数 92 → 93。** 増えたのは **1 操作と 1 スキーマだけ**で、
  **既存の 92 操作・92 スキーマは 1 バイトも動いていない**（焼き直す前に差分を測って確かめた）。
- **本番規模の実測（配備後・作品 2,897 件）**: 新設の問い合わせは **91 バイト・TTFB 中央値 30.8 ms**、
  これまでの一覧は **163,008 バイト・TTFB 中央値 1,201 ms**。**1 回あたり 1,791 分の 1 のデータ量・39 分の 1 の待ち時間**で、
  しかも**変わっていなければその 1 回すら起きない**。**総数・最新の時刻・最新の id は一覧の先頭と 3 つとも一致した。**
- **検査:** **server 2,898 passed / 31 skipped**（枝の新設 8 本・main 側 40 本・喪失 0）・
  **web 173 passed**・**cli 219 passed**・**ruff clean**・`npm run check` **0 errors / 2 warnings**（既存の a11y 2 件）・
  **i18n 0 errors**・**公開文書の整合 OK**。**摂動は 15 本すべて着弾**
  （**うち 2 本は、受入の側に判別力が無いことが分かって受入を直してから当たった**）。
- **決定的な層には 1 行も触れていない**ので、凍結コーパスの焼き直しは起きていない。

### v2.13.3 — エディタが、実在しないプラグイン名を書いている最中に教える（Build 887、2026-08-11・[I-207] 裁定 C）

- **`Nature.菖蒲` のように実在しない正式名を書くと、その文が丸ごと消えていた。** 展開層が `Nature.` を剥がすとき、
  その文を警告付きで取り除くためである。**警告は記録に残るのに、書き手には何も届いていなかった**
  （`plugin_warnings` を読む場所が web にも cli にも **0 箇所**だった）。
- **書いている最中に、エディタが示すようになった。** 登録されていない正式名は別の色になり、
  エディタの下に理由が並ぶ。**赤は使わない** —— プラグインは後から足せるので、いま無い名前が明日は有効になりうる。
  実態は「間違い」ではなく「このサーバーにはまだ無い」である。
- **発火語なら、外し方を案内する。** `Nature.菖蒲` は「「Nature.」を外すと「下草」として効きます」。
  `菖蒲` は `Nature.下草` を発火させる語で、**不正なのは正式名のほう**である。
  発火語でもなければ「この名前は登録されていません」。
- **描いた後にも残る。** `plugin_warnings` を解釈の直下に出す。**消えた後にしか分からないので、
  書いている最中の表示の代わりにはならない。両方置くのが裁定 C である。**
- **一覧 API が `fires_on_ja` / `fires_on_en` を返すようになった。** ⚠ エディタが実際に読んでいるのは
  `GET /api/plugins` ではなく **`GET /api/saijiki`** だったので、両方に載せた。
  **既存の鍵は 1 つも消していない・改名していない。**
- **⚠ 有効な正式名の見た目も変わった。** `Nature.青葉` はこれまで `青` だけが色カタログの色で塗られていたが、
  **正式名を 1 語として拾う**ので、いまは 1 つのプラグイン語として示される。
- **ドットの付かない語は今までどおり。** `菖蒲` とだけ書いた場合は普通の語として扱う。
- **API の面は動いていない。** ルート総数は **93 のまま**、`api-surface-baseline.json` の差分は **0 行**
  （`/api/plugins` の 200 応答は `additionalProperties` を持つ総称 object なので、entry の鍵はもともと面に載らない）。
  **したがってこの追加を baseline で測ると恒真になる**ので、受入は実応答の鍵を数える形に据えた。
- **web のテストがアプリのモジュールを呼べるようになった。** 拡張子なしの相対 import を解決するフックを足し、
  `test:unit` をそれ経由にした。**Node 自身の解決が失敗した後にだけ発火する**（受け入れ側で、
  存在しないモジュールを import すると赤くなることを摂動で確かめた）。
- **検査:** **server 2,903 passed / 31 skipped**（枝の新設 5 本・喪失 0）・**web 188 passed**（枝の新設 15 本）・
  **cli 219 passed**・**ruff clean**・`npm run check` **0 errors / 2 warnings**（既存の a11y 2 件）・
  **i18n 0 errors**・**公開文書の整合 OK**。**摂動 11 本のうち 10 本が着弾**し、
  **空振りした 1 本の継ぎ目には点呼を足した**（web が読む鍵名を、サーバーの実応答と突き合わせる検査）。
- **決定的な層には 1 行も触れていない**（`document_format.py` は 1 バイトも動いていない）ので、
  凍結コーパスの焼き直しは起きていない。**これは「教える」改修であって「直す」改修ではない。**

### v2.13.4 — 作品が一枚ものになって出ていく（共有カード）（Build 888、2026-08-11）

- **1 作品を 1 枚のカードとして書き出せるようになった。** 絵・詞書・render seed の下 4 桁・刻印を 1 枚に組み、
  **正方形（1080×1080）と縦長（1080×1350）の 2 版面**を選べる。履歴管理から書き出し、版面と刻印は設定で決める。
  **詞書が空の作品は絵だけのカードになり**、render seed を持たない作品では seed の行が出ない。
- **⚠ これまでリポジトリにフォントが 1 本も無かった。** ラスタライザは焼く機械にあるフォントを使っていたので、
  **同じ作品が機械ごとに違う文字で出ていた**。組版をサーバーの仕事にするなら、字が同梱されていなければならない。
  **Noto Serif JP（可変・12.95 MB・SIL OFL 1.1）を `server/src/inku_server/fonts/` に置いた** ——
  凍結 DDL コーパスとリポジトリの日本語散文から採った需要 1,233 字を 1 字も欠かさず、cmap は 16,726 字。
  **⚠ 詞書は利用者の自由入力なので、この 1,233 字は下限であって上限ではない。**
- **⚠ resvg は family 名が正本と一致しないと 1 画素も描かず、例外も出さない。** 可変フォントの
  name ID 1 は `Noto Serif JP ExtraLight`、**name ID 16 が `Noto Serif JP`** で、前者を書くと何も出ない。
  **綴りが同梱ファイルの答える名前と一致することを、専用の検査 1 本が測っている**
  （誤りは「空白のカード」としてしか現れないため）。
- **`svg_to_png()` に `skip_system_fonts` と `font_files` を足した。既定は従来のまま**なので、
  **既存の 6 か所の呼び出し**（アニメーション 2・奥書・自動推敲・API の描画・サムネイル）は今日と同じ画素を出す。
- **絵は画素として貼らず SVG のまま入れ子にする**ので、カードは最後の 1 回のラスタライズまでベクターで残る。
  **詞書が長いと絵の枠が縮み、6 行を超えた分は `…` で切る**（切らないと絵が版面から追い出される）。
- **CLI から `inku-cli export-card`**（`--out` `--layout` `--no-seal`）。**⚠ 存在しないディレクトリを
  `cards/` の形で渡すと `cards` という名の PNG ができ、2 枚目が 1 枚目を上書きしていた**
  （実装セッションが自分で見つけて直した。末尾の区切り文字も見るようにした）。
- **API は `POST /api/history/export-card` の 1 本**。身元確認を通り、**他人の作品 id では 404**。
  **公開ルートは 6 本のまま。**
- **⚠⚠ 同じ周に 2 本の枝が、同じ番人へ同じ数を書いていた。** 契約 3（12 秒ポーリング）とカードが
  どちらも「起点 92 ＋ 自分の 1 本」で **93** と書いており、**どちらを採っても間違い**だった。
  **正しい値は 94 で、受け入れでは両方を足した** —— `EXPECTED_ROUTE_COUNT` と api-surface の 3 つの件数、
  および凍結ファイルを持つ番人 2 本の許可一覧（**片方は衝突しないのに赤くなる**。
  凍結ファイルが起点の 92 本なので、増えた 2 本のうち 1 本が「宣言されていない追加」に見える）。
- **焼き直す前に集合差で測った** —— マージ後に増えたのは **operation 2 本（`POST /api/history/export-card`・
  `GET /api/history/state`）と schema 2 本**だけで、**先行する 92 の operation と 92 の schema は
  1 バイトも動いていない**（消えた operation 0 本・中身が動いた既存 0 本）。
- **検査:** **server 2,923 passed / 31 skipped**（**枝の新設 20 本**＝関数 19 本・うち版面の 1 本が 2 例に分かれる。**喪失 0**）・**web 191 passed**（枝の新設 3 本。残りは main 側の 2 契約が先に入れたもの）・
  **cli 220 passed**（**+1 は枝のもの** —— `export-card` を足したので、パーサの help を測る検査に
  `[export-card]` の 1 例が増えた）・**ruff clean**（server / cli）・`npm run check` **0 errors / 2 warnings**
  （既存の a11y 2 件）・**i18n 0 errors**（英語 1,046 文字列）。
  **摂動は実装セッションが 11 本を当て、契約の予想 15 件に対して 23 件が赤くなった**
  （**予想と食い違った 3 本は理由が測ってある**）。
- **決定的な層には 1 行も触れていない**（`renderer.py` は 1 バイトも動いていない）ので、
  凍結コーパスの焼き直しは起きていない。

### v2.13.5 — 共有カードがキャンバスからも出る（Build 890、2026-08-11）

- **キャンバス下のツールバーに `共有カード` を足した。** PNG の右にあり、**押すとすぐ表示中の作品のカードを 1 枚書き出す**。
  チェックも選択も要らない。版面と刻印は履歴管理から出すときと同じ設定に従う。
- **ボタンの語を `カード` から `共有カード` へ変えた**（日英とも。英語は `Card` → `Share card`）。
  **鍵は同じ 1 つ**（`historyCardExport`）なので、履歴管理側のボタンも同時に変わる。
- **まだ保存されていない作品にはカードがない。** `history_id` は省略可で `displayedHistoryItem` も
  null になりうるため、**キャンバス側のボタンは `!result` ではなく `currentHistoryId` でも無効になる**
  （id が無いまま要求が出ることがない）。
- **キャンバスのツールバー（`status-bar`）は可視性の 7 群のどれにも包まれていないので、
  このボタンは簡易 UI でも出る。**（履歴管理側のカードは従来どおり `history` 群の中にある）
- **検査:** **web 196 passed / 0 failed**（**新設 5 本**・既存 191 は 1 本も減っていない）・
  **`npm run check` 0 errors / 2 warnings**（既存の a11y 2 件）・
  **i18n 0 warnings / 0 errors**（英語 **1,047** 文字列・例外 47。起点は 1,046）・
  **server 2,923 passed / 31 skipped**・**cli 220 passed**・**ruff clean**。
- **摂動 5 本を当て、5 本とも予測どおり 1 本ずつ赤くした**（空振り 0）——
  ラベルを戻す／ボタンを PNG の左へ移す／`disabled` から `currentHistoryId` を落とす／
  受け渡しだけ消す（関数は残す）／型から鍵を落とす。
  **「置いただけで通り道が無い」を捕まえる摂動は 4 本目である。**

### v2.13.6 — どのキャンバスでも、痕は記述が述べた形を保つ（Build 891、2026-08-11・[I-135] 裁定ア・render engine 30）

- **痕の寸法を、キャンバスの短辺で画素へ直すようにした。** engine 29 は `size` を `canvas.width` と
  `canvas.height` で**別々に**伸ばしていたので、**同じ記述が縦横比ごとに違う形を描いていた** ——
  `size [0.3, 0.3]` と書いた正方形は黄金比で 1.61:1・柱絵で 0.20:1、
  **`size [0.4, 0.2]`（横長 2:1）と書いた楕円は柱絵で 0.40（縦長）と、記述の逆**になっていた。
- **`renderer.py` の 12 箇所を `_size_px` という 1 つの口へ通した。**
  **置き場所（`_px`）は 1 バイトも触っていない** —— 座標は従来どおり幅と高さに比例するので、
  **縦横比は「痕がどこに座るか」を決め続け、「痕がどんな形か」は決めなくなった。**
- **正方形は 1 バイトも動かない**（`unit == width == height` で新旧が同じ算術）。**焼き直しで動いたのは 3 件ちょうど**
  （`D-canvas-{pillar,vertical,wide}-filled-square-rotring`）。
- **コーパスに 4 件足して 553 件にした** —— **狭いキャンバスに横長の痕を描く case が 1 件も無く**、
  広げられた痕と保たれた痕を見分けられなかった。足した `D-canvas-pillar-ellipse-pen` は
  **起点の実装で 0.32（縦長）・engine 30 で 1.59（横長）**、`D-canvas-square-ellipse-pen` は**両方で 1.59**（対照）。
- **検査:** **server 2,941 passed / 31 skipped**（枝の新設 18 本＋カード契約の main 側 20 本）・
  **web 196 passed**・**cli 220 passed**・**ruff clean**・`check_docs.py` 一致。
  **摂動は実装セッションが 5 本を当て、P-1 は 12 箇所すべてで赤くなった**（契約の予想は 4 箇所）。
  **⚠ 契約の P-2 の予想は成り立たなかった** —— 長辺基準でも向きは保たれるので T-2 では捕まらず、
  **捕まえるのは「痕が紙に載る」ことだった**（実装が検査を 1 本足して塞いだ）。
- **Android の参照 fixture（`render-engine-30/` の 64 ファイル）は受け入れ側で焼いた。**
  **Kotlin の renderer は同じ配線をまだ持っている**（[I-217]・別契約）。

### v2.13.7 — ナビゲーションボタンが、どちらが新しい方かで一致する（Build 892、2026-08-11・[I-135] とは別系統の UI 契約）

- **移動の語を「新しい・古い」へ一本化した。** キャンバスは「次＝新しい方」、モーダルは「前＝新しい方」と
  **正反対の語を使っていた**（`ja.ts` の同じファイルの中で食い違っていた）。
  **`前` `次` `最初` は画面から消え、`←新しい` `古い→` `最新` `最古` になった**（日英とも）。
- **提示モードの `aria-label` がツールチップと逆だったのを直した。**
  `‹`（新しい方へ動く）に「古い1件 →」を読み上げていた。**同じ鍵を使う形にしたので二度と食い違わない。**
- **選択が外れているとキャンバスの移動 3 つが全部無効になっていたのを直した。**
  Stage 1 のモデル切替・色カタログの選び直し・系譜の切り離しなど**6 経路**が、
  作品を表示したまま選択だけを外していた。`-1` を「最新の 1 つ手前」として扱う。
- **「最新」を作品単位へ揃えた。** ストリップとモーダルはページ単位で判定していたので、
  同じ画面でキャンバスの「最新」だけが有効という状態が起きていた。
- **1 件送りと 1 ページ送りの着地を揃えた**（ページ 2 の 22 件目から「← 新しい」で 21 件目へ。従来は 1 件目へ飛んでいた）。
- **窓幅を変えたときに送りが重複・飛ばしを起こしていたのを直した**（新しい格子へ `offset` を座り直させる）。
- **取得の追い越しを塞いだ** —— `fetchHistoryOffset` と `fetchTrashPage` だけが連番も `AbortController` も
  持っていなかった。**取得中は移動ボタンを押させない。**
- **デモ実行中はキャンバスの移動も止める**（従来はストリップだけが止まっていた）。
- **判定を `web/src/lib/historyNavigation.ts` へ出した** —— キャンバス・ストリップ・モーダルが同じ関数を読む。
- **検査:** **web 225 passed / 0 failed**（**新設 29 本**・既存 196 は 1 本も減っていない）・
  **`npm run check` 0 errors / 2 warnings**（既存の a11y 2 件）・**i18n 0 warnings / 0 errors**（英語 1,048 文字列）・
  **server 2,941 passed / 31 skipped**・**cli 220 passed**・**ruff clean**。
  **摂動 28 本・空振り 0。**⚠ 契約の予想と食い違った 4 件は、いずれも**予想より多く赤くなった**側である
  （実装が食い違いの側を報告した）。
- **⚠ 実装セッションが 17 個のボタンすべてを実機の画面で押している**（履歴 2,924 件の pentala）。

### 2026-08-12 — 設計原則 4 が、否定しているものを言い当てる（**採番なし**・文書のみ）

**原則 4 の日本語の題「キャンバスを持たない」は、器そのものを否定しているように読めた。**
否定しているのは**固定した大きさ**であって、器でも縦横比の種類でもない（2026-08-11 作者裁定）。
**題を「固定した大きさを持たない」に改め、本文から `0.0〜1.0` という数を外した** ——
原則が述べているのは「絶対寸法を持たない」ことであり、比の刻みは原則の要件ではない。
**縦横比についても「固定しない」とだけ述べ、具体の比を 1 つも挙げていない** ——
**「比は持つ」という言い方は 1:1 が固定であるような含みを生む**ため、作者が退けた（同裁定）。
**縦横比は作品世界を規定する制約であって、記述が持つ寸法ではない**、と 1 文で述べている。
**直したのは 4 箇所**（`SPEC.ja.md` の設計原則・`README.ja.md` の原則の一覧と、それぞれの英語の相方）。
**英語の `SPEC.md` は "not fixed pixels" と元から意図どおりだったが、日本語と同じ 2 文を持つように揃えた。**
**`README.md` の "No fixed canvas" は日本語の旧題の直訳だったので、対にして改めた。**
**コードは 1 行も変えていない。**`check_docs.py` は一致。

### v2.13.8 — 痕の並びも、どのキャンバスでも形を保つ（Build 893、2026-08-12・[I-135] 裁定 R2 + R3・render engine 31）

- **環の半径と領域の広がりを、キャンバスの短辺で画素へ直すようにした。** engine 30 は**痕そのもの**の寸法を
  短辺基準にしたが、**その痕を並べている層はまだ比で伸び縮みしていた** ——
  `radial` の環は横へ `canvas.width`・下へ `canvas.height` で画素になるので、
  **柱絵（1:5）では環の縦横比が 0.19** になり、**丸い点が潰れた環の上に並ぶ**状態だった。
  `at.region` も同じで、**正方形の箱として述べた領域が比のぶんだけ縦長・横長になっていた。**
- **`at.region` の中心は動かしていない**（作者裁定 2026-08-12）。**「右上」はどの比でも右上**であり、
  置き場所は従来どおり幅と高さに比例する。**動かしたのは広がり（半幅・半高）だけである。**
- **`arrangement.margin` は今日どおりに残した**（同裁定で R1 は不採用）。
  **`scatter` / `horizontal` / `vertical` が枠いっぱいに広がることは、その layout の意味である。**
- **領域を読む箇所は 2 つあり、1 つのヘルパを両方から通した** —— `_resolve_at_region`（すべての region 命令が
  通る錨）と、grid 枝が自分で読む箇所。**片方だけ直すと、もう片方の検査が緑のまま残る。**
- **正方形のキャンバスは 1 バイトも動かない。** ただし**中心 ± 半幅の往復は倍率 1.0 でも浮動小数で元に戻らず**
  （region `[0.6, 0.18, 0.82, 0.4]` で `y0` が **2.78e-17** 動く）、丸めの境界をまたいで対照が動いた。
  **倍率が 1.0 なら領域をそのまま返す短絡**を置いて塞いだ。
- **コーパスに 16 件足して 569 件にした** —— **553 件のうち `radial` を持つ 5 件は全部が正方形で、
  `at.region` を持つ case は 1 件も無かった**。**つまり、この改修は足す前のコーパスには 1 バイトも残らなかった。**
  足したのは 4 題（環・単体の領域・領域の grid・領域が錨にしかならない群）× 4 比で、
  **非正方形 12 件はすべて動き、正方形 4 件はすべて動かない**（判別力は両側の木で実測）。
  **既存 553 件は 1 件も動いていない。**
- **本番での届き方**（生存 2,939 件・2026-08-12 実測）: **非正方形は 515 件（17.5%）**、
  そのうち **`radial` を持つ 63 件（12.2%）に R2 が効き**、**`at.region` を持つ 113 件（21.9%）に R3 が効く**。
  `at.region` を持つ命令 181 本のうち、**箱が痕を閉じ込めるのは単体の 88 本と grid** で、
  残りは錨だけが動く（見た目には出ない）。
- **検査:** **server 2,978 passed / 31 skipped**（**新設 37 本**・`test_arrangement_aspect.py`）・
  **cli 220 passed**・**ruff clean**・**Android JVM 530 tests / 0 failures**（98 suite）。
  **摂動 7 本・空振り 0**（契約の 6 本に、実装が版を戻す 1 本を足した）。
- **⚠ 実装セッションが「最初に書いた T-7 は恒真だった」と自分で報告している** —— 1 つの Score・seed 1 通りでは
  **2.78e-17 が丸めの境界をまたがず、摂動を当てても緑のままだった**。
  **題ごとに Score を分け seed を 2 通りにして 8 件中 5 件が赤くなり、算術層の検査と 2 層で据え直した。**
- **⚠ 契約が数え落としていた赤が 1 本あった** —— 件数を直書きしている番人（`553` / `D: 32`）は
  **段 5（case を足す）から赤くなる**もので、契約は case を足す前に赤を数えていた。**569 / `D: 48` へ直した。**
- **Android の参照 fixture（`render-engine-31/` の 64 ファイル）は受け入れ側で焼いた。**
  **Kotlin の renderer は同じ異方性をまだ持っている**（[I-217]・別契約）。
  **⚠ `gen_android_reference.py` の 3 箇所は `canvas` を渡さずに演奏を解決している**ので、
  **[I-217] で移植する日には、そこへ canvas を渡さないと Android の期待値が engine 30 以前を主張し続ける。**

### v2.13.9 — どのモードでも履歴が使え、作品はそこから一枚ものになって出ていく（Build 894、2026-08-12・契約 1 本 ＋ 対話による UI 改善 10 件）

- **簡易 UI が履歴を持つようになった。** これまでの簡易 UI は**描いて、見て、失うだけの画面**だった ——
  **作品を一枚もの（共有カード）として持ち出す口は、履歴側とキャンバス側の 2 つとも履歴群に属していた**ので、
  簡易 UI ではどちらも閉じていた。**`SIMPLE_UI_VISIBILITY.history` を true にし、キャンバスのツールバーを
  どのモードでも残した**（簡易では `共有カード` だけが残る）。**モードが変えるのは表示層だけ**という原則は変えていない。
- **ごみ箱の件数を 1 つの出どころから読むようにした**（[I-218]）。同じ量を 2 つの prop が運んでいたのをやめ、
  **`$effect` の依存も同じ 1 つへ向け直した** —— 依存ごと消すと、ごみ箱への出し入れで系譜が取り直されなくなる。
- **死に経路を消した**（[I-206]）。`preloadHistoryManagerFirstPage` と `preloadFirstPage` は呼ばれない経路だった。
- **1 ページ件数の正本を決めた**（[I-205]）。**格子を実測する `calculatePageSize` が正本**で、
  ページ側は**モーダルが開く前の初回取得の予測値**にすぎない。**両者の定数を CSS（`minmax(142px, 1fr)`）に揃え**、
  受入は「2 つの数の一致」ではなく**「式が CSS と一致していること」**に据えた。
- **系譜の起点の英語を `Root` から `Origin` にした**（[I-179]）。日本語の `起点` は動かしていない。
- **層の版の呼び方を画面のどこでも揃えた**（`Render engine version` / `DDL version` / `DDL engine version`）。
  **情報モーダルに 3 つの版を出すようにした。**
- **まとめ描きの最中は、入力欄が「いま描いている 1 行」に変わり、空いた場所に写生が出る。**
  観測ブロックは**本文の何行目から来たか**を名乗る。
- **4 案生成の進捗を「並列に走っている 4 本」の形で見せる**（候補ごとのレーン。マスコットは未選択の側で、
  位相は乱数で散らす）。
- **履歴帯に `最古` ボタンと `推敲のみ` の絞り込みを足した。** 押下状態の配色は履歴モーダルと同じ
  `--action-bg` / `--action-fg` を読む。
- **表示中の作品をモーダルから削除したとき、キャンバスが履歴帯の座り直しに追随する。**
- **ハッシュの方式（`rh3:` など）は値の属性であって値の一部ではない、という規則に統一した。**
  判定を `web/src/lib/hashIdentity.ts` に集約し、**コピーは digest だけを渡す**。
  **⚠ 保存形式は `<scheme>:<digest>` のままで、server も DB も 1 バイトも変わっていない** ——
  変わったのはコピーが渡す値で、**従来の接頭辞つきの値はアプリのどこにも一致しなかった**（検索は末尾 4 文字）。
- **検査:** **web 245 passed / 0 failed**（新設 20 本・既存 1 本は書き直しで本数不変）・
  **`npm run check` 253 FILES / 0 ERRORS / 2 WARNINGS**（既存の a11y 2 件）・
  **`lint:i18n` 1,049 strings / 0 warnings / 0 errors**（新設 1 語）・
  **server 2,978 passed / 31 skipped**・**cli 220 passed**・**ruff clean**。
  **摂動 11 本・空振り 0。**⚠ **予測 12 件に対し実測 16 件で、食い違った 4 本はいずれも「予測より多く赤い」側である。**
  **さらに前契約が置いたメタゲート（他の全テストを子プロセスで回すもの）が全摂動で道連れになるので、総数は 27 件になる。**
- **⚠ 契約の範囲は 1 commit 目までで、残る 10 件は 2026-08-12 の作者裁定によるオーバーライド**（対話で 1 件ずつ
  指示 → 実装 → 画面確認 → commit）。**すべて web で閉じており、server の改訂を要する項目は 1 件も無かった。**

### v2.13.10 — どの読み手も同じ数え方をする（Build 895、2026-08-12・[I-212]〜[I-216] 裁定・ddl engine 14）

- **個数を読む口は複数あるが、読み方が食い違っていた。** 5 つの裁定を 1 本の契約で入れた。
- **記述の言語が判定を決めるようにした**（[I-212] [I-216]）。数字の近くに CJK があると個数を落とす除外は、
  **本文が日本語のときだけ**効く。**英語の本文に書いた `12` は、隣にプラグイン語の漢字があっても 12 になった** ——
  それ以前は落ちて 1 単位だった。**言語は coerce の呼び手 5 つすべてが渡す**（点呼を受入に置いた）。
  **⚠ 契約が「ここに言語がある」と書いた 1 つは現物に無く**、実装が同じ解決器で解いて報告している。
- **参照の句が個数を述べていないときだけ、その文まで広げて読む**（[I-215]）。句が述べていれば文は上書きしない。
- **参照の句の中の助数詞なしの算用数字を個数として読む**（[I-213]。`緑のNature.下草を50散らす。` は 50）。
- **英語の道と日本語の道を 1 つの走査に載せた**（[I-214]）。英語側だけが持っていた
  **12 語の名詞表と独自の歩きは消えた**。句の切り方も日英で揃えた。
- **⚠ 走行中に作者裁定が 2 件出た** ——
  **① 軸を名指す語の除外**（方向・向き・種類・層・行・列・度・回・倍・割と英語の相当語）。
  名詞表を捨てたら `four directions` の 4 が本数として読まれ、**一面を埋める 400 マークの格子が 4 マークに落ちた**。
  **② 索引の除外**（`member 2` の 2 は「どれか」であって「いくつか」ではない）。
  展開層が自分で書いた DDL の member 番号から群を作っていた。**受入を 2 本足して満点は 22 → 24 本になった。**
- **Android を server に合わせた**（[I-214]）。`ServerScoreSemantics.countHintFromDdl` は委譲 2 行になり、
  **手書きの漢数字 21 語の表は消えた**。**旧実装は上限も除外も 1 つも持たず**、`radius 0.11` に 0、
  30 度回転に 30 を返していた。**期待値は手書きせず、server の実測から生成している。**
- **参照コーパスを `ddl-engine-14` として焼いた（42 件）。** 増えた 2 件は
  **句の外に述べた個数（20 単位）**と**句の中の裸の算用数字（50 単位）**で、**判定が動いたのは 1 件**
  （`C-plugin-count-as-a-numeral-beside-cjk` が 1 → 12 単位）。
  **⚠ 焼き直しの差分 3 件のうち 2 件は「新しいファイル」であって「動いた case」ではない。**
- **検査:** **server 2,997 passed / 31 skipped**（新設 19 本 ＋ 受け入れ側が足した 1 本）・**cli 220 passed**・
  **Android JVM 532 tests / 0 failures**・**ruff clean**・**凍結コーパスはバイト一致**・`check_docs.py` 一致。
  **摂動 14 本・実測 27 件**（契約の予測は 12 本 14 件）。**空振りは 1 度あり、実装が受入の穴として直した** ——
  T-12 の入力は句と文が同一で、「常に文で読む」に変えても答えが動かなかった。
- **⚠ 受け入れ側で 3 つ直した** —— **① 版数の直書き 3 箇所**（`test_ddl_reference.py` の 2 つと `test_api.py` の 4 行）、
  **② 判別力を失ったゲート 1 本**。「`twelve` と `12` の 2 case は digest が違わねばならない」は、
  **裁定が成立した結果 2 つが同じ意味になったので恒真の逆になった** —— **等しいことを測る形へ裏返した**
  （除外を英語の本文へ戻すと再び割れて赤くなる）。**③ 2 つの裁定の交点に受入が無かった** ——
  句の外の裸の数字を文まで広げて読む入力は 24 本のどこにも無かったので、**T-25 を足した**
  （摂動 2 本で赤くなることを確認）。

### 2026-08-12 — 面がどう在るかを言う語を、実装より先に仕様へ置いた（**採番なし**・文書のみ）

**歳時記に `おもて`（英語 `surfaces`）カテゴリを定めた** —— 空・べた・薄墨・粒・点・平行線・交差線・にじみ・
アクアチント・濃い・薄いの 11 語で、既定は `空`。
**つらなりが線の在り方（実線・破線・点線・一点鎖線）を言うのに対し、おもては閉じた図形の内側がどう在るかを言う。**
**⚠ この節が動かしたのは仕様だけで、実装はまだ無い** —— saijiki テーブルはいま 9 カテゴリ 70 語で `おもて` を持たず、
`GET /api/saijiki` も `inku-cli reference` も従来どおりを返す。**語彙の現行値の正は実装であり reference §1 が公開する**
（SPEC §3.1 の但し書きのとおり）ので、**仕様が先に立っている状態は実装が入るまで続く。**
- **動詞を入れない**（2026-08-12 作者裁定）—— 「塗る」ではなく「べた」。**面の在り方は静止画の状態であって
  時間の経過ではない**から、行為の語は §2 原則 5 と §3.1 の「配置する動作を中心とする」の両方に衝突する。
  `描く` が v1.92 で剪定されたのと同じ理由による。
- **質（空・べた・薄墨・粒・点・平行線・交差線・にじみ・アクアチント）と濃さ（濃い・薄い）の 2 次元**を持つ。
  ゆらぎが振幅・周波数・質の 3 次元を持つのと同じ形。
- **⚠ 濃い／薄いは相対の語であって絶対の濃さではない** —— 同じべた塗りでも道具によって
  原寸 1618px の平均輝度が 17.4〜131.1 に振れる（実測）。
- **⚠ 紙目は入らない** —— 面ではなく地の質で、`地:` が引き取る。**背景を埋める指示も面の話ではない**（`background` へ行く）。
- **ゆらぎの `滲む` と おもての `にじみ` を別のものとして書き分けた**（§13.6）——
  **線そのものが震えて滲む**のと、**塗った面の縁が広がる**のとは違う。**品詞も動詞と名詞に分かれる。**

**直したのは日英それぞれ 6 箇所**（§2 原則 5・§3.1 のカテゴリ数・§3.1 の語彙表・§3.1 のコアの性質・§7.5・§13.6）。
**コードは 1 行も変えていない。**`check_docs.py` は一致。

### 2026-08-12 — コーパスが見ていない退行を、CI が止めるようになった（**採番なし**・CI のみ）

**`checks` workflow を新設した**（[I-192] 決着）—— **server（`ruff` と `pytest`）・cli（`ruff` と `pytest`）・
web（`npm run check`・`test:unit`・`lint:i18n`）・公開文書（`check_docs.py`）**の 4 job。
既存の `reference-corpus`（凍結コーパスの再生成を照合する 3 job）はそのまま置いてある。
**これまで CI が回していたのは凍結コーパスの再生成だけで、pytest も ruff も 1 行も無かった。**
- **linux で初めて回したら 2 本落ちた** —— **① Android の参照 fixture の焼き直し**
  （`render-engine-31/manifest.json` と `renderer_variation_primitives.json` が違う）と、
  **② platform 安定性の対のテスト**（動いた case の集合が凍結値と違う）。
  **どちらも darwin で焼いた凍結物と照合する検査で、linux で焼いた値を欠陥と読む。**
  **CI では外し、理由を workflow に書いた。手元では従来どおり両方が走る。**
- **⚠ Android の参照 fixture のディレクトリは、既存の CI が照合していなかった** ——
  `reference-corpus` が見ているのは `server/reference/` と `android/design/preview/` の 2 つで、
  `android/app/src/test/resources/server_reference/` は入っていない。**OS 差は今回はじめて観測された。**
- **linux の実測**: server **2,987 passed / 40 skipped / 2 deselected**（299 秒）・cli **220 passed**・
  web **245 pass** ＋ 型 **0 errors**（既知の a11y 2 件）＋ i18n **0 errors**・`check_docs.py` 一致。
  **手元の 3,029 collected と数が合う。**
- **CI が見ていないものを列挙した**（workflow のコメントと PROJECT_CONTEXT）—— 鍵の要る **30 件**・
  ローカル専用の材料 **10 件**（`cli/bench/leaf` の 9 と `cairosvg` の 1）・上の **2 件**・
  **Android の JVM テスト**（gradle wrapper が無い）・**`no-git-sync/`**（git が追跡しない）。
  **skip の理由は `-rs` で毎回印字する。緑の件数ではなくこの一覧を読む。**

### 2026-08-12 — 除外設定が守れない場所を、commit の直前で番人が見るようになった（**採番なし**・運用のみ）

**`scripts/git/setup.sh` が `pre-commit` の番人を置くようになった**（[I-193] 決着）——
staged の blob を**内容 9 規則**（`nvapi-` / `sk-` / GitHub PAT / AWS AKID / Google / Slack /
PRIVATE KEY / `Bearer …` / `24桁以上の16進.トークン`）と**名前 9 規則**（`.pem` `.p12` `.pfx` `.key` /
`id_rsa` 等 / `*_key.txt` / `.env`。`.env.example` は除く）で走査し、当たったら commit を落とす。
**印字するのは規則名と行番号だけで、値は出さない。**
- **なぜ除外設定では足りないか** —— ローカル専用の作業物は `.gitignore` に載っているので、
  版管理へ入れるには `git add -f` が要る。**`-f` はまさに除外を無効にする旗**で、
  同じ手が秘密も一緒に staged にする。**番人は索引の後ろに置くしかない。**
- **⚠ 走査が見ない場所こそ、旗が入れる場所** —— `.gitignore` を尊重する `grep` は
  無視されたディレクトリを走査しないので、**作業ツリーの監査は 0 件を返しながら `add -f` は通す**（実測）。
- **⚠ 実行できない hook を git は黙って飛ばす**（実測）—— 最初 hook を symlink で張り、
  「リンク切れなら commit を拒む＝安全側」と書いたが**偽で、commit は普通に通った**。
  **番人が消えたことが誰にも見えない**ので、hook を **wrapper** にして**自分で実体の有無を見て落とす**形にした。
  規則の本体は 1 ファイルにしか無いので、写しが腐ることもない。
- **誤検出 0 件** —— **内容 9 規則を追跡テキスト 10,858 件**（1MB 超 483 件は除外）に、
  **名前 9 規則を追跡 17,204 件すべて**に当てて、どちらも 0 当たり。
  受入は **5 通り**（陽性 2・番人の消失 1・無害な commit 1・逃げ道 `INKU_ALLOW_SECRET=1` 1）。
  **自分が書いていない `pre-commit` は上書きしない**（マーカーで見分ける）。
- **公開 clone では何も起きない** —— 番人の本体はローカル専用の作業物なので公開の木には無く、
  `setup.sh` は**何も入れず何も言わない**（模擬して確認）。`--no-verify` では従来どおり外れる。

### v2.13.11 — 記述パネルが畳めて、プラグイン語が組み込み語と同じ顔で並ぶ（Build 896、2026-08-12・空の契約「対話で決めながら UI を直す」段 1〜3）

**枠だけを渡して対話で 1 段ずつ埋める型の契約で、3 段が入った。**

- **記述パネルの 2 つの節が折りたたみを覚える**（段 1）—— 写生（Stage 0.5）にトグルを新設し、
  展開後（Stage 2 入力）の既存トグルを永続化した。**開閉はブラウザではなくアカウントに載る**
  （`model_settings` の allowlist に `sketch_open` 既定 開・`ddl_expanded_open` 既定 閉）。
  **⚠ 発行日の前提が現物と食い違っていた** —— 契約は「展開後と同じく状態保存」と書いていたが、
  実測では展開後も**どこにも保存していなかった**（再読込のたびに閉じた状態へ戻っていた）。
  **見本が無かったので裁定を取り、両方に足した。**写生の本文だけが畳まれ、頭（トグル・区切り・編集ボタン）は残る。
  **編集を開くと畳まれていても開く。**
- **プラグイン語が組み込み語と同じ見え方になった**（段 2）—— 色は `--accent` / `--accent-light`
  （直書きの赤 6 箇所を撤去）、chip は組み込みと同寸で詰め、**説明は chip の下と `title` から上部プレビューへ移した**。
  プレビューは**題／効果／例／絵**の 4 点で、組み込み語と同じ形。ドロワーと指示書編集モーダルの両方。
- **プラグイン文書が絵を宣言できるようになった**（段 2・仕様の追加）—— 語ブロックに **`preview:`** を新設。
  **文書自身のディレクトリの中の PNG を 1 枚だけ**指せる。ディレクトリの外（相対・絶対とも）・非 PNG・
  512 KB 超・不在は拒み、**拒んでも文書は落とさない**（絵の無い語は組み込みと同じ落とし先へ）。
  **HiDPI は宣言でなく名前で探す**（`名前@2x.png`）。絵は歳時記の payload に載せず
  **`GET /api/saijiki/plugin-preview`** が配り、`<img>` で表示する —— **文書は画面にマークアップを置けない。**
  `nature-leaves` の 7 語ぶんの絵を同梱した。
- **`inku-cli` に `--fires-on` を足した**（段 2）—— `--input-mode ddl` は記述を送らないので、
  **DDL にプラグイン語を綴っても展開は 0 件だった。**発火を決めるのは記述であって DDL ではない。
- **空のキャンバスの図形が縦横比で歪むのを是正した**（段 3）—— 図形の座標が**縦と横それぞれの比率**で
  書かれていたため、枠の比がそのまま図形の比になっていた（Pillar 1:5 では三角が針・四角が薄片になる）。
  **円だけが無事だったのは、半径が片方の寸法を両軸に使っていたからである。**
  固定の正方形座標系で描き、**拡大率 1 つだけ**で短辺に合わせ長辺方向は中央へ置く形にした。
- **API の表面**: 追加は経路 1 本（`GET /api/saijiki/plugin-preview`・セッション必須）と
  `ComposeRequest` の任意 `fires_on`（既定 None・送らない呼び手の挙動は変わらない）。**削除と改名は 0 件。**
- **検査:** **server 3,028 passed / 31 skipped**（新設 30 本）・**cli 224 passed**（+4）・
  **web 272 pass**（+27）＋ 型 **0 errors**（既知の a11y 2 件のまま）＋ i18n **0 errors**・
  **凍結コーパスはバイト一致**・`check_docs.py` 一致。**摂動 23 本。**
- **⚠ 摂動の空振りが 2 本出て、実装がゲートを直した** —— ① 絵の受入が `/src=/` で照合しており、
  **`data-src=` の中にも一致していた**（属性名を変えると絵は消えるのに緑）。② traversal の摂動が
  1 例に効いていなかった —— **絶対パスの例が存在しないファイルを名指しており、番人でなく存在検査で落ちていた。**
- **⚠ 受入の欠落を 1 件、実装が自分で見つけて足した** —— **旗を足した周に、その旗が効くことを誰も測っていなかった。**
- **⚠ 受け入れ側で 2 つ直した** —— **① SPEC がプラグイン文書について「外部ファイル参照は許さない」と
  書いており、新設した `preview:` と正面から食い違っていた**（日英とも「唯一の例外」として条件つきに書き直した）。
  **② `document_format.py` のコメントが `preview: <file>.svg` と書いていた**（実装は `.png` しか受け付けない）。

### 2026-08-12 — 採番の道具が、言われたときだけ書くようになった（**採番なし**・道具のみ）

**`scripts/bump.py` は `--write` を渡したときだけ 6 ファイルへ書く**（[I-195] 決着）。
起票の事故は **`--scan-build` を「次の番号を読む」つもりで単独で渡すと、その場で書き込んでいた**ことだった。
- **`--dry-run` は削除した**（2026-08-12 作者裁定）。既定と同義の別名として残す案は採らなかった ——
  **古い `--scan-build --dry-run` は argparse が exit 2 にするので、別の意味になって静かに通ることがない。**
- **既定値を置かなかった** —— 渡し忘れた呼び出しは `TypeError` で止まり、黙って書かない。
  自動の呼び出し元は実測 0 件だった。
- 旗なしの実行は `nothing was written (N file(s) would change) -- add --write to stamp` を印字する。
- **受入 5 本**（`test_bump_stamps_only_when_told.py`）。**tmp へ複製して `git init` した木の中で走らせる** ——
  実リポジトリへ当てると、番人が退行したとき本物の `web/BUILD_NUMBER`（共有の採番器）が動いてしまう。
  摂動（`if write:` を `if True:` へ）で 3 本が赤。
- **⚠ 受け入れ側で、偽になった命令行を 5 箇所直した** —— `AGENTS.md` と git 管理の引き継ぎ文書に
  `--dry-run` と `--write` 無しの採番が残っていた。**実装は実行規約と `CLAUDE.md` とメモリを直しており、
  この 2 冊が漏れていた。**
- **製品の振る舞いは 1 バイトも変わっていない**（触ったのは `scripts/` と `server/tests/` だけ）。

### v2.13.12 — 図形が「面がどう在るか」を言える（歳時記に `おもて`・ddl-engine 15）（Build 897、2026-08-12）

**下の層に機構があるのに、上の層に到達路が無かった。** 閉図形を持つ 1,139 作品のうち塗りが**言葉で明示された**のは
**1.3%**（15 件）で、逆に**塗られた作品の 96.7%**（243 件中 235 件）は誰も塗れと書いていない。
原因は **Stage 1 が書ける語と Stage 2 が読む語の交わりが日本語で空**だったことである —— Stage 1 は `埋める` を書き、
Stage 2 は `塗る` を読んでいた。歳時記に **11 語のカテゴリ `おもて` / `surfaces`** を `つらなり` の直後へ足した。
**つらなりが線の在り方を言うのに対し、おもては閉じた図形の内側の在り方を言う。**

- **語は状態の名詞で、動作ではない**（作者裁定）—— **日本語は名詞の「塗り」**（動詞の「塗る」ではない）、
  **英語は `flat`**。`solid` を採らなかったのは**つらなりの `実線` が既に持っている**ためで、
  語彙ブロックの隣り合う 2 行で同じ語が別の意味になる。`fill` も うごき の `埋める` が持っている。
  **`flat` は 10 カテゴリと relations のどの語とも衝突しない**（機械的に照合した）。
- **11 語**: 空（既定）・塗り・薄墨・粒・点・平行線・交差線・にじみ・アクアチント・濃い・薄い。
  **`紙目` は入れない** —— 面ではなく支持体の質なので `地:` が引き取る。
  **`にじみ`（名詞）は ゆらぎ の `滲む`（動詞）と品詞で分ける。**
- **欠陥 A を直した（語の食い違い）** —— Stage 1 は `面: 斜めに埋める。` を書き、Stage 2 の対応表は `平行線` を
  読んでいた。**DDL に 4 件出て Score に 0 件**だった。両側を `平行線` / `hatch` に揃えた
  （**Stage 1 側には既に `面: 平行線（粗から密）。` があり、表に無いのは片方だけだった**）。
- **欠陥 B を直した（面が線に付くと 1 画素も描かれない）** —— renderer は 2 箇所で閉図形を要求するので、
  **線・弧に付いた `surface` は描かれない。実測で面指定 1,495 のうち 798（53.4%）が死んでいた**
  （`line` 739・`arc` 59。内訳は `wash` 453 / `grain` 251 / `bleed` 83 / `paper_grain` 9 / `hatch` 2）。
  **決定的な層（coerce）で、直前の閉図形へ移す。移せる先が無いか、その図形が既に面を持つときは落とす**
  （**推測で面を足さない。1 つの質感要求を 2 つの命令に複製しない**）。発火は `coerce_branch_counts` に記録する。
  **`INKU_COERCE_DISABLE` の経路にも結線した** —— 描けるかどうかは作風の問題ではない。
- **`_CLOSED_SHAPES` を `schema.py` へ移した** —— coerce 側には `cloudform` を欠いた別の組が 5 箇所に散っており、
  **2 つの層が別々に判定すると片方だけが古くなる。**
- **metadata が描かれない質感を報告していた** —— `build_texture_metadata` を renderer の判定と同じにした。
- **`DDL_ENGINE_VERSION` 14 → 15。参照コーパス `ddl-engine-15/` は 45 件**（`ddl-engine-14` の 42 件 + 3）。
  **⚠ 足した 3 件は改修そのものを通す部である** —— ddl-engine-14 の 42 件に `面:` の句を持つ入力が
  **1 件も無く**、そのまま焼くと**版の印だけが変わった同じコーパス**になり、digest の受入は何も測らない。
- **⚠ 到達は届いていない（実測）** —— `面: 塗り。` から `filled=true` への到達は **0/4**。
  **改修前からある英語 `Fill it solid.` も 0/1** で、**新しい語だから届かないのではない**。
  質感側は届いており、`薄墨` → `wash` が **4/4**、`平行線` → `hatch` が **2/4**（残る 2 件は
  対応表に無い `paper_grain` をモデルが選んだ）。**coerce の移送は本番で 4/4 発火し、非閉図形に残った面は 0。**
- **⚠ 副次で 3 つ見つけた（未着手・台帳へ起票した）** —— ① `面: 薄墨。` の濃さが既定から下がった
  （0.35/0.28 → 0.20/0.15）。Stage 2 の新しい行を、モデルが **薄墨 の「薄」そのもの**に当てている。
  ② Stage 2 が対応表に無い `paper_grain` を enum から選ぶ。③ `layer_versions.py` に engine 14 の注記が無い。
- **検査:** **server 3,048 passed / 31 skipped**（新設 15 本）・**cli 224 passed**・**web 272 pass**
  （型 0 errors・既知の a11y 2 件のまま）・**凍結コーパスはバイト一致**・**Android JVM 269 件 / 失敗 0**・
  `check_docs.py` 一致。**摂動 14 本すべてが赤を出した。**
- **⚠ 受け入れ側で 3 つ直した** —— **① web の凍結 fixture 4 件**（エディタは歳時記語を色付けするので、
  `薄墨` が語になった時点で色が付く）。**焼き直さず、置換を宣言して「4 件ちょうど」を検査する形にした。**
  **② Kotlin の複製プロンプト 4 定数**（`WebDdlSpec.kt`。server の文言が動くと `PromptFingerprintTest` が
  必ず赤くなる。**丸ごと写す道具を書いて写した**）。**③ Android の歳時記 UI** —— 色の一覧が 10 個しか無く、
  **11 個目のカテゴリが静かに 1 個目の色を借りていた**（`[index % size]` なので落ちない）。
- **⚠ この版はプロンプト層とクライアントを動かすが、`render_engine_version` は 31 のまま。**

### 2026-08-12 — 採番の道具が、共有の採番器の 3 面を全部舐める（**採番なし**・道具のみ）

**`python3 scripts/bump.py --scan-build` が 3 面を自分で舐めるようになった**（[I-196] 決着）。
**2 面しか見ていない走査は、3 面見た走査と同じ形の答えを印字する** —— 欠けた面は、番号を渡すその行では見えない。
これまで読んでいたのは `refs/heads/` だけで、**同じファイルのコメントが人へ「`ssh pentala` も見よ」と指示していた。**

- **3 面**: refs（**`refs/remotes/` を含む** —— 別 clone が採って push した番号はそこにしかない）／
  **全 worktree の作業ツリーの現物**（**採ったが commit していない番号はそこにしかない**）／**配備先を `ssh` で 1 回。**
  **舐めた面は番号を渡す行に並ぶ** —— `next build number: 898 (scanned: 163 refs, 7 worktrees, ddl-server@pentala)`
- **配備先の在処**は `INKU_REMOTE_HOST` / `INKU_REMOTE_REPO` が先で、無ければ未追跡の `deploy.sh` の既定 2 行から読む
  （**公開リポジトリに配備先を載せない**・2026-08-12 作者裁定）。**行の形が変わったら推測せずその場で落ちる**
- **ssh が通らなければ番号を出さずに exit 1**（同じ裁定）。**`--local` が host 面だけを落とし、
  読まなかった面を同じ行に残す。****`--local` 単独は exit 2** —— 黙って何もしない旗を作らない
- **受入 13 本**（`server/tests/test_bump_scans_every_face.py`）。**tmp の木に git リポジトリ・連結 worktree・
  PATH の先頭に置いた `ssh` のスタブを作り、面を 1 つずつ動かす。**摂動 6 本の赤は **1 / 7 / 1 / 2 / 2 / 1** で、
  **6 本とも実装前に凍らせた予測と一致した**
- **⚠ 摂動で受入の穴が 1 つ出て、実装が自分で塞いだ** —— 環境変数の優先には経路が 2 本あり
  （早期 return と、`deploy.sh` を読んだ後の名前ごとの上書き）、**当初の受入は前者しか通していなかった**
- **受け入れ側で製品の経路を叩いた** —— **main の checkout と worktree の両方から 3 面が届き、
  どちらも `next build number: 898`。`cycle.sh build` の独自の走査とも一致した。**
  **届かない host を渡すと exit 1 で番号を 1 つも印字しない。**
- **製品の振る舞いは 1 バイトも変わっていない**（触ったのは `scripts/` と `server/tests/` だけ）。
  **検査は 3,048 → 3,061 passed / 31 skipped。**

### 2026-08-12 — テストの入口が 1 本になり、古いバイトコードが答えなくなった（**採番なし**・道具のみ）

**リポジトリ直下に `Makefile` を置き、`make test` を server・cli・web の入口にした**（[I-198] 決着）。
**呼び方で結果が変わる罠は、覚えて避けるものだった** —— `uv run pytest` と `./.venv/bin/python -m pytest` は
PATH の `python` が違うので同じ木でも別の答えを出す。**実行規約が名指していた環境変数を入口の中へ閉じた。**

- **入口は 4 つ**: `make test`（3 つ全部）・`make test-server`・`make test-cli`・`make test-web`。
  **どれもリポジトリ直下から呼べる**（各レシピが自分で `cd` する）
- **`UV_CACHE_DIR` と `UV_PYTHON_INSTALL_DIR`** を Makefile が持つ。**`?=` なので、CI のように
  外から与えられている面では外の値が勝つ**
- **CI の 3 ジョブを同じ入口へ寄せた。****server の `--deselect` 2 件は `PYTEST_ARGS` で維持した**（[I-222]。
  darwin で焼いた凍結物を linux で照合すると落ちる 2 本）
- **`PYTHONDONTWRITEBYTECODE=1` を 3 面で立てた**（[I-197] 決着）—— Makefile・CI の workflow 2 本・
  運用の道具（非公開）。**戻した摂動が古い `.pyc` に答えられる罠が、その 3 面では消えた**
- **摂動で確かめた**: `__pycache__` を消してから回すと、**番人ありで `.pyc` は 0 件・番人を外すと 141 件**。
  **「`.pyc` が増えていない」という観察だけでは、キャッシュが既に最新だった場合と区別できない**
- **検査**: server **3,061 passed / 31 skipped**（655 秒）・cli **224 passed**・web **272 passed / 0 failed**。
  **製品コードは 1 行も動いていない**（触ったのは `Makefile` と CI と運用の道具だけ）
- **⚠ 入口はまだ 1 本ではない** —— 手で `uv run pytest` を叩く道は塞がっていない。**したがってこの 2 つの罠は、
  機構が入っても記録から消さない**
- **⚠ 残した穴**: `make test PYTEST_ARGS=…` は server 向けの引数を cli にも渡す。
  **CI は `test-server` にしか渡さないので、今日は無害である**

### Android — 作品も自分の色を憶えている（android `2.1.4-android.25`、2026-08-12）

**作品の色の正本が「カタログ id」だと、カタログの定義が変わった日に、保存済みの作品が別の色で描き直される。**
server は 2026-08-09 に直した（[I-123]）。**Android は同じ形の穴を持っていたが、穴の形は同じではなかった** ——
**記録はもう書かれていた**（`render_color_map` は最初の commit から metadata に在る）。**読み戻していなかっただけである。**

- **`WorkColorSnapshot`** が保存行の `render_metadata_json` から記録を読む。
  **判定は server の `_snapshot_render_metadata` を条件式のまま写した** ——
  **`JSONObject` でない、または空なら「記録なし」**（既定値つきの取得へ置き換えない。空の map だけ別の道を通る）、
  **id は `render_color_catalog_id` → `catalog_id` → 既定の順**
- **描き直しだけが記録を運ぶ。** 推敲の 3 経路のうち `RenderFromScore` だけが渡し、
  **新規描画（`paint` / `composeFromDdl`）は今日の定義で描く**（作品を名指していない要求は server でも別の枝を通る）
- **⚠ 記録で描くときカタログ id を書き換えない。****id は名札ではなく色の割り当ての種でもある** ——
  同じ map でも id が違えば割り当てが変わる。**記録があれば記録の id、無ければ今日のカタログが解決した id**
  という server の非対称をそのまま写した
- **記録の無い作品を拒まない。** 拒むと、記録より前の作品だけが描き直せないまま残る
- **受入 9 件**（`WorkColorSnapshotTest.kt`）。**id の落とし先は 3 通りの入力で測り、
  種が変わらないことは seed 1〜200 で測る**（固定 seed 1 つでは、差し替えても同じ割当になる場合がある）
- **摂動 6 本**（受け入れ側で当て直した）: **2 / 1 / 1 / 3 / 2 / 1 件**が赤くなった。
  **⚠ 摂動を「解決した id」へ差し替えると 2 件しか赤くならない** —— 既知の id では解決前と後が同じ値になるため。
  **契約が書いた「既定へ差し替える」形にして初めて 3 件目（種の検査）が赤くなる**
- **検査**: Android JVM **278 passed / 0 failed / 0 skipped**（マージ後の木）。
  **`APP_VERSION` と `web/BUILD_NUMBER` は動かしていない**（Android の版管理は `android/VERSION` だけ）
- **⚠ 残した差**: 記録で描くとき、カタログ名と副題は今日のカタログのものが入る。
  **server は未知のカタログのとき名前を id そのものへ落とす。**契約が引用していなかった行で、台帳へ回した

### v2.13.13 — まとまりと道筋も、どのキャンバスでも形を保つ（Build 898、2026-08-12・[I-135] ③・render engine 32）

- **まとまり（クラスタ）の帯と道筋（`path`）の交差軸のずれを、キャンバスの短辺で画素へ直すようにした。**
  engine 31 は環と `at.region` を短辺基準にしたが、**この 2 つの並びはまだ比で伸び縮みしていた** ——
  **本番で展開される痕の 36.2% がここを通る**（まとまり 27.1%・道筋 9.1%）。
- **まとまりの帯は回転した枠の中で組まれ、そのまま正規化空間へ書かれていた** ——
  柱絵（1:5）では縦の細い縞、CinemaScope では横の縞になり、**同じ記述で帯自身の縦横比が紙のあいだで 8.8 倍**
  動いていた（0.0395〜0.4646）。**engine 32 では全 5 紙で 0.19771（正方形の値）に揃う**（実測）。
  道筋も同じで、**`wave` の振れは正方形で 220px・柱絵で 44px** だった。
- **⚠ 順序が結果を決める。ずれを先に回してから短辺係数を掛ける** ——
  **回す前に軸を縮めると、回転そのものが剪断になる。**
- **触っていないもの**: `margin` と `span`（道筋が自分の線に沿って紙をどれだけ使うかの量。
  **[I-135] ③-b は未裁定**）・`right_half` の広がり・`_path_pos` の既定分岐・`_scatter_pos`
  （一様はアフィン写像で一様のまま）・**まとまりの中心**。
- **⚠ 結線先は 4 本ではなく 5 本ある** —— `_clustered_pos` は中心を解くために自分で `_path_pos` を呼ぶ。
  **そこへ canvas を渡すと中心まで揃い、「真ん中のまとまりが上にある」が紙ごとに別の意味になる**（R3 と同じ線引き）。
  **渡さない理由をコードのコメントに残した。**
- **正方形のキャンバスは 1 座標も動かない。** engine 31 の配置座標を凍らせた検査（4 主題）と、
  規則を落として描いた SVG とのバイト比較（4 主題 × seed 2 通り）の**2 層**で押さえた。
- **コーパスに 13 件足して 582 件にした** —— **足す前のまとまり・道筋の case 10 件は全部が正方形**だった。
  足したのは**動く 9 件と正方形の対照 4 件**で、**既存 569 件は 1 件も動いていない。**
  **⚠ 主題ごとに「その主題が広がる軸が長辺である紙」を選んだ** ——
  柱絵の `top_to_bottom` は係数がちょうど 1.0 なので、**凍らせても失敗しようがない case** になる。
- **⚠ 新規 ID はすべて `changed_from_previous` に数えられる**ので、
  **動く 9 件と対照 4 件は manifest では見分けられない。判別力は摂動でしか示せない。**
- **検査:** **server 3,101 passed / 31 skipped**（マージ後の木・9 分 29 秒。**新設 40 本**・
  `test_cluster_path_aspect.py` 38 と `test_render_reference.py` 2）・**cli 224 passed**・**ruff clean**・
  **凍結コーパスはバイト一致**・**Android JVM 278 件 / 失敗 0**。
- **⚠ 実装セッションが摂動 11 本のうち 2 本で自分の受入の欠陥を見つけ、直して当て直した** ——
  **どちらも「短辺の側で測ると何を壊しても緑になる」型**である。**P-5** は主題が `wave` で、
  交差軸が y しかないため x が長辺の紙では恒真だった（`diagonal` へ替えて 4/4 が赤に）。
  **P-7** は `horizontal` の `span` が x の量なのに縦長の紙で測っており、摂動が恒等になっていた
  （紙を軸に合わせて絞り、T-9 は 8 件 → 4 件）。
- **⚠ 実装前に凍らせた予測は満点 3,091 に対し実測 3,092**（外れ 1 件・向きつきで報告）。
  **摂動は 11 本中 4 本が予測どおりで、外れた 7 本のうち 5 本は予測の粗さ**
  （`_density_radius` がまとまり以外からも読まれており、P-8 は 4 件の予測に対し 12 件赤くなった）。
- **契約に無かった変更が 2 つある**（どちらも版が上がったことの帰結）: `test_api.py` の版数リテラル 4 箇所と、
  **Android の期待値 fixture（`render-engine-32/` の 64 ファイル）**。**Kotlin のソースには触れていない。**
- **⚠ Kotlin の `pathPosition` / `clusteredPosition` は短辺基準を 1 つも持っていない**（[I-233] として起票）——
  遅れは **30・31・32 の 3 版**にまたがる。**移植の日は、中心を解く 1 本に canvas を渡さないことが要点になる。**

### 2026-08-13 — テストの入口が、設定を当て忘れた clone を直すようになった（**採番なし**・道具のみ）

**`make test` / `test-server` / `test-cli` / `test-web` の 4 入口が、suite を始める前に
`scripts/git/setup.sh` を 1 回当てるようにした**（[I-199]）。**罠は「覚えていることで避けてきた」型である** ——
`web/BUILD_NUMBER` の競合を消す merge driver は、実行コマンドが版管理されない `.git/config` にあるので、
**`.gitattributes` の 1 行だけでは発火せず、clone ごとに人が当てる必要があった。**

- **4 入口とも `git-setup` を依存に持つ。** `make` は同じ前提条件を 1 回しか走らせないので、
  **`make test` でも `setup.sh` は 1 回しか出ない**（検査がそう測る）。
- **`SETUP.ja.md` と `SETUP.md` を日英とも直した** —— 「clone ごとに 1 回」から
  「clone の直後に。忘れた clone でも make のテスト入口が冪等に当てる」へ。
- **検査を 5 本足した**（`test_merge_driver.py` は 5 本 → **10 本**）——
  **4 入口それぞれで `make -n` の出力に `setup.sh` がちょうど 1 回出ること**（4 本）と、
  **未設定の clone を実際に作り、901 と 902 に分岐した `web/BUILD_NUMBER` を merge して
  競合せず 902 が残ること**（1 本）。**後者は `.git/config` とマージ結果の両方を見る。**
- **検査:** **server 3,106 passed / 31 skipped**（マージ後の木・13 分 36 秒。
  **満点 3,101 からの +5 は新設 5 本ちょうど**）・**cli 224 passed**・**ruff clean**。
  **製品コードは 1 行も動いていない** —— 動いたのは `Makefile`・`SETUP` の日英・検査だけである。
- **摂動 5 本・空振り 0。** 依存を 1 入口から外すと 1 本、4 入口から外すと 4 本、
  `git-setup` の本体を `@true` にすると 5 本、`setup.sh` から driver の設定を落とすと 1 本、
  **driver が小さいほうを採るようにすると 4 本**が赤くなる。
- **⚠ 受け入れ側の予測は 5 本中 4 本が的中し、driver の摂動だけ外した**（予測 3 / 実測 4）——
  **非数値の側を 0 と数える case が、比較を裏返すと「大きいほう」でなく「自分の 0」を書くので赤くなる。**
- **⚠ 塞がったのはテストの入口だけである。** 台帳の 1 枚ものは「テスト・**配備**の入口が当てる」と書いていたが、
  **配備の入口には当てていない。****`make` を一度も回さない clone では依然として当たらない**
  （**失敗の向きは安全側** —— 従来どおり競合するだけで、誤ったマージにはならない）。
- **⚠ git 管理下でないディレクトリでは `setup.sh` が exit 128 で落ちる**（実測）ので、
  **ソース書庫を展開しただけの木で `make test` を回すと失敗する。**
  `SETUP` はそこでテストを回せとは書いておらず、コンテナも `make` を叩かないので実害は出ていない。
- **⚠ `setup.sh` は merge driver だけでなく commit 直前の秘密の番人（[I-193]）も入れる**ので、
  **テストのたびにフックが冪等に書き直される**（他人が置いたフックは書き換えない）。

### v2.13.14 — 構図が、どの紙に描くのかを知って組む（Build 899、2026-08-13・[I-135] A）

**Stage 2 は、どの紙に描くのかを知らずに組んでいた。** キャンバス比は Renderer が SVG の枠を決めるためだけに使われ、
**構図を組む側には 1 バイトも届いていなかった** —— 柱絵（1:5）でも屏風（2.2:1）でも、正方形のつもりの構図が来て、
それが枠へ押し込まれていた。**v2.13.14 では、プロンプトが紙を述べる。**

- **述べるのは 3 点だけ** —— **どの紙か**・**合わせてよいのは大きさと配置であること**・
  **記述が述べた大きさを紙の都合で覆さないこと**。
  **⚠ 個数は動かさない**（作者裁定。実測で個数の 72.6% は記述由来で、紙で変えると記述にないものを足すことになる）。
- **紙は「支持体」と書いた** —— 図形の「横長」と同じ語を使うと、モデルが紙の話を図形の話として読む。
  `横に広い支持体` / `縦に長い支持体` のように、**紙にしか使わない語で書き分けている。**
- **描き直しのときも紙を述べる**（作者裁定）—— Stage 2 の再試行は**別のプロンプトを使う 2 本目の生成器**で、
  そこだけ紙を知らないままだと、再試行が起きた回にだけ紙が消える。**本番の経路で再試行が発火することを実測で確かめた。**
- **手本に square 以外の紙の対を日英 1 件ずつ足した**（作者裁定で**静的本文へ**。紙ごとに生成する案は採らなかった）。
- **紙の確定を Stage 2 の呼び出しより前へ移した** —— 以前は Stage 2 の**後**で確定していたので、
  構図に渡せる値がまだ無かった。**副作用として、対応していない比への 422 が Stage 2 の前に返るようになった**（速くなった）。

**⚠ Score の `canvas.aspect` の意味が変わった。**

- **Stage 2 が宣言した比が、要求比で上書きされなくなった。**`Score.canvas` は
  **「その構図が何のために組まれたか」の記録**になり、**実際に演奏した紙は `render_canvas_aspect*` が持つ**。
  **この 2 つは食い違いうる**（v2.13.14 より前に保存した作品は、両方に要求比が入っている）。
- **描き直しは 3 つの答えを順に読む** —— 呼び手の override → **作品の行が記録した演奏の紙** → Score の宣言。
  **古い作品は以前とまったく同じに描き直る**（列は以前から埋まっているため）。
- **`/api/render` と `/api/history` は従来どおり上書きする。** あの 2 経路は Score をクライアントから受け取るので、
  守るべき Stage 2 の宣言が無い。

**測定（LLM を通す。受入ではない）**: 30 本を焼いて測った。

- **プロンプトに紙が届いた** —— `stage2_prompt_digest` は改修前が 3 紙とも同一、改修後は 3 紙とも相異なる。
- **紙は大きさを動かしている** —— 円の半径は square `0.3 / 0.1` に対し pillar は `0.15` 一律、
  線の長さは square `0.424` に対し pillar `0.335`（改修前は square も pillar も `0.424`）。
- **個数は動いていない**（「百二十」の 6 本すべてが `count=120`）。**設計どおりである。**
- **⚠ 宣言率は square 以外で 2/8 にとどまる**（square は 0/5）。**届いてはいるが、まだ多くは使われていない。**
- **⚠ M-4（記述が「小さい」と言ったとき柱絵でも小さいままか）は測れなかった** ——
  比較すべき 2 本がハード timeout でフォールバックになり、**破れている証拠も守られている証拠も無い。**
- **再試行は 30 本中 6 本（20%）**。**過去は測れないので、これが最初の数字である**（共有 LLM が混んでいた時間帯）。

**検査:** **server 3,140 passed / 31 skipped**（マージ後の木・7 分 54 秒）・**cli 224 passed**・
**web 272 passed / `check` 256 FILES / 0 ERRORS / 2 WARNINGS**・**ruff clean**・
**凍結コーパスはバイト一致**・**`check_docs.py` 通過**・**Android JVM 278 件 / 失敗 0**。
**新設は 34 本**（実装 33 ＋ 受け入れが 1）。

- **⚠ 受け入れで摂動が 1 本空振りした。ゲートを足した** —— 走行中の裁定で 段 4 が
  「上書きをやめる」から「作品の行から演奏の紙を読む」へ変わったが、**その解決順を歩く検査が 1 本も無かった。**
  中段の分岐を落としても **186 件が緑のまま**だった（**既存作品では宣言と演奏が必ず一致するので、
  この分岐はこの改修以後に保存される作品でしか効かない**）。**T-6b を足し、同じ摂動で赤くなることを確かめた。**
- **⚠ 契約は「この digest を凍結している検査は 2 本」と書いていたが、実測は 4 本だった** ——
  同じファイルの別の関数がもう 2 つ凍結していた。**名指した行の数は関数の数ではない。**
- **実装セッションの予測は摂動 9 本中 6 本が的中**（外れた 3 本は向きつきで報告されている）。
  **⚠ うち 1 本は「当て先が受入に届いていなかった」** —— 紙に依存せず本文を変える摂動は、
  「紙を運んでもブロック以外は動かない」という主張に触れない。**摂動を 1 本足して 6 件の赤を確認している。**
- **Android は複製プロンプト 2 定数を同期し `2.1.4-android.26` を採番した**（`sync_android_prompts.py --write`）。
  **Kotlin のソースは 1 バイトも書いていない**（道具が定数を丸写しする）。
  **⚠ この `.26` は同じ日に別の枝も採っており、どこにも出荷されていない。両方の改修は `.27` に載っている**（下の Android の節）。

### Android — 作品は描かれたときの名前を保つ（android `2.1.4-android.27`、2026-08-13・[I-232] [I-231]）

**[I-170] が入れた「記録で描く」機構に退行が 1 件あった。** 記録を推敲候補へ運ぶゲートが**経路だけ**で判定しており、
**別のカタログを頼む色の推敲**（同じ経路を通る）にも親の記録が乗っていた。
**結果として、頼んだカタログの絵にならず親の絵と一致していた**（実測）。**ゲートに「頼んだカタログが親と同じか」を足した。**

- **記録から描くときの名前と副題を、server と同じ 3 段で解く** —— **記録の非空値 → 今日の同一 id のカタログ →
  id そのもの（副題は空文字）**。**空文字の記録は「持っていない」として次の段へ落ちる**（server の `or` と同じ判定）。
- **未知のカタログ id を既定へ落とさずに在否を問える読み取りを足した**（`ColorCatalogs.find`）。
  **14 個のカタログ定義値は 1 つも動かしていない。**
- **名前と副題は種に入らない** —— `computeColorAssignment` の材料は `catalogMap` / `renderSeed` / `catalogId` のままで、
  **メタデータにしか出ない**（絵は 1 画素も変わらない）。
- **検査は 288 件 / 失敗 0**（10 本新設）。**受け入れ側でも同じ数を測り、予測 278 + 10 と一致した。**
- **摂動 9 本を受け入れ側で当て直し、9 本とも契約が名指した受入が赤くなった**（実装が別モデルなので skip していない）。
  **⚠ P-8 だけ赤の本数が実装の報告と違う**（報告 2 / 当方 6）—— **当て先の広さが違うためである。**
  当方は種へ名前を無条件に混ぜたので記録を持たない描画まで動き、既存の色の検査 4 本も道連れになった。
  **名指された T-9 が赤くなる点は同じ。**
- **⚠ `android/VERSION` を 2 本の枝が同じ `.26` に採っていた** —— **値が同じなので git は衝突を報告しない。**
  **同じ日の 2 つの改修が 1 つの番号を名乗るところだった。正解は N+2 なので `.27` にした。**
- **`APP_VERSION` / `web/BUILD_NUMBER` は動かしていない。pentala へも送っていない**（`android/` は全同期経路から恒久除外）。

### v2.13.15 — 台帳の課題を、番号で選びながら 6 件直した（Build 902、2026-08-13・[I-230] [I-202] [I-200] [I-069] [I-090] [I-091]）

**直すものを先に決めない型の契約で、作者が台帳の ID を 1 つずつ指し、1 件 = 1 commit で直した。**
**絵は 1 画素も変わらない**（render engine 32 / ddl engine 15 のまま。凍結コーパスはバイト一致）。

- **[I-202] 共通のはずのボタンクラスが、どのコンポーネントでも効いていなかった** ——
  `ghost-btn` は 17 ファイルで使われながら、**基底の CSS が各コンポーネントに 44 箇所複製されていた**（Svelte のスコープでは
  クラス名を書いても他所の規則が届かない）。**基底・hover・disabled・`ghost-active` を `+page.svelte` のグローバル規則 各 1 本へ寄せ、
  17 コンポーネントの複製を消した。**寸法は `--btn-sm-*`、色は `--panel` / `--fg2` / `--border2` と `--action-*` を読む。
  **`ShareModal` の 1 つだけは `--action-bg` で塗る主操作だったので、見た目を変えずに `.action-btn` へ改名した**（ghost ではなかった）。
  plugin chip の accent 規則も 2 パネルの重複をグローバル 1 本へ移した。
- **[I-090] アプリはブラウザで描くと定めた** —— `.svelte.ts` のモジュール直下に `$state` を持つ 11 本は、
  SSR が有効なままだと**要求をまたいで共有されうる**。**認証付きの単一ページアプリなので `export const ssr = false` を置き、
  境界を設定として明示した。**モジュール直下の状態を要求ごとの context へ移す改修は**していない**。
  **⚠ 起票文が求めていたのは後者である**（下記の「実施しなかったこと」）。
  検査は SSR 無効の設定と、`$state` を持つ 13 本の内訳（instance-scoped 2・module-scoped 11）を一覧で凍結する。
- **[I-200] 参照コーパスの生成器が、途中で止まると前の版を汚していた** ——
  SVG と manifest を版ディレクトリへ**直接**書いてから同一性の番人を判定していたので、
  **止まった回が前版を半分書き換え、次の版の `changed_from_previous` が空に見えた**（実測で 8 件が 0 に見えた）。
  **全部を同じ親の一時ディレクトリへ書き、番人を公開の前へ移し、完成した木を rename 1 回で公開するようにした。**
  番人が止めた回は版ディレクトリへ 1 バイトも書かない。既存版の再凍結は旧版を固定退避名へ退かしてから公開し、
  **2 回の rename の間で止まっても次回実行が旧版を戻す。**
- **[I-230] `layer_versions.py` に ddl engine 14 の注記が無かった**（15 の次が 13 だった）。
  採番 commit `f6082b80` と当時の CHANGELOG から現物どおり 10 行を復元し、
  **注記が現在の版から 5 まで降順で連続することを検査に据えた**（この注記はどの検査も見ていなかったので、欠けても赤くならなかった）。
- **[I-091] ルート単位のガードを router 既定へ寄せた** —— **未使用の `Depends` 引数 31 本のうち 24 本を消した**
  （`settings` 16・`public` 5・`plugins` 1・`render` 1・`auth` 1）。
  `auth` に `_user_manager` 既定の router を、`public` に `_current_user` 既定の router を足して認証必須のルートを移した。
  **残る 7 本は `plugins` の管理者限定で、router 既定より強いガードなので残す。**
  **利用者から見える認証・認可の結果は変えていない**（ルート 95 本・公開 6 本は据え置き）。
- **[I-069] アニメーション書き出しの読まれていない経路に検査を足した** ——
  `fade_white` の中間フレームが赤から白を経て青へ移ること、`slide` の中間フレームが左半分に現フレーム・右半分に次フレームを置くこと、
  4K / 8K の高さ（2160 / 4320）と遷移ステップ数（4 / 2）、符号化ピクセル上限 600,000,000 の境界と 1 画素超過、
  履歴項目が無いときの 404 と保存 SVG が無いときの 409。**製品コードは 1 行も変えていない。**

**検査:** **server 3,150 passed / 31 skipped**（マージ後の木・7 分 44 秒）・**cli 224 passed**・
**web 275 passed / `check` 257 FILES / 0 ERRORS / 2 WARNINGS**・**ruff clean**・
**凍結コーパスはバイト一致**（**生成器を実際に走らせたうえで**）・**`check_docs.py` 通過**。
**新設は 15 本**（server 12・web 3）。**`check` の 257 は前周の 256 に `+layout.ts` が 1 本増えたぶんで、`*.test.ts` は数に入らない。**

- **摂動 8 本を受け入れ側で当て直し、8 本とも名指された受入が赤くなった**（実装が別モデルなので 1 つも skip していない）。
  **⚠ SSR の摂動だけ赤の本数が実装の報告と違う**（報告 1 / 当方 2）—— **当方は web を全走したのでメタゲート `T-15` が道連れになった。**
  対象の検査が赤くなる点は同じである。
- **⚠ 実施しなかったこと（実装セッションの逸脱）**: **[I-090] は起票文が求めた「モジュール直下の状態を要求ごとの context へ移す」を行っていない。**
  SSR を切ることで同じ事故を防ぐ形を選んでおり、**11 本のモジュール直下 `$state` はそのまま残っている。**
- **⚠ 台帳の断定が 3 件、現物とずれていた**（起票日の観測であって、いまのコードではない）——
  **[I-230]**「14 を上げた周は版数だけを動かした」は誤りで、`f6082b80` は**参照コーパス 42 件と Android 用参照と参照テストも**動かしていた。
  **[I-200]** の行番号 832 は現在 949。**[I-091]** の「寄せられるのは 25 本」は現在 24 本で、内訳も起票時と違う（ルート構成が変わったため）。
- **画面では何も見ていない。** UI の見た目を動かす改修（[I-202]）と描画境界の改修（[I-090]）を含むので、
  **配備後に実画面で 1 度見る必要がある。**

### v2.13.16 — 起動しているあいだ、画面は白紙にならない（Build 903、2026-08-13・[I-235]）

**v2.13.15 で `ssr = false` にした代償を閉じた。** shell の HTML が空なので、
**アプリが組み上がるまで画面が真っ白**だった（`app.html` に背景色が 1 行も無く、ブラウザ既定の白が出ていた）。

- **`app.html` に幕を置いた。** `html` と `body` と幕を暗いほうの `--bg` で塗り、
  幕は `%sveltekit.body%` より**前**に置いて画面全体を覆う。**inline の `<style>` だけで塗るので、
  外部リソースを 1 本も待たない。**
- **幕は JavaScript を使わずに消える** —— `body:has(main) #boot-curtain { display: none; }` の CSS 規則 1 本だけ。
  **`app.html` に `<script>` は 1 つも無い。**
- **⚠ 白い時間そのものが縮んだのではない。** 縮んだのは「何も見えない時間」で、
  **最初に何かが出るまでが中央値 712 ms から 106 ms になった**（開発サーバーでの実測・各 6 標本）。
  **SSR を戻した場合の 172 ms よりも速い** —— 幕は下位リソースを 1 本も待たないためである。
- **⚠ 明るいテーマの利用者は、暗い幕のあとに明るい画面へ切り替わる。**
  テーマは利用者行の `ui_theme` から来るので、shell には分からない。**実画面では確かめていない**（配線からの推測である）。

**検査:** **server 3,150 passed / 31 skipped**（マージ後の木・7 分 47 秒）・**cli 224 passed**・
**web 279 passed**（4 本新設）・**`check` 257 FILES / 0 ERRORS / 2 WARNINGS**・
**`lint:i18n` 1050-47-0-0**・**ruff clean**・**`check_docs.py` 通過**。

- **摂動 4 本を受け入れ側で当て直し、4 本とも契約の予測どおり各 2 failed だった**
  （名指された受入 ＋ web のメタゲート `T-15`）。**実装が別モデルなので 1 つも skip していない。**
- **⚠ 受け入れで検査の穴を 1 つ塞いだ** —— **`T-31` が `html, body` の背景色を直書きの `#171716` と
  突き合わせていた。**そのままだと、**正本の `--bg` が動いたときに `T-33` だけが赤くなり、
  幕を直せば shell の `html, body` は古い色のまま緑で通る。**
  **両方を `+page.svelte` から読む形にし、摂動 2 本で確かめた** ——
  `html, body` だけを変えると `T-31` が赤く（旧い形が守っていた面）、**正本の `--bg` を動かすと `T-31` と `T-33` の
  2 本が赤くなる**（旧い形では `T-31` が緑のままだった面）。
- **画面では、幕・アプリ・白の 3 コマを前の周に撮ってある**（`cli/out2/902-v2.13.15-ssr-ab/`）。
  **配備後の実画面はまだ見ていない。**

### v2.13.17 — 小さいと述べられた痕は、誰が書いても小さい（Build 904、2026-08-13・[I-234]・ddl engine 16）

**同じ記述から、痕を書いた層によって大きさが 4 倍ちがっていた。** Stage 2 が円を書いて半径を空欄にすると、
`_coerce_instruction` が `PRIMITIVE_SPECS` の **0.15** で埋める —— **記述を 1 文字も読まない値**である。
ところが **coerce が自分で痕を作るときは句を読んで 0.038 を答える。**「小さな円を三つ並べる。」と書いても、
**Stage 2 が寸法を省いた回だけ大きな円になっていた。**

- **省かれたという事実を、埋める場所まで運ぶようにした。** `_with_stated_size` を
  **既定が入る前の命令**に当て、**空欄の半径・楕円の寸法だけ**を、その primitive を名指す句から埋める。
- **値も読み手も `_fallback_instruction_from_clause` から借りている**（新しい語表も定数も作っていない）。
  **これが「二度と離れない」根拠である** —— 片方だけを動かすことができない。
- **句の単位で決める。** 「小さな円を三つ並べる。四角を一つ置く。」なら円だけが 0.038 になり、四角は既定のまま。
  **該当する句が 2 つ以上あるときは何もしない**（記述がどちらの痕の話をしているか述べていないため）。
- **モデルが書いた寸法は上書きしない。** 半径 0.3 を明示した円は、記述が「小さな円」でも 0.3 のまま。
  **`半径0.02の小さな円` のように句が値を述べていれば、0.038 ではなく 0.02 を使う。**
- **`INKU_COERCE_DISABLE` の経路にも同じ修理を置いた** —— **記述が述べた寸法に忠実であることは「様式」ではない。**
  同じ理由で 2 つの方眼の枝が既にこの経路で動いている。
- **`normalize.py` の既定 0.15 は動かしていない。** 何も述べていない記述の答えとして残る
  （動かすと **235 作品**の側が全部動く）。
- **効く件数は本番 2,974 件の実測で 82 作品 / 108 痕**（円 41 / 楕円 67）。
  **⚠ 発行日の見積もり 55 作品 / 78 痕は、著者が書いた `ddl` で数えた値だった** ——
  **本番が coerce へ渡すのは Stage 1.5 展開後の `expanded_ddl`** で、そちらで数えると 82 / 108 になる（→ [I-237]）。
- **`ddl_engine_version` 15 → 16。** 参照コーパスに 4 件足して **45 → 49 件**（`b_coerce` 26 → 30）。
  **`changed_from_previous` は新しい 4 件だけで、持ち越し 45 件はバイト一致** ——
  `branch_report` に分岐名を足していないためである（engine 11・15 は足したので全件が動いた）。
- **Android の `ddl-engine-16/` も焼いた。⚠ ただしどの Kotlin テストもまだ読んでいない**
  （`ReferenceCorpus.kt` は `ddlEngineVersion = "7"` に固定。遅れは [I-217]）。

**検査:** **server 3,161 passed / 31 skipped**（マージ後の木・8 分 13 秒。**+11 は新設ファイル 1 本**）・
**cli 224 passed**・**ruff clean**（server / cli）・**`lint:models` 68 checks**・
**凍結コーパスはバイト一致**（`check_frozen_corpora.py`）・**Android JVM 288 件 / 失敗 0**・
**`check_docs.py` 通過**。

- **実装が同じモデル（Opus 5）だったので、枝の上で測られた摂動 9 本と満点は測り直していない。**
  **受け入れ側で回したのは「誰も通していない木」＝マージ後の 7 面である。**
- **摂動 9 本は実装が枝の上で全部当て、受入 11 本すべてがどれかの摂動で赤くなった**（恒真は無い）。
  **実装は契約から 2 点強めている** —— **T-5 に楕円の肢を足し**（契約の文だけでは P-3 が素通りする）、
  **T-10 を「焼き直される記録」にせず `coerce_score` の再走と digest の突き合わせにした**（凍結バイトを読むだけでは
  製品コードを壊す摂動で赤くならない）。
- **⚠ 契約が「発行日に測って凍らせた」と書いた表に誤りが 1 つあった** —— cloudform の値を
  square / triangle として載せていた（実装への影響は無い。→ [I-238]）。
- **⚠ 大きさの述語には盲点が残っている** —— 「極細の楕円」「細い縦線」を読まない。
  語表は既定寸法の側と共有なので単独では直せない（→ [I-236]）。
- **⚠ 版マーカー以外に 2 つの表が古くなっていた**（この周に直した）——
  `render-engine-history` のコーパス一覧表が **描画 `render-engine-30/` 553（SVG 7）**・
  **DDL `ddl-engine-13/` 40** のままで、実測は **`render-engine-32/` 582（SVG 13）**・**`ddl-engine-16/` 49** である。

### v2.13.18 — 対話で決めながら UI を直した 11 件（Build 905、2026-08-13・[I-157]）

**作者が 1 件ずつ指し、決まった順に直した 11 件。** 枝は 12 commit で、**畳んでいない** ——
2 組は**足してから作者裁定で消した記録**で、1 本は**恒真だったゲートを摂動で見つけて直した記録**である。

- **歳時記の`おもて`11 語に、絵と説明（日英）が付いた。** 足す前は 11 語すべてが汎用の文（波線 1 本）で、
  **おもては 11 カテゴリで唯一まるごと欠けていた。****11 枚は同じ輪郭を共有し、変わるのは内側だけ**である。
- **生成情報ドロワーに写生（Stage 0.5）の節が付いた。** 足す前は `CanvasPanel` に "sketch" が 1 度も出てこなかった。
  **これは画面に出ている作品の記録**で、記述パネルの写生表示（次に描くときの作業コピー）とは別物である。
- **生成情報に「この作品に実際に当たった色語」の行が付いた。** `render_color_map` は
  「記録なし」判定のために読まれていながら、値そのものは捨てられていた。
- **⚠ 2 行を足したあとに作者裁定で消した** —— **写生の文の行**（記述パネルが持っており、長い作品で解釈と演奏を押し出していた）と、
  **色カタログの惹句の行**（カタログの定数で、この作品の事実ではない）。
- **2 つのドロワーが同じ振る舞いになった** —— 歳時記ドロワーが**外を押すと閉じ**、
  生成情報ドロワーが**右端から 0.25s で捲れる**（どちらも他方が先に持っていた挙動である）。
- **設定ダイアログに`標準`／`詳細`が付いた。** `詳細`のときだけ**プラグイン・制限値・未読語台帳・その他（サーバー）**の 4 タブが出る。
  **タブの名前は新しい `settingsDetail.ts` が 1 度だけ持ち、タブバーと本文の番人が同じ表を読む** ——
  片方だけが知っていると、**届かない箱**か**押しても何も起きないボタン**が残る。
- **レールの UI モードのアイコンを描き直し、メニューの並びをアイコンに合わせた。**
  **旧アイコンは 3 モードで同じ絵**（枠 ＋ 縦棒 1 本）で、**右下の 4×2px の点は原寸 22px では見えていなかった。**
  **いまは濃い横棒の本数がモード**（シンプル 1・カスタム 2・フル 3）で、**メニューもその順**である
  （旧: シンプル／フル／カスタム ＝ 真ん中の量が末尾）。**3 案を原寸で見て A を選んだ。**
- **[I-157] 描き直しは、保存されたときの演奏になった。** `GET /api/history/{id}/svg` の
  `display` 以外・web の書き出し・web の replay・cli の非 display の**4 経路すべてに、行が持つ 2 つの seed を渡す。**
  **⚠ 約束するのは「保存された絵と同じ」ではなく「違いがエンジンの版のぶんだけになる」こと** ——
  **原則 7 が過去の版を保持しないと定めるので、一致は約束できない。**
  **落とした seed は、版の差の上に積まれた 2 つ目の差だった。**
- **⚠ `GET /api/history/{id}/svg` の非 display の応答バイト列が変わる**（ルート・鍵・形は不変）。
  **同じ入力で別の絵が返るようになったのではなく、正しい絵が返るようになった。**
- **⚠ 起点から在った i18n の 2 鍵を消した**（`provenanceLabelCatalogSub` / `provenanceHintCatalogSub`）。
  **消えたことは `T-41` が日英と型の 3 面で検査している。**

**検査:** **server 3,165 passed / 31 skipped**（マージ後の木・7 分 43 秒）・**cli 225 passed**・
**web 332 passed / 0 failed**・**`check` 259 FILES / 0 ERRORS / 2 WARNINGS**・
**`lint:i18n` 1057 / 47 / 0 / 0**・**ruff clean**・**`check_docs.py` 通過**。
**増分は 4 面とも枝の申告と 1 つずつ一致した**（server +4・cli +1・web +53・`check` +2・`lint:i18n` +7）。

- **実装が同じモデル（Opus 5）だったので、枝の上で測られた摂動 26 本と 3 面の数字は測り直していない。**
  **回したのはマージ後の 7 面である。****⚠ 摂動 26 本・空振り 0 本。**
- **⚠ 起点が 4 版古く、main 側と 4 ファイルが重なっていた**
  （`CanvasPanel` / `SaijikiDrawer` / `SettingsModal` / `+page.svelte`。main 側は [I-202] の共通ボタン CSS）。
  **衝突 0 は正しいマージの証拠ではないので、両側の追加を名指しで数えた** ——
  **コンポーネント内の基底 CSS の複製は 0 件**（[I-202] が消したまま）・**`:global(.ghost-btn)` は在る**・
  **枝側の `data-saijiki-toggle` と `settingsDetail` も生きている。**
- **⚠ 検査が 4 本「消えた」ように見えたが、4 本とも main 側の追加だった**（枝の起点が古いため）。
  **枝が消した検査は 1 本も無い。**
- **⚠ 受入番号が [I-235] と衝突した** —— どちらも `T-29` を起点に `T-30` から採ったので、
  **web の木に `T-30`〜`T-33` が 2 組ある。**一意性を主張する検査は無いので**赤くはならない**が、読みにくい。

### v2.13.19 — 繰り返す単位は、痕 1 つとは限らない（Build 906、2026-08-13・[I-143]・render engine 33 / ddl engine 17）

**これまで arrangement が繰り返せたのは instruction 1 本だけだった。**
だから歳時記のプラグインが置く「対」——「弧と、その弧に両端で触れる弧」——は、
**解決済みの対を全部並べて**渡すしかなく、**各組の 2 本目は自分の相手ではなく、
たまたま手前にあった痕に触れていた。**

- **`Arrangement.group_size` が「1 単位が連続する instruction を何本ぶん占めるか」を述べる**（既定 1）。
  **Renderer は複合 span を先に写し、コピーごとにその中の relation を解く** —— この順序が関係を局所にする。
  member は自分の頭が受けた変換で運ばれる（**頭の錨のまわりの回転差・寸法比・色サイクルがあるときは頭の色**）。
- **文書プラグインは、対を「1 組の見本 ＋ `count=N / group_size=2`」の形で API へ渡す。**
  **公開展開は 1 バイトも動いていない** —— `instructions` は解決済みの対を全部持ったままで、DDL の本文も同じ。
- **作品全体の予算は `count * group_size` を数え、instruction の上限は単位の途中で切らない。**
- **⚠ `group_size` は 1 のとき直列化から外れる**ので、**保存済みの Score は読み方も描かれ方も変わらない。**
- **Android にも同じ判定を移した**（`android/VERSION` は `2.1.4-android.28`）。

**受け入れ側で見つけて直した 2 件**（作者裁定・2026-08-13）:

- **⚠ 番人が 1 本も無かった。** プラグインの圧縮形を Score へ入れる行（`render.py`）を
  古い書き方へ戻す摂動を当てたところ、**server 全走 3,170 passed が緑のまま**だった ——
  本番では複合が 1 件も乗らなくなるのに、誰も見ていない。**API の合流点を歩く検査を 1 本足した。**
- **⚠ どの span も上限に入りきらないと instruction が 0 本になっていた**（実測: 上限 2・span 3 で 0 本。
  従来の `group_size=1` なら 2 本残った）。**span をほどいて上限まで描くようにした** ——
  **何も入っていない作品は「小さい作品」ではない。**
  **上限は設定値なので、合流点で落としたものを応答の `render_limit_notes` へ記録するようにした**
  （**⚠ この鍵を読む画面は web にも cli にも無い。落ちた事実はまだ利用者に届かない**）。

**コーパス:** **描画は群 H を開いて 4 件増え 586 件**（動いた 4・不変 582）。
**DDL は 49 件のまま C の 6 件が全部動いた**（凍結へ `score_instructions` を足したため。A 13・B 30 はバイト一致）。
**⚠ H より上の case は全部 instruction 1 本だけの Score なので、足す前のコーパスはこの改修を 1 件も見られなかった。**

**検査:** **server 3,173 passed / 31 skipped**・**cli 225 passed**・**ruff clean**・
**凍結コーパス byte-identical**・**Android JVM 289 件 / 失敗 0**・**`check_docs.py` 通過**。
**実装は Codex（GPT-5）＝別モデルなので、検査も摂動も 1 つも skip せず全部当て直した** ——
**摂動 6 本**（契約の 2 本 ＋ 受け入れ側が足した 4 本）で、**予測と実測が名前まで 6 本とも一致した。**
**T-7（実機能経路）も NIM の鍵でローカル server を立てて `inku-cli` から再現した**（フォールバック 0 回）。

- **⚠ マニュアルの版マーカー 13 箇所は、前の周（v2.13.18 / Build 905）の改訂履歴が
  「更新した」と書いているのに 13 箇所とも v2.13.17 / Build 904 のままだった。**この周でそろえた。
- **⚠ 満点の記録器が、赤のある回の数字も満点として書いていた**（`cycle.sh` の `record_full_run`）——
  要約行は赤があっても「N passed」と出るので、**3,172 passed が緑でない commit の満点として 1 行入っていた。**
  **赤があれば記録しないようにし、入っていた 1 行を消した。**摂動で「赤なら 0 行・緑なら 1 行」を確かめてある。

### v2.13.20 — 塗りは、ほかの面の語と同じ 1 語になる（Build 907、2026-08-14・[I-227]・ddl engine 18）

**記述が「塗り」と言っても、絵の中は塗られないことがあった。** 歳時記のおもては閉じた図形の内側を
9 語で言うが、**そのうち 8 語だけが `surface.texture` へ行き、「塗り」だけが `filled` という真偽値へ行っていた。**
2026-08-13 の実測で、届かないのはその 1 語だけだと分かっている —— `texture` へ行く語は日英とも 12/14 で届き、
`filled` へ行く塗りは**英 0/14 ／ 日 2/4**（しかも 2 回とも重複した 2 本目の命令に乗ったもの）。
**言語の差でも語数の差でもなく、行き先の欄だけが到達を決めていた。**

- **`SurfaceTexture` に `solid` を足し、歳時記の `塗り` に `score_value="solid"`、`空` に `"none"` を与えた。**
  質の 9 語がこれで全部 enum に対応する（`texture_for_surface()` を `weight_for_surface()` と同じ形で追加。
  濃さの 2 語は**名指しで**除いた —— 「値が無いから除く」にすると、値を付け忘れた質の語まで静かに通る）
- **`filled` は残した。** 宣言順が凍っていること（`Instruction` の 25 フィールドは 1 つも動いていない）・
  保存済み作品が持っていること・coerce 自身が 4 箇所で立てていること、の 3 つとも実測どおりだった
- **coerce に枝 `_with_fill_as_a_surface_word` を足し、両方向を導出する** ——
  `texture="solid"` からは `filled=true` を（`filled` しか知らない読み手のため）、
  `surface` を持たない `filled=true` からは `texture="solid"` を（保存済み作品が新しい語彙で内側を言うため）。
  **線・弧では発火しない。両方の出口に結線し、`INKU_COERCE_DISABLE` の経路にも載せた**（描けるかどうかは作風の問題ではない）
- **⚠ 演奏 seed から `solid` を畳んだ。** これが無いと、段の枝が保存済みの `filled=true` に `surface` を足した瞬間に
  **本番 2,972 作品の塗られた閉図形すべてで筆致 seed が振り直される。**`_variation_seed_fields` が
  「演奏されない variation」を除いているのと同じ作法。**今日 `solid` を持つ Score は 1 つも無いので、既存のバイトは動かない**
- **`fill_is_asked_for()` を `schema.py` に 1 つ置き、読み手 3 つを寄せた**（renderer の塗り・
  「大きすぎる塗り図形」の抑制・「小さすぎる粒」の拡大）。**新しい言い方だけが抑制をすり抜けて
  画面いっぱいの黒い円になる**のを塞いだ
- **Stage 2 の対応表 4 行を書き換え、`面: 塗り。` / `Surface: flat.` の作例を 1 対足した。**
  **加えて、`filled=true` を教えていた既存の塗りつぶし作例 2 件も同じ形へ揃えた**（表が新しい規則を言いながら
  作例が古い規則を見せる内部矛盾を残さない）。**Stage 1 の定型は 1 文字も変えていない** —— DDL の見た目は `面: 塗り。` のまま
- **絵は変わらない。** `filled=true` だけの Score と `texture="solid"` だけの Score が**バイト一致の SVG** を出す
- **参照コーパス `ddl-engine-18` を焼いた** —— `changed_from_previous` は **30**（`b_coerce` 全件）だが、
  **Score が動いたのは 3 件（閉図形の命令 5 本）**で、残り 27 は `branch_report` に鍵が 1 つ増えただけ。
  `a_expand` 13 件・`c_plugin_expand` 6 件は 1 バイトも動いていない。生成器の 2 度目の走行はバイト一致
- **Android の複製プロンプトと指紋を同じ commit で揃えた**（`WebDdlSpec.kt` の Stage 2 定数 2 本と
  `prompts.json`）。**server のテストは指紋を 1 本も読まず、CI は Android を回さない**ので、
  揃えないと黙って腐る面である。`android/VERSION` は **2.1.4-android.29** へ採番した（実装が動いたため）

**LLM 実測（受入ではなく報告・全文は `cli/out2/906-v2.13.19-fill-as-a-surface-word-reach/`）:**
`面: 塗り。` は **`solid` 3/4**（903 の `filled` 2/4 から。**903 の 2 回は重複した 2 本目の円に乗っていたが、
本ランの 3 回はすべて単一の円**）。対照の `面: 薄墨。` は `wash` 4/4。
**⚠ 英語 `Surface: flat.` は 0/4 で、発行側の予測（8 回中 5〜8 回）を下回った。**
ただし**失敗の形が変わった** —— 903 では鍵そのものを書かない回があったが、本ランは 4 回とも `surface` を書き、
その中身に既定値の `"none"` を選んでいる。**行き先の欄には届き、選ばれた値が違う。**
**⚠ 本番のハード上限 120 秒を外して測った**（`INKU_STAGE1/2_HARD_TIMEOUT_SECONDS=900`。12 件中 7 件が 120 秒超・
中央 133.3 秒）。**フォールバック合成に落ちた回は 0 件。**この事実そのものが [I-227] の裁定材料である。

**検査:** **server 3,190 passed / 31 skipped**（+17 = 新しい受入 16 本 ＋ coerce golden の合成 case 1 件）・
**cli 225 passed**・**web 332 passed**・**ruff clean**・**凍結コーパス byte-identical**・
**Android JVM 289 passed / 失敗 0**・**`check_docs.py` 通過**。
**摂動 13 本を全走で当て、予測と実測を並べて報告している**（`perturb.py` 経由・13 本ともバイト一致で復元）。
**⚠ P-4（逆向きの導出だけ落とす）は T-6 を赤くしなかった** —— renderer が `filled` と `solid` を
同じ演奏として扱うので、逆向きの導出は**描画にとっては冗長**である。判別力を持つのは Score の言い方を測る T-5 と、
その先の読み手（保存済み作品の replay・Android・`filled` しか知らないクライアント）だけ。

- **受け入れで測ったもの（実装と同じ Opus 5 なので、枝の上で測られた数字は測り直していない）:**
  **禁止表の 8 件はすべて 0 ファイル**（`android/VERSION`・`APP_VERSION`・`web/BUILD_NUMBER`・
  `ServerScoreCoercer.kt`・`interpreter.py`・`ddl-engine-17` 以前・render コーパス全部・`cli/out2/903-*`）・
  **`Instruction` の宣言順 25 個がバイト一致**・**枝の木とマージ後の木が同じハッシュ**（`8337bac2`）・
  **名前で消えた 2 本は改名で、中身は主張を保っている**（`塗り` と `空` が値を持ったので
  「値を持たない 4 語」が 2 語になり、enum から除くのは `paper_grain` だけになった）
- **API 表面の宣言例外を狭く書いた** —— `SurfaceSpec` は**プロパティが 1 つも増えていない**ので、
  プロパティ集合の比較では enum の変化が見えない。**増えた enum 値を名指しで検査し、減っていないことも見る**形にした
- **起票 3 件:** **[I-248]**（Android が `solid` を選べない複製は 2 つある —— `ServerScoreCoercer.kt` の
  allowlist に加え、`ServerScoreSchemaJson.kt` の Score schema にも `solid` が無い）・
  **[I-250]**（`sync_android_prompts.py` は worktree から呼んでも main の checkout を測り、偽の緑を出す）・
  **[I-251]**（`texture_for_surface()` にまだ読み手が居ない。自然な結線先は `carriage.py`）

### v2.13.21 — 中断したバッチは、止まったところから続く（Build 908、2026-08-15・[I-257]）

**作者が 1 件ずつ指した 3 件。** 枝は 3 commit で、**畳んでいない**。
**⚠ この周は画面で 1 つも見ていない** —— ブラウザ拡張が 3 回とも未接続で、
ボタンの位置・一覧の高さ・スクロールバー・再開後の番号は誰の目にも触れていない。

- **バッチ記述履歴が 50 件になった**（20 件から）。**上限は server が持っており、
  読み出しと書き込みの両方で切っていた**ので、web だけ増やしても届かなかった。
  **下げ直すと保存済みの尾が落ちる。**
- **プルダウンを自前の一覧へ差し替えた。** ネイティブの `<select>` は**ポップアップの高さを CSS で指定できない**
  （ブラウザが決める）ので、**ウインドウの半分を天井にし、入りきらなければスクロールする**という指示を満たせなかった。
  いまは `role="listbox"` の一覧で、**外側クリックと Escape で閉じる。**
- **`描画する`の左に`中断時点から再開`が付いた。** 押すと**まだ作品になっていない行だけ**を、
  **原文の行番号のまま**描く。**「最後に描いた行の次から全部」ではない** ——
  途中で失敗した行があると、その後ろの描画済みを描き直してしまう。
  **ボタンは、最新のバッチ作品が最新の保存済み記述の最終行でないときにだけ出る。**
  照合は**番号と記述の両方**で行う（番号だけでは、記述を短くしたあとに無関係な run を「未完」と呼ぶ）。
- **再開の条件は前回最後に描かれた作品の記録から読む**（モデル・色カタログ・写生・暴れる・キャンバス）。
  **記録の無い条件は作らない** —— `render_wild` を持たない作品は「暴れるが off だった」のではなく
  「**その旗より古い**」のであって、`false` を作ると別の条件で再開することになる。
- **[I-257] 作品が「色カタログをどう指定したか」を記録するようになった**（`history.catalog_mode`）。
  **それまで行に残るのは解決後の id だけ**で、そのランが`記述から自動選択`だったかは
  **記録のどこにも無かった** —— `auto` で回したバッチを再開すると、
  **最後の行がたまたま解決したカタログに固定されていた。**
  **⚠ render hash の材料には入れていない** —— 入れると保存済み作品の同一性が全部作り直しになる。
  **既存の行は NULL のままで、それが正しい**（NULL は「記録されていない」であって「`auto` でなかった」ではない）。
- **⚠ `catalog_mode` を書かない保存の送り手が 2 つ残っている** —— `cli refine save` と web のデモ保存。
  **どちらも NULL を書く。**バッチ再開の経路はこの 2 つを通らない。

**API 表面:** **ルートの増減 0・operation の移動 0。** schema が 2 つ動いた
（`HistoryItem` と `HistoryPostBody` に `catalog_mode` が 1 つずつ増えた。任意・既定 null なので、
**送らない客はこれまでどおり通り、応答では null のとき鍵ごと消える**）。
DB は `history.catalog_mode`（VARCHAR・nullable）が 1 列増え、**migration は起動時に走る既存の仕組み**である。

**検査:** **web 357 passed / 0 failed**（+25）・**cli 225 passed**・
**server 3,193 passed / 31 skipped**（+3）・**ruff clean**・
**`npm run check` 260 FILES 0 ERRORS 2 WARNINGS**（警告 2 件は既存の a11y で、場所も件数も不変）・
**`lint:i18n` 1058 / 47 / 0 / 0**・**`lint:models` 68 checks passed**。
**摂動は 17 本**（段 1・2 が 11 本で全部発火、段 3 が 6 本）。
**⚠ 段 3 の A5 は空振りした** —— 列の宣言の**空白だけ**を変える摂動で、
受入の正規表現が空白の数に寛容だったため字面としても意味としても素通りした。
**空白に寛容であること自体は正しい**（整形で赤くしたくない）が、それでは判別力を 1 度も測っていないので、
**予測を凍結してから A6（列そのものを消す）を足し**、予測どおり赤くなることを見た。

- **受け入れで測ったもの（実装と同じ Opus 5 なので、枝の上で測られた数字は測り直していない）:**
  **枝の木とマージ後の木がバイト一致**（`dfa266b8`）・
  **`api-surface-baseline.json` の差分は 3 行**（2 schema と digest のみ。**route と operation は 1 つも動いていない**）・
  **凍結側の番人 3 本は焼き直しでなく宣言追加**（`test_the_card_only_adds_one_route.py`・
  `test_the_acl_only_adds_to_the_api_surface.py`・`test_the_groups_decide_what_you_may_do.py`。
  **「他は 1 バイトも動いていない」という主張は残っている**）
- **⚠ `server/` へ 2 度越境した**（契約は「触らない」面と書いていた）。**どちらも着手前に報告して作者裁定を得ている** ——
  段 1 は定数 1 行、段 3 は列 1 つと 5 ファイル・8 箇所の通り道。
- **起票 1 件・同じ周に解決:** **[I-257]**（受け入れ側が起票し、作者裁定で選択肢 B を採って段 3 で直した）

### v2.13.22 — 地は、名前で呼べる 7 つの支持体になる（Build 909、2026-08-15・render engine 34 / ddl engine 19）

**地は「紙・和紙・薄墨地・木炭地」の 4 種しか無く、しかも記述から届いていなかった** ——
`washi` は**本番 3,086 件で 0 件**、計測用の 2,125 件でも **0 件**だった。
**支持体を 7 種にし、歳時記に `じ` を新設して、名前で呼べるようにした。**

- **歳時記に `じ`（grounds）が付いた** —— 紙・和紙・薄墨地・木炭地・**カンバス**・**画用紙**・メゾチントの 7 語。
  `GroundMaterial` も 8 値（`plain` ＋ 7）になった。
- **7 種は `<pattern>` のタイルとして敷かれ、`<filter>` を 1 つも使わない。**
  engine 33 まで `display` だけが `feTurbulence` の矩形で、ほかの 2 profile は粒を撒いていた。
  **いまは同じ Score を 3 つの profile で描くと、地の層のバイトが完全に一致する。**
- **費用の歯止めが要素数からバイトへ変わった**（24 KB・2026-08-14 作者裁定）。
  engine 15 の「支持体は雑音の性格であって、描くものではない」は、
  **タイルの中の 80 本が画面に 1 度しか書かれない**実装では意味を失った。
- **⚠ バイトで最大なのはメゾチント（17,918 B）、タイル数で最大なのはカンバス（平織 21,626 枚）である** ——
  **この 2 つは別の量**で、Android の費用に効くのはタイル数のほう。→ **[I-256]**
- **Stage 1 の定型を 3 行から 8 行へ書き分けた**（日英）。**和紙を「紙」へ潰していた行も直した。**
- **裸の「紙」/ "paper" を引き金の一覧へ足した** —— 足す前は**日英 8 回とも `drawing_paper` に着いていた**
  （モデルは一覧に在る最も近い値を選ぶ）。足した後、`paper` が Stage 2 の生の応答に現れたのは**この改修が初めてである**
  （日 1/4・英 2/4）。**画用紙側は 8/8 とも自分の値のままで、恐れていた寄りは起きなかった。**

**LLM 実測（受入ではなく報告・全文は `cli/out2/907-v2.13.20-ground-named-by-the-llm/` と同 `-paper-trigger/`）:**
7 素材 × 日英各 4 回の 56 枚で、**Stage 1 は 56 回とも地の文を書いた**（発行日は 0/2,125）。
**最終 Score に `canvas.ground` が載ったのは日 18/28・英 16/28 で、落としているのは Stage 2 だけである。**
**⚠ 地を頼んでいない対照 8 枚のうち 2 枚に地が付いた** —— 付けたのは Stage 1 で、
`_enforce_ground_literal_gate` は「DDL に `地:` が無いのに Stage 2 が書いた」場合しか落とさないので素通しする。
**⚠ 本番のハード上限 120 秒を外して測った**（600〜1800 秒）。**フォールバックに落ちた回は日 1 / 英 11 / 対照 2。**

**検査:** **server 3,224 passed / 31 skipped**・**cli 225 passed**・**web 357 passed**・
**Android JVM 578 passed / 失敗 0**（debug と release の各 289）・**ruff clean**・
**`npm run check` 260 FILES 0 ERRORS 2 WARNINGS**・**`lint:i18n` 1058 / 47 / 0 / 0**・
**凍結コーパスはバイト一致**・**`check_docs.py` 通過**。
**摂動は 10 本 ＋ 判別力を直した後の 3 本。**

- **⚠⚠ 受け入れで参照コーパスを焼き直した** —— **枝が納めた `render-engine-34/` は、枝自身の commit から再現しなかった。**
  **588 件のうち 6 件**（すべて `paper` の地）**が 3 バイトずつ違い**、manifest は焼いた commit として
  **枝の起点 `f7933235` を記録していた** —— **描画の作業が終わる前に焼き、その後 1 度も焼き直していない。**
  受け入れでディレクトリごと消して焼き直し、**2 回目がバイト一致すること**と
  **`changed_from_previous` が同じ 13 件のままであること**を確かめた。
  **`check_frozen_corpora.py` はレポートが 1 度も名指していない検査である。**
- **⚠ マージで `api-surface-baseline.json` が衝突した**（v2.13.21 と同じ周に両方が焼き直したため）。
  **どちらかを採らず、マージ後のコードから焼き直した** ——
  動いた schema が **`{HistoryItem, HistoryPostBody} ∪ {CanvasGroundSpec}` の和集合**に一致し、
  **route と operation は 1 つも動いていない**ことを集合で確かめた。
- **⚠ `server/reference/render-engine-33` 以前と `ddl-engine-18` 以前は 1 バイトも動いていない**（禁止表 10 件とも 0 ファイル）。
- **⚠ 消えた検査 4 本は、2 本が改名で主張が強くなり**（`non_display_profiles` → `every_profile`）、
  **2 本は engine 15 の規則の失効である**（フィルタの中身を測る検査と、要素数の歯止め）。

---

### v2.13.23 — 絵が何でできているかを、その場で言う（Build 910、2026-08-15）

**生成情報ドロワーは「SVG サイズ」だけを出していた。バイトは重さの合計であって、内訳ではない。**
参照コーパスで最大級の 2 枚は **224,749 B で 158 要素**と **222,230 B で 680 要素**である ——
**同じ 220 KB が 4 倍ちがう数の図形でできている**。膨らませているのは図形の数ではなく、
**1 本の polyline が持つ点の数**だった。本版はドロワーに 2 行足し、**バイト・要素数・点の数の 3 つを並べて出す**。

- **数え方の正本を 1 つにした:** 新規 `web/src/lib/svgWeight.ts` の `measureSvgWeight()` は、
  推移を測る `no-git-sync/scripts/svg_weight.py` の `measure()` と **1 対 1** である
  （要素数 = 5 タグ〈`svg` `title` `desc` `metadata` `defs`〉を除いたタグの数、
  点の数 = `points` の空白区切りトークン数 ＋ `d` 内の数値の個数 ÷ 2、バイト = UTF-8 のバイト長）。
  **画面の数字と推移の数字が別の定義で数えられたら、両方とも使えなくなる。**
  **⚠ 数えるのは DOM ではなく `result.svg` の文字列である** —— `querySelectorAll('*')` は別の量を数える
  （除外が無く、比較表示では同じ絵が複数枚出ている）。
- **バイトの導出も同じ関数へ寄せた:** 表示行・i18n の鍵・出る文字列は 1 文字も変えていないが、
  `TextEncoder` の 1 行を残すと**バイトの定義だけ画面に 2 つある**ことになるため、`measureSvgWeight()` の
  結果から取るようにした（契約は「バイトの行は動かさない」と書いており、実装が理由を添えて報告した判断である）。
- **履歴の作品でも数えられる:** 配線は 1 本も足していない。`HistoryItem` は `svg` を持たないので、
  ほかの行と違い `statusHistoryItem ?? result` の形にせず `result` から数える。
- **日英と用語:** ヒント鍵 2 つを `ja.ts` / `en.ts` / `types.ts` の 3 面へ対で足し、
  `GLOSSARY.md` に **SVG objects / SVG points** の行を足した
  （**使ってはいけない訳** ~~elements~~・~~nodes~~・~~vertices~~ を理由つきで挙げてある）。
- **受入 `T-67`〜`T-71` を 14 本の test に割った**（新規 `web/src/lib/svgWeight.test.ts`）。
  **`T-71` の期待値は手で数えていない** —— test 内の fixture 文字列をそのまま `svg_weight.py` に当てて取った
  （`bytes=702 objects=10 points=14`）。
- **摂動 5 本を全部当てた**（実装が実測）: 予測 14 に対し**実測 20**。
  **差 6 は 2 本のメタゲート** —— **`T-15` は web の全 test ファイルを子プロセスで走らせる**ので
  どこが赤くなっても道連れになり（5 本すべてで +1）、**`T-16` は i18n-lint を回す**ので `en.ts` から
  鍵を落とした `P-4` でさらに +1。**今後この suite に摂動を当てる契約は、予測に一律「+1」を書く。**
- **費用:** 手元で最大の実物（321,053 B・78 要素・**13,536 点**）で **1.5 ms**、
  それを 16 回繰り返した**合成** 5.13 MB で 23 ms（Node・`$derived` なので絵が変わったときに 1 度だけ走る）。
  **本番 p90 の 4.79 MB に相当する実在の作品は手元に無いので、本番規模では測っていない。**
- **`svg_weight.py` との突き合わせを 1 枚でなく 3,012 枚で行った** ——
  `render-engine-10`〜`34` と `ddl-engine-1`〜`19` の参照 SVG 全部で、**3 量とも食い違い 0 枚**。
- **検証:** `make test-web` **371 passed / 0 failed**（起点 357・**+14 = 本契約の受入**）、
  `npm run check` **261 FILES / 0 ERRORS / 2 WARNINGS**（+1 FILES = 新規 `svgWeight.ts`。
  既知の a11y 2 件は起点と同数）、`npm run lint:i18n` **1,062 strings / 47 exceptions / 0 warnings / 0 errors**（+4）。
  **`server/` `cli/` `shared/` `android/` の差分は 0 で、その満点は測っていない。**

---

### Android — 移植が、痕を server と同じ物差しで測る（android `2.1.4-android.31`、2026-08-16・[I-177] [I-217]・render engine 26 → 30）

**移植が痕の大きさと揺れを「何と比べて」決めるかは、server と 4 版ぶん違っていた。**
遅れていたのは fixture ではなく**読む側の版数**で、`ReferenceCorpus` が版ごとのディレクトリを引くため
**遅れたままでも赤くならない**。本版は定数を 1 段ずつ上げ、各段でその版の機構を移した。

- **engine 27 — 手はもっと大きく振れる。** 成員ごとの大きさ ±25% → **±35%**、向き ±12° → **±27°**
  （`HAND_GROUP_SIZE` / `HAND_GROUP_ROT`）。**規則も除外も 1 行も動かしていない。**
- **engine 28 — 痕は自分の線の上に残る。** 揺らぎの物差しを**図形の代表寸法から線幅へ**
  （`AMPLITUDE_WIDTHS` fine 0.35 / medium 0.6 / broad 2.0。代表寸法の 0.40 というクランプは残した）、
  材質輪郭のオフセットを**意図した幾何ではなく演奏された墨から**、掠れを **`stroke-dasharray` から接触の場へ**、
  層の太さに**公称線幅の 0.33 倍**という上限を置いた。
- **engine 29 — 同じ紙の目を、どの機械でも同じだけ数える。** 接触判定が読む 5 つの量を**小数 6 桁の px 格子**へ。
- **engine 30 — どのキャンバスでも、痕は記述が述べた形を保つ。** `size` を**短辺基準の 1 つの口**（`sizePx`）へ通した。
  **置き場所の計算は 1 バイトも触っていない** —— 座標は従来どおり幅と高さに比例する。
- **⚠⚠ 段 2 が 1 件赤くなり、段 3 で消えた。** `23_square_filled_wild` の stratum-1 だけが食い違い、
  **`java.lang.Math.hypot` と CPython の `math.hypot` が同じ 80 点の総和で 1 ULP 違う**ことが原因だった
  （`1600.646920448216` 対 `1600.6469204482157`）。**その層だけ歩幅が `total/600` で決まっており、
  標本が 1 本増え、標本自身の分位点である閾値が別の値へ飛んだ。**
  **engine 29 が塞ぐ当の欠陥そのものなので、CPython の hypot を書き写さず段 3 の格子へ吸収させた。**
- **⚠ `renderEngineVersion` は `"30"` で止めた**（作者裁定 2026-08-15）。
  **`render-engine-31`〜`-34` は `-30` と `manifest.json` 以外バイト同一**なので、
  **定数だけ 34 にすると持っていない機構を名乗ることになる**（31 と 32 は [I-233] が負う）。
- **⚠ 台帳と本書の断定が 2 件とも誤りだった** —— **[I-177] と本書の engine 28 節が言う
  「移植は 27 を解決し続けている」は偽**で、定数の全履歴は **1 → 16 → 17 → 19 → 21 → 26**、
  **27 だったことは一度も無い**。**[I-217] の「`size` を読む 13 箇所」は発行日に 15 だった**
  （`render/` 全体では 22 で、短辺基準の口へ通したのは **8**、通さなかった 14 の内訳はレポートが持つ）。
- **手写しの期待値を持っていた検査 3 本を、機構と同じ commit で据え直した** ——
  `CornerShapeMaterialLayerTest` の sha256 リテラル 4 本を**版ごとのコーパス読みへ**（同じ instruction・
  同じ seed の作品が `31_triangle_pencil` と `32_polygon_brush_thin` に在る）、
  `testMaterialProportionalWiring` を engine 28 で到達不能になった `r=` の主張から**肯定と否定の対へ**、
  `testEachMemberOfATurningGroupFindsItsOwnAngle` の `±12` を**リテラルと定数の結びつきを主張する形へ**。
- **⚠ コーパス照合は線幅に対して盲である**（摂動 P-4 で実測）——
  51 枚の照合が比べるのは `d` / `points` / `stroke-dasharray` と class・要素数だけで、
  **`stroke-width` を動かす改修では 1 枚も赤くならない**。層の上限を守っているのは
  `renderer_proportional.json` を読む検査と本契約が足した受入の **2 本**である。
- **摂動 10 本・空振り 0 本**（実装が実測。予測 54 に対し**実測 39**）。
  **P-8 は正方形で必ず空振りし、P-10 が恒等を壊して受入を捕まえた** —— 契約が対で据えた 2 本が、
  **それぞれ別の摂動でしか赤くならないことを実測した**。
- **検証（受け入れ側がマージ後の木で測り直した）:** **Android JVM 295 passed / 0 failed / 0 skipped**
  （起点 289・**+6 = 本契約の受入**。`testDebugUnitTest` 単独の数で二重計上は無い）、
  **`test_android_reference_fixtures_are_current.py` 4 passed**。
  **参照 fixture は 1 バイトも焼き直していない**（読む側の版数だけを動かした）。
- **触ったのは `android/` の 11 ファイルだけ。** `APP_VERSION` / `web/BUILD_NUMBER` は動かしておらず、
  **pentala へも送っていない**（`android/` は全同期経路から恒久除外）。
- **台帳へ 3 件起票した**（ID は未採番）—— **`shapeBbox` に cloudform の枝が無く雲形の面が描かれない**・
  **弧長が角度差を 360 で正規化している**・**`testMaterialOutlinePointsAndDashArrayExactParity` が
  engine 28 以降、層の断片を 1 本も見ていない**（正規表現が `class="material-outline"` の完全一致を求めており、
  コーパスの engine 30 には**完全一致 0 本・`stratum-N` つき 3,867 本**。受け入れ側で検算した）。
  **3 件とも凍結コーパスでは赤くならない。**

### v2.13.24 — 面の質感は、それを持つ図形のものである（Build 911、2026-08-16・render engine 35・[I-259]）

**`面: 平行線` / `面: 交差線` は、図形の外にも斜線を敷いていた。**
面の質感は「その図形が何でできているか」であって、紙の模様ではない。
それまでのハッチは図形の外接矩形の対角線ぶんの長さで引かれ、輪郭で止まらなかったので、
**置いた墨の 6 割以上が図形の外に落ちていた**（画素の実測: 円 62.2%・三角 84.6%・四角 64.1%）。
本版は**行の両端だけを輪郭で切る**。

- **切ったのは端だけで、行の引き方は 1 つも動かしていない** —— 角度・間隔・`spacing_gradient`・
  1 行ごとの揺らぎ・行数の上限 80 はそのままである。**規則的に引かれた線が規則的なまま、
  図形の中だけに残る**。凹形（雲形）では空洞をまたがず、区間ごとに 1 本ずつ描く。
- **`clipPath` は使っていない。** 切るのは描く前の座標計算で行うので、
  **フィルタを使わない `compat` profile でも同じように図形の中へ収まる**。
- **はみ出しの実測**（限度は短辺基準で 20.0px）——
  `hatch` は三角で **413.9px → 0.7px**・四角で **353.5px → 0.5px**、
  `crosshatch` は三角で **423.3px → 0.7px**・四角で **353.5px → 0.6px**。
  **対照の `wash`（11.0 / 12.3px）と `stipple`（7.1 / 3.5px）は 1 桁目まで不変**で、
  これが他の面の語に触っていないことの実測である。
- **間隔は動かしていない。** `spacing_gradient="none"` の作品の間隔クラスは前の版と同じ値で、
  濃さの傾き（`coarse_to_dense`）も残っている。**「外を消す」だけで「中を減らす」はしていない。**
- **参照コーパスは 588 件のうち 9 件が動いた**（`hatch` 5 件・`crosshatch` 4 件）。
  **残り 579 件は前の版とバイト一致**で、**薄墨の 6 件もバイト一致**である。
- **⚠ 版上げの巻き添えが 1 本あった** —— 荒れた演奏が面の中のハッチまで届くことを測る検査が、
  ハッチの**本数**を 39 と 78 で凍結していた。輪郭と交わらない行が描かれなくなるので **29 と 58** になる。
  **痕の寸法は変わっておらず、減ったのは本数だけである**（主張は同じなので期待値を実測へ直した）。
- **⚠ 「焼き直される記録は検査ではない」の実例が 2 つ出た** —— 実装が最初に置いた 2 本の受入は
  **manifest どうしの比較**で、描画を壊す摂動 7 本すべてで緑のままだった。
  **どちらも生描き（現行の renderer で描いて digest と突き合わせる）を足して判別力を持たせている。**
- **摂動 11 本**（契約の 10 本＋実装が足した 1 本）。**空振りは 1 本**で、理由は
  **薄墨の枝がすでに輪郭で切られており、同じ切り方を当てても 1 バイトも動かない**ためである
  （受入の穴ではなく、当てた配線に判別力が無い型）。
- **申し送り（本版では触っていない）** —— 薄墨が縞に見えること・`bleed` が輪郭の外へ出ること
  （外へ出るのがその語の意味である）・面の質感が `line` と `arc` に付くと 1 画素も描かれないこと。
- **検証（受け入れ側がマージ後の木で測り直した）:** **server 3,238 passed / 31 skipped**
  （起点 3,224・**+14 = 本契約の受入**。main 側は server を 1 ファイルも触っていない）、
  **cli 225 passed**、**ruff は server / cli とも clean**、**凍結コーパスはバイト一致**、
  **Android JVM 295 passed / 0 failed**。
- **Android の参照 fixture は `render-engine-35/` の 64 ファイルが増えただけで、Kotlin のソースは
  1 行も動いていない**（`android/VERSION` は据え置き）。移植は自分が名乗る版のディレクトリを読む。

---

### Android — 面はそれを持つ図形の中に残り、広がりはどの紙でも形を保つ（android `2.1.4-android.32`、2026-08-16・[I-233]・render engine 30 → 35）

**移植は 4 版遅れていた。**そのうち **engine 33（繰り返す単位）は既に入っており**、
**engine 34（地の 7 支持体）は本版でも入れていない**ので、本版が移したのは **31・32・35 の 3 版**である。

- **engine 35 — 面の質感は、それを持つ図形のものである。** ハッチの行は外接矩形の対角線ぶん
  引かれて輪郭で止まらなかった。**行の両端だけを輪郭で切る** —— 角度・間隔・`spacing_gradient`・
  1 行ごとの揺らぎ・行数の上限 80 は 1 つも動かしていない。切るのは描く前の座標計算なので
  **`clipPath` を 1 つも使わない**。凹形では空洞をまたがず区間ごとに 1 本ずつ描く。
  **面の path は 41 本から 31 本へ減った**（輪郭と交わらない行が落ちた）。
  この移植には `surfaceContour` 相当が無かったので、輪郭を作るところから足した。
- **engine 31 — 環と `at.region` を短辺基準へ。** 環の半径も region の広がりも「1 つの長さ」なのに、
  x は紙の幅・y は紙の高さを掛けて画素になっていた。**region の中心は比のまま**で、
  動かしたのは半分の広がりだけである（「右上」はどの紙でも右上である）。
- **engine 32 — 道筋の横振れとまとまりの帯を短辺基準へ。** `margin` と `span` は動かさない。
  **まとまりの帯は、回した後の変位に係数を掛ける** —— 回す前に掛けると回転そのものが剪断になる。
  **⚠ まとまりの中心を解く `pathPosition` には紙を渡さない**（R3）。
  渡すと「真ん中のまとまりが他より上にある」が紙の形で意味を変える。
- **⚠ コンパイラに見えない呼び出し元が 2 つ、段 1 で赤くなった** ——
  `CompositeRepetitionTest` と `ServerRendererCloudformAndRelationsTest` は
  `resolvePerformanceScore` を**反射で**呼ぶので、引数を足しても compile では落ちない。
- **⚠ 発行した契約の測定が 3 箇所とも現物と食い違っていた**（発行後にコードが動いたのではない）——
  ① `expandArrangement` と `expandArrangementLayout` は**紙をすでに引数に取っていた**
  （結線が欠けていたのは 5 つでなく `pathPosition` / `clusteredPosition` / `resolveAtRegion` の 3 つ）、
  ② 「`21_hatch_computer.svg` を見ているゲートは無い」は偽で、**全 51 case を回す照合が見ていた**
  （ただし最初に食い違った case で止まるため、21 の破れは常に「06 が壊れた」としか報告されなかった。
  本版はそれを分離した）、③ 対照に指名した `wash` と `stipple` を**この移植は 1 つも描かない**
  （面の層は `hatch` と `crosshatch` にしか答えない）。
- **⚠ 契約が `<line>` で比べよと書いた行は `path` で比べた** —— `computer` は手描き重量なので、
  行は材質エンジンを通って `path` になる。**`<line>` は fixture にも出力にも 1 つも無い。**
- **摂動 13 本**（契約の 14 本のうち 1 本は 1 行で当てられず、現物の grep で測った）。
  **予測 29 件に対し実測 27 件で、外したのは 3 つ** —— **P-1** は契約の予測が誤りで
  （コーパスのハッチ 2 case はどちらも回転していない正方形なので、外接矩形と輪郭が一致する）、
  **P-3** は `computer` を非手描きと見ていた実装の見立て違い、
  **P-8**（正方形の早期 return を落とす）は**空振り**だった ——
  region の `y0` は 1 ULP 動くが、**この移植は anchor を小数 6 桁へ量子化する**ので丸めに吸収される。
  **早期 return は残した**（server が持っているものを落とす理由が無く、量子化をやめた日に効き始める）。
- **申し送り（本版では触っていない）** —— 面の層が `surfaceSeed` に本物の index でなく 0, 0 を渡していること・
  楕円の周長が描画枝（Ramanujan 第一近似）と面の輪郭（第二・server と 1 対 1）で 2 通りあること・
  `renderEngineVersion` が `"35"` を名乗るのに engine 34 は入っていないこと。
- **検証（受け入れ側がマージ後の木で測り直した）:** **Android JVM 55 クラス / 305 passed / 0 failed /
  0 errors / 0 skipped**（起点 295・**+10 = 本契約の受入**。削除も改名も 0 件）、
  **`test_android_reference_fixtures_are_current.py` 4 passed**、**参照 fixture は 1 バイトも動いていない**。
  **`server/` `web/` `cli/` `shared/` の差分は 0 で、その満点は測っていない。**
  **⚠ `cycle.sh accept` の「テストの増減」は `android/` を 1 ファイルも数えていない**
  （見ているのは `server/tests` `cli/tests` `web/src` だけ）ので、増減は手で数えた。
- **⚠ `APP_VERSION` も `web/BUILD_NUMBER` も動かしていない。** `android/` は全同期経路から
  恒久除外なので、この周は pentala へ送るものが 1 バイトも無い。

---

### v2.13.25 — 重い作品が、画面を止めずに開く（Build 912、2026-08-16・[I-264] の前段）

**本番でいちばん重い作品を開くと、画面が数秒間まったく操作できなくなっていた。**
発端の作品は **11,068,576 バイト・39,789 要素**で、これをページの中へ markup として入れると
**メインスレッドが 3,387ms 止まり、39,788 個のノードがページに残って**以降のレイアウトと
スタイル計算がそのすべてを歩いていた。本版は**運ぶ量**と**置き方**の 2 つを直す。

- **キャンバスは作品を画像として受け取る。** 作品の markup をページへ入れる代わりに、
  blob URL を作って `<img>` で描く。**ブラウザは 1 度ラスタ化し、ページに残るノードは 1 個**になる。
  同じ作品で**止まる時間 921ms → 681ms・絵が出るまで 1,859ms → 17ms**（784px の枠・白紙のページ）。
  **拡大と提示モードは変わらない** —— 拡大も移動も作品の外側の箱に掛かる CSS の変形なので、
  高倍率でも滲まない。**URL は作品が切り替わるときに解放する。**
- **⚠ 安全である根拠は、本番の全作品で数えた** —— **3,485 枚のすべてで `currentColor` 0 件・
  CSS 変数 0 件・`<style>` 0 件・`<script>` 0 件・外部 URL 0 件**。画像として描くと
  ページが与えていたものは届かなくなるが、**与えているものが 1 つも無い**。
  `class` 属性を持つ作品は 1,833 枚あるが、その 314 種類は engine の印で、
  **web の CSS に 1 つも一致しない**。
- **ゴミ箱の件数を数えるのに、絵を 100 枚ぶん運んでいた。** 数えるだけの取得が
  `include_svg=false` を言っていなかった。**運んだ絵は 1 枚も読まれていない**（`trashItems` に読み手が無い）。
- **API が gzip で答える。** 絵は文字なので **26.4% まで縮む**（11,068,576 → 2,917,551 バイト・level 6）。
  **level 9 は 0.9% 小さいだけで 204ms 余計に掛かる**ので採らない。
  **既に圧縮されている型は名指しで除外する** —— サムネイル PNG は 115,167 → 115,026 バイト（99.9%）で、
  仕事だけして何も減らない。**`image/` という族でまとめて除外しない** ——
  絵は `image/svg+xml` で出ていくので、族で書くとこの機能が本命の相手に効かなくなる。
- **⚠ 受け入れで退行を 1 件見つけて直した** —— **gzip の層が、描画中の進捗の知らせを最後まで握っていた**。
  `/api/paint/stream` は解釈が終わった瞬間に段の知らせを書き、画面はそれを読んで
  「いまどの段か」を出している。**圧縮は箱が埋まるまで送り出さない**ので、
  **5 通の知らせが 10 / 0 / 0 / 0 / 117 バイト**（先頭の 10 は gzip のヘッダ）となり、
  **絵が完成するまで 1 通も届かなかった**。本版は**1 通書くたびに押し出す**層に替えている。
  **失うものは無い** —— 圧縮は続き、最後の大きな本文はこれまでどおり縮む。
  **内容は前から壊れていない。壊れていたのは届く時刻だけである。**
- **⚠ この退行は、既存の 3 本の受入では 1 本も赤くならなかった**（層は在り、圧縮も除外も効いていた）。
  **「溜まるかどうか」を測る目を 1 本足した**（ASGI のメッセージ単位で数える。
  試験用クライアントは本文を繋いでしまうので、そこでは測れない）。
- **⚠ 座標の桁と個別フィルタには手を付けていない** —— **1 枚の SVG の数値 554,721 個のうち
  554,720 個が小数 6 桁**（[I-265]）で、**フィルタ参照は 24,446 個あり表示時の停止のおよそ 8 割**を占める
  （[I-264]）。**どちらも本版の範囲外で、台帳に起票してある**。
  **画面が固まる状態は消えたが、絵が出るまでの待ちは残っている。**
- **本番の分布**（3,485 枚）: SVG は**中央値 65,768 バイト・平均 346,834・p90 814,584・
  p99 4,420,887・最大 24,513,938**。**1MB を超える作品が 255 枚**ある。
- **摂動 6 本**（実装 5 本＋受け入れ側 1 本）。**予測と実測は 6 本とも一致した。**
- **検証（受け入れ側がマージ後の木で測り直した）:** **server 3,242 passed / 31 skipped**
  （起点 3,224・**+14 は main 側**〈engine 35 の契約〉・**+4 が本版**）、**cli 225 passed**、
  **web 373 pass / 0 fail**（起点 371・+2）、**`npm run check` 261 FILES / 0 ERRORS / 2 WARNINGS**（不変）、
  **`lint:i18n` 0 errors**、**ruff は server / cli とも clean**。

---

### Android — 移植は、server と同じ判定を返す（android `2.1.4-android.33`、2026-08-16・[I-262] [I-263] [I-267]）

**版はもう揃っていた。**`renderEngineVersion` は server と同じ `"35"` で、凍結コーパスの 51 枚は
全部バイト一致する。**それでも、この移植は 3 つの場所で server と別の答えを返していた。**
**3 件とも凍結コーパスでは 1 枚も赤くならない**（該当する case が 51 枚の中に無い）ので、
**受入は全部、性質として置いた** —— 測るのは「前と違うこと」ではなく「**server と同じ値であること**」で、
**動いてはいけない側の対照を必ず対で置いてある**。

- **[I-262] — 雲形にも面が付く。** `shapeBbox` は circle / ellipse / square+triangle / polygon の
  4 枝しか持たず、雲形は `else -> null` に落ちていた。**`面: 平行線` を書いた雲形は、
  server では面が描かれ、Android では 1 本も描かれなかった。**
  **⚠ 枝を足しただけでは面は 1 本も出なかった** —— 雲形の枝は `renderSurfaceVectors` を
  **そもそも呼んでいない**（他の 10 の primitive は全部呼んでいる）。server は面の層を
  primitive の分岐の**外**で instruction ごとに 1 度呼ぶので、雲形にも面が付く。**結線を足した。**
  **null は「面を描かない」と同義だったが、雲形では null に到達する前に面の層が呼ばれていなかった。**
- **[I-263] — 弧は端から端まで測る。** 弧長は「端の角度の差」であって「0〜360 に折り返した差」ではない。
  **4 箇所のうち 2 箇所が折り返していた**ので、`300°` から `20°` へ引いた弧を、server が `r × 4.8869` と
  測るところ `r × 1.3963` と測っていた。弧長は粉の個数と標本の数を決めるので、
  **逆向きの弧では墨の量そのものが違っていた。**
- **[I-267] — まとまりはそれぞれの律動を持つ。** server はまとまりごとに種を撹拌してから律動を解くが、
  移植は撹拌していなかったので、**3 つのまとまりが中で同じ間の取り方をしていた**。
  撹拌は **seed xor まとまりの番号**で、`k=0` は `seed xor 0 == seed` になる。
- **段 1 の出力を server と 1 対 1 で突き合わせた**（雲形＋`surface(hatch)`・`golden` 1618×1000・seed 12345）——
  **`path` / `polyline` / `rect` が 16 / 33 / 1 で一致**、class も
  `cloudform contour-v1 stroke-engine-touch` / `contour-stroke-v1 controls-245 events-0` /
  `material-outline stratum-0` / `stratum-1` / `surface-stroke-v1 hatch-spacing-22.500` の 5 つが一致する。
  **バイト数だけ 41,958 対 41,432 で違う**が、差は `<g>` の数（server 8・Android 4）で、
  **面の行数 33 は一致している。**
- **摂動 13 本**（当て先が 2 箇所ある P-8 と P-12 は a / b に割ったので、当てて戻したのは 15 回。
  **戻しは 15 回とも sha256 一致**）。**空振りは 1 本も無い。**
  **予測を外したのは 3 本** —— **P-7**（予測 2・実測 4）は「浮動小数点で 1 ULP 違いうるが動かない」と
  予測して**動いた**。`2πr × (Δ/360)` と `r × |radians Δ|` は数学的に同じでも `rint` の入力が変わって
  分割数が動き、**コーパスが 2 本道連れになった**（契約が「2 式を統一するな」と書いた根拠が実測で裏づいた）。
  **P-11**（予測 4・実測 3）は T-93 が緑のまま —— 実装が律動を**まとまりの中心を相殺してから**取り出す形に
  変えたためで、**受入の穴ではない**（中心の撹拌は T-92 が名指しで測り、P-11 で赤くなっている）。
  **P-12**（予測 4・実測 2）は**既存の検査が 1 本も反応しなかった** ——
  粉は `<circle>` で出るが、51 枚を走る検査は `d` / `points` / `stroke-dasharray` しか見ず、
  要素数を数える検査は 10 枚しか走らない。**その 10 枚で材料層を持つ弧は `04_arc_crayon` だけで、
  角度が `0→180` なので補角も 180 —— 値が動かない。****コーパスの弧の粉を見ている検査は 1 本も無い。**
- **申し送り（本版では直していない・4 件とも台帳へ起票済み）** ——
  `rhythmT` が server の `_rhythm_t` と **4 箇所で別の式**であること
  （`n<=1` で 0.5 対 0.0・accelerando が `base²` 対 `base^1.35`・loose の揺れ幅が
  `0.12/max(n/8,1)` 対 `0.16`〈n=20 で 3.3 倍差〉・syncopated の定数）、
  非 hand-stroke の弧＋`variation` で server が `<polyline>` 148 点・移植が `<path>` 147 点であること、
  紙の寸法が切り捨て対四捨五入で `oban` が 666 対 667 に割れること、上記の番人の穴。
  **本版の T-93 が「どの seed を読むか」だけを測り、律動が何をするかを測っていないのは、
  1 つ目の乖離があるためである。**
- **検証（受け入れ側がマージ後の木で測り直した）:** **Android JVM 56 クラス / 314 passed / 0 failed /
  0 errors / 0 skipped**（起点 305・**+9 = 本契約の受入**。削除も改名も 0 件・`^e: ` 0 件）、
  **`test_android_reference_fixtures_are_current.py` 4 passed**、**参照 fixture は 1 バイトも動いていない**。
  **`server/` `web/` `cli/` `shared/` の差分は 0 で、その満点は測っていない。**
  **⚠ `cycle.sh accept` の「テストの増減」は `android/` を 1 ファイルも数えない**（[I-270]）ので、
  増減は `git grep -c '@Test'` の総和で手で数えた（305 → 314・55 → 56 ファイル）。
- **⚠ `APP_VERSION` も `web/BUILD_NUMBER` も動かしていない。** `android/` は全同期経路から
  恒久除外なので、この周は pentala へ送るものが 1 バイトも無い。
- **予測を凍らせた文書は公開ツリーから外した**（`no-git-sync/` に置いてある。先例 `a5b07945`）。

### v2.13.26 — 公開の一覧は、ログインに要るものだけを持つ（Build 913、2026-08-16・[I-086]）

**公開ルートは 6 本から 3 本になった。** 残るのは `/health`（コンテナの生存確認・データを返さない）・
`/api/info`（版と developer_mode。ログイン画面が読む）・`/api/auth/login`（ログインの窓口そのもの）で、
**`/api/prompts`・`/api/color-catalogs`・`/api/auth/config` は認可の内側へ移した。**

**外した理由は「一覧に載る各項目は、測った理由を持たねばならない」である。**
`/api/color-catalogs` が挙げていた理由は「ログイン画面の描画に要る」だったが、
**ログイン画面を実際に測るとカタログを 1 つも受け取っていない。** 公開に留めていたのは、
**誰もログインしていない時点で走る起動時の取得**のほうだった。**ログインに本当に要るものだけを残した。**

- **server** —— `public.py` の 2 本を認可つきの router へ移し、`auth.py` に認可つきの router を新設した。
  **`PUBLIC` は 3 本になり、生きたアプリを走査してガードの無い path の集合と突き合わせる**
  （`test_route_authorization.py`）。**ルートは 1 本も増減していない**（`EXPECTED_ROUTE_COUNT = 95` は不変）。
- **web** —— **`login()` の後処理が、公開でなくなった 2 つの一覧を読み直す。**
  起動時の取得はログイン前に走るので両方 401 を受け取り、ページはそれを飲む。
  **`login()` が改めて訊かなければ、カタログはフォールバックのまま・プロンプトのタブは
  再読み込みまで空のまま**になる。検査は **`login()` の本体だけを切り出して**当てている ——
  同じ呼び出しはページの別の場所にもあり（`loadCurrentUser()` が 1 つ上に同じ並びを持つ）、
  **ファイル全文に当てると、ログインと無関係な出現で満たされてしまう。**
- **cli** —— `_fetch_color_catalogs` が `auth=False` を外した。
  **検査は文字列の不在ではなく、要求が実際に運んだ資格情報を読む**（記録役のスタブに送って header を見る）。
- **公開文書** —— `docs/architecture/server-components` の日英が挙げる公開 path を 3 本へ直し、
  **その一覧が `PUBLIC` と 1 対 1 で一致することを検査に載せた**（**日英とも読むので、片方だけずれることがない**）。

**API 表面の記録は焼き直した**（digest `d5d312e1…` → `a925c3de…`）。
**⚠ 凍結 2 本に置く宣言は、契約が示した `added_params` / `removed_params` の 2 欄では足りなかった** ——
**引数を 1 つも持たなかったルートには「検証の失敗」という応答が無く、認可の依存が引数を 2 つ持ち込んだ瞬間に `422` が生じる。**
（`/api/prompts` は `query:lang:opt` を既に持っていたので `422` も既にあり、増えたのは `params` だけだった。）
**`responses` を丸ごと宣言外にはせず、`added_responses` / `removed_responses` の欄を足して、
宣言した応答コードだけを取り除いてから残りをバイト単位で比べている** ——
**「200 を静かに失った窓口」はなお赤くなる。**

- **受入 11 本**（`T-79`〜`T-89`）・**摂動 10 本**。**予想と食い違った本数は 0** ——
  赤くなるはずの T は全部赤くなり、緑のままのはずの T（`P-2` の `T-85`、`P-4` の `T-79`〜`T-82`）は全部緑だった。
  **`P-4`（宣言していない query 引数を 1 つ足す）で `T-83`・`T-84` だけが赤くなることが、
  上の宣言の機構が白紙委任になっていないことを測っている。**
- **⚠ 予測に無かった道連れが 1 種類** —— web を赤くする摂動（`P-5`・`P-6`・`P-10`）では、
  **suite 全体を回すメタゲート `T-15` が必ず一緒に赤くなる。** 契約はこれを書いていなかった。
- **検証（受け入れ側がマージ後の木で測り直した）:** **server 3253 passed / 31 skipped / 0 failed**、
  **cli 227 passed**、**web 375 pass / 0 fail**、**ruff 2 面・`npm run check` 0 errors
  （警告は既存の a11y 2 件）・`lint:i18n` 0 errors・`check_docs.py` 一貫**。
  増分は面ごとに枝の申告と一致した（server +11・cli +2・web +2。**main 側は Android のみなので 0**）。
  **`accept` が「消えた」と挙げた 2 本は削除ではなく引数の追加**だった
  （`test_color_catalogs_are_served_by_api` と `test_the_catalog_list_serves_the_rename_table`。
  **起点に在るかを引いて判定した**）。
- **`docs/architecture/server-components` の router 別の件数表も直した**（受け入れ側が実測）——
  **「合計82」だけでなく表の各行が古く、10 行の和がちょうど 82 だった。**
  製品コードを走らせて数え直し、**history 12→17・settings 10→16・me 12→13・public 9→10・合計 82→95** とした
  （残る 6 行は不変）。**`public` と `auth` の「既定ガード」列も書き換えた** ——
  この 2 つの router で個別ガードを持たないのは **`/health`・`/api/info`・`/api/auth/login` の 3 本だけ**である。
  **⚠ この表には赤くする検査が無いので、その旨を表の直後に 1 行書いた**（件数の正本は `EXPECTED_ROUTE_COUNT`）。
- **⚠ 受け入れ側が最初に数えた「ガード無し」は 4 本で、テストの 3 本と食い違った** ——
  **自分で書いた走査のガード一覧に `_session_token` が抜けていた**（`/api/auth/logout` はこれを持つ）。
  **食い違ったのはテストではなく、その場で書いた道具のほうだった。**
- **申し送り（本版では直していない）** —— **`manual/` は当該 API を「認証不要」と書いている箇所を
  1 つも持たないので、偽になった記述は無い** —— **新たに公開 path の一覧を manual へ書き足すことはしなかった**
  （番人の付かない 3 つ目の写しになるため）。

---

### Android — 同じ間、同じ点、同じ紙（android `2.1.4-android.34`、2026-08-16・[I-273] [I-274] [I-272]）

**前の版で 3 つの判定を server に揃えたとき、範囲外として起票した 3 件**を本版が担う。
**どれも凍結コーパス 51 枚が 1 枚も捕らえない**ので、**受入は 11 本とも性質として置いた** ——
測るのは「前と違う」ではなく「**server と同じ値である**」で、**動いてはいけない側の対照を対で据えてある**。

- **[I-273] — 間の取り方そのものが違っていた。** 前の版は「まとまりごとに律動の種を撹拌する」までを
  直したが、**種を受け取る `rhythmT` が server の `_rhythm_t` と 4 箇所で別の式**だった ——
  **1 人だけのとき 0.5 対 0.0**・**accelerando が `base²` 対 `base^1.35`**・
  **loose の揺れ幅が巡数で割られていた**（`0.12/max(n/8,1)` 対 `0.16`。20 人で 3.3 倍差）・
  **syncopated の 2 定数**（`0.085`/`−0.055` 対 `0.09`/`−0.045`）。**呼び出し元 5 箇所は 1 文字も動かしていない。**
- **[I-274] — 揺らぎのある弧が、点・位相・端・要素の 4 つで違っていた。** 手描き合成を通らない弧
  （`rotring`）に `variation` が付くと、**点が 1 つ少なく**（`segmentCount` 対 `+1`）・
  **位相が 1 目盛ずれ**（`i/count` 対 `i/last`）・**両端まで揺れ**（server は接点契約のため両端を固定する）・
  **要素が `<path>`**（server は `<polyline>`）だった。**4 つとも server に合わせた。**
  **⚠ 同じ木にもう 1 本、最初から server と一致している関数がある**（手描き経路の
  `arcPointsWithVariation`）ので、**直したのは片方だけである。**
- **[I-272] — 紙そのものが 1 px 割れていた。** 比から画素を出すとき **server は四捨五入・移植は切り捨て**で、
  10 個の紙のうち **`oban` が 666 px と 667 px に分かれていた**。
  **⚠ `Math.round` で直すと乖離が 1 件増える** —— Python の `round` は**半分を偶数へ**丸めるので
  `vertical`（562.5）は 562 だが、`Math.round` は**半分を上へ**丸めるので 563 になる。
  **`Math.rint` を使った**（いまは切り捨てが偶然 562 を出して一致していた）。
- **摂動 15 本。予測を外したのは 4 本** —— **P-8**（予測 7・実測 5）は
  房の中の律動が `rhythmSpacing != "none"` の条件の内側にあり、`none` では `rhythmT` を通らないこと・
  要素数と class だけを見る照合は座標が動いても緑であることが理由。
  **P-9 と P-10**（各 +2）は、**実装が段 2 で書き換えた既存の 2 本を予測表に数え忘れた**もの。
  **P-13**（予測 3・実測 0）は**空振り**で、`2πr × (|Δ|/360)` と `r × |radians Δ|` は
  **`r` を 50〜400・`Δ` を 1〜360 の整数で全数当たっても分割数が分かれる入力が 0 件**だった。
- **⚠ 契約（発行側）の測定が 2 箇所誤っていた** —— ① T-130 が「`pen` の弧＋`variation` で `controls-72`」と
  書いたが、**`controls-72` は `variation` 無しの量で、揺らぐ弧は `controls-148`**
  （前の版の受入は 2 つを別々の assert で持っており、契約が 1 行に畳んだときに混ざった。
  **本版は両方を別々に測る**）。② 「前の版の摂動 P-7 が 2 式の統一でコーパスが赤くなることを実測した」は偽で、
  **P-7 は『折り返す式へ』であって統一ではない**（統一の予測は「動かない」で、**実測も 0 本**だった）。
  **したがって手描き側の式を守っているのは受入でも凍結コーパスでもなく、両式が同じ値を返すという事実である。**
- **⚠ 既存の受入の測定点を 1 箇所ずらした** —— loose の振幅が `0.048` から `0.16` になり、
  **20 人・刻み 1/19 では両端から 1 人目が `clamp01` に届いて `t` が affine でなくなる**ので、
  律動を読み出す窓を `j = 1..18` から **`j = 2..17`** へ狭めた（`base ≥ 0.105 > 0.08`）。
  **これは実装が着手前の凍結ノートに予測として書いていたものである。本数は増減していない。**
- **申し送り（本版では触っていない）** —— **揺らぎを通すかの判定が、server の 2 関数を 1 つに畳んでいる** ——
  `quality: "pink"` を server は blur で処理して弾くが移植は通し、線の `dimensions` も
  server は 2 軸・移植は 3 軸を見る（台帳へ起票した）。ほか [I-269]・[I-266]・[I-275]・engine 34・
  面の層の `surfaceSeed`・楕円の周長 2 通り。
- **検証（受け入れ側がマージ後の木で測り直した）:** **Android JVM 57 クラス / 325 passed / 0 failed /
  0 errors / 0 skipped**（起点 314・**+11 = 本契約の受入 T-121〜T-131**。削除も改名も 0 件・`^e: ` 0 件）、
  **`test_android_reference_fixtures_are_current.py` 4 passed**、**参照 fixture は 1 バイトも動いていない**。
  **`server/` `web/` `cli/` `shared/` の差分は 0。**
  **⚠ `cycle.sh accept` の「テストの増減」は `android/` を数えない**（[I-270]）ので手で数えた（314 → 325）。
- **⚠ `APP_VERSION` も `web/BUILD_NUMBER` も動かしていない。** `android/` は全同期経路から
  恒久除外なので、この周は pentala へ送るものが 1 バイトも無い。

---

### v2.13.27 — 薄墨は、縞ではなく面である（Build 914、2026-08-16・render engine 36・[I-260]）

**`面: 薄墨` は等間隔の縞になっていた。**
掃きは全部平行で間隔は一定、掃きと掃きのあいだにはどの層も届かず、
**図形の中の 19.9%（四角）/ 21.1%（三角）が紙のまま残っていた**。薄墨に白地は無い。
本版は**掃きの幅を間隔まで広げ、そのぶん濃さを下げる**。

- **動かしたのは 2 つだけ** —— 掃きの幅を**間隔の 0.44〜0.74 倍から 0.88〜1.48 倍へ**、
  濃さの係数を **0.42 から 0.22 へ**。**間隔・層の数・層の角度は小数の末尾まで前の版と同じ**である。
- **白地は四角 19.9% → 0.67%・三角 21.1% → 1.09%**。**残った紙は輪郭際の縁で、深さの中央値は
  2.0 / 2.2px** である（掃きは輪郭で切られ、手の筆は端で細るので、いちばん外側の帯だけが薄い）。
- **濃さは製品の水準に戻っている**（合成した平均 α が **+2.0% / +1.1%**）。
  隙間を埋めれば濃くなるが、係数を下げたぶんで相殺されている。
  **これは濃さの好みではなく、隙間を埋めた副作用を戻す要求である。**
- **退けた 3 案は 1 つも入れていない** —— 掃き 1 本ごとに角度を振る・間隔をばらつかせる・
  下に地を敷く。**どれも白地の量を動かさず、隙間を閉じるのは掃きの太さだけだった**（前周の実測）。
  受入 2 本（層の角度と間隔の不変・地の層が 0 個）がこれを留めている。
- **代償は輪郭からのはみ出しが倍になったこと**（12.3 / 11.0px → 25.8 / 21.7px）。
  **これは掃き 1 本の幅の半分で、前の版でも同じ関係だった** —— 筆が幅を持つ以上、
  縁はその半分だけまたぐ。粒系の絶対値 20.0px は粒の大きさで決まった数字なので薄墨には当てず、
  **受入は限度を製品の定数から計算する**（px を手で書かない）。
- **参照コーパスは 588 件のうち 6 件が動いた**（`wash` を持つ 6 件ちょうど）。
  **残り 582 件は前の版とバイト一致**で、**新規 ID は 0 件なので 6 件とも判別力を持つ**。
- **⚠ 版上げの巻き添えを 1 本、消さずに割った** —— 前の版の薄墨を留めていた検査は
  「凍結が engine 34 と一致する（歴史）」と「いまの木が engine 34 の薄墨を描く（本版が必ず破る）」の
  2 つを主張していた。**前者だけを残して過去形へ改名し、後者は engine 36 の生描きの検査が引き取った。**
  **あわせて engine 35 の検査 1 本を、現行 manifest から engine 35 の凍結 manifest へ向け直した** ——
  そうしないと「engine 35 が何を動かしたか」の主張の対象が、焼いた瞬間に黙って engine 36 の差分へ入れ替わる。
- **摂動 12 本・空振り 0 本。予測 55 に対し実測 66。** 外した 5 件はすべて**過小**で、
  中でも **`max()` の片側が binding になる場所は、もう片方を壊しても測れない**という型が 1 件
  （間隔を半分にすると筆自身の太さのほうが効く掃きが出る）。
  **`display` のときだけ変える摂動だけが予測を下回った** —— 生成器の profile が `editable` なので、
  道連れになるはずの 2 本のうち 1 本が構造上動かない。
- **申し送り（本版では触っていない）** —— ハッチの外漏れ・`bleed` の限度超え（外へ出るのがその語の意味である）・
  面の質感が `line` と `arc` に付くと 1 画素も描かれないこと・`surface.density` / `surface.opacity` の既定値。
- **⚠ この周が新しく書いたコメント 103 行が日本語だったので、受け入れ側が英語へ直した**
  （2026-07-30 の裁定を 2026-08-16 に作者が再確認した）。**既にあった 216 行の日本語は動かしていない。**
- **検証（受け入れ側がマージ後の木で測り直した）:** **server 3,271 passed / 31 skipped**
  （起点 3,238・**+18 = 本契約の受入**、main 側の +15 と合わせて 1 つずつ合う）、
  **cli 227 passed**、**ruff は server / cli とも clean**、**凍結コーパスはバイト一致**、
  **Android JVM 57 クラス / 325 passed / 0 failed**。
- **Android の参照 fixture は `render-engine-36/` の 64 ファイルが増えただけで、Kotlin のソースは
  1 行も動いていない**（`android/VERSION` は据え置き）。移植は自分が名乗る版のディレクトリを読む。

---

### v2.13.28 — 上限は、それぞれ別の相手に従っている（Build 915、2026-08-16）

**制限値タブの 9 つの数は、3 つの族に分かれていた。**
族の名前は `実際に描かれる数` / `述べた数の扱い` / `読み取りと検証の天井` で、
**「何を見て決めればよいか」を言っていなかった** —— 管理者は、どの数なら機材に合わせて上げてよいのかを判断できなかった。
本版は**族を「誰に従うか」で名づけ直す**。

- **3 族の名前が変わった** —— **`この機械が描ける量`**（capability）/ **`目で数えられる限界`**（legibility）/
  **`誤入力の番人`**（safety）。**族ごとにツールチップを新設**し、そこに何に従うべきかを書いた。
- **⚠ いちばん効くのは「連動させてはならない」ことである** —— **速い機械は目を速くしない**ので、
  **`目で数えられる限界` を `この機械が描ける量` に連動させてはならない**。
  この族が保つのは閾値の数字ではなく**見え方**（一つの群れに入る墨の量）である。
- **`max_instructions` が族を移った** —— `実際に描かれる数` から **`誤入力の番人`** へ。
  **暴走を止めるための数であって、この機械がどれだけ描けるかの話ではない**。
  **本番の実績は指示 27 個が最大（中央値 4）で、既定の 64 は実在の作品に一度も当たっていない。**
- **数そのものは 1 つも変えていない。** 既定値も、効き方も、プロンプトへの書き込みも同じである。
- **個々の数の説明に実測を足した** —— 格子の墨 1 本が SVG のおよそ 13 KB（ペン）〜16 KB（太筆）にあたるので、
  **既定の 400 本でも一枚が 5〜6.5 MB になる**。
- **⚠ 検査の本数は 1 本も増えていない。** 新しい検査関数は 0 本で、**既存の `test_t9` を強くした**
  （主張が 1 個 → 7 個。**族ごとの所属**・**日英の見出しとツールチップが揃っていること**・
  **パネルがツールチップを実際に呼んでいること**まで見る）。
- **検証（受け入れ側がマージ後の木で測り直した）:** **server 3,271 passed / 31 skipped**（前版と同数。上記の理由）、
  **cli 227 passed**、**web 375 pass / `check` 261 FILES 0 ERRORS 2 WARNINGS**、
  **`lint:i18n` は 1,063 English strings / 0 errors**（**前版 1,062 から +1** = 族のツールチップを足したぶん）、
  **ruff は server / cli とも clean**。
- **⚠ この周は台帳の票を 1 つも閉じていない** —— 枝の commit も契約も台帳 ID を名指しておらず、
  **[I-244] [I-245]（制限値タブの出し分け）は別件で、いまも未決である**。

---

### 2026-08-16 — 小数 6 桁は、実装の都合ではなく仕様である（**採番なし**・SPEC のみ・[I-265]）

**SVG へ書き出す数値の小数 6 桁が、SPEC に 1 行も書かれていなかった。**
根拠は実装（`master_grid.py`）の docstring と 2026-07-24 の裁定にしか無く、
**SPEC を読んだだけでは「6 桁は仕様なのか実装の都合なのか」が分からなかった。**
**2026-08-16 の作者裁定「現時点での仕様変更は無し。6 桁が仕様である」を SPEC §19 へ明記した。**

- **コードもコーパスも 1 バイトも動いていない。**変わったのは `SPEC.ja.md`（+10 行）と `SPEC.md`（+30 行）だけで、
  **`web/` の下は 1 ファイルも触っていないので pentala へ送るものが無い**（**採番もしていない**）。
- **桁を下げない理由は、量ではなく絵である** —— 本番規模の作品を 3 桁へ落とすと**原寸 1618px で 19.52% の画素が動く**。
  ところが**フィルタの数値だけ 6 桁に残すと 0.50%** に収まる（**動かしていたのはフィルタ 2 つ・数値 4 個だった**）。
  **2 桁は座標だけでも動く**（塗りで 17.52%・本番作品で 48.57%）——
  **粗い格子は、手の揺れで散らばっていた点を同じ座標へ集める。動くのは墨の量ではなく粒の置き場所である。**
- **⚠ 起票時の「gzip と併せて現行の 14.4%」は gzip 後を gzip 前と比べた値だった** ——
  転送は既に gzip されている。**同じ gzip どうしなら 3 桁 68.1%・2 桁 55.9%** である。
- **⚠ 起票時の「1,859ms → 533ms」は桁に帰属できない** —— その版はフィルタも丸めていた。
  **表示の停止の主因は個別フィルタ 24,446 個のままである。**
- 本文は設計議論セッションが書き（作者のオーバーライド指示）、**git 管理セッションが commit した**（SPEC は常時承認の対象外）。

---

### Android — 塗りは、どちらの言い方で書かれても同じ 1 つの要求（android `2.1.4-android.35`、2026-08-16・[I-248]）

**「中を塗る」は Score の上で 2 通りに書ける** —— `filled: true` とも `surface: {"texture": "solid"}` とも。
**server はこの 2 つを 1 つの要求として扱い、判定を関数 1 つに置いている**（`fill_is_asked_for`）。
移植はその 1 つを持たず、**ローカル経路の Stage 2 が「塗り」を書くことが構造的にできなかった** ——
差し出す schema の enum にも coerce の allowlist にも `solid` が無く、**モデルは行き先の欄が
差し出したものしか書かない**からである。

**⚠ 台帳は「複製は 2 つ」と書いていたが、同じ関数を server と 1 対 1 で読み直すと 5 箇所だった。**
**票は見つけた人が見えた分しか書いていない。**

- **塗るかどうかの判定** —— 2 箇所がめいめいに式を書き、**`surface` の欄が在るだけで塗りをやめていた**。
  server は「質感が `none` でも `solid` でもないときだけ塗らない」。
  **⚠ この乖離は `solid` を待たずに既に絵に出ていた** ——
  `surface: {"texture": "none"}` を持つ `filled: true` の図形が、server では塗られ移植では塗られない
- **演奏 seed** —— server は `texture="solid"` を `filled=true`＋`surface=null` へ正規化してから鍵を作る。
  **これが無いと、導出を入れた瞬間に保存済みの全作品の筆致が振り直される**
- **相互導出** —— `filled` と `texture="solid"` を互いに導く枝（閉図形のみ）が無かった
- **coerce の allowlist**（9 語）と **Stage 2 へ渡す schema の enum**（9 語）—— 票が名指した 2 つ

**段の順序に意味がある。**語を先に足すと `solid` が流れ込む先が判定も seed も持たないので、
**「選べるようになったが、選んでも塗られない」**という別の壊れ方になる。よって
**判定を 1 つへ集め → seed を守り → 導出を入れ → 最後に語彙を広げた**（6 段）。

- **凍結コーパス 51 枚は 6 段すべてで 1 枚も動かない** —— `solid` を書いた case が 0 件・
  `texture: "none"` の case が 0 件・**coerce はコーパス照合の経路に入らない**。
  よって**受入 10 本は全部を性質として置き、動いてはいけない側の対照を対で据えた**
- **摂動 11 本。**`P-11`（要求の判定を常に偽にする）**だけがコーパスを通る** ——
  通り道は `filled: true` を書いた 5 枚で、**これが「判定に到達している」ことの現物である**
- **⚠ P-11 の予測は本数が当たったが、中身を 4 本外した** —— **赤いと読んで緑だったのが 3 本・
  読まずに赤くなったのが 3 本で、−3 と +3 が打ち消し合って 10 になった**。
  読み落としの型は「**自分が段 6 で結線した先を数えなかった**」こと。**本数の一致を根拠にしない**
- **検証**: Android JVM **59 クラス / 334 tests / 0 failures / 0 errors / 0 skipped**
  （着手前 57 / 325 から **+2 クラス・+9 本**。既存の検査は 1 本も書き換えていない）。
  `server` 側は `test_thinness_declaration_position.py` 7 passed ・
  `test_android_reference_fixtures_are_current.py` 4 passed。**`server/` は 1 ファイルも触っていない**
- **申し送り（本版では直していない）** —— **非手描きの閉図形では、塗るかどうかを誰も決めていない。**
  `renderBodyShape` を通らない道が **8 本**あり、そこでは判定の値が読まれず
  `ServerRendererStyle` が**閉図形なら常に入れる色**がそのまま出る。
  **`05_circle_rotring` は塗られないはずの円が塗りつぶされて出ている**（実装セッションが実測）。
  **51 枚の照合は `d` / `points` / `stroke-dasharray` しか比べず `fill` を見ないので、
  どの受入もこれを捕らえない。****本契約より古い乖離なので触らず、台帳 [I-280] になった**
  （**⚠ 8 本は道の数であって破れの件数ではない** —— 実測で壊れが確かめられたのは `05_circle_rotring` の 1 件）
- **⚠ 契約 §0-B の断定が 1 つ誤っていた**（発行側の誤り）——
  「塗るかどうかは `renderBodyShape` が上書きするので絵に出ない」と書いたが、
  **`renderBodyShape` を呼ばない分岐を数えていなかった。**実装セッションが着手前に見つけて凍結ノートに書き、
  完了レポートで報告した
- **採番**: Android のみの変更なので `APP_VERSION` と `web/BUILD_NUMBER` は動かしていない
  （`android/VERSION` のみ `2.1.4-android.34` → `2.1.4-android.35`）。**pentala へは何も送っていない**

---

### v2.13.29 — 道具は、それが働きかける絵の上に立つ（Build 916、2026-08-16・UI 5 周目・**5 段**）

**絵の下にあった操作バーを廃した。**
星・ハッシュ・再現・生成情報・歳時記、そして絵を持ち出す 3 つのボタンが、
**絵の下の行から、離れたところで絵に働きかけていた**。どれも画面の作品についての道具なので、
**作品の上へ移した** —— 読み手が作品に付ける印は左（詞書きトグルの隣）、残りは右（元からある全画面の左隣）。
**持ち出す 3 つは 1 つの扉になった**（SVG / PNG / 共有カード。選ぶのは扉の前ではなく中である）。

- **DDL エディタの語が、UI の言語でなく DDL の言語に従う** —— "a thin line" と書いている人に `円` を
  差し出していた。**そこから取った語は DDL に入る**ので、別言語を出すのは誤った語を出すことだった。
  **判定は server の `resolve_instruction_lang` を 1 対 1 で移植**し、**打鍵に追随する**。
  **プレビューは 2 つの文が別の言語に従う**〈作者裁定〉—— **効果の散文は読み手の言語・例文 73 件は DDL の言語**。
- **キャンバス上部の帯が SVG のサイズを言う**（作成日時の左）。**ドロワーと同じ 1 つの計測・同じ 1 つの整形**。
  **生成情報ドロワーはタブごとに閉じた位置を覚える**（閉じる 4 経路を 1 関数へ寄せた）。
  **3 桁区切りは `en-US` に固定** —— UI 言語に従わせると同じ絵が別のインターフェイスで別の句読になり、
  **画面を突き合わせられなくなる**。
- **近い作品は絵の上から系譜タブへ移った**（浮いていたものが流れの中の行になり、`position: absolute` が要らなくなった）。
- **共有対象には受け口だけを置いた**〈作者裁定・[I-191] の範囲〉—— **今日は 1 画素も描かれない**。
  **旗の有無はフィールドの存在で判定する。値ではない** —— 無い＝サーバが知らない、`false`＝知っていて未指定。
  真偽値化すると**旗を持てる作品でだけ印が消える**。
- **⚠ 段 4 までをマージしたあとに作者裁定が 1 つ出て、段 5 になった** ——
  **3 つを 1 つの扉にまとめた副作用で、シンプル UI から共有カードが消えていた**
  （**まとめた扉は半分だけ隠せない**ため、カードが SVG / PNG と同じ群に従うようになった）。
  裁定は **「シンプル UI の際は、カード共有だけを呼び出すボタンとしてエクスポートボタンを表示」**。
  **どの UI モードもエクスポートボタンを隠さなくなり**、隠す代わりに**何を開くかを狭める** ——
  その状態ではメニューを出さず**カードを直接呼び、メニュー項目と同じ 2 つの場合に押せなくなり**
  （保存されていない作品・カードの生成中）、**名乗るラベルもカードのものになる**。
  **カードは道具ではなく、作品が他人のところへ出ていく道である。**
- **受入は `T-90`〜`T-107` の 47 本**（段 1〜4 が 43 本・段 5 が 4 本）。**摂動は 24 本**（段 1〜4 が 20 本・段 5 が 4 本）。
  **実装セッションの予測は 20 本すべてで「実測のほうが多い」向きに外れた**（40 → 52）——
  型は 1 つで、**自分が置いた受入が、予測より広い面を通っていた**。
- **⚠ 3 周持ち越された摂動の穴が、本周はじめて閉じた** —— `T-16`（英語語彙の番人）は
  **`en.ts` を触る摂動が無いあいだ 1 本も道連れにならなかった**。段 4 が `en.ts` に鍵を 2 つ足したので当てられ、
  **`T-106` `T-16` `T-15` の 3 本が赤くなった**。**穴は議論ではなく実測で閉じた**。
- **書き換えた既存の検査は 9 本**（段 1〜4 で 6 本・段 5 で 3 本）。**うち主張が変わったのは 2 本だけ**で、
  どちらも共有カードの扉についてである（**T-7** と「二つの扉」）。**残りは式の移動で、主張は不変**。
- **⚠ `lint:i18n` の基準値のほうに幻が 1 件あった** —— 規則が
  **`isJapanese ? 'ja' : 'en'` という言語コードの三項式を英語の表示文字列として数えていた**。
  規則側を直し、**恒真でないこと（その三項式が木に実在すること）まで測る受入 `T-94` を置いた**。
- **検証（受け入れ側がマージ後の木で測り直した）:** **server 3,271 passed / 31 skipped**（`server/` は 1 ファイルも触っていない）、
  **cli 227 passed**、**web 422 pass / 0 fail**（段 4 までの木で 418）、
  **`check` 265 FILES 0 ERRORS 2 WARNINGS**（警告 2 件は既存の a11y）、
  **`lint:i18n` 1,066 English strings / 0 errors**（**枝の +3 と main 側の純増 +1**）、**ruff は server / cli とも clean**。
- **⚠ 画面は 1 度も見ていない** —— 接続には周ごとの裁定が要り、本周は「見ないで次の 1 件へ」の裁定だった。
  **いちばん目で確かめたいのは、角に並んだアイコン 9 個の収まりである**（寸法は計算で見積もっただけで、
  実際の窓幅・ダーク / ライトでの見え方は測っていない）。

### v2.13.30 — 作品はそれが描かれた上限で描き直され、物差しは渡されたものを縮めない（Build 917、2026-08-16・[I-154] と [I-155] の 2 契約）

**同じ作品を描き直すと、今日の設定で切られていた。**
作品の行には描いたときの上限が記録されているのに、**読み返す側がそれを 1 度も使っていなかった** ——
設定を下げた設置では、去年の作品が開くたびに違う絵になる。
**描き直しは、その作品が描かれた上限で走るようになった。**

- **答えが「どちらの数で描いたか」を名乗る** —— 応答に **`render_limits_source`** が付き、
  値は **`work`（作品の行から）／`settings`（今日の設定）／`work_unrecorded`（行はあるが上限が記録されていない）** の 3 つ。
  **`/api/render-svg` は本文を持たないので、ヘッダ `X-Inku-Limits-Source` で同じことを言う。**
  **⚠ 3 つ目が要る理由は、記録の無い古い行と「設定で描いた」を同じ顔にしないためである。**
- **注文書に `limits` を書けるようになった**（`paint` / `render-svg` / `render-score`）。
  **要素ごとに今日の設定と `min` を取る** —— **上げる方向には効かない。**
  上限は管理者のものであって、注文する側が外せるものではない。
- **9 つの上限が、効いたときに自分の名前を言う** —— 痕の各行が上限の名前で始まる
  （`represented_count_max: 600 drawn as 120`）。**1 つの上限につき 1 行だけ残す。**
  **web と cli が痕を読むようになった** —— これまで応答には載っていたが、**どちらも読んでいなかった**。
  **文言は英語のまま出し、見出しだけを翻訳する**（`plugin_warnings` の先例と同じ）。
- **⚠ 既定では絵が 1 画素も変わらない作りにした** —— Score の `note` 欄へ埋める文字列は 1 バイトも変えず、
  **名前を前置したのはリストへ積む側だけ**である。参照コーパスは 5 本とも緑で、**焼き直しは 1 件も要らなかった**。
- **プラグインの予算が設定に従う** —— これまでは管理者が何を設定しても出荷時の数で走り、**警告までその数で書いていた**。
- **⚠ 修理経路の散布密度の予算は、設定を無視して 240 で走っていた**（`max_expanded_per_instruction` の出荷時の値）。
  **本周で実効の上限を通した。既定の設定では 1 ビットも変わらないが、設定を変えた設置では挙動が変わる。**
  これは以前に見つかった 4 つの直値とは別の **5 つ目**で、[I-282] として台帳に載せた。
- **凍結された API 表面の番人 3 本には、6 つのスキーマが得た鍵を名指しで宣言した**
  （**白紙委任ではない** —— 宣言した鍵を取り除いてから digest するので、**2 つ目の鍵が来れば今も赤くなる**）。
- **受入は `T-95`〜`T-108` の 14 本**、**摂動は 14 本**（契約の 13 本 ＋ 実装セッションが肯定形の主張に当てるため 1 本を足した）。
  **予測 17 項目に対し実測 29 項目** —— 外れた 3 か所はすべて「赤くなる面を少なく見積もった」向きだった。

**そしてもう 1 つ、数える側の物差しが渡されたものを縮めていた。**
描画改善の毎巡の道具は、**数え始める前に画像を半分に縮める行**を持っていた ——
**1618px で焼いても実効は 809px** で、**表の見出しだけが「原寸で測る」という裁定に従っていた。**

- **数える規則を `shared/` の 1 本に集めた** —— `measure_png` / `measure_dir`。
  **縮小しない**（`resize` / `thumbnail` / `reduce` をどこでも呼ばない）。
  **色ごとに度数表へ畳んでから計算する**ので、画素を 1 つずつ歩かない。
- **`inku-cli measure-raster --in <PNGDIR> [--out <JSON>]` を足した。**
  **幅・尺度の旗を 1 つも置いていない** —— **幅を決めるのは焼く段（`rasterize --width`）であって、数える段ではない。**
  **旗が無いことは受入で据えた**（宣言している旗は 4 つちょうど）。
  **サーバの旗も付けていない** —— 画素を数えるのに API は要らない。
- **旧実装との照合は自分で測った** —— **4 つの固定材料は完全一致**、
  **原寸の作品 10 枚は `ground` が完全一致・他の 7 量が差 1.42e-12 以下**（色ごとに畳む加算の順序差）。
  **⚠ 縮小の向きは一定ではない** —— 同じ 10 枚の中で `strong` が上がる例と下がる例が両方出た。
- **`shared/pyproject.toml` に `pillow>=12.3.0` を宣言した** —— これまで動いていたのは
  editable install の副作用であって、宣言があるからではなかった。
- **受入は `T-109`〜`T-120` の 12 本**、**摂動は 12 本**（契約の 11 本 ＋ 実装セッションが 1 本）。
  **⚠ 摂動 1 本は空振りした**（閾値を 24 → 25 にしても、材料の画素が 24 と 25 のあいだに 1 つも無い）。
  **「結線していない」のか「材料が境界を跨いでいない」のかを分けるために、24 → 300 の摂動を足した** ——
  4 本が赤くなり、閾値が製品の判定を通っていることが分かった。
- **⚠ この道具は追跡外の評価文書を直すためのもので、そちらに受入は置けない。**
  **新しい巡が古い道具を呼ばないことは、いまは文書の側でしか担保されていない。**

- **マージは 2 本を 1 つの版に入れた**（起点はどちらも `d8d2ac0a`）。
  **衝突は 1 本だけ**（両方が同じ試験ファイルの末尾へ追記した）で、**両方を残して解決した**。
  **後から入れたほうで CLI のマニュアルを生成器で焼き直した**（59 経路・手で書いていない）。
- **検証（受け入れ側がマージ後の木で測り直した）:** **server 3,300 passed / 31 skipped**、**cli 235 passed**、
  **web 427 pass / 0 fail**、**`check` 266 FILES 0 ERRORS 2 WARNINGS**（警告 2 件は既存の a11y）、
  **`lint:i18n` 1,067 English strings / 0 errors**、**`lint:models` 68 checks**、**ruff は server / cli とも clean**、
  **凍結コーパスはバイト一致**。
  **増分の帰属は 1 つずつ合った** —— **server +29**（10 と 19）・**cli +8**（3 と 5）・**web +5**・**English strings +1**。
- **⚠ GitHub の CI は待っていない**（作者裁定・push は周の終わりであって CI は周の一部ではない）。

---

### Android — 番人は、何件比べたかを言う（android `2.1.4-android.35` のまま・**採番なし**、2026-08-16・[I-266] [I-275]）

**移植の絵は参照コーパス 51 枚と突き合わせて守られている。ところがその突き合わせのうち何本かは、
比べる相手を 1 つも見つけられないまま「一致した」と言っていた。**

理由は 2 つある。**① 番人が名指す形が engine 28 で変わった** —— 材料層の輪郭を抜き出す 2 つの正規表現は
`class="material-outline"` の完全一致を求めるが、engine 28 が層を接触断片へ割ってクラスは
`material-outline stratum-N` になった。以来この正規表現は**どちらの辺にも当たらず、空リストと空リストを
比べて緑**だった。検査は 4 枚の絵を名指していたが、そのどれについても 1 バイトも比べていない。
**② 番人が見ている枚数がコーパスの一部でしかない** —— 要素の個数とクラスの一覧を比べる照合は 10 枚しか
回らず、**弧が落とす粉（`<circle>`）はこの照合でしか測られていない**ので、残り 41 枚の粉は誰も見ていなかった。

**本周は製品の絵を 1 画素も動かしていない。動いたのは番人の到達範囲だけである。**

- **前方一致へ広げた（段 1）:** `stratum-N` を名指しせず `material-outline` で始まるクラスに当てる
  （層の数え方が変わった日にまた同じ穴が開かないため）。属性の並びの 2 択は両方残した ——
  **参照は `class` が先、移植は `points` が先**である。**広げると 4 枚で計 192 本の点列が実際に比較され、
  それでも緑だった**（42 / 40 / 50 / 60）。**つまり生き返らせても製品は直さなくてよい。**
- **番人に件数を言わせた（段 2・T-143 / T-145 / T-146）:** 抽出の件数を、**要素を 1 つずつ歩いて
  属性表を読む別の数え方**と突き合わせる。**42 / 40 / 50 / 60 は検査に書いていない**
  （手で写した数はコーパスが動いた翌日から緑のままズレを守る）。
- **`stroke-dasharray` は engine 28 でコーパスから消えていた（新しく測った）:**
  版ごとの実測で **engine 21〜25 が 252 個・26〜27 が 640 個・28〜36 が 0 個**。
  したがって全 51 枚を舐める照合の 3 つ目の assert も空対空である。**ただし完全な恒真ではない** ——
  参照が 0 個なので**移植が余計に書けば今日でも赤くなる**。死んでいるのは「参照が持つものを移植が落とす」
  向きだけで、**この非対称を消さずに 1 本の検査（T-146）で明示した**。
- **構造照合をコーパス全体へ広げた（段 3・T-147）:** 回る対象を索引 `svg_index.json` の**全鍵**にした。
  **枚名も 51 という数も書かない**ので、絵が 1 枚増えた日に自動で追随する。
  **`<circle>` は 51 枚で 790 個あり、10 枚が見ていたのは 147 個** ——
  **643 個（81.4%）が誰にも見られていなかった**（全要素では 6,571 個のうち 838 個＝12.8%）。
- **数えるタグも索引から引くようにした:** 手で書いた一覧は `path` `circle` `rect` `polygon` `polyline` `line`
  の 6 種で、**索引が数える `ellipse` と `g` が入っていなかった**。**2 枚が持つ `<ellipse>` 24 個は誰にも
  比べられていなかった** —— 51 枚へ広げるだけでは、この 24 個は盲のままだった。
- **番人が読んだ絵が索引の言う絵かを主張した（段 4・T-148）:** 51 枚それぞれで期待値側の要素数マップが
  索引の `counts` と一致することを見る。**この `counts` を読む検査は木の中に 1 本も無く、T-148 が最初の読み手である。**
- **⚠ 契約の範囲外で 1 件見つかり、直さずに起票した:** **移植は印を包む群れ（`<g>`）の数が参照と 51 枚すべてで違う。**
  参照は `id` つきの群れ（`inku_artboard` / `layer_10_content` ほか）で包み、**これらは class を持たないので
  クラス一覧の照合には映らない**。向きは一定でなく、3 つ少ない枚も 4 つ少ない枚も 9 つ多い枚もある。
  **群れは幾何を持たないので絵は 1 画素も動かない**（`d` も `points` も一致する）。
  当該検査は `g` だけを「参照 対 移植」の比較から外し、理由と実測を docstring に書いた
  （`g` は「期待値 対 索引」には入っており、そちらは 51 枚とも一致する）。
- **採番していない:** **端末の上の実装が 1 行も変わっていない**（触ったのは検査 1 ファイルだけ）ので
  `android/VERSION` は `2.1.4-android.35` のまま。`renderEngineVersion` も `ddlEngineVersion` も動かしていない。
  **`android/` は全同期経路から恒久除外なので pentala へ送るものが無い。**
- **検証（受け入れ側がマージ後の木で測り直した）:** **Android JVM 338 tests / 0 failures / 0 errors / 0 skipped**
  （XML 59 本）、**`test_android_reference_fixtures_are_current.py` 4 passed**。
  **`@Test` の総和は起点 334 → 枝 338 の +4**（T-143・T-145・T-146・T-148。**消えた検査は無く、改名も無い**）。
  **⚠ `cycle.sh accept` の「テストの増減」は 2343 → 2343 と印字したが、それは `android/` を 1 ファイルも
  数えていないためである**（[I-270]）。
- **摂動 6 本**（実装セッションが枝の上で実施・`perturb.py` 経由で 6 本とも「戻した: バイト一致 ✓」）。
  **予測を外したのは 2 件で、どちらも数え漏れだった** —— **材料層の輪郭を吐く箇所は 1 つでなく 2 つあり**
  （開いた線と閉じた輪郭）、片方にしか当てていなかった。**`dasharray` を見ている既存の番人を 1 本見落としていた。**
  **T-145 は単独の摂動では赤くできない**（engine 35 では両辺 0 個）ので、**2 つを同時に手で当てて赤くなることを確かめてある**。
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10。**そもそも Android の JVM は `checks.yml` の 4 job に無い**）。

---

### Android — 移植でも、薄墨は縞ではなく面である（android `2.1.4-android.36`、2026-08-16・[I-285]）

**移植は面の質感として 10 語をモデルへ差し出しながら、実際に描けるのは 2 語（`hatch` と `crosshatch`）だけ
だった。残る 8 語は `renderSurfaceVectors` の分岐に一つも当たらず、関数末尾の空文字へ落ちていた。**
本周はそのうち `wash`（薄墨）の 1 語を、server の render engine 36 の値で閉じる。**描けない語は 8 → 7 へ、
差し出しながら描けない語は 6 → 5 へ縮んだ**（残るのは `stipple` / `grain` / `paper_grain` / `aquatint` / `bleed`）。

- **掃きの seed と掃き 1 本を作る機構を移した（段 1）:** server の `_surface_stroke_seed` と `_surface_sweep` は
  移植に 1 つも無かった（着手前に `grep` で 0 件を確認）。`ServerRendererGeometry.surfaceStrokeSeed` と
  `DefaultSvgRenderer.surfaceSweep` として写した。**この段だけでは絵は 1 画素も動かない**（呼び手がまだ無い）。
- **`wash` の枝を engine 36 の値で足した（段 2）:** 層 2・幅の下限 0.88・幅の振れ 0.60・1 掃きの不透明度 0.22 を、
  **式に埋めず名前のある定数**（`SURFACE_WASH_LAYERS` / `SURFACE_WASH_WIDTH_BASE` / `SURFACE_WASH_WIDTH_SPAN` /
  `SURFACE_WASH_OPACITY`）として置いた。**engine 36 で動いたのは幅と不透明度の 2 つだけ**なので、
  間隔・層数・角度・輪郭での切り方は分岐点のまま移してある。
- **⚠ 移植の薄墨は、この周が広げる前から server の薄墨と同じものだった:** 基準値を server から写さず、
  engine 35 の 3 定数を一時的に置いて移植自身に描かせ、同じ道具で測った。**その値が server の凍結値と
  6 桁まで一致した**（四角の墨 0.16229907 対 0.162299・三角 0.16225059 対 0.162251・
  素の紙 0.19852839 対 0.198528 と 0.21135029 対 0.211350）。`_surface_sweep` も `_surface_stroke_seed` も
  移植に無かったにもかかわらず、**間隔・層・角度・輪郭での切り方を作る部品はすべて既に一致していた**。
- **受入 8 本を性質で置いた（段 3・T-149〜T-156）:** **凍結コーパス 51 枚に面を持つ命令は 2 件しかなく、
  どちらも `hatch` である**（着手時に 51 枚の score を全部読んで数え直した）。**`wash` の case は 0 件**なので、
  受入はコーパスを 1 枚も読まず、四角と三角の 2 形を移植に描かせて測る。
  T-149（紙がほとんど残らない）と T-150（墨の量が分岐点とほぼ変わらない）は**必ず対で置く** ——
  T-149 だけなら「全部黒く塗る」で通ってしまう。
- **⚠ T-149 の閾値 1.5% は server と同じ値を採った:** 測り方が同じ（3px 格子の point-in-polygon で、
  部分的に覆われた点は紙に落ちる）ので比較できる。**実測は素の紙が四角 0.14% / 三角 0.79%、
  分岐点が 19.85% / 21.14%** で、閾値は通るべき側から 1 桁、落ちるべき側から 1 桁以上離れている。
- **⚠ 受入 1 本が一度は恒真だった（実装が自分で見つけて直した）:** T-154 は層の数を
  `SURFACE_WASH_LAYERS` そのものと突き合わせていたので、**定数を 1 に落とすと期待値も 1 に落ち、
  まさに捕まえるべき摂動 P-3 をすり抜けた**。**分岐点の 2 を検査側に書き、定数はそれとは別に
  突き合わせる形へ直し**（`branchPointLayers`）、直した後で P-3 を当て直して赤くなることを確かめてある。
  **なお赤くなった本数は直す前も後も予測と同数だった** —— **本数の一致は中身の一致ではない**。
- **既存の対照が 1 本、4 語から 3 語へ狭まった:** `ASurfaceKeepsToItsShapeTest` の
  `testTheOtherSurfaceWordsAreUntouched` は「`wash` / `stipple` / `bleed` / `aquatint` はどれも面の線を
  1 本も描かない」を主張していた。**`wash` が描けるようになったのだから、この 1 本が赤くなるのは
  この契約が変えにいっている当の主張である**。**着手前の凍結文書にこの 1 本を予告してあり**、
  それ以外の既存の検査は 1 本も赤くなっていない。**`@Test` の本数は動いていない。**
- **⚠ 摂動 6 本のうち 5 本しか当てられなかった（報告事項）:** **P-5（`display` のときだけ掃きを矩形へ替える）は
  当てる場所が存在しない** —— **移植の `renderSurfaceVectors` は `svgProfile` を受け取っていない**。
  profile を読んでいるのは `render()` の中のローカル変数 1 つだけで、面の層まで降りていない。
  **その結果 T-156（どの profile でも同じ掃きを描く）はこの移植では構造上必ず緑であり、
  摂動で確かめた緑ではない**。主張そのものは真で、しかも server より強い（server は `use_filters` で
  profile ごとに差が出うるが、移植は差が出ようがない）。**契約が名指した当て先が現物に無かった
  ＝発行側の測定の誤りである。**
- **予測が外れた 2 件と理由:** ① **P-3 で T-154 が緑**（上記の恒真）。② **P-6（掃きの前に図形全体を 1 枚で塗る）で
  T-150 が緑** —— **墨の測定は `<path class="surface-stroke-v1">` しか読まない**ので、下敷きとして置いた
  `<rect>` は測定に入らない（server の対も同じ構造で、下敷きは別の検査が捕まえる）。
  **予測を書いたとき、自分の測定器が何を読むかを数えていなかった。**
- **採番した:** **端末の上の描画が変わった**（`DefaultSvgRenderer.kt` と `ServerRendererGeometry.kt` の 2 本）ので
  `android/VERSION` を `2.1.4-android.36` にした。**⚠ `renderEngineVersion` は `"35"` のままである** ——
  35→36 で凍結の SVG が 1 枚も動かず、かつ**移植には engine 34 の地の層がそもそも無い**（`ground` に当たるのは
  背景矩形の 3 行・server は 105 行）ため、**発行側が上げない決定をした。版の扱いは作者の裁定を仰ぐ事項として
  残っている**。`ddlEngineVersion` も動かしていない。**`android/` は全同期経路から恒久除外なので
  pentala へ送るものが無い。**
- **検証（受け入れ側がマージ後の木で測り直した）:** **Android JVM 346 tests / 0 failures / 0 errors / 0 skipped**
  （XML 60 本・`rm -rf app/build` から 1m 18s）、**`test_android_reference_fixtures_are_current.py` 4 passed**。
  **`@Test` の総和は起点 338 → 枝 346 の +8**（T-149〜T-156。**消えた検査は無く、改名も無い**）。
  **⚠ `cycle.sh accept` の「テストの増減」は 2343 → 2343 と印字したが、それは `android/` を 1 ファイルも
  数えていないためである**（[I-270]）。
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10。**そもそも Android の JVM は
  `checks.yml` の 4 job に無い**）。

### v2.13.31 — 名前で呼んだ紙は、筆の走り方を変える（Build 918、2026-08-16・render engine 37 / ddl engine 20・[I-268]）

**地の 7 種は engine 34 から敷かれていたが、痕の挙動には 1 度も届いていなかった。**
支持体を表す値は一枚きりの定数で、**受け取る口は開いているのに渡す呼び手が 0 件**だった。
**同じ記述・同じ seed でも、和紙とカンバスでは痕が違う形で出るようになった。**

- **紙は 11 の合成呼び出し点すべてへ渡る** —— 描画の入口が Score から 1 度だけ引き、引数で下ろす。
  合成に関わる関数 13 本が支持体を受け取り、**うち 10 本は既定値なしのキーワード専用**なので
  渡し忘れは実行時エラーになる。**表に無い地の名前は既定へ落とさず例外を上げる。**
- **`面: 粒` と `面: にじみ` が、線と弧の上に残るようになった**（ddl engine 20）——
  面の 9 語のうちこの 2 語だけは**内側の在り方ではなく痕の走り方**を言う。
  これまでは閉じていない図形に付いた面をすべて手前の閉図形へ移すか捨てていたので、
  **本番の 406 件（粒 283・にじみ 123）で 1 度も描かれていなかった**
  （手前へ移されていた 49 件・戻す先が無く捨てられていた 191 件・戻す先が埋まっていて捨てられていた 166 件）。
  **描き直すと、この 406 件は今日から紙を強く働かせた痕で出る。**
- **強める幅には上限がある** —— 係数 2.0 倍・**上限 3.0 倍**。和紙（吸い 2.2）× にじみ = 4.4 は 3.0 で留まる。
- **残る 7 語（`薄墨`・`紙目`・`平行線` ほか）の扱いは 1 文字も変えていない。**
  `薄墨` は 2026-08-16 の裁定で別の契約（render engine 39）が扱う。
- **参照コーパスは 597 件になり、12 件が動いた** —— 新しく足した 9 件と、既存の 3 件（和紙と薄墨地の地）。
  **足した 9 件が要ったのは、この機構の通る case が 1 件も無かったからである** ——
  `display` の case は 4 件しかなく、4 件とも `pen` で、`pen` は質感の重みを持たない。
  **`pen` では紙の違いがほとんど出ない**（到着確率が約 0.005 で、`paper` より多く吸う 2 種だけが閾値を越える）。
  足した 9 件は `brush_thick` と `chalk` で書いてある。
  **DDL 側のコーパスは 49 件で、動いたのは 1 件**（線に付いた粒が落ちなくなった case）。
- **走行中に裁定を 1 件仰いだ** —— 段が凍結 DDL コーパスを 1 件動かすことが着手前の実測で分かり、
  **`ddl_engine_version` を 20 へ採番して焼く**を選んだ。本版は 2 層の版を上げている。

**検証（受け入れ側がマージ後の木で測った。すべて緑）**:
**server 3,315 passed / 31 skipped（539.65 秒）**・**cli 235 passed（14.82 秒）**・
**web 427 tests / 427 pass / 0 fail（5.54 秒）**・**Android JVM 346 tests / 0 failures / 0 errors / 0 skipped（XML 60 本・36 秒）**・
**`check_frozen_corpora.py` 緑（32 秒）**・**ruff 緑**。
**server の +15 は枝のぶん**（受入 11・コーパス検査 3・面の語の対照 1）で、
**残りは起点以降に main 側が足したぶんである。**

- **マージの衝突は 1 本**（`coerce/normalize.py` の import 1 か所）。**main 側の上限の記録と枝側の痕の語の集合を両方残した**
  —— マージ後に両方が生きていることを名指しで確かめてある（`note_limit` の呼び出し 9 か所・素通しの分岐 1 か所）。
- **受入 11 本・摂動 14 本は実装セッションが回した**（摂動の実測 103 件・空振り 0）。
  **摂動が受入の欠陥を 2 つ見つけ、実装が直している** ——
  ① 痕の語を付けた線と付けない線を比べる形は、機構を止めても赤くならなかった（演奏 seed が命令の内容から導かれるため）。
  ② 製品の集合を回す輪は、集合を空にする摂動で空回りして緑になった。
- **⚠ 版数リテラルは契約が測った 3 か所ではなく 4 か所だった**（`test_api.py`）。DDL 側にも 4 か所ある。
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### v2.13.32 — 上げた上限が、紙まで届く（Build 919、2026-08-16・[I-132] と [I-136]）

管理者が総量の上限を上げても、紙の上の墨は増えていなかった。**上げた値を読まない場所が三つ在り、そのどれもが出荷時の数字を直に書いていた。**

- **数詞の無い敷き詰めが、上限に従う** —— 「細い線を紙一面に敷き詰める」のように数を述べない記述は、
  展開の本数を出荷時の 400 で決めていた。**上限 1200 の設置では 400 のまま**だったものが、**1200 本になる。**
  呼び出し元は 2 つあり、**両方へ渡してある**（片方だけだと、その経路を通った作品だけが古い挙動で残る）。
- **代表化の帯が、上限に対する比になる** —— 群の濃さと群の数を決める境界（既定で 180 / 80 と 500 / 240 / 120）は
  出荷時の上限 120 を前提にした定数だった。**整数比で持ち直したので、既定では 1 つも動かず、上限を動かすとその比で動く。**
- **群の数の上端が、上限に追随する** —— **本当に止めていたのは `Arrangement.cluster_count` に書かれていた `le=12` だった。**
  上限 3 倍の設置では 1 群が 360 本を抱えることになり、1 群 24 本に収めるには 15 群以上が要る。
  **静的な上限を外し、実効値（`represented_count_max ÷ 10`）を composer が tool schema へ書き戻す**形にした
  —— `count` について同じファイルが既にしている作法である。**既定ではちょうど 12 に戻り、tool JSON はバイト同一。**
  **1 群あたりの墨は、上限 1/3 でも 3 倍でも既定の帯 13.3〜24.0 に収まる**（今日は 4.4 と 42.9 で外れていた）。
- **上限の数字が、何メガバイトかを名乗る** —— ステッパが数えているのは墨の本数で、閲覧者に届くのはファイルである。
  **1 本あたりのバイトを server が送り**（`bytes_per_mark`。細い筆致 12,924・太い筆致 16,138。
  2026-08-16 に既定の 400 本そのもので実測した値）、**設定パネルが総量の欄の下にだけ `≈ 5.2〜6.5 MB` と出す。**
  **秒は出していない**（今回の計測は CLI 往復の実時間で、描画そのものの時間ではない）。
  **他の八つの欄には出ない** —— あれらは指示ごとの上限・可読性の閾値・打ち間違いの番人で、メガバイトを支配していない。
- **既定の設置では絵が 1 バイトも動かない** —— `check_frozen_corpora.py` が byte-identical。
  **`render_engine_version` も `ddl_engine_version` も上げていない。**

**凍結された API 表面の番人 4 本**は、**動いた schema が 2 つ**（`RenderLimitsStatus` が鍵を得て、`Arrangement` が境界を失った）に
なったため 4 本とも動いた。**焼き直したのは記録 1 本だけで、残る 3 本には宣言を置いてある。**
**そのうえで宣言の機構を 2 か所広げた** —— ① 既定を持たない鍵は `required` にも名前が残るので、
宣言した鍵はそこからも外す（これまで宣言された 12 個はすべて任意で、誰も踏んでいなかった）。
② **境界が「消えた」ことは増減した鍵では表せない**ので、hash の前に戻す機構を新設した。
**⚠ カードと ACL の番人は `Arrangement` を `group_size` の宣言で既に持っており、
機構が鍵の集合しか比べないので `maximum` の消失を黙って通していた。名指しの検査を 2 本足して塞いである。**

**検証（受け入れ側がマージ後の木で測った。すべて緑）**:
**server 3,350 passed / 31 skipped（517.63 秒）**・**cli 235 passed（15.73 秒）**・
**web 431 pass / 0 fail / 0 skipped（5.61 秒）**・**`npm run check` 0 ERROR / 2 WARNING**（既知の a11y 2 件）・
**`lint:i18n` 0 warnings / 0 errors**・**`check_frozen_corpora.py` byte-identical**・**ruff 緑**。
**server の +35 は枝のぶんちょうど**（新設 8 関数・35 case）で、**main 側の増分と 1 つずつ合っている。**

- **受入 8 本・摂動 11 本は実装セッションが回した**（予測を着手前に commit で凍らせてある）。**外したのは 3 本**で、
  ① 呼び出し 2 つの役割を実装前に測っていなかったぶん（+1）、② web の全走メタゲートの道連れ（+1）、
  ③ 凍結コーパスが代表化の境界 240 を 1 件も踏んでいなかったぶん（−2）。**③ はこの境界の番人が受入 1 本だけであることを意味する。**
- **⚠ マージ後に受け入れ側が web の欠陥を 2 件直した**（作者裁定のうえで枝へ commit し、2 度目の `--no-ff` マージ）——
  **① `{@const}` が要素の直下に置かれており、パネルが compile できなかった**（`npm run check` が 2 ERROR。
  **契約 §8 の試験一覧にも枝の全走にもこの面が無く、`node:test` はファイルを文字列として読むだけなので緑だった**）。
  **② `+page.svelte` が自前の型を持っており、鍵を足された側と別の型になっていた** —— **対の相方が取り残されていた。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

---

### Android — 粒は一度当てた痕であり、段は粒でできている（android `2.1.4-android.37`、2026-08-16・[I-285]）

**移植が面の質感として差し出す 10 語のうち、実際に描けるのは 3 語だった。本周で 7 語になり、
差し出しておいて何も描かない語は 5 → 1（`bleed` だけ）に縮んだ。**
入ったのは `stipple` / `grain` / `paper_grain` / `aquatint` の 4 語で、
**この 4 語が 1 周に収まるのは、server が「輪郭の内側に位置を撒き、その 1 点ごとに道具を一度当てる」という
同じ機構で描いているからである** —— 一度当てる部分（`_surface_dab`・server 73 行）は 1 度移せば 4 語が使う。

- **粒は円ではない:** 1 点 ＝ 1 筆で、道具が `rotring` や `computer` のような機械の極のときだけ円になる。
  手の道具では材質エンジンの筆致が 1 本置かれ、**幅は道具の線幅と粒の大きさの太い方**で決まる
  （幅を線幅だけで決めると面が消えることを server の docstring が実測で持つ）。**両方の道を移した。**
- **`svgProfile` を面の層まで引いた（段 1）:** server は粒 3 語・`wash`・`aquatint`・`bleed` の 4 枝で
  `use_filters` を読み、**`hatch` だけが読まない**。**移植が持っていた唯一の枝がその `hatch` だったので、
  ここまで profile を引かずに済んでいた。**`renderSurfaceVectors` の呼び出し元 11 か所すべてに渡し、
  **その 11 か所が 1 つの関数（`renderInstruction`）の中にあったので、そちらにも引数を 1 本足した**。
  **既定値は付けていない** —— 付ければ新しい呼び出し元が黙って旧挙動を選べてしまう。
  **畳んだ形は `useFilters: Boolean` で、server の 2 段の合成が `profile == "display"` ちょうどであることを
  1 対 1 で読んでから畳んだ。**
- **⚠ 前の周が意図して無条件にしていた `wash` の `filter` を profile で切った。**
  **その行は 1 バイトも出力していなかった** —— 質感フィルタが定義される道具は `pencil` / `crayon` /
  `chalk` / `brush_thin` / `brush_thick` の 5 つで **`pen` は 1 つも書かず、前の周の受入 8 本は全部 `pen` で
  回っていた**。新しい受入を `pencil` で置いたのはこのためで、`pen` のままでは摂動が空振りする。
- **`aquatint` は撒き直さない:** 一度撒いてから、その点が図形の横のどの帯に落ちたかで残す濃度を決める。
  段の番号は `aquatint-step-N` のクラスに出す。**境界を揺らした先が輪郭の外なら元へ戻す** ——
  **戻さないと四角で 1 点・三角で 2〜4 点が外へ出る**（実測。摂動 P-6 が本当に赤くなった）。
- **受入 9 本を性質で置いた（T-157〜T-165）:** **凍結コーパス 51 枚に面を持つ命令は 2 件しかなく
  どちらも `hatch` で、この 4 語の case は 0 件**なので、コーパスを 1 枚も読まず四角と三角の 2 形で測る。
- **⚠ 恒真を 2 か所で避けた:** T-158 は上限 90 を検査側に `statedMarkCeiling` として書き、
  **製品の `SURFACE_MARK_MAX` との突き合わせを別の 1 行に分けた**（前の周の T-154 が定数と比べて
  恒真になった型を避けた）。**上限だけでは「上限がいつも効いている実装」が通る**ので、
  薄い面（density 0.05・実測 30〜32 本）と濃い面（density 1.0・実測 89〜90 本）を対で置いた。
  T-161 は score が要求した `tone_steps` そのものと突き合わせ、製品の定数を 1 つも読んでいない。
- **⚠ 実装が着手前の自分の予測を 1 つ訂正した:** 予測は「上限 90 は契約の三角では効かないので
  大きい形が要る」と書いたが、**面積係数を決めるのは三角の面積ではなく `shapeBbox` の `w * h` である**。
  **四角と三角は同じ bbox なので係数も同じ 1.0755 で、どちらも density 0.55 で上限に当たる。**
  大きい形は要らず、契約どおりの 2 形で回している。
- **⚠ 3 語が同じなのは面の層だけである:** T-159 は当初「3 語の SVG が完全一致」を主張する予定だったが、
  **命令自身の seed は命令の dump のハッシュで、その dump に texture の語が入っている**ため
  図形の輪郭の筆致が 3 語で違う。**server も同じ形なので移植の欠陥ではない。**
  T-159 は面の層が書いた要素だけを突き合わせる（実測: 四角 89 要素・三角 90 要素が 3 語とも同一）。
- **⚠ `pencil` は面の層でない `<circle>` を 66 個書く:** 材質層が書くその円はクラスを持たず、
  **移植の粒の円も server と同じくクラスを持たない**ので markup では区別できない。
  **契約は T-160 を `pencil` で名指していたが `pen` に替えた** ——
  `pen` は手の道具でありながら円を 1 つも書かず、`rotring` は書く円が全部粒である。
  **2 つの道を曖昧さなしに読めるのはこの対だけである。**
- **既存の対照が 1 本、3 語から 1 語へ狭まった:** `ASurfaceKeepsToItsShapeTest` の
  `testTheOtherSurfaceWordsAreUntouched` から `stipple` と `aquatint` が外れ、`bleed` だけが残った。
  **これは退行ではなく、本契約が変えにいっている当の主張である**（契約が着手前にこの 1 本を名指しで予告した）。
  **`@Test` の本数は動いていない。**
- **摂動 7 本のうち 6 本が予測どおり・1 本が外れた:** **P-4（`usesHandStroke` の分岐を殺す）は 1 本の予測に対し
  3 本が赤くなった。**外した理由は絵ではなく**検査の読み方を数え落としたこと**である ——
  T-157 と T-162 は機械の極を `<circle>` で読むので、機械の極にも筆致を通らせると
  **円が 1 つも出ず「痕が 1 つも無い」で落ちる**。**赤くなった理由は「中心が外に出た」ではなく
  「読む先が空になった」である。****予測に関数名だけを書いて読み方を書かなかったことが直接の原因。**
- **⚠ 契約の摂動の予測が 2 か所ずれており、実装が着手前にそれを見抜いていた:**
  ①**P-3 で T-164 も赤くなる**（契約は 2 本しか書いていなかった。T-164 は 4 語すべてを回るので、
  何も描かなくなった `paper_grain` は `display` でもフィルタを出さない）
  ②**P-7 は前の周の `AWashIsAFieldTest` に届かない**（契約は「届きうる」と書いた。
  T-156 が読むのは掃きの幅と不透明度だけで `filter` を 1 度も見ず、しかも `pen` で回っている）。
  **どちらも凍結ノートに理由つきで予告してあり、実測もそのとおりだった。**
- **採番した:** **端末の上の描画が変わった**（`DefaultSvgRenderer.kt`）ので `android/VERSION` を
  `2.1.4-android.37` にした。**⚠ `renderEngineVersion` は `"35"` のまま**（発行側の決定が生きており、
  **版の扱いは作者の裁定を仰ぐ事項として残っている**）。`ddlEngineVersion` も動かしていない。
  **`android/` は全同期経路から恒久除外なので pentala へ送るものが無い。**
- **検証（受け入れ側がマージ後の木で測り直した）:** **Android JVM 355 tests / 0 failures / 0 errors / 0 skipped**
  （XML 61 本・`rm -rf app/build` から 44s）、**`test_android_reference_fixtures_are_current.py` 4 passed**。
  **`@Test` の総和は起点 346 → 枝 355 の +9**（T-157〜T-165。**消えた検査は無く、改名も無い**）。
  **⚠ `cycle.sh accept` は「2370 → 2358」と印字して 12 本が消えたように見せたが、
  その 12 本は起点の後に main へ入った [I-132] の分で、枝が持っていないだけである**
  （`android/` は 1 ファイルも数えていない → [I-270]）。
- **⚠ 範囲外で 1 件見つかり、直さずに起票した:** **移植の `wash` と `hatch` は面の seed を
  `(0, 0)` で採る**（server は `(ins_idx, mark_idx)`）。**`surface.seed` が書かれていないとき、
  `arrangement` で展開された痕が全部同じ質感になる。**本周が足した 2 枝は server と同じ形なので、
  いま移植の中に 2 つの作法が同居している。**絵の差は未測定。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10。Android の JVM は `checks.yml` の 4 job に無い）。

### v2.13.33 — 質感は痕 1 本ずつではなく、続きの塊に掛かる（Build 920、2026-08-16・[I-264]）

1 枚の作品が **24,446 個の個別フィルタ参照**を持ち、表示時の停止時間の約 8 割を占めていた。
**同じ道具で続けて引かれた痕は、同じ質感を 1 本ずつ抱えている**ので、続きの塊を群 1 つにまとめれば
掛かる回数は約 12 分の 1 になる。

**⚠ 走行中に作者が裁定して、この改修の置き場そのものが変わった。**
契約は「Renderer が畳んだ SVG を出す（render engine 38）」という形だったが、
**実装が本番の 2 幅で測ったところ、契約が要求する「256px と 2160px の両方で速い」参照本数の閾値は
34〜26,675 参照のどこにも無かった** —— **向きを決めているのは参照本数ではなく焼く幅**である。

- **回数と面積が逆を向く** —— 畳むと掛かる回数は約 12 分の 1 になる代わりに、
  **掛かる面積の合計が 1.35〜1.41 倍に増える**（群の外接矩形は、置き換える要素の外接矩形の和より大きい）。
  **回数が効く小さい幅では速く、面積が効く大きい幅では遅い。**
  **「面積が増えない塊だけ畳む」も測って捨てた** —— 畳んで得をする塊がまさに面積の増える塊で、
  面積制限を掛けるとバイト数はむしろ増えた（12,292 参照で 4,764,476 → 4,770,076）。
- **裁定は「焼くときに幅で決める」**（2026-08-16・選択 UI）。**保存する SVG は 1 バイトも変えない。**
  したがって **`render_engine_version` は 37 のまま**で、参照コーパスも焼き直していない。
- **畳み込みは `shared/src/inku_analysis/texture_fold.py` に置き、`svg_to_png` が幅を見て自分で掛ける**
  —— **呼び出し点ごとの旗にしない**（旗は忘れられる）。**上限は 512px** で、
  本番のサムネイル 2 幅（256px と hidpi 512px）はどちらも入り、PNG 書き出しの既定 2160px とブラウザの表示幅は入らない。
- **塊の切れ目は 3 つ** —— 群の境・質感の無い痕・別の道具。**痕の並びは 1 つも動かない**
  （塊は文書の中でその場を置き換えるだけ）。**塊が 1 本のときも包む** ——
  描かれ方は同じで、「畳んだ文書には要素ごとの質感参照が 1 本も残らない」という読める性質になる。

**速さ（同じ周の中で取った対。正味 ＝ 現行 ÷（塊ごと ＋ 畳み込み自身））**:
**256px で 1.574〜1.973 倍**、**512px で 0.955〜1.207 倍**、**2160px は 0.79〜0.95 倍（＝遅い）**。
**受入が時計で断定しているのは 256px だけ**で、512px は雑音より狭いため性質でしか測っていない。

**絵の差（原寸 1:1・平均差 /255）**: 参照 127 / 378 / 4,616 / 12,292 に対し
**256px で 0.058 / 0.184 / 2.010 / 3.685**、**512px で 0.039 / 0.115 / 1.383 / 2.575**。
**作者が原寸のコンタクトシートで判定し、問題なしとした**（2026-08-16）。

**⚠ 既存のサムネイルは焼き直されない。** 陳腐化の判定は `source_render_hash` で、
**保存する SVG が動かない以上その hash も動かない。**
**速くなるのはこれから焼くぶんと、明示的な再構築（`/api/settings/thumbnails/rebuild`）だけである。**

**検証（受け入れ側がマージ後の木で測った。すべて緑）**:
**server 3,370 passed / 31 skipped**（489.93 秒）・**cli 235 passed**（14.95 秒）・
**ruff（server / cli / shared）緑**・**`check_frozen_corpora.py` byte-identical**。
**server の +20 は枝のぶんちょうど**（受入 T-129〜T-138 が 20 本）。**消えた検査は 0 本。**

- **受入 10 種 20 本・摂動 8 本は実装セッションが回した**（予測は着手前に凍らせ、
  **裁定で設計が変わった後の予測も、コードを書く前に凍らせ直してある**）。
  **外れたのは 2 本**（P-2 が 2→4・P-4 が 1→8。どちらも「畳み込みの出力を読む受入すべてに届いた」型）。
- **⚠ 参照コーパスはこの改修を 1 件も見られない** —— P-7（`render()` にも畳み込みを掛ける）で
  **8 本が赤くなったが、コーパスは 0 件**だった。**588 件のうち `display` は 4 件で、4 件とも `pen`、
  `pen` は質感フィルタを持たない**ためである。**engine 37 の 597 件でも同じだった。**
  **畳み込みが Renderer 側へ漏れたことを見ているのは受入 1 本だけである**（台帳 [I-289] に起票済み）。
- **⚠ 2160px の向きが、発行側の計測と逆に出ている** —— 発行側は 24,445 参照で 1.4 倍速いと測り、
  実装側は 26,675 参照で 0.79 倍（遅い）と測った。**どちらが正しいかは決めていない**（台帳 [I-290] に起票済み）。
  **本版はどちらの向きでも 2160px を畳まないので、この未決着は絵にも速さにも効かない。**
- **⚠ 受け入れ側が摂動を 1 本足した** —— **走行中の裁定で中身が入れ替わった受入のうち、
  T-136（畳み込みを通らない道が無い）に実装の摂動が 1 本も当たっていなかった。**
  ラスタライザの外へ backend の import を 1 行足す摂動で、**予定どおり 1 本だけ赤くなった。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

---

### Android — 滲みは縁で起こり、痕はそれぞれ自分の質感を持つ（android `2.1.4-android.38`、2026-08-17・[I-285] [I-288]）

**移植が面の質感として差し出す 10 語のうち、差し出しておいて何も描かない語が 1 → 0 になった。**
最後の 1 語 `bleed` が入り、`gen_android_reference.py` の docstring が言う
「移植は描けない地を Stage 2 へ差し出してはならない」と、移植のスキーマが差し出す語の一覧が、
**10 周かけて初めて一致した**（8 語 → 3 語 → 1 語 → 0 語）。
**同じ周に、面の seed の作り方を server と揃えた。**

- **滲みは縁の話である:** server は engine 15 まで bbox の中心に楕円を 1 個置いており、
  三角にも雲形にも同じ楕円が出て**端はどこも滲んでいなかった**。今の機構は輪郭そのものを外へ押し出した
  帯を 3 本重ね、**押し出す量が頂点ごとに揺れる**ので同心の輪郭ではなく染み出しとして読める。
  **内側の輪は輪郭に重なる**（`level` が `ring / (輪の数 - 1)` なので `ring == 0` で 0）。
  **移した実体は 64 行**（`renderer.py:3979`〜4042。**台帳と前の周の契約は「66 行」と書いていたが、
  境界の取り方が違い実数は 64 行だった**）。
- **新しく作った部品は定数 1 つだけだった:** `SURFACE_BLEED_RINGS = 3`。
  `centerlineNormals` `pointsCenter` `usesHandStroke` `synthesizeAlong` `surfaceStrokeSeed`
  `gridStepPx` `contourStrokePath` と材質フィルタの属性は**すべて前の周までに移植へ入っていた**。
  `useFilters` の搬送路も前の周の段 1 が引いてあったので、**この周は新しく引いていない**。
- **2 つの道を両方残した:** 機械の極（`rotring`）は class を持たない `<polygon>` を 3 本、
  手の道具は `<path class="surface-stroke-v1 bleed-ring-N">` を 3 本。
  **押し出しの向きは法線の多数決で決める**（符号を定数にしていない —— 図形によって法線の向きが揃わない）。
- **面の seed を枝に入る前に 1 度だけ作るようにした（[I-288]）:** server は
  `_render_surface_texture` で 1 度だけ作って全枝へ渡すのに対し、**移植は枝の中で 4 回別々に作り、
  そのうち `wash` と `hatch` / `crosshatch` の 2 回が痕の番号を捨てていた**。
  **製品側の呼び出しは 4 か所 → 1 か所**になり、**移植の中に同居していた 2 つの作法が 1 つになった**。
  これから足す枝が誤った作法を選べなくなる。
- **⚠ 契約が書いた前提が 1 つ、実装の実測と食い違った:** 契約は
  「`(0, 0)` を渡すと `arrangement` で展開された痕の面が**全部同じ質感になる**」と書いたが、
  **展開された痕は座標が書き換わり、その座標が seed の材料（命令の dump）に入っている**ので、
  **`(0, 0)` のままでも痕ごとに別の seed になっていた**。
  **乖離そのものは実在し、効いていなかったのは `insIdx` のほう** ——
  述べられた欄がすべて同じ命令を score に 2 本置くと、2 本を分けているのは索引だけになる。
  **受入はこの 2 段で据えた**（`arrangement` の側は契約の文言どおり・判別力を持つのは同じ命令 2 本の側）。
  **server も同じ構造なので、`mark_idx` は server 側でも座標と冗長である** ——
  Android 契約の中では直さず台帳へ起票した（[I-291]）。
- **受入 10 本を性質で置いた（T-166〜T-175）:** **凍結コーパス 51 枚に `bleed` の case は 0 件**、
  **`arrangement` と面を同時に持つ命令も 0 件**なので、コーパスは 1 枚もこの改修を測れない。
  **T-166 は既存の対照の置き換えである** —— `bleed` が何も描かないと主張していた検査は、
  **一覧から 1 語を外すと空回りの恒真になる**ので、主張を裏返して
  **「スキーマが差し出す語はすべて描く」**にした。**語の一覧はスキーマから読み、検査に焼き付けていない。**
- **⚠ 痕ごとの比較は機械の極で回している:** 手の道具は標本を原点に固定された格子へ量子化するので、
  **同じ質感を着ていても 2 つの痕が平行移動の関係にならない**（実測: `pen` は明示 seed を与えても
  24 個の掃きが 24 通りに散り、`rotring` では 24 個が 8 通りに落ちた）。
- **満点は 355 → 364（+9）:** 増えたのは 9 本で、T-166 は既存 1 本の置き換えなので数に入らない。
  **消えた検査は 1 本、その 1 本が T-166 に置き換わった。**
  **マージ後の木で XML 62 / tests 364 / failures 0 / errors 0 / skipped 0**、
  `test_android_reference_fixtures_are_current.py` **4 passed**。
- **摂動 9 本は実装セッションが枝の上で回した**（予測は着手前に凍結・overlay `cd678deb`）。
  **外れたのは 1 本** —— P-1（輪を 3 → 2 本）の予測が 1 本に対し実測 4 本で、
  **「輪が 3 本ある」を前提として読んでいた受入が他に 3 本あった**（型は「検査が何を読むかを数え落とす」）。
- **⚠ `synthesizeAlong` に `support` を渡していない** —— これは本周が作った乖離ではなく、
  **移植の 9 か所すべてが渡していない既存の作法**である（地の層そのものが移植に無い）。
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### v2.13.34 — 読み手が、帯に何を出すかを決める（Build 921、2026-08-17・UI 6 周目）

作者が 7 件をまとめて指示し、対話で 1 件ずつ決めながら直した周である。末尾に
「サーバーコード、DB の変更が必要なものはオーバーライド」が付され、**web で閉じない 4 件が越境した**。
うち 1 件（無指定時の写生の既定を「細かい」にする）は同日に取り消された。

- **履歴帯に添える情報を、読み手が選べるようになった:** 設定の`その他`に **世代・モデル・engine バージョン・
  ファイル容量**の 4 択を置き、**最大 2 つまで**選べる。**0 個も答えである** —— 何も選ばなければ帯には絵だけが並ぶ。
  **要になった規則は「不在」と「空リスト」を別物として扱うこと** —— 列を持たない口座は**未回答**なので
  既定（旧来の固定 2 つ）を取り、4 つとも外した読み手は**回答しており、答えは「何も出さない」**である。
  **片方の falsy 判定に畳むと「何も出さない」が保存できない設定になり**、再読み込みで既定へ戻る。
  **そしてその壊れ方は、正常系だけを見る検査を 1 本も赤くしない。**
- **帯が出すファイル容量が、表示中の 1 件以外すべて `0 B` だった:** 帯を埋める一覧は `include_svg=false` で
  取るのに対し、**server は鍵を消さずに空文字にして返す**ので、ブラウザ側は**空にした結果を測っていた**。
  **API が `svg_bytes` を送る形にした** —— UTF-8 バイトで、`measureSvgWeight().bytes` と
  `svg_weight.py` の `measure()` と同じ量である（**どちらの数え方にも触っていない**）。
  **⚠ 最初は `HistoryPostBody` に置いてしまい、保存時に呼び手が申告できる鍵になっていた** ——
  容量は server が保存済みの作品について報告する事実なので `HistoryItem` へ移した。
  **見つけたのは API サーフェスの番人である。**
- **履歴管理の検索が、全長の render hash を受け付けるようになった:** それまでは**4 文字ちょうど**しか
  hash と認めておらず、全長を貼ると本文検索（FTS）の道へ落ちて 1 件も見つからなかった。**下位 4 桁は従来どおり。**
- **AI 自動推敲が `invalid refinement advice JSON` で毎回落ちていた:** フェンス除去の正規表現が
  **生文字列の中で `\s` と書かれており**、生文字列ではそれは「バックスラッシュ 1 個 ＋ `s` の 0 回以上」であって
  空白ではないので、**一度も一致していなかった**。gemma はフェンスを付けて返すため、毎回そこで落ちていた。
  **⚠ 最初の直しは冗長だった** —— フェンス除去と波括弧抽出の 2 段にしたが、
  **フェンス付きの入力では波括弧抽出が必ず同じ結果を出す**ので、フェンス側を壊しても赤くならない。
  **「既存の道と必ず一致する配線には判別力が無い」**ので、**最初の `{` から最後の `}` までという 1 本の規則**へ畳んだ。
- **AI 推敲で選んだモデルが、描画にも効くようになった:** それまで選択は**助言の 1 呼び出しにしか届いておらず**、
  絵はページの設定のモデルで描かれていた。実行中の表示も、描いているモデルを名乗る。
- **バッチ実行中に、タブの「バッチ」が改行しなくなった:** 折り返していたのは進捗の数字ではなく**タブの語**だった。
  実測では `(3/12)` でタブ 38px・語 1 行、**`(12/12 ↻2)` で 46px・語 2 行、`(120/120 ↻2)` で 58px・語 3 行**。
  直したあと 38px・1 行に戻り、**パネル幅 300px まで健全**である。
- **空キャンバスの絵を、山・水・月の三筆へ描き直した:** 比に引きずられない機構は 2026-08-12 に既に入っており
  **壊れてはいなかった**ので、作者裁定で「図形そのものを描き直す」を選び、題材も作者が選んだ。
  **9 比すべてを焼き 5 比を目視した**（square / portrait / wide / banner 5:1 / pillar 1:5）。
- **DB に 1 列足した:** `user_accounts` へ
  `history_strip_fields TEXT NOT NULL DEFAULT '["generation", "model"]'`。
  **既定は「列ができる前に帯が出していたもの」なので、配備の日に誰の帯も動かない。**
- **満点はマージ後の木で server 3401 passed / 31 skipped / web 446 / cli 235 passed。**
  **`npm run check` は 268 FILES / 0 ERRORS / 2 WARNINGS**（既知の a11y 2 件のまま）。
  **増分は両側とも加法的に一致した** —— server の test 関数は起点 1821 に対し
  **main 側 +15（[I-264] の畳み込みの検査）・枝側 +15 でマージ後 1851**、
  収集数は **枝 +31・main 側 +20 でマージ後 +51**（関数の増分と収集数の増分は別の量である）。
- **摂動は実装セッションが枝の上で 13 本回し、13 本とも赤くなった**（外れたのは P-3 の 1 本で、予測 2・実測 4。
  文字クラスに `g` を足したため、桁数を緩めた 3 case に加えて `g`×64 の case も赤くなった）。
  **⚠ 受け入れ側で 1 本足した** —— **API サーフェスの番人へ宣言を足す段にだけ摂動が当たっていなかった**ので、
  製品の `HistoryItem` へ宣言していない鍵を 1 つ足したところ、**番人 2 本が予定どおり赤くなった**。
- **⚠ その摂動で分かったこと**: `test_the_groups_decide_what_you_may_do.py::test_t8` の
  `HistoryItem` の宣言は**不活性である** —— **凍結された 78 のスキーマ名に `HistoryItem` が入っていない**ので、
  その行は何も測っていない。**`catalog_mode` を宣言した時点から不活性で、本周はそこへ 1 語足しただけ**である
  （**他 14 の宣言はすべて生きている**）。**同じスキーマは他の番人 2 本が押さえているので、穴は開いていない。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### Android — 場所を述べていない図形は、外接矩形を持たない（android `2.1.4-android.39`、2026-08-17・[I-269]）

**`ServerRendererGeometry.shapeBbox` の 4 枝が、server なら枝に入らない入力を既定値で埋めて答えていた。**
server の `_shape_bbox` はどの枝でも**図形を置く 2 つの欄が両方述べられているときだけ**枝に入り、
片方でも欠ければ枝を降りて `return None` に落ちる。**移植はそれを既定値（`0.5` `0.12` `0.38` `0.24` ほか）で
埋めて矩形を返していた。****本周でその 4 枝を server と 1 対 1 の門にした。**

- **直した 4 枝:** `circle`（`center` と `radius`）・`ellipse`（`center` と `size`）・
  `square` / `triangle`（`position` と `size`）・`polygon`（`center` と `radius`）。
  **`cloudform` は既に正しかったので式を触っていない。**
- **`polygon` は二重の代用を両方落とした:** 移植は `center` が無いとき `position` + `size` から中心を組み立て、
  `radius` が無いとき `size` から半径を作っていた。**server はそこから組み立てない**ので、両方外した。
  **`position` と `size` を述べている多角形でも、`center` か `radius` を述べていなければ箱は無い。**
- **falsy 判定にしていない:** 配列の欄は `optJSONArray(...) == null`、数の欄は `isNull("radius")` で見る。
  **`[0, 0]` の `center` と `0` の `radius` は「述べている」として通る**（server の `is not None` と同じ判定）。
- **⚠⚠ 契約が前提として書いたことは、実装が測ると偽だった:** 契約は
  「`null` と『既定値で埋めた矩形』の差は、面を描くか描かないかの差になる」と書いていた。
  **そうなっていない。**面の層 `renderSurfaceVectors` は箱を読む前に `surfaceContour`
  （`DefaultSvgRenderer.kt:1962`）を呼び、**そこが同じ欄を、同じ書き方で、手写しで要求している。**
  満たさなければ `null` を返し、質感の各枝は `if (contour == null || contour.size < 3) return ""` で先に落ちる。
  **実測: `circle` の枝を直す前の形へ戻して同じ score を描いても、SVG は 11,792 バイトのまま 1 バイトも動かない**
  （面の行は前後とも 0）。**箱の門は 1 度も binding ではなく、欄を欠く図形に面が描かれたことは無い。**
  **直したのは「この関数が返す答え」であって、紙の上の痕ではない。**
- **⚠ したがって「これで面の描かれ方が変わった」とは書けない。**
  **同じ判定が 2 か所に手写しで在り、片方だけが効いている**という状態が残った。
  **畳むか、2 つが同じ答えを返すことを受入で縛るかは裁定待ちで、起票した**（未採番）。
- **受入 6 本（T-182〜T-187）:** 4 枝それぞれについて「欠けたら箱が無い・揃えば持つ」の対照（T-182〜T-185）、
  箱の無い図形に面が描かれないこと（T-186）、**欄が揃っている図形の箱は値まで今までどおり**（T-187・
  4 枝の箱の 4 つの数を検査側に書いて 2 紙で突き合わせる）。**凍結コーパスは 1 枚も読んでいない**
  （51 枚に欄を欠く case が 0 件）。
- **⚠ 摂動 7 本のうち 3 本が予測を外した:** P-1 / P-2 / P-3 は「T-186 も赤くなる」と予測したが、
  **実測は当該の 1 本だけ**だった。**理由は上の重複** —— 箱を元に戻しても `surfaceContour` が先に落とすので、
  面の行は 0 のままである。**T-186 は消していない**（主張は真で対照も効いている）が、
  **判別力が無いことを実測つきで KDoc に書いた**。**この改修を実際に縛っているのは T-182〜T-185 と T-187 の 5 本。**
  **型は「検査が何を読むかを数え落とす」**（前の周と同じ型で、2 周続けてここで外している）。
  **P-4 と P-5 は `polygon` の 2 つの代用を別々に戻して、どちらも単独で T-185 を赤くした。**
  **P-6 は既存の T-87 だけを赤くし、本周が `cloudform` に手を出していないことを示した。**
- **満点は 364 → 370（+6）:** **マージ後の木で XML 63 / tests 370 / failures 0 / errors 0 / skipped 0**、
  `test_android_reference_fixtures_are_current.py` **4 passed**。**着手前に凍らせた予測との差は 0。**
- **`cloudform` の枝の上にあったコメント 5 行は消えている** —— 内容（「server は両方を要求し、
  無ければ `None` を返す」）が**いま 5 枝すべてに当てはまる規則**になったため、関数全体の docstring へ移した。
  **枝の式・条件・既定値は 1 バイトも変わっていない**（T-87 は P-6 でだけ赤くなる）。
- **⚠ 受け入れ側で凍結ノートを公開ツリーから外した** —— 実装は `android/` の下に凍結ノートを
  1 ファイル置いていたが、**凍結ノートは 20 件すべて overlay 側に置く作法**なので、現物を
  overlay の契約置き場へ写して公開ツリーから削除した（md5 一致を確認）。
  **凍結した事実そのものは commit `593f90a6`（07:46:28）が持ち、最初の製品 commit `334f6399`（07:49:14）
  より 2 分 46 秒早い。**
- **起票 2 件（同日に採番され [I-299] と [I-295] になった）:** ①server は欄を欠く命令を 7 primitive で `ValueError` にするが移植は既定値で描く
  （**本周は面の門だけを直したので「図形は描かれるが面は描かれない」中間状態が残った**）
  ②上の門の重複。**どちらも裁定待ち。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### Android — 番人は、索引が宣言した道具立てで描き、色を突き合わせる（android `2.1.4-android.40`、2026-08-17・[I-280]）

**51 枚の凍結された参照 SVG と照合する番人が、索引 `svg_index.json` の宣言のうち 3 つ
（`color_catalog_id` / `fill_colors` / `stroke_colors`）を 1 行も読んでいなかった。**
**照合の足場は `colorCatalogId = "default"` を直書きしており、索引が `default` 以外を名指す 5 枚は、
参照を作ったときと違う道具立てで描き直されて比べられていた。**
**色は 1 度も比べられていなかった**ので、**[I-280]（機械の極が塗るかどうかを誰も決めていない）は
どの番人にも映っていなかった。**

- **索引を読む道を 1 つだけ作った:** 新規 `ReferenceRendering.kt`（`index()` / `entry(key)` /
  `catalogId(entry)` / `request(entry)` / `svg(entry)`）。**`catalogId` は `getString` で読み、
  既定値を置いていない** —— 索引に鍵が無ければ落ちる。
  **参照と突き合わせている 6 か所すべてを結線した**（`DefaultSvgRendererPhase2f` / `Phase2e` /
  `ServerRendererCloudformAndRelations` / `CornerShapeMaterialLayer` / `DefaultSvgRendererFillDab` /
  `GroupMembersReachEachEngine`）。**`Phase2f` の 36 行の複製が 1 行の委譲になった。**
- **⚠ 段 0 で「段 1 だけを当てると何が赤くなるか」を測った** —— **0 本**。
  **色を含まない量しか比べていなかったことが実測で裏づいた**（`d` / `points` / `stroke-dasharray` と
  class・要素数）。**色が食い違った case は 51 枚中 1 枚**（`05_circle_rotring`・`fill` のみ）で、
  **`stroke` の食い違いは 0 件**、**参照 SVG と索引の宣言の食い違いも 0 件**。
- **[I-280] を 1 行で閉じた:** `DefaultSvgRenderer.renderInstruction` の
  `val fill = if (ServerRendererGeometry.fillsInterior(ins)) attrs.fill else "none"`。
  **判定は移植へ既に入っていた `fillsInterior`（server の `_fills_interior` の 1 対 1 の移植）を
  読んだだけで、写していない。** **同じ出力を書く 8 か所すべてが 1 つの判定を通る**
  （幾何の道の 7 か所と `polygon()` ヘルパ）。**手描きの道 `renderBodyShape` は
  `regionFill` で既に正しかったので触っていない。**
  **`@Suppress("UNUSED_VARIABLE")` で置かれていた死んだ探り針 1 つが消えた。**
- **受入 6 本（T-176〜T-181）:** ①番人が索引の宣言したカタログを渡すか ②`fill` の色が宣言と一致するか
  ③`stroke` の色が一致するか ④**番人が何枚比べたかを言う** ⑤機械の極が求められていない塗りをしないか
  ⑥**索引の宣言を差し替えると描かれる色が変わるか**。**51 枚を回る形で置いてある。**
- **⚠ 恒真化を 2 通り塞いだ:** ④は**「比べた側」ではなく「移植が実際に描いた側」で数える** ——
  宣言の側で数えると移植が何も描かなくても 51 のままになる。**比較そのものが「比べたかどうか」を返し、
  walk がそれを数える**（**摂動 P-4 が、51 枚を回りながら 1 枚も比べない状態を作って実測した**）。
  ⑥は**46/51 が `default` を宣言している**ため、**宣言を無視する道でも ②③ は 46 枚で緑になる** ——
  それを塞ぐ 1 本である。**差し替えたのは記憶の上の `JSONObject` で、凍結ファイルは書き換えていない。**
- **⚠ 摂動 6 本のうち 1 本が予測を外した:** P-2（製品がカタログ id を使わない形へ戻す）は
  予測 3 本に対し**実測 13 本** —— **既存の 10 本が道連れになった**。
  **原因は予測を書くときに `colorCatalogId = "` のリテラルで数えたこと** ——
  **その 10 本はカタログ id を変数や `RefinementPlan` / repository 経由で渡しており、
  リテラルの形では 1 件も一致しない**。**当て先が製品の広い場所なら、読み手は
  「その id を書いた行」ではなく「その値を消費する検査」で数える。**
  **⚠ 層の見分けは崩れていない** —— **P-1（番人が索引を読むか）では自分の 4 本だけが赤くなり、
  P-2 では T-176 が緑のまま残る**。**この 2 つは実測で別のものとして分かれた。**
- **⚠ 発行側（契約）の断定が 1 つ実測と食い違った:** 契約は `05_circle_rotring` と
  `12_cloudform_rotring` の**両方**が塗りで宣言と食い違うと述べていたが、
  **`12_cloudform_rotring` は着手前から一致していた**。**`ServerRendererStyle` の閉図形の集合が
  `cloudform` を含まない**ため、その `fill` は今日すでに `"none"` だった
  （**`ServerRendererGeometry.CLOSED_SHAPES` には入っており、2 つの集合が食い違っている**）。
- **満点は 364 → 370（+6）。2 本を入れた木で XML 64 / tests 376 / failures 0 / errors 0 / skipped 0**、
  `test_android_reference_fixtures_are_current.py` **4 passed**。**凍結コーパスは 1 バイトも書き換えていない。**
- **⚠ 起点が周の途中で動いた** —— 別のセッションがこの作業場の枝を main の先端へ fast-forward した
  （`024df278` → `97400d43`）。**`android/` の部分木のハッシュは両者で同一**なので、
  着手前の満点はそのまま有効である。**受け入れは `97400d43..先端` で読んだ。**
- **申し送り 1 件を起票した（同日に採番され [I-298] になった）:** **`ServerRendererStyle.strokeAttrs` の `fill` は
  server の `do_fill` を読んでいない** —— ①塗らない図形に `fill-opacity` が付く
  ②同じ要求を `surface.texture="solid"` で書くと `cloudform` の塗りが消える。
  **どちらも凍結コーパスには映らない**（`fill-opacity` を動かす `color_hint` は 0 件・
  `solid` の `cloudform` は 0 件）。**潜在の乖離であって今日の絵の破れではない。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### v2.13.35 — 線に付いた薄墨は、太くて淡い掃きである（Build 922、2026-08-17・[I-279] [I-289]）

**記述が「面: 薄墨」と書いた線と弧に、薄墨がようやく出るようになった周である。**
本番 3,458 件のうち **567 件**がこの書き方をしていて、そのうち **490 件（86.4%）はどこにも描かれていなかった**
（手前に閉図形が無くて捨てられた 354 件と、手前の図形が既に面を持っていて捨てられた 136 件）。
**残る 77 件は、身に覚えのない別の図形へ移されていた。**

- **`面: 薄墨` は、線と弧の上に残るようになった:** 面の 9 語のうち痕の走り方を言う語が **2 語から 3 語**になった
  （`粒`・`にじみ`・`薄墨`）。**ただし 3 語の届く先は同じではない** —— `粒` と `にじみ` は紙の 2 量を上げる
  （engine 37）が、**薄墨は紙について何も言わない**。薄墨は「墨をどう溶いたか」なので、
  **renderer が幅 3.0 倍・不透明度 0.35 倍の帯として描く。**
- **痕の幅と濃さを決める場所を 1 つにした:** `_stroke_width_px` の呼び出し **15 か所すべて**を
  入口 2 つ（`_mark_width_px` / `_nominal_mark_width_px`）へ寄せた。
  **開いた図形から届くのは 7 か所**で、閉図形では入口が素通りするので絵は変わらない。
  **⚠ 試作は弧の経路（`_render_arc_hand_stroke`）を外していた** —— 15 か所を数え切る形にして塞いだ。
- **⚠ 契約に書かれていなかった 1 か所があった:** 集合 `MARK_SURFACE_WORDS` の読み手は 2 つあり、
  **後者 `stroke_engine.py` は集合の語を辞書引きしていた**（表は `粒` と `にじみ` の 2 語だけ）。
  **`薄墨` を足しただけで、開いた図形に薄墨が付いたあらゆる描画が例外で落ちる。**
  紙の 2 量を上げる語が無ければ支持体をそのまま返す形にした。**`粒` / `にじみ` の係数も分岐も動かしていない。**
- **[I-289] を同じ版で塞いだ:** 参照コーパスの `display` は 4 件しかなく**4 件とも `pen`** だったので、
  **質感フィルタの枝を 1 件も通っていなかった**（`filter="url(#texture-` を持つ SVG は engine 37 で **0 本 / 597 件**）。
  フィルタが実際に出る 4 道具の case を足して **5 本 / 606 件**になった。
- **⚠ 契約の前提が 1 つ偽だった:** 「`drypoint` はフィルタを書かないので現れない」の**後半が偽**で、
  **`url(#texture-drypoint)` を名指しで書く場所が 3 か所ある**（かえり〈burr〉の要素）。
  かえりは drypoint そのものなので、**一般の枝は 0 回・かえりが 1 回**という実測の形で受入を書いた。
  **現れないことを主張する相手がいなければ恒真になる**ので、`drypoint` の case も証人として足してある。
- **コーパスは 597 → 606 件。増えた 9 件だけが新しく、既存 597 件は 1 バイトも動いていない**
  （受け入れ側が manifest の digest を 597 件突き合わせて確かめた）。
  **`ddl-engine-20` は 1 バイトも動かなかった** —— ddl コーパスに「開いた図形に薄墨」の case が無いためである。
- **⚠ coerce の golden が 3 件動き、修理の証人が全滅した:** `H-06` / `H-10` / `H-13` は
  **`_with_surface_on_a_closed_shape` を発火させる唯一の case だった**ので、そのまま焼き直すと
  **修理そのものを削除しても golden が緑のままになる**。証人 case を 1 件足して
  `branches reached: 32 / with a witness: 32` に戻してある。
- **摂動 11 本のうち予測と一致したのは 6 本:** 3 本が予測を超えて赤くなり（+1 / +3 / +2）、
  **1 本は空振りした** —— 契約が名指した当て先（`_surface_dab`）を、足した case が 1 つも通らない。
  **`filter="url(#texture-` を書く場所は `renderer.py` に 14 か所**あり、契約の読み（5 か所）も外れていた。
  **判別力のある摂動を別に立て**（`use_filters` を 14 か所すべての上流で落とす）、**5 本が赤くなることを実測した。**
- **⚠ `brush_thick` だけ、幅 3.0 倍に対して紙に載る面積が 2.10 倍にしかならない**
  （ほかの道具は 3.00〜3.96 倍）。**穂先の細りと紙が切る箇所が幅に比例しない**ためで、
  **上限を置くべきかは未裁定である**（本番 567 件のうち `brush_thick` は 32 件で、
  作者が裁定したコンタクトシートには載っていない）。
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

---

### v2.13.36 — 作品は、どのグループが読んでよいかを自分で言う（Build 923、2026-08-17・[I-191]）

**作品が見える理由は、これまで 2 つしか無かった** —— 所属が決める既定の範囲（`admins` は全部・`leaders` は自分の組織・`users` は自分の作品）と、作品 1 件ずつ書く ACL である。**3 つ目を足した。作品が自分で「このグループなら読んでよい」と言う。**
束で示したい相手が居るとき、ACL は 1 行ずつ書くしかなく、**本番の `history_acl` は今日まで 0 行**である —— 1 作品ごとの共有は一度も使われていない。

**形は Linux のファイルシステムに寄せた**（作者裁定・2026-08-17）。所有者は `user_id`、グループは新しい列 `share_group_id`、読み取りビットは `for_share`。**world に当たるものは作らない。**

- **列は 2 つで、片方だけでは何も起きない:** `history.for_share`（既定 `0`）と `history.share_group_id`（既定 `NULL`）。**見えるのは「ビットが立っていて、かつグループが一致する」ときだけ**である。ビットだけは宛先の無い許可、グループだけは誰も開いていない宛先で、どちらも単独では権限を意味しない。索引 `ix_history_for_share_group` を張った。**起動時の移行は冪等**で、列を持たない DB を 2 回開いても同じ結果になる。
- **宛先を省いて立てると、所有者の組織グループが入る** —— 新しいファイルが作った人のグループを取るのと同じである。**他組織を名指せるのは管理者だけ**（`admins`。非 admin には 403）。**その 403 は「名指したとき」にだけ掛かる** —— `chmod g+r` はグループの変更権を要求しないので、**自分が一度開いた作品を開き直すのに管理者権限は要らない**。
- **下ろしても宛先は残る:** `chmod g-r` はグループを忘れない。**立て直すと同じ宛先へ戻る**（優先順位は **名指し > 作品が既に持つ宛先 > 所有者の組織**）。消してしまうと、次にビットが上がったときに黙って別の相手へ向き直ることになる。
- **旗は読み取りだけを広げる:** `_writable_by` は 1 行も動かしていない。**他人の旗の立った作品に星を付けることはできない。**
- **系譜の節点と辺は付いてくるが、奥書は付いてこない** —— 旗の節を ACL と同じ枝（作品 id を渡す呼び出し）へ置いたためで、**`list_okugaki` は作品 id を渡していない唯一の呼び出し**である。**生の SQL で書かれた全文検索の経路も同じ述語を通る**（漏れると「見えすぎる」ではなく「検索したときだけ見えない」になる）。
- **API:** 旗を立てる口は `PATCH /api/history/{item_id}/for-share`。一覧の絞り込みは `GET /api/history?for_share=true` で、**系譜の 2 ルートにも同じ query を足した**。**ルート総数は 95 → 96**（`EXPECTED_ROUTE_COUNT`）。**凍結された API 表面の番人へは、契約が書いていた 4 件ではなく 6 件を宣言した** —— 段が触る系譜の 2 ルートが、番人 2 本の凍結ファイルの両方に既に載っていたためである。
- **web:** キャンバスの印（`共有対象にする`／`共有対象から外す`）は**受け口が 2026-08-16 に完成していた**ので、今周はページ側からハンドラを渡してつないだだけである。**絞り込み `共有のみ` は履歴ストリップと履歴管理の両方**に置いた。**一覧の要求を組み立てるのは 4 か所**で、**4 という数そのものを検査が主張する**（3 か所直して緑になる検査は、4 か所目が抜けたまま緑になる）。
- **cli:** `--for-share` を `history` と `history-export` の 2 つへ足した。
- **契約に無かった実装判断 2 件を、そのまま採る裁定を得た**（作者・2026-08-17）—— **① 履歴ストリップ側の絞り込みボタン**（契約は履歴管理側の 1 つだけを指示していた。無いと 4 か所目が製品で一度も走らない）と、**② 立て直しの宛先の優先順位**（契約の表は「省略なら所有者の組織」だったが、それだと下ろしたときに残した宛先が上書きされる）。
- **本番への影響:** `history` は 3,486 行で、**既定が `for_share = 0`** なので**配備した日に見える範囲が動く作品は 0 件**である。3,484 件を持つアカウントは組織グループ `default` に 1 人しか居ない。
- **検証:** server **3,427 passed / 31 skipped**（+14）、cli **237 passed**（+2）、web **449 pass / 0 fail**（+3）、`npm run check` **268 files / 0 errors / 2 warnings**、`lint:i18n` **1,078 strings / 0 warnings / 0 errors**（+2）。**摂動 17 本を 18 回当てて空振り 0**（番人 ②③ へ 1 本ずつ割った）。**凍結された API 表面の番人 ④ はどの摂動でも緑**だが、これは空振りではない —— `HistoryItem` は番人 ④ が比べる凍結 78 名に入っておらず、そこへ何を宣言しても 1 行も測らないことを、契約が発行日に測って予告していた。
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

---

### 2026-08-17 — 申告表に書けるのは、実際に測られる名前だけになった（**採番なし**・検査と道具のみ・[I-305] [I-258]）

**凍結された API 表面の番人で、申告 1 件が何も測っていなかった。**
`test_t8` は権限グループより前の API の形を **78 名**ぶん指紋で凍結している。正当な追加は**申告表に名前を書くと、指紋を取る前にその鍵を抜く**ので、申告した 1 件だけが許されて残りはバイトで測られる —— よく効く仕掛けである。**ところが申告表を読むのは凍結名を回るループの中だけ**で、**`HistoryItem` は 78 名にも、変わった 3 schema にも入っていない。**そこへ何を宣言しても 1 行も読まれないのに、表の上では守られているように見えていた。

- **実測**: 申告表は **13 schema・鍵 14 件**を名指し、**不活性はそのうち `HistoryItem` の 1 件だけ**（鍵 2 つ `catalog_mode` / `svg_bytes`）。**残る 12 schema は生きている。**
- **直し方（作者裁定）**: **申告表に凍結名に無い名前が書かれたら赤くする。** 不活性な行を消し、3 つの申告表すべてに同じ主張を掛けた。
- **判別力**: **摂動 1 本で 1 failed / 16 passed**（対照は摂動前の 17 passed）。**⚠ 最初の摂動は行を置き換える形だったので、生きている宣言も一緒に消えて指紋のずれだけで赤くなりえた** —— 1 行に両方を収めて当て直し、落ちたのが新しい主張であることを文言で確かめた。
- **⚠ 守備範囲は広がっていない。** `HistoryItem` そのものは、この番人の外のままである。変わったのは「表に嘘が書けなくなった」ことだけである。
- **凍結コーパスを描き直して確かめる仕事の置き場を CI に定めた（[I-258]・作者裁定）。** 契約の受入にも摂動にも入れず、受け入れ側が手で回すのもやめる。**裁定の日に測ると、CI は既に同じことを回していた** —— `reference-corpus.yml` が PR と main への push で発火し、3 つの job が生成器を回してバイト一致を要求する。**足す仕事ではなく、契約と受け入れ側から降ろす仕事だった。**
- **⚠ 「CI の結果は待たない」（2026-08-16 裁定）と併せると、ずれは push の後に、誰も読まない job が捕まえる状態になる。** 過去 3 件の見逃し（engine 10 の機械差・退役した `contact` 鍵・銀筆の改名）は、まさにその形で起きていた。

---

### v2.13.37 — 作品を焼いているあいだも、サーバーは他の要求に答え続ける（Build 924、2026-08-17・[I-284]）

**重い作品を保存しているあいだ、1 つの要求が 4.65 秒まるごと止まっていた。**焼き付け（SVG → PNG）は API と同じプロセスのスレッドで走り、**ラスタライザはラスタ化のあいだ GIL を握り続ける**ので、そのあいだ他のスレッドは進めなかった（実測: 9.00 秒の焼き付け中、相方のスレッドが **8.93 秒＝99.2%** 止まった）。**一括再構築だけは既に子プロセスへ出してあり**、同じ理由が保存時の経路にも当てはまるのに、そちらはスレッドのままだった。

- **焼くのは子・書くのは親。** `thumbnails.py` に常駐の `ProcessPoolExecutor` を置き（**遅延生成**）、ラスタ化だけを子へ出す。**`thumbs.db` への書き込みは親に残る。**本数は既存の `INKU_THUMBNAIL_WORKERS` が決める。**新しい環境変数も新しい管理設定も 0 本である。**
- **壊れた pool は 1 度だけ作り直す。** 止めるときは `_lifespan` が畳み、**畳んだあとに届いた保存が子を蘇らせない旗**を持たせた。
- **⚠ これは速くする改修ではない。**変わったのは「焼いているあいだサーバーが他の仕事をできるか」だけで、**焼き付けの秒は 1 ミリ秒も縮んでいない。**
- **実測（改修前 → 改修後・同じ周・各 1 回）**: **1 秒を超えた要求 1 件 → 0 件**、**最遅 4,650 ms → 255 ms**。**⚠ 中央値は動いていない**（167.0 → 142.2 ms）—— それを作っているのは描画で、本契約の範囲外である。**⚠ 保存の往復が 13.8 → 8.7 秒になったのは副産物**（7.3 MB の SVG を gzip して書き出す仕事も、焼き付けが GIL を握っているあいだは進めなかった）。**1 回ずつの測定なので短縮として引用しない。**
- **⚠ 軽い作品はわずかに遅くなった**（7.2 → 12.4 ms）—— SVG を子へ渡して PNG を戻す往復のぶん。**⚠ 起動後の最初の 1 枚だけ、子の立ち上がりに 0.32 秒を払う**（2 枚目以降は払わない）。**先に払っておくかどうかは台帳 [I-306] へ起票した。**
- **試験の既定は同じプロセスで焼く。** 子を立てるのは `@pytest.mark.child_bake_pool` を付けた 3 本だけである。**この仕掛けが無いと、保存を含む検査が毎回 spawn を払う** —— 1 ファイル（18 本）で **7.08 秒 → 16.16 秒**だった。
- **⚠ 受け入れで衝突が出た** —— 枝は `3f56b7c2` から張られており、**同じ日に [I-191] が入った main と、版マーカー 4 ファイルの同じ行で衝突した**（両側が Build を採番していた）。**製品コードの重なりは 0 である。**マージ後に 1 度だけ採番し直した。
- **検証**: server **3,434 passed / 31 skipped**（+7）、cli **237 passed**、web **449 pass / 0 fail**、`npm run check` **268 files / 0 errors / 2 warnings**、ruff 2 面 clean、`check_docs.py` consistent。**摂動 9 本で延べ 30 の受入が赤くなり、空振りは 0 本**（実装の予測 19・契約の予測 13）。**参照コーパスは 1 バイトも動いていない。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### Android — 揺らぎの門は 2 つに割れ、塗りの属性は 1 つの判定を読む（android `2.1.4-android.41`、2026-08-17・[I-278] [I-298]）

**server は 1 つの判定を 1 か所で下し、要るところがそれを読む。移植は同じ判定を写して持っていた。**
**その写しが 2 か所に在り、それぞれ別の嘘をついていた。本周は 2 つとも、判定を 1 か所にして読ませた。**

- **揺らぎの門を server と同じ 2 つに割った（[I-278]）:** server は
  **線用**（`renderer.py:780`・`position_x` / `position_y` の 2 軸）と
  **輪郭用**（`:792`・`radius` を足した 3 軸）を持ち、**どちらも `quality` が `none` と `pink` のとき通さない**。
  **移植は 1 つの関数に畳んであり、どの呼び出しでも 3 軸を見て、`pink` を除外していなかった。**
  **差が出る入力は 2 つ** —— **`pink`**（server は滲みとして blur で描き、揺らぎは通さない。
  移植は blur も出しつつ揺らいでいた）と、**線に `radius` だけを書いた `variation`**（server は直線のまま）。
- **除外の一覧は 1 か所にした:** `NO_VARIATION_QUALITIES` を 1 つ置き、2 つの門が同じ集合を読む。
  **軸の一覧は server と同じく門ごとに別の定数にした**（`PATH_VARIATION_DIMS` / `CONTOUR_VARIATION_DIMS`）。
  **⚠ 除外は否定形で書いてある** —— `Quality` が 5 語の今は「通す 3 語」と同じ集合を指すが、
  **6 語目が足されたとき、否定形なら新しい語は揺らぎに加わり、肯定形なら黙って落ちる。**
- **呼び出し 23 か所を 1 つずつ読んで呼び分けた:** **線用は 2 か所だけ**
  （`"line"` 枝の機械の極と `renderHandStroke`）、**残り 21 か所は輪郭用**。
  **図形の枝が 1 つにつき 3 回呼ぶのは、server が `varied` を枝ごとに 1 度だけ解くのに対応する**（意味は増えていない）。
  **`edgeContourWithAnchors` は中で線の道具を使うが輪郭用である** ——
  対応する server の `_render_corner_shape` が輪郭用の門を読んでいる。
  **「中で線の道具を使う」ことと「線の門を読む」ことは別である。**
- **塗りの属性を 1 つの判定へ寄せた（[I-298]）:** `ServerRendererStyle.strokeAttrs` が
  **`fillsInterior` を関数の頭で 1 度だけ読み、`fill` の値にも `fill-opacity` の 7 分岐にも同じ変数を使う**
  （server の `_stroke_attrs` と同じ形）。**それまでは「閉図形か、`filled` が真か」という別の 2 つで決めており**、
  その集合は `surface.texture` を知らず `cloudform` を含まなかったので、
  **同じ要求が書き方によって別の道を通っていた。**
- **[I-280] の周に足した再判定 1 つが冗長になったので外した:** `DefaultSvgRenderer` の幾何の道は
  `attrs.fill` をそのまま書く。**判定を値が運ぶようになったので、そこで 2 度目を訊くほうが写しになる。**
  **`strokeAttrs` の `primitive` 引数も、誰も読まなくなったので落ちた。**
- **⚠⚠ 契約が「もう 1 つ冗長になる」と書いた箇所は、冗長ではなかった（発行側の誤り）:**
  契約は `renderBodyShape` の `regionFill` も外せると書いたが、**`regionFill` は `fillsInterior` の写しではなく別の量**で、
  **「内部を痕として描いたので本体要素は開けておく」**を意味する。
  **server も同じ量を持っている**（`renderer.py:6235` `_body_attrs_for_contour_stroke` の `region_fill`。
  **docstring が「`region_fill=False` のとき本体は塗りも持たない」と明言し、呼び出し 6 か所が渡している**）。
  **契約どおり外すと、手描きの塗り図形で「平らな塗り」と「痕の群」が二重になり server と別の絵になる。**
  **実装が段 4 の後半を入れずに報告し、作者裁定（2026-08-17）で「残す」が確定した。**
- **⚠ そのとき「どの要素が塗ったか」を測る受入が 1 本も無いことが分かった:** 段 4 後半を実際に当てて全走しても
  **赤は 0 本**だった。**既存の照合は塗りの痕の `d` しか比べず、色の照合は集合なので、
  本体要素が新たに塗られても集合は変わらない。****裁定でこの周に T-232 を足した** ——
  server が焼いた `03_square_filled.svg` と移植の描き直しの両方で、
  **手の道具は本体を開けたまま痕の群が塗り、機械の極は本体が塗って痕の群を持たない**ことを見る。
- **受入 9 本（T-224〜T-232）。摂動は契約の 9 本＋実装が足した 3 本で、12 本すべて `perturb.py` 経由**
  （戻しは 12 本ともバイト一致・`git checkout` は 1 度も打っていない）。
  **走行スクリプトに `^e: ` の検出を入れてある**（当て方の試験で 2 回わざと壊し、2 回とも検出した）。
- **⚠ 摂動の予測は 4 本外れた。型は 2 つで、どちらも「読み手を数え落とす」である:**
  **①道連れを凍結コーパスの側からしか数えず、製品関数を直接叩いて自分で入力を組む既存の検査を数えなかった**
  （P-4 と P-8 で計 3 本）。**②番人の値の一覧の外を「比べられる」と読んだ** ——
  `testMaterialOutlinePointsAndDashArrayExactParity` は索引の全 key ではなく名指しの 4 件しか回さないので、
  当該 case はその外だった。**⚠ P-9' は本数が 4 で一致したが、名前は 2 つ外れている**（相殺）。
- **満点は 376 → 385（+9）。マージ後の木で XML 65 / tests 385 / failures 0 / errors 0 / skipped 0**、
  `test_android_reference_fixtures_are_current.py` **4 passed**。
  **凍結コーパスは 1 バイトも書き換えていない**（**51 枚に `pink` は 0 件・線に `radius` だけも 0 件**なので、
  そもそもこの改修を 1 枚も測れない。受入は全部性質で置いた）。
- **申し送り 1 件を起票した（[I-307] として採番済み）:** **機械の極の線に、server が持たない揺らぎの枝がある** ——
  server は `rotring` の線を直線 1 本で引き、揺らぎの門を 1 つも呼ばない。**移植は門を読み、揺らいだ `<polyline>` を書く。**
  **コーパスに `variation` を持つ `rotring` の線が 0 件なので誰も赤くならなかった。**
- **⚠ もう 1 つ、恒真な検査が見つかった（直していない）:** `test03SquareFilledExactParity` は
  `fill-stroke-v1` の群を比べるが、**`03_square_filled` の内部の痕は `fill-texture-v1` で、
  `fill-stroke-v1` は両辺とも 0 件である。****両辺が空なので何を壊しても緑になる。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

---

### 2026-08-17 — 凍結物は、リリースが走る機械で焼くようになった（**採番なし**・検査と試験環境のみ）

**負荷が継続する試験を、配備先ホストのテスト専用コンテナへ移した。**開発は従来どおり macOS で行う。

- **測ってから決めた**: 同じ commit・同じ木で、**Mac は 3434 passed / 31 skipped / 504 秒**、**コンテナ（linux）は 3432 passed / 2 failed / 31 skipped / 276 秒**。**collect は両機とも 3,465 件で同数。**「同じ 8 コアだから速くはならない」という起草時の見込みは外れ、**直列で 1.8 倍・並列で 6.1 倍（82 秒）速かった。**⚠ 1 ファイルだけは遅くなる（`test_single_user_mode`）ので、順位は Mac の縮尺ではない。
- **⚠ 赤かった 2 本は、どちらも「量子化の網の外」を見る検査だった。** 製品が描く絵は量子化のおかげで両機でバイト一致する（**生成器 4 つが 837 ファイルを焼き直して 0 差分**）。割れたのは、**生の double を凍らせている移植の参照 fixture** と、**量子化を故意に外す摂動の対**である。
- **凍結物はリリースが走る linux で焼くことにした。** `renderer_variation_primitives.json` を linux で焼き直した（**6 値が最終桁で違う**。`-1.7282983464997077` 対 `-1.7282983464997073`）。**移植側は 1e-9 の許容差で比べており差は 4e-16 なので、JVM 試験は 1 本も動かない**（385 tests / 0 failures）。**バイトを比べる F-1 は、焼いた機械でしか問えないので darwin では skip する** —— **代償は「Mac で古びた fixture は Mac では捕まらない」ことで、捕まえるのはコンテナの走行である。**
- **1 ULP の摂動がどの case へ届くかは機械で違う。** darwin は engine 28 の 6 分割のうち 5 本、glibc は残る 1 本を動かす。**期待値を機ごとに持たせ、「2 つの記録の和集合がちょうど 6 本」を主張する検査を 1 本足した。**
- **移植の JVM 試験も同じ機械の別の器へ移した**（作者指示）—— `eclipse-temurin:21-jdk` ＋ Android SDK の 1.83GB。**実測 385 tests / 0 failures で Mac と同数**。**⚠ AGP 8.9.1 が要るのは `build-tools;35.0.0`**（`compileSdk` は 36 なのに）。**⚠ 2 度目の走行は 6 秒で全タスク up-to-date になり、前の走行の XML が「385 件」と言い続けた** —— 数える側に「この走行で書かれたか」を確かめさせ、`--rerun` を足した。
- `pytest-xdist` を dev グループへ入れた。**リリースのイメージには入らない**（`uv sync --frozen --no-dev`）。
- **SPEC を 4 か所（日英 2 対）改訂した。**

---

### Android — 機械の極の線は揺らがず、塗りの番人は何件比べたかを言う（android `2.1.4-android.42`、2026-08-17・[I-307]）

**2 つとも「誰も見ていなかった場所」の話である。**移植には server に対応物を持たない枝が 1 本あり、
その枝を守るはずの検査の隣に、**何を壊しても緑になる検査**が 1 本あった。

- **機械の極の線から、server に無い揺らぎの枝を消した（[I-307]）:** server は `primitive: "line"` かつ
  `weight: "rotring"` の命令に、**揺らぎの門も材質の層も通さず `dwg.line` を 1 本引いて答える**
  （`renderer.py:7688`）。**移植は同じ入力で門を読み、揺らいだ `<polyline>` を書いていた。**
  **凍結コーパス 51 枚に「揺らぎを持つ `rotring` の線」が 0 件**なので、誰も赤くならなかった
  （`variation` を持つ 4 件のうち線は 1 件だけで、道具は `pencil`）。**受入は性質でしか置けない。**
- **材質の群の呼び手も server に揃えた:** 移植は材質の群を**機械の極の道からだけ**呼んでおり、
  **server は手の道具の道からだけ**呼ぶ（`_render_hand_stroke`）。呼び出しを外し、
  **呼び手が 0 になった薄い委譲を関数ごと消した。**
  **⚠ 今日の絵は 1 バイトも変わらない** —— 材質表の 6 鍵（`pencil` `chalk` `brush_thin` `brush_thick`
  `crayon` `pen`）に `rotring` は無く、呼び出しは必ず `null` を返していた。
  **摂動でこの段を戻すと赤は 0 本** —— **「機械の極が材質を着るかどうか」を測る検査は 1 本も無い**、
  という実測がそのまま残る。
- **恒真だった検査を、参照が実際に持つ群へ向けた:** `test03SquareFilledExactParity` は
  `fill-stroke-v1` 群を両辺から取り出して比べていたが、**参照 `03_square_filled.svg` にその群は 0 件**で、
  **両辺 0 件どうしの比較だった**。参照が持つのは `fill-texture-v1` で、そこに **34 本の痕**が在る。
- **番人に「何件比べたか」を言わせた:** **34 を手で書かない** —— 群の class が
  `marks-34` として自分で宣言している数を読み、取り出した本数と突き合わせる（両辺で）。
  **0 件なら赤くなる。**
- **⚠ その検査は恒真であると同時に冗長でもあった（受け入れ側が発行日に発見）:**
  `testEveryReferenceSvgMatchesOnPathsPointsAndDashes` が**51 枚すべての ` d="…"` の並びを既に比べている**ので、
  34 本は群の外側では比べ済みだった。**全体照合が見ていないのは「どの痕がどの群に属するか」**なので、
  直した検査はそこを測る形にした。**同じ理由で `*ExactParity` の他の 4 本も部分集合である**（起票した）。
- **⚠ 走行中に作者裁定が 1 つ入った:** 段 1 が既存の受入 2 本を赤くした ——
  **`line()` の `weight` の既定が `"rotring"` で、2 本が「機械の極の線が揺らぐこと」を対照に使っていた**
  （契約はこれを測っていなかった＝発行側の誤り）。**裁定は「比較対象を `pen` に取り替える」**。
  同じ主張を 2 回述べないよう、機械の極の主張は新しい受入が持つ。
- **⚠ 発行側の誤りがもう 1 つ:** 契約は「道連れになる番人は 2 本」と書いたが、
  **`d` の並びを比べる 1 本は赤くならない**（class 名や `marks-N` を替えても `d` は動かない）。**実測は 1 本。**
- **検証**: **マージ後の木で Android JVM 387 tests / 0 failures / 0 errors / 0 skipped**
  （66 XML・53 秒・pentala のテスト専用コンテナ）。**`@Test` の総和は起点 385 → 枝 387**（受入 2 本ぶん。
  直した 1 本は名前も本数も変えていない）。**`test_android_reference_fixtures_are_current.py` 3 passed / 1 skipped。**
  **凍結コーパスは 1 バイトも動いていない。****触った木は `android/` の 4 ファイルだけ**（+224 / −27）。
  **摂動 4 本は実装セッションが枝の上で実施し、件数も名前も予測と一致した**（実行規約 §2-1 により
  受け入れ側は枝の上の再走と摂動の再実施を skip した）。
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### v2.13.38 — 作曲が落ちた作品は自分でそう言い、そこから推敲するときは一度だけ尋ねる（Build 925、2026-08-17・[I-292]）

**Stage 2（作曲）が落ちると決定的な代替が Score を書くが、その事実はどこにも保存されていなかった。**応答の `compose_fallback_used` はその 1 回の応答にしか無く、保存すると消える。**Stage 1 が落ちた作品には印が付くのに、同じ「言葉と対応が切れた作品」でも作曲が落ちた側は無印で履歴に並び、系譜の親にもなれた**（本番 3,459 件のうち Stage 1 の fallback は 33 件が記録されているのに対し、Stage 2 は 0 件）。

- **列を 1 つ足した**（`compose_fallback`）。**値は 3 状態**である —— 理由の文字列（落ちた）／`"none"`（落ちなかった、と送り手が明示した）／記録なし。**`null` を「落ちなかった」に使うと、列より前に保存された作品と区別が付かない**ため、送り手は落ちなかったときも `"none"` を積む。
- **書き手は 3 つで、いちばん多いのは server 自身である。** 着手前の実測で、**描いた作品を保存する主経路は `render.py` が自分で `_add_history_item` を呼ぶ道**であり、**`interpret_fallback` を積んでいる web / cli の行は 0 件**だと分かった（応答が返るのは行が書かれた後なので、クライアントは送り返せない）。**作者裁定で「server も Stage 1 とまったく同じ形で書く」を段 1 へ足した。**web と cli の送り手も積む。
- **印は層ごとに 1 枚。**両方落ちた作品はバッジが 2 枚並ぶ。**生成情報の欄は常に三状態を出す**（`あり` / `なし` / `記録なし`）。**⚠ 過去の作品に印は付かない** —— backfill しないので、既存 3,459 件は「記録なし」のままである（`ALTER TABLE` に `DEFAULT` も `UPDATE` も無い）。
- **印のある作品を親にして推敲すると、一度だけ確認が出る。** 記憶は画面の中の作品 id の集合で、**サーバーに状態を作らない**。**確認の口は 9 つ**（送信・再演・演奏／構図／解釈の変奏・候補生成・系譜からの記述編集／描点変更／DDL 編集）で、**9 つ全部を通す**。**取り消しが呼び手へ届く経路も足した**（届かないと、待っている推敲が永久に返らない）。
- **英語は `Score fallback`。**`Composition fallback` は配置の seed（`composition_seed`）と紛れるため採らず、GLOSSARY に不可の訳として併記した。
- **⚠ 写生の fallback は表示のみのままにした**（列も印も足していない）。写生が落ちても「あなたの言葉のまま解釈された」であって、対応は切れていない。
- **⚠ API 表面を凍結している検査が 4 本赤くなった**（実装の予測は 0 本）。`HistoryPostBody` と `HistoryItem` に 1 欄増えたためで、**基準ファイルを元の書式のまま焼き直し**（動いたのは 3 行）、**残る 3 本には増えた欄を名前で宣言した**。
- **⚠ 実装が自分で番人を 2 つ締め直した** —— 送り手の点呼が「ファイルに語が在るか」を見ていて摂動を 2 本とも通し、web の受入も鍵の名前しか見ていなかった。**どちらも「摂動が当たらない＝冗長」で片づけずに測ったから見つかった。**
- **検証**: **マージ後の木で 3445 passed / 31 skipped**（pentala のテスト専用コンテナ・起点は 3435 / 31）、**cli 240 passed**（起点 237）、**web 単体 463 / 0 fail**（起点 449）、**`npm run check` 270 FILES / 0 ERRORS / 2 WARNINGS**、**`lint:i18n` 1085 strings / 0 warnings / 0 errors**、**ruff 緑**。**凍結物は `rewrote 134 files; 0 differ`（`exit 0`）で 1 バイトも動いていない。****摂動は 12 本すべて当て、予測が当たったのは 5 本**（外した 7 本の理由は完了レポートにある）。**⚠ 描画・engine の版・参照コーパスは 1 つも動いていない。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### v2.13.39 — 待っているあいだ、どの層が働いているかが画面に出る（Build 926、2026-08-17・[I-302]）

**1 枚の絵は 4 つの層を順に通る** —— 写生（Stage 0.5）・解釈（Stage 1）・作曲（Stage 2）・描画。**そのうち 3 つはそれぞれモデルを 1 回呼ぶので、待ち時間の大半はそこにある。ところが画面に届く合図は 2 つしか無かった** —— 解釈が終わった知らせと、全部が終わった知らせである。**結果として、待ちの前半は「解釈中…」と出たまま実は写生をしており、後半は「構造化中…」と出たまま実は描画をしていた。表示が嘘をつくのではなく、表示に材料が無かった。**

- **合図を 2 つ足した。** `/api/paint/stream` の NDJSON に **`sketch`**（写生層が動いた回だけ・粒度とフォールバックの有無とトークン数と所要）と **`score`**（作曲と coerce が終わり Score が確定した時点・命令数とモデルとトークン数と所要）が入る。**写生文も Score 本体も載せない** —— どちらも `done` が既に運んでおり、記述は 10 万字に達しうる。
- **画面の段階表示が推測をやめ、合図で切り替わる。** 語を 2 つ足した（`写生中…` / `演奏中…`。英語は `sketching from life…` / `performing…`）。**既存 4 語は 1 字も変えていない。**
- **応答の形は 1 バイトも変わっていない。** `done` も `/api/paint`（非 stream）も従来どおりで、**CLI と Android は無改修**である。**`stage1` の `elapsed_ms` が写生を含んだままであることも変えていない** —— 直すと、いま読んでいる者の意味が黙って動く。**内訳は `sketch` の `elapsed_ms` を引けば出る。**
- **⚠ 失敗がどちら側に落ちるかの境目が、写生が動いた回だけ 1 段前へ動く。** 最初のイベントより前の失敗は HTTP、後の失敗は本文の `error` イベント、という規則は変わっていない。**変わったのは「最初のイベント」が `stage1` から `sketch` になったことである** —— 写生が動いた要求で Stage 1 が落ちると、HTTP 502 ではなく HTTP 200 ＋ 本文の `{"event":"error","status":502}` で届く（**写生 off の回は 502 のまま**）。**画面の見え方は変わらない**（どちらの道も同じ detail と status を同じ読み手へ渡す）。受入 2 本で固定し、`api_paint_stream` の docstring に書いた。
- **NDJSON を読む場所を `web/src/lib/paintStream.ts` へ出した**（`+page.svelte` に写しは残っていない）。**未知のイベントは従来どおり読み捨てる** —— この寛容さは改修前から在ったので、先に寛容にする独立の commit は要らなかった。
- **⚠ 段ごとの所要は保存していない**（本改修は表示の話である）。保存が要るなら別の票を起こす。
- **⚠ 凍結物の検査「差分 0 件」に判別力の穴が見つかった** —— 描画を動かす摂動を当てると生成器が自分の番人で**書き込む前に**落ち、1 バイトも書かないので差分は 0 のままになる。**機構は `exit=1` を立てているので、読むべきは終了コードと `gen_… exited nonzero` の行である。**台帳 [I-310] へ起票した。
- **⚠ 受入番号 T-242 が [I-292] と重なった**（発行側の誤り）。**一意性を主張する検査は無いので赤くはならない。**
- **検証**: **マージ後の木で 3454 passed / 31 skipped**（pentala のテスト専用コンテナ・[I-292] のマージ後は 3445 / 31 で、増分 9 は本改修の受入ちょうど）、**cli 240 passed**（±0）、**web 単体 467 / 0 fail**（463 → +4）、**`npm run check` 271 FILES / 0 ERRORS / 2 WARNINGS**（既知の a11y 2 件）、**`lint:i18n` 1087 strings / 0 warnings / 0 errors**、**ruff 緑**。**摂動 13 本はすべて当たり、空振りは 1 本も無い**（予測どおりだったのは 8 本で、外した 5 本の理由は完了レポートにある）。**⚠ 描画・engine の版・参照コーパスは 1 つも動いていない。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### Android — 移植は server に在る道だけを残し、番人は自分が何を比べたかを言う（android `2.1.4-android.43`、2026-08-17・[I-311] [I-312] [I-313]）

**前の周が残した 3 つを 1 本の枝で閉じた。3 つとも「検査が、自分が測っていると思っている場所を測っていなかった」話である。**

- **server に対応物の無い道を 1 本消した（[I-311]）:** 機械の極の線から揺らぎの枝が消えたとき、
  その枝だけが呼んでいた**私的な薄い委譲の呼び手が 0 になった**。**関数ごと消した。**
  **⚠ 同名の関数が幾何オブジェクト側にも在り、そちらは 2 か所から呼ばれているので残る**
  （契約は当初こちらを指して「消すな」と書いていた＝発行側の誤り）。
  **この段に摂動は無い** —— 呼び手が 0 の関数は、書き戻しても絵も検査も 1 つも動かない。
  **「摂動が 3 本しかない」ことは穴ではなく実測の帰結である。**
- **弱くなった受入を、門の在る道へ移した（[I-313]）:** 「線は `radius` だけの揺らぎに動かされない」を
  主張する検査が**線を機械の極で組んでおり**、その極はもう揺らぎの門を 1 つも読まないので、
  **「門が線用と輪郭用に割れている」ことを測らなくなっていた**。線を手の道具（`pen`）へ移した。
- **番人 4 本に「何件比べたか」を言わせた（[I-312]）:** 群を切って `d` を比べる 4 本が、
  **両辺 0 件でも緑**だった。参照側から取り出した本数が 1 件以上であることを主張させた。
  **手写しの定数は 1 つも書いていない**（4 本の本文に現れる裸の数字は 0 個であることを機械的に検算した）。
  **⚠ 4 本のうち 1 本には既に同じ主張が在ったが、件数一致の後ろに書かれていたので先に落ちるのは件数一致だった**
  （契約の表に無かった事実）。4 本が同じ順で落ちるよう前へ移した。
- **⚠⚠ 受入が 2 本とも「自分の行は 1 度も発火していない」と分かった。実装が段 4 の確認で見つけた:**
  **① 凍結コーパスを入力に取る受入は、製品コードへの摂動では原理的に発火しない** ——
  摂動が動かせるのは移植側だけで、参照側は何を壊しても空にならない。
  **P-1 / P-2 で 4 本は確かに赤くなるが、赤くしたのは既にあった件数一致の `assertEquals` である。**
  検査が名指す群名を替える摂動で発火を確かめた（**落ちた行は主張そのもの**）。台帳 [I-314] へ起票した。
  **② 門を測るはずの受入が、門の答えではなく seed の動きを測っていた** ——
  移植の `variationJson` が**門の条件式を手写しで持っている**ため、軸を名指すと
  **門の答えに関係なく演奏 seed の材料が組み替わり、絵が変わる**。
  `quality` の enum が 5 語しかなく、**門を通す入力と seed を動かす入力が完全に同じ集合**なので、
  **byte の差では原理的に分離できない**。距離でも分離できなかった（揺らぎが手の揺れの包絡より小さく、
  揺らいだ線のほうが軸に**近い**）。**痕が自分の軸をまたぐ回数**（門が拒む側 6〜9 回・通す側 38 回）へ据え直し、
  **seed だけが動く条件を雑音の底の対照として検査の中に持たせた**。手写しは台帳 [I-316] へ起票した。
- **⚠ 今日の絵は 1 バイトも変わらない。**動いたのは製品 1 ファイルの 6 行（呼び手 0 の関数）だけである。
- **検証**: **マージ後の木で Android JVM 387 tests / 0 failures / 0 errors / 0 skipped**
  （66 XML・37 秒・pentala のテスト専用コンテナ・受け入れの走行なので `--exclusive`）。
  **`@Test` の総和は起点も枝も 387 で不変**（段 2・段 3 は既存メソッドへ行を足しただけ）。
  **`test_android_reference_fixtures_are_current.py` 3 passed / 1 skipped。**
  **凍結コーパスは 1 バイトも動いていない。触った木は `android/` の 3 ファイルだけ**（+147 / −14）。
  **摂動 3 本は実装セッションが枝の上で実施した**（実行規約 §2-1 により、実装と受け入れが同じエージェントなので
  受け入れ側は枝の上の再走と摂動の再実施を skip した。**マージ後の全走は skip していない**）。
  **⚠ 予測は 3 本中 1 本だけが完全一致** —— P-1 は別ファイルの同型の照合を 1 本数え落とし、
  **P-2 は本数が 10 で一致したのに中身が 2 件ずれた**（相殺で一致した）。
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

---

### Android — 同じ絵に 2 度たどり着いても、それは 2 つの作品である（android `2.1.4-android.44`、2026-08-17・[I-201] [I-319]）

**移植の履歴は、同じ絵の指紋を持つ作品を 1 つしか置けなかった。server は置ける。** 2 度目の作品は
**黙って 1 度目を消していた**。今回それを server と同じ判定へ揃え、消えていたことを測る検査を据えた。

- **指紋の一意索引を非一意にした（段 1）:** `HistoryItemEntity` の `Index("render_hash", unique = true)` を
  `Index("render_hash")` へ。**Room の版を 8 → 9 へ上げ、`MIGRATION_8_9` を足した**
  （索引を落として作り直す 2 文。**表は作り直していない**）。`android/app/schemas/…/9.json` を焼いた。
  **⚠ 実機の作者のデータで移行が測れた** —— 計装の走行中にアプリ自身が 8 → 9 を通り、
  **移行の前後で `history_items` は 2 行のまま**だった（退避 2 回・18:25 と 19:24 に実測）。
- **主キー衝突の判定を server と揃えた（段 2）:** `HistoryDao` の `OnConflictStrategy.REPLACE` を
  **`ABORT`** へ（server は同じ場所で再送出する）。**`upsert` を `insert` へ改名し、呼び手 4 か所を追った**
  （製品 1・計装 3）。**他の DAO の `upsert` 7 つはそのまま。**
- **嘘になる文を実測へ合わせた（段 3）:** **契約は 2 か所と書いていたが、実測は 8 か所・6 ファイルだった**
  （発行側の数え落とし）。8 か所とも直し、**「中身を変えるな」と契約が書いた 2 本の検査は
  文だけを直して assert を 1 つも動かしていない。**
- **⚠ 走行中に作者裁定を 1 件仰いだ:** 段 1 を入れると既存の計装 `RefinementSaveTest.t6` が赤くなった。
  **この検査は「同じ候補を 2 度保存しても 1 行のまま」を主張していたが、repository に再保存の番人は無く、
  件数が伸びなかったのは一意索引が 1 行目を消していたからだった** —— **検査は自分が名乗っている番人
  （view model の三状態）に 1 度も到達していなかった。**裁定は「実測どおり 3 行へ直す」で、
  **「2 度目の保存は 2 つ目の作品である」を主張する形へ移した**（番人は view model に在り
  `RefinementScreenTest` が測っていることを docstring に書いた）。
- **⚠⚠ 受入 1 本が 2 つを主張しており、片方は摂動で 1 度も赤くできなかった（[I-319]）:**
  `T-268` は ①移行が行を失わない ②移行の後は索引が非一意である、の 2 つを 1 本で主張していた。
  **`MIGRATION_8_9` は 2 行しかなく、どちらの行を替えても `runMigrationsAndValidate` が
  `Migration didn't properly handle` を assert へ到達する前に投げる。**
  **➡ 摂動 P-3 も P-4 も壊した主張は②で、①は「赤くなる状況を 1 度も見ていない主張」だった。**
  **作者裁定（20:11）で T-268 を 2 本に割った** —— **新しい `T-290` は形の突き合わせを通さず
  `MIGRATION_8_9.migrate` を直接呼んで行数を数え**、T-268 は②だけを主張する形にして
  `runMigrationsAndValidate` を残した。**P-4 を当て直すと T-290 は
  `both works survived the migration expected:<2> but was:<0>` で赤くなり、①が測れる主張になった。**
  **⚠ ②は依然として assert ではなく枠組みの例外で赤くなる。**製品コードは 1 バイトも動かしていない。
- **受入は 5 本すべて計装に置いた**（T-266〜T-269・T-290）。**LLM を 1 度も回さず、凍結コーパスも読んでいない。**
- **⚠ 摂動 P-1 の空振りは原因を特定していない** —— entity に `unique = true` を戻すと
  形の突き合わせが食い違うはずだが、`t268` / `t269` は緑だった。**計装の assets に載る schema が
  摂動ビルドに追随していなかった疑いがあり、台帳へ起票した**（未測定）。
- **⚠ 摂動 P-3 の 1 つ目の形は測れていなかった** —— 契約が例示した `SELECT 1` を
  **Android の `execSQL` が拒む**ため、移行が例外で落ちただけだった。**本数と名前は当たっていたので、
  名前だけ見ると当たったように見える摂動だった。**`UPDATE` の無害な 1 文へ替えて当て直した。
- **⚠ 実機の退路を 4 度取った** —— `backup-android.sh` を計装の前と P-4 の前に計 4 回。
  **計装は 12 走行とも旗 `-Pandroid.injected.androidTest.leaveApksInstalledAfterRun=true` を付け、
  実機のアプリは 1 度もアンインストールされていない。**
- **検証**: **マージ後の木で Android JVM 387 tests / 0 failures / 0 errors / 0 skipped**
  （66 XML・39 秒・pentala のテスト専用コンテナ・受け入れの走行なので `--exclusive`）。
  **`test_android_reference_fixtures_are_current.py` 3 passed / 1 skipped。**
  **`@Test` の総和は JVM が 387 で不変・計装が 96 → 101**（+5 = 新規受入 5 本）。
  **触った木は `android/` の 14 ファイルだけ**（+1,083 / −46）。**凍結コーパスは 1 バイトも動いていない。**
  **計装は実装セッションが枝の先端で回した** —— **101 tests / 1 failure**（残る赤 1 本は起点から在る
  `VersionInfoPanelTest` で、本契約と無関係・台帳へ起票済み）。実行規約 §2-1 により、実装と受け入れが
  同じエージェントなので受け入れ側は枝の上の再走と摂動の再実施を skip した。
  **⚠ 計装だけはマージ後の木で回していない**（実機が要るため）。**`androidTest` の部分木ハッシュが
  枝の先端と一致すること**（`2a13af96`）を示したうえで、**マージ後の製品との差は
  main 側の `render/DefaultSvgRenderer.kt` 1 ファイルだけ**である。
  **⚠ 摂動の予測は、割った後の 3 面（JVM・計装・P-4 の巡）が実測と完全一致した。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### v2.13.40 — coerce が記述を判ずる語は、1 か所に在る（Build 927、2026-08-17・[I-115]）

**coerce は記述の語を読んで Score を直す。その語が 2 か所に書かれていた** —— 宣言側 `language_support/{ja,en}.py` の `COERCE_MARKERS` **574 語**と、`coerce/compose.py` の分岐の中に直書きされた **119 語**である。**語彙ファイルを読んだ者には 693 語のうち 574 語しか見えず、残りは分岐を 1 つずつ読まないと見つからない。移植には、それらが在ることを知る手立てが無かった。**

- **44 か所 / 119 語を宣言側へ移し、45 系統を立てた**（系統は **27 → 72**・ja 70 / en 71）。**1 か所 = 1 系統**とした —— 束ねると「宣言側に在るが中身が読めない」形になり、いまの問題を宣言側へ移すだけになる。**語を 1 つも捨てず・足さず・綴りも変えていない**（**574 + 119 = 693**・重複による目減りは無し。**ただし 270 語は 2 系統以上に載る**）。
- **⚠ 契約が数えた「39 か所 / 110 語」は実測と 4 か所ずれていた。**発行側の走査の道具が 3 つの形を見ていなかったためである —— **① `x not in text`**（`"crescent" not in ddl.lower()` は記述を判ずる語だった）・**② 名前に束ねた tuple**（`scene_markers = (...)` を後から渡す形が 5 か所）・**③ `note` を照合する箇所を語彙に数えていた**（−9 語）。**作者裁定 2026-08-17 で「実測どおり未宣言を全部」とした。**番人はこの 3 つとも塞いである。
- **絵は 1 バイトも動いていない。** 移設は照合の式を 1 か所も変えず、リテラルの tuple を定数へ置き換えただけである。**`_marker_in_text` が ASCII の語に語境界の正規表現をかけ、素の `in` が生の部分一致であることは、そのまま保った**（`"slow"` は前者では `"slowly"` に当たらず、後者では当たる）。
- **順序に判別力が無いことを実測した。**「一致した語そのものを返す」箇所は **0 か所**（56 か所すべてが真偽を返す）で、**語順を逆にした木の全走は起点と 1 本も変わらない**。順序を守る受入は据える先が無いので置いていない。
- **日英の対は揃えていない**（契約の設計。揃えるために語を足せば挙動が変わる）。**片言語の系統 3 本は名指しで列挙してある** —— `autumn_leaf_fall`（ja のみ・元の条件に英語が書かれていなかった）・`crescent_scene`（en のみ）・`radius_clause`（en のみ・日本語の `半径` は同じ行の正規表現の中に在る）。**列挙から外れた系統が片言語になったら赤くなる。**
- **⚠ 移していない直書きが残っている。** 語が全部宣言済みの 8 か所（作者裁定で選んだ範囲の外）と、**marker の形に移せない正規表現 3 か所**である。**うち 4 か所は、移設した 3 か所と同じ if 連鎖の中に在る** —— 連鎖の半分だけが宣言側を読む形になった。
- **SPEC はこの改修で初めて字義どおりになった。** §6.5 は「言語固有語彙を共通コアへ混入させない」「coerce の言語別 marker は各言語サポートが所有する」と書いており、**119 語が `compose.py` に在るあいだ、その記述は偽だった。SPEC は 1 行も直していない**（日英とも記述は在り、対応も取れている）。
- **検証**: **マージ後の木で 3462 passed / 32 skipped**（pentala のテスト専用コンテナ・**起点 `e188850b` は 3454 / 31**）、**cli 240 passed**（±0）、**ruff 緑**（server / cli とも）。**凍結コーパスは枝の先端で `exit 0`**（`rewrote 134 files; 0 of them differ`）で、**マージ後の `server/` の部分木は枝の先端とバイト一致なので引いた**（`d4af9421`）。**摂動は実装が 8 本**（予測 7 本赤に対し実測 10 本赤。外した 2 本はどちらも「予測より多く赤くなった」側で、原因は移設した語の集合の一致を見る受入が**足す向きにも効く**こと）、**受け入れ側が 1 本**（走行中の裁定で走査が広がった「名前に束ねた tuple」の形に当てて、**予測どおり 1 本だけ赤**）。
- **⚠ `web` / `cli` / `android` は 1 ファイルも触っていない。** web の検査（`npm run check` / `test:unit` / `lint:i18n`）と Android は**回していない**。**engine の版・参照コーパス・Score schema・Stage 1 / Stage 2 のプロンプトも 1 つも動かしていない。**
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。

### 2026-08-17 — 公開文書が木に追いつき、訳語の対応表が 1 枚にまとまった（**採番なし**・文書と検査のみ・[I-322]）

**製品コードは 1 行も動いていない。** 触ったのは公開文書・`check_docs.py`・それを回す検査 2 本・
用語の対応表である。**⚠ 本周は契約 [I-322] の段 0 まで進んだところで作者がタスクを差し替え、
以後は直接指定の 4 本を同じ枝で実施した** —— **契約の受入 `T-300`〜`T-306` と摂動 `P-1`〜`P-5` は実施していない。**

- **`docs/architecture` の 11 対が現況へ追いついた。** 版（v2.13.39 / Build 926 / render engine 38 /
  ddl engine 20 / Android `2.1.4-android.43`）だけでなく、**ルート数 96・公開 allowlist 3**（旧記載は 82/95・6）、
  CI が回す 4 job、参照コーパス 606 件も現物で測り直してある。
- **新しい対を 2 つ足した。** `description-to-svg.{ja,}.md`（入口の切除から保存までを実装関数の単位で辿り、
  判定 17 件を表にしたもの）と `future-plan.{ja,}.md`（改善提案の裁定後の姿。**実測が前提を裏返した 6 件**も記録した）。
  **`check_docs.py` の対の一覧と `test_manual_parity_gate.py` へ登録して 31 → 33 対。**
- **訳語の対応表を 1 枚へ統合した** —— **`docs/i18n/glossary.md` が訳語対応の正本**である。
  それまで対応は `web/src/lib/i18n/GLOSSARY.md` と、公開されていない私家版ノート 2 枚に分かれていた。
  **`GLOSSARY.md` は文体規則・禁止語と制限語・`lint:i18n` との対の正本として残り、表は指し先になった。**
  **裁定待ちだった対応は美術用語として通用する語で確定し、★印を付けてある**（利用者 = author / user の文脈分け、
  要求配達 = request delivery、new performance / replay / render / rasterize の境界）。
- **SPEC が実装に追いついた。** 実装と食い違っていた 13 箇所を直し（歳時記 11 カテゴリ ＋ あいだ、
  PNG テンプレートの既定 3 つ、§18 の色語彙 6 → 9、付録の木）、**設計史と現行を分けた**
  （§12 の Opus 4.7 前提は記録であること、リトライは「理由つき 1 回 ＋ 決定的フォールバック」が現行であること）。
- **英語側の取りこぼし 18 件が 0 になった。** v1.82 の会計 14 段落と Build 600 / 605 の転写境界文を英語へ移した。
  **⚠ 印だけを貼るのではなく、その印が付いている記述ごと移してある。**
- **⚠ 測り方の罠が 1 つ見つかった** —— **版の印を数える正規表現に `\b` を付けてはならない。**
  かな・漢字が隣接すると語境界が立たず、「Build 557では」を数え落とす（実装が 2 度誤測した）。
- **⚠ 走行中に潜在の赤を 2 本作り、枝の先端の全走が捕らえた** —— `GLOSSARY.md` の行を読む検査 2 本
  （`test_limits_are_settings.py` と web の `svgWeight.test.ts`）が、表の移動に追随していなかった。
  **全走を回さない周が作った赤を、次の周の全走が拾った形である。**
- **検証**: **マージ後の木で 3464 passed / 32 skipped**（pentala のテスト専用コンテナ・直前の main は 3462 / 32 で、
  **増分 2 は新しい 2 対の parametrize ちょうど**）、**`npm run check` 271 FILES / 0 ERRORS / 2 WARNINGS**（既知の a11y 2 件）、
  **web 単体 467 / 0 fail**、**`lint:i18n` 1087 strings / 48 exceptions / 0 errors**、**`check_docs.py` 緑（33 対・禁止語 30 文書）**、
  **ruff 緑**。**cli は 1 ファイルも差分が無いので回していない。**
- **⚠ 実装は Fable（別モデル）なので、受け入れ側は skip を 1 つも行っていない。**
  **ただし契約の受入も摂動も実施されていないため、当て直す対象は存在しない** —— 検証したのは枝の現物と、
  誰も通していないマージ後の木である。
- **⚠ 採番していない**（文書と検査だけの周）。**pentala へも送っていない**（製品コードに差分が無い）。
- **⚠ GitHub の CI は待っていない**（作者裁定・実行規約 §2-10）。
- **⚠ 中断した契約の残り**（段 1 の CHANGELOG 2 対・段 2 の据え置き判定・段 4 のゲート・受入と摂動の一式）は、
  続けるか書き直すかが未裁定である。**`CHANGELOG` の版の印が ja のみ 6 件ある事実は [I-326] として起票した。**

### v2.13.41 — パスワードを忘れた管理者に、戻る道がある（Build 928、2026-08-18・[I-328]）

- **`inku-admin reset-password` を足した。** サーバー自身の環境の中から 1 アカウントの password を設定する
  （配布イメージなら `docker compose exec api inku-admin reset-password --username admin`）。
  **セルフサインアップが無く、`.env` はアカウントが 0 件のときしか読まれず、変更は Web UI からしかできなかったので、
  管理者が 1 人だけの自己ホストでは忘れた時点で戻る道が無かった**（2026-08-18 にベンチの docker 版で実際に起きた）。
- **password はコマンドラインに置かない。** 既定は 2 度尋ねる対話で、`--password-stdin` を付けたときだけ標準入力の 1 行目から読む。
  **8 文字未満は拒む**（bootstrap の規律と同じで、入口によって規則が変わらないようにした）。
  **アカウントを指定しなければ `INKU_BOOTSTRAP_ADMIN_USERNAME` が作った管理者が対象**で、
  **無い名前を指定すると在るアカウントを印字する**（名前のほうを忘れた人のため）。
- **⚠ これは 2 つ目の入口ではない。** 実行できるのはコンテナかそのファイルを握っている者で、その者は既に `/data/inku.db` を書ける。
  **手で SQL を書く手間を省くだけである。**
- **⚠ 配布の利用者へ届くのは次のリリース以降である**（イメージはリリースの周に焼かれるので、公開済みの v2.13.40 には入っていない）。
- **⚠ `cli/` には置けない** —— `server/Dockerfile` は `shared/` と `server/` しか COPY しないので、
  配布イメージに `inku-cli` は存在しない。
- **同じ版に [I-327] を畳んだ**（**タグを打つ前の未公開の版なので、番号は増やさない**）——
  **配布イメージの `/api/info` が `version: unknown` を返していた。**
  `server/Dockerfile` が `web/BUILD_NUMBER` は COPY するのに `web/APP_VERSION` を持たず、
  **読み手 2 つ**（`reference.py:89` と `api_core/common.py:26`）**がどちらも読めなかった。**
  **`COPY` を 1 行足して直し、同じ型の穴を静的に見る番人を置いた** ——
  製品コードが読む `web/…` のファイルを走査し、Dockerfile の `COPY` と突き合わせる
  （`server/tests/test_the_image_can_name_its_own_version.py`）。
  **⚠ イメージを組む workflow はタグ push でしか発火しないので、常時回る道は静的な走査だけである。**
  **⚠ `release_version`・`build_number`・画面の版は以前から正しく出ていた。**

### v2.13.42 — 中継は、自分が解いた本文に圧縮の札を貼り直さない（Build 929、2026-08-18・fix-forward）

- **配布イメージの Web UI が、ログインの直後で必ず止まっていた。** `web/src/hooks.server.ts` の中継は
  `/api/*` を `fetch` で上流へ渡すが、**この `fetch` は gzip を解いて本文を平文にする。**
  それなのに応答を組み直すとき上流のヘッダをそのまま返していたので、**`content-encoding: gzip` と
  圧縮時の `content-length` が平文に付いたまま**ブラウザへ出ていた。**ブラウザは展開に失敗し、
  status が 200 でも本文の読み取りが `TypeError: Failed to fetch` になる。**
- **⚠ ログインが「失敗した」ように見えていたが、実際は成功していた。** `login()` は本文の `r.json()` まで
  `try` の中で読むので、**200 が返り session cookie も張れているのに `Failed to fetch` が表示され、
  後続の 11 本の取得が 1 本も飛ばない。**
- **直したのは 2 つのヘッダを落とすことだけである**（`content-encoding` と `content-length`。
  本文は既に平文で、長さも変わっているため）。**新しい `web/src/lib/proxyResponse.ts` が正本で、
  中継はそれを通す。**
- **⚠ いつからか**: gzip は 2026-08-16 の `d7955139` で入り、**`v2.13.40` が最初にこの穴を配ったイメージである**
  （中継のコードは 2026-07-16 から変わっていない）。**手元の開発は Vite の proxy がバイトをそのまま流すので壊れない。**
  **壊れるのは配布イメージの構成だけである。**
- **受入 3 本**（`web/src/lib/proxyResponse.test.ts`・`T-298`〜`T-300`）。
  **⚠ 「gzip の札が付いた平文を読むと落ちる」ことは node:test では再現できない**（展開するのはブラウザの
  ネットワーク層で、Node の `Response` は展開しない）。**そこで番人は「札を落とすこと」と「中継がその道を通ること」を測り、
  実物は公開後にイメージへ認証つきで gzip を要求して確かめる。**
- **検証**: **web 単体 470 pass / 0 fail**（+3）、**摂動 3 本で予測どおり各 2 本ずつ計 6 本が赤**
  （各回とも全走メタゲート `T-15` が道連れ）。

### v2.13.43 — 束は、道具の走れる側で組む（Build 930、2026-08-19・fix-forward）

- **`v2.13.42` の web イメージは公開されなかった。** タグの run（`32092151332`）は
  **api の job が 109 秒で success**、**web の job が 21,576 秒で cancelled** で終わっている。
  **GHCR に出たのは `inku-api:2.13.42` だけで、`inku-web:latest` は `2.13.41` を指したままだった** ——
  **つまり中継の修正（v2.13.42）は、配布の利用者に 1 人も届いていなかった。**
- **⚠ 遅かったのではない。止まった。** job のログの時刻を数えると、
  `#22 [linux/arm64 build 6/6] RUN npm run build` が **t=56 秒で `rendering chunks...` を印字したあと、
  21,480 秒（5 時間 58 分）1 行も出さず**、6 時間の job 上限に当たって切られている。
  **同じ run の amd64 側は同じ段を 14.4 秒で終えている**（`npm ci` も 3.4 秒でキャッシュが効いていた）。
  **QEMU の上の rollup が進まなくなった形なので、待っても終わらない。**
- **直したのは `web/Dockerfile` の 1 語である** —— **build 段を `--platform=$BUILDPLATFORM` へ固定した。**
  道具（vite / rollup）は、焼いている機械そのものの上でだけ走るようになる。
- **⚠ これが正しいのは web に本番依存が 1 件も無いからである**（`package.json` の `dependencies` は空で、
  adapter-node が吐くのは純 JS）。**束はどちらの arch で組んでも同じもので、
  runtime 段は target のまま置いてある** —— 配布される node の土台はそちらから来る。
- **受入 3 本**（`server/tests/test_the_web_image_is_built_where_its_toolchain_can_run.py`・`T-301`〜`T-303`）。
  **⚠ 規則は両方向で測る** —— 道具を走らせる段が pin されていること（`T-301`）と、
  **イメージが出ていく段が pin されていないこと**（`T-302`。あれを pin すると arm64 の札で amd64 の器を配る）。
  **`T-303` は走査が何も見つけていない場合を赤くする** —— 前の 2 本は、読めない Dockerfile でも緑になる。
  **⚠ イメージを組む workflow はタグ push でしか発火しないので、常時回る道は静的な走査だけである**
  （[I-327] の番人と同じ理由で、同じ形をしている）。
- **⚠ `inku-web:2.13.42` は今後も存在しない。** タグは 1 本も打ち直していない（fix-forward）。
  **`2.13.42` の中身は、この版で初めて配布イメージに載る。**
- **検証**: **コンテナ全走 3476 passed / 32 skipped**（採番前の `782c2a84` は 3473 / 32。**+3 は新設ぶん**）、
  **摂動 3 本で予測どおり各 1 本だけが赤**（`--platform` を外す / runtime 段に付ける / 走査の当て先を消す）。

### v2.13.44 — UIモードは表示量の順に並ぶ（Build 931、2026-08-20・[I-246]）

- **設定の「その他」にある3択を、シンプル → カスタム → フルへ並べ直した。** レールのアイコンが示す
  濃い棒1本・2本・3本の順と同じになり、表示量が途中で最大にならない。
- **日英manualの表も同じ順へ揃えた。** 値、保存処理、表示文言は変えていない。
- **受入は順番を別の定数として写していない。** アイコン自身の淡色規則から各モードの濃い棒の本数を読み、
  レール・設定・日英manualが1・2・3の順かを同じ検査で確かめる。旧状態では設定とmanualが `[1, 3, 2]` で2本赤、
  修正後は対象7本およびWeb単体472本がすべて通った。
- **検証:** `npm run check` 0 errors / 2 warnings、Web単体472 passed、`lint:i18n` 0 errors、
  production build成功、`check_docs.py` 緑。

### Android `2.1.4-android.45` — DDL の各道を証明して engine 20 を名乗る（2026-08-20・[I-181]）

- **Android の決定的 DDL 修復を engine 8〜20 へ追随させた。** 色の配達と一色化、明示数の一意な割当と
  total 400 の all-or-nothing、面の閉形状への戻し、小形の半径、複合群れを含む密度予算を server と同じ順で行う。
  engine 13 の document plugin loader は server 専用なので追加していない。既存だった engine 9 / 14 / 18 / 19 の道も
  入口から検査し、すべて通った後で Android の DDL 参照版を `7` から `20` へ上げた。
- **正式受入で3件を reject し、回帰を先に赤くして直した。** `group_size=1` の不要な挿入、複合群れがあると通常群れの
  密度制限を迂回する穴、明示数の修復が別の明示数の唯一の回答を奪い whole-DDL の素材ヒントも失う穴である。
- **検証:** 起点 387、最終 **395 tests / 0 failures / 0 errors / 0 skipped**（67 XML）。`P-1`〜`P-8` と
  受入追加 `A-1`〜`A-4` は予測どおりの検査だけを赤くし、fixture-current は **3 passed / 1 skipped**。
  計装・実機は対象外で、pentala へは送っていない。
- **手続上の逸脱を記録する。** 段1〜5を別々の中間木に保存して各段の全走を行わず、統合木で全走した。
  最終木では全走と全摂動を完了し、受入側が差分を再審査したうえで受け入れた。

### v2.13.45 — compat SVGを定義済みportable subsetで検査する（Build 934、2026-08-21・[I-332]）

- **互換優先SVGの保存境界に、単一の明示allowlistとfail-closed XML checkerを置いた。** 未知のelement・attribute・namespace・外部/欠落referenceを拒否し、`filter`と`clipPath`を許可しない。
- **現行groundの`pattern`はsubsetへ明示許可した。** fallbackや描画変更は加えておらず、代表compat SVGは変更前後とも35,256 bytesでSHA-256一致した。render engineは38のまま。
- **API・履歴・Web・CLIは既存のprofile伝播で同じserver実装へ到達する。** Web/CLIの保存ロジックは変えず、Web日英help、CLI help/README、日英SPECを実装した保証へ同期した。
- **検証:** server直接検査85 passed、履歴API 1 passed、CLI 2 passed、Web伝播1 passed、ruff、`npm run check` 0 errors、`lint:i18n` 0 errors。bulk描画・外部アプリ検証・無変更componentのsuiteは行っていない。

### v2.13.46 — solid塗りをmottleとprofile別fallbackへ移す（render engine 40）（Build 935、2026-08-21・[I-335]）

- **computer以外の`solid`を、多数の走査線からbase fillと決定的なmottleへ置き換えた。** `display`と`editable`は標準SVG filterでむらを重ね、filterを読まない場合もbase fillが残る。
- **`compat`はfilter-freeのflat vector fallback、`computer × solid`は従来の一方向periodic scanを維持した。** profileの用途説明をWeb、CLI、日英SPECへ同期した。
- **render engine 40の参照はsolidの代表4件だけが変化し、既存606件は不変だった。** generatorの2回目はbyte-identical。作者はdisplay mottle、compat fallback、computer scanの3画像を受け入れた。
- **検証:** server直接・reference検査11 passed、ruff、docs、Web check 0 errors、`lint:i18n`、CLI help/README直接検査。全suite・全摂動・Android・外部app matrix・benchmark・containerは行っていない。

### Android `2.1.4-android.46` — 推敲元の記述は `source_text` が一度だけ決める（2026-08-22・[I-153]）

- **推敲元の記述と `description_edit` 判定を、server/webと同じ一段へ揃えた。** `source_text` が在ればその値を使い、NULLの行だけ `original_input` へ落ちる。batchの `#N ` とdemoの `[demo] ` をもう一度解釈して剥がす道は削除した。
- **現行の接頭辞付き保存は、同時に接頭辞なしの `source_text` を書く。** 通常描画でNULLになる行の `original_input` は接頭辞を持たないため、Room移行と保存形式は変更していない。
- **検証:** focused JVM 1 test / failures 0 / errors 0。旧い接頭辞除去を一時再現すると同じ検査が1件redになり、HEAD復元後にgreenへ戻った。最初のpre-edit実行を落とした手続逸脱はcompletion reportへ記録した。JVM全走、計装、実機、reference生成、pentalaは対象外。

### Android `2.1.4-android.47` — 代表化の帯を端末内上限の整数比へ移す（2026-08-22・[I-271]）

- **密度帯を代表化上限の `3/2`・`2/3`、cluster帯を `25/6`・`2/1`・`1/1` で算出する。** clusterのband値も出荷時上限120を基準に整数scalingし、端末内上限を変えた場合に同じ比を保つ。
- **出荷値80–120の結果は不変。** 密度境界180/80、cluster境界500/240/120、band値9/7/5/3を維持する。server設定同期、UI、永続化、network/APIは追加していない。
- **最初の実装はserverの24-marks-per-cluster capまで移植し、受入で停止した。** 既定count 73–119が3から4/5へ変わる到達可能な差だったため、作者裁定でcapを対象外とし、修正用fail-firstを本体再編集前に追加した。
- **検証:** 初回fail-first 3 tests / 3 failures、修正fail-first 3 tests / 2 failures。修正後のfocused JVM 3 testsは実装中・worker最終・受入側でgreen。JVM全走、計装、実機、reference生成、pentalaは対象外。

### Android `2.1.4-android.48` — 推敲候補の順番を画面に見せる（2026-08-23・[I-348]）

- **4候補の生成中、生成済みをチェック、現在の1候補を反対Mascot、未着手を中点で示す。** 1から4のlaneを固定位置に置き、逐次生成を並列fan-outのようには見せない。1候補と非busy時には表示しない。
- **既存の候補数・生成済み候補・busy stateだけを読むUI変更である。** ViewModel、repository、pipeline、render、永続化、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは未実装symbolでexit 1。実装中とbranch-tip finalは同じ3 testsがexit 0。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.49` — 履歴をキャンバスのそばに置く（2026-08-23・[I-349]）

- **通常Compose画面のキャンバス直下に、履歴サムネイルの横stripを追加した。** 現在作品を選択枠で示し、タップは既存の選択処理へ渡し、選択変更時はその位置へ自動スクロールする。
- **履歴0件、presentation/full-screen、controls非表示では出さず、描画・推敲中は操作を無効にする。** 既存の履歴画面、検索、star filterは複製していない。ViewModel、repository、Room/query/schema、履歴順、replay生成、永続化は変更していない。
- **検証:** production edit前のfocused JVM testは未実装helperでexit 1。実装中とbranch-tip finalは同じ2 testsがexit 0。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.50` — 履歴stripにモデル名を添える（2026-08-23・[I-350]）

- **各履歴サムネイルの直下に、既存summaryのStage 1モデル名を表示する。** provider接頭辞を外し、長い名前は既存の14文字compact規則で省略する。nullまたは空の旧履歴ではラベル行自体を出さない。
- **I-349の選択枠、タップ、scroll、操作lock、履歴順序は保った。** 新しいproducerやsummary fieldは追加せず、ViewModel、repository、Room/query/schema、pipeline、render、永続化、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは未実装helperだけでexit 1。実装中、branch-tip final、受入側の同じtargeted testはexit 0。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.51` — モデルroutingから順序依存の推測を除く（2026-08-23・[I-351]）

- **provider選択を明示prefix、enabled providerによる完全一致の単独所有、default-localの3段規則へ揃えた。** 所有者が0件または複数なら一覧の先頭を選ばずdefault-localへ進むため、Roomの並び順で答えが変わらない。
- **disabled providerは所有者へ数えず、明示指定されたdisabled providerは別providerへredirectせず実行前に停止する。** 未知prefixらしき語とモデルID内部のcolonを保持し、OpenAI互換requestは自providerの先頭prefixだけを外す。Stage別設定、`ModelRequest`、provider catalog、Room/schema/migration、pipeline、render、永続化、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは新resolver/helperの未実装symbolだけでexit 1。実装中、branch-tip final、受入側の`RoutingModelProviderTest`はexit 0。全JVM suite、計装、実機、network、実モデル、Room migration、reference生成、pentalaは対象外。

### Android `2.1.4-android.52` — 履歴stripで作品をその場でStar／解除する（2026-08-23・[I-354]）

- **各履歴サムネイル右上に`★`／`☆`のStar controlを追加した。** サムネイル選択とは別のtap targetで、Star操作だけではキャンバスの選択作品を切り替えない。描画・推敲中は既存lockに従って両操作を無効にする。
- **既存`HistoryListItem.starred`と`toggleStar(HistoryListItem)`だけを結線した。** I-349の選択枠、tap、scroll、表示条件とI-350のモデル名表示を保ち、新しいproducer、ViewModel action、repository/DAO/query、Room/schema/migration、永続化、pipeline、render、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは新helperの未実装symbolだけでexit 1。実装中、branch-tip final、受入側の`HistoryThumbnailStripTest`はexit 0。mainは枝分岐後に動いておらず競合0のため、Fast規約どおりmerge後の同testは再実行していない。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.53` — 表示中のPrompt／JSONをコピーする（2026-08-23・[I-355]）

- **通常Compose画面と既存Canvas panelのPrompt／JSONタブに`Copy` controlを追加した。** Promptは表示中のStage 1／Stage 2入力とsystem prompt全文、JSONは表示中のrender情報全文をclipboardへ渡す。表示とcopyは同じ計算済み文字列を使い、Artworkまたは作品未選択ではcontrolを出さない。
- **既存`LocalClipboardManager`と表示helperだけを結線した。** tab切替、本文、hash copy、export、canvas messageを保ち、新しいViewModel state/action、producer、生成規則、repository/DAO/query、Room/schema/migration、永続化、pipeline、render、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは新helperの未実装symbolだけでexit 1。実装中、branch-tip final、受入側の`RenderTabCopyTest`はexit 0。mainは枝分岐後に動いておらず競合0のため、Fast規約どおりmerge後の同testは再実行していない。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.54` — 保存済みの生成情報を読む（2026-08-23・[I-356]）

- **通常Compose画面と既存Canvas panelに、選択中作品の読み取り専用`生成情報`sheetを追加した。** 保存済みの写生、Stage 1／2モデルと言語、各seedと変奏、色カタログ、キャンバス、render hash／engine、作成日時、処理時間を5節に整理し、null／blankまたは壊れたmetadataは`—`で扱う。
- **既存`HistoryItemEntity`と`renderMetadataJson`だけを読む。** Androidにproducerが無い世代、派生、コメント、batch、tokensは足さず、ViewModel state/action、repository/DAO/query、Room/schema/migration、永続化、lineage取得、token収集、SVG解析、pipeline、render、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは新projection helper／typesの未実装symbolだけでexit 1。実装中とbranch-tip finalはexit 0。mainが枝分岐後に別のWeb変更で進んだため、重なり0・競合0を確認してmerge後に`GenerationInfoSheetTest`を1回実行しexit 0。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.55` — 作品が保存した色の対応を表示する（2026-08-23・[I-357]）

- **生成情報sheetの演奏sectionに、保存済み`render_color_map`の色語・色コード・swatchを表示する。** 色語順で安定させ、map欠落／空／壊れたmetadataでは行を出さない。不正コードでもsheetを落とさずneutral swatchにし、保存文字列はそのまま残す。
- **既存`workColorSnapshot(renderMetadataJson)`だけを再利用した。** 現在の色カタログから再計算せず、producer、色コードの書換え／正規化、ViewModel state/action、repository/DAO/query、Room/schema/migration、永続化、SVG解析、pipeline、render、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは新color-map projection helper／typeの未実装参照だけでexit 1。実装中とbranch-tip finalの`GenerationInfoColorMapTest`はexit 0。mainは枝分岐後に動いておらず競合0のため、Fast規約どおりmerge後の同testは再実行していない。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.56` — 作品が保存した色カタログ名を表示する（2026-08-23・[I-358]）

- **生成情報sheetの既存`色カタログ`行に、作品が保存したカタログ名と同じsnapshotのIDを`保存名 (catalog-id)`として表示する。** 名前欠落／blank、snapshot欠落、空map、壊れたmetadataでは従来のIDだけを保ち、名前とIDが同一なら重複させない。
- **既存`workColorSnapshot(renderMetadataJson)`だけを再利用した。** 現在の`ColorCatalogs`やrename tableは参照せず、Webで除去済みの固定taglineを戻さないため`catalogSub`は表示しない。producer、ViewModel state/action、repository/DAO/query、Room/schema/migration、永続化、pipeline、render、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは新catalog-name formatting helperの未実装参照だけでexit 1。実装中とbranch-tip finalの`GenerationInfoCatalogNameTest`はexit 0。mainは枝分岐後に動いておらず競合0のため、Fast規約どおりmerge後の同testは再実行していない。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.57` — 履歴stripで保存済みの両モデルを確認する（2026-08-23・[I-359]）

- **履歴stripの既存Stage 1短縮ラベルへ、保存済みStage 1／Stage 2 model IDを2行で示すtooltipを追加した。** provider prefixを含む完全IDを表示し、Stage 2欠落は`—`、Stage 1欠落では従来どおりラベル行とtargetを出さない。
- **画面上の14文字短縮ラベルとstrip操作を保った。** サムネイル寸法、作品選択、Star、選択位置scroll、描画・推敲中の操作lockを変えず、既存`HistoryListItem`とMaterial tooltipだけを使った。新しいstate、model resolver、repository/DAO/query、Room/schema/migration、pipeline、render、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは新tooltip-text helperの未実装参照4件だけでexit 1。実装中とbranch-tip finalの`HistoryStripModelLabelTest`はexit 0。mainが枝分岐後に別のWeb変更で進んだため、重なり0・競合0を確認してmerge後に同testを1回実行しexit 0。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.58` — 履歴stripのtooltipに保存日時と色カタログIDを表示する（2026-08-23・[I-360]）

- **I-359のmodel tooltipへ作品の保存日時と保存済み色カタログIDを追加した。** 日時は生成情報sheetと同じISO-8601 UTC表現で、既存の日英labelを再利用する。日時が0以下または変換不能、catalog IDがblankなら`—`とする。
- **既存`HistoryListItem.createdAt`と`colorCatalogId`だけを読む。** 現在の色カタログ一覧から名前を引かず、I-359のmodel ID、欠落表示、14文字短縮ラベル、サムネイル寸法、作品選択、Star、scroll、操作lockを保った。新しいstate、repository/DAO/query、Room/schema/migration、pipeline、render、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testはtooltip formatterの追加予定4引数の未実装参照12件だけでexit 1。実装中とbranch-tip finalの`HistoryStripModelLabelTest`はexit 0。mainは枝分岐後に動いておらず競合0のため、Fast規約どおりmerge後の同testは再実行していない。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.59` — 履歴stripのtooltipに作品hashとキャンバスを表示する（2026-08-23・[I-361]）

- **I-360のmodel tooltipへ保存済み作品hashとキャンバスを追加した。** 短縮render hashは既存の`F` prefix表記と`作品の来歴ハッシュ`label、`canvasAspect`は既存の`キャンバス`labelを使い、blank値は`—`とする。
- **既存`HistoryListItem.renderHashShort`と`canvasAspect`だけを読む。** full itemを追加loadせず、現在のcanvas optionから再計算しない。I-360のmodel ID、保存日時、色カタログID、14文字短縮ラベル、サムネイル寸法、作品選択、Star、scroll、操作lockを保った。新しいstate、repository/DAO/query、Room/schema/migration、pipeline、render、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testはtooltip formatterの追加予定4引数の未実装参照12件だけでexit 1。実装中とbranch-tip finalの`HistoryStripModelLabelTest`はexit 0。mainは枝分岐後に動いておらず競合0のため、Fast規約どおりmerge後の同testは再実行していない。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。

### Android `2.1.4-android.60` — VersionInfoPanel計装testのengine期待値を製品定数から組み立てる（2026-08-23・[I-318]）

- **`VersionInfoPanelTest`の期待表示を`CompatibilityConstants.renderEngineId`と`renderEngineVersion`から組み立てる。** 古い`default 26` literalを外し、testの責務を現行番号の独立固定ではなく、VersionInfoPanelが製品互換定数へ結線されていることの確認に限定した。
- **製品挙動は変えていない。** production Kotlin、表示文字列、Gradle dependency、runner、Espresso stub、保存形式、Room、pipeline、render、server/web/sharedは変更していない。I-064も未変更で残る。
- **検証:** 最初の計装はscreen OFF・keyguard表示中の環境redでCompose hierarchy前に停止した。private I-362でread-only preflightを実装し、unlock後の有効なfail-firstは古いliteralだけで1/1 failure。実装後greenとbranch-tip finalは同じ1 testがexit 0。各runはAPK保持flagとDB退避を使い、history 2、lineage 2、schema 9、thumbnail 1,239件を保持した。全instrumentation、全JVM suite、screenshot、reference生成、pentalaは行っていない。

### Android `2.1.4-android.61` — 履歴gridで作品をその場でStar／解除する（2026-08-23・[I-363]）

- **履歴画面の各作品card右上へ、保存済みStar状態を`★`／`☆`で常時示すcontrolを追加した。** controlのtapは既存`toggleStar(HistoryListItem)`だけを呼び、cardの作品選択を発火させない。
- **既存のcard tapとlong press Star切替を維持した。** 選択枠、thumbnail、作品title、検索、Starのみfilter、順序、件数も変えていない。既存`HistoryListItem.starred`、toggle action、`HistoryBadge`だけを使い、新しいproducer、ViewModel action、repository/DAO/query、Room/schema/migration、i18n、pipeline、render、server/web/sharedは変更していない。
- **検証:** production edit前のfocused JVM testは新helperの未実装参照2件だけでexit 1。実装中は`HistoryGridStarControlTest`がexit 0、branch-tip finalもexit 0。mainは枝分岐後に動いておらず競合0のため、Fast規約どおりmerge後の同testは再実行していない。全JVM suite、計装、実機、screenshot、reference生成、pentalaは対象外。
