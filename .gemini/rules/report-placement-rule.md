# 成果物レポート配置ルール

- **成果物レポートの出力先**:
  タスク指示ファイル（`.../tasks/*.md` など）に基づく作業において成果物レポート（`*-result.md` など）を作成する場合、作業リポジトリ直下ではなく、**必ずタスク指示ファイルが存在するディレクトリ**（例: `/Users/oikawas/projects/ddl-server/no-git-sync/fable5/claude_code/tasks/`）に出力・保存してください。
