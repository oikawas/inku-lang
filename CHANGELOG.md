# inku Changelog

**Public English release notes** — See [SPEC.md](SPEC.md) for the current English specification and [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md) for the short developer entry point.

This file records changes chronologically. If a historical note conflicts with the current specification, the current specification wins. The more detailed canonical history is maintained in Japanese in [CHANGELOG.ja.md](CHANGELOG.ja.md).

---

### v1.72 — Refine and Compare UI

- Unified touch, layout, and reading selection, including the hierarchy in which reading regenerates layout and touch.
- Unified one/four-candidate selection and saving, cancellable generation, cross-panel generation exclusion, dynamic progress/cost copy, and DDL hover previews.
- Replaced sequential variation counters with independent JavaScript-safe random seeds, made touch changes visible for fixed shapes, and restored seeds when loading history into the canvas.
- Persisted caption visibility per user.
- Preserved tab context during previous/next navigation and expanded model comparison to three Stage 1/2 modes.

### v1.73 — System prompt optimization (2026-07-12)

- Fixed self-contradictions in the Stage 1 / Stage 2 prompts: conversion examples that used vague counts ("several"/「数本」) against the prompts' own concrete-count rule, and inconsistent radius notation. Added the missing motions category to the ja Saijiki list and the missing sparse-validity rule to the ja Stage 2 prompt (the en prompt has had it since Build 415).
- Stabilized the "Ground: ..." / 「地: ...」 route to canvas.ground: added ground-retention examples to the Stage 1 example pool, added an explicit Ground→canvas.ground / Surface→main-shape routing rule with an anti-duplication guard to Stage 2, and canonicalized the Stage 2 ground example inputs to the actual Stage 1 output form.
- Fixed a defect in api.py where a canvas_aspect override replaced the whole canvas value and destroyed any Stage 2-generated canvas.ground (the true cause of the 0/12 ground adoption recorded at v1.71). Targeted 12-prompt bench: ground adoption 0/12 → 5/12.
- Merged duplicated placement-mapping bullets and grouped the ~70-bullet Stage 2 rule list under eight subsection headers (content and order effectively unchanged).
- JP/EN 30+30 regression bench on the Build 448 prompt set: JP improved on every metric (visual_event 89.7→94.9); a main-branch baseline run attributed the apparent EN drop vs 448 to pre-existing v1.70/v1.71 drift, with this change improving on main across all quality metrics. All fingerprint gates pass.

### v1.74 — NIM Qwen3.5 397B Migration & Relation Tuning (2026-07-12)

- **Switched Default Models to Qwen3.5 397B**:
  - Switched the default Stage 1 / Stage 2 models to NVIDIA NIM `qwen/qwen3.5-397b-a17b`.
  - Restricted the `is_qwen3` thinking-trace suppression override (`/no_think` prefix injection) to local OVMS provider runs only. This change enables NIM Qwen3.5 397B to fully leverage its reasoning capability, improving both generation quality and latency by roughly 30%.
- **relation Duplication Prevention (F-1)**:
  - Tuned the Stage 2 prompt (`composer.py` relations section and examples) in both Japanese and English to add an anti-duplication guard: "Generate at most one relation per fixed relation phrase. Do not replicate the same relation phrase into multiple instructions," and added corresponding negative examples.
- **Achieved 100% ground Mapping Adoption (F-3)**:
  - Achieved a **6/6 (100%)** `canvas.ground` mapping adoption rate in the targeted 12-prompt texture benchmark, resolving the remaining ground-routing misses from v1.73. Verified no quality regressions on the JP30/EN30 regression sets.


### v1.74.1 — ground hotfix (2026-07-13)

- Added Japanese and English Stage 2 rules that prohibit inferred `canvas.ground` unless normalized DDL contains an explicit 「地: ...」 / "Ground: ..." sentence.
- Added a general Stage 1 support-preservation rule for Qwen3 Next: explicit 「〜の地」「〜の紙に」 / "... ground" / "on ... paper" wording must remain a `地:` / `Ground:` sentence and must not be rewritten as a background fill.
- Added a drop-only ground literal gate after composition. It removes only unmarked ground while preserving canvas aspect; it never creates, repairs, or replaces ground when a marker is present. Drops remain observable through a warning log.
- Moved display ground texture opacity (clamped to 0.02–0.18) onto the texture rect itself and changed the filter alpha table to `0 1`. Filter-capable browsers retain the same effective alpha, while filter-free PNG rasterizers now degrade to a faint veil instead of an opaque gray wall.
- Audited every renderer filter use and found no other wide filtered shape whose transparency depended entirely on its filter.
- Build 508 passed 314 tests with 30 skipped on both Mac and pentala; ruff, web check, and web build were green. Fixed-model Qwen3 Next benchmarks completed 12/12 surface/ground prompts (6/6 explicit ground, zero inferred ground, no gray wall) and 30/30 prompts in both Japanese and English (zero inferred ground, no quality drop beyond the threshold, all fingerprint gates passed, and zero 502s, timeouts, or fallbacks). Full results are recorded under “v1.74.1: ground hotfix” in the local benchmark log.


### v1.75 — Literal tiling pattern field (2026-07-13)

- Added the physical motion word `tile` / 「敷き詰める」 to Saijiki. It is selected only for a literally requested regular repeated surface, never inferred from merely “many” or “countless.”
- Added `layout="grid"` with optional `rows`, `cols`, and `jitter`. Grid fills `at.region` or the margin-bounded canvas; explicit rows×cols take priority over count. The schema limit is 2000 for literal tiling while ordinary arrangements keep the existing 1–1000 prompt contract.
- Grid performance layers seeded cell jitter, distinct per-element variation phase, and material-specific weight behavior while preserving bit-identical replay for the same Score and seed. Coerce does not add fade, clustering, preserved space, or count reduction to grid. Build 515 limits English literal markers to tile/tiled/tiling and adds a drop-only boundary so a motif label containing grid alone cannot create a spontaneous grid.
- Added matched Japanese and English Stage 1 / Stage 2 rules and wallpaper, four-direction, and square-grid examples. Four directions remain a maximum of four overlaid instructions; no new primitive was introduced.
- Build 515.


### v1.76 — Artwork lineage (2026-07-14)

- Added `dh1:<sha256>` as description identity over NFC-normalized text with LF line endings and trimmed outer whitespace. Batch labels such as `#1` are presentation metadata and do not affect `dh1`. Description identity, the existing `rh2` edition identity, history IDs, and lineage node IDs remain separate concepts.
- Added independent lineage nodes and edges. Parentage is recorded only by explicit touch, layout, interpretation, model, DDL-edit, description-edit, replay, or canvas-aspect-change operations; it is never inferred from hashes, timestamps, or visual similarity. Existing history rows are backfilled as separate roots.
- When generation, DDL drawing, or further refinement continues from an unsaved refinement candidate, only that direct ancestor is automatically retained as an intermediate `lineage_only` work. The Canvas labels the preview as unsaved and reports that automatic intermediate retention is hidden from regular history. If retention fails, drawing does not silently continue as a new root. Intermediate cards are labeled as hidden from history; they do not affect regular history counts or starred views, and the user can promote the same node and edges with “Save to regular history” from the lineage view.
- Added a focused Canvas lineage view showing nearby generations as artwork thumbnails with arrows between parent and child cards and labels for the operation that produced each child. Opening a card changes both the displayed artwork and the parent for the next refinement. Card checkboxes support confirmation-gated bulk moves to trash. Trash preserves lineage; permanent deletion removes content and hashes while leaving a content-free tombstone to preserve the path.
- Users can explicitly start a new root. Ordinary DRAW actions are not automatically attached to the latest history item.
- Lineage depth and branching are not quality scores, achievements, or generation controls. The graph records the creative process; it does not choose a best branch.
- History-manager artwork selection uses a compact, dedicated check control independent from opening an artwork, with exactly one state change per activation. Thumbnails do not show enlarged hover previews.
- Stars toggle immediately without a confirmation dialog. Artwork comments are stored independently from star state and are edited in lineage-card details; removing a star does not erase an existing comment.
- Works generated in Refine's Model comparison subview use the same circular image-corner `+` adoption control as Adjust candidates and show `✓` after being saved to history. Starring remains independent from adoption.
- The displayed artwork and the bottom-history current marker stay synchronized by history ID. Opening an off-page work from lineage, History Manager, or replay uses `anchor_id` to load the page containing that work. Background Compare/Refine saves do not steal the marker, and unsaved candidate previews do not leave a different history work marked current.
- While History Manager is open, external-history polling does not replace its items or page size. Thumbnail action areas use a uniform height, and the page row count is calculated once from the maximum measured card height, preventing clipped final rows and the refresh loop that replaced roughly 90 items with the bottom strip's smaller item count.
- Build 525.

**v1.76 closure (2026-07-15):** Build 525 is the accepted v1.76 release. Successive real-browser reviews confirmed that any lineage work can become the next derivation source, parent-child paths remain traceable through generation arrows, the displayed work stays synchronized with bottom history, and History Manager selection, bulk deletion, and page sizing remain stable. Feedback was incorporated across Builds 518–525. Subsequent work, beginning with v1.80, uses this lineage contract and the separation of the four identities as its foundation.

### v1.87 — Printmaking Grammar and Vocabulary Refinement (2026-07-16)

- Added a deterministic five-layer stroke performance: intended path, damped hand dynamics, one shared 1/f-like latent energy signal, sparse events, and per-tool grammar. Width, lateral deviation, and apparent density covary; rotring explicitly blocks the expressive engine to preserve uniformity.
- Added `burin` and `drypoint`. Burin tapers at both ends and swells through the cut; drypoint adds a seed-selected one-sided burr. These model general tool behavior, not an artist or period.
- Extended existing surfaces with `hatch`, `crosshatch`, and stepped `aquatint`; added `mezzotint` ground and `mode="carve"` with `light|half|bright`. Rendering order is ground, additive marks, carved light, then plate tone.
- Plate tone, mezzotint grain, drypoint burr, and register shift remain deterministic under the existing texture-seed convention. The `rh2` canonical payload is unchanged.
- Printmaking fields are literal-input only. Stage 1.5 cannot inject them, and invalid carve without a dark ground is dropped without repair.
- Removed `rope` from the core vocabulary, Score schema, renderer, prompts, and Saijiki. Because inku is unreleased, v1.87 uses the opportunity to remove an ambiguous object-metaphor touch instead of carrying compatibility debt. The resulting touch vocabulary has ten entries.
- SVG transfers the grammar of engraved line and tone; it does not claim to reproduce physical plate indentation or raised ink.
- **Build 568:** Fixed the rendering order that allowed legacy fixed-width lines to cover variable-width stroke outlines. Burin taper and swell, drypoint's one-sided burr, and the existing writing-tool gestures now remain visible in the primary mark. All ten Saijiki touch previews were aligned with those observable renderer differences.
- **Build 569:** Normalized DDL now names one touch for every visible line, arc, or outline. Japanese and English Stage 1 rules and examples were updated, dynamic few-shot selection guarantees a non-pen material example, and Stage 1.5 applies the same rule to its added marks. Filled shapes are not assigned material mechanically, and post-rewrite double expansion is prevented.
- Builds 567–569.

### v1.88 — Okugaki Lineage Recitation (2026-07-17)

- Added manual first-person recitation of one root-to-target branch using structurally prefix-only generation requests.
- Deterministic feature differences and invariants come from the existing read-only composition mirror; SVG works are rasterized to PNG for vision input, and the server adds the model/date signature.
- Added user-scoped append-only storage, oldest-first display, deletion without editing, and Idempotency-Key handling.
- Added the explicit Lineage UI action and `inku-cli okugaki` with `--dry-run`. The feature remains disconnected from dh1/rh2 and every generation or refinement decision.
- Build 570.
- **Build 571:** Okugaki Idempotency-Key generation now uses a UUID fallback based on `getRandomValues()` (with a final compatibility fallback), so appending works in LAN HTTP browser contexts where `crypto.randomUUID()` is unavailable.
- **Build 572:** History Manager thumbnail capacity is now derived from a stable layout contract instead of measuring partially painted cards. Removing per-card `content-visibility` prevents missing thumbnails, page-size oscillation, and flicker inside the modal.

### v1.89 — UI Refinement (2026-07-17)

- **Model selection (Build 573):** Replaced provider/model dropdowns with a dialog that presents available model names grouped by provider. The `Stage 1/2` tab assigns one model to both stages, while the `Stage 1` and `Stage 2` tabs allow separate choices. Existing confirm, cancel, and per-user persistence behavior remains intact.
- **LLM / Vision settings split (Build 574):** Separated the Vision default from Stage 1/2 LLM settings across the model dialog, admin model-purpose settings, Okugaki, API, and CLI. The legacy `/api/models` `catalog` and CLI `--model` option remain compatibility paths.
- **Model evaluation metadata (Build 575):** Seeded 29 verified NVIDIA NIM models with LLM/Vision purposes, five-level recommendations, bilingual evaluation comments, and measured speed classes and labels. Administrators can edit the metadata, and admin and user model selection show it on hover. Existing database overrides and custom models are retained.
- **Contextual model selection UI (Build 576):** Batch now shows its current Stage 1/2 models and opens the shared dialog. Demo instruction generation and Okugaki replace their old dropdown/fixed display with provider-grouped LLM and Vision card pickers respectively, including metadata hover details.
- **Model-selection context and readability (Build 577):** Batch has no image input, so its model dialog now hides Vision and selects only Stage 1/2. Demo adds a rendering Stage 1/2 model button separate from its instruction-generation LLM picker. Evaluation tooltips now use a dark background, white body text, and bright secondary labels for consistent high contrast.
- **Vision autonomous refinement (Build 578):** Lineage autonomous refinement now offers Random and AI Vision methods. Vision runs select a model from provider-grouped cards and feed each saved image's observation, one next direction, and one allowed variation kind into the next generation. They never score, rank, accept, reject, or discard generations; method, model, and advice are recorded in derivation metadata, every generation remains in lineage, and the human makes the final decision.
- **Lineage card action readability (Build 579):** Moved the selection checkbox to the card's top-left corner, placed Displayed, Intermediate, Same text, and Same edition labels immediately to its right, and retained the three-dot menu at top right. Menu-item emoji were removed, while text size, line height, spacing, and menu width were increased.
- **Artwork menu and history lineage context (Build 580):** Removed Manual Refine from the artwork menu and added direct entries into the existing drawing-element, model, and language comparison views with the selected artwork as target. The trash action now uses an explicit white-on-red label. Selecting a bottom-history item while Lineage is open preserves that tab and centers the selected node. History responses now include generation and node state, which replace elapsed time in the bottom strip.
- **Artwork menu clipping fix (Build 581):** The card with an open menu releases its overflow clipping, while both the card and its generation are raised above neighboring lineage content. The wider menu no longer loses its left side or falls behind later-generation cards.
- **Modal artwork-menu comparisons (Build 582):** Adjust, Model comparison, and Language comparison launched from an artwork menu now open as modal dialogs targeting the selected work. Backdrop click, the close button, or Escape returns to Lineage. Opening Refine from the regular top tab retains the existing panel layout.
- **Okugaki request optimization (Build 583):** Preserved prefix-only disclosure and one observation per generation. Successful model responses are cached for 30 minutes by model, language, prefix, and image hashes, so retrying after a mid-branch timeout does not repeat completed generations. Vision payloads are reduced to 512px singles and aspect-correct 768×384 pairs. Timeouts return 504 with retry guidance, and the UI displays the detail instead of raw JSON.
- **Okugaki model persistence and lineage editing (Build 584):** The Okugaki Vision choice is stored per user separately from the general Vision default and restored on reopen. Artwork menus add Edit description and Edit DDL modal flows. Drawing saves a `description_edit` or `ddl_edit` child and returns to Lineage focused on the newest child.


### v1.89.1 — Cloudform (2026-07-18)

- **Cloudform and its selection boundary (Build 585):** Added cloudform to the core forms. Stage 1 selects it only when explicit or when the subject itself is amorphous, such as cloud, smoke, haze, stain, island silhouette, or puddle. It is never an unknown-object fallback. Stage 2 only transcribes center and size; Stage 1.5 and coerce never inject it.
- Renderer v4 deterministically combines a periodic 1/f base contour with a second signal evaluated along arc length. Local radius and curvature clamps plus a positive single-valued polar radius structurally prevent self-intersection, and output follows a 49-point closed-Bezier budget. Identical seeds replay exactly; separate performances and arranged marks receive distinct contours.
- Existing variation, proportion, touch, surface, carve, relation, place, and motion vocabulary composes without cloudform-only modifiers. Relations use the performed contour bounding box and contour points. Contour coordinates are not stored in Score, the database, or rh2 inputs.
- The CLI refinement ledger records cloudform instruction count, expanded count, sample rate, and contexts as a diagnostic mirror only. It does not create a governor, floor, or quality gate.
- **Refinement accounting:** This version removes no existing feature. It adds no dedicated modifier vocabulary or contour templates and relies only on existing vocabulary composition. The strict boundary makes unknown objects collapsing into cloudform, contours becoming fixed score data, and geometry safety expanding into aesthetic automation less likely.
- **Author acceptance:** Visual review of eight Japanese and eight English cases plus repeated performances of the same score found no broken drawing within the cloudform image. More radical contour variation is reserved for separate tuning without changing this version’s geometry safety or selection boundary.


### v1.90.0 — Touching Relation (2026-07-18)

- **Formal contact relation (Build 586):** Added `touching` as the fifth relation word with `contact: both_ends` for line/arc endpoint coincidence. Japanese and English Stage 1, Stage 2, and Saijiki select it only from explicit fixed contact phrases; it is never spontaneous.
- The renderer uses the previous instruction’s performed endpoints and reconstructs a minor arc from signed sagitta with `r=c²/(8|b|)+|b|/2`. A new arc bulges opposite a previous arc by default, minor-arc winding shares the SVG rendering implementation, and variation and stroke performance pin endpoints.
- Closed forms and endpointless targets are drop-only with a recorded warning; degenerate performed geometry is also drop-only. No coordinate repair, relation governor, or API-boundary workaround was added, and `continuing` remains deferred.
- SVG-output regression tests cover closure, cusps of at least 30 degrees, minor arcs below 180 degrees, sagitta, deterministic same-seed replay, and changed performances across 200 seeds. Formal leaf benchmark Scores 00/01/02/04 are local-only under `cli/bench/leaf/`; a 30-input general set enables no touching phrase.
- **Refinement accounting:** Closed organic contours can now retain performed placement and rotation, at the cost of adding the first exact endpoint constraint to the otherwise loose relation family. Score versioning, migration, Render Engine metadata, and the canonical rh2 payload remain unchanged.
- **Transform-composed relation coordinates (Build 587):** Unified the endpoints and outlines observed by touching, along, not-touching, cutting, and between in canvas coordinates after SVG rotation and other drawing transforms. SVG tests recursively compose ancestor transforms; regular 00/01/02/04 Scores remain separate from black-rotring judge copies. Score schema and rh2 calculation are unchanged.
- **Duplicate relation removal (Build 588):** Removed a path that could attach the same touching relation twice during Stage 1.5 processing, preserving each explicit contact once.
- **Declarative plugin documents (Build 589):** Added parsing, hard validation, and deterministic expansion for `.inku-plugin.md` manifests and bilingual word templates. Recursion, expansion above 48 instructions, fixed-coordinate repetition, namespace collisions, URLs, and file references reject the document with reasons.
- Pipeline order is Stage 1 -> plugin expansion -> core DDL -> Stage 1.5 -> Stage 2. Only explicit qualified terms and `fires_on` nouns stated as subjects may fire. Stage 1 receives the vocabulary list but not template bodies; metaphors, unknown objects, Stage 1.5, and coerce cannot inject plugin words.
- API/history provenance records the input-to-plugin term mapping without storing plugin content or dependencies in Score, canonical artwork data, or rh2. Runtime add/delete/reload, settings rejection details, Saijiki notes, `inku-cli plugin list / validate / reload`, and delete/replay/reinstall regression coverage were added.
- **Refinement accounting:** No primitive, Score field, coerce rule, artwork governor, or code execution was added. The new boundary only writes vocabulary down to the core and records provenance. It reduces recursion, stamping, metaphor over-triggering, and saved-work plugin dependency. The leaf plugin itself remains outside this build.
- **Protect structurally expanded DDL (Build 590):** Fixed Stage 1.5 appending an unrelated ellipse recipe, and Stage 2 potentially inventing another support arc, after plugin expansion had assigned numeric regions to each member. Core DDL with numeric regions is composition-resolved: Stage 1.5 adds no auxiliary shape and Score instruction count cannot exceed the explicit region count. This is a generic explicit-region boundary, not plugin-namespace injection into later stages. The minimal twin-arcs fixture stays at two visible arcs with Mistral. Qwen may still repeat those two instructions through arrangement and produce more than two visible arcs; Build 590 accepts this model-dependent distinction between instruction count and visible multiplicity.
- **Plugin format v2 acceptance (Build 591):** Fixed the implementation-attributed mismatches found by reconciling the leaf plugin (Nature.leaves v0.3.0) Stage 3 review against reference (Build 590), reflecting the approved `spec-draft-plugin-format-v2`. The expansion layer now accepts: `member name: definition` local composites inlined at each member (undefined references rejected at load); `note:` comment lines exempt from expansion and closure; a `bottom band` region and a computed upper-left-to-lower-right diagonal band (member sub-regions along a descending diagonal); load-time rejection of unknown region keys, removing the silent center fallback (runtime falls back to the default band with a recorded warning); English repetition units (`leaf forms`, `forms`, `blades`, `cloudforms`, `spots`, `arcs`) with unit-preserving singulars; `anchor ... at N to M spots` nested repetition (spots by per-anchor members, depth two); and longest-match `fires_on` resolution at a position, so the input "枯草" no longer also fires the "草" undergrowth word while phrases at different positions still fire.
- The closure marker table provisionally gains the Saijiki modifier categories (material, color, variation, angle, ratio, place) and the verbs draw and fill, sourced from reference §1, to be replaced by table-derivation in the v1.92 Saijiki restructuring.
- **Refinement accounting:** No new primitive, Score field, coerce rule, or artwork governor. Only the expansion layer's acceptance syntax and pre-execution checks grew; Score, rh2, and existing fixtures are unchanged. Japanese/English placement equivalence (guarding the 枯葉 regression) and English repetition execution are pinned by regression tests, and Nature.leaves v0.3.0 passes `plugin validate`.


### v1.92.0 — Saijiki structuring (2026-07-19)

- **Single source of truth for vocabulary (Build 592):** A saijiki table (`saijiki.py`) now owns the core vocabulary (nine categories plus relations, with prompt/display/marker flags, marker ordering, and relation phrases). The Stage 1 prompt vocabulary block and touch enumeration, the plugin closure marker table, the Stage 2 relation-phrase table, and reference §1 are all derived from it. The pre-restructuring prompts are frozen as golden fixtures; any assembly difference beyond the explicitly allowed pruning fails tests.
- **Vocabulary pruning:** By the author's decision, 描く (ja draw) and 髪 / hair were removed from the vocabulary, the Stage 1 prompts, the closure markers, and the display. The Score `Weight` enum keeps `hair` so saved works replay unchanged. The web display drops 彫る (carve, absent on the server) and freezes the static Nature.wind/undulation/stillness entries until their declarative-plugin migration.
- **Unified web delivery:** A new `GET /api/saijiki` serves core categories plus loaded declarative plugin words. The web Saijiki and vocabulary highlighting read a synchronous store seeded from a generated snapshot (`saijiki.generated.ts`, produced by codegen and pinned to the table by tests) and hydrated from the API after login.
- Verification: full pytest / ruff / web check and build, live checks on the deployment, reference dump diff limited to the pruned words, Nature.leaves v0.3.0 still validating, and a live bench (leaf mini set ja/en with five replays plus 20+20 gallery prompts) matching v1.91 firing on 19 of 20 word-language pairs (the single difference attributed to Stage 1 LLM variance).
- **Specification rebaseline:** SPEC now refers to the saijiki table / reference dump for vocabulary and constants, and internal contradictions (legacy vocabulary tables, the separated modifier-line example, the fixed model table, plugin expansion layering, broken cross-references, the unnumbered cloudform section) were resolved. Philosophy prose is unchanged.
- **Refinement accounting:** This release reduced the number of vocabulary definition sites from four to one and removed two words. It makes vocabulary drift between tables, silent prompt-assembly omissions, and irreversible pruning (the old prompt can be regenerated from table flags for A/B attribution) less likely.

### v1.93 — RAW trace option (Build 593)

- Added an observation option that returns every pipeline layer's RAW intermediate in a single generation. `/api/paint` and `/api/compose` accept `include_trace` (default false, backward-compatible); when set, the response gains a top-level `trace` with `stage1_raw` / `stage1_thinking` / `stage1_ddl` (before plugin expansion), `plugin_expanded_ddl`, `stage15_ddl` (the Stage 2 input), `stage2_raw_attempts` (every attempt including retry/fallback, each with raw text and parse status), `score_pre_coerce`, and the existing coerce/plugin aggregates.
- **A mirror, not a governor.** Collection is observation-only, threaded through the interpreter and composer via an optional `trace_sink`; it changes no interpret/expand/compose/coerce/render decision, branch, or count. Without `include_trace` the response is field-for-field identical (the `trace` key does not appear). Trace is response-only and never persisted to the database (history, lineage). For the same input and seed, Score and render_hash are invariant to `include_trace`. A collection error never fails generation; the affected key is null with an in-response warning.
- Added `inku-cli paint --trace`, which saves the response `trace` as a separate `<prefix>-trace.json` in the output directory (independent of `--full-json`; a missing trace on an older server is a warning only).
- Tests pin the invariants (unchanged response, non-persistence, score/render_hash invariance, no generation branching, auth boundary, attempt shape) with a mocked LLM. The specification text is unchanged (this is the entry point for the intent-audit trace harness, plan-intent-audit step 4.5).

### v1.94 — Input, canvas, and history UI cleanup (Build 594–599)

Web-UI-only work. The drawing machinery (Score, render, pipeline) is untouched and there are no server changes.

- **Bottom history strip cleanup (Build 594):** Removed the lineage-state badge (Active / Intermediate / Deleted) from thumbnails while keeping the state row in the hover tooltip. Abbreviated the English "Generation" label to "Gen." (Japanese "世代" unchanged).
- **Current selection relocation and a hash on the canvas status bar (Build 595):** Moved a read-only "current selection" (model, color catalog, canvas) into the input tab, below the instruction and buttons and above the input box. Removed the model/catalog/canvas display from the canvas status bar and put a render-hash button there instead (last four hex digits, uppercase — the existing `render_hash_short`); clicking it copies the full hash. Made the left panel (input, batch, demo) collapsible to the left via a thin rail between the panel and the canvas. Added mouse-wheel zoom of the canvas artwork (canvas tab with an artwork only, through the existing zoom clamp, page scroll suppressed).
- **Model display and vision cleanup (Build 596):** In the input "current selection", when Stage 1 and Stage 2 differ, show both with interpretation/rendering labels. Persist the vision model chosen during AI autonomous refinement as `vision_model` (the first choice becomes the default thereafter; okugaki keeps its own persisted `okugaki_model`, so the two stay independent by purpose). Dropped the Vision tab from the model dialog opened from the input tab (vision is not used in generation, only for readings and refinement observation). Moved the hash button to sit immediately left of the Generation Info button, and moved the Latest button to the left-side canvas nav. Added the model name to bottom history thumbnails.
- **Display refinements (Build 597):** Show input-tab model names in full (no truncation). Bottom history thumbnails now show only the Stage 1 short name; the tooltip shows Stage 1 and Stage 2 in full on separate rows. Addressed clipped description tooltips in the refinement/okugaki model picker. Aligned the "Start a new root" button with the input tab's "new" button, and the hash button with the other status-bar buttons (hash value color preserved). Removed "Move to trash" from the lineage artwork card menu (the header bulk-trash stays). Swapped the input-tab buttons to model-selection-then-catalog order.
- **Model picker tooltip placement fix (Build 598):** Switched the card description tooltip to `position: fixed` to escape scroll-container clipping. It measures the card's viewport position and opens upward only when there is not enough room below and more room above, clamping to the viewport edges. Even in a short dialog with few candidates, both the first and last rows show in full.
- **Hash button height fix (Build 599):** The hash button was shorter because its inner monospace line-height drove its box height; aligned it to the same display and line-height as its sibling buttons (Generation Info, SVG, PNG).

### v1.94 — Double-arc performance fix (Build 600, 2026-07-19)

- **Fixed region silently disabling relations**: an instruction carrying both a region (`at`) and a relation had its relation discarded without a warning during region placement, so touching resolution was never reached. Region placement now runs first and relation resolution follows, so plugin-member double arcs (leaf forms) perform as designed: endpoint-pinned opposing minor arcs (intent-audit finding F-1).
- **Recorded warnings for performance-time drops**: relations that become unresolvable only at performance time (ineligible primitives for touching, degenerate geometry, grid layouts, endpointless priors) now drop with a recorded warning per §14.4. No coordinate repair or governor was introduced.
- Verification: six new regressions (vesica formation under region+touching, determinism, in-region placement, warned drops), full pytest 511 passed. Replaying the saved leaf-bench score with the same seed now chains three touching arcs with shared endpoints, minor sweeps, and endpoint-pinned strokes. The rh2 hash contract and Score schema are unchanged; replays under the new build are new editions by design.
- **Pair separation in the expansion layer**: a member definition containing a relation literal ("place an arc, then an arc touching the previous arc at both ends") now expands into separate sentences per pair element, both carrying the same region. This structurally removes touching chains (all arcs collapsing onto one chord or curling into a ring) and stays consistent with the Build 590 explicit-region cap (2N regions for 2N arcs). The parse-time instruction budget also counts pair segments.
- **Adjective-tolerant English repetition**: ranges such as "5-7 tall blades" are now accepted (one intervening adjective; "to" excluded), with adjective-preserving singulars (one tall blade).
- **Softened fires_on drop sensitivity**: a stray namespaced reference introduced by Stage 1 now removes only the offending sentence with a recorded warning instead of dropping the whole expansion (full fallback remains only when nothing survives). This resolves the total loss observed for inputs like 菖蒲.
- **Deterministic pair transcription (A)**: pair-member sentences generated by the expansion layer (placed arc + touching arc) are now written directly into Score instructions by the expansion layer itself, bypassing the LLM. Geometry derives from member region, rotation, and seed with deterministically varied sweeps (no stamping); "bulge kept slim" maps to slimmer sweeps. A leading style sentence ("In rotring, in red.") is consumed to set weight/color while any motion remainder stays in text. Transcribed instructions merge after coerce; Score and rh2 contracts are unchanged. Even the weakest model (gemma) now performs correct leaf forms with the right material and color for every word.
- **Carriage mirror (B, inspection only)**: vocabulary explicitly written in the DDL (line styles, touches, semicircle=180°, three unambiguous movement words) is checked against the final Score, surfacing `carriage_warnings` in responses only. No retries, gates, or DB storage.
- **Contrastive examples (C)**: four ja/en composer examples (dashed, burin, semicircle=180°, broad+pink) raise first-pass carriage across models.


### v1.95 — UI phase 2: refinement flow and DDL editor consolidation (Build 601–604, 2026-07-19)

- The three comparison dialogs opened from the artwork menu (elements / model / language) now show only their own view, and the standalone refine output tab was removed (output tabs are Artwork / Lineage; reachability moves to the artwork menu, and starting refinement from unsaved previews was retired as off-concept).
- The instruction tab was restructured around instructions. The normalized-DDL area is read-only (highlighting plus a Saijiki toggle), while DDL creation and editing consolidate into a shared editor dialog (line numbers, highlighting, Saijiki insertion). "New from DDL" starts blank and saves as a standalone work (new lineage root, `display_label='DDL'` badge with the first line shown). Instruction-based actions (edit description, change reading, model/language comparison) are hidden for DDL-origin works. The auto-repair toggle moved into the settings modal.
- The Drawing output tab was renamed Artwork; the canvas top bar shows the generation (Gen. N / Standalone). Autonomous-refinement elements now use concrete action names with speed/cost tooltips and an always-visible generation stepper. Lineage keeps scroll position when expanding children, the overview button reads Close, and closing a comparison dialog refreshes the lineage. Web-only changes; server, Score, and rh2 are unchanged. APP_VERSION is v1.95.0.

### v1.96 — Plugin management, scenery level, UI phase 3 (Build 605–606, 2026-07-19)

- **User plugin management API (Build 605):** five new admin endpoints under `/api/plugins` — content read, create (load from file), overwrite, delete, and enable/disable (validation failures return 422 reasons; filename collisions 409). Items gained `id` (= file basename) and `enabled`, and status gained `disabled`. Disabling keeps the document but removes it from expansion, prompt vocabulary, and collision reservation; the disabled set persists in `.plugin-state.json` inside the plugin directory and joins the reload signature. Writes that would newly reject any loaded plugin through cross-file collisions (in either load order) are rolled back and reported as 422. The settings-modal user-plugin section (switches, delete, load-from-file, code view/edit) is now fully functional.
- **Scenery level tenkei (Build 605):** a new generation option `tenkei` (`none` / `sparse` / `auto`, default auto = current behavior) on `/api/paint`, `/api/compose`, `/api/interpret`, and `inku-cli --tenkei` lets the user choose how much scenery accompanies the subject. The level maps deterministically onto all three layers before generation: Stage 1 gets level-specific norm sections (at none, inputs consisting solely of plugin terms bypass Stage 1 entirely), Stage 1.5 shrinks its candidate pools (none = no additions, focal rewrite kept; sparse = one candidate), and coerce gates its autonomous scenery insertion branches behind an insertion budget (none = 0, sparse = 1; complex-motif joins the gate when plugin transcription already carries the subject). Repair and mutation branches run at every level, and no post-hoc thinning governor was introduced. tenkei is recorded in responses and history metadata but excluded from rh2. Live bench (gemma): scenery count for `Nature.紅葉` drops auto 127 → sparse 87 → none 1.
- **Pair-transcription guard restoration (Build 605, §4.6):** Build 600 pair transcription left no numeric region sentences in the expanded DDL, silently bypassing the Build 590 Stage 1.5 addition guard. Additions are now also suppressed whenever plugin expansion produced instructions, at every level (removing boilerplate scenery such as overtone arcs and leaf-piece ellipses).
- **Lineage generation fix (Build 605):** `get_lineage` / `get_lineage_branch` now attach the computed `lineage_generation` to each `node.history`, fixing artworks opened from the lineage panel showing "Standalone" (single shared computation with the history list).
- **UI phase 3 (Build 606):** the mascot became a 5×5-pixel inku cube and the single-generation progress bar was replaced by a one-line status (cube + dynamic stage name + elapsed + stop). The same status element now appears in the DDL editor, description edit, autonomous refinement, model comparison, and language comparison dialogs, each with a working abort (AbortSignal wiring). Language comparison was redesigned as direct Stage 1 × Stage 2 combination cards. Model metadata is single-sourced in `modelMeta.ts` + `ModelMetaCard`, giving comparison cards the same description-card design as the instruction tab. Kiwi and crab mascots are preserved but not rendered (temporarily hidden per the author).
- **Refinement accounting:** no new primitives or Score fields. tenkei is a deterministic pre-generation switch (an explicit user operation, like vary); the only coerce change is suppression of insertions, never new ones. What grew is the plugin-document management boundary (validation gates every save); what became harder is scenery overwhelming the subject, boilerplate scenery after pair transcription (the bypassed contract), and lineage-generation display drift.

### v1.97 — Per-artwork scenery level with lineage inheritance, UI wiring (Build 607–608, 2026-07-19)

- **Per-artwork persistence and parent inheritance (Build 607):** v1.96 left tenkei as a request-time option, so any refinement running at the default silently returned scenery to a lineage begun at "none" — a defect surfaced during UI wiring (with systematic 10-plus-generation trials the initial choice was lost). By author ruling the level is now stored per artwork: history gains a `tenkei` column (additive migration, NULL = works saved before this build), and resolution — **explicit request value > inherited from the lineage parent's artwork > auto** — happens server-side, so AI refinement loops, the CLI, and every client keep a lineage's level without resending it. `/api/paint` inherits via `lineage_parent_node_id`, `/api/compose` gains an inheritance-only `lineage_parent_node_id`, and `POST /api/history` resolves at save time so renderer-only derivations (touch variation) never break the chain. Request `tenkei` became optional (omitted = inherit); responses, history items, and lineage `node.history` return the resolved value. Excluded from rh2.
- **UI wiring (Build 608):** the instruction tab's generation-control row gains the scenery selector (None / Sparse / Auto, localStorage-persisted, sent explicitly for new roots). Six refinement dialogs (element comparison, model comparison, language comparison, description edit, DDL edit, autonomous refinement) show a compact 3-way selector **defaulting to the parent artwork's level with an inheritance mark**; untouched, no tenkei is sent and the server inherits — changed, the explicit value becomes a branching point in the lineage. Touch/color replays (renderer-only) show no selector. The generation-info dialog and history tooltip display a Staffage row for works with a stored level.
- **Refinement accounting:** the level's semantics are unchanged from v1.96 (three-layer mapping, no governor). What grew is one column letting an artwork remember its own generation condition and a single inheritance-resolution point; what became harder is silent level loss through refinement and drift between display (generation info, tooltips) and actual behavior.

### v1.98 — Description-panel rework, paint streaming, model catalog v2 (Build 609, 2026-07-20)

- **Paint-stage streaming:** single drawing moved to the new `POST /api/paint/stream` (NDJSON). A `stage1` event fires as soon as interpretation completes (normalized DDL, models used, token counts, elapsed time, fallback flag) so the interpretation appears while Stage 2 and rendering continue; the final `done` event carries the same `PaintResponse` as before. `POST /api/paint` became a wrapper over the same generator with an unchanged response shape, so the CLI and Android need no changes. Running indicators were standardized on a shared `RunStatus` component and stop wording unified. This also fixed a pre-existing bug where `RequestSizeLimitMiddleware` never delivered `http.disconnect` after its buffer drained, blocking StreamingResponse forever (surfaced by streaming, not caused by it).
- **Input-side vs. expanded DDL split, explicit focus:** history gains three columns — `expanded_ddl`, `focus`, `interpret_fallback` — and `ddl` is redefined from "expanded" to "input side" (the user's text or the Stage 1 output). A one-time backfill moves existing works' `ddl` into `expanded_ddl`; the input side was never stored and cannot be recovered, so older works keep their content with an "expanded" label. `/api/paint` and `/api/compose` accept `focus` (omitted = deterministic choice from the DDL text as before; unknown values are treated as omitted), and responses add `focus` and `source_ddl`.
- **Empty interpretation is a failure, fallback marking:** an empty Stage 1 output is treated as a failure instead of drawing from nothing. Works drawn through a fallback path record an `interpret_fallback` reason and are marked in the UI. Single-drawing history saving moved from client submission to server-side saving.
- **Model catalog v2 (per-purpose recommendations, EOL, measured rebuild):** the verified NVIDIA NIM catalog was rebuilt from two measured benchmark runs over 86 models (3 LLM runs + 1 Vision run each, repeated at a different time of day) — `MODEL_CONFIG_VERSION` 2.0.0, 29 → 43 entries. Recommendations split into `recommendation_llm` / `recommendation_vision` (the old single key is read-compatible only). Two retired models stay listed as unselectable EOL entries at the end rather than being removed, preserving model references in saved works (no model disappears from the catalog; no history-referenced model is missing). Every model list now orders EOL last, then the purpose-at-hand recommendation descending, then label (`sortModels()`, one code path for all four render sites). The 502 `detail` extends to a kind object (`model_gone` / `provider_auth` / `provider_rate_limit` / `provider_error` with stage and provider message) that the UI explains per kind; the legacy string path is kept.
- **Browse-only Saijiki and UI cleanup:** the Saijiki toggle moved to the canvas toolbar and the drawer became browse-only (chip click = preview). Word insertion is concentrated in the DDL editor dialog's inline Saijiki, which now also lists loaded plugin vocabulary. Two unused legacy DDL edit panel components (734 lines) were removed. The generation label is prefixed with its lineage and matches the model meta typography, the remaining describe-tab controls gained tooltips, a theme-aware `--danger` variable was defined, and the 12 DDL token color classes were unified into global definitions fixing dark-theme readability.
- **Verification:** pytest 548 passed on Mac and pentala, ruff clean, `npm run check` 0 errors, build success. The pentala DB migration ran after a backup with count verification (1565 works).


### v1.99 — Render core: variation performance on arcs and closed shapes (F-4, Build 610, 2026-07-20)

- **Extended variation performance:** `variation`, previously performed only on lines, now also performs on arcs and closed shapes (circle, ellipse, triangle, square, polygon). The gate mirrors the line gate: `quality ∈ {perlin, wave, white}` with `dimensions ∩ {position_x, position_y, radius} ≠ ∅` (radius added as a shape's natural axis). Circles and ellipses use an 80-segment contour with seam-continuous periodic noise (perlin wraps its lattice mod freq; wave closes automatically at integer frequencies); squares, triangles, and polygons perform each edge with line variation while keeping corners fixed; arcs subdivide and offset with both endpoints exactly fixed, preserving the touching contact contract. The pink (blur) and quality=none paths are unchanged, and arcs reconstructed by touching go through the same branch.
- **Dead-field audit resolution:** rerunning the A/B intent audit under identical conditions turned 11 of 16 previously-SAME cases (position/radius axes) into DIFF; the 5 rotation/thickness/angle/length cases remain SAME as designed (a separate SPEC §17.A item outside this gate). The composer already emits variation with position dims on squares and ellipses, so the gate fires on real scores.
- **Render engine version 5:** because the same Score and seed now render differently for affected works, the engine version was bumped from 4 to 5 (coordinator's ruling). Past works carrying variation on arcs or closed shapes will look different on replay; saved SVGs, Scores, and rh2 are untouched.
- **Verification:** pytest 578 passed / 39 skipped (42 new tests: 6 shapes × 3 qualities, per-dims performance, byte-identical output with the gate closed, determinism, pink blur preservation, arc endpoint fixation, polygon corner fixation, periodic sampler seam continuity). Zero regressions, ruff clean. No Score schema, coerce, or rh2 changes. At merge time, three local-only leaf-bench touching checks were found failing because performed arcs are now emitted as polyline elements (the render worktree had skipped them for lack of the fixture directory). A follow-up extended the SVG arc extractor to polylines (endpoint-constrained circle fitting, minor-arc rederivation from the swept angle, and a gate/shape agreement assert) with zero relaxation of the checks; final counts 602 passed / 30 skipped.
- **Remaining:** the author's visual confirmation of real performances ("trembling circle", "slowly swaying square", "trembling arc", improvement of the leaf "ruler arc" issue), the Phase 2 decision on material-outline tracking, and amplitude/frequency tuning if needed (currently shared with lines).


### v2.0 — Stage 1.5 variation ("hensou"), retirement of the focus input (Build 611, 2026-07-20)

- **Variation — shaking the expansion layer deterministically:** `expand_intermediate_ddl()` gains `variation_amplitude` (small/medium/large) and `variation_seed`. A variation plan is built once, deterministically, from (amplitude, seed) using only `_seed()` hashes, and moves seven axes — type swap and adopted count (tier 1), touch material, focus, and main/contrast colors (tier 2), composition family and type family (tier 3) — under weighted staged release: small moves one tier-1 axis, medium one or two axes from tiers 1–2, large two to four from all tiers. Small never moves the composition family or focus. Unless both arguments are present the expansion is byte-identical to the previous behavior, preserving reproducibility of existing works.
- **Visibility guarantee (two steps beyond the contract):** shifting a value does not always change the output (e.g., no adopted sentence contains `{touch}`), so the plan (1) test-runs each axis alone and nudges its offset (up to 8 times, replacing the axis with another allowed one if it still cannot move) and (2) detects combinations canceling back to the default, dropping axes one by one. About 2 ms per large run. As a side effect, descriptions with too few movable axes may move fewer axes than the nominal range — visibility wins. `moved_axes` reports only axes whose lone application actually changed the output, as official-vocabulary from → to pairs shown as chips on candidate cards; the 34 expansion candidates (ja/en) received hand-written display labels.
- **tenkei orthogonality:** under `none` only the focus axis is eligible; under `sparse` totals are re-clamped to one; count shifts apply after the cap and never exceed it or the real pool length.
- **API and candidate generation:** `PaintRequest` / `ComposeRequest` gain `variation_amplitude` / `variation_seed`, responses gain `variation_moved_axes`. Seed issuing lives in the new `POST /api/variation/seeds`; the four candidates reuse the existing candidate-grid machinery (parallel fetch, progress, abort). History gains two columns, `variation_amplitude` / `variation_seed` (`moved_axes` is recomputable, so no column). `hensou` joins `LINEAGE_DERIVATION_KINDS`.
- **Focus input retired:** `PaintRequest.focus` / `ComposeRequest.focus` / `_validated_focus` and the "vary focus" refinement UI are removed; refinement is back to four kinds (touch, layout, reading, color). The expansion-layer focus machinery, default hash selection, `history.focus` column, and `HistoryPostBody.focus` remain. The layer's resolved focus is wired into render_metadata as `resolved_focus`, so `history.focus` never goes NULL — ordinary drawings record the default hash choice.
- **UI:** the adjust dialog gains a variation section (three amplitudes + a draw button) separate from the four refinement radios; candidate cards show moved-axis chips. The word is 変奏/variation throughout — never 揺らぎ/wobble.
- **Verification:** pytest 636 passed / 30 skipped (+34: 30 in `test_variation.py`, 5 in `test_api.py`, −1 removed focus test), covering all seven contract test items (reproducibility, byte-identity when off, staged release swept over seeds 0–199, visibility, tenkei orthogonality, report consistency, focus-retirement regression). ruff clean, `npm run check` 0 errors, build success.
- **Known follow-ups:** the variation section's placement differs from the author's mental image and will be reworked (the feature ships as is). `loadIterationItem` does not restore variation fields (they are saved, so recomputation stays possible; same treatment as focus). A pre-existing `api_history_neighbors` bug (history `score` passed to the comparison as a raw string) was confirmed in pentala logs — unrelated to variation, not yet fixed.


### v2.0.1 — Model catalog v2.1 (late-night bench run folded in, Build 612, 2026-07-21)

- **Third measured run integrated:** the late-night benchmark of the 86 NVIDIA NIM models (2026-07-20 23:38 → 02:54 the next morning, 86/86 completed, no sleep interruption) joins the midday and evening runs, so scores now combine three measured runs (`MODEL_CONFIG_VERSION` 2.0.0 → 2.1.0, 43 → 44 entries). `openai/gpt-oss-20b` enters the catalog; nothing is removed. Speed labels now name all three runs. The rubric is unchanged, but rank 5's "clean across every run" bar gets stricter with each added run: four promotions (`qwen/qwen3.5-397b-a17b` and `z-ai/glm-5.2` to 5, `meta/llama-3.3-70b-instruct` and `poolside/laguna-xs-2.1` to 4) and seven demotions (`mistralai/mistral-nemotron` 5→4, `minimaxai/minimax-m3` 4→2, and five others). No model referenced by stored artwork drops out (preservation check clean).
- **Time-of-day question closed:** all three runs land within 3 h 16–20 m of total wall time; late night lowers the response median (62.8 s → 41.9 s) but fallbacks rise monotonically (35 → 37 → 38) and cancel the gain. The "an idle window will be faster" hypothesis is retired. Details in `no-git-sync/fable5/mode-api-claude/RUN-LOG.md`.
- **Verification:** pytest 636 passed / 30 skipped (test changes only update asserted catalog values), `npm run check` 0 errors. Deployed to pentala.


### v2.0.2 — Two small bug fixes (history-neighbors 500, variation fields lost on history load, Build 613, 2026-07-21)

- **`GET /api/history/{id}/neighbors` no longer 500s:** `list_neighbor_candidates()` was the only history path returning `history.score` (a TEXT column holding JSON) without `json.loads`, so `score.get(...)` inside `composition_distance()` raised `AttributeError: 'str' object has no attribute 'get'` on every call (79 occurrences on pentala since 2026-07-20; with even one stored score among the candidates the endpoint always failed, so the neighbors feature was effectively dead). The new `_neighbor_score()` parses the column and falls back to `{}` on broken JSON, NULL, or non-object values without dropping the candidate. One regression test added — confirmed to fail with the exact AttributeError before the fix (`history.score` is NOT NULL, so the NULL case is exercised via an empty string instead).
- **Variation fields survive a history load:** `loadIterationItem()`'s `result` reconstruction gains `variation_amplitude` / `variation_seed` (saving was already wired in v2.0; reloading left them undefined). `focus` stays unrestored — the external input was retired. Consuming the restored values (e.g., carrying the seed into a re-run) is out of scope.
- **Verification:** pytest 637 passed / 30 skipped (baseline 636 + 1 new), ruff clean, `npm run check` 0 errors. Implementation report: `no-git-sync/fable5/claude_code/tasks/small-bugs-v202-result.md`.



### v2.0.3 — Variation as the fifth refinement element, adjust-dialog rework (Build 630, 2026-07-21)

- **Variation becomes the fifth refinement element (author's ruling):** the v2.0 known follow-up — the variation section's placement — was settled as ruled. `RefineKind` gains `hensou`; the radios now read layout / reading / color catalog / variation / touch words (moved above "touch words" by author instruction). Only while variation is selected, the amplitude choice (small/medium/large, default medium) appears indented under its radio, each amplitude carrying a tooltip matching the staged release in `ddl_expander.py`. Execution folds into the existing one/four-candidate buttons (one candidate = one fresh seed, four = four, issued by `POST /api/variation/seeds`). The "draw variation" button, the separate section, and their i18n/CSS are removed. As a by-product of the integration, variation candidates now also run the lineage parent-visibility pass (`ensureVisibleLineageParentId`).
- **Thirteen incremental author-directed refinements to the dialog, candidates, and card menu:** the speed indicator moves to its own row right under the draw buttons. Candidate save buttons are right-aligned and now have three states — unsaved, saving, saved — with saved ones unpressable. The candidate grid fits the window (`max-height`, fixed aspect-ratio dropped), and a single candidate fills the full width. The chosen refinement element is remembered in `localStorage` (falling back to `touch` when `reading` is hidden). A dashed placeholder keeps the candidate area visible before any draw. The artwork-card menu gains the heading "Edit this artwork", is reordered to drawing elements → description → DDL → model → language → autonomous AI refinement, with labels shortened to the target noun and tighter rows. The adjust/model/language dialogs are retitled "Edit drawing elements" / "Edit model" / "Edit language". Random autonomous refinement now states the condition under which the direction text applies (reading generations only; behavior unchanged).
- **Variation joins autonomous AI refinement:** the server's `ALLOWED_KINDS` gains `hensou` and the enabled-kind cap rises from 4 to 5 (a two-file, two-line server change; the contract's "no server changes" clause was lifted by the author). Variation generations fix the amplitude to medium with server-issued seeds. The web `PaintOptions` gains `variationAmplitude` / `variationSeed`.
- **Button size tokens (ruled into convention):** `+page.svelte`'s `:root` gains `--btn-sm-font-size` / `--btn-sm-padding` / `--btn-sm-radius`; `InputPanel`'s `.ghost-btn` and `LineagePanel`'s buttons are converted (`.ghost-btn` was defined in ~37 places, so fixing one side kept drifting from the others). The convention — components whose button CSS is touched migrate to the tokens incrementally — is recorded in `docs/inku-dev-conventions.md` §3-2-1 and AGENTS.md, together with the standing approval for code-change rsync + restart + build numbering (§4-5).
- **Verification:** pytest 637 passed / 30 skipped (only the two-line server change; zero regressions), ruff clean, `npm run check` 0 errors (2 known a11y warnings), build success — reconfirmed on the main checkout after merge. Builds 614–629 were deployed to pentala incrementally during implementation and checked by the author on the live screen. Implementation report: `no-git-sync/fable5/claude_code/tasks/hensou-ui-5th-refine-result.md`.
- **Remaining:** autonomous-refinement variation is fixed at medium amplitude (no UI choice). Size-token migration covers two components so far (incremental by convention).


### v2.0.4 — Autonomous-refine variation amplitude, size-token migration completed (Build 634, 2026-07-21)

- **Variation amplitude choice in autonomous AI refinement:** the amplitude, fixed to medium in v2.0.3, is now selectable. Only while the variation element checkbox is on, a small/medium/large choice appears indented below it (default medium, same pattern and tooltips as the adjust dialog's inline amplitude, reusing existing i18n keys). The chosen amplitude applies to every variation generation in the run; running with the default is identical to v2.0.3. The change touches `AIRefineModal.svelte` only — no server change (the amplitude still travels per generation via `PaintOptions`). The three amplitude tooltips open upward because the modal body's scroll container would clip them.
- **Small-button size-token migration completed:** conversion to the `--btn-sm-*` tokens now covers everything. Twelve fully-matching blocks across ten files (nine `.ghost-btn` plus `.danger-btn`/`.confirm-btn`/`.ddl-new-btn`) were straight substitutions with no visual change. The six partially-matching blocks initially held back were also unified under the author's ruling ("convert the unconverted ones too"), changing dimensions in the demo panel, unread-words panel, profile dialog, auth panel, and the history manager's lineage-member row (9px→11px); the lock badge stays visually identical (its pill radius alone remains untokenized). All 14 files defining `.ghost-btn` are now migrated and no small button hardcodes the metrics. Colors, hover, and disabled styles are untouched throughout.
- **Verification:** pytest 637 passed / 30 skipped (no-server-change regression), `npm run check` 0 errors (2 known a11y warnings), build success on Mac and pentala — reconfirmed on the main checkout after merge. Builds 631–633 were deployed to pentala incrementally; the author checked the amplitude UI and token conversions on the live screen (a tooltip clipping issue was fixed in Build 632). Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-v204-followups-result.md`.
- **Remaining:** the author's visual confirmation of v1.99 F-4 and the unimplemented SPEC §17.A dimensions (awaiting ruling) carry over.


### v2.0.5 — Seed-derived wave phase, material outlines follow the performance seed (F-4 Phase 2, Build 636, 2026-07-21)

- **Wave seed-independence bug fixed:** the author's F-4 visual check revealed that variation candidates, touch changes, and re-performances never changed the wobble. The cause: the wave branch of `_sample_offset` / `_sample_offset_periodic` was a fixed-phase `sin(t·2πf)·amp` that ignored the seed (behavior dating back to the line-only era; perlin/pink/white are seed-dependent). `_wave_phase(seed) = _hash01(...)·2π` now feeds `sin(2π·t·freq + φ(seed))·amp`. Closure of closed contours at the integer frequencies (slow 2 / medium 6 / high 14), exact arc endpoints, and fixed polygon corners (per-edge independent phases) are preserved.
- **Material outlines follow the performance seed (Phase 2):** `render_seed` is threaded into the five material-outline sites (`_add_material_{circle,ellipse,rect,arc}_outline`, `_material_line_group`), now calling `_seed_for_instruction(ins, render_seed)`. Shape parameters (offset/width/opacity/dash/speck) keep their ranges and vocabulary. With no performance seed the output is byte-identical to before.
- **Render engine version 5 → 6:** the same Score and seed now render differently. Three expected values updated in `test_api.py` (historical literals and `test_cloudform.py` untouched).
- **Verification:** pytest 693 passed / 30 skipped (56 new tests in `test_renderer_wave_phase.py`: seed-following across 7 primitives, determinism, closure, endpoint/corner pinning, material-outline seed differences, None backward compatibility). The new tests were stash-verified to fail 11 cases against main's implementation. Naive full-SVG comparison was always-unequal because of the display touch filter, so comparisons strip it first; a Mac/Linux sin/cos last-digit difference broke a golden hash and was fixed by 6-digit rounding, matching across all three environments. PNG comparison artifacts: `cli/out2/635-v2.0.5-wave-phase/` (3 scores × seeds 111/222/333, replay, contact sheet).
- **Author check:** confirmed in the UI that variation candidates and touch changes now change the wobble, with the caveat that "even large variation is not a striking amount of change" — attributable to the absolute-px amplitude design (`AMPLITUDE_PX` fine 7 / medium 12 / broad 30, i.e. 0.7–3% of the 1000 px canvas). **Plan B — amplitude proportional to shape size — is to be contracted next** (expected to re-bump the engine to 7).


### v2.1.0 — Rendering moved from absolute pixels to proportional systems, material strength recalibrated (Build 638, 2026-07-21)

- **A layer (wobble / bleed) now proportional to shape size:** the amplitude vocabulary fine / medium / broad changed meaning from absolute pixels on a 1000px canvas (`AMPLITUDE_PX` 7 / 12 / 30) to ratios of a shape's representative size, `AMPLITUDE_RATIO` (0.025 / 0.08 / 0.18, candidate P3 chosen in author calibration). Representative size: radius for circle / polygon / arc, geometric mean of the radii for ellipse, half the short side for square / triangle / cloudform, line length for line (floor `canvas.unit × 0.02`, amplitude cap 40% of representative size). Bleed (pink) uses `BLUR_RATIO` 0.009 / 0.03 / 0.07 (P3). Polygons share a single representative-size-derived amp across all edges to avoid anisotropy. Contour segments went from fixed 80 to length-proportional (clamp 32–200), stroke samples from fixed 49 to length-proportional (clamp 17–129). Blur filter ids now embed the std value (`blur-{amplitude}-{std*10}`).
- **B layer (material) made `canvas.unit`-relative with strength recalibration:** stroke widths, dasharrays (including 14 hardcoded ones), texture filters (`TEXTURE_FILTERS` replaced by `TEXTURE_SPECS` + dynamic XML generation, `baseFrequency` inversely proportional to unit), material outlines, and specks all scale by `s = unit/1000`. Speck counts went from fixed 18/28/36 to perimeter-proportional (anchor = circle of radius 0.2, floor 10). A two-round calibration prompted by the author's "the material effects barely register" adopted intensity level **s1**: floors on material-outline offset (`unit × 0.0035`), outline opacity (0.50), speck opacity (0.40), and a speck count gain of 2.6 — a floor scheme that lifts only the weak pencil / crayon while leaving the already-readable brush weights untouched. Texture filters stay as-is per the author's ruling (option 3). Material outlines now carry `class="material-outline"` (mechanical separation from primary lines; the touching test switched from an opacity threshold to the class).
- **C layer (display filter) made `canvas.unit`-relative:** `_performance_touch_filter` scales `baseFrequency` / `scale`; formatting unchanged.
- **Render engine version 6 → 7:** the same Score and seed now render differently. At `unit=1000` the material dimensions, dasharrays, stroke widths, texture filters, and display filter are byte-identical to v2.0.5 (the only differences are perimeter-proportional speck counts and length-proportional stroke samples, proven by an experiment reverting counts alone). Public `/api/reference` keys renamed to `amplitude_ratio` / `blur_ratio` / `segment_target_ratio` etc.
- **Verification:** pytest 724 passed / 30 skipped (31 new tests in `test_renderer_proportional.py`), ruff clean. The legacy golden at `test_renderer.py:1590` (weight=pen, no variation) passes unchanged, serving as the stroke-width byte-identity sentinel. The perlin / white "double size → double amplitude" check uses a rel=0.25 tolerance because the seed depends on the instruction content; exact proportionality is guaranteed by testing `_amplitude_px` directly. Calibration artifacts: `cli/out2/637-v2.1.0-proportional-calibration/` (bleed and material comparisons moved to HTML/browser after discovering cairosvg also skips `feGaussianBlur`). Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-v21-proportional-result.md`.
- **Remaining:** texture filters stay below the perception threshold (pencil displacement ≈ 1px on screen; the real fix is the PNG rasterizer contract). `PRIMITIVE_AMP_GAIN` is an empty dict (no line damping; a one-line tweak if real works need it). Follow-up contracts: hand strokes for closed shapes (`opus-closed-shape-strokes.md`) and a filter-capable PNG rasterizer (`opus-png-filter-rasterizer.md`).


### v2.2.0 — Closed-shape contours drawn with hand strokes (Build 640, 2026-07-21)

- **`synthesize_along` in `stroke_engine.py`:** stroke synthesis along an arbitrary centerline, sharing the full ToolGrammar (damping, latent energy, catch/fade/correction, taper/bulge) with the line-only `synthesize_stroke` — only the target path changes. Output is a filled band of the two banks (outer + inner subpaths, `fill-rule="evenodd"`, `class="contour-stroke-v1"`). Corner `anchors` pin ideal positions and reset the follower (a corner is a brush seam); cornerless closed contours close their seam with a linear ramp (envelope floor 0.35 so the seam thins without looking cut). **The integrator feed-forwards the intended step and leaves only the residual to the spring** — absolute-position tracking showed ±10% radial distortion on curved paths in measurement, prompting the redesign; all weights now stay within ±1% radius deviation with corner overshoot ≤ 1.42px.
- **Closed-shape path (circle / ellipse / square / triangle / polygon):** all GRAMMARS weights except rotring participate (same gate as lines). Body elements stay geometric; solid style sets `stroke="none"` so they only carry the fill (bbox, touching, and coordinates untouched). Dashed/dotted styles keep a 0.42×-thinned geometric contour since the line style is itself the description. The band's centerline is the contour **after variation is performed** (contract item 4). Material outlines and specks coexist with the band (item 5). Drypoint burrs are emitted as `<polygon>` to avoid arc-extractor false positives. Arcs are excluded by author ruling (the touching test's arc extractor only counts `<path d="M..A..">` / `<polyline>`; a follow-up contract will redesign the extractor), as is cloudform (it has its own contour generator).
- **Render engine version 7 → 8:** the same Score and seed render differently. Line and arc output is byte-identical to v2.1 (pinned by the unchanged `brush_thin_line` / `crayon_arc` digests in `MATERIAL_NONE_SEED_DIGESTS`).
- **`test_gate_closed_output_unchanged` reinterpreted (author-approved):** the old byte-identity claim was F-4's (v1.99) containment guarantee, not a permanent contract, and already failed on main for every weight except pen / rotring (`_seed_for_instruction` includes `variation` in the seed key whether or not it is performed). Renamed to `test_gate_closed_geometry_unchanged`, checking that the intent geometry (everything except the hand-stroke layer) is unchanged — a wrongly opened gate still fails it because the body element turns into a wobble polygon. The alternative ("unperformed variation should not affect the seed") is parked with the `filled`-semantics bundle of appearance-changing candidates.
- **Verification:** pytest **852 passed / 30 skipped** (128 new tests in `test_closed_shape_strokes.py`: band presence, determinism, seed-following, corner pinning, seam closure, evenodd structure, post-variation synthesis, material coexistence, dashed geometry, rotring invariance), ruff clean on Mac and pentala. Band adds 1.5–19KB per shape, max 29KB (drypoint octagon + broad/high variation). **Author visual check OK** (2026-07-21, live pentala UI). Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-closed-shape-strokes-result.md`.
- **Discovered along the way (awaiting author ruling):** closed shapes are always filled regardless of `filled=False` (`_stroke_attrs`'s `do_fill` is unconditionally True for closed shapes — a dead field), a prime suspect for "the material effects barely register". Reference captures: `cli/out2/639-v2.1.0-closed-shape-strokes/unfilled-*`. Also recorded is a three-prototype investigation of "fill = covering the interior with fine strokes" (`synthesize_along` reusable as-is, no clipPath needed, spacing floor essential, 40–63KB per shape, engine 9 class). Per author instruction, deferred until pending items are done.


### v2.2.1 — Filter-capable PNG rasterizer (resvg), Python 3.12 unification (Build 643, 2026-07-21)

- **resvg-py 0.3.3 adopted:** cairosvg silently ignores `feTurbulence` / `feDisplacementMap` / `feGaussianBlur`, so texture filters and bleed were missing from every PNG path (downloads, AI Vision input, Okugaki thumbnails, CLI). A measured comparison (librsvg, Playwright, skia-python, resvg CLI) selected resvg-py: renders all three filters, a 1.2MB wheel with no system dependencies, MIT/MPL-2.0. A shared helper `shared/src/inku_analysis/rasterizer.py` (`svg_to_png` / `rasterizer_backend` / `rasterizer_info`) prefers resvg, falls back to cairosvg, and raises `RasterizerUnavailable` otherwise; the missing-dependency warning + skip behavior is preserved. Okugaki's nested data-URI `<image>` SVGs render under resvg too.
- **Nine call sites replaced, contract corrected:** three server paths (`api.py` PNG download, `autonomous_refine.py` Vision input, `okugaki.py` thumbnails) plus five CLI sites (`paint --png`, `analyze --replay`, `history-export`, `refine --png`, `inspect --png`). The contract's claim that CLI PNGs reduce to the server API was wrong: **the API has no PNG endpoint** (its responsibility ends at the SVG string); the CLI rasterizes the response's `svg` in its own process. With author approval the CLI sites now use the same helper.
- **Python 3.12 unification (author-approved option 1):** resvg-py ships no macOS cp310 wheel, so a 3.10 venv triggers a 2-minute Rust build. `requires-python` raised to `>=3.12` across `server` / `cli` / `shared`, `.python-version` unified, `uv.lock` regenerated. pentala uses the linux cp312 manylinux wheel — no Rust build.
- **Backend visibility (follow-up implementation, author ruling):** against the risk of a silent cairosvg fallback making PNGs look cleaner than the actual work, (a) the CLI warns once per process on stderr when falling back, and the server logs resvg = INFO / fallback or absent = WARNING at import (journal delivery verified on pentala); (b) PNG outputs record `paths["png_rasterizer"]` (backend/version) in artifacts and `summary.json`. Server and CLI venvs produce byte-identical PNGs from the same SVG (measured, resvg-py 0.3.3). Lock unification (uv workspace) is deferred to a separate contract by author ruling.
- **Editable install (deployment trap removed):** `inku-analysis` was installed as a copy into each venv, so rsyncing `shared/` and restarting kept running stale code until `uv sync`. `editable = true` under `[tool.uv.sources]` removes the trap; `shared/` changes now take effect with rsync + restart.
- **Unchanged:** SVG bodies, `render_hash` (rh2), and the render engine version (8). PNGs are not hash material.
- **Verification:** server **863 passed / 30 skipped** (11 new tests in `test_rasterizer.py`: the two-directional claim that cairosvg drops and resvg renders the filters, fallback, absence path, four output-size patterns identical across backends, `rasterizer_info`), cli **68 passed** (3 new: metadata recording, warn-once, no warning under resvg), ruff clean. All 15 stage2 fixtures × 3 profiles = 45 rasterizations error-free. pentala also at 863/30 (Python 3.12.13, backend=resvg). Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-png-filter-rasterizer-result.md`.
- **Handover notes:** newly generated PNGs are pixel-incompatible with old ones (filters now render; direct pixel comparison against past `cli/out2/` runs is invalid). resvg costs ~6× cairosvg (207ms at 768px). cairosvg stays as the fallback dependency. Syncing the CLI to pentala is separate if that workflow is ever used. The dormant Android script's cairosvg call is out of scope.


### v2.3.0 — Stroke fill for closed shapes, `filled` restored, surface hatching as brushwork (Build 645, 2026-07-21)

- **Closed-shape interiors move from area fill to stroke fill (`fill-stroke-v1`):** interior rendering is now a three-way choice (`surface` specified = surface vectors / `filled=True` = fill-stroke group / `filled=False` = nothing; the body element's fill is `none` in every case). `_fills_interior` replaces `_stroke_attrs`'s `do_fill` (which unconditionally filled closed shapes — a dead field), **restoring the semantics of `filled`** (`True` = fill the interior with material strokes / `False` = contour only). `_scanline_segments` intersects scanlines with the closed contour and returns interior spans (half-open edge tests avoid double-counting vertices; no clipPath; concave shapes work as intersection pairs). `_render_fill_strokes` renders one span = one brush stroke through `synthesize_along(closed=False)`, pulls endpoints half a stroke-width inside the intersections, and alternates direction per scanline. Per-stroke seeds come from `_fill_stroke_seed` (sharing the contour band's seed would repeat the same energy waveform inside). Opacity follows the color hint's fill side (`fill_opacity`, else `stroke_opacity`). Cloudform scans a dense polygon sampled from its drawn curve.
- **Four author rulings before implementation (2026-07-21):** scan angle = seed-derived, uniform over 0–180°, spacing `max(stroke width × 1.5, canvas.unit × 0.012)` with ±12% jitter (full coverage is not the goal — paper grain remains) / rotring keeps area fill (`True` = solid / `False` = contour only) / tiny shapes with fewer than three scanlines degrade to area fill / surface strokification covers hatch / crosshatch only (grain and bleed textures excluded).
- **Surface hatching as brushwork (`surface-stroke-v1`):** `_render_surface_vectors`' hatch / crosshatch lines become `synthesize_along` bands with centerline, angle, spacing, and count unchanged; rotring keeps geometric lines (`_uses_hand_stroke` gate). When `surface` is specified the material fill is suppressed (fill = the material's default covering; `surface` = an explicit printmaking expression).
- **Unperformed variations excluded from the seed key:** `_variation_seed_fields` keeps only the fields actually consumed, and reduces an unconsumed `variation` to the same shape as an Instruction without one. Per-primitive inactivity rules (lines activate on position_x|y; closed shapes on position_x|y|radius; pink consumes only quality + amplitude; cloudform's contour generator always consumes quality / amplitude / frequency, so only `dimensions` is inactive there). For the first time, the invariant "an inactive variation does not change the rendered bytes" holds across all weights.
- **Render engine version 8 → 9:** the same Score and seed render differently. Lines and arcs are unchanged (`MATERIAL_NONE_SEED_DIGESTS`' `brush_thin_line` / `crayon_arc` digests were not updated, pinning the containment; the seed-key change moved neither).
- **Verification:** pytest **985 passed / 30 skipped** (122 new tests in `test_fill_strokes.py`: fill groups present for 9 hand-drawn weights × 6 closed shapes, absent under `filled=False`, one path = one stroke, circle containment within radius + stroke width, concave cloudform overshoot within one stroke width (0.46px measured), seed-diverse scan angles, spacing jitter, tiny-shape degradation, rotring unchanged, no fill strokes on line / arc, determinism, surface suppression and banding, byte-identical output under inactive variations), cli 68 passed, ruff clean (Mac and pentala). Golden digest updates each carry a reason (e.g. shapes that no longer fill lose their fill opacity).
- **Size observation (no cap, author ruling):** 11–123KB per shape (median 45KB), 422KB for a 10-instruction work. The maximum is not the fill but surface crosshatch at 192KB (80 geometric lines × 2 layers become bands). A cap will be designed later from production distribution. Data: `cli/out2/644-v2.2.1-stroke-fill/size-observation.json`.
- **Author's response (after PNG report):** "The amount of information and nuance jumped at once. I think this is a breakthrough." The author also noted the adjacent risk of human emulation; henceforth brushwork changes are never justified as "more human-like" but as **whether the range of describable differences grows** (recorded permanently).
- **Remaining:** live-UI inspection (especially fill spacing density), size cap rules, strokification of grain / bleed surface textures. Arc strokification is already ruled (keep an invisible intent arc so the extractor needs no change; contact ends stay tapered) and goes to the next contract as engine 10 / v2.4. Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-v23-stroke-fill-result.md`.


### v2.3.1 — Arc strokes (Build 647, 2026-07-21)

- **Arcs perform as hand-drawn stroke bands (`arc-stroke-v1`):** the arc branch of `_render_instruction` splits on `_uses_hand_stroke` (symmetric with lines); rotring / non-GRAMMARS weights keep the geometric arc, hand-drawn weights render through the new `_render_arc_hand_stroke`. The centerline is a densely sampled geometric arc (no variation) or the performed arc (with variation; endpoints pinned to intent). The band is one `synthesize_along(closed=False)` call. This closes the last strokification exclusion left by v2.2.0.
- **Invisible intent arc (ruling A, 2026-07-21):** the geometric arc remains as a `stroke="none"` intent element (`<path d="M..A..">` without variation, `<polyline>` with). The touching (contact) contract is verified by reading this intent arc back from the rendered SVG, so **the arc extractor `_svg_arcs` is unchanged** (full `test_touching.py` pass is the key check; the band is a filled polygon with no arc command, so nothing is double-counted; drypoint burr's low opacity keeps it out of the polyline branch).
- **Contact ends stay tapered (ruling, 2026-07-21):** the envelope converges to zero at both ends with no width floor. Since the intent arc guarantees contacts by coordinates, the band fades at contact points like free ends (leaf tips and bases fade softly — author-approved).
- **Style / drypoint / material:** dashed and dotted styles make the intent arc itself visible as a thin dashed / dotted line (no extra element, so the arc count stays one). Drypoint emits burr via normal offsets along the performed centerline, and its band carries no texture filter. Material outlines and specks (`_add_material_arc_outline`) coexist with the band. Z-order: intent element → band → burr → material outline. The only profile difference is the display-only texture filter on the band. **The first-prototype trap (a band without material reversed the author's judgment) was written into the contract and implemented with materials attached.**
- **Render engine version 9 → 10:** the same Score and seed render differently. Everything but arcs is unchanged (`MATERIAL_NONE_SEED_DIGESTS`' `brush_thin_line` and the three closed-shape digests pass without update; only `crayon_arc` was re-captured with a reason).
- **Verification:** pytest **1022 passed / 30 skipped** (37 new tests in `test_arc_strokes.py`: bands for 9 hand-drawn weights, rotring unchanged, invisible intent arc under solid with exactly one arc by extractor criteria, polyline intent under variation, dashed visibility, drypoint burr, material coexistence, determinism, seed responsiveness, variation-then-band composition order), cli 68 passed, ruff clean (Mac and pentala). Full `test_touching.py` pass including the 200-seed geometry / replay contract.
- **Visual samples:** `cli/out2/646-v2.3.1-arc-strokes/` (8 SVG / PNG pairs via resvg with texture filters: pencil taper, brush_thick shading, crayon / chalk grain, drypoint burr, geometric rotring, dashed, wave variation).
- **Remaining:** live-UI inspection (band brushwork, tapered ends, weight differences). Since leaves change appearance, re-inspecting Stage 3 leaves is a candidate (against the earlier "twin arcs read as a circle" impression). Size cap rules remain deferred (no exhaustive size capture this time). Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-v24-arc-strokes-result.md`.


### v2.3.2 — Interactive UI adjustments for the v2.3.1 feature set, admin render-concurrency settings, layered vocabulary (Build 683, 2026-07-22)

- **35 interactive UI adjustments (Builds 648–682):** carried out in an instruct → implement → verify-on-live-UI loop with the author. Highlights:
  - **English saijiki highlighting** (hydrate `/api/saijiki` in both languages, longest-match against both vocabularies; ASCII surfaces require word boundaries and ignore case; coloring keyed by category `key`)
  - **Instructions editor upgrades** (a drawing-model picker with the current model shown, a "edit DDL" button on the describe tab, switching to the canvas tab after drawing completes)
  - **Progress for 4-candidate drawing** (candidates run in parallel so a "which one" does not exist; a completed count `n/N` shows in the status meta line)
  - **EXIF capture date in PNG downloads** (new `pngMetadata.ts`: `eXIf` chunk `DateTime*` / `OffsetTime*` plus `tEXt` "Creation Time" from the artwork's creation date; verified by reading back with Pillow)
  - **Reorganized input tabs** (shared button row and settings strip across describe / batch / demo; input-box-first layout; batch and demo now send the staffage setting — previously they silently drew with the server default; the description label moved above the box with a hint, and the length meter gained units — `34 / 31 字` from tanka's 31 morae, 12 words for English input)
  - **Contact sheets** (build PNG sheets from artworks selected in the history manager: people variant = 7×4 per sheet with captions; AI variant = 3×4, 1568px long edge, index badges only, plus **a markdown notes file** linking number → description → provenance for three-way verification; `ddl` included, `expanded_ddl` excluded by author approval. Nested-SVG composition was rejected because weight-derived filter ids collide; the canvas rasterization path avoids it. The author confirmed a vision model reads it well)
  - **Generation-info details** (created date, SVG byte size, and an always-visible staffage row — previously hidden entirely for pre-v1.97 works)
  - **Login screen** (version / build footer; no initial "please sign in" message)
  - **Dark-mode contrast fixes** (a new `--accent-fg` token adopted across 13 accent-filled controls; three `var(--fg)`+`#fff` literals moved to `--action-*`; two references to undefined tokens resolved — **`ConfirmDialog` and `LineagePanel` change appearance in light mode too**, since the previous state was a defect)
  - **Batch following** (the history-strip "current" badge now re-resolves by the on-screen artwork id; the batch input box is fixed at `clamp(200px, 42vh, 640px)` with gutter-synced line numbers and auto-scroll to the running line)
- **Admin render-concurrency settings (server change, two author rulings):** four simultaneous candidate renders exceeded the default concurrency of 2 and returned 503. Both sides addressed: the server replaces its fixed `BoundedSemaphore` with `_RenderCapacity` (runtime-adjustable), adds DB settings `render_concurrency_settings` (`server_limit` / `client_limit`, 1–16), `PUT /api/settings/render-concurrency` (admin only), and `GET /api/client-config` (authenticated read). `INKU_RENDER_CONCURRENCY` is demoted to the initial value when the DB has no setting. The client retries only `render capacity is full` 503s (up to 3 times, honoring `Retry-After`), caps candidate fan-out, and shows a dedicated message. Two admin-only numeric inputs live in the settings "Other" tab.
- **Layered vocabulary (author doctrine settled, 2026-07-22):** "LeWitt's instruction sheet = the normalized DDL; inku adds a poem-like input layer above it." UI vocabulary was stratified by layer (input = 記述 / Description; Stage 1's act = 解釈 / Interpret; its product = **指示書 / Instructions (Normalized DDL)**; 詞書 = the description re-presented). 13 i18n keys relabeled and a permanent vocabulary table added to the App Info modal. **SPEC.ja §5 revised** (a four-stage pipeline diagram separating description from normalized DDL, the LeWitt correspondence spelled out, and the vocabulary table added as §5.3); SPEC.md §2 gained the LeWitt sentence and the same table; both READMEs carry the table in "How it works" (the UI dialog content is the single source of truth).
- **Operational discovery (Build 664):** `web/BUILD_NUMBER` lives outside `web/src/` and was never covered by the usual rsync; `vite.config.ts` injects it via `define` at startup. Reflecting a build number therefore requires an explicit single-file rsync plus an `inku-server.service` (Vite) restart. Deployment since then uses the three-step set (`web/src/` + explicit `BUILD_NUMBER` + Vite restart).
- **Unchanged:** render engine version stays 10. Score schema / coerce / rh2 and renderer / stroke_engine untouched (the only server change is concurrency control).
- **Verification:** pytest **1023 passed / 30 skipped** (new `test_render_concurrency_settings_are_admin_only`: non-admin PUT 403, admin PUT applied, `/api/client-config` auth, out-of-range 400), cli 68 passed, ruff clean, `npm run check` 0 errors / 2 warnings (pre-existing a11y). Mac and pentala. Numerous live-UI confirmations by the author during the loop. Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-ui-adjustments-result.md`.
- **Carried over:** further interactive UI adjustments (new session), the two unused mascots, measurements (render duration, CPU at concurrency 4, 503 rate), and the history strip returning to page 0 during a batch (awaiting the author).

### v2.4.0 — Release distribution pipeline (GHCR container images, tag-driven Actions, user-facing compose) (Build 684, 2026-07-22)

- **Distribution established (author ruling 2026-07-22, the standard shape for a personal OSS project):** pushing a git tag `vX.Y.Z` triggers `.github/workflows/release.yml`, which builds and publishes `ghcr.io/oikawas/inku-api` / `inku-web` as multi-arch images (linux/amd64 + linux/arm64 via QEMU + buildx). `docker/metadata-action` assigns semver tags (`X.Y.Z` / `X.Y` / `latest`) and OCI labels (source, licenses=MIT). `workflow_dispatch` performs a build-only verification without logging in or pushing; branch pushes do not trigger the workflow at all.
- **User-facing `deploy/`:** a compose file referencing the GHCR images (`INKU_IMAGE_TAG` and ports overridable; api 8100 / web 5173 by default), `.env.example` (required: LLM keys and the bootstrap admin password; optional: ports, ORIGIN, cookie, CORS), and a bilingual `README.md` (quickstart, first account, data persistence, version pinning, HTTPS). The development/bench `compose.yaml` keeps its `build:` references and stays separate.
- **Build number baked into the image:** `server/Dockerfile` gains `COPY web/BUILD_NUMBER /app/web/BUILD_NUMBER` (after `uv sync`, so version bumps do not bust the dependency layer). This closes a structural gap present since the first containerization: container `/api/info` `build_number` and the `render_build_number` stamped into generated works were null. Verified as `"683"` in an isolated build on the deployment host.
- **`/api/info` version made single-source (author ruling: the API reports the server implementation version):** the hard-coded `_APP_VERSION = "0.1.0"` in `api.py` now derives from `importlib.metadata.version("inku-server")` (recorded as a justified deviation in the implementation report). `server/pyproject.toml` is stamped per release from now on; this release moves it 0.1.0 → **2.4.0**. The test assertion was rewritten to compare against the installed metadata so future stamps do not touch tests.
- **nature-leaves plugin tracked in git and bundled (author ruling):** the canonical v0.3.0 document (177 lines) moved into `server/plugins/`. The existing `COPY server/` bundles it automatically; a fresh container with an empty DB reports `Nature.leaves 0.3.0 enabled` without any `plugin install`. `.plugin-state.json` stays untracked (runtime state).
- **Bootstrap admin blank-string hardening (author chose both compose enforcement and code tolerance):** with no self-signup path, the bootstrap admin (`INKU_BOOTSTRAP_ADMIN_PASSWORD`, 8+ characters) is the only way in — but compose `${VAR:-}` interpolation passes an empty string, which the code read as a zero-length invalid password, crash-looping first-time starts against a fresh DB (existing DBs never hit it, so only first-release users would). `db.py` now treats the empty string as unset, and both compose files require the variable via `:?` before start. Two behavior tests added. The premise ("no self-signup; without a bootstrap admin nobody can sign in; set the password and restart to recover") was added to `manual/` (ja/en, four documents) and `SETUP*.md`; SPEC carries it as §15.4 (ja) / §12.1 (en) in this release.
- **Unchanged:** render engine version stays 10. Score schema / coerce / rh2, the renderer, and `web/src/` (UI) untouched; the development and bench environments are unaffected.
- **Verification:** pytest **1025 passed / 30 skipped** (+2 blank-string tests), static YAML checks of the workflow and compose files, and a full isolated rehearsal on the deployment host (ports 8102/5175, dedicated volume, local tags): real multi-arch-ready builds, `deploy/compose.yaml` started as-is, health / info / login / bundled-plugin checks, then teardown. Bench container and bare metal untouched. Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-release-pipeline-result.md`.
- **Publishing cautions (from the report):** images reach GHCR only at the moment a `v*` tag is pushed — beware `git push --tags` dragging tags along. A first-published GHCR package defaults to private, so anonymous pulls require a one-time manual switch to public. Deleting a git tag does not delete the image. (In the actual first publish the packages were public from the start — measured 2026-07-22.)

### README visuals — works gallery, UI screenshots, structural rewrite (docs only, no version stamp, 2026-07-22)

- **A six-work gallery added (`docs/assets/gallery/`):** selected from the author's starred history (no new generation; all six from Build 667 / render engine 10, Stages 1 and 2 both `nvidia:google/gemma-4-31b-it`). Each entry pairs a 1000×1000 display PNG with its kotobagaki and a `<details>` block holding the instructions, the original SVG, and the seed. A hero strip of three works opens the page. Descriptions, instructions, and Score excerpts are quoted verbatim from `history-export` data (line breaks and `color` values as-is). The 21-work contact sheet used for selection is kept at `cli/out2/684-v2.4.0-readme-gallery/` for future swaps.
- **Six UI screenshots (`docs/assets/ui/`, three per language, captured at Build 685):** works / lineage / instructions-editor, ordered as the actual working loop. Named `*.ja.png` / `*.en.png` so each README references only its own language (an author ruling changing the contract's shared-image plan). A full-pixel confidentiality pass required no removals. A saijiki-dialog i18n mismatch spotted during capture was out of contract and handed back (the author fixed it at Build 685 on `feat/ui-adjustments-2`; it is not part of this merge).
- **README restructure (both languages):** the "Examples" section replaced by "Works — descriptions become pictures" with real works and folded into "How it works", which gains a layer-by-layer walkthrough of one work's actual Score; a new "Screens" section; the "Documentation" section moved to the end. Vocabulary follows the 2026-07-22 layer stratification (Instructions (Normalized DDL)).
- **Image borders via single-cell `<table>`:** grip measurements showed GitHub's sanitizer strips `style` / `border` / `class` from `<img>`, leaving the single-cell table as the only surviving method (`.markdown-body table td` borders follow the theme and stay 1px at any scale). Applied to all nine standalone images.
- **`.gitignore` change:** a wholesale `docs/` exclusion cannot re-include paths beneath it, so it became `docs/*` + `!docs/assets/` (existing local documents under `docs/` verified still ignored via `git check-ignore --no-index`).
- **Post-publication gallery swap (author call, merge `9f3ada1`):** the second work dissolved into GitHub's white background even with the border, so it was replaced by the silver-shoal work (rh2 `B962`, Build 667 / engine 10, matching the other five). All three places using the file (hero strip, gallery, layer walkthrough) were updated; the walkthrough was rewritten around the new work's actual Score (description→Score correspondences grew from 2 to 4). The new PNG has 544 colors and no gradients, so a measured 256-color palettization (0.10% of pixels changed, effectively lossless) keeps the displayed total at 2.8 MB — not applied to other images unless measured lossless.
- **Verification:** all 55 relative references resolve, no orphan assets, full `alt` coverage, balanced tags. Live GitHub rendering loads all 12 images in both READMEs; raw delivery matches by SHA-256; the removed asset returns 404.
- **Unchanged:** no code changes (`web/src/`, server), render engine version stays 10. No `APP_VERSION` / `web/BUILD_NUMBER` / pyproject stamps (docs only). No pentala deployment (the README is not served). Merges `7e1469a` + swap `9f3ada1` (as a contract exception, Opus performed merge, push, and worktree removal). Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-readme-visuals-result.md`.

### v2.4.1 — UI adjustments round 2 (cross-language saijiki pairing fix, bilingual word previews, describe-tab wording) (Build 687, 2026-07-22)

- **Interactive adjustments (Builds 685–686, two sessions):** all 69 word-preview entries in the DDL editor localized (`effectEn` / `exampleEn`; the three Nature plugin words merged into bilingual entries; previously missing explanations for `敷き詰める` / `tile` and `触れる` / `touching` added; the generic fallback text localized too). The two sentences under the lineage-tab heading removed (only the overview-mode description remains). The English "Read the Okugaki" button label simplified to `Okugaki`. The describe tab no longer states the tanka guide twice: the mention moved from the hint text into the counter (`文字数 12 / 31字（短歌）` / `Characters 12 / 31 (tanka)`; the English hint shrank from 85 to 49 characters and fits on one line).
- **Cross-language saijiki pairing fixed (two bugs found mid-work; the author ruled to fix the server too):** ① the pruned `髪` / `hair` (P0-3, `display=False`) lingered in the i18n word lists, shifting the display-word-to-explanation join by one across the whole tezawari category (hovering `pencil` in the English UI showed the pen explanation); ② `words_en` in `saijiki.py` was not ordered like `words_ja`, crossing the explanations for `引く`↔`fill` and `埋める`↔`draw` in both languages (`ja.ts` had yet another order). **Root cause: hand-maintained i18n copies of the server's canonical vocabulary.** `words_en` reordered to match ja (`place, line-up, draw, scatter, fill, tile`), the i18n `saijikiWords` removed, and display words now come straight from the hydrated `SAIJIKI` / `SAIJIKI_EN` (`gen_saijiki_ts.py` also emits `GENERATED_SAIJIKI_EN`; components use `saijikiWordsFor(key, isJapanese)`). A test pins all 68 words across 10 categories against an explicit ja/en table (**a same-length transposition is invisible to length checks**, so the correspondence itself is fixed). The golden test gains a declared substitution `_REORDERED_EN` (fixture untouched).
- **Side effect:** the `motions:` line of the English Stage 1 system prompt changes order (the word set is unchanged). English-input interpretation may shift marginally per model; no bench run yet. The SPEC.md vocabulary table's motions row follows the new order (this docs commit).
- **Unchanged:** render engine version stays 10. Score schema / coerce / rh2 and renderer / stroke_engine untouched (server changes are limited to the saijiki table, the generator script, and tests).
- **Verification:** pytest **1026 passed / 30 skipped** (+1 pairing test), cli 68 passed, ruff clean, `npm run check` 0 errors / 2 warnings (pre-existing a11y). The implementation session also ran `display_categories('ja'/'en')` directly, parsed `saijiki.generated.ts` against the server table, and confirmed all 68 display words resolve to preview entries. Mac and pentala. Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-ui-adjustments-2-result.md`.
- **Carried over:** interactive UI adjustments continue (round 3). The two mascots, the history strip during batches, and the three measurements remain from round 1. The structural fix for `words_ja` / `words_en` (a `SaijikiWord` carrying both surfaces) stays out of scope — adding a word requires updating both the server table and the test's correspondence table.

### v2.4.2 — Saijiki word pairing carried by structure, not by position (Build 689, 2026-07-23)

- **Positional coupling removed:** the two parallel tuples `words_ja` / `words_en` on `SaijikiCategory` are gone, replaced by a single `words`. A `SaijikiWord` now carries `surface_ja` and `surface_en` in one entry (`surface_en` may be `None`), and the `default` / `prompt` / `display` / `marker` flags are shared across languages — flags describe the word, not the language. The ja/en correspondence is now held by **the entry**, not by index. This brings the katachi–wariai categories in line with `RelationWord`, which had carried `surface_ja` / `surface_en` in one entry from the start.
- **Why:** the two pairing bugs fixed in v2.4.1 (the lingering `髪` / `hair` shifting the join by one; `ugoki`'s mismatched order crossing explanations) were addressed by **reordering plus a pinning test** — not by fixing the structure. The rule "keep `words_en` in the same order as `words_ja`" lived only in a comment, so the failure mode remained available to any future word addition or reordering.
- **The Score-value coupling went with it:** each tezawari and iro word now carries `score_value`, and `_surface_value_map` — which zipped the category's word list against `_WEIGHT_VALUES` / `_COLOR_VALUES` with only a length check as its guard — is deleted. **Reordering a tezawari word used to mean silently reassigning Score weight values**, which is heavier than a shifted explanation in the vocabulary panel.
- **Special cases preserved:** the pruned tombstone `描く` (in `ugoki`, no English counterpart) stays in the same word list with `surface_en=None`. `並べる` / `line-up` is a single entry whose English closure marker keeps its legacy surface via `marker_surfaces_en=("arrange",)`. `髪` / `hair` remains `_PRUNED` while keeping `score_value="hair"` so saved Scores still replay.
- **Byte-identical output (the acceptance condition):** all 15 outputs — `prompt_block`, `texture_material_enumeration`, `display_categories`, `saijiki_marker_table`, `core_grammar_markers`, `shape_markers`, `relation_literal_markers`, `reference_categories`, `weight_for_surface`, `color_for_surface` across both languages — hash identically before and after. Re-running `gen_saijiki_ts.py` reproduces `web/src/lib/saijiki.generated.ts` byte for byte (no git diff). The 68 pairs in `_EXPECTED_PAIRING` **pass unedited**. The Stage 1 prompt does not change by a single byte in either language.
- **Unchanged:** render engine version stays 10. Score schema / coerce / rh2, renderer / stroke_engine, and the vocabulary itself (no words added, removed, or renamed) are untouched.
- **Verification:** pytest **1028 passed / 30 skipped** (+2 structural tests, confirmed fail-first against the old structure), cli 68 passed, ruff clean, `npm run check` 0 errors / 2 warnings (pre-existing a11y), `npm run build` exit 0. Mac and pentala. Implementation report: `no-git-sync/fable5/claude_code/tasks/opus-saijiki-word-pairing-result.md`.
- **Positional correspondence that remains:** the delivery formats still pair by position (`GET /api/saijiki` returns per-language word lists, `saijiki.generated.ts` holds two arrays, and the display-list test joins them by index), but all of these are derived from the one bilingual word list. No hand-maintained parallel list survives on the canonical side.

### v2.4.3 — UI adjustments round 3 (developer mode, lineage orientation toggle, demo timeout) (Build 693, 2026-07-23)

- **Developer mode `INKU_DEVELOPER_MODE` (Build 690; the server change was an author call):** one environment variable now decides whether developer-facing options appear at all. With it off, NVIDIA NIM drops out of the display catalogs (`GET /api/models`, the administrator model settings get and update, and the provider model refresh), and the always-on Build number disappears from the left rail, the sign-in screen, and the app info dialog. `model_settings.py` carries a `developer_only` flag on the provider definition, and `model_provider_catalog` / `public_model_settings` take an `include_developer` boundary. `/api/info` gained `developer_mode`, which the web app reads before sign-in (`loadPublicAppInfo`).
- **Only the display is gated (the point of the design):** provider resolution at run time (`provider_for_model`) is untouched, so **NIM still draws with the flag off**. Stored model settings, the model information on history entries, and each artwork's `render_build_number` also survive. Saving from an administrator screen that cannot see NIM does not drop it either, because the update merges an `exclude_unset=True` patch into the full stored settings. Only the on-screen selection is corrected: if Stage 1, Stage 2, Vision, or Okugaki points at a hidden provider, it falls back to the first model in the public catalog.
- **Defaults:** the distributed `deploy/compose.yaml` leaves it **off** (`${INKU_DEVELOPER_MODE:-0}`, with a commented opt-in in `deploy/.env.example`); the top-level development and bench `compose.yaml` leaves it **on** (`:-1`). `server/.env.example` records `INKU_DEVELOPER_MODE=1` for source development. Documented in SPEC §15.4 / §12.1.
- **Lineage orientation toggle (Build 691, front end only):** the lineage panel and the lineage overview share one vertical/horizontal toggle. Vertical puts the origin generation at the top and proceeds downward; horizontal puts it at the left and proceeds rightward, stacking same-generation works vertically. Connector arrows run parent-bottom to child-top when vertical and parent-right to child-left when horizontal, with the Bézier control points recomputed per direction; the overview description follows the direction too. Switching recomputes node placement and arrows and scrolls the focused card back into view. The choice persists to the browser as `inku-lineage-orientation` (only `vertical` / `horizontal` are accepted; the default is vertical). **The lineage graph API, schema, stored data, and generation numbering are unchanged** — the change is placement and interaction only.
- **Demo timeout (Build 692; the server change was an author call):** the demo settings gained a 1–1,440 minute (up to 24 hour) timeout. The screen works in minutes; the API and stored value work in seconds (60–86,400, default 3,600). The deadline is fixed at each start and ignores changes made while running. **A run started before the deadline finishes its description generation and rendering even past it**, and its result flows into the screen, the statistics, and any configured persistence before the demo stops. No new generation starts after the deadline, including when the deadline lands during the inter-render wait. While running, the remaining time shows as `HH:MM:SS`, and the "until next render" line is suppressed while only the timeout is being waited on. The reached-timeout message appears only on automatic stop, never on a manual one. Existing users whose stored settings predate `timeout_seconds` get the default filled in.
- **Token discipline:** `DemoPanel`'s `.step-btn` moved from `font-size: 14px` to `--btn-sm-font-size` (an in-passing migration). The orientation toggle buttons inherit the existing `--btn-sm-*` and use `--accent` + `--accent-fg` for the selected state. No new size tokens and no hard-coded button dimensions were added. The timeout label, remaining time, and automatic-stop message are localized across `ja.ts` / `en.ts` / `types.ts`.
- **Unchanged:** render engine version stays 10. Score schema / coerce / rh2 and `renderer.py` / `stroke_engine.py` are untouched (the server changes are the display-catalog boundary and the demo setting, nothing else). The version-pinned pentala bench container (8101) was not rebuilt.
- **Verification:** pytest **1029 passed / 30 skipped** (+1 pinning that NIM leaves the public catalog outside developer mode; two existing NIM tests now state developer mode explicitly), cli 68 passed, ruff clean, `npm run check` 0 errors / 2 warnings (pre-existing a11y). Mac and pentala. The author confirmed all three on the live UI: Build 690 (temporarily switched to ordinary mode to see NIM and the Build number absent), 691 (both lineage directions, arrows, scrolling, direction preserved across reload), and 692 (timeout input bounds, remaining time, automatic stop, setting preserved across reload). Merge `e15d63f`. Implementation report: `no-git-sync/fable5/claude_code/tasks/codex-ui-adjustments-3-result.md`.
- **Two documentation corrections folded in here** (documentation alone does not open a version entry):
  - **`SETUP.ja.md` / `SETUP.md` (`fe63584`):** a container section was added (GHCR pull and build-from-source, with the details deferred to `deploy/README.md`), along with three corrections — ① "what the distribution contains" had omitted `compose.yaml`, `deploy/`, and both Dockerfiles; ② **the Python requirement was stated as 3.10 or later when it is actually 3.12 or later** (an error users hit for real on `uv sync`); ③ PNG rasterization was described as CairoSVG only, whereas the implementation prefers resvg and loses the material filters when it falls back.
  - **`README.ja.md` / `README.md` (`a15f699`):** the regeneration section was rewritten from "three kinds of again" into **pursuing a work through refinement**. The implementation offers five refinement kinds (`RefineKind`) plus two autonomous-refinement methods, and the color-catalog change, the variation, and autonomous refinement had all been missing.
- **Carried over:** interactive UI adjustments continue (round 4). The two mascots, the history strip returning to page 0 during a batch, and the three measurements remain from round 1.

### v2.4.4 — freezing what engine 10 draws (reference corpus and a byte-identity CI gate) (Build 694, 2026-07-23)

- **The goal, in the author's words:** as the drawing core is versioned forward, make it easy — **mainly for an AI** — to tell **what changed in the output when a given thing changed**. A version number carries only one bit: that something moved. What moved, and how, can only be answered from **the actual output**.
- **Why the hurry:** **the outputs of render engines 1 through 9 were never saved, and the code is gone. They cannot be recovered.** Since replay always runs on the latest engine, **engine 10 will be lost the same way unless it is frozen while it is still current**.
- **`server/reference/render-engine-10/` freezes 220 cases:** stage A, the base grid of 10 tools × 8 shapes (80); stage B, variation across 4 qualities × 3 amplitudes × 3 shapes × 2 tools (72); stage C, fills, 8 surfaces, 6 `ground` materials, and `density` / `opacity` / `absorbency` discriminators (40); stage D, discriminators (28). Each case records its full input, a coordinate-normalized digest, byte count, element counts, and class strings. 3.2 MB total (412 KB of manifest).
- **Stage D does the real work.** `pillar` (`unit_scale` 0.2) exposes leftover absolute px; **seeds above 2^63** (2^63+1 and 2^64−1) expose sign confusion; a tiny shape exposes the `FILL_MIN_SCANLINES` degeneration to area fill. Measured: `square` and `pillar` digests differ, both high seeds differ from an ordinary seed, and the tiny shape carries **no** `fill-stroke-v1` (it degenerates to `contour-stroke-v1 controls-17 events-0` plus `material-outline`).
- **Inputs are literals inside the generator (the crux of the design):** the color map and every field of Score, Instruction, Surface, and Ground are **written out in full**, never read from `renderer.COLOR_MAP`, `color_catalogs.py`, or the schema defaults, and `coerce_score` is never invoked. That leaves **only `renderer.py` and `stroke_engine.py`** able to move `render-engine-10/`. Demonstrated by temporarily rewriting `COLOR_MAP["white"]` and the `Score.background` default and regenerating: all 220 cases were unchanged (both reverted afterwards).
- **The CI gate is the substance of this change (`.github/workflows/reference-corpus.yml`):** regenerate, then `git diff --exit-code -- server/reference/` plus an untracked-file check. That turns "**an existing case regenerates byte-identically; if it does not, the layer version must be incremented**" into a constraint that cannot be forgotten. The old discipline was something people remembered — it held six times, with nothing guaranteeing the seventh. **Moving it from memory to machine is the point.** The generator carries a second guard: if case output moves while none of the manifest identity fields (`corpus_format_version`, `engine_version`, `schema_version`, `color_map_digest`) move, it exits non-zero, because that means a dependency was left unfixed — a bug in the corpus design itself.
- **The gate was proven to fire:** changing the coordinate format in `stroke_engine.py`'s `polygon_path` from `.3f` to `.4f` and regenerating produced exit 1 and "bump the appropriate version instead of rewriting a frozen corpus". The perturbation and corpus were then restored and regeneration confirmed byte-identical again. The contract required this because **"I wrote the CI" is not acceptance**.
- **`absorbency` turns out to be a dead field (a by-product):** three cases vary `ground`'s `density`, `opacity`, and `absorbency` independently. The first two move the digest; **`absorbency` returns the baseline digest exactly** (`4c267d64…`). **The current renderer never reads it.** A suspicion recorded in an earlier audit is now confirmed. **It was not fixed here** — the corpus exists to detect, not to repair.
- **Operating rules sit in three layers:** the contract in the SPEC (§12.2 / §15.5), **the procedure in `server/reference/README.md`, next to the artifacts** — where anyone about to regenerate will look first — and enforcement in CI. `docs/`, `CLAUDE.md`, and `AGENTS.md` are untracked and invisible to anyone who clones, so they cannot hold canonical rules.
- **Kept out of the distribution:** a new `.gitattributes` carries `server/reference/ export-ignore`, and `git archive HEAD` was measured to contain zero entries under `server/reference/`. `SETUP.ja.md` → `SETUP.md` gained a line under "excluded".
- **Unchanged:** **not one byte of drawing output moved.** `render_engine_version` stays 10; `renderer.py`, `stroke_engine.py`, the Score schema, coerce, and rh2 are untouched; and all five `MATERIAL_NONE_SEED_DIGESTS` **pass unedited**. The web UI is unchanged apart from the version stamp.
- **Verification:** pytest **1033 passed / 30 skipped** (+4: case counts, input explicitness, the stage D discriminators, and SVG-to-manifest agreement), cli 68 passed, ruff clean, `npm run check` 0 errors / 2 warnings. **The git session reproduced this independently**: rerunning the generator left `git status` empty (220 files byte-identical), the archive exclusion held, and the CI guard fired and was restored. The new `test_render_reference_inputs_are_fully_explicit` compares the generator's literals against the live Pydantic field sets, so **adding a schema field makes it fail** — an unfixed dependency surfaces by itself. Implementation report: `no-git-sync/fable5/claude_code/tasks/codex-reference-corpus-result.md`.
- **Still ahead (Phases 2–5, not started):** move the edition ID to rh3 (dropping `render_build_number` and `vary_seed`, keeping rh2 rows as legacy); a ddl corpus (`a_expand` / `b_coerce`) with new `ddl_engine_version` and `ddl_version`; `stage1_prompt_digest` / `stage2_prompt_digest`; and showing the version gap on redraw. **Across every phase `render_engine_version` must stay 10.**

### v2.4.5 — the work-edition ID moves to rh3, dropping the build number and the Score-side seed (Build 695, 2026-07-24)

- **What was broken:** rh2 included `render_build_number` in its payload. That number is whatever sits in `web/BUILD_NUMBER`, and **it is stamped for UI-only changes**. So a drawing that had not changed by a single byte could get a new edition ID — a false difference. Against engine 10, the build number has already run to 689, moving hundreds of times with nothing to do with drawing.
- **How it got there:** `render_build_number` arrived in v1.25 as provenance and entered the hash in v1.60 **as insurance, because there was no versioning discipline for the engine yet**. Since v1.99 `render_engine_version` has carried that role. The insurance is no longer needed.
- **What rh3 is:** the payload is five fields — `score`, `render_seed`, `render_engine_id`, `render_engine_version`, `render_color_catalog_id`. **`render_build_number` and `vary_seed` are out.** The Score-side seed is redundant: **a different Score already gives a different ID**. Canvas aspect is not added separately because `_score_with_canvas` applies it before rendering and it is baked into the saved score.
- **`render_build_number` stays.** Its column, persistence, and response paths are untouched. The split is deliberate: **it is worth keeping as provenance and not worth putting in the definition of identity.**
- **rh2 is retained as legacy and never recalculated.** Its computation survives as `_legacy_render_hash_for_item`. Stored `rh2:` rows keep their values and no destructive migration runs, matching how the earlier 64-character hex hashes were left alone. **`rh2` and `rh3` are separate hash spaces and must not be compared to decide sameness.** The startup backfill (`db.py:484`) writes rh3 only for rows whose `render_hash` is empty, and never touches an existing rh2 row.
- **Nothing behaves differently.** A prior read-only survey confirmed that **no code path in the server compares `render_hash` for equality** (it is stored, indexed, and displayed; the five web files and `cli.py` only show the four-character suffix), and this change adds no such path. `render_hash_short` is unchanged.
- **The serialization is the existing one.** `_canonical_json` (sorted keys, tight separators) and `_canonical_seed` (`int()` coercion) are reused rather than reinvented, so the string `"12345"` and the integer `12345` produce the same rh3, and **a different installation produces the same value**.
- **Expectations were measured first by the git session, pinned in the contract, and matched by the implementation.** Against the reference `rh3:1f28ff5586ca6047…`, four cases must land on the **same** digest (build changed, `vary_seed` changed, both, seed as a string) and four must land on **specific different** digests (`render_seed`, catalog, `render_engine_version`, a 2^63+1 seed). "It came out different" was explicitly not acceptable.
- **Unchanged:** render engine version stays 10. Renderer, stroke_engine, the Score schema, coerce, and DDL interpretation are untouched, and **regenerating `server/reference/render-engine-10/` produces no diff**. All five `MATERIAL_NONE_SEED_DIGESTS` pass unedited. The web UI changes only its version stamp.
- **Verification:** pytest **1038 passed / 30 skipped** (+5: the reference value, the excluded fields, the retained fields, legacy rh2 still computable, and the backfill), cli 68 passed, ruff clean, `npm run check` 0 errors / 2 warnings. **The git session reproduced this independently**: all twelve contract digests recomputed from the implementation and matched, and a real SQLite database holding one rh2 row and one null row was run through the backfill, confirming **the rh2 row unchanged and the null row filled with the rh3 reference value**. Implementation report: `no-git-sync/fable5/claude_code/tasks/codex-reference-corpus-result.md`.
- **Still ahead (Phases 3–5):** the ddl corpus (`a_expand` / `b_coerce`) with new `ddl_engine_version` and `ddl_version`; `stage1_prompt_digest` / `stage2_prompt_digest`; and showing the version gap on redraw.

### v2.4.6 — the engine as a woodblock (putting the stance and the layer map into the SPEC) (Build 696, 2026-07-24)

Documentation only. No code, drawing, or API changes; `render_engine_version` stays 10.

- **A seventh design principle in both SPECs:** **"the engine does not go backwards."**
  Like a woodblock being carved, the drawing engine only moves in one direction; past
  versions are not kept in the system and cannot be selected. **What remains is the
  printed work — the saved SVG — not the block as it was before the cut.**
- **`SPEC.md` §12.2 / `SPEC.ja.md` §15.5, "Deterministic and Non-Deterministic Layers":**
  one table covering all nine pipeline layers — implementation, determinism, LLM call
  count, and the version each carries. **The deterministic layers are not adjacent**
  (Stage 2's LLM sits between Stage 1.5 and coerce), so "DDL through to Score" cannot be
  a single baseline. It also states **why LLM layers get no version**: a version number
  implies "same version, same result", so attaching one where that does not hold would be
  a lie. A prompt digest records that the inputs differed, and claims nothing more.
- **§12.3 / §15.6, "Versions and Identity IDs":** `render_engine_version` 10,
  `ddl_engine_version` 1, `ddl_version` 1, Score `version` 0.1.0, `APP_VERSION`, and
  `web/BUILD_NUMBER` in one table **that makes the separate namespaces legible**, each with
  the condition that increments it. The two `ddl_*` entries are documented ahead of their
  implementation by the author's ruling (2026-07-24). Why rh3 excludes
  `render_build_number` and `vary_seed` is consolidated here.
- **§12.4 / §15.7, "Comparing Generations Through the Corpus":** the v2.4.4 text grown into
  **how generations are actually compared**. A rising version creates a new directory,
  lists only the case IDs that moved in `changed_from_previous`, and **stores only those
  cases' actual output**. "How does engine 11 draw case X?" resolves by finding **the last
  version in which X moved**. **The number of directories is itself the record of how many
  times that layer changed.**
- **§12.5 / §15.8, "The Engine Does Not Go Backwards (Implemented as Printmaking)" — the
  heart of this revision:** replay always runs on the latest engine; a recorded version is
  provenance, not an input. Reproducing the edition as it was is **guaranteed by returning
  the saved SVG**, never by redrawing.

  > The carving advances. The block only changes in one direction. The prints that came off
  > it remain, but **the block cannot be returned to what it was before the cut**. If the
  > application itself is thought of as a work, this is the implementation that follows.

  The work is **the print**; the engine is **the block**. Refusing to conflate them, refusing
  to warehouse old blocks, is the choice. **The cost has already been paid: the output of
  engines 1 through 9 is gone.** That is why prints are pulled while a version is current —
  **a corpus is a proof print, taken before the next cut.** **Recording only the version
  number while discarding the output is like noting the date of the carving and throwing
  away the print.**
- **A new "The engine as a woodblock" section in both READMEs**, carrying the same essence at
  a reader's density, plus the seventh design principle. It states that the corpus holds 220
  cases (ten tools against eight shapes as the base, plus variation, fills, canvas ratios, and
  boundary values), that CI enforces byte-identical regeneration, and that generations 1
  through 9 are lost.
- **Verification:** every number was checked against the implementation (manifest cases = 220,
  stage A = 80 = ten tools × eight shapes, `render_engine_version` = `"10"`). No code changed,
  so pytest, cli, and ruff are identical to v2.4.5.

### Android `2.0.0-android.1` — the drawing core reaches render engine 10 and follows to 11, Stage 1.5 through variation (Phases 1–3b and 2h, android Builds 148077–148082, 2026-07-23/24)

**Recording policy:** the Android port does not open a version entry per phase. It gets **one entry at the moment engine 10 is reached**. Android has its own version namespace, with `android/VERSION` and `android/BUILD_NUMBER` as the canonical files. What follows covers Phase 1 through 2f.

- **Where it lands:** `render_engine_version` in `DefaultSvgRenderer.kt` goes from **`"2"` to `"10"`**. The same drawing machinery as web/server engine 10 (as of v2.3.1) now runs on the device. `android/VERSION` moves from `1.48.0-android.1` to **`2.0.0-android.1`** (the author's ruling: 2.x once engine 10 is reached). **Roughly two and a half months and 44 versions of drift are closed.**
- **The declared version was held back deliberately (author's ruling):** naming an intermediate version mid-port would break history compatibility through the render hash, so Android kept declaring `"2"` until the whole of engine 10 was in place, and moved to `"10"` **in the same commit that completed 2f**.
- **Phase 1 / 1.5 (merge `89f3c9b`, Build 148069):** the Score schema JSON gained cloudform, burin, drypoint, mode/carve_depth, grid, at/relation/surface, with `additionalProperties:false` on every object; the coercer followed; `rope` was removed; the `rh2` edition hash and the `dh1` description hash were ported.
- **Phase 2a / 2a′ (merge `d5dff1b`, Build 148071):** variation playback (white / perlin / pink / wave).
- **Phase 2b / 2b′ (merge `9bdfdd5`, Build 148073):** the engine 7 proportional system — amplitude, bleed, segment counts, and material intensity move from absolute px to `canvas.unit` relative.
- **Phase 2c (merge `651b4ab`, Build 148074):** 438 lines of `stroke_engine.py` ported to `ServerStrokeEngine.kt`.
- **Phase 2d (merge `3f9538c`, Build 148075):** line hand strokes (`stroke-engine-v1`).
- **Phase 2e (merge `1be5229`, Build 148076):** closed-shape contours as `contour-stroke-v1` bands.
- **Phase 2f (merge `1c8d22d`, Build 148077):** fill (`fill-stroke-v1`), surface hatching (`surface-stroke-v1`), and arcs (`arc-stroke-v1`) ported as one stage. Scanline clipping covers every primitive, the arc endpoint contract is synchronized, and material outlines carry `class="material-outline"`.
- **What made the port work — handing over the reference corpus first:** expected values measured on the server were placed under `android/app/src/test/resources/server_reference/` as JSON and SVG and **given to the implementation session up front**. Because the SVG classes carry **numbers** (`contour-stroke-v1 controls-62 events-1`), structural agreement is machine-checkable without byte equality. `05_circle_rotring.svg` pins the negative case: it must carry **no** class at all.
- **Traps hit (recorded):** ① `_hash_to_unit` is not a wrapper around `hash01` but an **independent construction** (no salt; first 8 bytes as a signed little-endian int64; divided by `2**63`) — getting it wrong shifts every kind of variation; ② **seeds are unsigned 64-bit** (`struct.unpack("<Q", ...)`), and about half of real seeds exceed 2^63, printing negative through a Kotlin `Long` and shifting every seed-keyed hash; ③ the hatch count `range(-count // 2, ...)` relies on floor division (Kotlin's `-73/2` truncates toward zero and lands one line off). **Each was caught only because a discriminating case sat in the corpus.**
- **Verification:** `gradle :app:testDebugUnitTest --rerun-tasks` run independently on main gives **44 tests / 0 failures / 0 errors** (tallied by hand from `app/build/test-results/testDebugUnitTest/*.xml`). All ten reference SVGs match on class-attribute list and element counts; four of them (`03_square_filled`, `04_arc_crayon`, `06_surface_hatch`, `10_arc_wave`) match their `<path d>` **as exact strings**. `05_circle_rotring` is pinned by a negative test asserting no stroke bands.
- **Unchanged:** the changes are confined to `android/`. server, web, cli, and shared are untouched, so **web/server rendering, `APP_VERSION`, and `web/BUILD_NUMBER` do not move**, and no pentala deployment is involved.
- **What 2f handed on (all three settled in 2g):** ① the tests pass `colorCatalogId = "sumi"`, which is **not a real catalog id** (the real ones are `default` and ten others); ten call sites remain uncorrected. The comparisons do not depend on color, so the engine version is unaffected. ② Unknown catalog ids are handled **asymmetrically** — the server returns HTTP 422, Android silently falls back to `default`. ③ The 2f section of `ANDROID_SPEC` overstates the result as "all ten reference SVGs match `path d` exactly" (it is the four above) and does not mention reaching engine 10. Stage 2g covers cloudforms, touching, and the v1.94 double-arc fix.
- **Phases 2g / 2g′ / 2g″ (merge `8819729`, Build 148080) — Phase 2 complete:** cloudform contour
  generation (1/f basis, 49-point closed Bezier, the waist displacement), the `touching` minor-arc
  reconstruction, and **the region-before-relation order from v1.94**, plus the three items 2f handed
  on. **The engine stays at `"10"`** and `android/VERSION` stays `2.0.0-android.1` — this stage filled
  in the inside of engine 10 rather than adding to what it can draw.
- **It was remanded twice, and both defects had the same shape: the function was right and the output
  was wrong.**
  - **2g, first attempt:** the cloudform code introduced **a weight table that does not exist in the
    server**, inline — while the correct table had already been ported into `ServerStrokeEngine` in 2c
    and simply was not called. The only test called the same function twice and compared the results,
    and touching and the double arcs had no test at all. **This was the one stage handed over without a
    reference corpus prepared in advance**, and it is the one stage that failed this way.
  - **2g′:** the normal orientation was inverted against the server (`cloudform.py:229`), so the waist
    displaced outward instead of denting inward. All 14 contour cases were off by up to 0.03, but **the
    test allowed a tolerance of 0.05**, which no case could exceed — the spread across all ten tools is
    only 0.0013. **It was no longer a tautology, but it still discriminated nothing.**
  - **2g″:** with that fixed, a second defect surfaced: **the cloudform call site passed the raw seed**
    where the server passes `_seed_for_instruction(ins, seed)`. **Function-level parity stays green
    through this bug** — the corpus test hands the function a seed that is already derived — and only
    the `<path d>` comparison against `11_cloudform_pencil.svg` caught it.
- **The discipline these three stages leave behind:** ① **measure the expected values and write them
  into the contract before handing a stage over** (`renderer_cloudform_and_relations.json` holds
  `energy_lateral` per tool, 14 contour cases, the minor-arc reconstruction, and four resolved Scores);
  ② **choose a tolerance together with the spread it has to distinguish** — not "1e-9" alone but "the
  tools differ by only 0.0013, so 1e-9 is required"; ③ **never make function-level parity the whole
  condition; always pair it with a check on the output** — without that, the second defect in 2g″ would
  have passed.
- **Verification:** `gradle :app:testDebugUnitTest --rerun-tasks` run independently on main gives
  **54 tests / 0 failures / 0 errors** (aggregated from the XML). All 49 points of all 14
  `cloudform_contour` cases match **within 1e-9** with `path_d` **string-equal**, and reference SVGs
  `11` through `14` match on `<path d>` **exactly**. `14_region_then_relation` pins the v1.94 order by
  landing its final center **outside** the region `[0.55, 0.55, 0.95, 0.95]` (x = 0.35) — `relation` is
  `null` whether it resolved or was rejected, so only `center` can tell the two apart.

- **Phase 3a (merge `642a476`, android Build 148081) — the core of the Stage 1.5 expansion layer.** The expansion filter itself (variation, tenkei, focus, plugin expansion) was ported, including the split between `ddl` and `context_text` and the fact that **`vary_seed` applies only to `context_text`** (it is not `variation_seed`). **Arguments for 3b–3d are accepted but ignored, and a test says so explicitly**, so that "the argument exists but is never read" cannot survive silently into a later phase. The 39 expected cases in `ddl_expand.json` were **measured and handed over before the work started**. The 15 `A-*` cases take the same input as the server's own `server/reference/ddl-engine-1/a_expand/` corpus and match byte for byte **apart from one trailing newline** (a comparison script needs `rstrip("\n")`). Verified by counting the JUnit XML directly: **58 tests, 0 failures, 0 errors** (baseline 54 plus 4 new), with the seven cases from §5 of the contract matching under `assertEquals`. Six files under `android/` changed; **`render_engine_version` stays `"10"`** and `android/VERSION` stays `2.0.0-android.1`.
- **Engine 11 makes the parity fixtures stale (handover note; settled by Phase 2h).** v2.4.8 moved web/server `path d` from `.3f` to six fixed decimals, so the SVG comparisons under `server_reference/` **would all have failed as they stood**. That is a different axis from Stage 1.5 Phases 3b–3d, so the drawing-layer catch-up was filed as **Phase 2h under its own contract**.
- **Phase 2h (merge `eeb9bd7`, android Build 148081 unchanged) — following the master grid, engine 11.** Every emitted number now lands on the six-decimal grid, and `render_engine_version` moves from **`"10"` to `"11"`**. **No geometry changed — only the places that turn a number into a string.** `ServerRendererGeometry` declares `MASTER_GRID_DECIMALS = 6`, the duplicate `fmt3` is gone, and `applyMasterGrid` runs once over the assembled SVG the way `renderer.py::_apply_master_grid` does, exempting the same three attributes (`version` / `class` / `id`). **Kotlin's `Double.toString()` emits exponent forms such as `1.0E-5`** when interpolated directly, so without that pass those numbers would sit off the grid.
- **Two places where the server's own quirks had to be copied.** (1) `class="hatch-spacing-…"` is an identifier rather than a coordinate, so it **stays at three digits** (as in `renderer.py:2190`). (2) Cloudform contours keep a local three-digit format because **the server quantises there too** (`cloudform.py:134,143`) and `applyMasterGrid` then pads to six. **Writing six digits directly there would diverge from the server by one digit.**
- **Three discriminating tests were added, and each was shown to fail under the perturbation it targets** (re-run independently by the git session during acceptance): (1) `MASTER_GRID_DECIMALS` 6 → 5 fails `testEveryEmittedNumberSitsOnMasterGrid`; (2) gridding integers as well fails `testIntegersRemainIntegers`; (3) dropping `Locale.US` fails `testLocaleIndependence`. **All 12 tests that were red at the start are green, and not one of their assertions was rewritten** — nothing was loosened to make it pass.
- **Formatting fidelity was measured, not assumed.** `String.format(Locale.US, "%.6f", v)` was compared against `BigDecimal(v).setScale(6, HALF_EVEN)` — which is what Python's `f"{v:.6f}"` means — over **two million** random values in 0..1000, with **zero mismatches**. The concern that the JVM formatter rounds via a shortest representation and could diverge from Python does not materialise at six digits in this range.
- **Verification:** `gradle :app:testDebugUnitTest --rerun-tasks` run independently on main gives **61 tests, 0 failures, 0 errors** (JUnit XML counted by hand: baseline 58 plus 3 discriminating). `android/VERSION` stays `2.0.0-android.1` and `android/BUILD_NUMBER` stays `148081`. Only `android/` changed; **the web/server drawing output, `APP_VERSION` and `web/BUILD_NUMBER` did not move**, and no pentala deployment was needed.
- **Phase 3b (merge `467877f`, android Build 148082) — porting Stage 1.5 variation.** The variation layer transforms the DDL body and the `variation_report` deterministically, but **only when `variation_amplitude` and `variation_seed` are both present**. `VariationPlan`, the three amplitudes, the seven axes (`type_swap` / `count` / `touch` / `focus` / `color` / `composition` / `type_family`), and `buildVariationPlan` / `effectiveVariationPlan` (which keeps only the axes that actually move the output) / `variationMovedAxes` / `resolveFocusId` were copied from the server's `ddl_expander.py:676-1040`. **Focus belongs to this phase too** (`_resolve_focus_id` is part of the variation plan).
- **The starting state was measured into the contract beforehand.** Of the 16 cases, two pass even without variation (`amplitude-only` / `seed-only`, identical to the base), so the phase began with **14 in DIFFER**. **Variation can shorten the DDL** — four cases collapse to 24 characters via a `type_family` reduction, so an "append-only" implementation cannot pass. **All 16 cases carry a `variation_report`**, so the test checks `moved_axes` (axis, from, to) and `resolved_focus` on every case in addition to exact output equality.
- **Independently reproduced by the git session:** (1) `testDebugUnitTest --rerun-tasks` gives **62 tests, 0 failures, 0 errors** on both `feat/android-phase3` and main (JUnit XML counted by hand: baseline 61 plus one new test wrapping 16 cases). (2) **Discrimination confirmed by perturbation** — reverting `java.lang.Long.toUnsignedString` to a plain `toString` makes it fail (proving the two seeds above 2^63 bite), and corrupting the `axis` in `moved_axes` makes it fail **even with the output still correct** (proving the report check bites independently of output — the eleven distinct output+report signatures rule out the "just truncate" implementation the contract worried about). (3) The two focus cases with equal output length (`upper_left` / `upper_edge`, 92 chars) are told apart by their body content. **Only `android/` changed** (six files); `render_engine_version` stays **`"11"`** and `android/VERSION` stays `2.0.0-android.1`.
- **Handover:** Phase 3c (staffage) then 3d (plugins, saijiki) remain. 3c covers the three `tenkei` levels and `_cap_category_plan`; its expected cases are already pinned in `ddl_expand.json` (`A-tenkei-*` 3, `B-tenkei-*` 9).

### v2.4.7 — freezing the deterministic DDL layers (a DDL reference corpus, plus `ddl_version` / `ddl_engine_version`) (Build 697, 2026-07-24)

**The same thing v2.4.4 did for drawing, done at the front of the pipeline.** Drawing is
not the only deterministic layer: plugin expansion, Stage 1.5 expansion and variation, and
coercion all return the same result from the same input and seed. They now have versions
and a frozen corpus of their own.

- **A new reference corpus for the deterministic DDL layers:** `server/reference/ddl-engine-1/`,
  29 cases, generated by `server/scripts/gen_ddl_reference.py`. **A and B are deliberately
  unchained**: A freezes the output of `expand_intermediate_ddl` (15 cases, full expanded DDL),
  B freezes the output of `coerce_score` (14 cases, the full coerced Score plus its
  `branch_report`). **B's input Scores are literals inside the generator**, never A's output —
  chaining them would let a defect in expansion mask a defect in coercion.
- **Why two corpora rather than one:** the deterministic layers are not adjacent. Stage 2's
  LLM sits between Stage 1.5 (DDL→DDL) and coercion (Score→Score), so "DDL through to Score"
  cannot be a single baseline (the layer map in SPEC §12.2).
- **`ddl_version` and `ddl_engine_version` introduced**, both starting at **`1`**, with
  `layer_versions.py` as the source of truth. **`ddl_version` tracks the DDL language itself**
  (grammar, keywords); **`ddl_engine_version` tracks the deterministic transformation layer.**
  They are separate namespaces. **The implementation matches the values SPEC §12.3 recorded
  ahead of it in v2.4.6.**
- **Recorded on works:** both values travel with newly generated compose / paint / render-score
  responses, history rows, and saved artifacts; the history table gained nullable columns via a
  non-destructive `ALTER TABLE`. **Existing rows are not backfilled** — inferring a version for
  a work that never recorded one would be fabricating provenance.
- **Edition identity is unchanged:** the `ddl_*` fields are **not** in the rh3 payload, keeping
  v2.4.5's rule that an ID must not move when not one byte of the drawing has.
- **Built to discriminate:** passing or omitting the `ddl` argument takes the same Score from
  **0 fired branches to 6, and 1 instruction to 3**; the three `tenkei` levels (auto / sparse /
  none) split it **6 / 4 / 3 fired and 3 / 2 / 1 instructions**. Cases that fire nothing (forty
  lines, a cloudform, presence alone) are frozen too, so **"nothing happens" is pinned as well**.
  The `branch_report` check fixes the fired keys **per case** and never the global key set — a
  global literal would break every time a branch is added, which is not a real check.
- **CI:** `reference-corpus.yml` gained an independent `ddl-engine` job, and the render job was
  narrowed to `render-engine-10/`. **The gate was shown to actually fail, from two directions** —
  the implementing session perturbed `coerce.py` (the B side) and the reviewing session perturbed
  the return value of `ddl_expander` (the A side); both stopped the generator with exit 1 and
  `DDL corpus changed without an identity-field change; bump the appropriate version instead of
  rewriting a frozen corpus`.
- **Drawing untouched:** `render_engine_version` stays **`10`**, and regenerating the render
  corpus leaves all 220 cases byte-identical.
- **Verification:** pytest **1043 passed / 30 skipped** (1038 in v2.4.6 plus 5 new), cli 68 passed,
  ruff clean, `npm run check` 0 errors / 2 warnings. Regenerating both corpora leaves
  `server/reference/` with no diff.
- **Known gap (recorded):** the saijiki (`saijiki.py`) is not read by any deterministic layer; it
  feeds the Stage 1 prompt, a layer that carries no version. **Adding vocabulary therefore does
  not move this corpus.** New words are a `ddl_version` event, but **nothing detects that
  mechanically yet** (Phase 4's `stage1_prompt_digest` will cover half of it).

### v2.4.8 — declaring one master grid for the performed output (render engine 11), plus prompt provenance digests (Build 698, 2026-07-24)

#### A. Render engine 11 — every emitted number lands on one grid

- **It began with eight consecutive CI failures.** The `render-engine` job of the
  `reference-corpus` workflow had **never been green** since the workflow was added. The
  frozen corpus was taken on the author's macOS machine while CI regenerates it on Linux,
  and `math.sin` differs there by one unit in the last place
  (`344.12754953531663` versus `344.1275495353167`). Because `points` / `cx` / `cy`
  reached svgwrite as raw Python floats, seventeen digits landed in the file and that
  difference became a byte difference. **81 of the 220 cases** differed, with **zero
  structural differences** and a largest relative difference of **2e-16**. The failure was
  reproduced on pentala (Ubuntu x86_64) before anything was changed.
- **Three numbers were measured:**
  - **The platform noise floor sits between ten and eleven decimal places.** Rounding both
    corpora to a varying number of digits, macOS and Linux agree completely through ten
    decimals; four cases diverge at eleven and eighty at thirteen. **Of the seventeen
    digits, only ten carried reproducible information.**
  - **The coordinate grid was uneven.** Drawing geometry was formatted with `.1f` in one
    place, `.2f` in nine, `.3f` in seven, with raw seventeen-digit floats mixed in. **The
    master's ruling varied from 1e-4 to 1e-19 depending on where it was written.**
  - **Shape fidelity is 2.2e-4 of the artboard.** Strokes are emitted as chains of straight
    segments rather than curves (77,666 `L` commands against 490 `C`), with a median
    segment of 2.07% of the canvas width. For a circle of radius 240 units the chord
    sagitta is 0.224 units — roughly a **4.5K raster equivalent**.
- **So this is not a change that discards resolution.** The digits were already two hundred
  times finer than needed, and the effective resolution is set by sampling. This change
  **gathers scattered precisions into one declaration**, and the drawing geometry moved
  **up**, from one-to-three decimals to a uniform six.
- **`master_grid.py` is the single declaration:** six fixed decimal places, a
  canvas-relative step of **1e-9** on a 1000-unit canvas. That is four orders of magnitude
  above the noise floor and three below any physical limit — 100nm when the artboard is
  stretched across a 100m wall, finer than the wavelength of visible light.
- **Fixed `.6f`, trailing zeros kept (author's ruling).** The reason is not file size but
  **verifiability**. With a fixed width every emitted number matches `-?\d+\.\d{6}`, so a
  single regular expression can confirm from the artifact itself that the grid was applied.
  Trimming would leave `695.45787` indistinguishable from a raw float, and the claim would
  rest on trusting the procedure. The price is 11.6% in size (2.48MB to 2.77MB).
- **The grid is enforced at one point in the output**, applied once to the return value of
  `render()`. There are 48 svgwrite call sites, and **fixing them one at a time leaves
  omissions that pass silently**. Only `version="1.1"` and the `class` / `id` identifiers
  are excluded.
- **The drawing did not change.** Across all 220 cases the count of numbers is identical
  and **no number moved by more than 5e-4**, exactly the half-step of the old three-decimal
  formatting. The six golden digests were re-taken only after confirming the same property.
- **The engine 10 corpus is kept (author's ruling).** The 10 → 11 diff is the evidence that
  **only the written digits changed and the drawing did not**. Engine 10 cannot be
  reproduced outside macOS, so CI no longer checks it; `server/reference/README.md` now
  says so. This is the **first practical use of the reference corpus**.
- **CI** now checks the whole `server/reference/` tree. A version bump therefore leaves a
  new directory untracked and fails the build until it is committed — intended, and
  documented in the README.
- **Two checks, both confirmed to fail when perturbed:** one over freshly rendered output
  across the `editable` / `compat` / `display` profiles, one over all 220 frozen files. For
  the latter, a single digit was removed from one file to watch it fail before restoring.
- **Reproducibility:** the 220 files and the manifest are **byte-identical** on macOS arm64
  and Ubuntu x86_64. Running the generator twice leaves the tree unchanged.

#### B. Prompt provenance digests (reference corpus Phase 4)

- **Closing the gap Phase 3 left.** The saijiki (`saijiki.py`) is not referenced by any
  deterministic layer; it flows into the **Stage 1 prompt, a layer that carries no
  version**. Adding a word therefore moves neither the DDL corpus nor CI.
  `stage1_prompt_base_digest` is the only mechanism that can detect it.
- **Four fields.** `stage2_prompt_base_digest` is deliberately **not** created, because
  Stage 2 means nothing without its tool schema.
- **None of the three discriminating tests is vacuous:** one asserts that the digest of
  what was actually sent **moves** with the input while the base does **not**; one asserts
  that the base under an override path **differs** from the module constant, which proves
  it is not computed from that constant; one asserts that changing a schema description
  moves the combined digest while `SYSTEM_PROMPT` alone does **not**, which records in the
  test why the tool schema has to be included.
- **Existing rows are not backfilled.** Nullable columns were added with a non-destructive
  `ALTER TABLE`; the versions of works that carry no record are not guessed.

#### C. Folded in, and what is left

- **`description` in `server/pyproject.toml`** (`2308059`): a leftover from the uv template
  that had been replaced. It changes no behaviour, so it had not been numbered on its own.
- **Android has to follow.** Engine 11 changes `path d` from `.3f` to six fixed decimals,
  so **the Android parity fixtures all break**. That is a different axis from Stage 1.5
  Phases 3b–3d, so the drawing-layer catch-up is filed as **Phase 2h under its own
  contract**.
- **Verification:** pytest **1062 passed / 30 skipped** (v2.4.7's 1043, plus 17 from
  Phase 4 and 2 from engine 11), cli 68 passed, ruff clean, `npm run check` 0 errors /
  2 warnings.

### v2.4.9 — showing the version drift on replay (reference corpus Phase 5, complete) (Build 705, 2026-07-25)

**This completes all five phases of the parent contract on the reference corpus and layer versions.** The frozen corpus that lets the engine 10 → 11 difference be shown as a change in written digits only is now brought to the user's screen: **the work states which version it was drawn with and which version is on screen now, in words.** The drawing core, stroke engine, Score schema, `server/reference/`, and every version number (`render_engine_version="11"`, `ddl_version="1"`, `ddl_engine_version="1"`) are all left as they were — **only the display was added.**

- **A read point for the current layer versions on `GET /api/info`.** This unauthenticated endpoint gained `render_engine_id` / `render_engine_version` / `ddl_version` / `ddl_engine_version`, taken from `current_render_engine()` and `DDL_VERSION` / `DDL_ENGINE_VERSION` rather than written as constants. The test pins that **monkeypatching the engine object makes `/api/info` follow** (a form that would fail if the values were hard-coded). The existing `name` / `version` / `build_number` / `developer_mode` are kept, and the version fields are returned regardless of `developer_mode`.
- **A "DDL specification / transform layer" line in the generation-info drawer.** Its source is the `ddl_version` / `ddl_engine_version` recorded on the selected history entry or the current result. When absent it shows **"not recorded" and does not guess a value.** Of the 1704 rows in the DB, 590 (35%) carry no engine-version record, so this distinguishes **"no record" from "version 1" on screen.**
- **A replay comparison modal (new `ReplayComparisonModal.svelte`).** Replay sends the saved Score to `/api/render-svg`, re-renders it with the current Renderer, and **places the saved SVG (original) and the re-rendered SVG (current) side by side.** Above each work it shows the recorded and current Renderer versions; when they differ it shows "This artwork was rendered with engine 10. The image now on screen was re-rendered with engine 11." (with Japanese) **outside the work, above the comparison grid** — never overlaid on the SVG. No drift notice appears when the recorded and current versions match, when `/api/info` fails and the current version is unknown, or when a saved work is merely opened from history.
- **Return to the caller.** Opened from the history manager, the work tab, or the lineage tab, closing returns to where it came from. A replay button was added to the left of "generation info" on the bottom canvas bar, and the corresponding buttons in `CanvasPanel` and `HistoryManager` were aligned to the `--btn-sm-*` size tokens.
- **Provisional comparison for seed-less works (author's added instruction).** History with neither `render_seed` nor `seed_text` can now be compared. **The DB is not written back; a fixed seed of `0` is supplied only for the duration of the request,** so re-comparing the same work does not change the current side. The modal notes "Provisional display with a supplemented seed (seed: 0)." Because the API omits NULL columns from the response, **completeness of a history entry is decided by the presence of the saved `score` and `svg`, not by the presence of the `render_seed` field** (getting this wrong left the bottom-bar replay button disabled in Build 703; confirmed from an author screenshot and fixed in Build 704).
- **What was deliberately not carried (stated).** No retention, selection, or recall of past Renderers. Saved SVGs are not replaced. Existing history versions and `render_seed` are neither backfilled nor written back. The `/api/render-svg` SVG body response is unchanged. No web test harness was added.
- **The spec did not move, so SPEC was not touched** (this implements `SPEC.ja.md` §15.8 and stays within contract §3.1).
- **Verification:** pytest **1063 passed / 30 skipped** (+1 for the `/api/info` version fields), cli 68 passed, ruff clean, `npm run check` **0 errors / 2 warnings** (217 files including the new `ReplayComparisonModal.svelte`), `npm run build` succeeded. The change touches only one server file plus its test and web.

---

### v2.5.0 — de-regularizing the performance, and "unleashed" (render engine 12) (Build 706, 2026-07-25)

**Up to engine 11 the performance looked varied but was periodic.** The width envelope was a fixed `max(0, sin(pi t))` hump, so **every stroke was fattest exactly at its midpoint and thinned symmetrically to both ends**. The correction event beat with `sin((i % 5) * pi / 2)` — **a period of five**. Closed contours carried a thin seam opposite a fat middle, and the material outline was drawn with an even dash and evenly spaced specks. Engine 12 replaces all four with seeded low-frequency noise, and adds **a length-scaled gesture to the centreline itself**.

- **De-regularized envelope.** Replaced by `_edge_window(t) * _swell(t, seed)`. `_edge_window` is a raised-cosine ramp over the outer 16% at each end and **imposes no hump in the middle** — it states only the fact that the endpoints are pinned. `_swell` is per-seed low-frequency noise, so **where a stroke reads as fullest now wanders from stroke to stroke**. A closed contour has no endpoints, so it skips the edge window and uses `_swell` alone (`CLOSED_ENVELOPE_FLOOR` is deleted; the spurious thin seam is gone).
- **The correction event is length-based.** The sample-index modulus `i % 5` is replaced by a seeded hash kick, so **texture no longer changes when the sample count does**.
- **Centreline gesture.** `ToolGrammar` gains a tenth field, `gesture`: a low-frequency wander of the centreline itself, scaled by stroke length, producing bends, curls, and self-overlap. It is a distinct quantity from `energy_lateral`, which is normalized by pen width. Endpoints stay pinned by the window; determinism comes from the seed.
- **Material outline layer.** It now follows the gestured centreline (`<polyline class="material-outline">`), with a variable mark/gap dash, a wandering offset, and non-uniform speck placement. `rotring` lines pass straight through — the machine pole.
- **The "unleashed" (`wild`) toggle.** One switch **for the whole work** that lifts the performance ceiling (`WILD_GAIN = 3.5`). **It removes only the amplitude ceiling and the ban on self-intersection; endpoint pinning and determinism hold whether it is on or off.** It is recorded as a generation parameter next to `render_seed` (`history.render_wild`; NULL means a work from before the column existed, which is kept distinct from OFF), used on replay, and **included in the edition identity `rh3`**. The UI is a toggle in InputPanel with ja/en strings. This sits in a different layer from variation (Stage 1.5): it is a Renderer-layer knob.
- **The `rh3` payload change is absorbed by the version bump.** Adding `render_wild` changes the material, but **`render_engine_version` is inside the same payload**, so any value computed under the old material contains `"11"` or lower and any under the new contains `"12"` or higher. The two can never collide, so the format name stays `rh3`. This argument **only holds because the engine version moved at the same time**, and SPEC now says the material must never be extended on its own.
- **Froze the `render-engine-12/` reference corpus (199 of 220 changed).** **21 cases did not move, and what they are is itself the explanation of engine 12.** The 12 `rotring` cases are byte-identical: its grammar is all zeros, so de-regularization has nothing to reach — **the machine pole of the tool vocabulary is exactly where it was**. The 9 `cloudform` cases are byte-identical for a different reason: a cloudform is emitted as a Catmull-Rom path, never enters `stroke_engine`, and carries no material outline layer. **This is a gap engine 12 exposed, not one it created** (the contour does vary by tool — all 10 tool digests differ).
- **Implemented the "store only what moved" rule in the generator.** The README and SPEC §15.7 had required it for some time, but the generator wrote all 220 bodies whenever it created a new directory. Engine 11 moved every case, so **the two behaviours were indistinguishable**. `gen_render_reference.py` now compares digests against the previous manifest, derives `changed_from_previous` by measurement, and **writes SVG bodies only for those IDs**. A resolver (`_resolve_svg`) walks back to the last version where a case moved, with a test that **fails if the walk-back has nothing to resolve**.
- **Added "when the performable vocabulary grows" to the version-bump rule (author's ruling).** Adding a tool leaves every existing corpus case untouched, because no case uses the new word — **output does not move and CI does not fail**. Bumping only when results change would allow vocabulary to be added without a version, leaving "an engine 12 that can perform the word" beside "an engine 12 that cannot". **The meaning of a version number — same version, same result — then breaks on the input set rather than the output.** (SPEC §15.6.)
- **Explicitly not done.** Android was not brought forward (still engine `"11"`). The "computer" touch vocabulary was not started (only its design was ruled on). `cloudform` was not moved onto stroke synthesis (the finding is recorded, not fixed). Past engines are still not retained or selectable (§15.8 stands). No stored SVG was replaced. `render_wild` was not backfilled on existing history (NULL still distinguishes "before the column" from OFF).
- **SPEC.** A section on "unleashed" in §13.4; the extended bump condition and the `rh3` material note in §15.6; the engine 12 measurements (the 199/21 split) in §15.7.
- **Verification.** pytest **1066 passed / 30 skipped** (+7: the 6 goldens re-baselined for engine 12, plus the new walk-back test), cli 68 passed, ruff clean, `npm run check` **0 errors / 2 warnings** (217 files). The 6 re-baselined goldens were confirmed to fail as a set when `_swell` is perturbed by 1e-6 — the discriminating power was measured, not assumed. Running the corpus generator twice leaves no tracked diff.
