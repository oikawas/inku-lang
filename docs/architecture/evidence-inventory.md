# Evidence inventory

## Snapshot

| Subject | Value |
|---|---|
| Date | 2026-08-10 (JST); fully refreshed 2026-08-17; Web/core refactoring reconciled 2026-08-24; completed SQLite persistence refactoring reconciled 2026-08-27; completed typed DDL Step 8 reconciled 2026-09-02; fully reconciled after the shared Rust pipeline cutover 2026-09-25 |
| Source branch / implementation commit | `main` / `46f17da8c5b438511f9bd915395763262b55fb72` (implementation baseline before this document update) |
| Source state | Clean at the implementation baseline; this update changes `docs/architecture/` only |
| Project Context | `PROJECT_CONTEXT.ja.md`, target `v2.15.28 / Build 1104`. Its version table still carries some values older than the implementation (`known-differences.md` F-05) |
| Japanese specification | `SPEC.ja.md`, document version `v1.92.0` |
| Web / app | `web/APP_VERSION` = `v2.15.28`; `web/BUILD_NUMBER` = `1104` |
| Render Engine | `default` / `68` (`core/crates/inku-render/src/lib.rs`); core API `0.1.0` |
| DDL | `ddl_version=13`; `ddl_engine_version=47` (`server/src/inku_server/layer_versions.py`) |
| Shared pipeline binding | binding `1.1.0`, protocol `1.0.0` (`pipeline_candidate.py:PipelineBinding` requires both) |
| Score | New works use the minimum version their fields need, from the resource-aware compact 0.10 baseline up to 0.15. Saved 0.1.0 through 0.9.0 Scores and versionless artifacts remain readable |
| Android | `android/VERSION` = `2.1.4-android.80`; `android/BUILD_NUMBER` = `148155`; Room schema 12; the shared Rust pipeline and Render Engine 68 |
| Server DB | Migration registry version 3 (`developer_provider_observations`) |

Environment-variable names may appear, but values, credentials, production DB contents, and deployment-specific identifiers were outside the investigation.

## Inventory

| ID | Element / boundary | Responsibility | Implementation evidence | Specification evidence | Confidence |
|---|---|---|---|---|---|
| SYS-USER | User | Starts descriptions, direct DDL, approval of completion proposals, explicit derivations, settings, and exports | `web/src/routes/+page.svelte`; `cli/src/inku_cli/cli.py`; `android/app/src/main/java/app/inku/mobile/ui/InkuApp.kt` | `SPEC.md` §7, §12.7.1, §23 | Confirmed |
| SYS-WEB | Web frontend | SvelteKit UI, same-origin API proxy, browser state, display of shared-pipeline executions and forwarding of author commands | `web/src/routes/+page.svelte`; `web/src/hooks.server.ts`; `web/src/lib/features/pipeline/`; `web/package.json` | §7, §12.7.1, §21 | Confirmed |
| SYS-CLI | CLI | Public HTTP API client, functional testing, benchmark support | `cli/src/inku_cli/cli.py` (`ApiClient`, parser); no server-internal imports | §23 | Confirmed |
| SYS-ANDROID | Android | Kotlin/Compose UI, Room history, provider transport, and the JNI host for the shared Rust pipeline, renderer, and raster | `InkuRepository`; `AndroidWorkPipeline`; `SharedPipelineHost`; `NativePipelineBridge`; `SingleAttemptModelEffectProvider`; `AndroidRenderHost`; `RustArtworkRasterizer`; `InkuDatabase` | `android/ANDROID_SPEC.md`; `SPEC.md` §12.7.1 | Confirmed |
| SYS-API | FastAPI app | Middleware, lifespan, and assembly of ten routers plus the shared pipeline router | `server/src/inku_server/api.py` (`app`, `_lifespan`, `include_router`, `pipeline_router`, `register_pipeline_errors`) | §22; Project Context "server" | Confirmed |
| SYS-CORE | Shared Rust core | Owns Score types, the typed DDL compiler, the authoring state machine, the Render Engine, and SVG raster independently of any host | `core/Cargo.toml` (8 crates); `core/crates/{inku-score,inku-ddl,inku-pipeline,inku-render,inku-svg-raster}` | §12.7.1, §12.14 | Confirmed |
| SYS-LLM | LLM providers | External inference for the sketch, color catalog selection, the Stage 1 work plan, and known-hole completion | `pipeline_provider.py` (`SingleAttemptProvider`, `resolved_stage_model`); `model_settings.py`; Android `RoutingModelProvider` | §12.5–12.8 | Confirmed |
| SYS-DB | Server DB | Canonical SQLite and domain persistence; `db.py` is the compatibility and composition façade | `persistence/{config,engine,schema,access,accounts,groups,sessions,identities,settings,history,search,lineage,okugaki,feedback,variation_authority}.py`; `provider_observation.py`; `db.py` | §21–22 | Confirmed |
| SYS-FILES | Work-file area | Optional derived SVG/JSON/DDL/input/PNG files | `api_core/rendering.py` (`_submit_history_artifact_save`); jobs submitted from `pipeline_product.py:save_result` | §21 | Confirmed |
| SYS-LOG | Log area | Stdout and rotating application files. Compiler results are logged only as diagnostic counts and a safe projection | `logging_setup.py` (`configure_logging`); `pipeline_product.py` (`pipeline_compiler_outcome`) | §21 | Confirmed |
| SYS-BACKUP | DB backup area | Shared WAL-safe SQLite snapshots and generations for migration, manual, and scheduled backup | `persistence/backup.py`; thin delegation in `db.py` | §22 | Confirmed |
| DATA-MIGRATION | Server schema lifecycle | SQLite-only preflight, versioned registry (v1 legacy baseline → v2 shared-pipeline sidecars → v3 provider observations), allowlisted legacy fingerprints, single-writer migration, PK and canonical history byte invariants, fail-closed behavior | `persistence/{config,engine,migrations,legacy_schema,invariants,backup}.py`; portable persistence tests | §22 | Confirmed |
| API-ROUTERS | Routers | Ten routers plus the `/api/pipeline` shared pipeline router. A static count gives 94 + 11 = 105 endpoints | `api_core/routers/{public,auth,me,plugins,settings,users,history,lineage,render,feedback}.py`; `pipeline_api.py:pipeline_router` | Project Context "server" | Inferred (static count; `EXPECTED_ROUTE_COUNT` in `test_route_authorization.py` still says 95; `known-differences.md` F-06) |
| API-AUTH | Authentication and authorization | Bearer/cookie sessions, role guards, three public paths. The shared pipeline router passes `_current_user` as the owner of every operation | `api_core/deps.py`; `routers/auth.py`; `api.py` (`pipeline_router(_pipeline_service, _current_user, ...)`); `test_route_authorization.py` | §22 | Confirmed |
| API-LIMIT | Capacity boundaries | Limits on body, request, render, pipeline workers, retained runs and effect steps, envelope bytes, and the save queue | `security.py`; `api_core/state.py`; `pipeline_api.py:PipelineService`; `pipeline_defaults.py` (`host_limits`, `envelope_limits`) | §22 | Confirmed |
| PIPE-HOST | Server pipeline host | Supplies trusted configuration, provider, and save effects to the shared core, serializes calls, and stores execution snapshots. It holds no semantic branches | `pipeline_runtime.py`; `pipeline_api.py:PipelineService`; `pipeline_candidate.py` (`PipelineBinding`, `CandidateExecution`); `pipeline_product.py:ProductPipelineEffects`; `pipeline_settings.py`; `pipeline_defaults.py`; `pipeline_compat.py` | §12.7.1 | Confirmed |
| PIPE-MACHINE | Shared authoring state machine | Snapshot + input envelope → next snapshot + events + at most one effect. Decides retry, fallback, authority transitions, and when to compile | `core/crates/inku-pipeline/src/{machine,protocol,authority,prompts,core_boundary,hole_completion,replay,byte_envelope}.rs`; `inku-pipeline-uniffi` (`step`) | §12.7.1, §12.8 | Confirmed |
| PIPE-LIMITS | Resource authority | Resolves the hard policy and the operational budget (four existing limits plus six new ones), saves them with the work, and checks demand before any instance is made | `pipeline_defaults.py` (`ADDITIONAL_RESOURCE_LIMITS`); `limits.py` (`Limits`, `DEFAULT_LIMITS`); `pipeline_settings.py`; `inku-score/src/score_resources.rs`; `api_core/rendering.py:_limits_for_render` (replay) | Latter part of §12.16; Project Context "design contracts" | Confirmed |
| PIPE-SKETCH | Sketch | Only when the author chooses it, supplements place and light before Stage 1. A failure never stops the drawing | `inku-pipeline/src/prompts.rs:build_sketch_prompt`; `machine.rs:finish_sketch`; `sketch.py` (types for saved state only) | §12.6.1 | Confirmed |
| PIPE-CATALOG | Automatic color catalog selection | Only for `catalog_mode=auto`, selects a catalog from the description and falls back to `default` on failure | `prompts.rs:build_catalog_selection_prompt`; `machine.rs:select_catalog`; `pipeline_product.py:prepare` | §12.7.1 | Confirmed |
| PIPE-S1 | Stage 1 work plan | Receives a closed-typed work-plan JSON for the description and prints it deterministically as visible DDL. On compiler rejection it renormalizes within a finite budget; once that is spent, only a drawable residual can be proposed | `prompts.rs` (`build_stage1_prompt_with_sketch`, `parse_stage1_response`); `inku-ddl/src/work_plan.rs` (`normalize_work_plan`, `print_work_plan`); `machine.rs` (`correct_stage1`, `residual_execution_preflight`) | §12.6, §12.8 | Confirmed |
| PIPE-TYPED-DDL | Typed DDL compiler | Source spans of visible DDL, the typed semantic document, lock/binding/bounded expansion of generic Macros, the compiler lock (four states), and fail-closed diagnostics for ambiguity | `inku-ddl/src/{document,parser,clause,semantic_association,semantic_instruction,semantic_document,macro_resolution,macro_parameter_binding,macro_expansion,compiler_lock}.rs` (`compile_typed_ddl`) | §4.4–4.6, §12.4 | Confirmed |
| PIPE-HOLE | Known-hole completion (Stage 2) | Requests bounded patch proposals only for known holes in saved DDL and CAS-saves them after author approval. Never outputs a Score | `machine.rs` (`complete_holes`, `hole_response`); `hole_completion.rs`; `inku-ddl/src/visible_patch.rs`; `prompts.rs:build_hole_completion_prompt` | §12.7, §12.7.1 | Confirmed |
| PIPE-MACRO | MacroDefinition | Generic Macro definitions, the bundled `Nature.leaves`, and catalog resolution for new works. Old Markdown plugins are omitted with a warning, and their prose is not reinterpreted | `inku-ddl/src/macro_definition.rs`; `NATURE_LEAVES_V1_JSON`; `inku-pipeline-uniffi/src/macro_catalog.rs`; `macro_catalog.py`; `plugins/document_format.py` (the enable handle) | §4.5–4.6 | Confirmed |
| PIPE-S15 | Typed Stage 1.5 | Accepts only `CanonicalReady` meaning and deterministically transforms only focus and explicit variation | `inku-ddl/src/stage15_transform.rs` (`transform_stage15`); `compiler_execution.rs:prepare_stage15_execution` | §12.11, §12.13 | Confirmed |
| PIPE-LOWER | Plan, resource selection, materialization | Lowers verified effective meaning once to a symbolic Plan, selects resources, and writes a compact Score recipe. Local omissions stay in the diagnostics | `inku-ddl/src/{compiler_execution,composition_plan,plan_resources,score_materialization,score_lowering}.rs` (`compile_ddl_to_score_with_resources`) | §12.7, latter part of §12.16, §18 | Confirmed |
| PIPE-COMPAT | Saved Score compatibility | For saved or external Scores older than 0.10 only: structural defaults, a bridge between the two fill spellings, removal of unresolvable relations, and the recorded limits. Never accepts DDL or prose | `saved_score_compat.py:coerce_saved_score`; `coerce/observability.py`; `inku-score/src/compatibility.rs` | §12.14, §18 | Confirmed |
| PIPE-RENDER | Render Engine | Produces SVG and render metadata from a Score, seeds, resolved host options, and resource authority. Compact Scores go through typed performance; older Scores keep the established checked performance | `inku-pipeline/src/core_boundary.rs` (`render_delivery`, `render_saved_score`); `inku-render/src/render.rs` (`render`, `render_with_resources`); `checked_performance.rs`; `typed_performance.rs`; `render_engines/default/adapter.py` (older Score path); `inku-render-python`; `inku-render-android` | §12.14, §13.8 | Confirmed |
| PIPE-RASTER | SVG raster presentation | Converts saved or just-rendered canonical SVG to resource-independent premultiplied RGBA8 with explicit pixel format, dimensions, and stride | `core/crates/inku-svg-raster`; `NativeRenderBridge`; `RustArtworkRasterizer` | §12.14; Android specification | Confirmed |
| PIPE-HISTORY | History save | Saves a shared-pipeline performance as a history row and links its authority revision and fork sidecar | `pipeline_product.py:save_result`; `db.py:add_item` façade; `persistence/history.py`; `VariationAuthorityStore.link_history` | §12.7.1, §21 | Confirmed |
| DATA-AUTHORITY | Variation authority | CAS of origin, authority, and revision; idempotent action acknowledgment; execution snapshots; history-link sidecars | `inku-pipeline/src/authority.rs`; `persistence/variation_authority.py` (`VariationAuthorityStore`); `persistence/schema.py` (`variation_authority`, `variation_authority_actions`, `pipeline_candidate_executions`, `pipeline_history_links`) | §12.7.1 | Confirmed |
| DATA-DH1 | `dh1` | Identity of the normalized description | `identity.py:description_hash` | Project Context "design contracts" | Confirmed |
| DATA-RH3 | `rh3` | Edition identity from Score, render seed, wild, engine, and color catalog | `db.py:render_hash_for_item`; `test_render_hash.py` | Project Context "design contracts" | Confirmed |
| DATA-RH2 | legacy `rh2` | Compatibility retention for older edition hashes | `db.py:_legacy_render_hash_for_item`; `test_render_hash.py` | Project Context "design contracts" | Confirmed |
| DATA-LINEAGE | Lineage nodes/edges | Creates edges only from an explicit parent and derivation kind | `LineageNodeRow`, `LineageEdgeRow`, `db.py:add_item`; `test_lineage_acceptance.py` | §21; Project Context "design contracts" | Confirmed |
| DATA-SAIJIKI | Saijiki | The vocabulary's source of truth is the shared-core asset. The Stage 1 projection, the Server display table, the Web/Android saijiki, and the reference derive from it | `core/crates/inku-ddl/assets/saijiki-v1.json`; `inku-ddl/src/saijiki.rs`; `saijiki.py`; `test_saijiki_api.py`; `test_saijiki_kt_is_current.py` | Project Context "vocabulary" | Confirmed |
| DATA-FALLBACK | Per-layer result records | New works record the sketch in `sketch_state`, provider failures in the execution context, and the compiler outcome with its four diagnostic channels in history-link sidecar v2. The old `interpret_fallback` / `compose_fallback` columns remain only to display older works | `pipeline_product.py` (`provider_for`, `save_result`); `variation_authority.py:history_pipeline_diagnostics`; `db.py:HistoryRow`; `web/src/lib/composeFallback.ts` | §12.7.1, §12.8 | Confirmed |
| WEB-FEATURES | Web feature modules | Session, Work, Batch, Demo, Refinement, Settings, history/lineage, viewport, and pipeline owners separated from the route shell, with stateless operations and focused Canvas/Settings views | `web/src/routes/+page.svelte`; `web/src/lib/features/{session,work,batch,demo,run,history,canvas,settings,pipeline}/`; `web/src/lib/components/{CanvasPanel,SettingsModal,DdlEditorDialog,PipelineStatus}.svelte`; ownership tests | §7.8; Project Context "web" | Confirmed |
| WEB-REGISTRY | Three settings registries | localStorage, user settings, and render payload aggregation | `persisted-settings.ts`; `user-settings.ts`; `render-payload.ts` | Project Context "web" | Confirmed |
| WEB-I18N | UI terminology and tokens | Japanese/English UI, English glossary, CSS tokens | `web/src/lib/i18n/*`; `GLOSSARY.md`; `+page.svelte` `:root` | §6–7 | Confirmed |
| OPS-COMPOSE | Compose distribution | API/Web services and persistent volume | `compose.yaml`; `deploy/compose.yaml`; `server/Dockerfile`; `web/Dockerfile` | §22 | Confirmed |
| TEST-SERVER | Server tests | pytest, API surface, authorization, route ownership, the shared pipeline host, and the authority store | `server/tests`; `test_api_surface.py`; `test_route_authorization.py`; `test_pipeline_{api,candidate,compat,product,provider}.py`; `test_persistence_variation_authority.py` | §11; Project Context "test surface" | Confirmed |
| TEST-CORE | Shared Rust tests | Per-crate unit and integration tests. The authoring flow is tested through a representative flow that never calls the native renderer | `core/crates/inku-ddl/tests/`; `core/crates/inku-render/tests/`; `core/crates/inku-score/tests/`; `inku-pipeline/src/focused_flow.rs` | §11 | Confirmed |
| TEST-CORPUS | Frozen corpora | A new version directory is created only at an explicit full-update checkpoint. The latest frozen records are 620 Render Engine 66 cases and 3 DDL engine 45 cases (shared Rust pipeline). DDL engines 1–26 are historical records of the retired expansion, coerce, and plugin layers | `server/reference/render-engine-66/manifest.json`; `ddl-engine-45/manifest.json`; `server/reference/README.md`; `reference-corpus.yml` | §11, §22 | Confirmed |
| TEST-ANDROID | Android acceptance | Host JVM tests and device acceptance for the shared pipeline, plus SVG byte and raw pixel comparison of a few canonical corpus cases | `SharedPipelineHostTest`; `AndroidSharedPipelineTest`; `SharedPipelineDeviceAcceptanceTest`; `NativeRenderDeviceTest`; `prepareRustParityAssets` | Android specification | Confirmed |
| TEST-WEBCLI | Web/CLI tests | Svelte check/unit/lint and CLI pytest | `web/package.json`; `web/src/**/*.test.ts`; `cli/tests/test_cli.py` | Project Context "test surface" | Confirmed |
| CI-GATES | Current CI | Server/CLI lint+pytest (with the Rust toolchain guard), Web check+unit+lint:i18n, bilingual documents and the portable persistence verifier, a path-scoped shared-Rust/raster/JNI/Android-host native gate, a corpus comparison that runs only on manual dispatch, and release-tag container build | `.github/workflows/checks.yml`; `android-native.yml`; `reference-corpus.yml`; `release.yml` | §11, §22; Android specification | Confirmed |

## Retired IDs

The 2026-09-14 cutover removed the Python layers these IDs pointed to from the path of new works. The IDs and their successors stay here so older documents and changelog entries can still be followed.

| Old ID | Old implementation | Successor |
|---|---|---|
| PIPE-PLUGIN | Prose-triggered deterministic expansion by `plugins/document_format.py:expand_plugin_ddl` | PIPE-MACRO |
| PIPE-S2 | LLM Score composition by `composer.py:compose` | PIPE-HOLE (patch proposals for known holes only) and PIPE-LOWER |
| PIPE-COERCE | Delivery, governance, and ceilings in `coerce/` | PIPE-LOWER (new works) and PIPE-COMPAT (older Scores) |

## Confidence

- **Confirmed**: Direct evidence exists in an entry point, call site, schema, or test.
- **Specification**: Present in the specification but not confirmed in the current implementation.
- **Inferred**: Derived from multiple static sources, with the reason stated nearby.
- **Unknown**: Requires measurement or deployment information not examined here.
